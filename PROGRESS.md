# PROGRESS.md — Project Status Tracker

> Single source of truth for project status. Update IMMEDIATELY when state
> changes. The lead owns the Phase Status table; each teammate owns their own
> rows in the other sections. Never report status that isn't written here.

## Phase Status

| Phase | Name                          | Status | Build | Tests | QA | Human ✓ | Completed | Commit |
| ----- | ----------------------------- | ------ | ----- | ----- | -- | ------- | --------- | ------ |
| 0     | Scaffolding & Environment     | ✅ complete | ✅ | ✅ 5/5 | ✅ APPROVED | ✅ 2026-06-11 | 2026-06-11 | `d64281e` |
| 1     | Schema, Auth & RLS            | ✅ complete | ✅ | ✅ 88/88 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `691662f` |
| 2     | Authentication & App Shell    | ✅ complete | ✅ | ✅ 49/49 + load | ✅ APPROVED + re-review | ✅ 2026-06-12 | 2026-06-12 | `5773b4a` |
| 3     | Admin Area & User Management  | ✅ complete | ✅ | ✅ 43/43 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `cb28ef3` |
| 4     | Form Builder & Versioning     | ✅ complete | ✅ | ✅ 8/8 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `d32e7e9` |
| 5     | Wizard Filling, Conditional Sections & Resume | ✅ complete | ✅ | ✅ 63/63 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `8418991` |
| 6     | Section Sign-offs & Submission Lifecycle | ✅ complete | ✅ | ✅ 70/70 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `94566f2` |
| 7     | Multi-Phase Cases             | ✅ complete | ✅ | ✅ 81/81 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `28e0405` |
| 8     | Dashboards & Submissions Browser | ✅ complete | ✅ | ✅ 106/106 | ✅ APPROVED | ✅ 2026-06-14 | 2026-06-14 | `a50e0e0` |
| 9     | Deployment                    | 🔜 not started | – | – | – | – | – | – |
| 10    | Meetings                      | ✅ complete | ✅ | ✅ 141/141 | ✅ APPROVED | ✅ 2026-06-15 | 2026-06-15 | `5e2780c` |
| 11    | Interviews                    | ✅ complete | ✅ | ✅ 152/152 | ✅ APPROVED | ✅ 2026-06-15 | 2026-06-15 | `3d7376b` |
| 12    | Case Timeline                 | ✅ complete | ✅ | ✅ 169/169 | ✅ APPROVED | ✅ 2026-06-16 | 2026-06-16 | `0feaa9a` |
| 13    | Audit Trail                   | ✅ complete | ✅ | ✅ 195/195 | ✅ APPROVED | ✅ 2026-06-18 | 2026-06-18 | `a8739b5` |
| 14    | Patient-Safety / NSP (Triage, RCA & CAPA, 14a–14d) | ✅ complete | ✅ | ✅ E2E 65/65 + pgTAP 516 | ✅ APPROVED | ✅ 2026-06-18 | 2026-06-18 | 14a `984e787` · 14b–d `c4e20b3` |
| 14e   | **Attachment PHI Classification (Cases & Meetings)** — `contains_phi boolean DEFAULT true` on `case_documents` + `meeting_attachments`; audited single-door open (`case_document.read`/`meeting_attachment.read`, sensitive files no longer pre-signed in list); staff-admin downgrade; carried classification + SQL-COMMENT contract for Phase-19 export redaction. Default-sensitive, not an opt-in checkbox. Plan: [docs/phases/phase-14e-attachment-phi-classification.md](docs/phases/phase-14e-attachment-phi-classification.md). | 🔜 not started (planned 2026-06-30) | – | – | – | – | – | – |
| 15    | Quality Indicators            | 🔜 not started | – | – | – | – | – | – |
| 16    | Standards Crosswalk & Readiness | 🔜 not started | – | – | – | – | – | – |
| 17    | Controlled-Document Lifecycle | 🔜 not started | – | – | – | – | – | – |
| 18    | Self-Assessment & Internal Audit | 🔜 not started | – | – | – | – | – | – |
| 19    | Surveyor Access & Evidence Export | 🔜 not started | – | – | – | – | – | – |
| 20    | Notifications & Escalation    | 🔜 not started | – | – | – | – | – | – |
| 21    | Committee Charters & Cadence  | 🔜 not started | – | – | – | – | – | – |
| 22    | Inter-Committee Case Referrals | ✅ complete | ✅ | ✅ 29/29 + full 276/326 | ✅ APPROVED 2026-06-21 | ✅ 2026-06-21 | 2026-06-21 | `768b9f1` |
| 23    | Patient Identity & Cross-Committee Linkage (MRN/encounter) | ✅ complete | ✅ | ✅ E2E 15/15 + pgTAP 10/10 sweep | ✅ APPROVED 2026-06-22 | ✅ 2026-06-22 | 2026-06-22 | `da4d127` |
| MT    | **Multi-Tenancy** — organizations → hospitals → commissions; vendor `platform_admin` vs customer `org_admin`; RLS rewrite + 3-tier audit + multi-org PHI guard (ADR [0041](docs/decisions/0041-multi-tenancy-organizations-hospitals.md)) | ✅ complete | ✅ | ✅ pgTAP 1029 (171 cross-org 74/74 · 173 guard 18/18) + E2E 292/0 (124 skip = NSP/referral module-off) | ✅ APPROVED 2026-06-25 | ✅ 2026-06-25 | 2026-06-25 | `ee35299…82ea157` (branch `feat/phase-d-multitenancy-frontend`) |
| NSP-per-org | **NSP-per-org** — lift the multi-org PHI guard by binding the PQS roster + every PHI door **to an organization** (per-org `pqs_members`/`pqs_department`, `nsp_coordinator` grant, org-scoped doors, forbid cross-org referrals, org-scope the patient-index 4th surface). Restores NSP + referral modules under multi-tenancy; un-quarantined 124 E2E specs. ADR [0042](docs/decisions/0042-nsp-per-org.md); design [nsp-per-org-design.md](docs/progress/nsp-per-org-design.md). | ✅ complete | ✅ | ✅ pgTAP **1102/1102** + full E2E **421/0** (1 stale-test fixed · 4 known skips) | ✅ **APPROVED** A [core](docs/reviews/nsp-per-org-a-review.md) + B [whole-phase](docs/reviews/nsp-per-org-b-review.md) | ✅ 2026-06-25 | 2026-06-25 | branch `feat/nsp-per-org` (`b0e15f4…9c53035`) |
| result-rec | **Result-based phase recommendation** — `recommend_when` becomes a combinable answer/result group; recommend a phase from an EARLIER phase's result (specific option or `adverse`), mixed freely under TODAS/QUALQUER. Suggestion-only; zero `eval_condition` drift. ADR [0043](docs/decisions/0043-phase-result-based-recommendation.md). Detail: [docs/progress/result-rec.md](docs/progress/result-rec.md). | ✅ complete | ✅ | ✅ pgTAP **1122/1122** + Vitest **164/164** + full E2E **431/0** (4 known skips) | ✅ **APPROVED** 2026-06-26 [review](docs/reviews/result-rec-review.md) | ✅ 2026-06-26 | 2026-06-26 | branch `feat/result-based-recommendation` (`6c5baeb…`) · ✅ remote re-baselined 2026-07-01 |
| processless-cases | **"Sem processo" (process-less cases)** — coordinator mints a template-less case (`template_id` NULL, zero phases; ad-hoc phases grown later) with an OPTIONAL hand-picked offered-outcome set + OPTIONAL patient identifiers. Two coordinator-gated DEFINER RPCs (`create_case`, `set_case_offered_outcomes`); no new RLS shape; one intentional ADR-0024-D15 divergence (process-less offered sets mutable while non-terminal). ADR [0044](docs/decisions/0044-processless-cases.md). Detail: [processless-cases.md](docs/progress/processless-cases.md). | ✅ complete | ✅ | ✅ pgTAP **1153/1153** (+31 new) + feature E2E **8/8** + full E2E **439/0** (dev, 4 known skips) | ✅ **APPROVED** 2026-06-30 [review](docs/reviews/processless-cases-review.md) (0 BLOCKER · 0 MAJOR · 1 MINOR informational) | ✅ 2026-06-30 | 2026-06-30 | branch `feat/processless-cases` (`cdf26d0`) · ✅ remote re-baselined 2026-07-01 |
| form-model-norm | **Form data-model normalization** — pull `form_items.options` JSONB into a normalized `form_item_options` table (stable hidden `code`, label, color, nullable `score`, free-text `analytics_code`, ordering; version-scoped + cloned), and normalize answer selections into `answer_selected_options` (hard FK to option row). `answers.value` → scalars only. Evaluator + shared vectors stay byte-for-byte unchanged via an `app.answer_map` rewrite. Pre-launch **squash-to-clean-baseline + full reset** (no users, no feature flag). Plan: `~/.claude/plans/snappy-juggling-duckling.md`; decisions memory `form-model-normalization-decisions`. | ✅ complete | ✅ | ✅ T-1 green (evidence) | ✅ APPROVED 2026-07-01 ([review](docs/reviews/form-model-normalization-review.md)) | ✅ 2026-07-01 | 2026-07-01 | squash `ffc0ea5`; baseline `20260620000000`; merged → main |
| answer-model-v2 | **Answer-Model v2 + form-definition forward-compat** — uniform answer row (choice items get a parent `answers` row; selections → `answer_id`), typed scalar cols (`value_number/date/time`, trigger-derived; `value` stays the canonical evaluator input), instance-ready answer key (`answers.group_instance_id` + `response_group_instances` + `form_items.parent_item_id`) **scaffolding only — NO repeating-group / new-block UX**, `answered_at` + reserved `confidentiality_level`, and question **default values**. Evaluator byte-for-byte unchanged (Rule 3). ADRs [0045](docs/decisions/0045-answer-model-v2.md) · [0046](docs/decisions/0046-forward-compat-form-capabilities.md); plan [docs/plans/answer-model-v2.md](docs/plans/answer-model-v2.md). | ✅ complete | ✅ | ✅ pgTAP 1205 / Vitest 176 / E2E: feature 6/6 + scoped regression 60/60 + full suite 456p/7 pre-existing-contamination (0 regressions, confirmed in isolation) | ✅ APPROVED 2026-07-01 ([report](docs/reviews/answer-model-v2-review.md)) | ✅ 2026-07-01 | 2026-07-01 | branch `feat/answer-model-v2`; re-squashed → baseline `20260620000000` · ✅ remote re-baselined 2026-07-01 |
| ad-hoc-narratives | **Ad-hoc Case Narratives** — a coordinator adds a narrative to an OPEN case (`add_ad_hoc_narrative` DEFINER RPC + `case_narratives.is_ad_hoc`), mirroring `add_ad_hoc_phase`; type from the vocabulary or inline create-or-reuse (un-archives). Reverses ADR 0032 D7's "no per-case add" for open cases only (remove/reorder stay template-authored); closes the process-less-case gap (could grow phases but never a narrative). ADR [0047](docs/decisions/0047-ad-hoc-case-narratives.md). Detail: [ad-hoc-narratives.md](docs/progress/ad-hoc-narratives.md). | ✅ complete | ✅ | ✅ pgTAP **1219/1219** (+14 `178`) · Vitest **176/176** · feature E2E **5/5** · full E2E **461p/7 pre-existing** (0 regressions) | ✅ **APPROVED** 2026-07-01 ([review](docs/reviews/ad-hoc-narratives-review.md)) — 0 BLOCKER · 1 MAJOR + 2 MINOR all cleared | ✅ 2026-07-01 | 2026-07-01 | branch `feat/ad-hoc-case-narratives` |

| user-reg | **User Registration & Identity Management** — org_admin registers users (professional category + optional council credentials + home hospital/matrícula), verify+activate via invite, searchable org directory + full per-user management (committees/roles, deactivate/suspend/reactivate/resend); **real `is_active` enforcement**; drops DOB (LGPD). Detail → [user-registration.md](docs/progress/user-registration.md); ADR [0048](docs/decisions/0048-user-registration-identity.md). | ✅ complete | ✅ | ✅ feat 12/12 · pgTAP 1257 · full 467p/10-contam (0 reg) | ✅ APPROVED | ✅ 2026-07-02 | 2026-07-02 | `117319d` |

> **Accreditation & Quality-Governance Track (13–21)** — planned 2026-06-17; specs in
> [PHASES.md](PHASES.md) (§ Accreditation track), rationale in ADR
> [0028](docs/decisions/0028-accreditation-governance-roadmap.md). **13 = Audit Trail** and
> **14 = Patient-Safety/NSP (Events, Triage, RCA & CAPA — sub-phases 14a–14d)** are the agreed first two. Each phase is feature-flagged, individually
> testable, and gated by §6. **Deployment plan: pilot after Phase 16** (P0 core); Phase 9
> stays pending until then (also validates the ADR 0009 prod-auth gap), and Phases 17–21
> follow the pilot. This track is built ahead of Phase 9 (same convention as Phases 10–12).

Status legend: 🔜 not started · 🏗️ in progress · 🧪 testing · 🔍 QA review · ⏸️ awaiting human approval · ✅ complete · ❌ blocked

## Current Phase Tasks

<!-- Lead recreates this table at the start of each phase. At the §6 Record step the
     completed phase's task detail is archived to docs/progress/phase-N.md (or a
     feature-named file) and replaced here by a one-line pointer (CLAUDE.md §7). -->

### Current work — Action-items fold + `visibility_scope` + case-access expiry (2026-07-02)

Three related increments on the shared `action_items` hub + the per-case ACL (partner-handoff
evaluation, ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md);
plan `~/.claude/plans/ethereal-tumbling-music.md`; decisions Q1–Q11 locked with human).
(1) `visibility_scope` (committee|case_restricted|assignees_only) + scope-aware `SELECT` on the hub
+ both satellites via `app.can_read_action_item`; guard hard-forces `case_restricted` for
`source_type='case'`, RPC computes default + coordinator override for cross-linked meeting/manual.
(2) Fold `case_action_items` into the hub as `source_type='case'` (new `source_case_id`/
`source_case_phase_id`; global status keys; single `committee_*` RPC family branching authority by
source, ADR 0033 D4 preserved; drop old case RPCs + table). (3) `case_access.expires_at`+`reason`,
expiry filter on **all six** consulters incl. the referral-PHI arm (Rule 12) + `list_my_cases`.
Pre-launch clean reset; remote push deferred. Full crew gate (RLS surface changed).

| # | Task | Owner | Status | Notes |
| - | ---- | ----- | ------ | ----- |
| B1 | Migration 1 `fold_case_action_items`: `visibility_scope` + `source_case_id`/`source_case_phase_id` + CHECKs/indexes; extend `guard_action_item` (case same-commission + force scope); `app.can_read_action_item`; scope-aware hub + 2 satellite `SELECT` policies; extend 5 `committee_*` RPCs (source branch, `p_visibility_scope`, `p_source_case_phase_id`); repoint `list_my_action_items`/`get_member_overview`/`case_action_items_kpis`; audit `v_cols` += new cols; drop 4 old case RPCs + `advance_action_item_core` + `case_action_items` table; flag descriptions | backend | ✅ | `supabase/migrations/20260707000000_fold_case_action_items.sql`. Also folded the old `action_items_meeting_link_check` into a single 3-source `action_items_case_link_check` (the old 2-value CHECK rejected every `case` row — caught by SQL probe). Guard-forces `case_restricted` (probed); KPI/readers repointed (probed `open/overdue/completed_ytd=1/1/1` as staff_admin). |
| B2 | Migration 2 `case_access_expiry`: `expires_at`+`reason` (nullable); expiry filter on all **six** consulters (`can_read_case`/`can_read_case_patient`/`can_write_case_content`/`get_member_overview` + **`referral_target_analyst`** [PHI, Rule 12] + **`list_my_cases`** both arms); `grant_case_access(+p_expires_at,+p_reason)`; `list_case_access` returns them; audit trigger tracks them | backend | ✅ | `supabase/migrations/20260708000000_case_access_expiry.sql`. `get_member_overview` redefined HERE (not 20260707) so its case-count grant arm gets the filter only after the column exists (ordering fix). `list_case_access` dropped-then-recreated (return-type change). Probed: `can_read_case` = t (live) / f (expired) / t (restored). |
| B3 | Local `db reset`; regen `database.ts` from local; repoint `queries/case-action-items.ts`, `cases/action-items-actions.ts`, `case-access/actions.ts` (+`listCaseAccess`); lint+typecheck; post contract signatures | backend | ✅ | Full local `db reset` clean (all migrations + seed). Types regen'd from local. Repointed the 3 files + extended `queries/cases.ts::listCaseAccessGrants`/`CaseAccessGrant` (the existing `list_case_access` reader) with `grantedAt`/`expiresAt`/`reason` — no new redundant fn. `npm run typecheck` GREEN (0 err, after clearing stale `.next/types`), `npm run lint` 0 errors (46 pre-existing e2e warnings). **No frontend-owned file needed editing** — all signature changes backward-compatible. Contract signatures posted to lead. |
| B4 | Scoped `revalidatePath` route-prefix fix (human-approved): the 2 feature-touched action files still used the pre-multi-tenancy `/c/[slug]/…` literal — a no-op on the standalone prod build, so the roster/panel/dialog never refreshed post-mutation (root cause of tester BUG-AIF-001). Swap `/c/[slug]` → `/o/[org]/c/[commission]` in `cases/action-items-actions.ts` (3 paths) + `case-access/actions.ts` (2 paths). `'page'` scope arg unchanged. | backend | ✅ | Mechanical 5-line prefix swap; verified route dirs exist + mirror `pqs/actions.ts`. `npm run lint` 0 errors, `npm run typecheck` GREEN. The other ~10 files with identical debt are OUT of scope → separate systemic task (untouched). Lead re-runs the 2 affected prod-build specs. |
| F1 | Verify case action-items panel/form works unchanged against the folded backend (`action-items-actions.ts` signatures UNCHANGED → live preview check) + fix 2 stale `case_action_items` comments (`layout.tsx:92`, `event-model.ts:42`). **Visibility toggle DEFERRED** — meeting item form (`action-item-form.tsx:102`) doesn't expose a case cross-link ("not in this form for v1"), so a "shown only when case linked" toggle can't render; ships later with a case-linking UI (follow-up below). | frontend | ✅ | **DONE.** No component code change needed (signatures unchanged). Verified LIVE (local dev): created a case action item (→ `create_committee_action_item`) rendering **Aberto** label, then completed it (→ `complete_committee_action_item`) rendering **Concluído** — labels from `case-extras-labels.ts` unchanged; 0 console/network errors. 2 stale comments updated. **Visibility-toggle confirmation:** re-grepped `src/components/meetings/**` — the only case reference is `case-linker.tsx` (links a MEETING↔case for the timeline, not an action item) and `action-item-form.tsx:103` carries `item?.caseId` through but never lets a user SET it. No creation path attaches a case to a meeting/manual item → toggle correctly deferred. |
| F2 | Case-access panel grant **dialog** (level + expiry presets 30/90 dias + date + "sem prazo" + optional reason); per-row "Expira em"/"Expirada" badge + reason display; wire `listCaseAccess` | frontend | ✅ | **DONE.** Rebuilt `case-access-panel.tsx`: per-row **"Conceder/Editar acesso"** button opens a grant `Dialog` (level radios Leitura/Edição in a fieldset+legend; expiry `NativeSelect` presets Sem prazo / 30 dias / 90 dias / Data específica → shared `DatePicker`; optional Motivo textarea). Presets computed client-side to end-of-day ISO. Per-row shows level badge + **"Expira em dd/mm/aaaa"** (future) or **"Expirada"** badge (past; icon+text, not colour-alone) + **"Motivo: …"**; revoke X on granted rows. Wired to `listCaseAccessGrants` (expiresAt/reason) — closes QA INFO-N3. Keyboard-operable + labelled. Verified LIVE: granted Edição+30d+reason (→ badge + "Expira em 01/08/2026" + Motivo), revoked, and backdated a grant to confirm **Expirada** renders (screenshot to lead). lint 0-err / typecheck GREEN. |
| T1 | SQL RLS probes (scope matrix on hub + satellites; coordinator override; expired-grant lockout incl. `save_narrative_body` + referral PHI; revoked; `assignees_only`) + update `cases-extras`/`member-action-items-overview`/`case-access`/`phase10-meetings` specs + new ACs + keyboard grant dialog | tester | ✅ | **GREEN on the tester scope (pgTAP + 4 targeted specs).** pgTAP full suite **51 files / 1373** (rewrote `113_case_action_items` → 36 case-source hub assertions [scope matrix on hub+2 satellites · guard force-restrict · create/update/advance/complete/delete authority · fold KPIs]; NEW `183_case_access_expiry` 19 [all 6 consulters future-vs-expired incl. referral-PHI + `save_narrative_body`, revoked-denies, `grant_case_access` future-validation, `list_case_access` projection]; extended `182` +6 drop-invariants [table+4 RPCs+`advance_action_item_core` GONE]; fixed `181`/`182` for the new 12-arg/7-arg `committee_*` signatures; repointed `115`/`171` off the dropped table). Targeted E2E (chromium, w=1, **dev** server after `db reset`): `cases-extras` 24/24 · `member-action-items-overview` all · `case-access` 18p/1cond-skip (incl. NEW AC-3e expiry-grant · AC-3f Expirada+bounce · AC-3g keyboard-only grant dialog · rewired AC-3c revoke-X + AC-3d dialog-radio) · `phase10-meetings` 15/15 · **all 4 together 57/0**. **No app-code bugs filed:** the 3 prod-build failures (AC-Docs/AC-Tags/AC-ActionItems) are a **pre-existing baseline flake** on `manage/cases/[caseId]` (dialog-close/`router.refresh` under the standalone build) — they hit UNCHANGED tests (documents/tags actions not in this diff), item IS created + RLS-readable, and ALL pass on dev. Filed as BUG-AIF-001 (INFO, pre-existing, owner=frontend). Full E2E gate = lead. |
| Q1 | QA RLS-surface review (scope matrix, `can_read_action_item` authority, case-source RPC branches, 6-consulter expiry, folded-row audit, Rule 11/12) → `docs/reviews/action-items-fold-review.md` | qa | ✅ | **APPROVED** (0 BLOCKER · 0 MAJOR · 0 MINOR · 4 INFO), validated LIVE against the DB. Existence leak closed (scope matrix on hub + both satellites); guard force-restrict; ADR-0033-D4 write-grantee preserved; six-consulter expiry incl. referral-PHI arm complete in live bodies; folded rows gain audit; frontend Rule 9/10 + a11y clean. Report `docs/reviews/action-items-fold-review.md`. |
| L1 | Lead: ADR 0050 · task table · Follow-ups (satellites · case-link toggle · break-glass) · full E2E gate · `backend-state.md` · commit | lead | ✅ | **LANDED 2026-07-02 (human-approved).** ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md); 3 follow-ups; full E2E gate **443/41 = 0 new regressions**; backend-state.md updated; committed on `feat/shared-action-items`. Prod-build UI-refresh (BUG-AIF-001) diagnosed (layout-scope revalidation) + deferred to the systemic task per human. Remote `db push` deferred to human. |

### Current work — "Meus itens de ação" + member "Visão Geral" (layout feature)

Two additive UI features, current-commission-scoped. (1) A new **"Meus itens de ação"** page (MEU TRABALHO nav) — read-only unified list of action items assigned to the current user across `case_action_items` + `meeting_action_items` (NOT CAPA); columns Título · Gerado de · Criado em · Prazo · Status + Atribuído por + description preview + overdue flag; default active-only w/ toggle; source-type filter; link out to source. (2) **Visão Geral** rebuilt from placeholder tiles → a 5-card member dashboard (Casos não concluídos · Itens de ação pendentes · Reuniões não concluídas · Assinaturas pendentes · Respostas em andamento), same for staff + staff_admin; coordinator Painel untouched. Requirements captured via lead↔human interview; no ADR (no new architecture — reuses existing RLS/attribution rules).

| # | Task | Owner | Status | Notes |
| - | ---- | ----- | ------ | ----- |
| B1 | Contract stubs: `src/lib/queries/action-items.ts` (`listMyActionItems` → `MyActionItem[]`) + `overview.ts` (`getMemberOverview` → `MemberOverview`) | backend | ✅ | Final types shipped; FE built against them. |
| B2 | Migration `20260704000000`: DEFINER RPCs `list_my_action_items` + `get_member_overview`; REVOKE-before-GRANT (t19 pass); search_path pinned; no new RLS shape | backend | ✅ | Count 1 personal rule; phase/narrative attribution counted even w/ `case_access` OFF. |
| B3 | pgTAP `181_member_overview` (17 assertions: anon-exec deny + self-scope + flag-off omission + attributed-with-`case_access`-OFF counted); regen types; `backend-state.md` updated | backend | ✅ | Full pgTAP 1277/1277; typecheck+lint clean. |
| F1 | Nav: add "Meus itens de ação" to MEU TRABALHO (`ListTodo`), shown when `cases_extras` OR `meetings` on | frontend | ✅ | DONE. `layout.tsx` resolves `casesExtrasEnabled()`; `actionItemsOn = casesExtrasOn \|\| meetingsOn` → new `actionItemsEnabled` prop; sidebar item gated via `requiresActionItems`. Nav badge skipped v1 (lead-ack'd). |
| F2 | New page: unified action-items table (filters, sort, overdue, source badges + links) | frontend | ✅ | DONE. Route `meus-itens-de-acao/{page,loading,error}.tsx` → `listMyActionItems`. Client island (active-only default + toggle, source filter, dueDate-asc/overdue-first sort, description preview, overdue red flag). **3 notes honored:** dedicated `ActionItemStatusBadge` (honest open/in_progress/done/cancelled mapping, NOT case enum); neutral outline `ActionItemSourceBadge` type discriminator; **case link → member route `casos/{id}` (verified via `MyCaseCard`; NOT coordinator `manage/cases` which 404s for staff), suppressed to plain text when `case_access` OFF; meeting link → `meetings/{id}`.** |
| F3 | Visão Geral rebuild: 5 count cards (reuse card pattern + `StatCount`), clickable, secondary hints, replace area tiles | frontend | ✅ | DONE. `page.tsx` → `getMemberOverview`; 5 clickable cards (reuse `rounded-2xl … animate-rise-in` + `StatCount`), render at 0. Card 1 → `meus-casos`/`minhas-fases` per `case_access`; overdue + next-meeting hints. **typecheck + lint GREEN** (my files 0 warnings; typecheck ran against backend's regenerated types → RPC contract integrates). Ready for tester. |
| T1 | E2E for both features + full-suite green | tester | ✅ | Feature spec `member-action-items-overview` **16/16 in isolation** (re-verified after AC-12/13 hardening → lower-bound assertions, contamination-immune). Full suite **482p/14f/4skip**: 10 = cross-test contamination (all pass in isolation: AC-12 + phase4/5/7/8), 4 = **pre-existing `config.toml` SMTP change** (unrelated; fails in isolation too). **0 regressions from this feature.** Lead ran all runs (subagent watchdog). |
| Q1 | QA review (requirements + RLS/leak audit of the 2 RPCs) | qa | ✅ | **APPROVED** (0 BLOCKER, 0 MAJOR, 2 informational MINOR). Both DEFINER RPCs self-scoped + PHI-free + t19-guarded; Count 1 faithful to `can_read_case` (attribution regardless of `case_access`, grant leg flag-gated); flag-off omits/zeroes; frontend Rule 9 + member-reachable links + correct badge vocab + pt-BR/a11y/strict-TS. Report → `docs/reviews/member-overview-action-items-review.md`. |

### Shared (non-PHI) `action_items` hub — Option A unification (2026-07-02)

Unify the two non-PHI, membership-gated action-item sources (meetings + manual) into one shared `public.action_items` hub (hub-and-spoke; configurable status/urgency w/ normalized `category` + global defaults). `case_action_items` + CAPA deliberately left alone. No users → clean drop-and-recreate (no data migration); old `meeting_action_items` table + its 4 RPCs + guard/core dropped at end. Approved plan: `create-a-plan-for-tranquil-thimble.md`.

| # | Task | Owner | Status | Notes |
| - | ---- | ----- | ------ | ----- |
| B1 | Migration `20260706000000`: 5 tables (`action_items` hub + `action_item_{statuses,urgency_levels,assignments,status_history}`), RLS on all 5, guard + 2 audit triggers, 5 `committee_*` DEFINER RPCs, global status/urgency seed, `action_items` flag; repoint `list_my_action_items`/`get_member_overview` meeting arm to shared table; drop old meeting surface | backend | ✅ | Applies clean from `db reset`; RLS verified (hub member-read/staff_admin-write; satellites+lookups SELECT-only, DEFINER writes). Guard HC032 + one-active-owner partial unique + status_history + audit rows all verified. Status keys exactly `open/in_progress/done/cancelled`. |
| B2 | Data-access repoint: `meeting-action-items.ts` (read shared table, map status key), `meetings/actions.ts` (5 fns → `committee_*` RPCs; advance resolves key→id), `queries/meetings.ts` (pending-count read), `action-items.ts`+`overview.ts` (+`'manual'` source); regen `database.ts` | backend | ✅ | `src/lib` typecheck + lint clean. Contract posted to lead. |
| B3 | Contract gap: public `action_items_enabled()` flag-reader RPC (mirror `cases_extras_enabled`/`meetings_enabled`; t19 REVOKE→GRANT; added to same migration next to flag seed) + `actionItemsEnabled()` reader in `queries/action-items.ts` (fail-closed) for the sidebar gate | backend | ✅ | RPC returns true; t19 verified (anon=f, authed=t); regen types; full typecheck GREEN (FE added `'manual'` badge entry in interim). |
| F1 | FE: `action-item-badges.tsx` (+`'manual'` label/icon in the `Record<ActionItemSource,…>`), `action-items-table.tsx` (source cell/filter), sidebar gate (`… OR action_items` via `actionItemsEnabled()`) | frontend | ✅ | `'manual'` badge entry landed (full typecheck GREEN). Sidebar wiring in `layout.tsx` pending FE per lead. |
| T1 | Tests: update `120_meetings`/`171_cross_org_isolation`/`181_member_overview` off the dropped `meeting_action_items` surface; add `182_action_items.sql` (grants·hub RLS·create·advance/complete·one-active-owner·guard·delete·readers·audit); update `member-action-items-overview.spec.ts` for the `manual` source (Avulso badge/filter/keyboard) | tester | ✅ | **pgTAP full suite GREEN — 50 files / 1331 tests** (`182` 48/48; `181` 19/19; `120` 32/32; `171` 76/76). Targeted E2E (chromium, w=1, fresh reset): `member-action-items-overview` **16/16**, `phase10-meetings` **15/15**. `00_setup.sql` re-seeds the global status/urgency lookups after its `truncate organizations cascade` (which table-cascades into the NULL-commission globals). **0 bugs filed.** Full E2E green = lead. |

### Layout-adjustments batch (2026-07-02) — direct, no phase gate

Five scoped UI/UX adjustments (lead↔human interview; delivery = direct impl + lint/typecheck + live preview verify, no tester/qa gate — human-approved). All verified live in the running app (coordinator persona, `rede-a`/`ccih`).

| # | Change | Files | Status |
| - | ------ | ----- | ------ |
| L1 | "Minhas respostas" → bordered divider-row **list** (mirrors Casos), whole-row link + chevron (in_progress→resume, submitted→view) | `respostas/page.tsx`, `my-response-card.tsx` | ✅ verified |
| L2 | Status **pill badges**: "Em andamento" = blue (`accent`), "Enviada" = green (`success/12`); reuses `TOKEN_STYLES` | `my-response-card.tsx` | ✅ verified (computed colors) |
| L3 | Moved **Narrativas** builder from Configurações → **Construtor** page (route-based tabs "Formulários"/"Narrativas"); old `settings/narrativas` deleted (404s); `SettingsTabs` prop dropped; flag-gated | new `manage/forms/narrativas/page.tsx` + `construtor-tabs.tsx`; `manage/forms/page.tsx`; `settings-tabs.tsx` + 3 settings callers; `case-narratives/actions.ts` revalidate path | ✅ verified (tab loads; old route 404) |
| L4 | Fill-wizard `free_text` textarea starting height **doubled** (`min-h-20`→`min-h-40`, 80→160px); still `field-sizing-content`; other textareas untouched | `wizard/input-item.tsx` | ✅ verified (computed 160px) |
| L5 | Coordinators add **only already-registered** org users via searchable picker (invite-by-email removed); DEFINER RPC `list_addable_commission_members` (coordinator-gated, org-scoped, excludes members + `is_admin`), reworked `addStaff(userId)` action w/ server-side re-verify | migration `20260705000000`; `database.ts` (hand-added RPC type — regen on next `gen types`); `queries/members.ts`; `members/actions.ts`; new `add-member-picker.tsx`; `manage/members/page.tsx`; deleted `invite-staff-form.tsx` | ✅ verified end-to-end (add flow works; email path gone; t19 REVOKE ok) |

⚠️ Follow-up: run `supabase gen types` after the migration is applied remotely (RPC type was hand-added to `database.ts` to match the migration) and update `docs/backend-state.md` with the new RPC.

### Completed work (archived to docs/progress/)

- **User Registration & Identity Management** (`org_admin` registers users w/ professional credentials; verify+activate via invite; searchable org directory + full per-user management; **real `is_active` enforcement** — `app.is_active()` fold + `signIn`/`getSessionContext` gate + `/conta-inativa` — plus `suspended_until`; token_hash pt-BR email templates) — ✅ **COMPLETE 2026-07-02**, QA APPROVED (1 BLOCKER + 4 MINOR all cleared), feature E2E 12/12 + pgTAP 1257/1257 + full E2E 467p/10-contamination (0 regressions). Bugs UREG-001..004 found+fixed en route. ADR [0048](docs/decisions/0048-user-registration-identity.md) → [user-registration.md](docs/progress/user-registration.md). ⚠️ Phase-9 dep: upload the invite/recovery email templates to the Supabase Dashboard.
- **Ad-hoc Case Narratives** (flag `case_narratives`; `add_ad_hoc_narrative` DEFINER RPC + `case_narratives.is_ad_hoc`; coordinator adds a narrative to an open case, mirroring `add_ad_hoc_phase`) — ✅ **COMPLETE 2026-07-01**, QA APPROVED (0 BLOCKER · 1 MAJOR + 2 MINOR cleared), pgTAP 1219/1219 + feature E2E 5/5 + full E2E 461p/7 pre-existing (0 regressions). ADR [0047](docs/decisions/0047-ad-hoc-case-narratives.md) → [ad-hoc-narratives.md](docs/progress/ad-hoc-narratives.md).
- **Answer-Model v2 + form-definition forward-compat** (uniform answer row + typed scalar cols `value_number/date/time` + instance-ready answer-key scaffolding + question defaults; evaluator byte-for-byte unchanged) — ✅ **COMPLETE 2026-07-01**, QA APPROVED, pgTAP 1205/1205 + Vitest 176/176 + feature E2E 6/6. ADRs [0045](docs/decisions/0045-answer-model-v2.md)/[0046](docs/decisions/0046-forward-compat-form-capabilities.md) → [answer-model-v2.md](docs/progress/answer-model-v2.md).
- **Form data-model normalization** (`form_items.options` jsonb → normalized `form_item_options` + `answer_selected_options`; dual evaluator byte-for-byte via `app.answer_map`) — ✅ **COMPLETE 2026-07-01**, QA APPROVED → [form-model-normalization.md](docs/progress/form-model-normalization.md).
- **Result-based phase recommendation** (`recommend_when` combinable answer/result groups; recommend from an earlier phase’s result; suggestion-only) — ✅ **COMPLETE 2026-06-26**, QA APPROVED. ADR [0043](docs/decisions/0043-phase-result-based-recommendation.md) → [result-rec.md](docs/progress/result-rec.md).
- **NSP-per-org** (per-org PQS roster + `nsp_coordinator` + org-scoped PHI doors; restores NSP + referral modules under multi-tenancy) — ✅ **COMPLETE 2026-06-25**, QA APPROVED (sub-phases A+B). ADR [0042](docs/decisions/0042-nsp-per-org.md) → [nsp-per-org.md](docs/progress/nsp-per-org.md).
- **"Sem processo" (process-less cases)** (flag `processless_cases`; template-less case + optional offered-outcome set + optional PHI; 2 coordinator-gated DEFINER RPCs, no new RLS shape) — ✅ **COMPLETE 2026-06-30**, QA APPROVED (0 BLOCKER/0 MAJOR/1 informational), pgTAP 1153/1153 + feature E2E 8/8 + full E2E 439/0 (dev). ADR [0044](docs/decisions/0044-processless-cases.md) → [processless-cases.md](docs/progress/processless-cases.md). ✅ remote re-baselined 2026-07-01.
- **`case_phase_results`** (per-phase categorical result + manual override; flag ON) — ✅ QA APPROVED 2026-06-23, tester pgTAP 45/45 + E2E 7/7, passed in the MT full-suite 292/0 → [case-phase-results.md](docs/progress/case-phase-results.md)
- **Form Builder Enhancements** (new question types · option colors · per-question conditions · observations) — ✅ 2026-06-23 → [form-builder-enhancements.md](docs/progress/form-builder-enhancements.md). ✅ Fully reconciled — **BUG-FBE-004** RESOLVED (`deb436c`, re-verified 2026-06-30).
- **Phase 23 — Patient Identity & Cross-Committee Linkage** (`patient_index`) — ✅ 2026-06-22 → [phase-23.md](docs/progress/phase-23.md)
- **Phase 22 — Inter-Committee Case Referrals** (`case_referrals`) — ✅ 2026-06-21 → [phase-22.md](docs/progress/phase-22.md)
- **`case_patient`** (third PHI module; ADR 0038) — ✅ 2026-06-22 → [case-patient.md](docs/progress/case-patient.md)
- **Case Access Control & "Meus Casos"** (flag `case_access`) + dialog/attribution refinement — ✅ 2026-06-19 → [case-access.md](docs/progress/case-access.md)
- **Case Narratives** (feature-flagged) — QA CHANGES REQUESTED 2026-06-19 → [case-narratives.md](docs/progress/case-narratives.md)

## Bug Log

<!-- Filed by tester; status updated ONLY by tester after re-verification.
     Resolved/closed bugs are archived at phase close → docs/progress/bug-log-archive.md (~78 rows). -->

| ID | Phase | Severity | Description / repro | Expected | Actual | Spec | Owner | Status |
| -- | ----- | -------- | ------------------- | -------- | ------ | ---- | ----- | ------ |
| BUG-AIF-001 | Action-items fold | INFO (pre-existing, NOT a fold regression) | On the **standalone prod build** only, mutation dialogs on `manage/cases/[caseId]` do not close after a successful write. Repro: coordinator opens "Novo item de ação" (or the document-upload / tag menus), submits → the server action returns **200** and the row IS created + RLS-readable, but the dialog stays open (its `useEffect` close on `state.ok` + `router.refresh()` doesn't complete). Reproduces deterministically 3/3. Affects **UNCHANGED** tests (documents/tags actions are not in this diff), so it is NOT introduced by the fold. **Root cause CONFIRMED (lead, 2026-07-02) — NOT the stale path.** The stale `/c/[slug]` path was fixed in B4 but the specs stayed red. Real cause: the case-access panel + its grants load in the case-detail **`(detail)/layout.tsx`** (line 86), but the case actions `revalidatePath(…, 'page')` — which never revalidates LAYOUT data on the standalone build, so `router.refresh()` returns stale RSC. Fix = **`'page'`→`'layout'` scope** (mirror `pqs/actions.ts`), systemic across ~12 case action files. The grant/item DOES persist correctly (DB-verified: AC-3e grant landed with right level/expiry/reason) — logic sound; only the prod UI refresh is stale. Now also blocks our own AC-3e/AC-3f (grant) + AC-ActionItems (item) on prod. | Dialog closes + panel shows the new item after a successful create (as on dev). | Dialog stays open under standalone prod build; item created but panel doesn't refresh. All GREEN on `npm run dev`. | `cases-extras.spec.ts` AC-Docs/AC-Tags/AC-ActionItems | separate systemic task | OPEN — deferred (human-approved 2026-07-02; fix = `'page'`→`'layout'` scope across the ~12 case action files; our 2 feature files already have the correct PATH from B4) |

> **1 OPEN bug (INFO, pre-existing prod-build baseline — not a fold regression; see BUG-AIF-001).** All resolved/closed bugs (incl. the user-reg + NSP-per-org + form-model + form-builder rows) archived → [docs/progress/bug-log-archive.md](docs/progress/bug-log-archive.md).

## Test Run Summary

<!-- Full historical run log (Phases 0–MT, ~78 rows) archived → docs/progress/test-run-archive.md.
     Keep only the most recent gate's rows here. -->

| Date | Phase | Specs | Passed | Failed | Notes |
| ---- | ----- | ----- | ------ | ------ | ----- |
| 2026-07-02 | Shared `action_items` hub (Option A) | pgTAP full suite | 1331 | 0 | **GREEN — 50 files.** New `182_action_items.sql` 48/48 (grants·hub RLS·create·advance/complete·one-active-owner·guard HC032/23514·delete·readers·audit). Updated `120_meetings` 32/32, `171_cross_org_isolation` 76/76, `181_member_overview` 19/19 (now covers a `manual`-source item + `action_items`-flag-off omission). `00_setup.sql` re-seeds the global status/urgency lookups (its `truncate organizations cascade` table-cascades into the NULL-commission globals). |
| 2026-07-02 | Shared `action_items` hub (Option A) | targeted E2E (chromium, w=1, fresh reset) | 31 | 0 | `member-action-items-overview` **16/16** (mixed case+meeting+**manual**; Avulso badge/no-link, source filter incl. Avulso, keyboard-only op of the Avulso radio). `phase10-meetings` **15/15** (meeting UI now writes the shared hub via `create_committee_action_item`). Full-suite E2E green = lead (subagent watchdog). |
| 2026-07-02 | Meus itens de ação + Visão Geral | full E2E (chromium, w=1) | 482 | 14 (4 skip) | **0 regressions.** 10 failures = cross-test contamination → all pass in isolation (feature `member-action-items-overview` **16/16**, phase4 8/8, phase5 12/12, phase7 15/15, phase8 24/24). 4 = pre-existing `supabase/config.toml` SMTP switch (Resend vs local Mailpit) breaking `user-registration` invite-email flow — unrelated to this feature; also fails in isolation. |
| 2026-07-02 | Action-items fold + `visibility_scope` + case-access expiry | pgTAP full suite | 1373 | 0 | **GREEN — 51 files.** Rewrote `113_case_action_items` (19→**36**: case-source hub scope matrix on hub+`action_item_assignments`+`action_item_status_history` · guard force-restrict + link CHECKs · create/update authority `can_write_case_content` · advance/complete assignee-OR-content-writer HC027 · delete staff_admin · fold KPIs `open/overdue/completed_ytd`). NEW **`183_case_access_expiry`** (19): all six consulters future-grants vs expired-denies incl. `referral_target_analyst`→`can_read_referral_phi` (Rule 12) + `save_narrative_body` block + `get_member_overview` + both `list_my_cases` arms; revoked-denies; `grant_case_access` past-expiry reject; `list_case_access` returns `expires_at`/`reason`. Extended `182` +6 catalog drop-invariants (`case_action_items` table + 4 old RPCs + `advance_action_item_core` GONE) + fixed its `has_function_privilege` sigs for the 12-arg/7-arg `committee_*`. Fixed `181` (old `create_action_item`→hub). Repointed `115`/`171` off the dropped table. |
| 2026-07-02 | Action-items fold + case-access expiry | **full E2E gate (prod standalone, chromium, w=1, fresh reset)** | 443 | 41 (3 skip · 16 did-not-run) | **0 NEW regressions — lead-triaged all 41.** ~9 = `config.toml` SMTP (user-registration + phase3 invite); ~26 = known prod-build flaky baseline in specs the diff can't touch (phase4/5/7/8/10/11 — `waitForURL`/`getByLabel` timeouts); ~6 = **BUG-AIF-001** revalidate-scope (incl. our AC-3e/AC-ActionItems). Feature logic PROVEN green independently (grant DB-persisted with correct level/expiry/reason + pgTAP 1373/0 + QA live). Scoped `revalidatePath` PATH fix (B4) applied but insufficient → real cause is layout-scope revalidation (BUG-AIF-001, deferred per human 2026-07-02). Re-verify of the 3 affected specs after B4 confirmed the path swap alone doesn't green them (evidence for the layout-scope diagnosis). |
| 2026-07-02 | Action-items fold + `visibility_scope` + case-access expiry | targeted E2E (chromium, w=1, dev after `db reset`) | 57 | 0 | **All 4 targeted specs GREEN together (57/0).** `cases-extras` 24/24 (AC-ActionItems rewired: create-via-UI → `create_committee_action_item`, complete via `complete_committee_action_item`, status read from the hub). `member-action-items-overview` all (case-source fixtures repointed to `action_items`/`source_case_id`/`status_id`; counts unchanged). `case-access` 18p/1cond-skip — rewired AC-3c (revoke X button) + AC-3d (dialog level radios) + NEW **AC-3e** (grant+30-dias → "Expira em"), **AC-3f** (past grant → "Expirada" + expired member bounced to notFound), **AC-3g** (keyboard-only grant dialog). `phase10-meetings` 15/15 (meeting items create/advance via hub unchanged; visibility toggle DEFERRED, not tested). **Prod-build note:** 3 `manage/cases/[caseId]` dialog-close failures under the standalone build (AC-Docs/AC-Tags/AC-ActionItems) = pre-existing baseline (BUG-AIF-001), all pass on dev. |

> Most recent gate: **Action-items fold + `visibility_scope` + case-access expiry** — pgTAP **1373/0** (51 files) + targeted E2E **57/0** (dev) + **full E2E gate 443/41 (0 new regressions, all 41 pre-existing)** (prod standalone, chromium, w=1). BUG-AIF-001 (prod-build layout-scope revalidation) diagnosed + deferred per human. Prior gate (Meus itens de ação) archived → [docs/progress/test-run-archive.md](docs/progress/test-run-archive.md).

## QA Verdicts

<!-- One row per phase/feature. Full reviews under docs/reviews/; verbose pre-collapse copy in docs/progress/qa-verdicts-archive.md -->

| Phase / Feature | Verdict | Report | Notes |
| --------------- | ------- | ------ | ----- |
| Action-Items Fold + `visibility_scope` + Case-Access Expiry (ADR 0050) | **APPROVED** (2026-07-02) | [report](docs/reviews/action-items-fold-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 4 INFO. RLS surface verified **live** against the DB (matrix under `set role authenticated` + JWT). **Existence leak CLOSED:** on a clean case, a plain member sees only the `committee` item — blind to the `case_restricted` row AND both satellites (no leak via `status_history.comment` or `assignments`); phase-assignee + `case_access` grantee + staff_admin see it; `assignees_only` → assignee + staff_admin only. **Guard invariant:** `source_type='case'` force-set `case_restricted` on INSERT + re-forced on UPDATE (can't be made committee-visible); single 3-source link CHECK (old meeting-CHECK folded in) rejects all four malformed-row shapes. **ADR-0033-D4 HOLDS:** a WRITE-grantee who is only `staff` (not staff_admin) creates a case action item via the RPC; read-only grantee denied (42501); advance/complete = assignee OR content-writer (HC027 proven both ways). All committee_* + case_access RPCs SECURITY DEFINER + owner postgres + pinned search_path; **t19 anon-exec clean** (ACL = authenticated/postgres/service_role; live `set role anon` denied). **Rule 12 six-consulter expiry COMPLETE:** `(ca.expires_at is null or ca.expires_at > now())` in the live body of `can_read_case`, `can_read_case_patient`, `can_write_case_content`, `referral_target_analyst` (PHI arm), `get_member_overview`, and `list_my_cases` (BOTH arms) — expired read grant → `can_read_case`=false; expired write grant → case-item create denied. **Rule 11:** folded case rows now emit `action_item.*` audit (new coverage); diff allow-list adds only `source_case_id/source_case_phase_id/visibility_scope`, copies NO title/description/PHI; `trg_audit_case_access` tracks `level/expires_at/reason`. **Drop complete:** `case_action_items` + 4 old RPCs + `advance_action_item_core` gone (live catalog); no app-code refs. **Frontend:** no inline supabase-js (Rule 9), pt-BR (Rule 10), a11y (dialog labels, fieldset/legend radiogroup, `aria-describedby`, expired badge = icon+text not colour-alone), strict-TS clean. Backend's 2 beyond-spec calls valid: (a) folded 3-source CHECK complete; (b) `get_member_overview` redefined in migration 2 (ordering-correct — 20260708 newest, live body carries the expiry filter). typecheck clean · lint 0 errors · Vitest 193/193. INFO-1..4: hub inline vs satellite-helper (intentional), per-row parent lookup (indexed, norm), `list_case_access` returns expired rows by design (config read, not authority), BUG-AIF-001 pre-existing/out-of-scope. |
| Shared (non-PHI) `action_items` table (Option A) | **APPROVED** (2026-07-02) | [report](docs/reviews/shared-action-items-review.md) | 0 BLOCKER, 0 MAJOR, 1 NIT + 2 OBSERVATIONS (all non-blocking). Security props verified **live** against the DB, not just SQL-read. **RLS enabled on all 5 tables** (`relrowsecurity=t`); hub SELECT = flat membership OR org-admin (byte-identical to dropped `meeting_action_items_select`); hub WRITE (`ALL`) = staff_admin/org_admin with **both** USING+WITH CHECK (no cross-commission reassignment). Satellites SELECT-only via `commission_of_action_item`; lookups SELECT `true`; **no write policy** on either → RLS default-deny confirmed live (direct `authenticated` INSERT into assignments/history/statuses each → "violates row-level security policy"); only the DEFINER RPCs (owner postgres) write. All 8 RPCs SECURITY DEFINER + owner postgres + pinned search_path; **no anon/PUBLIC EXECUTE** (t19 clean live); authority internal + `auth.uid()`-based (create/delete staff_admin→42501; advance/complete assignee-or-admin→HC037; assignee-member→HC021; status cross-commission injection blocked). **Rule 11:** audit added (was absent on old table); `audit_diff` allow-list `{source_type,status_id,urgency_id,due_date,assigned_to,case_id}` — title/description (free-text) excluded, no PHI copied. **Rule 12:** no PHI in hub; CAPA + `case_action_items` untouched/isolated (grep-confirmed). **Rule 9/10:** mutations via `committee_*` RPCs, reads via `src/lib/queries/`, components carry no inline supabase-js, pt-BR strings + a11y (caption/scope/radiogroup/focus rings/sr-only overdue), typecheck clean. Old `meeting_action_items` table + 4 RPCs + core + guard **fully dropped** (live). Repointed `list_my_action_items`/`get_member_overview` read shared hub with zero `meeting_action_items` refs (live). Every FK indexed + one-active-owner & active/overdue partials present. pgTAP `182` (48 assertions) covers grants/RLS/create/advance/owner/guard/delete/readers/audit; `171`+`181` updated; e2e adds `manual`/Avulso. NIT-1: `resolveActionItemStatusId` uses inline supabase-js but is inside `src/lib/` (Rule 9 OK). OBS-1: policies delegate to `is_*` helpers vs `(select auth.uid())` — house pattern, intent met. OBS-2: standalone pgTAP re-run blocked by search-path/fixture harness artifact, not a failure. 18 E2E fails pre-root-caused unrelated (accepted). |
| Member Overview & "Meus itens de ação" | **APPROVED** (2026-07-02) | [report](docs/reviews/member-overview-action-items-review.md) | 0 BLOCKER, 0 MAJOR, 2 MINOR (informational, no change). Both `SECURITY DEFINER` RPCs (`list_my_action_items`, `get_member_overview`) strictly self-scoped (`= auth.uid()` on every arm), STABLE, `search_path` pinned, `owner postgres`, `REVOKE…FROM public` before `GRANT…authenticated` (t19 guard, asserted). Label joins expose only PHI-free `case_number`/`label`/`meeting_number`/`scheduled_start`/`full_name` — no `*_md`/free-text/answers/`case_patient`/PHI; no audit row correct (self-scoped, non-PHI — Rule 11). **Count 1** faithful to `app.can_read_case`: phase/narrative attribution counts regardless of `case_access`, grant leg flag-gated, non-terminal (excl. `concluido`/`cancelado`), `cases_extras`-gated — pgTAP t17 proves attributed-but-not-granted counted with `case_access` OFF. Flag-off sources omitted from union / counts 0/null, never raise. Frontend: Rule 9 (no inline supabase-js; RPCs in regenerated types); case link → member `casos/{id}` degrading to plain text when flag OFF (route 404s; verified `getCaseDetail` re-enforces `can_read_case` server-side), meeting → `meetings/{id}`; status badge = action-item vocab (not case enum); source badge distinct discriminator. pt-BR + a11y (table semantics, labeled radiogroup/checkbox, focus rings, overdue not color-alone, reduced-motion) + strict-TS (no `any`) all pass. Sub-entitlement note examined: an action-item assignee who isn't a `can_read_case` reader sees only PHI-free label material by design — no gated content leaks. E2E 16/16 (isolation), pgTAP 1277/1277. |
| User Registration & Identity Management | ✅ APPROVED (CHANGES→APPROVED, re-re-review 2026-07-01) | [report](docs/reviews/user-registration-review.md) | 1 BLOCKER (B1: profiles peer-branch bypassed the `app.is_active()` fold) + 4 MINORs — all resolved + independently re-verified. BUG-UREG-004 attributed to **M4** (invalid Go template), not B1. Full trail → [user-registration.md](docs/progress/user-registration.md). |
| ad-hoc-narratives | CHANGES→**APPROVED** (2026-07-01) | [report](docs/reviews/ad-hoc-narratives-review.md) | 0 BLOCKER; all 3 findings RESOLVED + re-verified against source. Security core sound: `add_ad_hoc_narrative` (SECURITY DEFINER, consistent with the narrative-write RPC family) re-derives every gate — coordinator (42501), terminal HC020 *before* type resolution, assignee HC021, cross-commission type HC054; inline vocab insert commission-pinned to the case's own commission (no cross-commission write); `REVOKE…FROM public` precedes GRANT (t19 guard); `search_path` pinned both fns; no dynamic SQL. Rule 11: allow-list += only `is_ad_hoc`; body_md/title/instructions stay OUT (pgTAP proves it). Rule 7: dialog collects no body. Rule 9/§8: routes via RPC, re-checks commission for clean pt-BR forbidden, maps HC020/21/54/42501, generic default (no raw leak). Precedent parity: display_position UNION verbatim, is_ad_hoc=true/status='aberta'/is_expected=false. UI gated on open+staff_admin (layout notFound). a11y + keyboard pass (E2E AC-5). **M1 (MAJOR) RESOLVED:** new ADR `0047-ad-hoc-case-narratives.md` reverses ADR 0032 D7 for open cases only (remove/reorder explicitly out of scope); 0032 D7 carries a supersession note → 0047. **M2 (MINOR) RESOLVED:** chip now pt-BR `adicional` (card L210) matching `case-phase-article.tsx:91`; E2E AC-1 asserts `/adicional/i`. **M3 (MINOR) RESOLVED:** ON CONFLICT do-update now sets `archived=false` (migration L89); pgTAP `plan(14)` test 3b asserts reuse-no-dup + `archived→false`. Coordinator reports E2E 5/5, pgTAP 1219/1219 green. Cleared for human approval. |
| answer-model-v2 | **APPROVED** (2026-07-01) | [report](docs/reviews/answer-model-v2-review.md) | 0 BLOCKER, 3 MINOR (non-blocking). **Keystone (Rule 3) HOLDS:** no evaluator fn redefined in either migration, no TS twin (`conditions.ts`/`effective-visibility.ts`) in the branch diff, `60_answer_map_golden` guards `answer_map`/`_by_item`/`case_phase` byte-for-byte (new uniform-row fixture). **RLS:** `response_group_instances` mirrors the inline `answers` predicate verbatim (not a stub; no `commission_of_response` helper); re-keyed `answer_selected_options` re-derives via `answer_id→answers` + indexed + grants re-asserted; 61 proves non-creator denied / creator allowed. **Immutability:** new `answers` cols + `form_items` cols ride the pre-existing whole-row guards; `guard_submitted_selections` twin resolves the response via `answer_id`; `response_group_instances` covered — 61 `throws_ok` on all three. **Typed trigger** exception-guarded (never fails a save; `value` canonical). **HC080** publish validation by type + `version_has_option_code`; **option-code.ts** the single client+server generator (no double-mint, code stable across rename); **BUG-AMV2-001** (`publishVersion` maps HC080 as well as 23514) + **BUG-AMV2-002** (client mints real code) both correct. Types regenerated; `withDefaults` seeds only visible unanswered items; scaffolding inert. MINORs: (1) back-port new-surface immutability cases into `10_immutability.sql`; (2) `VERSION_TREE_SELECT` PGRST201 hint now defensive-only; (3) ARCHITECTURE.md Rule 2 + backend-state.md doc-sync deferred to Record (tracked). Cleared for human approval + 2→1 re-squash. |
| form-model-norm | CHANGES→**APPROVED** (2026-07-01) | [report](docs/reviews/form-model-normalization-review.md) | 0 BLOCKER, 0 open. **MAJOR-1 RESOLVED (`e13081f`+`e158e91`, re-verified live):** per-row position loop replaced by a single-transaction SECURITY INVOKER RPC `reconcile_item_options` (mig `20260630017000`) — delete removed→update kept→insert new in a temp band→assign ALL positions in one `UPDATE` (unnest…with ordinality), so the DEFERRABLE unique tolerates the transient collision. Reproduced my exact repro (move option UP into occupied slot + rename + add) live through the RPC → no unique violation, codes preserved; `prosecdef=f` (INVOKER); published-item reconcile still blocked by `guard_published_structure` (verified). Coverage: pgTAP `61_option_reconcile` 9 assertions (exact repro + codes preserved + atomic add/remove/reorder + kept-row update code-frozen + HC013 dup), **suite 1180/1180**; E2E **NORM-5** (edit-dialog reorder existing options into occupied slot → persists + codes preserved + survives reload + condition-on-reordered-code resolves + publishes). Fix diff scoped to reconcile+RPC+tests; evaluator/vectors byte-for-byte unchanged. **All other dimensions PASS** (original review): RLS byte-identical to `form_items`/`answers` mirrors; immutability guards sound; evaluator non-drift verified; clone copies codes verbatim; publish + template validators assert option-code existence; dashboards by code + current label; caught+fixed a real `case_phase_answer_map` bug; full wishlist delivered, translations deferred. FMN-001 (PGRST201) + FMN-002 fixed in-loop. Cleared for human approval + BE-7 squash. <!-- superseded original CHANGES-REQUESTED detail below; kept for trail --> ~~1 MAJOR, 0 BLOCKER. **MAJOR-1:** reordering an EXISTING choice item's options fails with a duplicate-key error — `reconcileOptionRows` (`src/lib/forms/actions.ts:931`) updates each kept row's `position` in a SEPARATE transaction, but `form_item_options_item_id_position_key` is DEFERRABLE-but-not-INITIALLY-DEFERRED, so moving an option UP collides with the row still holding that slot (reproduced live on the real table). `updateItem`→`MESSAGES.generic`, reorder silently NOT persisted. Uncovered by E2E (NORM-1/4 reorder only via the ADD/batch-insert path; NORM-1 edit only renames). pt-BR error, no raw leak, no RLS/immutability/corruption. Fix = single-txn reorder (INITIALLY DEFERRED / bulk update / two-phase bump / delete+reinsert preserving code) + an edit-dialog reorder E2E. **Everything else PASS:** both new tables' RLS byte-identical to `form_items`/`answers` mirrors; immutability guards (published options + submitted selections) sound; **evaluator non-drift verified byte-for-byte** (`conditions.ts`+vectors empty diff, no eval fn redefined, `answer_map`/`answer_map_by_item` rebuild correct shapes, pgTAP 21 proves the unchanged evaluator agrees); clone copies codes verbatim; publish + template validators now assert option-code existence; dashboards group by code + resolve current label; caught+fixed a real `case_phase_answer_map` bug (recommendation/result engine was blind to choice answers); full author wishlist delivered (label/color/score/analytics_code/ordering/stable code+UUID), translations correctly deferred. FMN-001 (PGRST201) + FMN-002 (sign-off review) already fixed in-loop.~~ |
| result-rec | **APPROVED** (2026-06-26) | [report](docs/reviews/result-rec-review.md) | 0 blockers, 0 majors. Zero evaluator drift confirmed (eval_condition unchanged; synthetic-map reuse exact SQL↔TS). HC063/HC064 enforced at add/update/publish + materialization. Suggestion-only invariant holds (only `recommended` flag mutated). Anon-execute leak closed by migration 5. All pt-BR, a11y, strict-TS clean. Remote re-baselined 2026-07-01 (subsumed by the single-baseline reset). INFO-1: app-schema helpers no explicit grant (consistent with all peers). INFO-2: combinator hidden for 1 row (intentional UX). |
| NSP-per-org (sub-phase B, whole-phase) | **APPROVED** (2026-06-25) | [report](docs/reviews/nsp-per-org-b-review.md) | Closes the per-org NSP console + the post-A migration deltas. pgTAP 1102/1102 (independently re-ran `176`+A-core regression green); typecheck clean; lint 0 errors. **2 RLS broadenings** (`organizations_select`/`commissions_select` += PQS/coordinator) verified minimal + cross-org-denied + **plain-member NOT widened** (probed live, `176 §D3` keystone). **Appoint flow** airtight: `list_org_eligible_users_for_pqs` coordinator/org_admin-gated + PHI-free; orphan-the-org guard refuses demoting an org_admin; revoke role-filtered; three-way duty separation proven. **BUG-NSP-004** (`advance_capa_action_core`→`can_write_capa`) + **005** (commissions broadening) correct. **A-core isolation STILL holds** under B (re-probed). **No PHI/console reachable by non-PQS**: platform@/foreign-org/plain all 404; non-enrolled coordinator sees empty (doors deny: 0 events/0 inbox/NULL PHI). Coverage adequate (`176 §A–D` + `nsp-cross-org-isolation` X-1..X-6 + 124 re-homed). 1 INFO (`authorizeOrgAdmin` admits platform_admin for membership mgmt — not PHI, consistent w/ MT posture). |
| NSP-per-org (sub-phase A) | iter1/2 CHANGES → iter3 **APPROVED** (2026-06-25) | [report](docs/reviews/nsp-per-org-a-review.md) | Closes sub-phase A backend security core. pgTAP 1073/1073 (incl. `175` dropped-symbol catalog invariant + `143 §M3` result-scope guard). All findings resolved + independently re-verified live: **M1** bundle `service_role`-only (bypass denied); **M2** `capa_viewer_can_manage` per-entity org-correct; **M3** (iter3 `5f4baf5`) `capa_kpis` **result set** org-scoped via `in_scope` CTE incl. the overdue subquery (non-event fallback aligned w/ `can_write_capa`) — proven pqs.a 1→1 vs iter-2's 1→2; **I1** `dispose_case_phi` walled (platform_admin & cross-org org_admin DENIED). **Independent body-not-scoped sweep:** 9 DEFINER set/jsonb doors over the 5 PHI tables, each org-filtered or entity-keyed (spot-checked 3 bodies) — class empty. |
| Multi-Tenancy | **APPROVED** (2026-06-25) | [report](docs/reviews/multitenancy-review.md) | 0/0. platform_admin wall + multi-org PHI guard verified; cross-org isolation proven. INFO: remove legacy `getCommissionAccess(slug)`. |
| Form Builder Enhancements | **APPROVED** (2026-06-23) | [report](docs/reviews/form-builder-enhancements-review.md) | 0/0. MAJOR-1 (number-target lexical compare) resolved. ✅ BUG-FBE-004 RESOLVED (`deb436c`, re-verified 2026-06-30). |
| `case_phase_results` | **APPROVED** (2026-06-23) | [report](docs/reviews/phase-results-review.md) | 0/0. RLS + dual-entry override correct; non-PHI; flag OFF. |
| 23 — Patient Identity | **APPROVED** (2026-06-22) | [report](docs/reviews/phase-23-review.md) | 0/0. `patient_xref` QPS-only; audit truncates MRN. |
| `case_patient` (ADR 0038) | **APPROVED** (2026-06-22) | [report](docs/reviews/case-patient-review.md) | 0/0. Audited single-door; `dispose_case_phi`; flag OFF. |
| 22 — Inter-Committee Referrals | **APPROVED** (2026-06-21) | [report](docs/reviews/phase-22-review.md) | 0/0. `referral_patient` single-door; 3-layer PHI lockdown; flag OFF. |
| PHI/HIPAA Remediation (WS0–E) | **APPROVED** (2026-06-20) | [report](docs/reviews/phi-remediation-review.md) | 0/0. `event_patient` lockdown; 22 PHI free-text cols → Phase-19 AC. |
| 14b–14d — Triage/RCA/CAPA | CHANGES→**APPROVED** (2026-06-18) | [report](docs/reviews/phase-14-review.md) | BLOCKER-1 + MINOR-1 resolved; pgTAP 516/516. |
| 14a — NSP Foundation | CHANGES→**APPROVED** (2026-06-18) | [report](docs/reviews/phase-14a-review.md) | M1 (`pqs_department` SELECT) + N1 fixed. |
| 13 — Audit Trail | **APPROVED** (2026-06-18) | inline | 0/0. Append-only guard + hash chain + data-min allow-list; MINOR-1 resolved. |
| 12 — Case Timeline | **APPROVED** (2026-06-16) | [report](docs/reviews/phase-12-review.md) | 0/0. Read-only composition; MINOR-1 resolved. |
| 11 — Interviews | **APPROVED** (2026-06-15) | [report](docs/reviews/phase-11-review.md) | 0/0. `can_write_interview` shape verified; P11-MINOR-1 resolved. |
| 10 — Meetings | **APPROVED** (2026-06-15) | [report](docs/reviews/phase-10-review.md) | 0/0. Sign-own-row bypass + `can_sign_meeting` verified; 2 MINOR pre-record. |
| Case Access Control | **APPROVED** (2026-06-19) | [report](docs/reviews/case-access-control-review.md) | 0/0. CA-001 BLOCKER + CA-002 MAJOR resolved; 4 INFO → Follow-ups. |
| Case Narratives | **CHANGES REQUESTED** (2026-06-19) | [report](docs/reviews/case-narratives-review.md) | BLOCK-1 (`form.set` id→narrativeTypeId) + BLOCK-2 (aria-label). |
| Case data-model (D1–D15) | **APPROVED** (2026-06-14) | [report](docs/reviews/case-model-adjustments-review.md) | 0/0. Dropped-helper cleared; 2 MINOR. |
| Cases-Extras (R1–R5) | **APPROVED** (2026-06-14) | [report](docs/reviews/cases-extras-review.md) | 0/0. RLS on 6 tables + storage; MINOR PII note. |
| 8 — Dashboards | **APPROVED** (2026-06-14) | [report](docs/reviews/phase-8-review.md) | 0/0. in_progress-answers invariant holds; MINOR-1/2 resolved in-phase. |
| 7 — Multi-Phase Cases | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-7-review.md) | 0/0. Isolation invariant confirmed; HC0xx remap complete. |
| 6 — Sign-offs | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-6-review.md) | P6-001 BLOCKER resolved in-phase; signoff definer rewrite verified. |
| 5 — Wizard | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-5-review.md) | 0/0. MINOR-1/2 → Phase 6. |
| 4 — Form Builder | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-4-review.md) | MAJOR-1 keyboard test + 2 MINOR resolved. |
| 3 — Admin/Users | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-3-review.md) | 2 MINOR resolved; INFO-1 carry. |
| 2 — Auth & Shell | **APPROVED** + re-review (2026-06-12) | [report](docs/reviews/phase-2-review.md) | MAJOR-1 + BLOCKER P2-002 (login race) fixed + load-verified. |
| 1 — Schema/RLS | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-1-review.md) | M8: 2 MAJOR + 3 MINOR resolved. |
| 0 — Scaffolding | **APPROVED** | [report](docs/reviews/phase-0-review.md) | MINOR lang=pt-BR; ADR 0001 note. |

## Decisions

<!-- One line per decision; full rationale in docs/decisions/ (ADR) + docs/progress/decisions-log.md -->

| Date | Decision | Ref |
| ---- | -------- | --- |
| 2026-07-02 | **Action-items fold + `visibility_scope` + case-access expiry** — scope-aware hub `SELECT` via `can_read_action_item`; fold `case_action_items` → `source_type='case'` (drop old table/RPCs; ADR 0033 D4 preserved); `case_access.expires_at`+`reason` filtered on all 6 consulters (incl. referral-PHI arm, Rule 12) | [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md) |
| 2026-07-01 | **Answer-Model v2** (planned) — uniform answer row + typed scalar cols (`value_number/date/time`) + instance-ready answer key (`group_instance_id`/`response_group_instances`); evaluator unchanged (Rule 3); plan [answer-model-v2.md](docs/plans/answer-model-v2.md) | [0045](docs/decisions/0045-answer-model-v2.md) |
| 2026-07-01 | **Forward-compat form capabilities** (planned) — `form_items.parent_item_id` + reserved hooks for repeating groups / file-signature-matrix answers / field confidentiality (structure only) + question default values | [0046](docs/decisions/0046-forward-compat-form-capabilities.md) |
| 2026-06-25 | **NSP-per-org** — bind the PQS roster + every PHI door to an organization (per-org `pqs_members`/`pqs_department`, `nsp_coordinator` grant via the role-CHECK seam, org-scoped doors + patient-index 4th surface, forbid cross-org referrals, vendor-walled erasure); lifts ADR 0041 amendment 10's interim multi-org PHI guard; split A (backend, pgTAP-gated) → B (FE + E2E) | [0042](docs/decisions/0042-nsp-per-org.md) |
| 2026-06-25 | **Multi-Tenancy** — pooled single-DB (organizations → hospitals → commissions); vendor `platform_admin` vs customer `org_admin`; RLS rewrite + 3-tier audit + multi-org PHI guard | [0041](docs/decisions/0041-multi-tenancy-organizations-hospitals.md) |
| 2026-06-20 | **PHI-remediation Round 3** — RCA-write severance (drop `is_admin` from 7 `rca_*_write`) + WS B audited free-text reads/PHI classification + WS E efficiency | plan §A4/B/E; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS C** — PHI retention & disposal (`dispose_event_phi`, one-shot HC056, constrained reason enum) | plan §C; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS A** — structured-identifier lockdown: real `pqs_members` roster, tight `can_read_event_patient`, single audited door, sever platform-admin from NSP PHI | plan §A; ADR 0030/0031 |
| 2026-06-20 | **Migration squash** → domain-partitioned dump-based baseline (replaced 86 incremental migrations; WS0) | plan §WS0 |
| 2026-06-19 | **Case Access Control** — per-case read/write ACL + attribution-driven full-case read; restrictive `can_read_case`; narrative lifecycle | [0033](docs/decisions/0033-case-access-control.md) |
| 2026-06-11 | Scaffolding & toolchain: Next 16/React 19, shadcn (radix/neutral), vitest ESM, Supabase CLI devDep, Chromium-only | [0001](docs/decisions/0001-scaffolding-and-toolchain.md) |
| 2026-06-11 | `jsdom` pinned `^25` (jsdom@27 ESM dep crashes Vitest forks on Node 20) | – |
| 2026-06-12 | Admin claim via custom access-token hook reading `profiles.is_admin`; RLS helper DB-read fallback | [0002](docs/decisions/0002-admin-claim-access-token-hook.md) |
| 2026-06-12 | DB tests via pgTAP (`npx supabase test db`) | [0003](docs/decisions/0003-pgtap-for-db-tests.md) |
| 2026-06-12 | Sign-off enforcement gated by an `app.feature_flags` row read by `submit_response` | [0004](docs/decisions/0004-signoff-feature-flag.md) |
| 2026-06-12 | `visible_when` v1 = single condition (no AND/OR), CHECK-enforced shape + publish-time validation | [0005](docs/decisions/0005-visible-when-shape.md) |
| 2026-06-12 | Keep legacy env-var names while accepting new publishable/secret CLI keys | [0006](docs/decisions/0006-supabase-api-key-naming.md) |
| 2026-06-12 | Phase 2: `multi@test.local` as staff of both ccih+farmacia (commission-picker E2E); seed 8 users/8 memberships | – |
| 2026-06-12 | Middleware is a coarse auth gate; role-aware landing deferred to root `/` Server Component | [0007](docs/decisions/0007-middleware-coarse-gate-root-landing.md) |
| 2026-06-12 | GSAP 3.15.0 pinned for the auth-hero animation (dynamic import, aria-hidden, reduced-motion) | [0008](docs/decisions/0008-gsap-animation-dependency.md) |
| 2026-06-12 | Auth identity on the request hot path via LOCAL JWT verification (`getClaims()`, ES256/JWKS) | [0009](docs/decisions/0009-jwt-local-verification-gate.md) |
| 2026-06-12 | Phase 3: denormalize `email` (nullable citext) onto `public.profiles` (trigger-populated) | [0010](docs/decisions/0010-denormalize-email-on-profiles.md) |
| 2026-06-12 | Phase 4: position reorder via DEFERRABLE INITIALLY IMMEDIATE unique constraints + CASE swap | [0011](docs/decisions/0011-position-reorder-deferrable-swap.md) |
| 2026-06-12 | Phase 4: `clone_form_version` returns the existing draft when one exists (idempotent edit) | [0012](docs/decisions/0012-clone-returns-existing-draft.md) |
| 2026-06-12 | Phase 4: fixed latent Phase-1 RLS defect in `form_versions_staff_admin_write` WITH CHECK | [0013](docs/decisions/0013-form-versions-insert-rls-fix.md) |
| 2026-06-12 | Phase 5: response-fill via two security-invoker RPCs (`save_section_answers`, `start_or_resume_response`) | [0015](docs/decisions/0015-response-fill-rpcs.md) |
| 2026-06-13 | Phase 7 (P7-002): custom SQLSTATE class `P00xx` → `HC0xx` (HC010–HC022) | [0018](docs/decisions/0018-custom-sqlstate-class.md) |
| 2026-06-13 | Phase 7: Multi-phase cases — 4 new tables (process_templates/phases; cases/case_phases) | [0017](docs/decisions/0017-multi-phase-cases.md) |
| 2026-06-13 | Maintenance: the default (anchor) section may carry a title once a form has ≥2 sections | [0019](docs/decisions/0019-default-section-may-carry-title.md) |
| 2026-06-13 | Phase 8: dashboard-countable responses exclude case-phase responses (`case_phase_id IS NULL`) | [0020](docs/decisions/0020-dashboard-countable-responses.md) |
| 2026-06-14 | Cases-Extras R2: configurable per-committee case status (no longer fixed 3-state CHECK) | [0023](docs/decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Cases-Extras R1 documents+events, R3 tags, R4 action items | [0023](docs/decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Post-Phase-8: due dates for case phases (`default_due_days` + `case_phases.default_due_*`) | [0021](docs/decisions/0021-phase-due-dates.md) |
| 2026-06-14 | Case data-model adjustments — phase blocking, FIXED statuses, outcomes (supersedes R2) | plan `the-current-case-data-bright-hartmanis.md` |
| 2026-06-13 | Phase 6: staff_admin sign-off read via two narrow security-definer RPCs (`list_signoff_queue`, `get_response_for_signoff`) | [0016](docs/decisions/0016-signoff-definer-read-path.md) |
| 2026-06-16 | Phase 12: Case Timeline (read-only viz; no migration, reuses table RLS) | [0027](docs/decisions/0027-case-timeline.md) |
| 2026-06-15 | Phase 10: Meetings (B1–B5) — 8 migrations; 6-state lifecycle | [0025](docs/decisions/0025-meetings.md) |
| 2026-06-18 | Phase 14 batch: build 14b (Triage) → 14c (RCA) → 14d (CAPA) in dependency order as one build | accred-track §14b–14d |

## Follow-ups / Deferred Items

<!-- OPEN backlog only (reviewed at each phase start). Resolved [x] items archived →
     docs/progress/follow-ups-archive.md (full snapshot). -->

- [ ] **Action-items hub — deferred satellites (partner-handoff Phases 2–4; ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md)).** Activity feed (`action_item_updates`), evidence, scheduled follow-ups, formal reviews, checklist items, dependencies, per-committee custom fields, per-commission status/urgency **management UI** (the tables are configurable already; only global defaults are seeded + only the 4 global keys are surfaced), reminder/escalation rules, and effectiveness checks. Adopt selectively when real committee workflows demand them — the hub-and-spoke schema already accommodates each as an additive satellite (no core-table change), and status gating flags (`requires_comment`/`requires_evidence`/`requires_review`) become meaningful once evidence/review land. Owned by lead (scheduling) + backend/frontend when picked up.
- [ ] **Action-items case cross-link UI + `visibility_scope` toggle for meeting/manual items (deferred from ADR 0050 F1).** The backend supports a `case_id` cross-link on meeting/manual hub items and the `committee_*` RPCs accept `p_visibility_scope` (default `case_restricted` when cross-linked, coordinator override to `committee`), but **no UI creates a case-linked meeting/manual item** — `src/components/meetings/action-item-form.tsx:102` hard-codes `caseId` and comments "not exposed in this form for v1". So the "Visível para toda a comissão" toggle (Q4) has nothing to render against yet. When a case-linking affordance is added to the meeting/manual item form, add the toggle beside it (shown only when a case is linked, default off = restricted). Latent capability, like `assignees_only`. Owned by frontend.
- [ ] **Break-glass access (logged, reasoned, time-boxed emergency access to restricted cases / PHI).** Target Phase 20 (Notifications & Escalation). A dedicated `break_glass_access_events` record + a temporary-grant path that requires a reason, is always audited, notifies privacy/security, and appears in audit reports — NOT platform-admin-sees-everything. Partner handoff §19 as the reference model; composes with `case_access` expiry (ADR 0050) and the audited-single-door PHI posture (Rule 12). Owned by lead (scheduling) + backend.
- [ ] **Pre-existing uncommitted working-tree edits present at 2026-07-02 session start (NOT from the Meus-itens/Visão-Geral feature) — human to confirm intent.** Two files were already `M` before this feature's work: (a) `supabase/config.toml` — uncommented `[auth.email.smtp]` and pointed it at **Resend** (`smtp.resend.com`, `pass=env(RESEND_API_KEY)`) instead of local Mailpit; with `RESEND_API_KEY` unset locally this breaks the local invite-email flow and is the sole cause of the 4 `user-registration` E2E failures (registration action errors on SMTP send; invite emails bypass Mailpit). Likely intended for prod email setup (pairs with the Phase-9 template follow-up below) but blocks local E2E. (b) `src/app/page.tsx` — the NoAccess "Entrar com outra conta" link became a `signOut` form (a plain `/login` link is bounced by the proxy `AUTHED_REDIRECT_AWAY`). Neither was touched by this feature; left as-is pending human decision (revert for local testing vs keep for deploy).
- [ ] **User Registration — Phase-9 email-template deploy dependency (feature COMPLETE; deploy-time task).** The pt-BR invite + recovery templates (`supabase/templates/{invite,recovery}.html`) must be pasted into Supabase **Dashboard → Auth → Email Templates** for Cloud (self-hosted `config.toml` templates don't auto-apply), preserving the `{{ .TokenHash }}` + `?type=invite|recovery` link shape — alongside the already-flagged custom SMTP. Until then invite/recovery links work locally but NOT in prod. Owned by backend at deploy. See [user-registration.md](docs/progress/user-registration.md).
- [ ] **Answer-Model v2 — PLANNED, not started (2026-07-01; ADRs [0045](docs/decisions/0045-answer-model-v2.md)/[0046](docs/decisions/0046-forward-compat-form-capabilities.md), plan [docs/plans/answer-model-v2.md](docs/plans/answer-model-v2.md)).** Pre-launch schema-shape hardening from the 2026-07-01 forms data-model review: uniform answer row (choice items get a parent `answers` row; `answer_selected_options` → `answer_id`), typed scalar columns (`value_number/date/time`, trigger-derived; `value` stays the canonical evaluator input), instance-ready answer key (`answers.group_instance_id` + `response_group_instances` + `form_items.parent_item_id`) as **scaffolding only — NO repeating-group or new-answer-block UX**, `answered_at` + reserved `confidentiality_level`, and question **default values**. Evaluator byte-for-byte unchanged (Rule 3); additive forward-only migrations, no flag, user-run remote `db push`. Run as a mini-phase (§6), full plan review on the answer migrations; at implementation update ARCHITECTURE.md Rule 2 + docs/backend-state.md. Owned by `backend` (+ `frontend` for the defaults UI).
- [ ] **Broader org-member-role-management UI (multi-tenancy gap; surfaced building NSP-per-org B3).** NSP-per-org added a *focused* `/o/[org]/manage/equipe-nsp` that toggles only the `nsp_coordinator` role, but there is **no general org-member management UI** — an `org_admin` cannot add/remove other `org_admin`s or manage org membership through the app (only the seed + direct DB). Build a proper `/o/[org]/manage/membros` surface (+ `organization_members` role CRUD actions). Owned by frontend + backend.
- [ ] **NSP appoint-picker: annotate/exclude current org_admins (minor UX, NSP-per-org B3).** The appoint picker lists all org users incl. `org_admin`s; selecting one returns the "já é administrador" refusal (the DB guard is the safety). Cleaner: disable/annotate current org_admins in the picker. Owned by frontend.
- [ ] **`appointNspCoordinator` TOCTOU hardening (optional, NSP-per-org B3).** The SELECT-role-then-upsert has a negligible race (admin-only, deliberate, low-frequency); a DB-level guard (trigger / partial constraint forbidding a coordinator-upsert over an `org_admin` row) would close it fully — over-engineering for now. Owned by backend.
- [ ] **Multi-tenancy — org_admin TS-gate gap in the invoker `authorize*` helpers (QA INFO / lead #15, non-blocking).** ~8 invoker-context `authorize*` server-action helpers gate on `is_staff_admin_of` / `isAdmin` but do **not** yet grant the org_admin → coordinator branch that `getCommissionAccessByOrg` grants on the read path. RLS is the security backstop (these are invoker-context, not service-role, so the worst case is an org_admin being *denied* a write they should be allowed, not an escalation), but the gates should be aligned with the read path for consistency. Enumerate + add the `is_org_admin_of_commission` term.
- [ ] **Pre-existing full-serial-suite contamination (NOT a form-builder regression).** A single full serial run is RED on `main` itself (~17–19 failures; branch ≤ baseline, 0 net new — proven by failing-title diff). Cross-spec seed-mutation in lexical run order (phase10–14/22 before phase2–8). Separate spec-isolation effort (phase13-saga class); the team's green path is chunked runs with a fresh reset per chunk. Tracked, does not gate any single feature.
- [ ] **Harden `e2e/form-builder-enhancements.spec.ts` to a throwaway commission (QA INFO-4, nice-to-have).** It builds fixtures in the seeded CCIH commission (`COMM_A`/`/c/ccih`) rather than a probe commission/users (cf. P13-005/006 lessons). Acceptable now (branch added 0 net contamination); harden (use `makeProbeCommission`/`makeProbeUser`) if it joins the full gate matrix.
- [ ] **`dispose_referral_phi` — LGPD-erasure parity for the referral PHI module (backend; surfaced 2026-06-22 building `case_patient`; ADR 0038 Consequences).** The referral module (Phase 22, ADR 0037) ships `referral_patient` + PHI free-text but, unlike NSP (`dispose_event_phi`) and Cases (`dispose_case_phi`), has **no** LGPD Art. 18 erasure RPC. Add `dispose_referral_phi(referral, reason)` modeled on `dispose_event_phi`/`dispose_case_phi`: delete `referral_patient`, redact `description_md`/`decline_note`/`frozen_body_md`/`result_md`, preserve skeleton + audit chain, one-shot, constrained reason enum, `phi_disposed_*` on `case_referral`, emit `referral_patient.disposed`, source/target-coord-or-admin gate, + pgTAP. (Spawn chip `task_e51cc87d` was filed; chips don't persist across restarts — THIS is the durable record.)
- [ ] **`case_patient` disposal UI — "Descartar dados do paciente" (frontend, not blocking; mirrors the NSP WS C item below).** `dispose_case_phi` + the `disposeCasePhi(caseId, reason)` action are live; the UI is a coordinator/admin-gated button on the case detail opening a confirm dialog with a reason-category `<select>` bound to `PhiDisposeReason`/`CASE_PHI_DISPOSE_REASON_LABELS` (NO free-text — constrained category), reflecting `has_patient=false` + the `phi_disposed_*` stamp post-action. One-shot (HC056).
- [ ] **WS A FE — PQS-membership management UI (frontend, not blocking).** `pqs_members` is admin-managed via `add/remove/list_pqs_members` RPCs + a seeded admin (functional/testable now). A roster-management screen under `/admin` (enroll/remove PQS staff, list members) is a frontend task; in prod the first admin enrolls staff via `add_pqs_member`. Mirror the `assignStaffAdmin` admin-action pattern (`requireAdmin` + admin client) for the server actions.
- [ ] **WS A FE — `/admin/nsp` gating + patient-panel affordance (frontend/tester, surfaced by WS A trace 2026-06-20).** (a) `/admin/nsp/[eventId]/page.tsx` gates ONLY on `context.isAdmin`. Post-WS-A a **non-PQS admin** degrades to `notFound()` (clean pt-BR 404 — `getSafetyEvent`'s `patient_safety_event` SELECT is now `can_read_event`, which denies a non-PQS/non-custodian admin → null → 404), NOT a crash/broken-empty page. Ideal: gate the NSP routes on `is_pqs_member` (e.g. via `patient_safety_enabled()` + a PQS check / a new `public.is_pqs_member()` read) for a tailored "não autorizado" instead of a generic 404. (b) The patient panel renders on `event.hasPatient` ALONE; entitlement is enforced at the data layer (the `get_event_patient` RPC returns null for an unentitled caller → `<PatientPanelEmpty>`), so **no PHI leaks**, but the affordance ideally gates on `hasPatient` AND entitlement to avoid showing an empty panel to an entitled-event-but-unentitled-PHI viewer. **No reporter-facing route renders the panel** (verified: `/c/[slug]/eventos` is governance-only, PHI-free; the panel is admin-route-exclusive) — so a reporter, incl. after custody handoff, never reaches it. Flagged per coordinator; NOT fixed in WS A.
- [ ] **WS E / M2 — per-vocabulary reorder/archive RPC consolidation DEFERRED (backend, 2026-06-20).** The reorder/archive RPCs for `case_tags`/`case_outcomes`/`case_narrative_types` (commission-scoped, `assert_*_enabled`+`is_staff_admin_of`) vs `pqs_event_types`/`pqs_sentinel_criteria` (GLOBAL, `assert_patient_safety_enabled`+`is_pqs_member`, two-step negative-offset against a deferrable position unique) diverge by gate/flag/scope/collision-strategy/pt-BR message. A shared `app.reorder_vocab(table,…)` helper would need table-name-interpolated dynamic SQL (injection surface + allow-list) and couldn't encode the per-table divergence — less auditable than the current explicit static RPCs. Revisit only if the vocab set grows materially. No code change.
- [ ] **WS B — authoritative PHI-bearing free-text column list (for ARCHITECTURE.md Rule 11/12 + ADR alignment, lead-owned). FINAL count = 22 columns** (18 + the 4 borderline, lead ruled CLASSIFY 2026-06-20). Backend applied SQL column COMMENTs (`'PHI-BEARING free text (WS B; Rule 11/12)…'`) to: `patient_safety_event.description_md`, `event_triage.disposition_notes_md`, `rca.{what_md,expected_md,summary_md,impact,scope}`, `rca_factors.text`, `rca_root_causes.text`, `rca_timeline_entries.description`, `capa_plan.lessons_learned_md`, `capa_effectiveness.method_md`, `capa_action_task.description`, `capa_measure_result.note`, `meetings.minutes_md`, `case_interviews.summary_md`, `case_narratives.body_md`, `case_events.body` **(the original 18)** + **`meeting_agenda_items.{description,discussion_notes,resolution}` + `case_interview_subjects.note` (the +4 addendum** — all are multi-line textareas; agenda free-text already read-audited via `meeting.viewed` on `getMeetingDetail`/`listMeetingAgenda`, subject note via `interview.viewed` on `getInterviewDetail`/`listInterviewSubjects`; no new audit emit needed). **EXCLUDED** (governance metadata, PHI-free by the title invariant): all `*.title` + `case_interview_subjects.clinical_role`. Comment-only; `db diff` clean; types unchanged. Lead aligns ARCHITECTURE.md/ADRs from this 22-column list (backend left those docs untouched per instruction).
- [ ] **WS C FE — "Descartar dados do paciente" disposal UI (frontend, not blocking).** The `disposeEventPhi(eventId, reason)` server action + the `dispose_event_phi` RPC are live and E2E-testable; the UI is a frontend task: an admin/PQS-gated button on the NSP event detail (`/admin/nsp/[eventId]`) opening a confirm dialog with a **reason-category `<select>`** bound to `PhiDisposeReason` / `PHI_DISPOSE_REASON_LABELS` (NO free-text field — the reason is a constrained category), a destructive-action confirm, and post-action it should reflect `has_patient=false` (panel gone) + show the `phi_disposed_at/by/reason` stamp ("dados descartados em … por … — motivo: …"). Disposal is one-shot (HC056 → "PHI já descartada"). Owned by `frontend`.
- [ ] **WS B/C FE — discourage PHI in `*.title` / structured short fields (frontend, not blocking; surfaced 2026-06-20).** The minimum-necessary invariant assumes titles stay PHI-free (they ride on queue/list paths). Add helper text / a soft validation note ("Não inclua dados do paciente.") to the title inputs on event/RCA/CAPA/meeting/interview/case forms (mirrors the existing case-action-item dialog note). Soft guidance only — no hard block. Owned by `frontend`.
- [ ] **Migration squash — REMOTE rollout is a full reset, owner-run (Workstream 0, 2026-06-20).** The 12-file consolidated baseline (`20260620001000`–`012000`) replaces the entire 86-migration history; it was verified ONLY against local `supabase db reset` (db diff "no changes", pgTAP 619/619, types byte-identical, personas green). It is **forward-only and additive going forward**, but adopting it on the linked remote means the remote's `supabase_migrations.schema_migrations` ledger no longer matches — the remote must be reset to this baseline (`supabase db reset --linked` is destructive; the project is pre-live per the plan). **Auto-blocked for background agents** (remote pushes need the owner). The PHI-remediation Workstreams A–E (the actual security fixes) build ON this baseline and ship together; do the remote reset when that round lands, not before. Supersedes the per-increment `…092xxx`/`…093xxx`/`…110xxx` remote-push deferrals below (those migration files no longer exist — folded into the baseline).
- [ ] **Case Access — `case_interviews` still member-read (ADR 0033 D2 fast-follow; QA INFO-N1).** The restrictive boundary tightened cases + child tables to `can_read_case`, but `case_interviews` kept its existing `commission_of_interview` member-read to bound blast radius. Case-scope it to `can_read_case` to fully close the boundary. Backend-owned.
- [~] **Case Access — `listCaseAccess(caseId)` read for the access panel (QA INFO-N3).** BEING ADDRESSED by the action-items-fold + case-access-expiry work (2026-07-02, ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md), tasks B3/F2): `list_case_access` now returns `(user_id, level, granted_at, expires_at, reason)` and the rebuilt access panel wires it. Close when that feature passes its gate.
- [ ] **Case Access — push migrations `…110000–110004` (+ the in-place CA-001 edit to `…110002`) to the linked REMOTE** (`supabase db push --linked`) when taking the feature live. Verified ONLY against local `supabase db reset` (pgTAP 619/619, seed personas). **Auto-blocked as a production deploy — needs explicit human/lead go-ahead** (mirrors the Cases-Extras / case-model remote-push deferrals above).
- [ ] **E2E regression suite is NOT reliably green against a PROD build (test-harness debt, surfaced 2026-06-18; NOT a Phase-14 defect).** The freeze-proof gate now requires `next build`+`next start` (the heavy NSP pages crawl + balloon the `next dev` server to 4.3 GB — see user MEMORY `e2e-gate-prod-build`). But the pre-≤13 specs were authored against `next dev` and flake against the prod build: (a) Radix dialog close-animations (`data-[state=closed]:animate-out`) race tight `toBeHidden`/`toHaveCount` timeouts because — unlike the Phase-14 specs — the older specs DON'T set `reducedMotion: 'reduce'`; (b) the suite shares one mutable DB with no per-test reset, so Playwright `retries` (and parallel workers) CASCADE write-pollution: retries=2 produced MORE hard failures (25) than retries=0 (14). **Phase-14 specs are clean (65/65).** Evidence (2026-06-18, prod build, LOCAL Docker): full suite workers=1/retries=0 → 246 pass / 14 fail; non-14 specs workers=4/retries=2 → 162/13 flaky/20 fail; non-14 specs workers=1/retries=2 → 167/3 flaky/25 fail. Every failure is a pre-14 spec. **Fixes (test-infra; `tester` + `backend` for config):** add `use: { reducedMotion: 'reduce' }` to `playwright.config.ts` (one line, stabilizes animation timing globally); point `webServer.command` at a prod build for the gate; give the older mutation specs DB isolation (unique per-test fixtures or reset-per-file). Until then, the older specs' "green" depends on the `next dev` model.
- [ ] **E2E `case-narratives` AC-1b spec-isolation (tester-owned; surfaced 2026-06-19 during the case-access refinement triage).** AC-1b renames the "Resumo Clínico" narrative TYPE to "…(Renomeado) {timestamp}" and never restores it, so on the shared DB a re-run/later-ordered AC-1 (which asserts the original label) fails. NOT a code regression — pre-existing test debt, an instance of the no-per-test-DB-isolation problem in the item above. Fix: AC-1b restores the original label in a teardown/`finally` (or uses a throwaway type). Until then `case-narratives` AC-1 is order/state-dependent.
- [ ] **Phase 14a deferred (QA re-verify INFO):** sweep the success string out of `ActionState.error` for the 4 remaining safety actions (`transferEventCustody` / `updateEvent` / `setEventPatient` / `cancelEvent`) into the new `message` field — harmless today (all consumers gate on `!result.ok`), do it on the next `src/lib/safety/actions.ts` touch (e.g. Phase 14b). Backend-owned. The 2 flagged in QA N1/I2 (`notifySafetyEvent`/`acknowledgeEvent`) are already done.
- [ ] **Interviews — "Minhas entrevistas" discovery surface for plain-`staff` interviewers (Phase 11, deferred per lead).** v1 has NO dedicated list for a plain-`staff` registered interviewer to find interviews they may write — they reach the detail by DIRECT LINK only (the case-detail "Entrevistas" panel is coordinator-gated). The interview detail page renders correctly for them (membership guard + `viewerCanWrite` controls), and the detail header back-link points non-coordinators at the commission home (`/c/[slug]`), never the coordinator case page. A future "Minhas entrevistas" surface (mirroring "Minhas fases") would close the discovery gap. Owned by `frontend` when scheduled.
- [ ] **Case data-model — D12 status-file DELETIONS deferred to land WITH frontend §H (backend tasks #2/#3 handoff).** Deleting the configurable-status modules now would red-line ~12 not-yet-reworked frontend consumers (kanban drag, the "Estado" picker, the status manager, `case-derive`, the badges, the cases/detail/settings pages), which is the §H rework owned by `frontend` — so to hand over a GREEN `lint`+`typecheck` baseline without touching `src/components/**`/`src/app/**`, backend INTERIM-SHIMMED the two backend D12 lib modules instead of deleting them:
  - `src/lib/queries/case-statuses.ts` → now a deprecated module: re-exports `CaseStatusColorToken` from the new home and **`listCaseStatusDefs` synthesizes the 5 FIXED statuses** into the legacy `CaseStatusDef` shape (NO DB read), `caseStatusIsTerminal` delegates to the fixed helper. So the un-reworked board/table/badge/lifecycle render the CORRECT fixed statuses live during §H.
  - `src/lib/cases/status-actions.ts` → deprecated INERT no-ops (`setCaseStatus`/`create/update/reorder/archive_CaseStatus` return a pt-BR "estado é automático"); their RPCs are gone.
  - **TO DELETE during/after §H (frontend removes the importers, then backend removes the files):** `src/lib/queries/case-statuses.ts`, `src/lib/cases/status-actions.ts`, `src/components/cases/status-manager.tsx`, `src/components/cases/status-def-dialog.tsx`, `src/app/c/[slug]/manage/settings/statuses/page.tsx`. **KEEP** `src/components/cases/color-token-picker.tsx` (shared by tags/outcomes; its `CaseStatusColorToken` import should be re-pointed to `@/lib/cases/case-status` during §H). The new single source of truth is **`src/lib/cases/case-status.ts`** (`CaseStatus`/`CASE_STATUSES`/`CASE_STATUS_META`/`isTerminalCaseStatus`/`CaseStatusColorToken`).
  - **§H consumer rework owned by frontend** (plan §H): kanban → 5 fixed read-only columns (drop drag/`setCaseStatus`); table/view chips → fixed statuses + outcome/adverse filters; `case-lifecycle-actions` → drop the "Estado" picker, "Concluir" (outcome-capture dialog → `setCaseOutcome` then `closeCase`) + "Cancelar"; `case-derive` → import the fixed model; blocked-phase rendering ("Bloqueada por Fase N"); process outcome picker + case outcome selector + the `desfechos` settings manager + dashboard % adverse breakdown; `settings-tabs` drop "Estados" add "Desfechos". Backend posted the contracts (task #1) + impls (task #3): `case-outcomes.ts`, `outcomes-actions.ts`, `setTemplatePhaseBlocks`.
  - **✅ FRONTEND §H DONE 2026-06-14 (frontend, task #5).** All consumers reworked off the configurable-status modules onto `@/lib/cases/case-status`; the 3 frontend-owned D12 files DELETED (`status-manager.tsx`, `status-def-dialog.tsx`, `settings/statuses/page.tsx` + its now-empty route dir). **The 2 backend-owned shims (`src/lib/queries/case-statuses.ts`, `src/lib/cases/status-actions.ts`) are now ORPHANED — zero `src/app`/`src/components` importers remain (verified by grep; the only residual references are inside the two shim files themselves + the deprecated `CaseStatusKey` alias in `src/lib/queries/cases.ts`, all backend-owned). Backend can delete the 2 shims (and drop the `CaseStatusKey` alias) in lockstep.** Built: `case-status-badge.tsx` (fixed-key `CaseStatusBadgeFixed` + still owns `TOKEN_STYLES`/`TOKEN_COLOR_VAR`); `case-derive.ts` (`groupByFixedStatus`, `activePhases` for A5, `blockedBy` D4, `computeOutcomeBreakdown` D14, `computeCaseKpis(rows)` rebased on `isTerminalCaseStatus`); `cases-kanban.tsx` (5 fixed read-only link-columns, outcome chip + "N fases ativas"); `cases-table.tsx` (Desfecho column, fixed status badge/sort, A5); `cases-view.tsx` (fixed status chips + outcome `<select>` + "Apenas adversos" toggle, all client-side over board rows); `cases-kpi-strip.tsx` (outcome breakdown panel: per-outcome counts + % adverse); `case-lifecycle-actions.tsx` (Concluir dialog requires an outcome when offered → `setCaseOutcome` then `closeCase`; Cancelar confirm → `cancelCase`); NEW `case-outcome-selector.tsx` (non-terminal + D15-hidden when no offered outcomes; advisory markers); `coordinator-phase-actions.tsx` (blocked "Ativar e atribuir" disabled + "Bloqueada por Fase N" via `blockedBy`); NEW `outcome-manager.tsx` + `outcome-def-dialog.tsx` + `settings/desfechos/page.tsx` (mirror tag-manager; reorder via Flip; the requires-action-plan/is-adverse toggles); `settings-tabs.tsx` drops "Estados" adds "Desfechos"; settings index redirect → `desfechos`; NEW `phase-blocks-editor.tsx` + `process-outcomes-picker.tsx` wired into `phase-slot-dialog.tsx` / `template-builder-shell.tsx` (draft-only; blockers persisted via `setTemplatePhaseBlocks` chained off the slot save; offered outcomes via `setProcessOutcomes` + inline create); `phase-slot-card.tsx` shows "Bloqueada por: Fase N"; `layout.tsx` open-cases sidebar count → `isTerminalCaseStatus`; case-detail page off `case-statuses`. **Verified:** `npm run typecheck` + `npm run lint` + `npm run test` (24/24) + `next build` ALL GREEN; build route map confirms `/settings/desfechos` added and `/settings/statuses` gone. Client/server boundary sound — every `@/lib/queries/*` import in client components is `import type` (no `next/headers` leak). Local DB probe confirms `case_outcomes` table present + `case_status_defs` dropped (093000–093003 applied locally), so the running dev server HMR-compiled all changed pages without error (all 307 auth-redirects, no 500s). Live click-through of Radix dialogs/menus left to the tester (trusted events) — same env limitation noted in prior waves; could not start an own preview server (Next single-instance lock held by the tester's running :3000 dev server — did not disturb it). **NOTE for backend:** no missing envelope fields found — `Case.outcomeId`, `CasePhase.blocks`, `CaseDetail.outcome`/`offeredOutcomes`, board-row `outcome`, and all outcome/blocker actions matched the posted contracts exactly; built against them directly with no provisional shapes.
- [ ] **Case data-model — push migrations `…093000`–`…093003` (all 4) to the linked REMOTE** (`supabase db push --linked`) at the verification/tester stage. Backend verified ONLY against a local `supabase db reset` (4 migrations apply clean; functional probe confirmed the conclude gate HC031, terminal-first close, recompute → `pendente`; types regenerated; lint+typecheck+unit green). The defensive `update cases set status='nao_iniciado' where status not in (5 keys)` in `…093001` lets the additive CHECK apply cleanly on the drifted remote. **Auto-blocked as a production deploy — needs explicit human/lead go-ahead** (lead is coordinating the remote push at the tester wave). pgTAP for this batch is task #4 (tester-owned specs; backend owns the enabling migrations + seed enrichment).
- [ ] **Cases-Extras — push migrations `…092000`–`…092006` (all 7) to the linked REMOTE** (`supabase db push --linked`). Backend verified ONLY against a local `supabase db reset` (191/191 pgTAP green, types regenerated); the remote drifts from local (user MEMORY) and the tester's remote runs need these. `db push` was auto-blocked as an unapproved production deploy — **needs explicit human/lead go-ahead** before the tester hits remote. (Lead is making the remote-vs-local test-gate call before the tester wave.)
- [ ] **Phase 8 deploy checklist — production Supabase Cloud MUST use asymmetric (ES256/RS256) JWT signing keys** (Phase 2 QA re-review, ADR 0009). Otherwise `getClaims()` silently falls back to a per-request `getUser()` GoTrue round trip, re-introducing the P2-002 post-login race in production (behavioral regression, not a security hole — tampered tokens still fail closed). Add a testable verification step.
