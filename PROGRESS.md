# PROGRESS.md — Project Status Tracker

> Single source of truth for project status. Update IMMEDIATELY when state
> changes. The lead owns the Phase Status table; each teammate owns their own
> rows in the other sections. Never report status that isn't written here.

## Phase Status

| Phase | Name                          | Status | Build | Tests | QA | Human ✓ | Completed | Commit |
| ----- | ----------------------------- | ------ | ----- | ----- | -- | ------- | --------- | ------ |
| 0     | Scaffolding & Environment     | ✅ complete | ✅ | ✅ 5/5 | ✅ APPROVED | ✅ 2026-06-11 | 2026-06-11 | `d64281e` |
| 1     | Schema, Auth & RLS            | ✅ complete | ✅ | ✅ 88/88 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `691662f` |
| 2     | Authentication & App Shell    | ✅ complete | ✅ | ✅ 49/49 + load | ✅ APPROVED + re-review | ✅ 2026-06-12 | 2026-06-12 | `5773b4a` |
| 3     | Admin Area & User Management  | ✅ complete | ✅ | ✅ 43/43 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `cb28ef3` |
| 4     | Form Builder & Versioning     | ✅ complete | ✅ | ✅ 8/8 | ✅ APPROVED | ✅ 2026-06-12 | 2026-06-12 | `d32e7e9` |
| 5     | Wizard Filling, Conditional Sections & Resume | ✅ complete | ✅ | ✅ 63/63 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `8418991` |
| 6     | Section Sign-offs & Submission Lifecycle | ✅ complete | ✅ | ✅ 70/70 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `94566f2` |
| 7     | Multi-Phase Cases             | ✅ complete | ✅ | ✅ 81/81 | ✅ APPROVED | ✅ 2026-06-13 | 2026-06-13 | `28e0405` |
| 8     | Dashboards & Submissions Browser | ✅ complete | ✅ | ✅ 106/106 | ✅ APPROVED | ✅ 2026-06-14 | 2026-06-14 | `a50e0e0` |
| 9     | Deployment                    | 🔜 not started | – | – | – | – | – | – |
| 10    | Meetings                      | ✅ complete | ✅ | ✅ 141/141 | ✅ APPROVED | ✅ 2026-06-15 | 2026-06-15 | `5e2780c` |
| 11    | Interviews                    | ✅ complete | ✅ | ✅ 152/152 | ✅ APPROVED | ✅ 2026-06-15 | 2026-06-15 | `3d7376b` |
| 12    | Case Timeline                 | ✅ complete | ✅ | ✅ 169/169 | ✅ APPROVED | ✅ 2026-06-16 | 2026-06-16 | `0feaa9a` |
| 13    | Audit Trail                   | ✅ complete | ✅ | ✅ 195/195 | ✅ APPROVED | ✅ 2026-06-18 | 2026-06-18 | `a8739b5` |
| 14    | Patient-Safety / NSP (Triage, RCA & CAPA, 14a–14d) | ✅ complete | ✅ | ✅ E2E 65/65 + pgTAP 516 | ✅ APPROVED | ✅ 2026-06-18 | 2026-06-18 | 14a `984e787` · 14b–d `c4e20b3` |
| 14e   | **Centralized Attachment Substrate** *(supersedes the narrow "Attachment PHI Classification" plan)* — polymorphic `attachments` core + physically-tiered PHI buckets + hard audited PHI-read door (service-role signed); folds in case/meeting/interview docs, adds action-item attachments, reserves form-item uploads (design-only ingress contract). **Now = phase F2 of the [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md) — built AFTER F1 (participants), because `attachment_subjects` re-keys to the `participants` registry (§1 C-β).** ADR [0063](docs/decisions/0063-centralized-attachments-substrate.md) (design accepted, refines 14e; not implemented). Plans: [phase-14e](docs/phases/phase-14e-attachment-phi-classification.md) · [foundations-program](docs/plans/pre-pilot-foundations-program.md) · durable → [f2-attachments.md](docs/progress/f2-attachments.md). | ✅ **complete (F2)** | ✅ | ✅ pgTAP **1957** · F2 E2E **24/24** (audited PHI door proven) · full **590p/24f** (F2-owned 14/14 green; 0 F2 reg, ≈ F1 baseline) | ✅ **APPROVED** (0B/0M/3m/4i, all closed) [review](docs/reviews/phase-F2-review.md) | ✅ 2026-07-11 | 2026-07-11 | `37057c0` local · **remote ✓ 2026-07-12 (pilot reset)** |
| 15    | Quality Indicators *(built 1st of 15 → 17 → 16 — ADR [0057](docs/decisions/0057-indicators-doc-control-replan.md))* | ✅ complete | ✅ | ✅ 12/12 (prod-standalone) | ✅ APPROVED (3 MINOR fixed) [review](docs/reviews/phase-15-review.md) | ✅ 2026-07-06 | 2026-07-06 | merge `26187dc` (pushed; **remote deployed via pilot reset ✓ 2026-07-12**) |
| 16    | Standards Crosswalk & Readiness *(builds 3rd, after 17; pilot follows)* | 🔜 not started | – | – | – | – | – | – |
| 17    | Controlled-Document Lifecycle *(pre-pilot — builds 2nd, before 16; ADR 0057)* | ✅ complete | ✅ (tsc/lint 0 · Vitest 206) | ✅ pgTAP 47/47 (full 1717) · phase E2E 14/14 · full regr 588p/10 env-only (0 Phase-17 reg) | ✅ APPROVED (3 MINOR cleared) [review](docs/reviews/phase-17-review.md) | ✅ 2026-07-06 | 2026-07-06 | merge `1152d75` (ff-only from `feat/phase-17-controlled-documents`; pushed) |
| 18    | Self-Assessment & Internal Audit | 🔜 not started | – | – | – | – | – | – |
| 19    | Surveyor Access & Evidence Export | 🔜 not started | – | – | – | – | – | – |
| 20    | Notifications & Escalation *(**pulled pre-pilot** 2026-07-12 — ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md))* | 🔜 not started (pre-pilot) | – | – | – | – | – | – |
| 21    | Committee Charters & Cadence *(**pulled pre-pilot** 2026-07-12 — ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md))* | 🔜 not started (pre-pilot) | – | – | – | – | – | – |
| 22    | Inter-Committee Case Referrals | ✅ complete | ✅ | ✅ 29/29 + full 276/326 | ✅ APPROVED 2026-06-21 | ✅ 2026-06-21 | 2026-06-21 | `768b9f1` |
| 23    | Patient Identity & Cross-Committee Linkage (MRN/encounter) | ✅ complete | ✅ | ✅ E2E 15/15 + pgTAP 10/10 sweep | ✅ APPROVED 2026-06-22 | ✅ 2026-06-22 | 2026-06-22 | `da4d127` |
| MT    | **Multi-Tenancy** — organizations → hospitals → commissions; `platform_admin` vs `org_admin`; RLS rewrite + 3-tier audit + multi-org PHI guard. ADR [0041](docs/decisions/0041-multi-tenancy-organizations-hospitals.md). | ✅ complete | ✅ | ✅ pgTAP 1029 + E2E 292/0 | ✅ APPROVED 2026-06-25 [review](docs/reviews/multitenancy-review.md) | ✅ 2026-06-25 | 2026-06-25 | `ee35299…82ea157` |
| NSP-per-org | **NSP-per-org** — bind the PQS roster + every PHI door to an organization; restores NSP + referral modules under multi-tenancy. ADR [0042](docs/decisions/0042-nsp-per-org.md); design [nsp-per-org-design.md](docs/progress/nsp-per-org-design.md). | ✅ complete | ✅ | ✅ pgTAP 1102/1102 + full E2E 421/0 | ✅ APPROVED A [core](docs/reviews/nsp-per-org-a-review.md) + B [whole](docs/reviews/nsp-per-org-b-review.md) | ✅ 2026-06-25 | 2026-06-25 | `b0e15f4…9c53035` |
| result-rec | **Result-based phase recommendation** — `recommend_when` combinable answer/result group; recommend from an earlier phase's result; suggestion-only, zero evaluator drift. ADR [0043](docs/decisions/0043-phase-result-based-recommendation.md) → [result-rec.md](docs/progress/result-rec.md). | ✅ complete | ✅ | ✅ pgTAP 1122 + Vitest 164 + E2E 431/0 | ✅ APPROVED 2026-06-26 [review](docs/reviews/result-rec-review.md) | ✅ 2026-06-26 | 2026-06-26 | `6c5baeb…` · ✅ remote re-baselined 2026-07-01 |
| processless-cases | **"Sem processo" (process-less cases)** — template-less case + optional offered-outcome set + optional PHI; 2 coordinator-gated DEFINER RPCs, no new RLS shape. ADR [0044](docs/decisions/0044-processless-cases.md) → [processless-cases.md](docs/progress/processless-cases.md). | ✅ complete | ✅ | ✅ pgTAP 1153 + E2E 8/8 + full 439/0 | ✅ APPROVED 2026-06-30 [review](docs/reviews/processless-cases-review.md) | ✅ 2026-06-30 | 2026-06-30 | `cdf26d0` · ✅ remote re-baselined 2026-07-01 |
| form-model-norm | **Form data-model normalization** — `form_items.options` JSONB → normalized `form_item_options` + `answer_selected_options`; evaluator byte-for-byte unchanged via `app.answer_map`; pre-launch squash + full reset. → [form-model-normalization.md](docs/progress/form-model-normalization.md). | ✅ complete | ✅ | ✅ T-1 green | ✅ APPROVED 2026-07-01 [review](docs/reviews/form-model-normalization-review.md) | ✅ 2026-07-01 | 2026-07-01 | squash `ffc0ea5`; baseline `20260620000000`; merged → main |
| answer-model-v2 | **Answer-Model v2 + forward-compat** — uniform answer row + typed scalar cols + instance-ready answer-key scaffolding + question defaults; evaluator unchanged (Rule 3). ADRs [0045](docs/decisions/0045-answer-model-v2.md) · [0046](docs/decisions/0046-forward-compat-form-capabilities.md) → [answer-model-v2.md](docs/progress/answer-model-v2.md). | ✅ complete | ✅ | ✅ pgTAP 1205 / Vitest 176 / E2E 6/6 + 456p (0 reg) | ✅ APPROVED 2026-07-01 [report](docs/reviews/answer-model-v2-review.md) | ✅ 2026-07-01 | 2026-07-01 | baseline `20260620000000` · ✅ remote re-baselined 2026-07-01 |
| ad-hoc-narratives | **Ad-hoc Case Narratives** — coordinator adds a narrative to an OPEN case (`add_ad_hoc_narrative` DEFINER RPC + `case_narratives.is_ad_hoc`); reverses ADR 0032 D7 for open cases. ADR [0047](docs/decisions/0047-ad-hoc-case-narratives.md) → [ad-hoc-narratives.md](docs/progress/ad-hoc-narratives.md). | ✅ complete | ✅ | ✅ pgTAP 1219 · Vitest 176 · E2E 5/5 + 461p (0 reg) | ✅ APPROVED 2026-07-01 [review](docs/reviews/ad-hoc-narratives-review.md) | ✅ 2026-07-01 | 2026-07-01 | branch `feat/ad-hoc-case-narratives` |
| user-reg | **User Registration & Identity Management** — org_admin registers users (professional category + optional council credentials + home hospital/matrícula), verify+activate via invite, searchable org directory + full per-user management (committees/roles, deactivate/suspend/reactivate/resend); **real `is_active` enforcement**; drops DOB (LGPD). Detail → [user-registration.md](docs/progress/user-registration.md); ADR [0048](docs/decisions/0048-user-registration-identity.md). | ✅ complete | ✅ | ✅ feat 12/12 · pgTAP 1257 · full 467p/10-contam (0 reg) | ✅ APPROVED | ✅ 2026-07-02 | 2026-07-02 | `117319d` |
| hospital-admin | **Phase A — Hospital-admin tier, 4-tier audit & committee titles** — `hospital_admin` (org_admin mirrored, hospital-scoped, incl. disposal + response reads); `organization_members` role widen + `hospital_id` + `NULLS NOT DISTINCT` cardinality; new `app.is_commission_admin_of()` swapped into ~60 sites; audit gains a **hospital tier** (chain key org/hospital/commission, lockstep `audit_canonical`/`audit_write`/`verify_audit_chain`); per-commission `commission_member_titles` vocab + `title_id`. Design [hospital-roles-nsp-titles-design.md](docs/progress/hospital-roles-nsp-titles-design.md). | ✅ complete | ✅ | ✅ feature spec 38/38 · pgTAP 1454 · full regr 497p/26f (0 Phase-A reg) | ✅ APPROVED (CHANGES REQ→all fixed) [review](docs/reviews/hospital-admin-tier-review.md) | ✅ 2026-07-03 | 2026-07-03 | `99e2d09` |
| DB-hardening | **Pre-pilot DB hardening (Waves 1+2)** — consolidated remediation of the 2026-07 external DB audit: critical set (C-1…C-6, H-8) + §4/§5 perf/data-model (P1–P10, D1–D4/D6-flip/D7/D9). Wave-1 push (WS-1…WS-5) + Wave-2 perf sweep (WS-6: P2/P3/P4/P5). Structural, no flag, pre-pilot reset-OK. Plan: [pre-pilot-db-hardening-program](docs/plans/pre-pilot-db-hardening-program.md). Detail → [W1](docs/progress/pre-pilot-hardening-wave1.md) · [W2](docs/progress/pre-pilot-hardening-wave2.md). **Waves 3+4 dispositioned 2026-07-10 → [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md)** (D12 closed · D5/D6 superseded/cancelled · D3/WS-8 → F-cleanup · P6/P7 deferred · §6.1 **pulled pre-pilot** → ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)). | ✅ **Waves 1+2 done; W3+4 → foundations pgm** | ✅ | ✅ pgTAP **1644** · Vitest **206** · E2E W1 531p / W2 546p+13 acc (0 reg) | ✅ APPROVED — W1 [review](docs/reviews/pre-pilot-hardening-wave1-review.md) · W2 [review](docs/reviews/pre-pilot-hardening-wave2-review.md) | ✅ 2026-07-05 | 2026-07-05 | W1 `68b393b` · W2 `a2a7fab` · remote `reset --linked` 2026-07-05 · **merged → main `27d9e5f`** |
| flexible-forms | **Flexible-Forms Foundation** *(45-item partner forms-model gap disposition + pre-pilot bones)* — item_type CHECK reserves `group`/`repeating_group`/`matrix`/`risk_matrix`/`reference`; `form_item_options.is_exclusive`+`risk_weight`; `form_versions.behavior_config`; evaluator operators `contains`/`not_contains`/`is_empty`/`is_not_empty` (dual SQL↔TS + golden parity); repeating-group schema shape only (position-uniqueness; write RPCs → FF-1, Rec C); `form_item_validations` reserved. Field-type/validation/power-authoring UX deferred to post-pilot FF-1…FF-5. File-upload answers dropped 2026-07-07 (broad PHI ingress). Structural, no flag, pre-pilot reset-OK. **Now = phase F3 of the [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md); hardening D6/§6.3 metadata-item-types refactor CONFIRMED CANCELLED by this ADR (§1 C-ε), `form_items.phi_policy` reservation dropped (§1 C-ζ).** ADR [0060](docs/decisions/0060-flexible-forms-foundation.md). | ✅ **complete (F3)** | ✅ | ✅ pgTAP **2023** (209 38/38) · Vitest **356** (parity 81/81) · E2E **632p/6f → GREEN, 0 F3 reg** | ✅ APPROVED (0B/0M/0m · 4 INFO) [review](docs/reviews/phase-F3-review.md) | ✅ 2026-07-12 | 2026-07-12 | `94f03c3` (local; **remote ✓ 2026-07-12 pilot reset**) → [detail](docs/progress/f3-flexible-forms.md) |
| case-participants | **Case-Participants E0** *(ADR [0064](docs/decisions/0064-case-subject-generalization-participants.md); = phase F1 of the [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md))* — `participants` + `case_participant_roles` + `case_participants` (typed-registry identity, `UNIQUE(id,participant_type)` + subtype composite-FK+CHECK, R5); `professional_profiles` (Class-2 professional-identity: case-scoped RLS + audited reads, no isolated door); **re-key `case_patient` → `patient_identifiers(participant_id)` N-per-case while flag OFF ⇒ zero prod PHI migration** (generalize `dispose_case_phi` + purge `patient_xref` per participant, R3; `get_case_patient` → `get_participant_patient`); `case_types` + `case_type_terminology`; denormalize `organization_id` onto `cases` (R2). Amends Rule 12 (adds professional-identity class) + Rule 2. **Precedes F2** (attachment_subjects → participants). Flags `case_participants`/`case_types` seed **OFF** (m2 hard gate: no flip on real ethics data until E1 — **now pre-pilot, ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)**). E1 access-spine + E2 ethics procedure **pulled pre-pilot (0071)**. | ✅ **complete (F1)** | ✅ | ✅ pgTAP **1913** · E2E F1-surface **54/54** (0 reg; marathon reds env-only) · Vitest **294** | ✅ APPROVED (CHANGES→APPROVED) [review](docs/reviews/phase-F1-review.md) | ✅ 2026-07-10 | 2026-07-10 | `ef66b0a`+`6805bd9` (local; **remote ✓ 2026-07-12 pilot reset**) → [detail](docs/progress/f1-case-participants.md) |
| pre-pilot-foundations | **Pre-Pilot Foundations Program** *(umbrella — one collision-free plan for ADR 0060 + 0063 + 0064 + DB-hardening Waves 3+4)* — F0 design gate (ratify 2 polymorphism dialects [closes D12], one Rule-12 sensitivity taxonomy, disposal-compose order) → **F1 participants → F2 attachments** → Phase 16 → **F3 flexible-forms** → F-cleanup (D3/WS-8) → pilot reset. Six collision resolutions (§1 C-α…ζ); only hard ordering = participants before attachments. Plan: [pre-pilot-foundations-program](docs/plans/pre-pilot-foundations-program.md). | 🏗️ **F0–F3 + F-cleanup ✅ · pilot reset (remote `db reset --linked`) ✅ verified 2026-07-12 — F1+F2+F3+F-cleanup (+Ph15/17) live on remote; remaining = Phase 16 (deferred) + Coolify app deploy + origin push** | – | – | – | – | – | – |
| nsp-per-hospital | **Phase B — NSP-per-hospital + `nsp_org_admin`** — re-key the PQS roster + **every PHI door** org→hospital (`is_pqs_member_of(hospital)`); `nsp_org_admin` (org-level, **zero-PHI** rollups + roster curation + coordinator appointment); `nsp_coordinator` = full **local** operator; dual-hospital same-org referral reads; `dispose_referral_phi`. Detail → [nsp-per-hospital.md](docs/progress/nsp-per-hospital.md); design [nsp-per-hospital-design.md](docs/progress/nsp-per-hospital-design.md); ADR [0052](docs/decisions/0052-nsp-per-hospital.md). | ✅ complete | ✅ pgTAP **1446** · Vitest 193 · tsc 0 · eslint 0 · build ✓ | ✅ feature **32/32** · prod-PHI **86/86** · full regr **0 Phase-B reg** (NPH-003 fixed; 10 residual non-Phase-B — 3 base-proven pre-existing) | ✅ APPROVED (CHANGES→APPROVED same day) [review](docs/reviews/nsp-per-hospital-review.md) | ✅ 2026-07-03 | 2026-07-03 | BE `4ab7618`+`7a4ffa6`+`12888b1`+`c186954`+`693ea60` · FE `ccb6bc3`+`6eab3d1` · E2E `8ddc3b9`+`a323750` |
| administrativo | **Administrativo delegated-capability role** — per-commission appointment + curated, finite capability menu (`schedule_meetings`/`create_cases`/`assign_case_phases`/`view_signoffs`) without full `staff_admin` authority; no new role enum, decoupled from the display-only "Secretário" title; flag `administrativo` **flipped permanently ON 2026-07-08** (`5a6c668`). Detail → [administrativo.md](docs/progress/administrativo.md); ADR [0061](docs/decisions/0061-administrativo-delegated-role.md). | ✅ complete | ✅ tsc 0 · eslint 0 | ✅ pgTAP **50/50** · feature E2E **10/10** · full regr E2E 574 pass (0 reg) | ✅ APPROVED [review](docs/reviews/administrativo-review.md) | ✅ 2026-07-08 | 2026-07-08 | `75c903f`+`3956b32` → merged `1010f07`; flag-on `5a6c668` (pushed) |
| f-cleanup | **F-cleanup — residual DB-hardening** *(= the pre-pilot-foundations program's F-cleanup)* — **D3** result-engine jsonb/array → FK junctions (D3-mid; `result_ruleset` stays jsonb, `blocks` stays int[], emits⇒no-allowed re-enforced = HC067), **D10** `updated_at` touch triggers (`cases`/`commissions`/`forms`), **D8** forward-FK pgTAP lock, **D11** anglicize ALL 12 status-enum internal keys → English (1:1, pt-BR labels kept). Reset-OK, no flag. Plans [f-cleanup](docs/plans/f-cleanup.md)/[d11](docs/plans/f-cleanup-d11.md); ADRs [0068](docs/decisions/0068-result-engine-fk-junctions.md)/[0069](docs/decisions/0069-status-key-anglicization.md) → [detail](docs/progress/f-cleanup.md). | ✅ complete | ✅ tsc 0 · lint 0 · vitest 356 · `next build` | ✅ pgTAP **2100/0** · full E2E **51/51 specs green** (3 stale test-assertions fixed; 0 app/schema reg) | ✅ APPROVED (0B/0M/2m/3i; Minor-1 re-enforced) [review](docs/reviews/f-cleanup-review.md) | ✅ 2026-07-12 (merged to main + remote reset) | 2026-07-12 | merged → main `5f81286` (ff-only; `f-cleanup` branch deleted) · **remote `db reset --linked` ✓ verified 2026-07-12** (origin push pending) |
| referrals-v2 | **Referrals v2 — Dialogue & Governance** *(pre-pilot expansion of Phase 22; ADR [0037](docs/decisions/0037-inter-committee-case-referrals.md) Amendment 1)* — pilot-prep testing surfaced the missing **mid-review two-way dialogue**; evaluated a partner referral data model ([eval](docs/design/temp/referral_data_model_evaluation.md)) and adopt **Essential** (`referral_messages` thread + `awaiting_information`/`waiting_on_committee_id`, R1) + **Deferred** triage/SLA · reopen+answered/resolved split · `parent_referral_id` · assignments · committee-private notes · context versions · receipts · redaction · idempotency (R2–R5); **avoid** `row_version`/`referral_status_events`/participants-ACL/PHI-`structured_context`/referral-outbox. Extends (never contradicts) the Phase-22 core (snapshot boundary · PHI single-door · QPS plane · hash-chained audit); pre-pilot/reset-OK behind `case_referrals`; **R1–R5 all pre-pilot (ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)); R1 first**; HC block `HC0A0+`. Plan: [referrals-v2-dialogue-governance](docs/plans/referrals-v2-dialogue-governance.md). | 🔜 **planned (design accepted 2026-07-12; not implemented)** | – | – | – | – | – | – |
| interviews-v2 | **Interviews v2 — Sessions + Reporting/Confidentiality** *(pre-pilot revision of Phase 11; ADR [0070](docs/decisions/0070-interview-data-model-v2-sessions.md))* — pilot-prep testing surfaced the **one-encounter-per-interview** rigidity; evaluated a partner interview data model ([handoff](docs/design/temp/interview_data_model_handoff.md)) and adopt **Now**: `interview_sessions` 1:N *(comprehensive; reset-OK **hard-cut** of the 5 scheduling cols → session; interview stays coordinator, gains `awaiting_follow_up`; `conducted_at`→per-session `actual_start/end`)* + `interview_category` (N1, required) + subject `relationship_to_case` (N2, **staff-only**) + **non-enforcing** `confidentiality_level`; **defer → E1** participant-registry wiring · access-grants + confidentiality **enforcement** · attendance · participant-roles M2M · topics · versioned/per-audience summaries · status-history table · doc semantic-role · org/hospital denorm; **avoid** column-encryption *(contradicts ADR 0035/0037/0038)* · recording/consent/transcripts+segments · statements · interview-specific findings *(dup case RCA/CAPA)* · external-access portal. Extends (never contradicts) the Phase-11 core (case-scoped RLS · `can_write_interview` grant · single `case_events` registry · staff-only/no-PHI); pre-pilot/reset-OK behind `interviews`; **own Phase Gate**. Plan: [interviews-v2-sessions](docs/plans/interviews-v2-sessions.md). | 🔜 **planned (design accepted 2026-07-12; not implemented)** | – | – | – | – | – | – |
| pre-pilot-release | **Pre-Pilot Release Scope Expansion** *(umbrella — 12 initiatives pulled into the pilot release 2026-07-12; ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md); sequenced build plan [pre-pilot-release-scope-expansion](docs/plans/pre-pilot-release-scope-expansion.md))* — **S0** design gate (author Ethics E1/E2 ADRs · §6.1 plan · supersession ADR · AI-satellite spec; ratify SQLSTATE/flags/collisions) → **S1** substrate (**N** Notifications Ph20 · **MEM** §6.1 `memberships` collapse · **SUP** supersession engine) → **S2** cores (**RV2** R0→R1 pilot-critical · **IV2** sessions · **AI** satellites+cross-link) → **S3** **ETH·E1** access-spine (releases the m2 flag gate on F1) → **S4** **ETH·E2** procedure · **RV2** R2–R5 · **CH** Charters Ph21 → **S5** **ETH·E3** terminology/UX (E3b accreditation-link needs Phase 16). Only Phases **18–19** stay post-pilot; two hard orderings = MEM-before-ethics/referrals-RLS · IV2-before-E1. Each track keeps its own flag/Phase Gate; specs still to author at S0 (Ethics E1/E2, supersession [0060 Gap 38], §6.1 plan, AI-satellites). **Pilot follows this whole block** + Coolify deploy + origin push. | 🏗️ **in progress — S1 substrate** (branch `pre-pilot-release-s0`; **S0 signed off 2026-07-13** — spine + §I block-ratified; **S1 = MEM → SUP → N** serialized; **MEM ✅ recorded 2026-07-13** (single `memberships` collapse, QA APPROVED, pgTAP 2161/0); **SUP opening next**; spine → [s0-ratification](docs/plans/pre-pilot-release-s0-ratification.md)) | – | – | – | – | – | – |

> **Accreditation & Quality-Governance Track (13–21)** — planned 2026-06-17; specs in
> [PHASES.md](PHASES.md) (§ Accreditation track), rationale in ADR
> [0028](docs/decisions/0028-accreditation-governance-roadmap.md). **13 = Audit Trail** and
> **14 = Patient-Safety/NSP (Events, Triage, RCA & CAPA — sub-phases 14a–14d)** are the agreed first two. Each phase is feature-flagged, individually
> testable, and gated by §6. **Deployment plan (revised 2026-07-05, ADR [0057](docs/decisions/0057-indicators-doc-control-replan.md)):
> remaining pre-pilot phases build in order 15 → 17 → 16** (P0 core, now incl. document
> control), **plus the twelve initiatives pulled pre-pilot 2026-07-12 (ADR
> [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)): Phases 20–21, Referrals v2,
> Interviews v2, Ethics E1–E3, action-items satellites + cross-link, §6.1, supersession engine —
> pilot after that whole block**; Phase 9 stays pending until then (also validates the ADR 0009
> prod-auth gap), and **Phases 18–19** follow the pilot (**20–21 pulled pre-pilot**). Phase 15/17 specs revised against the
> post-hardening platform (option-`code` derived config, hybrid taxa, two-tier CAPA hook,
> hospital rollups, approver read arm) — see the ADR + accreditation-track.md. This track is
> built ahead of Phase 9 (same convention as Phases 10–12).

Status legend: 🔜 not started · 🏗️ in progress · 🧪 testing · 🔍 QA review · ⏸️ awaiting human approval · ✅ complete · ❌ blocked

## Current Phase Tasks

<!-- Lead recreates this table at the start of each phase. At the §6 Record step the
     completed phase's task detail is archived to docs/progress/phase-N.md (or a
     feature-named file) and replaced here by a one-line pointer (CLAUDE.md §7). -->

### 🏗️ ACTIVE — S1 · Substrate (Pre-Pilot Release Scope Expansion, ADR 0071)

**S0 signed off 2026-07-13** (PO said "implement S1") — the ratified spine + **all §I dispositions block-ratified**
(incl. the ★ set: MEM O-2 writes-behind-door · MEM O-6 unified `membership.*` audit · E2 O-3 vote-quorum · E2 O-7a
X-η seed-only · E1-owns-`cases.case_type_id`) → [s0-ratification.md](docs/plans/pre-pilot-release-s0-ratification.md) §J.

S1 = the substrate, built **serialized** (one local DB + one `database.ts`/`seed.sql`; MEM must not run concurrently
with RLS-heavy tracks — MEM spec §3): **MEM → SUP → N**, each its own Phase Gate.

| Track | Spec | Owner | Status |
| ----- | ---- | ----- | ------ |
| **MEM** — single `memberships` collapse | [memberships-collapse-s6-1.md](docs/plans/memberships-collapse-s6-1.md) · `HC0G*` | backend | 🏗️ **readiness pass ✅ 2026-07-13** — plan verified sound + matches the signed-off spec; missed-predicate risk cleared (all 27 survivor wrappers present; 32-name grep + predicate-extraction sub-agent agree). **3 non-structural deltas, lead-dispositioned:** (1) O-6 repoint also touches `src/lib/queries/audit.ts` pt-BR labels + `AuditAction` union (the only FE-visible change) + 7 pgTAP + 6 E2E — **hard-cut** legacy `*_member.*` verbs → `membership.*` only (PO override 2026-07-13; pilot ships on a fresh reset ⇒ no historical rows) + pgTAP guard the legacy verbs no longer appear; (2) `users/actions.ts:676 assignMemberCommittee` service-role write routes through the door (in-scope under O-2); (3) staff self-escalation stays `42501` (`_deny_self_grant` covers it), `HC0G3` reserved. **✅ human-approved 2026-07-13 (hard-cut audit labels).** 🏗️ **Build complete (Gate step 1) 2026-07-13:** 6 migrations `20260720000000–000500` (table + shape CHECKs · `has_role` family + wrappers · `grant_role`/`revoke_role` door + 10 shims · `membership.*` audit + 3 legacy tables dropped · `profiles` SELECT-policy CASCADE fix in `…000500`) + ADR [0075](docs/decisions/0075-memberships-collapse-write-path-split.md) (service-role vs RLS-scoped write split — **no privilege change**; zero `authenticated` DML grant, boundary intact). **pgTAP 2158/0** (new `224` = 61/61) · **Vitest 356** · lint 0 · typecheck 0 (lead-reconfirmed). Frozen signatures held (only `AuditEntityType` `commission_member`→`membership`; FE `audit-icon.tsx` updated — QA/frontend to confirm). ⚠️ mid-build concurrency incident reverted lead PROGRESS.md edits (recovered from `stash@{0}`; stash left for human to drop). 🧪 **Tester gate (step 2):** audit-verb repoint clean (0 legacy verb strings left in `e2e/`) + new `mem-memberships-collapse.spec.ts` **7/7** + `phase13-audit` **34/34**. **BUG-MEM-001 (BLOCKER):** `listMembers` (`members.ts`) embeds `profiles(...)` off `memberships` — now ambiguous (MEM added `granted_by→profiles` FK) → PGRST201 → **silent-empty roster** (query swallows `{error}`); breaks `phase3-admin` AC2 (3/4) + `hospital-admin` HA-4/HA-K (3). Fix = qualify embed `profiles!memberships_principal_id_fkey(...)` + add error check. **BUG-MEM-001 fixed (fix loop 1):** `members.ts:66` + `commissions.ts:84,120` embeds qualified + `{error}` throws added (2nd embed in `commissions.ts` was a nested-embed PGRST201 the grep missed → admin commission-list silent-empty risk); `org.ts`/`pqs.ts` column-hint form verified safe; sweep clean. lint 0 · typecheck 0 · Vitest 356. **Tester gate GREEN (fix loop 1 closed): focused 87/87** (hospital-admin 38/38 · phase3-admin 15/15 · mem-collapse 7/7 · audit 27/27; admin commission-list confirmed populating). **Full `e2e:prod` parity oracle GREEN 2026-07-13: 586 passed · 0 failed · 8 flaky (9 batches)** — flakies = known UI-timing + E2E-leftover member-count (resolved on isolated rerun; CCIH seed = 9 confirmed), no MEM regression. **Build + Test gates ✅.** **QA ✅ APPROVED 2026-07-13 (0B/0M/3 cheap minor)** [review](docs/reviews/memberships-collapse-review.md) — missed-predicate `is_entitled_document_approver` caught+locked (pos+neg pgTAP); door authority incumbent-verbatim (no widening); `membership.*` audit PHI-free; ADR 0075 sound; CASCADE bounded to 2 `profiles` policies. Minors: m1 grant/revoke `is_admin()` asymmetry (safe narrowing, no live caller) · m2 InitPlan `(select auth.uid())` in one wrapper · m3 3 pgTAP §7 asserts use `is not null` smoke vs true/false pairs. **✅ Human-approved 2026-07-13 (Gate step 4).** **Minors cleared** (m1 asymmetry comment + ADR 0075 note · m2 `is_pqs_operator_of` InitPlan wrap · m3 `224` §7 3 smoke → 6 truth pairs, 61→64): **pgTAP 2161/0** (fresh reset), lint 0, typecheck 0, Vitest unaffected (356, no `src/` change). **✅ COMPLETE — Recorded 2026-07-13 (Gate step 5)** on `pre-pilot-release-s0`; `docs/backend-state.md` updated (membership surface = one `memberships` + one `grant_role`/`revoke_role` door + one `has_role` family); `graphify update`. **Remote deploy DEFERRED to the pilot reset.** |
| **SUP** — supersession correction engine | [supersession-correction.md](docs/plans/supersession-correction.md) · ADR [0074](docs/decisions/0074-supersession-correction-model.md) · `HC0H*` | backend | 🏗️ **readiness pass ✅ 2026-07-13** (opus, read-only) — plan sound; **load-bearing §3.A retrofit verified line-by-line** (all 10 dashboard/derived consumers route through the single `app.submitted_form_responses` helper ⇒ ONE-line WHERE add propagates; zero direct edits). Precedents/guards/audit-sig/flag-count/`HC0H*`-free all ✅. **2 lead-applied deltas:** (1) migration numbering — MEM took `…000000–000500`, so SUP renumbers `…000600` core + `…000610` flag-on; (2) **MEM redefined `commission_overview` in `…000300` (memberships-scoped)** — SUP must add the `NOT EXISTS` to that post-MEM body (plan's baseline-L8292 target is stale) or it regresses the MEM repoint; correct plan §3.B + §7. Lead dispositions O-2 (discard_response abandon) + O-3 (`is_staff_admin_of OR is_commission_admin_of` authority) = confirm-existing-pattern. **✅ Plan-approved 2026-07-13 (human): build SUP, O-1 = PRE-POPULATE** the correction draft. Plan doc corrected (migrations `…000600`/`…000610`; `commission_overview` retrofit on MEM's post-collapse body; §7/§9 updated). 🏗️ **Build (Gate step 1) — backend ✅ 2026-07-13:** 2 migrations `…000600` (core: `supersedes_id` + partial-unique + `guard_supersession_coherent` HC0H4/HC0H1 · `assert_response_correction_enabled` · `supersede_response` DEFINER RPC [pre-populates answers + `answer_selected_options`, `response.superseded` audit, t19] · `submitted_form_responses` + **`commission_overview` (post-MEM body)** `NOT EXISTS` retrofit) + `…000610` (flag ON). `feature-flags.ts` 20th key `response_correction` + wrapper; `submissions.ts` `SubmissionDetail` += `supersedesId`/`supersededById`/`badge`/`canCorrect` (server-computed); `responses/actions.ts` `supersedeResponseAction` (HC0H0–HC0H4→pt-BR); `isDashboardCountable` twin + Vitest; seed flag ON; types regen. **pgTAP 2196/0** (new `225` = 34) · typecheck 0 · lint 0 · Vitest **360**. Spec ambiguity resolved: `getResponseDetail`→real `getSubmissionDetail`. **List-badge gap closed** (backend, TS-only): `SubmissionRow` += `badge` via `resolveSupersessionBadge` + one batched `.in()` lookup in `listSubmissions` (keyset-safe); Vitest **364**, lint/tsc 0. **Full FE contract ready** — list `badge` · detail `supersedesId`/`supersededById`/`badge`/`canCorrect` · `supersedeResponseAction {ok,error?,successorId?}`. **FE UX (§5) ✅ 2026-07-13:** new `supersession-badge.tsx` (`SupersessionBadgePill` — muted `substituído`/accent `atual`, icon+text never color-alone) + `correct-submission-button.tsx` ("Corrigir envio", `canCorrect`-gated, Dialog + required "Motivo da correção" textarea, real `htmlFor` label, `onOpenAutoFocus`); submission detail page + `submission-row.tsx` render badges; successor routes into the existing `responder/[responseId]` wizard. **Race bug found+fixed in verify:** success-path revalidation flips `canCorrect`→false & unmounts the button, racing a `useEffect` `router.push` (silently dropped) → fixed by calling the action in `useTransition` + synchronous `router.push`. Preview-verified (dev, flag ON, `chefe.ccih`): button standalone-only · empty-submit blocked · full mouse + keyboard-only flows land in the pre-populated successor draft · list+detail badges end-to-end · 0 console/network errors. lint/typecheck 0. **BUILD (step 1) COMPLETE — backend + FE.** 🧪 Tester (step 2) next. |
| **N** — Notifications & Escalation (Ph 20) | [accreditation-track.md](docs/phases/accreditation-track.md) · `HC0C*` | backend | 🔜 queued (external cron → server route; after SUP) |

**Two hard orderings (S1+):** MEM before RLS-heavy ethics/referrals-governance · IV2 before ETH·E1.

---

### ✅ SIGNED OFF — S0 · Design & Sequencing Gate (record; detail → [s0-ratification.md](docs/plans/pre-pilot-release-s0-ratification.md))

Design/spec gate — **no migration**. Signed off 2026-07-13; all 6 specs authored + reconciled; spine + §I ledger
block-ratified. Task detail below (retained as the S0 record).

| Task | Owner | Status |
| ---- | ----- | ------ |
| Ratify the spine — SQLSTATE (HC099 high-water) / flags / conventions / **collision contracts X-α…θ** / PO decisions / scheduler → `s0-ratification.md` | lead | ✅ authored (awaiting sign-off) |
| **§6.1 memberships-collapse** scoped plan (MEM; column-per-scope + shape CHECK, `has_role()` family, `case_access` **kept separate**) → `docs/plans/memberships-collapse-s6-1.md` | backend | ✅ authored (awaiting sign-off) — 27 `is_*_of` wrappers, unified `membership.*` audit, `HC0G0–HC0G9`; 6 open decisions (O-1…O-6: `case_id` drop, `commission_members` behind door, `title_id` inline, verb-rename) |
| **Supersession** correction ADR **0074** + plan (`responses.supersedes_id` + latest-in-chain **aggregation retrofit** — A4 confirms required) → `docs/plans/supersession-correction.md` | backend | ✅ authored (awaiting sign-off) — `supersede_response` DEFINER (standalone-only), `HC0H0–HC0H4`; **retrofit is 1 choke-point** (`app.submitted_form_responses` → all 6 dashboards + 4 derived-indicator paths) + `commission_overview` + TS twin; 3 brief-corrections accepted (no `submit_response`/allow-list/RLS change); 3 open decisions (→ ratification §I) |
| **AI-satellites** spec (reminders + updates-feed + checklists — reuse `can_read_action_item`) → `docs/plans/action-items-satellites.md` | backend | ✅ authored (awaiting sign-off) — 3 tables (`action_item_reminders`/`_updates`/`_checklists`, verbatim RLS reuse, no new shape), 11 `committee_*` RPCs, `HC0I0–HC0I2` mapped (`HC0I3–9` reserved), reminder→N scan-arm contract specced (inert until N ships), AI·ui gaps enumerated (`visibility_scope` unprojected in `case-action-items.ts`/`action-items.ts`, `list_my_action_items` RPC needs widening, `meetings/action-item-form.tsx` needs the `case_id`+visibility toggle); 4 open decisions (O-1 null-case fail-closed check, O-2 updates/checklist write-authority PO call, O-3 reminder-recipient case-read gap, O-4 cosmetic) |
| **Ethics E1** ADR **0072** + access-spine plan (m2-gate release; `can_read_case` terms R6; IV2 fold-in; **M2-erasure-vs-CFM-retention posture proposed for sign-off**) → `docs/phases/ethics-e1-access-spine.md` | backend | ✅ authored (awaiting sign-off) — ADR [0072](docs/decisions/0072-ethics-access-spine.md) + [plan](docs/phases/ethics-e1-access-spine.md). `can_read_case` **4 new terms** (respondent-exclusion + recusal — HARD DENY before grants; conditional member-wide arm on new `cases.visibility_policy`; confidentiality ceiling on the F2 attachment reader), **all R6-safe in the DEFINER over base tables** (mirrors shipped `can_read_professional_profile`); flag-OFF byte-for-byte (deny-terms inert on empty tables, all cases `commission_default`). New tables `case_recusals`/`case_conflict_declarations` (SELECT-only RLS + recusal self/coordinator asymmetry). **X-δ:** `cases.confidentiality_level` + `case_types.default_confidentiality_level` snapshot-at-create on the **7-value F2 taxonomy**; clearance rides **`case_access.max_confidentiality`** (O1 — recommend column, NOT a new grant table). Participant write authority = DEFINER RPCs (`case_participants` stays SELECT-only). **X-γ IV2 fold-in:** `participant_id` FK + confidentiality **enforcing** (remap IV2's 3-value → F2 7-value, O3) + attendance/roles-M2M/topics/summaries. `HC0E0–HC0E9` mapped (E8/E9 reserved). **M2 posture = RECOMMENDED: minimise-not-destroy + retention-pin (CFM-1821/2007), correction always, no `dispose_professional_profile` at E1** (ADR-0035 basis) — human sign-off required. 4 open decisions (O1 clearance shape · O2 which labels gate the doc ceiling · O3 IV2 remap · O4 M2 sign-off). m2-flip checklist = the gate; flags flip LAST (BE-8) + added to `FeatureFlags` interface. |
| **Ethics E2** ADR **0073** + procedure plan (owns `ethics` flag; CRM/CFM modeled; N notice-deadline scan arm) → `docs/phases/ethics-e2-procedure.md` | backend | ✅ authored (awaiting sign-off) — 9 tables + M2 pin/redaction + `case_votes` recusal; `HC0F0–HC0F9`; **X-η corrected** (no `meeting_type` DDL — seed "Audiência" catalog); open decisions → ratification §I |
| **Ethics E3** surfacing plan — **E3a** (terminology/UX + dashboards) now / **E3b** (accreditation link) deferred to Phase 16 → `docs/phases/ethics-e3-surfacing.md` | backend | ✅ authored (awaiting sign-off) — E3a dashboards (RLS-scoped, no service-role) + terminology wiring + `case_events` widen; E3b deferred sketch; **caught the `cases.case_type_id` gap → E1 owns it** (§I) |

**Lead notes (S0):** Groundwork verified on branch (A1 SQLSTATE `HC099` · A2 mig `20260719000800` · A3 no
`pg_cron` → external cron/server route · A4 `responses.supersedes_id` absent → SUP retrofits Ph-15/dashboard ·
A5 `case_participants`/`case_types` in DB-not-interface). **PO decisions locked (2026-07-13):** AI = reminders+
feed+checklists · `case_access` kept separate · E3 split (E3a now, E3b post-Ph16) · E2 owns `ethics`. All six
S0 docs are **design-only** (no migration/app code); mirror `docs/phases/case-access-control.md`. **Two hard
orderings for S1+:** MEM before the RLS-heavy ethics/referrals-governance work · IV2 before ETH·E1.
**PO full sign-off (2026-07-13):** M2 = minimise/don't-destroy · AI·O-2 = stakeholders-only + **all remaining §I
dispositions block-ratified**. **S1 open — MEM → SUP → N, serialized, MEM first.** S0 committed on `pre-pilot-release-s0`.

### Ad-hoc (out-of-phase) — Case "Reuniões" panel · ✅ complete — merged to `main` `fbe215f` (2026-07-12)

**Case↔Meeting reverse link on the Case detail view.** A read-only "Reuniões" rail card on the case
**Detalhes** tab listing every meeting the case was discussed in (reverse of the meeting's "Casos
discutidos" `CaseLinker`). Reuses `listCaseMeetings` (`src/lib/queries/case-timeline.ts:170`) —
**no migration/RLS/new-route**; pure frontend. Decisions (human-approved): read-only · right rail ·
all meetings regardless of state · always render (empty state) · card gated on `meetingsEnabled()`.

| Task | Owner | Status |
| ---- | ----- | ------ |
| FE — new `case-meetings-panel.tsx` rail card + wire `CaseDetailView` + both host pages (coordinator `(detail)` + staff `casos/[caseId]`) | frontend | ✅ built · `tsc`/`eslint --max-warnings=0`/`next build` clean (lead re-verified) |
| E2E — focused `case-meetings-panel.spec.ts` (link→appears · row links out · empty state) | tester | ✅ 2/2 green (chromium dev loop, `--workers=1`) · 0 defects · screenshot proof |

_Files:_ NEW `src/components/cases/case-meetings-panel.tsx` + NEW `e2e/case-meetings-panel.spec.ts`; EDIT `case-detail-view.tsx`, `manage/cases/[caseId]/(detail)/page.tsx`, `casos/[caseId]/page.tsx`. No backend/schema touched. Follow-up (optional, backend-owned): relocate `listCaseMeetings` out of `case-timeline.ts` to a home shared by both tabs. **Merged to `main`** (fast-forward `bf22926..fbe215f`; feature branch deleted; QA gate skipped by decision — no new schema/RLS/route). **Not pushed** to origin (`main` is 1 ahead of `origin/main`); `graphify-out/` graph refresh left uncommitted.

### No active phase — F-cleanup ✅ complete (2026-07-12; on branch `f-cleanup`, awaiting human merge)

**F-cleanup — residual DB-hardening (D3 + D10 + D8 + D11)** passed the full gate (build → tester GREEN →
QA APPROVED → human decisions) on branch `f-cleanup`; durable record →
[f-cleanup.md](docs/progress/f-cleanup.md). pgTAP **2100/0** · whole-project `tsc` 0 · `next build` ok ·
**full E2E all 51 specs green** · **QA ✅ APPROVED** (0B/0M/2m/3i) [D3 [review](docs/reviews/f-cleanup-review.md)];
QA Minor-1 re-enforced (HC067). ADRs [0068](docs/decisions/0068-result-engine-fk-junctions.md) (D3) ·
[0069](docs/decisions/0069-status-key-anglicization.md) (D11). **Not merged / remote-deferred** — committed to
`f-cleanup` for human review; remote deploy folds into the pilot reset.
**Remaining pre-pilot work (expanded 2026-07-12 — ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)):** Phase 16 — Standards Crosswalk (🔜 **deferred** 2026-07-11, needs replanning)
· **Phases 20–21** (Notifications & Escalation; Charters & Cadence — pulled pre-pilot)
· **Referrals v2 — Dialogue & Governance** (🔜 planned 2026-07-12; R1 dialogue core + **R2–R5 governance now all pre-pilot**; [plan](docs/plans/referrals-v2-dialogue-governance.md))
· **Interviews v2 — Sessions** (🔜 planned; ADR [0070](docs/decisions/0070-interview-data-model-v2-sessions.md) / [plan](docs/plans/interviews-v2-sessions.md))
· **Ethics track E1→E2→E3** (access-spine first — needs ADRs; ADR 0064 foundation done as F1)
· **Action-items hub satellites** + **case cross-link UI**
· **§6.1 single-`memberships` collapse** + **supersession correction engine** (ADR 0060 Gap 38)
· the **Coolify app deploy** + origin push (the pilot DB reset landing F1+F2+F3+F-cleanup is ✅ done 2026-07-12).
No teammate task table is live.

> **F3 — Flexible-Forms Foundation: ✅ COMPLETE** (2026-07-12; merged → `main`) — durable record →
> [f3-flexible-forms.md](docs/progress/f3-flexible-forms.md).

<!-- F3 task detail rotated to docs/progress/f3-flexible-forms.md at the Record step (lead-playbook §5). -->

> **F2 → F0 pointers below are prior-phase history; kept as one-line rotations.** 

> **F2 — Centralized Attachments: ✅ COMPLETE** (approved 2026-07-11; `37057c0`). pgTAP **1957/1957** (208 = 50
> keystones) · F2-surface E2E **24/24** (audited PHI door proven — one `attachment.read`/open, zero inline
> `<a>` on PHI rows) · full suite **590p/24f** (F2-owned 14/14 green; 0 F2 reg, ≈ F1 baseline) · **QA
> APPROVED** (0B/0M/3m/4i, all closed in fast-follow). Two bugs the gate caught + fixed: the ADR-0051 stale
> symbol (`is_org_admin_of_commission`→`is_commission_admin_of`, 6 sites) and the `p_uid` dispatcher
> contract fix (→ `_for` variants). BUG-AIF-001 (prod-standalone upload dialogs) is pre-existing /
> cross-cutting, not F2. Durable record → [f2-attachments.md](docs/progress/f2-attachments.md).

> **F1 — Case-Participants E0: ✅ COMPLETE** (approved 2026-07-10; commits `ef66b0a` build + `6805bd9`
> QA-fix). pgTAP **1913/1913** · F1-surface E2E **54/54** (0 reg; marathon reds env-only) · **QA APPROVED**
> (MAJOR-1 inert-RLS-grant defect fixed + K9-locked; PHI door untouched). Durable record →
> [f1-case-participants.md](docs/progress/f1-case-participants.md).

> **F0 — Foundations Design Gate: ✅ COMPLETE** (signed off 2026-07-10; commit `ea06bf4`). Durable record
> = [ADR 0065](docs/decisions/0065-pre-pilot-foundations-conventions.md) (three polymorphism dialects
> [closes D12] · one Rule-12 sensitivity taxonomy · disposal-compose order · catalog-vs-enum · freeze
> principle · reference→participants bridge · supersession model) + ARCHITECTURE.md Appendices A/B.
> `form_items.phi_policy` drop confirmed (C-ζ).

**Pre-Pilot Foundations Program — F3 ✅ COMPLETE (F0 ✅ · F1 ✅ · F2 ✅ · F3 ✅ all on `main`; Phase 16 DEFERRED).** The product owner decided to land four structural initiatives before the
pilot: ADR 0060 (flexible-forms), ADR 0063 (attachments 14e), ADR 0064 E0 (case-participants), and the
DB-hardening Waves-3+4 remainder — reconciled into **one collision-free plan**:
[pre-pilot-foundations-program](docs/plans/pre-pilot-foundations-program.md). Sequence:
**F0 ✅ → F1 participants ✅ → F2 attachments ✅ → ~~Phase 16~~ (deferred 2026-07-11) → F3 flexible-forms
(now) → F-cleanup → pilot reset.** **Phase 16 (Standards Crosswalk) was deferred by the PO** (needs
replanning before build); proceeding to F3 is sanctioned by the program (§3/§8 — no dependency forces F3
after 16; F3 needs only F0+F1). The hard ordering constraint (**participants before attachments**) is
satisfied. Six documented collision resolutions (§1) close hardening **D12**, supersede **D5**, and
cancel **D6/§6.3**. All lands pre-pilot on a disposable DB, dark behind flags where a flag exists.

**BUG-AMV2-002 — choice-default publish regression (ad-hoc, pre-F3 — 2026-07-11).** Publishing a
form with a `multiple_choice`/`checkbox` "Valor padrão" was rejected `valor padrão inválido`
(HC080) on the current baseline (dev **and** prod): `reconcileOptionRows` regenerated the
client-minted option `code` for **new** rows (it preserved a submitted code only when
`existingCodes.has(code)` — empty for a brand-new item), orphaning the stored default. **Fixed**
on branch `fix/bug-amv2-002-choice-default-publish` (off `main`) via a pure, unit-tested
`resolveOptionCodes` (`src/lib/forms/option-code.ts`) that honors client codes; `reconcileOptionRows`
delegates to it. Verified on a **fresh prod standalone build**: `answer-model-v2` **6/6** (DV-2 fixed ·
DV-4 un-masked · DV-6 confirms HC080 still rejects a truly invalid default) · Vitest **302** (incl. 8
new) · tsc + eslint clean. **Not yet merged / remote-deployed.** Detail →
[answer-model-v2.md](docs/progress/answer-model-v2.md); memory `choice-default-publish-regression`.

The **UI/layout fixes batch** (ad-hoc, frontend + backend — 2026-07-08) shipped: 9 frontend fixes
(F1–F9: meeting-badge rename, stacked held/schedule fields, source-badge hug, Meus Casos filter chips,
single "Aparência Condicional" checkbox, red flag on flagged options, discard-draft, Aprovações-pendentes
nav shell, "Config. de Reuniões" moved into Configurações) + 2 backend fixes (B1 discard-draft RPC
`discard_response`; B2 fresh-add "Outro" option). Scoped E2E pass 81/81 GREEN (NOT a formal full-regression
gate). Committed `2bc51f4` (new migration `20260715000200` local-only — remote `db push` deferred to
deploy). The **base-branch failure triage** (6 inherited-RED tests: 1 real FE layout regression fixed +
5 test/env fixes) committed `96a6330`. Full detail archived →
[ui-batch-2026-07.md](docs/progress/ui-batch-2026-07.md).

The **Administrativo delegated-capability role** (ADR 0061, ad-hoc, out-of-phase) completed the
Phase Gate 2026-07-08 — QA APPROVED, feature E2E 10/10 + full regr 574 pass (0 reg), human-approved,
merged → main `1010f07` (5 migrations local-only; remote push deferred to deploy), and its flag was
flipped **permanently ON** the same day (`5a6c668`, migration `20260715000300`, mirroring
`controlled_docs`/`quality_indicators`). Full detail archived → [administrativo.md](docs/progress/administrativo.md).

The **Meeting actual-occurrence time (`held_at`/`held_end`)** feature (ADR 0062, ad-hoc, out-of-phase)
completed the Phase Gate 2026-07-08 — QA APPROVED, pgTAP 21/21 + meeting E2E 33 pass, human-approved,
committed (`bfce6e8`; housekeeping `e427612`). Migration `20260715000100` **local-only — remote
`supabase db push` deferred to the user**. Full detail archived → [meeting-held-time.md](docs/progress/meeting-held-time.md).

The **Form-Builder Enhancements batch** (ad-hoc, out-of-phase) completed the Phase Gate
2026-07-07 — QA APPROVED, full-suite verified green on DEV (chunked), human-approved, migrations
pushed to remote. Full detail archived → [adjustments-batch.md](docs/progress/adjustments-batch.md).
**Next pre-pilot phase: 16 — Standards Crosswalk & Readiness/Gap Engine** (builds 3rd per ADR 0057).

---

_Prior phase context:_ Phase 17 (Controlled-Document Lifecycle) completed 2026-07-06 — full detail archived to
[phase-17.md](docs/progress/phase-17.md). **Next pre-pilot phase: 16 — Standards Crosswalk &
Readiness/Gap Engine** (builds 3rd per ADR 0057; pilot follows). See PHASES.md /
docs/phases/accreditation-track.md before starting.

### Completed work (archived to docs/progress/)

- **F3 — Flexible-Forms Foundation** (ADR [0060](docs/decisions/0060-flexible-forms-foundation.md); pre-pilot form-engine bones + frozen inert answer set + the one live feature = dual-evaluator ops `contains`/`not_contains`/`is_empty`/`is_not_empty`; item_type 10→15 inert; 5 inert answer tables incl. `answer_references→participants`; no flag, no phi_policy, no upload type) — ✅ **COMPLETE 2026-07-12**, QA APPROVED (0B/0M/0m · 4 INFO) [review](docs/reviews/phase-F3-review.md). pgTAP **2023** (`209` 38/38) · Vitest **356** (golden parity 81/81) · E2E **632p/6f → GREEN, 0 F3 reg**. Remote deploy deferred → pilot reset (needs F1 `participants`). Detail → [f3-flexible-forms.md](docs/progress/f3-flexible-forms.md).

- **UI/layout fixes batch + base-branch triage** (ad-hoc, frontend + backend — F1–F9 UI fixes · B1 discard-draft RPC · B2 fresh-add "Outro" · 6-test base-branch triage incl. 1 real FE layout regression) — ✅ **COMPLETE 2026-07-08**, scoped E2E 81/81 GREEN (not a formal full-regression gate). Committed `2bc51f4` (UI batch) + `96a6330` (triage); migration `20260715000200` local-only (remote `db push` deferred to deploy). Detail → [ui-batch-2026-07.md](docs/progress/ui-batch-2026-07.md).
- **Meeting actual-occurrence time** (`held_at`/`held_end`; ADR [0062](docs/decisions/0062-meeting-actual-occurrence-time.md); nullable occurrence window separate from the schedule — captured at the `→ realizada`/Concluir transition, coordinator-correctable while `realizada` only, `meeting.held_changed` audit; schedule never overwritten) — ✅ **COMPLETE 2026-07-08**, QA APPROVED (0 BLOCKER · 0 MAJOR · 2 MINOR: HC084 cleared + 1 accepted) [review](docs/reviews/meeting-held-time-review.md). pgTAP `206` **21/21** · meeting E2E **33 pass** · committed `bfce6e8` (housekeeping `e427612`). Errcodes HC081–HC084; product-owner calls: allow-null (fill-later), `realizada`-only edit gate, future-schedule → blank default, `MeetingListItem` deferred. Migration `20260715000100` **local-only — remote `db push` deferred to the user**. Detail → [meeting-held-time.md](docs/progress/meeting-held-time.md).
- **Administrativo delegated-capability role** (ADR [0061](docs/decisions/0061-administrativo-delegated-role.md); per-commission appointment + curated capability menu — `schedule_meetings`/`create_cases`/`assign_case_phases`/`view_signoffs`, no new role enum) — ✅ **COMPLETE 2026-07-08**, QA APPROVED (0 BLOCKER/MAJOR/MINOR · 3 INFO) [review](docs/reviews/administrativo-review.md). pgTAP **50/50** · feature E2E **10/10** · full regr E2E 574 pass (0 regressions). BUG-ADM-001 found+fixed+verified. Merged → main `1010f07`; flag **flipped permanently ON 2026-07-08** (`5a6c668`, migration `20260715000300`). 5 migrations `…000000–000300` local-only (remote push deferred to deploy). Detail → [administrativo.md](docs/progress/administrativo.md).
- **Form-Builder Enhancements batch** (ad-hoc, out-of-phase — Departments · Flagged + aggregate result criteria · form-builder dialog redesign · "Others" open option · wizard UX + restored segmented time control · views/labels · meeting participants · openNarrativeCount) — ✅ **COMPLETE 2026-07-07**, QA APPROVED (0 BLOCKER/MAJOR/MINOR · 3 INFO) [review](docs/reviews/adjustments-batch-review.md). Batch E2E 29/29 + full-suite chunked 619p (0 app reg) on DEV. 5 bugs (BUG-FBE-005…009) found+fixed+verified. 6 migrations `…000500–001000` pushed to remote 2026-07-07. Detail → [adjustments-batch.md](docs/progress/adjustments-batch.md).
- **Phase 17 — Controlled-Document Lifecycle** (`controlled_docs`; ADR [0057](docs/decisions/0057-indicators-doc-control-replan.md)) — ✅ **COMPLETE 2026-07-06**, QA APPROVED (0 BLOCKER/0 MAJOR · 3 MINOR cleared · 4 INFO) [review](docs/reviews/phase-17-review.md). pgTAP **47/47** (full 1717) · phase E2E **14/14** · full regr 588p (0 Phase-17 reg) · tsc/lint 0 · Vitest 206. 5 bugs (BUG-DOC-001…005) found+fixed+verified; flag flipped ON (`…013000400`). Detail → [phase-17.md](docs/progress/phase-17.md). **Next pre-pilot phase: 16 (Standards Crosswalk).**
- **Phase 15 — Quality Indicators** (`quality_indicators`; ADR [0057](docs/decisions/0057-indicators-doc-control-replan.md)/[0058](docs/decisions/0058-derived-measurement-compute.md)) — ✅ **COMPLETE 2026-07-06**, QA APPROVED (3 MINOR fixed), E2E 12/12 prod-standalone · tsc/lint 0 · Vitest 206. Merged → main `26187dc` (pushed); **remote DB deploy deferred to the pilot**. Detail → [phase-15.md](docs/progress/phase-15.md). **Next pre-pilot phase: 17 (Controlled-Document Lifecycle).**
- **Pre-Pilot DB Hardening — Waves 1+2** (2026-07 external-audit remediation: C-1…C-6/H-8 + do-now data-model/perf [W1] + P2/P3/P4/P5 perf sweep [W2]) — ✅ **COMPLETE + deployed + merged → main `27d9e5f` 2026-07-05**, both waves QA APPROVED (live-verified), pgTAP **1644** · Vitest **206**. Detail → [W1](docs/progress/pre-pilot-hardening-wave1.md) · [W2](docs/progress/pre-pilot-hardening-wave2.md); program plan [pre-pilot-db-hardening-program](docs/plans/pre-pilot-db-hardening-program.md). **Deferred backlog (do NOT act without user direction):** MINOR-5 (revoke TRUNCATE on the 2 membership tables), D3 (jsonb→junction), P7 (audit partition by chain_key), FE disposal-UI follow-ups, Waves 3–4. Pushed to `origin/main`.
- **Phase B — NSP-per-hospital + `nsp_org_admin`** (re-key PQS roster + every PHI door org→hospital; `nsp_org_admin` zero-PHI aggregates + coordinator appointment; `nsp_coordinator` full local operator; dual-hospital referral reads; `dispose_referral_phi`) — ✅ **COMPLETE 2026-07-03**, QA APPROVED (CHANGES→APPROVED same day), pgTAP 1446 + feature E2E 32/32 + prod-PHI 86/86 + full regr 0 Phase-B reg. Bugs NPH-001/002/003 + QA-B-1 found+fixed en route; 3 base-proven pre-existing failures human-deferred. ADR [0052](docs/decisions/0052-nsp-per-hospital.md) → [nsp-per-hospital.md](docs/progress/nsp-per-hospital.md).
- **Phase A — Hospital-admin tier, 4-tier audit & committee titles** (`hospital_admin` = org_admin mirrored/hospital-scoped; `is_commission_admin_of` swapped into ~145 objects; 4-tier audit hash chain; per-commission committee titles; hospital-scoped user management) — ✅ **COMPLETE 2026-07-03**, QA APPROVED (CHANGES REQUESTED → all 5 fixed), feature E2E 38/38 + pgTAP 1454 + full regr 497p/26f (0 Phase-A reg). Bugs HAT-001/002/003 found+fixed en route. ADR [0051](docs/decisions/0051-hospital-admin-tier-and-hospital-audit-tier.md) → [hospital-admin-tier.md](docs/progress/hospital-admin-tier.md).

- **Shared Action-Items Hub** (Option A unification of the non-PHI action-item sources → fold `case_action_items` into the hub with `visibility_scope` + case-access expiry [ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md)] → member "Meus itens de ação" page + "Visão Geral" dashboard) — ✅ **COMPLETE 2026-07-02**, all three QA-APPROVED → [action-items-hub.md](docs/progress/action-items-hub.md).
- **Layout-adjustments batch** (5 scoped UI/UX adjustments — respostas list, status pills, Narrativas→Construtor move, wizard textarea height, registered-user member picker) — ✅ **COMPLETE 2026-07-02** (direct, human-approved) → [layout-adjustments-2026-07.md](docs/progress/layout-adjustments-2026-07.md).
- **User Registration & Identity Management** (`org_admin` registers users w/ professional credentials; verify+activate via invite; searchable org directory + full per-user management; **real `is_active` enforcement** — `app.is_active()` fold + `signIn`/`getSessionContext` gate + `/conta-inativa` — plus `suspended_until`; token_hash pt-BR email templates) — ✅ **COMPLETE 2026-07-02**, QA APPROVED (1 BLOCKER + 4 MINOR all cleared), feature E2E 12/12 + pgTAP 1257/1257 + full E2E 467p/10-contamination (0 regressions). Bugs UREG-001..004 found+fixed en route. ADR [0048](docs/decisions/0048-user-registration-identity.md) → [user-registration.md](docs/progress/user-registration.md). ⚠️ Phase-9 dep: upload the invite/recovery email templates to the Supabase Dashboard.
- **Ad-hoc Case Narratives** (flag `case_narratives`; `add_ad_hoc_narrative` DEFINER RPC + `case_narratives.is_ad_hoc`; coordinator adds a narrative to an open case, mirroring `add_ad_hoc_phase`) — ✅ **COMPLETE 2026-07-01**, QA APPROVED (0 BLOCKER · 1 MAJOR + 2 MINOR cleared), pgTAP 1219/1219 + feature E2E 5/5 + full E2E 461p/7 pre-existing (0 regressions). ADR [0047](docs/decisions/0047-ad-hoc-case-narratives.md) → [ad-hoc-narratives.md](docs/progress/ad-hoc-narratives.md).
- **Answer-Model v2 + form-definition forward-compat** (uniform answer row + typed scalar cols `value_number/date/time` + instance-ready answer-key scaffolding + question defaults; evaluator byte-for-byte unchanged) — ✅ **COMPLETE 2026-07-01**, QA APPROVED, pgTAP 1205/1205 + Vitest 176/176 + feature E2E 6/6. ADRs [0045](docs/decisions/0045-answer-model-v2.md)/[0046](docs/decisions/0046-forward-compat-form-capabilities.md) → [answer-model-v2.md](docs/progress/answer-model-v2.md).
- **Form data-model normalization** (`form_items.options` jsonb → normalized `form_item_options` + `answer_selected_options`; dual evaluator byte-for-byte via `app.answer_map`) — ✅ **COMPLETE 2026-07-01**, QA APPROVED → [form-model-normalization.md](docs/progress/form-model-normalization.md).
- **Result-based phase recommendation** (`recommend_when` combinable answer/result groups; recommend from an earlier phase’s result; suggestion-only) — ✅ **COMPLETE 2026-06-26**, QA APPROVED. ADR [0043](docs/decisions/0043-phase-result-based-recommendation.md) → [result-rec.md](docs/progress/result-rec.md).
- **NSP-per-org** (per-org PQS roster + `nsp_coordinator` + org-scoped PHI doors; restores NSP + referral modules under multi-tenancy) — ✅ **COMPLETE 2026-06-25**, QA APPROVED (sub-phases A+B). ADR [0042](docs/decisions/0042-nsp-per-org.md) → [nsp-per-org.md](docs/progress/nsp-per-org.md).
- **"Sem processo" (process-less cases)** (flag `processless_cases`; template-less case + optional offered-outcome set + optional PHI; 2 coordinator-gated DEFINER RPCs, no new RLS shape) — ✅ **COMPLETE 2026-06-30**, QA APPROVED (0 BLOCKER/0 MAJOR/1 informational), pgTAP 1153/1153 + feature E2E 8/8 + full E2E 439/0 (dev). ADR [0044](docs/decisions/0044-processless-cases.md) → [processless-cases.md](docs/progress/processless-cases.md). ✅ remote re-baselined 2026-07-01.
- **`case_phase_results`** (per-phase categorical result + manual override; flag ON) — ✅ QA APPROVED 2026-06-23, tester pgTAP 45/45 + E2E 7/7, passed in the MT full-suite 292/0 → [case-phase-results.md](docs/progress/case-phase-results.md)
- **Form Builder Enhancements** (new question types · option colors · per-question conditions · observations) — ✅ 2026-06-23 → [form-builder-enhancements.md](docs/progress/form-builder-enhancements.md). ✅ Fully reconciled — **BUG-FBE-004** RESOLVED (`deb436c`, re-verified 2026-06-30).
- **Phase 23 — Patient Identity & Cross-Committee Linkage** (`patient_index`) — ✅ 2026-06-22 → [phase-23.md](docs/progress/phase-23.md)
- **Phase 22 — Inter-Committee Case Referrals** (`case_referrals`) — ✅ 2026-06-21 → [phase-22.md](docs/progress/phase-22.md)
- **`case_patient`** (third PHI module; ADR 0038) — ✅ 2026-06-22 → [case-patient.md](docs/progress/case-patient.md)
- **Case Access Control & "Meus Casos"** (flag `case_access`) + dialog/attribution refinement — ✅ 2026-06-19 → [case-access.md](docs/progress/case-access.md)
- **Case Narratives** (feature-flagged) — QA CHANGES REQUESTED 2026-06-19 → [case-narratives.md](docs/progress/case-narratives.md)

## Bug Log

<!-- Filed by tester; status updated ONLY by tester after re-verification.
     Resolved/closed bugs are archived at phase close → docs/progress/bug-log-archive.md (~78 rows). -->

| ID | Phase | Severity | Description / repro | Expected | Actual | Spec | Owner | Status |
| -- | ----- | -------- | ------------------- | -------- | ------ | ---- | ----- | ------ |
| BUG-F3E2E-001 | F3 gate (pre-existing, NOT F3) | LOW — test-only; feature verified working | `nsp-per-hospital.spec.ts` AC-1b:315 + AC-5:716 deterministically time out (30s) on `page.waitForLoadState('networkidle')` after a **successful** `page.goto('/o/rede-a/nsp/{event}')` on the **prod-standalone** build — the event-detail page never reaches network-idle (GSAP/long-poll keeps the connection busy; the known Playwright `networkidle` anti-pattern), so the following `expect(bodyText).toContain(MRN)` is never reached. Reproduces on fresh-reset ISOLATION + retry #1 (nsp-alone = **30 pass / 2 fail**, deterministic, not flaky). NOT F3: `git diff` shows F3 touches ZERO NSP/event-detail/app-route files (only `conditions.ts` evaluator + 3 OP_LABELS files + inert migrations), so the served page is byte-identical to main; 30/32 nsp tests pass incl. EVERY real PHI-isolation/read assertion (AC-1/AC-6/AC-7). | Event-detail PHI panel renders; MRN assertion passes. | 30s `networkidle` timeout; assertion never reached. | `nsp-per-hospital.spec.ts:315,716` | tester | **RESOLVED 2026-07-12** (tester, per lead directive). Fix applied spec-side (no app code): AC-1b:315 + AC-5:716 `networkidle` → web-first `await expect(page.getByText(MRN)).toBeVisible()`; **all 23** non-deliberate `networkidle` waits in the spec converted to web-first assertions (the 1 remaining is an INTENTIONAL network-leak-audit settle on `/nsp-org`, now commented). Verified on a fresh `supabase db reset --local` + `npm run build` prod-standalone run (`node .next/standalone/server.js`, chromium, `--workers=1`, `unset CI`): **`nsp-per-hospital.spec.ts` 32/32** (38.3s) — was 30/32; the two 30s deterministic timeouts are gone. |
| BUG-F3E2E-002 | F3 gate (pre-existing test-infra, NOT F3) | LOW — test isolation/harness; no product impact | **Full-suite-only** failures that ALL pass on a fresh-reset isolated batch → cross-spec seed contamination + the Windows prod-standalone monolith backlog-collapse (memory `e2e-prod-build-flaky-baseline`). (a) `case-access` AC-2:297 staff4 Meus-Casos not empty (earlier spec's case grant persisted) → PASS 1.4s isolated; (b) `ui-batch-2026-07` S1:725 seeded meeting badge not "Realizada" (earlier meetings spec concluded it) → PASS 2.2s; (c) `views-labels-participants` AC-4:359 convoked members 11 vs 9 (earlier user-registration/hospital-admin specs added 2 CCIH members) → PASS 2.7s (=9); (d) `perf-sweep-wave2` P2-b:250 setup `expect(referralsOn).toBe(true)` false (transient flag-enable) → PASS 2.0s. The 31 "did-not-run" are serial-`describe` siblings of the 6 failures — all RUN+PASS once the blocker clears. NOT F3 (surface untouched). | Each passes under proper isolation. | Fails only under full-suite ordering / monolith backlog. | `case-access:297` · `ui-batch:725` · `views-labels:359` · `perf-sweep-wave2:250` | tester/infra | **OPEN — non-blocking for F3.** Durable fix: per-test DB isolation + `reducedMotion` in playwright.config + chunked-batch gate. |
| BUG-AIF-001 | Action-items fold | HIGH — **RESOLVED 2026-07-11** (root cause found + fix verified; regression gate running) | **[★ RESOLVED 2026-07-11 (session 2): ROOT CAUSE = upstream Next.js App-Router bug — a `loading.tsx` Suspense boundary + the server-action deferred `router.refresh()` (a discarded action advances the router action queue against stale state, so the refresh never flushes; `vercel/next.js` issues #86151/#86055, fix PR #95391). NOT the "RSC truncation" theory below (that was a CDP `getResponseBody` misread — React had already consumed the body). PROVEN on a prod standalone build via a 3-run control matrix: (1) baseline 16.2.9 + loading.tsx → HANGS; (2) 16.2.9, both ancestor `loading.tsx` removed → AC-ActionItems PASSES 6.8s; (3) `next` 16.2.9 → 16.3.0-preview.5, loading.tsx intact → PASSES 6.1s. FIX LANDED = the Next bump (revert to 16.3.0 stable when it ships). Trail → memory `case-dialog-prod-refresh-layout-revalidate`; old handoff `docs/progress/bug-aif-001-linux-handoff.md`.]** On the standalone prod build **and Linux/Docker**, mutation dialogs on `manage/cases/[caseId]` do not close after a successful write. Repro: coordinator opens "Novo item de ação" (or the document-upload / tag menus), submits → the server action returns **200** and the row IS created + RLS-readable, but the dialog stays open (its `useEffect` close on `state.ok` + `router.refresh()` doesn't complete). Reproduces deterministically 3/3. Affects **UNCHANGED** tests (documents/tags actions are not in this diff), so it is NOT introduced by the fold. **DEEP-DIAGNOSED 2026-07-04 (lead) — prior "`page`→`layout` scope" theory DISPROVEN.** Instrumented diag on a CLEAN prod-standalone build: the action POST returns **200 text/x-component** but the response **body is never delivered** (`getResponseBody: No data found for resource`) → `useActionState` hangs on "Salvando…". The DB write lands; **no server/React/console error**. Ruled OUT by experiment: Next 16.2.9/Node/OS (minimal repro clean on Windows + Linux Node 22/24), the `@supabase/ssr` proxy (short-circuited → still truncates), `revalidatePath` path & scope (valid `/`, stale `[param]` route, and none-at-all all repaint fine — Next auto-re-renders the current route after any action regardless), response size/latency (300KB/1.2s fine), and the dialog wiring. `net::ERR_ABORTED` is a benign red herring (present even when the action resolves). Specific to the REAL app's RSC output on the **Windows prod-standalone** server — a minimal app can't reproduce it; NOT pinned to a one-liner. **OPEN:** whether Linux/production (Docker) is affected (untested) — strong signal it's a Windows-local-standalone artifact (dev works; minimal repro works on Linux). | Dialog closes + panel refreshes after a successful create (as on dev). | 200 response body never delivered; dialog stuck on "Salvando…". All GREEN on `npm run dev`. | `cases-extras.spec.ts` AC-Docs/AC-Tags/AC-ActionItems; **also confirmed F2** (2026-07-11, tester): `phase-f2-attachments.spec.ts` F2-1 (meeting-attachment upload dialog) + F2-4 (case-document upload dialog) — both `useActionState`-based upload dialogs, same "Enviando…" hang under standalone prod build; both **GREEN under `npm run dev`** (6/6 F2 pass). Confirms the artifact is generic to any `useActionState` mutation dialog on Windows-standalone, not case-page-specific. | lead | **RESOLVED 2026-07-11** — root cause found (upstream Next.js bug, fix PR #95391) + fix verified on a prod standalone build (3-run control matrix); fix landed = `next` 16.2.9 → 16.3.0-preview.5. **Regression gate PASSED (triaged 2026-07-11):** full E2E on the 16.3 prod build = **608 pass / 12 fail**; every failure is pre-existing, NONE a 16.3 regression — 6 are stale/failing form-builder specs that fail **IDENTICALLY on a 16.2.9 prod build** (e.g. `phase4-builder-smoke` `.check()`es the pre-rename label "Exibir somente sob condições"; the UI now reads "Visibilidade condicional" — ConditionBuilder/FBE refactor), the other 6 are known prod-build flakes (below the ~18–27 baseline). Trail → memory `case-dialog-prod-refresh-layout-revalidate`. |
| BUG-MEM-001 | S1·MEM (memberships collapse) | **BLOCKER** — real product regression, not test-only | `src/lib/queries/members.ts:64` `listMembers()` embeds `profiles(full_name, email)` inside a `.from('memberships').select(...)`. Since MEM added a second FK from `memberships` to `profiles` (`granted_by → profiles.id`, alongside the pre-existing `principal_id → profiles.id`), PostgREST can no longer resolve the unqualified `profiles(...)` embed and returns **`PGRST201`** ("Could not embed because more than one relationship was found for 'memberships' and 'profiles'"), HTTP **300**. `members.ts` does not check `{ error }` on the query result, so the failed call silently degrades to `(data ?? [])` → an EMPTY roster is rendered — "Esta comissão ainda não tem membros." — for a commission that genuinely has members. **Repro (live-verified, fresh `supabase db reset`, `npm run dev`):** sign in `chefe.ccih@test.local`, `GET /o/rede-a/c/ccih/manage/members` → renders 0 members. Direct PostgREST call under her real session JWT for `memberships?commission_id=eq.<ccih>&select=id,principal_id,role` (no embed) returns all **9** seeded rows correctly (RLS/`has_role`/predicate family all correct — NOT an RLS bug); the SAME query WITH the embed (`select=id,...,profiles(full_name,email),commission_member_titles(name)`) returns `{"code":"PGRST201", "hint":"Try changing 'profiles' to one of the following: 'profiles!memberships_granted_by_fkey', 'profiles!memberships_principal_id_fkey'"}`. This also breaks 3/4 of `phase3-admin-members.spec.ts` AC2 tests when re-run targeted (`-g "AC2"`): "adds an existing org user via the picker" / "removes a staff member via AlertDialog" / "AlertDialog Cancelar" all fail on `memberRow` not found (the newly-added member never appears because the roster read returns empty). **Fix (for `backend`, not applied by tester):** qualify the embed with the explicit FK hint per PostgREST's own error message — `profiles!memberships_principal_id_fkey(full_name, email)` — in `members.ts:64`. | `manage/members` roster renders all commission members (staff + staff_admin); AC2 add/remove flows see the added member appear. | Roster renders 0 members / "Esta comissão ainda não tem membros." regardless of actual membership count; `listMembers` query returns HTTP 300 PGRST201, silently swallowed to `[]`. | `phase3-admin-members.spec.ts` AC2 (3 of 4 tests); violates MEM plan §6 E2E acceptance "staff_admin adds AND removes a staff member" (docs/plans/memberships-collapse-s6-1.md §6) and Rule 9 (typed data-access — the query must handle its own ambiguity, not silently drop rows) | backend | **RESOLVED + VERIFIED 2026-07-13 (tester).** Backend fix (fix loop 1, TS-only, no schema change): `members.ts:66` qualified to `profiles!memberships_principal_id_fkey(...)` + real `{error}` throw. Backend also caught a SECOND ambiguous embed my repo-wide grep missed — nested one level deep inside the `memberships!memberships_commission_id_fkey(...)` embed in `commissions.ts:84,120` (`listCommissionsForAdmin`/`getCommissionForAdmin`, backing `/admin/audit`'s commission list + `/o/[org]/manage/comissoes/[slug]`) — same PGRST201 risk, same fix pattern applied + error throws added. `org.ts`/`pqs.ts` use the column-hint form (`profiles:principal_id(...)`) and were confirmed already safe (unchanged). **Re-verification (fresh `supabase db reset`, `npm run dev`, chromium `--workers=1`):** ran the previously-failing specs + the regression-confirm specs together — `e2e/hospital-admin-tier.spec.ts` **38/38** (was 35/38, HA-4/HA-K now green), `e2e/phase3-admin-members.spec.ts` **15/15** (was 12/15, AC2 now green), `e2e/mem-memberships-collapse.spec.ts` **7/7** (regression-clean), `e2e/phase13-audit.spec.ts` **27/27** (regression-clean, audit-verb repoint still holds) — **87/87 total, 0 failures.** The admin commission-list path (`listCommissionsForAdmin`/`getCommissionForAdmin`) is live-exercised by `phase3-admin-members` AC1 ("admin opens commission detail page") and `phase13-audit` AC-3f-platform (`/admin/audit`) — both green, confirming it now populates. **CLOSED.** |

> **1 OPEN — test-only, NON-BLOCKING, NOT F3** (BUG-F3E2E-002 full-suite contamination + Windows monolith backlog — see row; no product defect, F3 surface untouched). **BUG-F3E2E-001 RESOLVED 2026-07-12** (tester) — the nsp `networkidle` spec-fragility; the 2 timeouts + all 23 non-deliberate `networkidle` waits in the spec converted to web-first assertions, verified 32/32 on a fresh-reset prod-standalone run. **BUG-AIF-001 RESOLVED 2026-07-11** — root cause was an upstream **Next.js** App-Router bug (a `loading.tsx` Suspense boundary + the server-action deferred `router.refresh()`; fix [PR #95391](https://github.com/vercel/next.js/pull/95391)), **NOT** the RSC-truncation theory. Fix = `next` 16.2.9 → 16.3.0-preview.5, verified on a prod standalone build (baseline hangs → upgrade passes with `loading.tsx` intact). Same fix covers the whole class — the `useActionState` upload dialogs (F2-1/F2-4) and `ActivatePhaseDialog`/reassign (edit-meta was always immune: it closes via `setOpen`, not a transition). Regression gate PASSED (608 pass / 12 fail on the 16.3 prod build; every failure is pre-existing — the form-builder specs fail identically on a 16.2.9 prod build, the rest are known flakes — so no 16.3 regression). Move to 16.3.0 stable when it ships. Separately flagged: several form-builder E2E specs are stale after the ConditionBuilder label rename (test-only). **BUG-ADM-001 RESOLVED + verified** — archived → [bug-log-archive.md](docs/progress/bug-log-archive.md).

## Test Run Summary

<!-- Full historical run log (Phases 0–MT, ~78 rows) archived → docs/progress/test-run-archive.md.
     Keep only the most recent gate's rows here. -->

| Date | Phase | Specs | Passed | Failed | Notes |
| ---- | ----- | ----- | ------ | ------ | ----- |
| 2026-07-13 | **S1·MEM — BUG-MEM-001 fix re-verification** (fix loop 1: `members.ts:66` + `commissions.ts:84,120` ambiguous-embed fixes, TS-only, no schema change). Fresh `supabase db reset` (once), `npm run dev`, chromium `--workers=1`, all 4 relevant specs in one invocation. | `hospital-admin-tier` 38/38 · `phase3-admin-members` 15/15 · `mem-memberships-collapse` 7/7 · `phase13-audit` 27/27 | **87/87** | **0** | ✅ **BUG-MEM-001 CLOSED.** Previously-failing specs now fully green: `hospital-admin-tier` HA-4/HA-K (was 35/38) and `phase3-admin-members` AC2 (was 12/15) both recovered. Regression-confirmed clean: `mem-memberships-collapse` (7/7) and `phase13-audit` (27/27, audit-verb repoint still holds) unaffected by the fix. The admin commission-list path (`listCommissionsForAdmin`/`getCommissionForAdmin`, the second embed backend caught) is live-exercised by `phase3-admin-members` AC1 + `phase13-audit` AC-3f-platform (`/admin/audit`), both green — confirms it now populates. **MEM tester gate: GREEN.** Reporting to lead; lead runs the full `npm run e2e:prod` parity oracle next (not run by tester). |
| 2026-07-13 | **S1·MEM — single `memberships` collapse** — audit-verb E2E repoint (`e2e/phase13-audit.spec.ts` 6 named sites + `makeProbeCommission`/AC-3a helper repointed off the dropped `commission_members` table + `hospital-admin-tier.spec.ts`'s leaked-grant cleanup repointed off `organization_members`) + new `e2e/mem-memberships-collapse.spec.ts` (7 specs: nsp_org_admin PQS enrollment, direct-write 401/403 lockdown ×3, self-grant 42501, multi@ picker, audit-trail "Função concedida"). Fresh `supabase db reset`, `npm run dev`, chromium `--workers=1`. | `mem-memberships-collapse` 7/7 · `phase13-audit` 34/34 · `hospital-admin-tier` 35/38 | **76/79** | **3 (BUG-MEM-001, all `hospital-admin-tier.spec.ts` HA-4/HA-K)** | ✅ **Audit-verb hard-cut repoint fully clean** — grepped the whole `e2e/` tree post-fix, zero live references to `commission_member.*`/`organization_member.*`/`pqs_member.*` or the three dropped tables remain (only my own explanatory comments). `mem-memberships-collapse.spec.ts` all-green proves: PQS enrollment via `nsp-org/coordenadores` (nsp_org_admin authority arm), `/rest/v1/memberships` INSERT/UPDATE/DELETE all reject 401/403 under a real staff_admin/org_admin session, `grant_role` self-grant rejects 42501 (never a raw Postgres leak), `multi@test.local` still resolves both ccih+farmacia cards, and a grant renders as "Função concedida" / entity "Função" on the audit page end-to-end. ❌ **Found BUG-MEM-001 (BLOCKER, filed above)**: `listMembers()` (`members.ts:64`) embeds `profiles(...)` off `memberships`, now ambiguous (2 FKs to `profiles`) → PGRST201 → silently empty roster. Confirmed via live direct-REST reproduction that RLS/`has_role`/predicate layer is 100% correct (un-embedded read returns all 9 CCIH rows under the real session) — the bug is narrowly the embed syntax. Breaks 3/4 `phase3-admin-members.spec.ts` AC2 tests + 3 `hospital-admin-tier.spec.ts` HA-4/HA-K tests that read the roster after `chefe.ccih` signs in fresh. **STOPPED here per the spawn contract** — handing to backend; will re-verify once fixed, then re-run the focused suite. |
| 2026-07-12 | **BUG-F3E2E-001 fix + e2e `networkidle` purge** (spec-side only; no app code). Prod-standalone (`db reset --local` + reused `next build` standalone), chromium `--workers=1`, `unset CI`. | `nsp-per-hospital` 32/32; +21 specs converted | **—** | **0 conv-reg** | ✅ **RESOLVED.** (1) **nsp: 30/32 → 32/32 (38.3s)** — the 2 deterministic 30s `networkidle` timeouts (AC-1b event-PHI + AC-5 coordinator PHI) fixed via web-first `expect(getByText(MRN)).toBeVisible()`; 23 sites converted, 1 network-leak-audit settle kept. (2) **Purged `networkidle` across 21 more specs (158 sites → web-first)**, +3 deliberate preserved (perf-sweep `clickNextPage`, phase13-audit side-effect settle, nsp network-leak audit). eslint 0/0 + `tsc` clean. Full prod-suite run hit the documented Windows monolith collapse (**73× `ERR_CONNECTION_REFUSED`**) — triaged: every failure is env/known-flake (collapse + case-access AC-2 contamination + perf-sweep P2-b flag), **ZERO conversion-caused**; all pre-collapse converted specs passed live, and the 6 collapse-hit converted specs (phase8-dashboard 24/24, phi-remediation, ui-batch, recommend-result, views-labels 7/7, wizard-others) all **re-verified GREEN in isolation** (fresh reset + GoTrue warmup). |
| 2026-07-12 | **F3 — Flexible-Forms Foundation** (ADR 0060) — tester E2E triage of the lead's full prod-standalone gate. F3 is backend/inert (3 bones migrations + 5 frozen inert answer tables + 4 **non-authorable** evaluator ops + OP_LABELS); no new user-facing flow → **no new Playwright specs** (acceptance covered by pgTAP `209_flexible_forms` **38/38** + golden dual-evaluator parity `20_conditions.sql` / `conditions.test.ts` **81/81** — tester concurs). Re-ran the 6 failing specs on fresh `db reset --local` prod-standalone batches (chromium, workers=1). | full suite (lead-run): 632p / 6f / 4skip / 31dnr (22.5m) | **632** | **6 → 0 F3 reg** | ✅ **F3 E2E VERDICT = GREEN.** All 6 triaged NON-F3 (`git diff` = F3 touches zero app routes / NSP / cases / meetings / participants / audit). **4/6 contamination/flake → PASS in isolation** (case-access AC-2 1.4s · ui-batch S1 2.2s · views-labels AC-4 =9 members 2.7s · perf P2-b 2.0s). **2/6 = nsp AC-1b/AC-5 deterministic prod-standalone `networkidle` spec-fragility** (nsp-alone 30/32 pass, all PHI assertions green) → pre-existing, not F3. Filed BUG-F3E2E-001/002 (both non-blocking). My 5-spec batch itself hit the documented Windows monolith backlog-collapse (`ERR_CONNECTION_REFUSED` cascade) → chunked into small fresh-server batches per memory. |
| 2026-07-11 | **F2 — Centralized Attachments** — new `e2e/phase-f2-attachments.spec.ts` (6 specs, all F2 acceptance criteria) + fold-in fixes to `cases-extras.spec.ts` (AC-Docs) and `phase11-interviews.spec.ts` (`getAttachments` helper + AC4's non-https-link assertion) broken by the F2 substrate swap. `npm run dev`, fresh `supabase db reset --local`, chromium `--workers=1`. | `phase-f2-attachments` 6/6 · `cases-extras` 8/8 · `phase11-interviews` 10/10 | **24/24** | **0** | ✅ ALL GREEN under `npm run dev`. Under the **prod-standalone** run recipe, F2-1 (meeting-attachment upload) and F2-4 (case-document upload) hang on the pre-existing **BUG-AIF-001** Windows-standalone artifact (row above) — the other 4 F2 specs (F2-2/3/5/6, none of which is a `useActionState` dialog) pass GREEN under prod-standalone too. Not a new bug; not app code touched. Two own-test bugs found+fixed en route (not filed — never shipped): (1) unescaped `(` `)` in dynamically-built `RegExp` from titles containing literal parens (e.g. "Transcrição assinada (rascunho)") silently failed to match; (2) headless Chromium serves a real `application/pdf` signed URL as a native `download` event, not a page navigation — `popup.url()` never commits, must race `context.waitForEvent('download')`. See PROGRESS.md F2 section below for the full acceptance-criteria mapping. |
| 2026-07-08 | **Meeting `held_at`/`held_end` (ADR 0062) — `meeting-held-time.spec.ts` selector + test-data fix; `next dev`, fresh `supabase db reset`, chromium `--workers=1`** | 4/5 tests were RED on the first tester run; ALL were spec-side, NOT app bugs (feature verified-correct in-browser by the lead). Fixed test-side: (1) picker helpers scoped the DatePicker button / "Hora" TimeField as descendants of the `<label>` but they are SIBLINGS (shared parent) → new `fieldContainer()` (label's parent via `xpath=..`) in `date-pickers.ts`, header "Editar" locators rewritten to the outer span (T1.2/T1.4); (2) T1.1/T1.2 picked a current-month day (15/10) that is in the FUTURE (today=8th) → HC082 rejection → added `monthsBack` to `pickDate`/`setDateTimeField` and pick the same day in the prior month (valid past `held_at`, same day-of-month, still ≠ schedule). No app code changed; tsc clean. | **meeting-held-time 5/5** | **0** | ✅ **ALL GREEN** on fresh reset. Covers: T1.1 mark-realizada with changed held date (header "Realizada em", schedule unchanged, held_at persisted, minute 15), T1.2 inline header edit persists (minute 45, schedule untouched), T1.3 future held_at → readable pt-BR error (not raw PG), no status flip, T1.4 post-Concluir read-only (no edit button) + `set_meeting_held_window` HC083 while em_assinatura, T1.5 keyboard-only transition (minute 30). **Gate closed:** the lead full-suite run then caught **2 REAL transition bugs** (future-schedule pre-fill → HC082; unmapped HC08x → generic error), both FE-fixed; re-verified meeting-held-time + phase10-meetings + cases-meetings-minor = **33 pass, 0 fail** (no spec edits). pgTAP `206` **21/21** (HC084 added). QA APPROVED; committed `bfce6e8`. |
| 2026-07-08 | **UI/layout fixes batch (ad-hoc) — scoped pass, NOT a formal gate** (route-move F8 spec fixes + new `ui-batch-2026-07.spec.ts` + F1 badge-rename assertion sweep). Detail → [ui-batch-2026-07.md](docs/progress/ui-batch-2026-07.md). | ui-batch 10/10 · phase10 15/15 · hospital-admin-tier 38/38 · meeting-held-time+cases-meetings-minor 18/18 | **81/81** | **0** | ✅ ALL GREEN (each spec/small group on a fresh reset). Full regression NOT run (scoped). Env note: running all affected specs in ONE invocation flakes 4–6 local-GoTrue login-timeouts; splitting clears it. |

> **Most recent CLOSED gate: Meeting `held_at`/`held_end` (ADR 0062, above) — pgTAP 21/21 + meeting E2E 33 pass; QA APPROVED; committed `bfce6e8` (remote `db push` deferred).** Prior closed gate: Administrativo delegated-capability role (ADR 0061) — feature E2E 10/10 + full regr 574 pass (0 reg), QA APPROVED, BUG-ADM-001 fixed. Full run-by-run history + prior gates → [test-run-archive.md](docs/progress/test-run-archive.md) · [meeting-held-time.md](docs/progress/meeting-held-time.md) · [administrativo.md](docs/progress/administrativo.md).

## QA Verdicts

<!-- One row per phase/feature, ONE LINE only: verdict + date + link to docs/reviews/*.md
     (the full analysis already lives there — do not restate rationale here, and do not
     copy it to docs/progress/qa-verdicts-archive.md either, that's redundant with the
     review file). Legacy rows predating this rule may still point at qa-verdicts-archive.md. -->

| Phase / Feature | Verdict | Report | Notes |
| --------------- | ------- | ------ | ----- |
| S1·MEM — Single `memberships` collapse (ADR 0071/0075; S0 §I) | ✅ **APPROVED** (2026-07-13) | [report](docs/reviews/memberships-collapse-review.md) | 0 BLOCKER · 0 MAJOR · 3 minor (cheap; lead's discretion pre-Record). 145-site RLS collapse **live-verified on the running DB**: `memberships` has exactly 1 SELECT policy, `authenticated` holds SELECT only (no DML grant, no anon/PUBLIC) — pgTAP `224` §2 proves direct INSERT/UPDATE/DELETE as org_admin/staff_admin/nsp_org_admin all `42501`; all 3 legacy tables dropped with **0** residual policy/function references. Door arms = incumbent **verbatim** (cross-checked each `assign_*`/`add_pqs_member`/commission-write authority) — no widening; `_deny_self_grant` stays 42501, HC0G1 anti-lockout holds, staff cannot self-escalate. Predicate family: 18 membership-readers all resolve via `has_role` (secdef+owner=postgres+search_path-pinned, t19 grants, no PUBLIC leak, `is_active` never dropped); the **missed predicate** `is_entitled_document_approver` (read `commission_members`, absent from the spec's list) was caught+repointed and locked by pgTAP §7.10/7.11. Shape CHECK exhaustive (`else false`; §10 all illegal shapes rejected). Unified `membership.*` audit is PHI-free (catalog scan: only comment matches) with legacy verbs hard-cut (§6.3); trigger fires for service-role writes too. ADR 0075 split sound (service-role direct-write RLS-exempt but audited; RLS-scoped removers via door). CASCADE fix bounded to the **only** 2 `profiles` policies, restored byte-for-byte. BUG-MEM-001 embed disambiguation correct, no other ambiguous embed. `case_access` kept separate (no role coupling, §12). minors: (m1) grant-vs-revoke `is_admin()` asymmetry on commission arms — a safe **narrowing**, no live caller, but should be commented/symmetric + noted in ADR 0075; (m2) `is_pqs_operator_of` bare uses `auth.uid()` not `(select auth.uid())` (InitPlan nit); (m3) `224` §7.7/7.8/7.9 use `is not null` smoke not true/false pairs for `is_org_level_admin_within`/`is_hospital_member_of`/`is_org_member` (test-hardening follow-up). |
| F3 — Flexible-Forms Foundation (ADR 0060/0065) | ✅ **APPROVED** (2026-07-12) | [report](docs/reviews/phase-F3-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 4 INFO (forward-notes for FF phases). Two hardest invariants live-verified on the running DB: **(a) Rule-3 parity** — `app.eval_condition` spot-checked on the 9 trickiest vectors (no-coercion, 0-vs-empty, array-membership, missing) all match golden + the TS side (Vitest `conditions.test.ts` **81/81** re-run); the 4 new ops are **non-authorable** (`is_valid_condition` allow-list UNCHANGED; live `is_valid_visibility('contains'/'is_empty')`=false, `equals`=true) — evaluator-only, builder pickers untouched (OP_LABELS-only, `visible_when` stays visibility-only); `eval_condition` stays IMMUTABLE+search_path-pinned; no `is_true/is_false`. **(b) K9 write-inert RLS** on all six inert tables — live `has_table_privilege` matrix: RLS on, SELECT grant present, INSERT/UPDATE/DELETE **absent**, 1 SELECT policy, 0 write policies. Both `item_type` constraints widened (value enum 15 + shape CHECK `required=false` arms + `ELSE false`); Flag-5 by construction (`response_required_complete` has no item-type filter, live-verified); `clone_form_version` diffed vs prior authority = faithful, **zero column-carry regression** (Rule 5). No new PHI surface (Rule 12); `answer_references.participant_id` id-only. No file-type, no `phi_policy`, `supersedes_id` forward-note only. INFO: FF-2/FF-3 must extend clone to copy the version-scoped inert defn tables; FF-5 must wire PHI-read audit on `answer_references`; manual SQL↔JSON vector sync; `answer_matrix_cells` coherence deferred. |
| F2 — Centralized Attachments (ADR 0063) | ✅ **APPROVED** (2026-07-11) | [report](docs/reviews/phase-F2-review.md) | 0 BLOCKER · 0 MAJOR · 3 MINOR · 4 INFO. PHI door proven end-to-end: `attachments-phi` has **no** authenticated SELECT policy; `open_attachment` is NULL-out-of-scope (0 rows/0 audit on denial, exactly 1 `attachment.read` per allowed phi open); no adapter/panel/query signs a phi blob outside the audited door (service-role `server-only`). K9 grant+policy pairs present + pgTAP-locked (F1's blocking lesson closed); HC096 immutability guard, atomic fold-in FK-repoint (RESTRICT/SET NULL preserved; `get_referral_detail` transparent), D10 disposal (HC097/HC098 + legal-hold skip), audit triple-mirror + forge-guard all live-locked in `208` (46/46) + `191`/`197`. Rules 6/7/8/9/10/11/12 clean; flag OFF (migration)/ON (seed). MINORs: dead tier-unaware `getMeetingAttachmentDownloadUrl` (no call sites, cannot leak); interview-arm case-scoping + action_item non-committee scopes unasserted by pgTAP (code correct, coverage nit). |
| F1 — Case-Participants E0 (ADR 0064/0066) | ✅ **APPROVED** (CHANGES→APPROVED same day, 2026-07-10) | [report](docs/reviews/phase-F1-review.md) | 0 BLOCKER · 1 MAJOR · 2 MINOR · 3 INFO — **all closed in fix loop**. MAJOR-1 (7 new non-PHI tables shipped RLS SELECT policies `to authenticated` with **no table `GRANT`** → inert; direct read = `permission denied`) fixed: `grant select` added on all 7 (live `has_table_privilege`=`t`; PHI tables still door-only). MINOR-1: catalog write grants added (org-admin-gated), dead `case_participants_write` policy dropped (writes DEFINER-only, INSERT still ungranted). MINOR-2/INFO-1 comment fixes. Regression lock: new K9 block (+9) exercises the grant+policy pair as `authenticated` (`set local role`+`claims_for`, not superuser/DEFINER) — real lock, fails if grant reverted; pgTAP 1913/1913. Class-1 PHI isolation, R5, HC094/HC095, disposal/xref purge, t19 REVOKE, flags-OFF all live-verified clean. |
| Meeting actual-occurrence time `held_at`/`held_end` (ADR 0062) | ✅ **APPROVED** (2026-07-08) | [report](docs/reviews/meeting-held-time-review.md) | 0 BLOCKER · 0 MAJOR · 2 MINOR (defensive, unreachable from UI). DEFINER hygiene + grants + `realizada`-only edit gate + no-schedule-overwrite + non-leaking audit diff all live-verified; pgTAP 19/19, meeting E2E 33 pass. |
| Administrativo delegated-capability role (ADR 0061) | ✅ **APPROVED** (2026-07-08) | [report](docs/reviews/administrativo-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 3 INFO. All §5 leak guardrails + escalation/PHI checks verified live; pgTAP 45/45, E2E 574 pass. |
| Form-Builder Enhancements batch (Departments · Flagged/aggregate results · Others · wizard UX · meeting participants) | ✅ **APPROVED** (2026-07-07) | [report](docs/reviews/adjustments-batch-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 3 INFO. All 10 deliverables met; RLS/Rule-3/Rule-11 verified; batch E2E 29/29. |
| 17 — Controlled-Document Lifecycle (Gestão de Documentos Controlados) | ✅ **APPROVED** (2026-07-06) | [report](docs/reviews/phase-17-review.md) | 0 BLOCKER · 0 MAJOR · 3 MINOR (cleared before Record) · 4 INFO. pgTAP 47/47, E2E 14/14. |
| 15 — Quality Indicators (Indicadores de Qualidade) | ✅ **APPROVED** (2026-07-06) | [report](docs/reviews/phase-15-review.md) | 0 BLOCKER · 0 MAJOR · 3 MINOR · 4 INFO. Derived==dashboard parity proven live; `can_write_capa` untouched. |
| Pre-Pilot DB Hardening — Wave 2 (WS-6 perf sweep: P2/P3/P4/P5) | ✅ **APPROVED** (2026-07-05) | [report](docs/reviews/pre-pilot-hardening-wave2-review.md) | 0 BLOCKER · 1 MAJOR (cursor `.or()` injection, fixed pre-deploy in `a2a7fab`) · 1 MINOR · 3 INFO. |
| Pre-Pilot DB Hardening — Wave 1 (C-1…C-6, H-8; D1/D2/D4/D6-flip/D7/D9; P1/P9/P10) | ✅ **APPROVED** (2026-07-05) | [report](docs/reviews/pre-pilot-hardening-wave1-review.md) | 0 BLOCKER · 0 MAJOR · 1 MINOR · 4 INFO. Full critical set closed, every blocking invariant live-DB verified. |
| Phase B — NSP-per-hospital + `nsp_org_admin` (ADR 0052) | ✅ **APPROVED** (2026-07-03; CHANGES→APPROVED same day) | [report](docs/reviews/nsp-per-hospital-review.md) | 1 BLOCKER (RESOLVED) · 0 MAJOR · 1 non-blocking docs note. Security design sound; dual-hospital isolation proven. |
| Phase A — Hospital-admin tier, 4-tier audit & committee titles (ADR 0051) | **CHANGES REQUESTED** (2026-07-03) | [report](docs/reviews/hospital-admin-tier-review.md) | 0 BLOCKER · 2 MAJOR (both fixed) · 3 MINOR. Security core sound (pgTAP 184/187/188). |
| Action-Items Fold + `visibility_scope` + Case-Access Expiry (ADR 0050) | **APPROVED** (2026-07-02) | [report](docs/reviews/action-items-fold-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 4 INFO. Verified live against the DB: existence leak closed (scope matrix on hub + both satellites); guard force-restricts `source_type='case'` → `case_restricted`; ADR-0033-D4 write-grantee preserved; six-consulter expiry incl. referral-PHI arm (Rule 12); folded rows gain audit (Rule 11); frontend Rule 9/10 + a11y clean. Full notes → [qa-verdicts-archive.md](docs/progress/qa-verdicts-archive.md). |
| Shared (non-PHI) `action_items` table (Option A) | **APPROVED** (2026-07-02) | [report](docs/reviews/shared-action-items-review.md) | 0 BLOCKER · 0 MAJOR · 1 NIT + 2 OBS. Verified live: RLS enabled on all 5 tables; hub SELECT byte-identical to the dropped `meeting_action_items_select`; satellites SELECT-only, DEFINER-only writes; all 8 RPCs DEFINER + no anon EXECUTE (t19); audit added (Rule 11); no PHI (Rule 12). Full notes → [qa-verdicts-archive.md](docs/progress/qa-verdicts-archive.md). |
| Member Overview & "Meus itens de ação" | **APPROVED** (2026-07-02) | [report](docs/reviews/member-overview-action-items-review.md) | 0 BLOCKER · 0 MAJOR · 2 MINOR (informational). Both DEFINER RPCs self-scoped + PHI-free + t19-guarded; Count 1 faithful to `can_read_case`; flag-off omits/zeroes; frontend Rule 9/10 + a11y + strict-TS clean. Full notes → [qa-verdicts-archive.md](docs/progress/qa-verdicts-archive.md). |
| User Registration & Identity Management | ✅ APPROVED (CHANGES→APPROVED, re-re-review 2026-07-01) | [report](docs/reviews/user-registration-review.md) | 1 BLOCKER (B1: profiles peer-branch bypassed the `app.is_active()` fold) + 4 MINORs — all resolved + independently re-verified. BUG-UREG-004 attributed to **M4** (invalid Go template), not B1. Full trail → [user-registration.md](docs/progress/user-registration.md). |
| ad-hoc-narratives | **APPROVED** (2026-07-01) | [report](docs/reviews/ad-hoc-narratives-review.md) | 0 BLOCKER · 1 MAJOR + 2 MINOR cleared. `add_ad_hoc_narrative` DEFINER RPC; reverses ADR 0032 D7 for open cases. pgTAP 1219 · E2E 5/5. → [ad-hoc-narratives.md](docs/progress/ad-hoc-narratives.md) |
| answer-model-v2 | **APPROVED** (2026-07-01) | [report](docs/reviews/answer-model-v2-review.md) | Uniform answer row + typed scalar cols + instance-ready answer-key scaffolding; evaluator unchanged (Rule 3). pgTAP 1205 · E2E 6/6. ADRs [0045](docs/decisions/0045-answer-model-v2.md)/[0046](docs/decisions/0046-forward-compat-form-capabilities.md) |
| form-model-norm | **APPROVED** (2026-07-01) | [report](docs/reviews/form-model-normalization-review.md) | `form_items.options` JSONB → normalized `form_item_options` + `answer_selected_options`; evaluator byte-for-byte via `app.answer_map`. → [form-model-normalization.md](docs/progress/form-model-normalization.md) |
| result-rec | **APPROVED** (2026-06-26) | [report](docs/reviews/result-rec-review.md) | `recommend_when` combinable answer/result groups; recommend from an earlier phase's result; suggestion-only, zero evaluator drift. pgTAP 1122 · E2E 431/0. ADR [0043](docs/decisions/0043-phase-result-based-recommendation.md) |
| NSP-per-org | **APPROVED** (2026-06-25; superseded by NSP-per-hospital) | A [core](docs/reviews/nsp-per-org-a-review.md) · B [whole](docs/reviews/nsp-per-org-b-review.md) | Per-org PQS roster + org-scoped PHI doors; restores NSP + referral under multi-tenancy. pgTAP 1102 · E2E 421/0. ADR [0042](docs/decisions/0042-nsp-per-org.md) |
| Multi-Tenancy | **APPROVED** (2026-06-25) | [report](docs/reviews/multitenancy-review.md) | 0/0. platform_admin wall + multi-org PHI guard verified; cross-org isolation proven. INFO: remove legacy `getCommissionAccess(slug)`. |
| Form Builder Enhancements | **APPROVED** (2026-06-23) | [report](docs/reviews/form-builder-enhancements-review.md) | 0/0. MAJOR-1 (number-target lexical compare) resolved. ✅ BUG-FBE-004 RESOLVED (`deb436c`, re-verified 2026-06-30). |
| `case_phase_results` | **APPROVED** (2026-06-23) | [report](docs/reviews/phase-results-review.md) | 0/0. RLS + dual-entry override correct; non-PHI; flag OFF. |
| 23 — Patient Identity | **APPROVED** (2026-06-22) | [report](docs/reviews/phase-23-review.md) | 0/0. `patient_xref` QPS-only; audit truncates MRN. |
| `case_patient` (ADR 0038) | **APPROVED** (2026-06-22) | [report](docs/reviews/case-patient-review.md) | 0/0. Audited single-door; `dispose_case_phi`; flag OFF. |
| 22 — Inter-Committee Referrals | **APPROVED** (2026-06-21) | [report](docs/reviews/phase-22-review.md) | 0/0. `referral_patient` single-door; 3-layer PHI lockdown; flag OFF. |
| PHI/HIPAA Remediation (WS0–E) | **APPROVED** (2026-06-20) | [report](docs/reviews/phi-remediation-review.md) | 0/0. `event_patient` lockdown; 22 PHI free-text cols → Phase-19 AC. |
| 14b–14d — Triage/RCA/CAPA | CHANGES→**APPROVED** (2026-06-18) | [report](docs/reviews/phase-14-review.md) | BLOCKER-1 + MINOR-1 resolved; pgTAP 516/516. |
| 14a — NSP Foundation | CHANGES→**APPROVED** (2026-06-18) | [report](docs/reviews/phase-14a-review.md) | M1 (`pqs_department` SELECT) + N1 fixed. |
| 13 — Audit Trail | **APPROVED** (2026-06-18) | inline | 0/0. Append-only guard + hash chain + data-min allow-list; MINOR-1 resolved. |
| 12 — Case Timeline | **APPROVED** (2026-06-16) | [report](docs/reviews/phase-12-review.md) | 0/0. Read-only composition; MINOR-1 resolved. |
| 11 — Interviews | **APPROVED** (2026-06-15) | [report](docs/reviews/phase-11-review.md) | 0/0. `can_write_interview` shape verified; P11-MINOR-1 resolved. |
| 10 — Meetings | **APPROVED** (2026-06-15) | [report](docs/reviews/phase-10-review.md) | 0/0. Sign-own-row bypass + `can_sign_meeting` verified; 2 MINOR pre-record. |
| Case Access Control | **APPROVED** (2026-06-19) | [report](docs/reviews/case-access-control-review.md) | 0/0. CA-001 BLOCKER + CA-002 MAJOR resolved; 4 INFO → Follow-ups. |
| Case Narratives | **CHANGES REQUESTED** (2026-06-19) | [report](docs/reviews/case-narratives-review.md) | BLOCK-1 (`form.set` id→narrativeTypeId) + BLOCK-2 (aria-label). |
| Case data-model (D1–D15) | **APPROVED** (2026-06-14) | [report](docs/reviews/case-model-adjustments-review.md) | 0/0. Dropped-helper cleared; 2 MINOR. |
| Cases-Extras (R1–R5) | **APPROVED** (2026-06-14) | [report](docs/reviews/cases-extras-review.md) | 0/0. RLS on 6 tables + storage; MINOR PII note. |
| 8 — Dashboards | **APPROVED** (2026-06-14) | [report](docs/reviews/phase-8-review.md) | 0/0. in_progress-answers invariant holds; MINOR-1/2 resolved in-phase. |
| 7 — Multi-Phase Cases | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-7-review.md) | 0/0. Isolation invariant confirmed; HC0xx remap complete. |
| 6 — Sign-offs | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-6-review.md) | P6-001 BLOCKER resolved in-phase; signoff definer rewrite verified. |
| 5 — Wizard | **APPROVED** (2026-06-13) | [report](docs/reviews/phase-5-review.md) | 0/0. MINOR-1/2 → Phase 6. |
| 4 — Form Builder | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-4-review.md) | MAJOR-1 keyboard test + 2 MINOR resolved. |
| 3 — Admin/Users | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-3-review.md) | 2 MINOR resolved; INFO-1 carry. |
| 2 — Auth & Shell | **APPROVED** + re-review (2026-06-12) | [report](docs/reviews/phase-2-review.md) | MAJOR-1 + BLOCKER P2-002 (login race) fixed + load-verified. |
| 1 — Schema/RLS | **APPROVED** (2026-06-12) | [report](docs/reviews/phase-1-review.md) | M8: 2 MAJOR + 3 MINOR resolved. |
| 0 — Scaffolding | **APPROVED** | [report](docs/reviews/phase-0-review.md) | MINOR lang=pt-BR; ADR 0001 note. |

## Decisions

<!-- One line per decision; full rationale in docs/decisions/ (ADR) + docs/progress/decisions-log.md -->

| Date | Decision | Ref |
| ---- | -------- | --- |
| 2026-07-12 | **Pre-pilot release scope expansion (product-owner decision).** Pull **12** initiatives from post-pilot/deferred into the pilot release: **Phases 20–21**; **Referrals v2 R1→R5** (all); **Interviews v2**; **Ethics E1→E2→E3** (E1 = the m2 flag gate + respondent-exclusion RLS, first); **action-items hub satellites** + **case cross-link UI**; **§6.1 single-`memberships` collapse**; **supersession correction engine + UX** (0060 Gap 38). Only **Phases 18–19 stay post-pilot**; pilot ships later, after the whole block + Coolify deploy. Each item keeps its own ADR/plan/flag/Phase Gate; supersedes the *timing* in 0057/0064/0060/0050 + the §6.1 deferral. Specs still to author: Ethics E1/E2/E3, supersession, §6.1. | [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md) |
| 2026-07-12 | **Referrals v2 — Dialogue & Governance (pre-pilot expansion of Phase 22).** Evaluated a partner team's from-scratch referral data model vs the shipped module; adopt the missing **two-way dialogue** (Essential: `referral_messages` thread + `awaiting_information`/`waiting_on_committee_id`) + 12 deferred items (triage/SLA · reopen+answered/resolved split · `parent_referral_id` · assignments · committee-private notes · context versions · receipts · redaction · idempotency) across gated phases **R0→R5**; **avoid** `row_version` (redundant w/ DEFINER+`FOR UPDATE`), `referral_status_events` (redundant w/ hash-chained audit), PHI-in-`structured_context` (Rule 12), `referral_participants` ACL (forks auth), referral-local outbox (→ Phase 20). Extends (never contradicts) the Phase-22 core; pre-pilot/reset-OK behind `case_referrals`; R1 pilot-critical; HC block `HC0A0+`. | [0037-A1](docs/decisions/0037-inter-committee-case-referrals.md) · [plan](docs/plans/referrals-v2-dialogue-governance.md) · [eval](docs/design/temp/referral_data_model_evaluation.md) |
| 2026-07-10 | **Pre-Pilot Foundations Program — one collision-free plan for ADR 0060 + 0063 + 0064-E0 + DB-hardening Waves 3+4.** Sequence F0 (design gate) → F1 participants → F2 attachments → Phase 16 → F3 flexible-forms → F-cleanup. Six resolved collisions: **C-α** two ratified polymorphism dialects (owner-dispatch + typed registry) **close D12**; **C-β** `attachment_subjects` re-keys to the `participants` registry (one subject vocabulary; forces F1-before-F2); **C-γ** one merged Rule-12 sensitivity taxonomy (patient-PHI ×3 + professional-identity + attachments tier/label); **C-δ** compose disposal (participant-keyed then attachment-D10), **drop D5 patient-master** (participant model + `patient_xref` suffice); **C-ε** **cancel D6/§6.3** metadata item-types (0060 keeps the CHECK enum); **C-ζ** `form_upload` stays inert, drop `form_items.phi_policy`. R1 verified: no `is_multi_org()` in-tree; case-PHI door is per-hospital PQS-operator + per-commission. HC high-water HC093 → allocate HC094+. All pre-pilot / reset-OK; flags OFF (m2 gate). Branch `feat/pre-pilot-foundations-plan`. | [program](docs/plans/pre-pilot-foundations-program.md) · [0060](docs/decisions/0060-flexible-forms-foundation.md) · [0063](docs/decisions/0063-centralized-attachments-substrate.md) · [0064](docs/decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Case subject generalization → participants / roles / professional registry / case-types (E0 foundation for the Ethics Committee + other non-patient-centered committees)** — ONE generalized `cases` model, never a forked `ethics_cases`/`patient_cases` root; full generic `participants` + `case_participant_roles` + `case_participants` (+ no-polymorphic-FK subtype tables); `professional_profiles` as its **own audited sensitivity class** (case-scoped RLS + audited reads, NOT the patient-PHI isolated-single-door apparatus); `case_patient` generalized **1:1 → N per case**, re-keyed `patient_identifiers(participant_id)`, isolation/audited-door/disposal preserved and reworked **while the flag is still OFF ⇒ zero prod PHI migration**; **dedicated** `case_types` + `case_type_terminology` (org-scoped; `primary_subject_kind` / `default_visibility_policy`; pt-BR label bundles). Amends Rule 12 (now **patient-PHI + professional-identity** classes) + supersedes ADR 0038 cardinality. Access-spine (E1) + ethics procedure (E2) explicitly deferred. Product-owner decisions taken 2026-07-09; **not implemented**; eval [case-generalization-evaluation.md](docs/design/case-generalization-evaluation.md) | [0064](docs/decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Centralized-attachments substrate — DMS-handoff evaluation → 14e refinement** — adopt into the 14e core: single authorizing owner + non-authorizing `attachment_references`, descriptive PHI-safe `attachment_subjects`, `document_group_id`/`supersedes_id` (versioning/redaction), `confidentiality_label`, `scan_status` malware gate, `legal_hold`+`dispose_attachment_phi`; RESERVE kind-policy lookup / per-doc grants / retention; REJECT materialized effective-permissions cache + security-groups + `app_resources` + OCR pipeline. Per-feature metadata via 1:1 extension tables stays a reserved additive path (14e D2). Revised core DDL → [attachments-core-schema-draft.md](docs/design/attachments-core-schema-draft.md); not implemented | [0063](docs/decisions/0063-centralized-attachments-substrate.md) |
| 2026-07-08 | **Administrativo delegated-capability role** — per-commission appointment + curated, finite capability menu (`schedule_meetings`/`create_cases`/`assign_case_phases`/`view_signoffs`) instead of a new role enum; escalation closed by construction (`_deny_self_grant`); phase-assignment auto-grant design REVERTED same day — assignment/creation now grants case READ, not write (coordinator `grant_case_access` stays the sole content-write path) | [0061](docs/decisions/0061-administrativo-delegated-role.md) |
| 2026-07-06 | **Coolify as the pre-Phase-9 dev/staging deployment target** — Dockerfile-only (Coolify's Traefik replaces the Phase-9-planned Caddy/compose stack), points at the existing remote Supabase Cloud project, `NEXT_PUBLIC_*` vars marked build-time. Not Phase-9 completion; runbook [deploy-coolify.md](docs/deploy-coolify.md) | [0059](docs/decisions/0059-coolify-deployment-target.md) |
| 2026-07-05 | **Phase 15/17 revision + pre-pilot re-sequencing** — build order **15 → 17 → 16** (documents pulled pre-pilot; pilot after 16; 18–21 post-pilot *(20–21 later pulled pre-pilot — [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md))*); both phases commission-owned + read-only DEFINER hospital rollups; derived indicators on option `code`s + `value_number`, hybrid taxa one-step; off-target→CAPA = **two-tier escalation** (PQS-operator "Abrir CAPA", hub fallback; CAPA write surface untouched); doc approvers = any active same-hospital user via an approval-row read arm, all-must-sign; forms metadata-only at publish; indicator SQLSTATEs from `HC084` | [0057](docs/decisions/0057-indicators-doc-control-replan.md) |
| 2026-07-06 | **Phase 15 derived-measurement compute** — `compute_derived_measurement` replicates the `dashboard_distributions` mechanics (submitted spine, option-`code` grouping, window) so **derived == dashboard by construction** (parity-by-construction, pgTAP-locked); hybrid taxa one-step (denominator inline) + preserve-on-recompute; comparator-driven classification | [0058](docs/decisions/0058-derived-measurement-compute.md) |
| 2026-07-02 | **Action-items fold + `visibility_scope` + case-access expiry** — scope-aware hub `SELECT` via `can_read_action_item`; fold `case_action_items` → `source_type='case'` (drop old table/RPCs; ADR 0033 D4 preserved); `case_access.expires_at`+`reason` filtered on all 6 consulters (incl. referral-PHI arm, Rule 12) | [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md) |
| 2026-07-01 | **Answer-Model v2** (planned) — uniform answer row + typed scalar cols (`value_number/date/time`) + instance-ready answer key (`group_instance_id`/`response_group_instances`); evaluator unchanged (Rule 3); plan [answer-model-v2.md](docs/plans/answer-model-v2.md) | [0045](docs/decisions/0045-answer-model-v2.md) |
| 2026-07-01 | **Forward-compat form capabilities** (planned) — `form_items.parent_item_id` + reserved hooks for repeating groups / file-signature-matrix answers / field confidentiality (structure only) + question default values | [0046](docs/decisions/0046-forward-compat-form-capabilities.md) |
| 2026-06-25 | **NSP-per-org** — bind the PQS roster + every PHI door to an organization (per-org `pqs_members`/`pqs_department`, `nsp_coordinator` grant via the role-CHECK seam, org-scoped doors + patient-index 4th surface, forbid cross-org referrals, vendor-walled erasure); lifts ADR 0041 amendment 10's interim multi-org PHI guard; split A (backend, pgTAP-gated) → B (FE + E2E) | [0042](docs/decisions/0042-nsp-per-org.md) |
| 2026-06-25 | **Multi-Tenancy** — pooled single-DB (organizations → hospitals → commissions); vendor `platform_admin` vs customer `org_admin`; RLS rewrite + 3-tier audit + multi-org PHI guard | [0041](docs/decisions/0041-multi-tenancy-organizations-hospitals.md) |
| 2026-06-20 | **PHI-remediation Round 3** — RCA-write severance (drop `is_admin` from 7 `rca_*_write`) + WS B audited free-text reads/PHI classification + WS E efficiency | plan §A4/B/E; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS C** — PHI retention & disposal (`dispose_event_phi`, one-shot HC056, constrained reason enum) | plan §C; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS A** — structured-identifier lockdown: real `pqs_members` roster, tight `can_read_event_patient`, single audited door, sever platform-admin from NSP PHI | plan §A; ADR 0030/0031 |
| 2026-06-20 | **Migration squash** → domain-partitioned dump-based baseline (replaced 86 incremental migrations; WS0) | plan §WS0 |
| 2026-06-19 | **Case Access Control** — per-case read/write ACL + attribution-driven full-case read; restrictive `can_read_case`; narrative lifecycle | [0033](docs/decisions/0033-case-access-control.md) |
| 2026-06-11 | Scaffolding & toolchain: Next 16/React 19, shadcn (radix/neutral), vitest ESM, Supabase CLI devDep, Chromium-only | [0001](docs/decisions/0001-scaffolding-and-toolchain.md) |
| 2026-06-11 | `jsdom` pinned `^25` (jsdom@27 ESM dep crashes Vitest forks on Node 20) | – |
| 2026-06-12 | Admin claim via custom access-token hook reading `profiles.is_admin`; RLS helper DB-read fallback | [0002](docs/decisions/0002-admin-claim-access-token-hook.md) |
| 2026-06-12 | DB tests via pgTAP (`npx supabase test db`) | [0003](docs/decisions/0003-pgtap-for-db-tests.md) |
| 2026-06-12 | Sign-off enforcement gated by an `app.feature_flags` row read by `submit_response` | [0004](docs/decisions/0004-signoff-feature-flag.md) |
| 2026-06-12 | `visible_when` v1 = single condition (no AND/OR), CHECK-enforced shape + publish-time validation | [0005](docs/decisions/0005-visible-when-shape.md) |
| 2026-06-12 | Keep legacy env-var names while accepting new publishable/secret CLI keys | [0006](docs/decisions/0006-supabase-api-key-naming.md) |
| 2026-06-12 | Phase 2: `multi@test.local` as staff of both ccih+farmacia (commission-picker E2E); seed 8 users/8 memberships | – |
| 2026-06-12 | Middleware is a coarse auth gate; role-aware landing deferred to root `/` Server Component | [0007](docs/decisions/0007-middleware-coarse-gate-root-landing.md) |
| 2026-06-12 | GSAP 3.15.0 pinned for the auth-hero animation (dynamic import, aria-hidden, reduced-motion) | [0008](docs/decisions/0008-gsap-animation-dependency.md) |
| 2026-06-12 | Auth identity on the request hot path via LOCAL JWT verification (`getClaims()`, ES256/JWKS) | [0009](docs/decisions/0009-jwt-local-verification-gate.md) |
| 2026-06-12 | Phase 3: denormalize `email` (nullable citext) onto `public.profiles` (trigger-populated) | [0010](docs/decisions/0010-denormalize-email-on-profiles.md) |
| 2026-06-12 | Phase 4: position reorder via DEFERRABLE INITIALLY IMMEDIATE unique constraints + CASE swap | [0011](docs/decisions/0011-position-reorder-deferrable-swap.md) |
| 2026-06-12 | Phase 4: `clone_form_version` returns the existing draft when one exists (idempotent edit) | [0012](docs/decisions/0012-clone-returns-existing-draft.md) |
| 2026-06-12 | Phase 4: fixed latent Phase-1 RLS defect in `form_versions_staff_admin_write` WITH CHECK | [0013](docs/decisions/0013-form-versions-insert-rls-fix.md) |
| 2026-06-12 | Phase 5: response-fill via two security-invoker RPCs (`save_section_answers`, `start_or_resume_response`) | [0015](docs/decisions/0015-response-fill-rpcs.md) |
| 2026-06-13 | Phase 7 (P7-002): custom SQLSTATE class `P00xx` → `HC0xx` (HC010–HC022) | [0018](docs/decisions/0018-custom-sqlstate-class.md) |
| 2026-06-13 | Phase 7: Multi-phase cases — 4 new tables (process_templates/phases; cases/case_phases) | [0017](docs/decisions/0017-multi-phase-cases.md) |
| 2026-06-13 | Maintenance: the default (anchor) section may carry a title once a form has ≥2 sections | [0019](docs/decisions/0019-default-section-may-carry-title.md) |
| 2026-06-13 | Phase 8: dashboard-countable responses exclude case-phase responses (`case_phase_id IS NULL`) | [0020](docs/decisions/0020-dashboard-countable-responses.md) |
| 2026-06-14 | Cases-Extras R2: configurable per-committee case status (no longer fixed 3-state CHECK) | [0023](docs/decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Cases-Extras R1 documents+events, R3 tags, R4 action items | [0023](docs/decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Post-Phase-8: due dates for case phases (`default_due_days` + `case_phases.default_due_*`) | [0021](docs/decisions/0021-phase-due-dates.md) |
| 2026-06-14 | Case data-model adjustments — phase blocking, FIXED statuses, outcomes (supersedes R2) | plan `the-current-case-data-bright-hartmanis.md` |
| 2026-06-13 | Phase 6: staff_admin sign-off read via two narrow security-definer RPCs (`list_signoff_queue`, `get_response_for_signoff`) | [0016](docs/decisions/0016-signoff-definer-read-path.md) |
| 2026-06-16 | Phase 12: Case Timeline (read-only viz; no migration, reuses table RLS) | [0027](docs/decisions/0027-case-timeline.md) |
| 2026-06-15 | Phase 10: Meetings (B1–B5) — 8 migrations; 6-state lifecycle | [0025](docs/decisions/0025-meetings.md) |
| 2026-06-18 | Phase 14 batch: build 14b (Triage) → 14c (RCA) → 14d (CAPA) in dependency order as one build | accred-track §14b–14d |

## Follow-ups / Deferred Items

<!-- OPEN backlog only (reviewed at each phase start). Resolved [x] items archived →
     docs/progress/follow-ups-archive.md (full snapshot). -->

- [ ] **Ethics Committee & non-patient-centered case track — DESIGN LANDED, NOT IMPLEMENTED (2026-07-09). PULLED PRE-PILOT 2026-07-12 — ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md) (E1→E2→E3, access-spine first; each still needs its own ADR).** The platform's `cases` root already *is* a generic committee-matter engine; the one axiom still assuming a patient world is the **case subject** (`case_patient`, patient-only). This track generalizes the subject so a case can be **doctor-centered** (Ethics), patient-centered, or entity-centered — **one model, never a forked root.** Full evaluation + feature-by-feature comparison vs the external design → [case-generalization-evaluation.md](docs/design/case-generalization-evaluation.md); foundation decisions → ADR [0064](docs/decisions/0064-case-subject-generalization-participants.md) (product-owner-approved 2026-07-09). Ships as a new track behind flags (`case_participants`, `case_types`), OFF until in-phase flips. **Sequencing:**
  - **E0 — Foundation (ADR 0064) — SCHEDULED 2026-07-10 as phase F1 of the [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md)** (build plan + migration batching now in that program; sequenced **before** attachments F2): `participants` + `case_participant_roles` + `case_participants`; `professional_profiles` (own audited class); `case_types` + `case_type_terminology`; `case_patient` → N per case (`patient_identifiers(participant_id)`, done while flag OFF). Prereq for everything below. Amends ARCHITECTURE.md Rule 2 (schema) + Rule 12 (adds the professional-identity sensitivity class) **at implementation time**.
  - **E1 — Access spine (ADR TBD, ~0065) — BUILD BEFORE ANY REAL COMPLAINT DATA (external-doc §7 warning):** confidentiality levels; wire `case_types.default_visibility_policy` → `can_read_case` (Ethics = explicit-grants-only, committee membership ≠ case access); **respondent-exclusion** RLS (a doctor can't read their own case); `case_conflict_declarations` + `case_recusals` folded into `can_read_case` (RLS-enforced, not UI); document-level access grants. The external doc §14.4 checklist is a good `can_read_case` acceptance spec.
  - **E2 — Ethics procedure (ADR TBD):** `ethics_case_details` (admissibility/notification/response deadlines); `ethics_allegations` + `ethics_findings`; `ethics_notifications` (deadlines); `case_decisions` + `ethics_decision_details` (**sanctions**, **CRM/CFM** external reporting, decision letter, appeal window); `case_votes`; `ethics_hearings`; `ethics_appeals`. Some may debut as narrative/form phases and graduate to tables where deadlines/querying demand.
  - **E3 — Terminology/UX + accreditation linkage:** ethics label bundles, procedural-timeline categories, dashboards; link ethics decisions/CAPA to the standards crosswalk (Phase 16) as evidence.
  - **Open sub-decisions carried in ADR 0064 §"Open items":** `form_responses.target_case_participant_id` (participant-targeted forms); assignment-role vocabulary on `case_phases`; reconcile the interview-scoped `case_interview_subjects` with case-level participants; whether a doctor-under-review needs its own audited door vs shared reader.
  - **Same E0 work unlocks** credentialing/privileging, risk/legal, and enriched sentinel-event review (all currently blocked by the patient-subject axiom).
  - **Design reviews (2026-07-09):** `backend` **SOUND WITH CHANGES** ([adr-0064-backend-review.md](docs/reviews/adr-0064-backend-review.md)) + `qa` **CHANGES REQUESTED** ([adr-0064-qa-review.md](docs/reviews/adr-0064-qa-review.md)) — design sound, no BLOCKER, E0-before-E1 valid; **all findings resolved in ADR 0064 §"Review outcomes & resolutions"** (documentary/spec, design unchanged). Load-bearing corrections a future E0 engineer MUST honor: **R2** (denormalize `organization_id` onto `cases` — no org col today, so no pure composite FK), **R3** (re-key couples to `patient_index`/`patient_xref` ADR 0039 — disposal must purge per-participant), **R5** (`UNIQUE(id,participant_type)` + subtype composite-FK+CHECK — the class-separation invariant), **R6** (E1 anti-recursion), **M1/M2** (Rule 12 final wording + professional-identity erasure posture). **⚠ R1 caveat:** the backend review's `is_multi_org()`/migration-`…629000000` claim **could not be verified** (no such symbol/migration in-tree) — E0 must verify the *actual* PHI-door tenant gate against the live catalog, not trust that detail. **m2 HARD GATE:** do not flip the `case_participants`/`case_types` flags on real ethics data until E1's respondent-exclusion RLS lands. Owned by lead (scheduling) + backend/frontend when picked up.
  - **Doc-hygiene spun off:** CLAUDE.md §3's Rule-12 summary still says "PHI lives in exactly two modules" — stale since ADR 0038 (case = 3rd); must become "three patient-PHI modules + professional-identity class" at E0 (or sooner). Flagged as a separate task.

- [ ] **P7 — `audit_log` range-partitioning DEFERRED (lead decision 2026-07-05, pre-pilot hardening WS-5).** Declarative range-partition-by-time is **structurally incompatible** with the audit chain: Postgres forces the partition key into every unique index, so the 4 partial-unique per-chain `seq` indexes would become `(chain_key, seq, occurred_at)` — permitting the same `seq` in different months and destroying the global-per-chain monotonic-seq invariant `verify_audit_chain` + the hash chain rely on. A wrong partition on the tamper-evidence table is worse than none. **If audit volume ever demands partitioning, the correct axis is `chain_key` LIST/HASH partitioning (NOT time)** — a designed track with the chain-integrity model reworked, not a cheap pre-launch win. Pairs with P6 (checkpointed `verify_audit_chain`) + the §6.5 evidence work (pre-Phase-19). Owned by lead (scheduling) + backend.
- [ ] **D3 — jsonb/array → junction-table normalization DEFERRED to its own scoped plan (user decision 2026-07-05, pre-pilot hardening WS-3b).** Move FK-bearing data out of `case_phases`/`process_template_phases.allowed_result_ids` (46 refs), `result_ruleset` result-ids (62), and `blocks integer[]` (120) into junction tables. **No reachable defect** — the shipped `reorder_template_phase` remaps `blocks` atomically; D3 prevents *future* dangling-`phase_results.id` UUIDs. Heaviest item (~14 functions across the case-phase result/recommendation engine; the `result_ruleset` structured jsonb is the awkward part — junction vs. jsonb+validation-trigger is the open design decision). Do it as a WS-1-style scoped plan **pre-pilot while data is disposable** (reset-OK). **Dispositioned 2026-07-10 → [Pre-Pilot Foundations Program](docs/plans/pre-pilot-foundations-program.md) F-cleanup** (the sibling structural tracks are resolved there: D12 closed, D5/§6.2 & D6/§6.3 superseded/cancelled, P6 stays deferred). Owned by lead (scheduling) + backend.
- [ ] **D7 — thread `p_hospital_id` for hospital-scoped NSP vocab. RE-SCOPED 2026-07-07: NOT backend-only — needs FE + a product decision (deferred).** Investigation (Batch B) found the vocab-CRUD call sites in `src/lib/safety/triage-actions.ts` (`create/update/reorder/archive` × EventType/SentinelCriterion) omit `p_hospital_id` → hit the GLOBAL arm (`can_curate_pqs_vocab(NULL)` = `is_admin` only) → non-admin hospital operator gets 42501. But it can't be threaded server-side: "current NSP hospital" is resolved from the **`?hospital=` URL param** (`nsp-hospital-scope.ts`), which a `'use server'` action can't read, and there's no cookie/server persistence; for a **multi-hospital** operator the hospital is a genuine UI choice. Also the vocab **UI is currently GLOBAL** (`nsp/configuracoes` documents vocab as shared, no hospital prop, global list reads) — so scoping it needs (a) a product decision on global-vs-per-hospital vocab semantics + list behavior (global ∪ hospital, per-row editability) and (b) wiring `hospitalId` through page→managers→dialog→actions (frontend). Owned by frontend + backend + product when scheduled.
- [ ] **WS-3c FE follow-up — manual-CAPA UI should pass `p_hospital_id` for MULTI-hospital operators (backend, non-breaking). BLOCKED — confirmed 2026-07-07 (Batch B).** No multi-hospital manual-CAPA UI exists to drive it: `open_capa_plan('manual', …)` auto-derives the hospital for the common single-hospital operator (works today); the multi-hospital branch raises **HC083** and has no caller. Stays deferred until a multi-hospital manual-CAPA UI is built — then thread the chosen hospital into `openCapaPlan` (`src/lib/safety/capa-actions.ts`). Owned by backend (+ frontend).
- [ ] **WS-4 C-6 FE follow-ups — PHI-disposal UI + copy (backend + frontend; before the pilot exposes disposal UI).** The backend is complete; three product-facing pieces remain (QA INFO-3, ADR 0056): (a) **`dispose_meeting_minutes` action + UI** — the standalone meeting-minutes disposal RPC exists but has no server-action/UI; (b) **disposal copy must reflect the NARROWED claim** — user-facing text must NOT say "tudo apagado"/fully-erased (storage blobs are retained encrypted under the 20-yr LGPD/ANVISA/CFM retention regime; DB-side PHI is erased) — copy should state DB-side PHI removed + attachments retained under retention; (c) **non-PHI "motivo da recusa" field** — after §6.4, `decline_note` is nulled to non-PHI referral readers, so a separate non-PHI decline-reason surface is needed for the metadata view. Owned by frontend (+ backend for the action). See [pre-pilot-hardening-wave1.md](docs/progress/pre-pilot-hardening-wave1.md).

- [ ] **BUG-AIF-001 — Linux/production safety confirmation (the only open sub-question).** Deep-diagnosed 2026-07-04 (see Bug Log + memory `case-dialog-prod-refresh-layout-revalidate`): real Windows-prod-standalone truncation of RSC-bearing server-action responses; NOT Next-version/proxy/revalidate/size. Remaining unknown = whether the Linux Docker deploy target is affected. Decisive test = build+run the REAL app in a Linux container (match the deploy) + run the saved instrumented diag (`scratchpad/_aif-diag.spec.ts`) on one dialog: a clean pass ⇒ production unaffected ⇒ fix = run the prod E2E gate on Linux/Docker (folds into Phase-9 deploy). User chose to document + defer this run (credit constraint). Owned by lead/backend when picked up.
- [ ] **Action-items hub — deferred satellites (partner-handoff Phases 2–4; ADR [0050](docs/decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md)). PULLED PRE-PILOT 2026-07-12 (ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)).** Activity feed (`action_item_updates`), evidence, scheduled follow-ups, formal reviews, checklist items, dependencies, per-committee custom fields, per-commission status/urgency **management UI** (the tables are configurable already; only global defaults are seeded + only the 4 global keys are surfaced), reminder/escalation rules, and effectiveness checks. Adopt selectively when real committee workflows demand them — the hub-and-spoke schema already accommodates each as an additive satellite (no core-table change), and status gating flags (`requires_comment`/`requires_evidence`/`requires_review`) become meaningful once evidence/review land. Owned by lead (scheduling) + backend/frontend when picked up.
- [ ] **Action-items case cross-link UI + `visibility_scope` toggle for meeting/manual items (deferred from ADR 0050 F1). PULLED PRE-PILOT 2026-07-12 (ADR [0071](docs/decisions/0071-pre-pilot-release-scope-expansion.md)).** The backend supports a `case_id` cross-link on meeting/manual hub items and the `committee_*` RPCs accept `p_visibility_scope` (default `case_restricted` when cross-linked, coordinator override to `committee`), but **no UI creates a case-linked meeting/manual item** — `src/components/meetings/action-item-form.tsx:102` hard-codes `caseId` and comments "not exposed in this form for v1". So the "Visível para toda a comissão" toggle (Q4) has nothing to render against yet. When a case-linking affordance is added to the meeting/manual item form, add the toggle beside it (shown only when a case is linked, default off = restricted). Latent capability, like `assignees_only`. Owned by frontend.
- [ ] **Break-glass access (logged, reasoned, time-boxed emergency access to restricted cases / PHI).** Target Phase 20 (Notifications & Escalation). A dedicated `break_glass_access_events` record + a temporary-grant path that requires a reason, is always audited, notifies privacy/security, and appears in audit reports — NOT platform-admin-sees-everything. Partner handoff §19 as the reference model; composes with `case_access` expiry (ADR 0050) and the audited-single-door PHI posture (Rule 12). Owned by lead (scheduling) + backend.
- [ ] **User Registration — Phase-9 email-template deploy dependency (feature COMPLETE; deploy-time task).** The pt-BR invite + recovery templates (`supabase/templates/{invite,recovery}.html`) must be pasted into Supabase **Dashboard → Auth → Email Templates** for Cloud (self-hosted `config.toml` templates don't auto-apply), preserving the `{{ .TokenHash }}` + `?type=invite|recovery` link shape — alongside the already-flagged custom SMTP. Until then invite/recovery links work locally but NOT in prod. Owned by backend at deploy. See [user-registration.md](docs/progress/user-registration.md).
- [ ] **Broader org-member-role-management UI (multi-tenancy gap; surfaced building NSP-per-org B3).** NSP-per-org added a *focused* `/o/[org]/manage/equipe-nsp` that toggles only the `nsp_coordinator` role, but there is **no general org-member management UI** — an `org_admin` cannot add/remove other `org_admin`s or manage org membership through the app (only the seed + direct DB). Build a proper `/o/[org]/manage/membros` surface (+ `organization_members` role CRUD actions). Owned by frontend + backend.
- [ ] **NSP appoint-picker: annotate/exclude current org_admins (minor UX, NSP-per-org B3).** The appoint picker lists all org users incl. `org_admin`s; selecting one returns the "já é administrador" refusal (the DB guard is the safety). Cleaner: disable/annotate current org_admins in the picker. Owned by frontend.
- [ ] **`appointNspCoordinator` TOCTOU hardening (optional, NSP-per-org B3).** The SELECT-role-then-upsert has a negligible race (admin-only, deliberate, low-frequency); a DB-level guard (trigger / partial constraint forbidding a coordinator-upsert over an `org_admin` row) would close it fully — over-engineering for now. Owned by backend.
- [ ] **Multi-tenancy — org_admin TS-gate gap in the invoker `authorize*` helpers (QA INFO / lead #15, non-blocking).** ~8 invoker-context `authorize*` server-action helpers gate on `is_staff_admin_of` / `isAdmin` but do **not** yet grant the org_admin → coordinator branch that `getCommissionAccessByOrg` grants on the read path. RLS is the security backstop (these are invoker-context, not service-role, so the worst case is an org_admin being *denied* a write they should be allowed, not an escalation), but the gates should be aligned with the read path for consistency. Enumerate + add the `is_org_admin_of_commission` term.
- [ ] **Pre-existing full-serial-suite contamination (NOT a form-builder regression).** A single full serial run is RED on `main` itself (~17–19 failures; branch ≤ baseline, 0 net new — proven by failing-title diff). Cross-spec seed-mutation in lexical run order (phase10–14/22 before phase2–8). Separate spec-isolation effort (phase13-saga class); the team's green path is chunked runs with a fresh reset per chunk. Tracked, does not gate any single feature.
- [ ] **Harden `e2e/form-builder-enhancements.spec.ts` to a throwaway commission (QA INFO-4, nice-to-have).** It builds fixtures in the seeded CCIH commission (`COMM_A`/`/c/ccih`) rather than a probe commission/users (cf. P13-005/006 lessons). Acceptable now (branch added 0 net contamination); harden (use `makeProbeCommission`/`makeProbeUser`) if it joins the full gate matrix.
- [ ] **`case_patient` disposal UI — "Descartar dados do paciente" (frontend, not blocking; mirrors the NSP WS C item below).** `dispose_case_phi` + the `disposeCasePhi(caseId, reason)` action are live; the UI is a coordinator/admin-gated button on the case detail opening a confirm dialog with a reason-category `<select>` bound to `PhiDisposeReason`/`CASE_PHI_DISPOSE_REASON_LABELS` (NO free-text — constrained category), reflecting `has_patient=false` + the `phi_disposed_*` stamp post-action. One-shot (HC056).
- [ ] **WS A FE — PQS-membership management UI (frontend, not blocking).** `pqs_members` is admin-managed via `add/remove/list_pqs_members` RPCs + a seeded admin (functional/testable now). A roster-management screen under `/admin` (enroll/remove PQS staff, list members) is a frontend task; in prod the first admin enrolls staff via `add_pqs_member`. Mirror the `assignStaffAdmin` admin-action pattern (`requireAdmin` + admin client) for the server actions.
- [ ] **WS A FE — `/admin/nsp` gating + patient-panel affordance (frontend/tester, surfaced by WS A trace 2026-06-20).** (a) `/admin/nsp/[eventId]/page.tsx` gates ONLY on `context.isAdmin`. Post-WS-A a **non-PQS admin** degrades to `notFound()` (clean pt-BR 404 — `getSafetyEvent`'s `patient_safety_event` SELECT is now `can_read_event`, which denies a non-PQS/non-custodian admin → null → 404), NOT a crash/broken-empty page. Ideal: gate the NSP routes on `is_pqs_member` (e.g. via `patient_safety_enabled()` + a PQS check / a new `public.is_pqs_member()` read) for a tailored "não autorizado" instead of a generic 404. (b) The patient panel renders on `event.hasPatient` ALONE; entitlement is enforced at the data layer (the `get_event_patient` RPC returns null for an unentitled caller → `<PatientPanelEmpty>`), so **no PHI leaks**, but the affordance ideally gates on `hasPatient` AND entitlement to avoid showing an empty panel to an entitled-event-but-unentitled-PHI viewer. **No reporter-facing route renders the panel** (verified: `/c/[slug]/eventos` is governance-only, PHI-free; the panel is admin-route-exclusive) — so a reporter, incl. after custody handoff, never reaches it. Flagged per coordinator; NOT fixed in WS A.
- [ ] **WS E / M2 — per-vocabulary reorder/archive RPC consolidation DEFERRED (backend, 2026-06-20).** The reorder/archive RPCs for `case_tags`/`case_outcomes`/`case_narrative_types` (commission-scoped, `assert_*_enabled`+`is_staff_admin_of`) vs `pqs_event_types`/`pqs_sentinel_criteria` (GLOBAL, `assert_patient_safety_enabled`+`is_pqs_member`, two-step negative-offset against a deferrable position unique) diverge by gate/flag/scope/collision-strategy/pt-BR message. A shared `app.reorder_vocab(table,…)` helper would need table-name-interpolated dynamic SQL (injection surface + allow-list) and couldn't encode the per-table divergence — less auditable than the current explicit static RPCs. Revisit only if the vocab set grows materially. No code change.
- [ ] **WS B — authoritative PHI-bearing free-text column list (for ARCHITECTURE.md Rule 11/12 + ADR alignment, lead-owned). FINAL count = 22 columns** (18 + the 4 borderline, lead ruled CLASSIFY 2026-06-20). Backend applied SQL column COMMENTs (`'PHI-BEARING free text (WS B; Rule 11/12)…'`) to: `patient_safety_event.description_md`, `event_triage.disposition_notes_md`, `rca.{what_md,expected_md,summary_md,impact,scope}`, `rca_factors.text`, `rca_root_causes.text`, `rca_timeline_entries.description`, `capa_plan.lessons_learned_md`, `capa_effectiveness.method_md`, `capa_action_task.description`, `capa_measure_result.note`, `meetings.minutes_md`, `case_interviews.summary_md`, `case_narratives.body_md`, `case_events.body` **(the original 18)** + **`meeting_agenda_items.{description,discussion_notes,resolution}` + `case_interview_subjects.note` (the +4 addendum** — all are multi-line textareas; agenda free-text already read-audited via `meeting.viewed` on `getMeetingDetail`/`listMeetingAgenda`, subject note via `interview.viewed` on `getInterviewDetail`/`listInterviewSubjects`; no new audit emit needed). **EXCLUDED** (governance metadata, PHI-free by the title invariant): all `*.title` + `case_interview_subjects.clinical_role`. Comment-only; `db diff` clean; types unchanged. Lead aligns ARCHITECTURE.md/ADRs from this 22-column list (backend left those docs untouched per instruction).
- [ ] **WS C FE — "Descartar dados do paciente" disposal UI (frontend, not blocking).** The `disposeEventPhi(eventId, reason)` server action + the `dispose_event_phi` RPC are live and E2E-testable; the UI is a frontend task: an admin/PQS-gated button on the NSP event detail (`/admin/nsp/[eventId]`) opening a confirm dialog with a **reason-category `<select>`** bound to `PhiDisposeReason` / `PHI_DISPOSE_REASON_LABELS` (NO free-text field — the reason is a constrained category), a destructive-action confirm, and post-action it should reflect `has_patient=false` (panel gone) + show the `phi_disposed_at/by/reason` stamp ("dados descartados em … por … — motivo: …"). Disposal is one-shot (HC056 → "PHI já descartada"). Owned by `frontend`.
- [ ] **WS B/C FE — discourage PHI in `*.title` / structured short fields (frontend, not blocking; surfaced 2026-06-20).** The minimum-necessary invariant assumes titles stay PHI-free (they ride on queue/list paths). Add helper text / a soft validation note ("Não inclua dados do paciente.") to the title inputs on event/RCA/CAPA/meeting/interview/case forms (mirrors the existing case-action-item dialog note). Soft guidance only — no hard block. Owned by `frontend`.
- [ ] **E2E regression suite is NOT reliably green against a PROD build (test-harness debt, surfaced 2026-06-18; NOT a Phase-14 defect).** The freeze-proof gate now requires `next build`+`next start` (the heavy NSP pages crawl + balloon the `next dev` server to 4.3 GB — see user MEMORY `e2e-gate-prod-build`). But the pre-≤13 specs were authored against `next dev` and flake against the prod build: (a) Radix dialog close-animations (`data-[state=closed]:animate-out`) race tight `toBeHidden`/`toHaveCount` timeouts because — unlike the Phase-14 specs — the older specs DON'T set `reducedMotion: 'reduce'`; (b) the suite shares one mutable DB with no per-test reset, so Playwright `retries` (and parallel workers) CASCADE write-pollution: retries=2 produced MORE hard failures (25) than retries=0 (14). **Phase-14 specs are clean (65/65).** Evidence (2026-06-18, prod build, LOCAL Docker): full suite workers=1/retries=0 → 246 pass / 14 fail; non-14 specs workers=4/retries=2 → 162/13 flaky/20 fail; non-14 specs workers=1/retries=2 → 167/3 flaky/25 fail. Every failure is a pre-14 spec. **Fixes (test-infra; `tester` + `backend` for config):** add `use: { reducedMotion: 'reduce' }` to `playwright.config.ts` (one line, stabilizes animation timing globally); point `webServer.command` at a prod build for the gate; give the older mutation specs DB isolation (unique per-test fixtures or reset-per-file). Until then, the older specs' "green" depends on the `next dev` model.
- [ ] **E2E `case-narratives` AC-1b spec-isolation (tester-owned; surfaced 2026-06-19 during the case-access refinement triage).** AC-1b renames the "Resumo Clínico" narrative TYPE to "…(Renomeado) {timestamp}" and never restores it, so on the shared DB a re-run/later-ordered AC-1 (which asserts the original label) fails. NOT a code regression — pre-existing test debt, an instance of the no-per-test-DB-isolation problem in the item above. Fix: AC-1b restores the original label in a teardown/`finally` (or uses a throwaway type). Until then `case-narratives` AC-1 is order/state-dependent.
- [ ] **Phase 14a deferred (QA re-verify INFO):** sweep the success string out of `ActionState.error` for the 4 remaining safety actions (`transferEventCustody` / `updateEvent` / `setEventPatient` / `cancelEvent`) into the new `message` field — harmless today (all consumers gate on `!result.ok`), do it on the next `src/lib/safety/actions.ts` touch (e.g. Phase 14b). Backend-owned. The 2 flagged in QA N1/I2 (`notifySafetyEvent`/`acknowledgeEvent`) are already done.
- [ ] **Interviews — "Minhas entrevistas" discovery surface for plain-`staff` interviewers (Phase 11, deferred per lead).** v1 has NO dedicated list for a plain-`staff` registered interviewer to find interviews they may write — they reach the detail by DIRECT LINK only (the case-detail "Entrevistas" panel is coordinator-gated). The interview detail page renders correctly for them (membership guard + `viewerCanWrite` controls), and the detail header back-link points non-coordinators at the commission home (`/c/[slug]`), never the coordinator case page. A future "Minhas entrevistas" surface (mirroring "Minhas fases") would close the discovery gap. Owned by `frontend` when scheduled.
- [ ] **Phase 8 deploy checklist — production Supabase Cloud MUST use asymmetric (ES256/RS256) JWT signing keys** (Phase 2 QA re-review, ADR 0009). Otherwise `getClaims()` silently falls back to a per-request `getUser()` GoTrue round trip, re-introducing the P2-002 post-login race in production (behavioral regression, not a security hole — tampered tokens still fail closed). Add a testable verification step.
