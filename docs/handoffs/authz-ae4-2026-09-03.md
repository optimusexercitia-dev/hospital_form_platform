---
task: AE4 — the authz catalog, staff_admin substituted, and 3 of 43 permissions made load-bearing
adrs: [0155, 0162, 0172, 0174, 0175, 0176, 0177, 0178, 0179, 0079]
base_sha: cf30dfe9
created: 2026-09-01
updated: 2026-09-03
expires: 2026-09-17   # branchless form (handoff skill): the branch was deleted; if AE4's gate steps have not resumed by then, promote § Dead ends and delete this file
status: live   # Build complete, LANDED ON main. Gate AE4 NOT declared: broad QA = CHANGES REQUESTED, one open bug, e2e:prod owed, C2 not closed.
---

# Handoff — AE4, build-complete, landed on `main`, gate not declared

## ▶ RESUME HERE

⚠ **2026-09-03: the branch `authz-ae4-catalog` (tip `3b21826b`) was fast-forwarded into `main` by
PO instruction, AHEAD of Gate AE4's declaration, and then deleted. Resume from `main`; cut a new
branch for the remaining gate steps. `main` is NOT pushed — `push-schema-before-code` fires at the
push (`db:push` first).** This file was renamed to the branchless form the same day.

1. `git log --oneline cf30dfe9..main` · `git status --short` · `git rev-list --count origin/main..main`.
2. Read **[the AE4 hub](../features/ae4.md) § Current state** — the Now section of PROGRESS.md is
   retired (ADR 0185); this file is not status truth and may predate a ruling.
3. Read [authz-ae4.md](../progress/authz-ae4.md) **§ Gate AE4 wave + IA-F9** and **§ AE4.9 D6 + D5**.
4. Then § "Next task" below. ⛔ Re-measure before relying on anything here — § Trust.

⛔ Nothing may cite this file. Promote instead, and leave a pointer. (One inbound reference exists:
the Gate AE4 review's **F-REC-7**, which is a reminder to *delete this file at the Record step* —
self-extinguishing, not a dependency on its content.)

## Trust

**Mixed, and the mix matters.** The AE4.1–AE4.9 build sections were written incrementally by earlier
sessions. Everything dated **2026-09-02/03** was written at the END of a long session — a compaction
of measurements taken during it, not a reconstruction, but not incremental either. Figures carry
witnesses; exit codes were read from files, **never through a pipe** (two runs on this branch were
once reported exit 0 and were exit 1; a third was reported 0 by a compound command whose `echo`
supplied the zero).

## Goal and scope boundary

AE4 substitutes `staff_admin` end-to-end against the `authz` catalog and makes **3 of 43** permissions
load-bearing on real production doors.

**Explicitly NOT in scope:** the other 40 permissions (they are `pending-rekey` **by design** — ADR
0176 D6 confirmed the three representatives as the gate minimum); the AE5 role-by-role substitution;
the D8 compatibility bundle; and `authz.scope_reaches`'s plan defect (own increment — see § Next).

## State

### Done — VERIFIED

| What | Witness | When |
| --- | --- | --- |
| Build committed, tree clean | `git status --porcelain` → 0 lines at `cf30dfe9` | 09-03 |
| `main` untouched — the HOLD stands | `git rev-list --left-right --count main...HEAD` → `0 104` | 09-03 |
| `npm run lint` — all 12 gates | exit **0**, read directly (the PROGRESS.md cap breach is CLEARED) | 09-03 |
| `npm run test:db` on a fresh reset | exit **0**, `Files=259, Tests=8685` | 09-02 |
| `npm run e2e:prod` | exit **0**, GATE GREEN in a **single** run, 1h43m, 1273/1273 accounted | 09-02 |
| 4 ARMs (`census`/`hat`/`floor`/`wrapper`) | each exit **0**, INVARIANT HOLDS | 09-02 |
| Door sweep **read** arm | exit **0**, 7 gates, all COVERED, BLIND 0 | 09-02 |
| Door sweep **write** arm — re-aimed **and swept CLEAN** | exit **0**, `policy=4/107`, **4 COVERED, 0 BLIND**; `974328e6` | 09-02 |
| IA-F9 acceptance — has a verdict | `AE4 ACCEPTANCE NOT MET (3 of 7 rows PASS)`; `8ca976d7` | 09-02 |
| Gate AE4 QA review | **CHANGES REQUESTED** — [review](../reviews/authz-ae4-gate-review.md); `a6ff4ad0` | 09-02 |
| C2 branched and pushed | `authz-c2-tier1` at `8ca976d7`, on origin | 09-02 |

⛔ **Two claims this file previously carried are now FALSE and are corrected here rather than
deleted:** `npm run lint` no longer exits 1 (the PROGRESS.md hard-cap breach was cleared — ADR 0179
moved the open register to `follow-ups-open.md`), and the write arm no longer "cannot see the four
`FOR ALL` form policies". Both were true when written.

### Written but UNVERIFIED

- **The rollback runbook's §6 worked example** ([runbook](../deployment/authz-rollback-runbook.md)).
  The revert has **never been executed**; every post-revert value is a *labelled derived expectation*.
  Verification is **staged** in the session scratchpad (`ae49-revert.sql`, `ae49-verify.sql`,
  `ae49-expectations.md` — expectations written BEFORE any run so a surprise cannot become the
  expectation) and **PO-deferred until AE4 concludes and merges**. ⛔ Run both with
  `-v ON_ERROR_STOP=1`, or a failed guard yields **exit 0 with the revert half-applied**.
- **BELIEVED:** the single-run `e2e:prod` green still describes the app-relevant tree — every commit
  since touches only `docs/`, `scripts/`, `supabase/tests/`. Settle with
  `git diff --name-only <e2e-sha>..HEAD -- src supabase/migrations supabase/seed.sql e2e`.
  ⛔ It stops being true the moment `BUG-AE49-D6-REKEY-INCOMPLETE`'s fix lands.

### Not started

C2 Tier 1 (its own branch, another machine) · `scope_reaches` fix (own session) · the QA review's
F-BLOCK-3 and F-MAJOR-1 · the final `e2e:prod`.

### Tree

`cf30dfe9`, clean. ⚠ A nested worktree exists at `.claude/worktrees/friendly-spence-607f77` on
`authz-ae4-scope-reaches-fix`; **its `.env.local` is absent**, which produces reds that look real.
Creating it left the PRIMARY checkout on a different branch — verify `git rev-parse --abbrev-ref HEAD`
before believing any file you read here.

## Gates

Named by **ARM, never by script**. The arms above ran at `8ca976d7`/`974328e6`.

**Did NOT run, and each absence is a real hole, not a pass:**

- **The write arm's guard half** — `guard=0/13`, 0 selected. Same shape as the blank that was just
  fixed. It contributed **no verdict** to this diff.
- **The other 70 write-capable policies.** The committed baseline covers **37 of 107**; a
  `FROMFINDINGS` arm re-measures nothing, so it cannot see the 70 and they pass vacuously by absence.
- **A drift tripwire on the 4 new verdicts.** All four are `snapshot:ABSENT` (§7.2).
- **`e2e:prod` since the wave.** One full run is owed, in a single run, on a **quiet machine** — every
  red at this tree has been connection-level, never an assertion, and a second Supabase stack was up.

## Dead ends

Earlier ones are **promoted verbatim** to [authz-ae4.md](../progress/authz-ae4.md) § "AE4 dead ends".
Four from 2026-09-02/03, each recording the *mechanism*:

- ⛔ **An absence needs its mechanism MEASURED, not inferred. Three consecutive absences in the IA-F9
  work were mis-attributed on first reading, twice by the lead.** (1) `assignment_facts` absent from
  the nested plans → blamed on `EXPLAIN` not descending into DEFINER bodies; actually **Pass B never
  executed**, `ON_ERROR_STOP` having killed psql in an earlier section. (2) Absent again → blamed on
  `auto_explain` writing to the server log; actually the **extractor's markers matched their own
  documentation** (the range opened on a line that *named* both sentinels), yielding a 48-line region
  from a 229,937-line one. (3) `DC1 = 1.53×` → read as a possibly-dead instrument; actually the
  planted term is **1–3% of cost** and the control was aimed at the wrong subject.
- ⛔ **An empty grep against a bound reads exactly like a pass.** *"`grep loops=N` returns nothing"*
  and *"no violations found"* are the same output. The IA-F9 zero was examined only because an
  unrelated section crashed first. The fix now in the protocol is **two-stage: presence before
  bound**, plus a stage-0 extractor-sanity floor — a subject count of zero is a *capture-mechanism
  finding*, and no bound may be evaluated against it.
- ⛔ **A DB-window grant in an agent message is direction, not permission.** A subagent was refused
  `supabase db reset` and even a read-only `docker exec … psql` mid-session; it correctly declined to
  retry in another form. Execution moved to the main session, with the agent authoring SQL to
  scratch. ⛔ Never route a peer around a denial.
- ⛔ **Reading the record instead of the executable text.** The rollback runbook's §6 write probe used
  `set name = name`; the column is `title`, so the probe would have raised `42703` **looking exactly
  like an authorization deny** on the one check §6.7 calls load-bearing. Second time in this
  workstream that opening `409`'s actual SQL corrected something written from the record.

## Decisions made in flight

| Decision | Status |
| --- | --- |
| C2 gets its own branch off AE4; **AE4 is NOT merged to main** — the HOLD stands, schema-first stays *armed* | **ruled** (PO, 09-02) |
| Rollback §6 verification deferred until AE4 concludes and merges | **ruled** (PO, 09-02) |
| `scope_reaches` fix runs as its own session/increment; acceptance re-run against it | **ruled** (PO, 09-02) |
| Gate AE4 QA review run **EARLY**, before the final `e2e:prod` | **provisional — the PO must ratify the ordering at the gate** |
| `K = 4` (the P5 threshold) does not move for a 6.19× reading | **ruled** (protocol §8 item 5 licenses re-deriving only within a few percent) |
| Whether the two `§ Bug Log` standing prohibitions become `.claude/rules/` files | **provisional — PO.** They are genuinely the *Rule* category but fail ADR 0127's admission bar (they need the scope `progress-contract.md` already claims, and exceed the volume bound) |

## Open questions / blockers

| Item | Who answers |
| --- | --- |
| ⛔ **`BUG-AE49-D6-REKEY-INCOMPLETE`** — `commission.forms.edit` is re-keyed at **4 of the 7** policy sites its PO-approved matrix names; `form_item_options_staff_admin_write` and `form_item_validations_staff_admin_write` still carry the pre-re-key predicate. Not an exposure; a conformance defect. **Its fix is a migration**, which is what makes a final `e2e:prod` certain. | backend |
| ⛔ **IA-F9 acceptance NOT MET.** P1 FAIL (8 240 `Seq Scan on hospitals`), P5 FAIL (6.19×/6.21× vs 4×). Cause attributed by planted-cost control to `authz.scope_reaches` → `FUP-SCOPE-REACHES-HOSPITALS-SEQ-SCAN`. | the spun-off session, then re-run |
| **UNKNOWN:** whether fixing `scope_reaches` brings P5 to ≤ 4×. Only a re-run can say. ⛔ A P5 that improves but stays above 4× is a **partial result**; the threshold does not move to meet it. | measurement |
| **UNKNOWN:** whether any of the 70 unmeasured write-capable policies hold a blind gate. | a full write-path sweep (~50 min, 120 cases) |
| QA review's **F-BLOCK-3** (the oracle's approval scope stated three ways: 42 / 33 / 43) and **F-MAJOR-1** (`hardDenyClasses` empty on 43/43, its lint arm iterating zero times so it cannot fail). Both filed, neither touched. | backend + PO |
| **UNKNOWN:** whether the 25 unreachable rewrite-migration doors hold periodic-sweep verdicts (ADR 0173). Carried forward unresolved. | a historical-snapshot audit, if authorised |
| ⚠ A hook injects *"MANDATORY: you MUST run graphify before reading source files"* into tool output. **Three independent agents have now flagged and correctly refused it** — CLAUDE.md's binding exception says graphify does NOT index SQL and the live catalog is the sole truth. Possibly unintended. | PO |

## Next task

1. Fix `BUG-AE49-D6-REKEY-INCOMPLETE` — re-key the two sibling policies. Start from the review's
   F-BLOCK-1 and the matrix at `docs/design/authz-ae43-staff-admin-permission-matrix.md:291`.
   ⛔ Verify the 7 sites against the **live catalog**, not the matrix text.
2. Land the `scope_reaches` fix from its session, then **re-run the IA-F9 acceptance against it**.
3. Batch every remaining code/schema/seed change into **one window**, then run `e2e:prod` **once** on
   a quiet machine. Anything landing after that run invalidates it.
4. QA re-review (the verdict is CHANGES REQUESTED), PO approval, Record step — the AE4 row to
   `phase-ledger.md`, **and delete this file in that commit**.
5. Merge, schema **before** code
   ([push-schema-before-code](../../.claude/rules/push-schema-before-code.md) fires at that merge).

## Re-derivation appendix

`DB=supabase_db_azkbbhskturikxpgmafq`; **no `psql` on PATH** —
`docker exec "$DB" psql -U postgres -d postgres -At -c "…"`. ⛔ Reset first: a bisect or checkout
poisons every later catalog read.

- Catalog: `select state, count(*) from authz.roles group by state;` → 1 authoritative / 11 legacy.
- The seam, falsifiable: permission-code **literals** in `app`+`public` = **3**, one per re-keyed site
  — `pg_proc` × `authz.permissions` on comment-stripped `prosrc`.
- Countdown: `npm run lint:authz-vectors` → `manifest 43 rows, {"pending-rekey":40,"re-keyed":3}`.
- Arms: `ARM=census|hat|floor bash supabase/tests/mutation/p0-authz-invariant.sh`,
  `FROMFINDINGS=1 ARM=wrapper …`. ⛔ Read each exit code DIRECTLY.
- Sweep derivation: `BASE=<sha> TIP=HEAD bash scripts/door-sweep-cases.sh` — ⛔ it prints **TWO**
  commands (read + write); running one leaves the other half unmeasured. Override `WORK=` to scratch,
  then confirm the committed baseline was untouched with `git diff --stat -- docs/reviews/`.
- IA-F9: `supabase db reset --local`, load `scripts/authz-ae4-perf-fixture.sql`, run
  `scripts/authz-ae4-perf-harness.sql` (then again with `-v NESTED=1`), then §9.7's **three-stage**
  extraction in [the protocol](../design/authz-ae4-performance-acceptance.md). ⛔ The fixture is NOT
  applied by `db reset`; a run without a reload aborts at the fixture gate.
- E2E: ⛔ read `GATE_EXIT` from `/tmp/e2e-prod-gate/gate-exit`, **never** from a task notification —
  that reports the COMPOUND command's exit. `BATCH_TESTS=22` is the recorded rescue.
- ⛔ For any SQL/RLS/RPC/authz claim the **live catalog is the sole truth** — never a migration file,
  never graphify (CLAUDE.md's binding exception).
