# C2 command-door neutralizer — findings

Generated by `supabase/tests/mutation/c2-command-door-neutralizer.sh`.
⛔ Derived per run; do not hand-edit. A subset run writes to $WORK, never here (ADR 0153).

| enforcer | tier1 doors depending | authz raises | verdict | note |
| --- | ---: | ---: | --- | --- |
| `public.withdraw_referral(p_referral_id uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.withdraw_correction(p_request_id uuid)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.update_session(p_session_id uuid, p_session_type text, p_modality text, p_scheduled_start timestamp with time zone, p_scheduled_end timestamp with time zone, p_location_text text, p_meeting_url text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.update_referral_internal_note(p_note_id uuid, p_title text, p_body_md text, p_kind text)` | 1 | 5 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.update_referral_assignment(p_assignment_id uuid, p_status text, p_due_at timestamp with time zone, p_assignment_role text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.update_professional_profile(p_profile_id uuid, p_full_name text, p_professional_type text, p_license_number text, p_license_region text, p_specialty text, p_affiliation_status text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.update_interview_summary(p_interview_id uuid, p_summary_md text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.update_interview_subject(p_subject_id uuid, p_clinical_role text, p_note text, p_external_name text, p_external_org text, p_relationship_to_case text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.update_interview(p_interview_id uuid, p_title text, p_case_phase_id uuid, p_interview_category text, p_confidentiality_level text)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.update_event(p_event_id uuid, p_title text, p_description_md text, p_suspected_harm_level text, p_event_type_id uuid, p_location text, p_discovered_at date)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.unlink_referral_case(p_link_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.unassign_referral_internal_note(p_note_id uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.unassign_narrative(p_narrative uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.transfer_event_custody(p_event_id uuid, p_to_owner_kind text, p_to_commission_id uuid, p_note text)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.suggest_carry_forward(p_commission uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.submit_response(p_response_id uuid)` | 1 | 6 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8495) — the suite aborted rather than failed; not a verdict |
| `public.submit_rca_for_review(p_rca_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.submit_minutes_job(p_job_id uuid, p_service_job_id text)` | 1 | 2 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8614) — the suite aborted rather than failed; not a verdict |
| `public.submit_ethics_appeal(p_case_id uuid, p_decision_id uuid, p_appeal_reason_md text, p_submitted_by_participant_id uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.start_session(p_session_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.start_correction_draft(p_request_id uuid)` | 1 | 4 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.soft_delete_document(p_document_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.sign_meeting(p_attendee_id uuid, p_method text, p_note text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_referral_patient(p_referral_id uuid, p_name text, p_mrn text, p_date_of_birth date, p_age_years integer, p_sex text, p_encounter_ref text, p_unit text, p_attending text)` | 1 | 4 | **ERROR** | MUTATION DID NOT LAND (hash unchanged): psql:/tmp/_c2mut.sql:15: ERROR:  C2MUT: 2 raise(s) survived the rewrite
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE |
| `public.set_referral_deadline(p_referral_id uuid, p_response_due_at timestamp with time zone)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_professional_link_state(p_profile_id uuid, p_link_state text, p_user_id uuid)` | 1 | 5 | **ERROR** | MUTATION DID NOT LAND (hash unchanged): psql:/tmp/_c2mut.sql:15: ERROR:  C2MUT: 1 raise(s) survived the rewrite
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE |
| `public.set_primary_subject(p_case_participant_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_participant_patient(p_case_id uuid, p_participant_id uuid, p_name text, p_mrn text, p_date_of_birth date, p_age_years integer, p_sex text, p_encounter_ref text, p_unit text, p_attending text, p_role_id uuid)` | 2 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_interview_subject_participant(p_subject_id uuid, p_participant_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.set_interview_participant(p_interview_id uuid, p_participant_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.set_interview_interviewer_participant(p_interviewer_id uuid, p_participant_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.set_interview_confidentiality(p_interview_id uuid, p_level text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_event_patient(p_event_id uuid, p_name text, p_mrn text, p_date_of_birth date, p_age_years integer, p_sex text, p_encounter_ref text, p_unit text, p_attending text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.set_case_participant_role(p_case_participant_id uuid, p_role_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.set_case_narrative_assignment_role(p_narrative_id uuid, p_role_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.send_referral(p_referral_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.schedule_session(p_interview_id uuid, p_session_type text, p_modality text, p_scheduled_start timestamp with time zone, p_scheduled_end timestamp with time zone, p_location_text text, p_meeting_url text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.schedule_ethics_hearing(p_case_id uuid, p_hearing_type text, p_meeting_id uuid, p_scheduled_at timestamp with time zone)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.save_triage(p_event_id uuid, p_is_pse boolean, p_pse_closure_reason text, p_reach text, p_harm_severity text, p_natural_course boolean, p_review_pathway text, p_disposition_notes_md text, p_sentinel_criteria_ids uuid[])` | 1 | 6 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.save_narrative_body(p_narrative uuid, p_body_md text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.save_minutes_draft(p_job_id uuid, p_draft jsonb)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.save_correction_draft_body(p_request_id uuid, p_body_md text)` | 1 | 3 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.save_block_to_library(p_item_id uuid, p_name text, p_description text)` | 1 | 5 | **ERROR** | UNMUTABLE — 5 authz raise(s) but only 4 match the anchor; refusing a partial mutation |
| `public.revoke_printed_document(p_id uuid, p_reason_class text, p_reason text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.review_correction(p_request_id uuid)` | 1 | 3 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.resubmit_correction(p_request_id uuid)` | 1 | 5 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.resolve_referral(p_referral_id uuid, p_summary_md text, p_follow_up boolean)` | 1 | 2 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8584) — the suite aborted rather than failed; not a verdict |
| `public.request_referral_information(p_referral_id uuid, p_body text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.request_document_disposition(p_document_id uuid, p_reason text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.reopen_triage(p_event_id uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.reopen_referral(p_referral_id uuid, p_reason text)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.reopen_rca(p_rca_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.reopen_meeting(p_meeting_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.reopen_interview(p_interview_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.reopen_capa_plan(p_capa_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.reject_correction(p_request_id uuid, p_reason text)` | 1 | 4 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.redact_referral_note(p_note_id uuid, p_reason text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.redact_referral_message(p_message_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.redact_professional_profile(p_profile_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.record_session_attendance(p_session_id uuid, p_participant_id uuid, p_attendance_status text, p_role_at_session text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.record_referral_message_receipt(p_message_id uuid, p_event text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.record_ethics_finding(p_allegation_id uuid, p_finding text, p_rationale_md text, p_evidence_summary_md text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.read_minutes_transcript(p_job_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.provide_referral_information(p_referral_id uuid, p_body text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.post_referral_message(p_referral_id uuid, p_message_type text, p_body text)` | 1 | 6 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.open_reserved_session(p_meeting_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.open_referral_snapshot_document(p_shared_item_id uuid)` | 1 | 5 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.open_capa_plan(p_source text, p_classification text, p_source_id uuid, p_hospital_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.nsp_org_roster(p_org_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.nsp_org_event_rollup(p_org_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.nsp_org_capa_rollup(p_org_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.notify_safety_event(p_reporting_commission_id uuid, p_title text, p_description_md text, p_suspected_harm_level text, p_case_id uuid, p_event_type_id uuid, p_location text, p_discovered_at date)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.no_show_session(p_session_id uuid, p_reason text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.mint_printed_document(p_id uuid, p_source_kind text, p_source_id uuid, p_template_key text, p_template_version integer, p_content_hash text, p_verification_token text, p_verification_short_code text, p_contains_phi boolean, p_source_revision integer)` | 1 | 16 | **ERROR** | MUTATION DID NOT LAND (hash unchanged): psql:/tmp/_c2mut.sql:15: ERROR:  C2MUT: 2 raise(s) survived the rewrite
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE |
| `public.meeting_cadence_status(p_commission uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.log_document_previa(p_source_kind text, p_source_id uuid, p_template_key text)` | 1 | 4 | **ERROR** | MUTATION DID NOT LAND (hash unchanged): psql:/tmp/_c2mut.sql:15: ERROR:  C2MUT: 1 raise(s) survived the rewrite
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE |
| `public.log_audit_access(p_action text, p_entity_type text, p_entity_id uuid, p_commission uuid, p_summary text, p_metadata jsonb)` | 13 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.list_pqs_members(p_hospital_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.list_org_eligible_users(p_org_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.list_hospital_eligible_users_for_pqs(p_hospital_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.link_referral_related_case(p_referral_id uuid, p_case_id uuid, p_relationship_type text)` | 1 | 5 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8624) — the suite aborted rather than failed; not a verdict |
| `public.link_referral_case(p_referral_id uuid, p_target_case_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.link_evidence(p_commission uuid, p_standard uuid, p_kind text, p_artifact uuid, p_note text)` | 1 | 6 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8658) — the suite aborted rather than failed; not a verdict |
| `public.issue_ethics_notification(p_case_id uuid, p_notification_type text, p_delivery_method text, p_recipient_participant_id uuid, p_recipient_user_id uuid, p_due_at timestamp with time zone, p_related_document_id uuid, p_notes_md text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.issue_decision(p_decision_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.get_referral_case_access_summary(p_referral_id uuid, p_commission_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.file_correction_request(p_kind text, p_case_phase_id uuid, p_case_narrative_id uuid, p_reason text, p_classification text, p_permitted_corrector uuid)` | 1 | 8 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.ensure_professional_participant(p_profile_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.dispose_referral_phi(p_referral_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.dispose_meeting_minutes(p_meeting_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.dispose_event_phi(p_event_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.dispose_case_phi(p_case_id uuid, p_reason text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.delete_ad_hoc_case_narrative(p_narrative_id uuid)` | 1 | 3 | **ERROR** | MUTATION DID NOT LAND (hash unchanged): psql:/tmp/_c2mut.sql:15: ERROR:  C2MUT: 1 raise(s) survived the rewrite
CONTEXT:  PL/pgSQL function inline_code_block line 12 at RAISE |
| `public.decide_admissibility(p_case_id uuid, p_status text, p_rationale_md text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_referral_internal_note(p_referral_id uuid, p_committee_id uuid, p_body_md text, p_title text, p_kind text, p_assigned_to uuid)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_referral_draft(p_source_case_id uuid, p_target_commission_id uuid, p_referral_type_id uuid, p_subject text, p_response_expected boolean, p_description_md text, p_priority text, p_requested_action_id uuid, p_response_due_at timestamp with time zone, p_parent_referral_id uuid, p_target_hospital_id uuid)` | 2 | 5 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_professional_profile(p_org uuid, p_full_name text, p_professional_type text, p_license_number text, p_license_region text, p_specialty text, p_affiliation_status text, p_user_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_minutes_job(p_meeting_id uuid, p_filename text)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_interview(p_case_id uuid, p_title text, p_case_phase_id uuid, p_interview_category text, p_confidentiality_level text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_dsr_request(p_hospital_id uuid, p_mrn text, p_file_ref text, p_encounter text, p_due_days integer)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_committee_action_item(p_commission uuid, p_source_type text, p_meeting_id uuid, p_agenda_item_id uuid, p_case_id uuid, p_title text, p_description text, p_assigned_to uuid, p_urgency_id uuid, p_due_date date, p_source_case_phase_id uuid, p_visibility_scope text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_case_from_template(p_template_id uuid, p_label text, p_department_id uuid, p_department_other text, p_case_type_id uuid, p_custom_fields jsonb, p_patient jsonb)` | 2 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.create_case(p_commission_id uuid, p_label text, p_patient_enabled boolean, p_outcome_ids uuid[], p_department_id uuid, p_department_other text, p_case_type_id uuid, p_patient jsonb)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.confirm_triage(p_event_id uuid)` | 1 | 6 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8662) — the suite aborted rather than failed; not a verdict |
| `public.conclude_referral_internal_note(p_note_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.conclude_referral(p_referral_id uuid, p_reply_outcome_id uuid, p_result_md text, p_acknowledged_only boolean)` | 1 | 3 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.conclude_narrative(p_narrative uuid)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.conclude_meeting(p_meeting_id uuid, p_held_at timestamp with time zone, p_held_end timestamp with time zone)` | 1 | 6 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.conclude_interview(p_interview_id uuid)` | 1 | 2 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8656) — the suite aborted rather than failed; not a verdict |
| `public.complete_session(p_session_id uuid, p_actual_end timestamp with time zone)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.complete_rca(p_rca_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.complete_dsr_task(p_task_id uuid, p_note text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.close_capa_plan(p_capa_id uuid, p_lessons_learned_md text)` | 1 | 3 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8661) — the suite aborted rather than failed; not a verdict |
| `public.cast_case_vote(p_decision_id uuid, p_vote text, p_rationale_md text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.cancel_session(p_session_id uuid, p_reason text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.cancel_referral_assignment(p_assignment_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.cancel_minutes_job(p_job_id uuid)` | 1 | 2 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8614) — the suite aborted rather than failed; not a verdict |
| `public.cancel_interview(p_interview_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.cancel_event(p_event_id uuid)` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.cancel_capa_plan(p_capa_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.bulk_create_cases(p_template_id uuid, p_deadline date, p_phase_scope text, p_rows jsonb)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.begin_document_upload(p_resource_type text, p_resource_id uuid, p_title text, p_description text, p_confidentiality_level text, p_document_id uuid, p_declared_file_name text, p_declared_mime text, p_declared_size bigint, p_kind text, p_occurred_on date)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.assume_role(p_role platform_role)` | 1 | 2 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8669) — the suite aborted rather than failed; not a verdict |
| `public.assign_referral_reviewer(p_referral_id uuid, p_commission_id uuid, p_assignee_user_id uuid, p_assignment_role text, p_due_at timestamp with time zone)` | 1 | 5 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.assign_referral_internal_note(p_note_id uuid, p_user_id uuid)` | 1 | 3 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.assign_narrative(p_narrative uuid, p_assignee uuid)` | 2 | 4 | **BLIND** | nothing in the suite noticed the guard vanish — 2 Tier-1 door(s) depend on it |
| `public.approve_correction(p_request_id uuid, p_note text)` | 1 | 5 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.apply_minutes_review(p_job_id uuid)` | 1 | 8 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8655) — the suite aborted rather than failed; not a verdict |
| `public.adjudicate_dsr_request(p_request_id uuid, p_outcome text, p_outcome_basis text, p_legal_consultation_ref text, p_dispose_meeting_ids uuid[])` | 1 | 1 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8635) — the suite aborted rather than failed; not a verdict |
| `public.add_reserved_item(p_session_id uuid, p_case_id uuid, p_substance text, p_decision text, p_withdrawals text, p_quorum_met boolean, p_reader_uids uuid[])` | 1 | 2 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.add_referral_shared_item(p_referral_id uuid, p_kind text, p_source_narrative_id uuid, p_source_document_id uuid)` | 1 | 8 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_rca_member(p_rca_id uuid, p_role text, p_user_id uuid, p_external_name text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.add_rca_evidence(p_rca_id uuid, p_kind text, p_title text, p_document_id uuid, p_external_url text, p_citation_target text, p_cited_entity_id uuid, p_citation_label text)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_interview_subject(p_interview_id uuid, p_user_id uuid, p_external_name text, p_clinical_role text, p_external_org text, p_note text, p_relationship_to_case text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_interview_interviewer(p_interview_id uuid, p_user_id uuid, p_external_name text, p_external_org text, p_role text, p_note text)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_ethics_allegation(p_case_id uuid, p_category_id uuid, p_description_md text, p_severity text, p_alleged_event_date date)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_case_participant(p_case_id uuid, p_participant_id uuid, p_role_id uuid, p_is_primary_subject boolean, p_involvement_summary text)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.add_capa_action_evidence(p_action_id uuid, p_kind text, p_title text, p_document_id uuid, p_external_url text)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `public.add_ad_hoc_narrative(p_case_id uuid, p_narrative_type_id uuid, p_new_type_label text, p_title text, p_instructions text, p_assigned_to uuid)` | 1 | 4 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `public.activate_phase(p_case_phase_id uuid, p_assigned_to uuid, p_due_date date)` | 1 | 5 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8629) — the suite aborted rather than failed; not a verdict |
| `public.acknowledge_event(p_event_id uuid)` | 1 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.revoke_role_impl(p_actor uuid, p_scope_type text, p_scope_id uuid, p_role text, p_user uuid)` | 1 | 12 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.resolve_document_version_bytes(p_document_version_id uuid, p_rendition_kind text, p_uid uuid)` | 1 | 8 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.recover_orphan_person_to_org_impl(p_actor uuid, p_user uuid, p_organization uuid, p_started_on date)` | 1 | 6 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.print_source_series(p_source_kind text, p_source_id uuid)` | 1 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 1 Tier-1 door(s) depend on it |
| `app.grant_role_impl(p_actor uuid, p_scope_type text, p_scope_id uuid, p_role text, p_user uuid, p_title_id uuid, p_expires_at timestamp with time zone, p_allow_anchorless boolean)` | 7 | 16 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.ensure_provisioned_org_affiliation(p_actor uuid, p_user uuid, p_organization uuid, p_started_on date, p_allow_anchorless boolean)` | 7 | 5 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_respondent_linkage_resolved(p_participant_id uuid, p_role_id uuid)` | 4 | 1 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8638) — the suite aborted rather than failed; not a verdict |
| `app.assert_referral_target_acts(p_referral_id uuid, p_expected text[])` | 7 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_referral_due_future(p_due timestamp with time zone)` | 4 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_referral_draft_writable(p_referral_id uuid)` | 4 | 2 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_rca_writable(p_rca_id uuid)` | 22 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_phase_result_ready(p_case_phase_id uuid)` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_patient_required_fields(p_mode text, p_required text[], p_patient jsonb)` | 5 | 1 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8654) — the suite aborted rather than failed; not a verdict |
| `app.assert_not_case_excluded(p_case_id uuid)` | 19 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_item_bounds(p_response_id uuid, p_item_id uuid, p_item_type text, p_config jsonb, p_label text, p_group_instance_id uuid)` | 1 | 1 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8634) — the suite aborted rather than failed; not a verdict |
| `app.assert_interview_writable(p_interview_id uuid)` | 19 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_ethics_typed(p_case_id uuid)` | 4 | 1 | **BLIND** | nothing in the suite noticed the guard vanish — 4 Tier-1 door(s) depend on it |
| `app.assert_ethics_enabled()` | 11 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_ethics_coordinator(p_case_id uuid)` | 8 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_documents_wave_d_enabled()` | 4 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_documents_wave_c_enabled()` | 2 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_documents_wave_b_enabled()` | 1 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_documents_enabled()` | 4 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_charters_enabled()` | 2 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_case_corrections_enabled()` | 8 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_capa_writable(p_capa_id uuid)` | 18 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_audio_minutes_enabled()` | 6 | 1 | **COVERED** | a keystone asserts through this guard (red under mutation, green restored) |
| `app.assert_accreditation_enabled()` | 1 | 1 | **ERROR** | mutated run failed BUT the restored run did not come back green (FAIL, shape=Files=259, Tests=8376) — the failure is not attributable to this mutation |
| `app.affiliate_person_to_org_impl(p_actor uuid, p_user uuid, p_organization uuid, p_started_on date)` | 1 | 5 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8288) — the suite aborted rather than failed; not a verdict |
| `app.affiliate_person_impl(p_actor uuid, p_user uuid, p_hospital uuid, p_employee_id text, p_started_on date, p_job_title text, p_work_email text, p_work_phone text)` | 1 | 6 | **ERROR** | run SHAPE changed (Files=259, Tests=8685 → Files=259, Tests=8288) — the suite aborted rather than failed; not a verdict |
