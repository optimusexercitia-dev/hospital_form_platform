# AUTHZ Invoker-Wrapper Audit — Findings

AUDIT-INVOKER-WRAPPER (ADR 0078 §7.14 / ADR [0079] Amendment 7). Generated by
`supabase/tests/mutation/p0-authz-invoker-audit.sh`. Domain: every `public`,
`authenticated`-reachable, **INVOKER** (`prosecdef = f`) plpgsql function — the class
all three prior sweeps are structurally blind to, because each begins `and p.prosecdef`.

Method: open the wrapper's OWN gate — an RLS existence probe (G1), a
`perform app.assert_*` naming an identity assert (G2), or an identity-primitive `if`
condition (G3) — run the FULL pgTAP suite, read `Result:`. **COVERED** = suite went
`FAIL` (a keystone asserts through the gate). **BLIND** = suite stayed `PASS`.
**ERROR** = run shape != baseline (harness bug, not a result). **UNSUPPORTED** = no
guard of any class matched, so NO verdict is returned — not swept, stays in the
census backlog.

Baseline: Files=187, Tests=5899, Result: PASS.
Public INVOKER functions in the live catalog: 88. Swept: 88.

> ⚠ **Run in FOUR passes.** Passes 1–3 shared one baseline (Files=187, Tests=5899,
> Result: PASS); pass 4 ran after pgTAP 327 landed, so its baseline is Files=188,
> Tests=5906 — a HIGHER baseline, which is what makes its COVERED verdicts meaningful
> rather than an artifact of a changed denominator.
> · **pass 1** cases 1–80, aborted (below) · **pass 2** the 8 it skipped · **pass 3** the
> 12 whose verdicts the G1/assignment-form defects made unsafe · **pass 4** the 4 that
> pgTAP 327 keystoned, re-swept to confirm they flipped BLIND → COVERED through the
> harness itself and not merely by hand-probe.
>
> ⚠ **Pass 1 aborted at case 81/88 on a
> harness defect: it failed to capture a rollback point for `update_meeting` (slug
> longer than the filename limit) and **mutated the function anyway**, leaving its
> gate open in the shared local stack until a `db reset`. The guard that now makes
> the sweep refuse to mutate without a verified rollback point was added in response,
> and the remaining 8 were swept after it. Recorded here rather than quietly re-run:
> a harness that mutates before it can prove it can undo is a worse hazard than the
> blindness it audits.

## BLIND — the work-list (no keystone exercises these)

⚠ **BLIND is not a synonym for vulnerable.** For an INVOKER wrapper the write still
runs under RLS. Where the target table's write policy re-states the wrapper's own
identity predicate, the gate is defence in depth and a green suite is CORRECT —
forcing a keystone there would assert on a redundant layer. See the triage column.

| gate / policy | arm | direction | verdict | note |
|---|---|---|---|---|
| public.add_ad_hoc_phase(p_case_id uuid, p_form_id uuid, p_title text, p_recommend_when jsonb, p_assigned_to uuid) | invoker | open-guard(g1=1,g2=0,g3=1) | BLIND |  |
| public.archive_case_narrative_type(p_narrative_type_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.archive_case_outcome(p_outcome_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.archive_case_tag(p_tag_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.archive_meeting_type(p_type_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.archive_phase_result(p_result_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_case_narrative_type(p_commission_id uuid, p_label text, p_description text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_case_outcome(p_commission_id uuid, p_label text, p_color_token text, p_requires_action_plan boolean, p_is_adverse boolean) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_case_tag(p_commission_id uuid, p_name text, p_color_token text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_meeting(p_commission_id uuid, p_title text, p_meeting_type_id uuid, p_scheduled_start timestamp with time zone, p_scheduled_end timestamp with time zone, p_modality text, p_location_text text, p_meeting_url text) | invoker | open-guard(g1=1,g2=0,g3=1) | BLIND |  |
| public.create_meeting_agenda_item(p_meeting_id uuid, p_title text, p_description text, p_discussion_notes text, p_resolution text) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.create_meeting_type(p_commission_id uuid, p_name text, p_color_token text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_member_title(p_commission_id uuid, p_name text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.create_phase_result(p_commission_id uuid, p_label text, p_color_token text, p_is_adverse boolean) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.delete_meeting_agenda_item(p_agenda_item_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.delete_member_title(p_title_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.link_meeting_case(p_meeting_id uuid, p_case_id uuid, p_agenda_item_id uuid, p_summary text, p_decision text) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.mark_meeting_held(p_meeting_id uuid, p_held_at timestamp with time zone, p_held_end timestamp with time zone) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.remove_group_instance(p_response_id uuid, p_instance_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.remove_meeting_attendee(p_attendee_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.rename_case_tag(p_tag_id uuid, p_name text, p_color_token text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.rename_meeting_type(p_type_id uuid, p_name text, p_color_token text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.rename_member_title(p_title_id uuid, p_name text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.reorder_case_narrative_types(p_commission_id uuid, p_ordered_ids uuid[]) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.reorder_case_outcomes(p_commission_id uuid, p_ordered_ids uuid[]) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.reorder_group_instances(p_response_id uuid, p_group_item_id uuid, p_instance_ids uuid[]) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.reorder_meeting_agenda_item(p_agenda_item_id uuid, p_direction text) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.reorder_member_titles(p_commission_id uuid, p_ordered_ids uuid[]) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.reorder_phase_results(p_commission_id uuid, p_ordered_ids uuid[]) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.set_process_outcomes(p_template_version_id uuid, p_outcome_ids uuid[]) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.unlink_meeting_case(p_case_link_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.update_case_narrative_body(p_narrative_id uuid, p_body_md text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.update_case_narrative_type(p_narrative_type_id uuid, p_label text, p_description text) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.update_case_outcome(p_outcome_id uuid, p_label text, p_color_token text, p_requires_action_plan boolean, p_is_adverse boolean) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.update_meeting_agenda_item(p_agenda_item_id uuid, p_title text, p_description text, p_discussion_notes text, p_resolution text) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.update_meeting_attendee(p_attendee_id uuid, p_role text, p_attendance text, p_note text, p_external_name text, p_external_org text) | invoker | open-guard(g1=0,g2=1,g3=0) | BLIND |  |
| public.update_meeting_settings(p_commission_id uuid, p_quorum_rule_type text, p_quorum_value numeric) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.update_phase_result(p_result_id uuid, p_label text, p_color_token text, p_is_adverse boolean) | invoker | open-guard(g1=0,g2=0,g3=1) | BLIND |  |
| public.add_template_phase(p_template_version_id uuid, p_form_id uuid, p_title text, p_recommend_when jsonb, p_default_due_days integer, p_blocks integer[], p_result_ruleset jsonb, p_emits_result boolean, p_allowed_result_ids jsonb) | invoker | open-guard(g1=1,g2=0,g3=0) | BLIND | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) —  |
| public.remove_template_phase(p_phase_id uuid) | invoker | open-guard(g1=2,g2=0,g3=0) | BLIND | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) —  |
| public.update_template_phase(p_phase_id uuid, p_form_id uuid, p_title text, p_recommend_when jsonb, p_clear_recommend_when boolean, p_default_due_days integer, p_clear_default_due_days boolean, p_blocks integer[], p_clear_blocks boolean, p_result_ruleset jsonb, p_clear_result_ruleset boolean, p_emits_result boolean, p_allowed_result_ids jsonb, p_clear_allowed_result_ids boolean) | invoker | open-guard(g1=1,g2=0,g3=0) | BLIND | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) —  |

## COVERED (asserted-through) + ERROR (harness bug)

| gate / policy | arm | direction | verdict | failing files / note |
|---|---|---|---|---|
| public.cancel_case(p_case_id uuid) | invoker | open-guard(g1=0,g2=0,g3=1) | COVERED | 205_administrativo.sql,314_qob_org_admin_content_wall.sql |
| public.close_case(p_case_id uuid) | invoker | open-guard(g1=1,g2=0,g3=1) | ERROR | 150_referrals.sql,205_administrativo.sql,314_qob_org_admin_content_wall.sql |
| public.mark_notification_read(p_id uuid) | invoker | open-guard(g1=1,g2=0,g3=1) | COVERED | 226_notifications.sql |
| public.set_case_outcome(p_case_id uuid, p_outcome_id uuid) | invoker | open-guard(g1=1,g2=0,g3=1) | COVERED | 115_case_outcomes.sql,205_administrativo.sql |
| public.set_meeting_held_window(p_meeting_id uuid, p_held_at timestamp with time zone, p_held_end timestamp with time zone) | invoker | open-guard(g1=0,g2=1,g3=0) | COVERED | 206_meeting_held_time.sql |
| public.archive_process_template(p_template_id uuid) | invoker | open-guard(g1=2,g2=0,g3=0) | COVERED | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) — 100_dashboard.sql,297_process_template_versioning.sql |
| public.get_response_validation_errors(p_response_id uuid) | invoker | open-guard(g1=1,g2=0,g3=0) | COVERED | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) — 274_ff3_validations.sql |
| public.reconcile_item_options(p_item_id uuid, p_options jsonb) | invoker | open-guard(g1=1,g2=0,g3=0) | ERROR | ⚠ g1-only: PROVISIONAL, hand-classify (the opened probe may be a domain check, not the gate) —  |
| public.sign_section(p_response_id uuid, p_section_id uuid, p_note text) | invoker | open-guard(g1=0,g2=0,g3=1) | COVERED | 367_deferred_staff_signoff.sql. ⭐ **RE-MEASURED 2026-08-24 (`FUP-DSS-SIGN-SECTION-INVOKER-VERDICT-STALE`)** on a FRESH `supabase db reset`, baseline `Files=218, Tests=7226, PASS`; transcribed here because a subset run overwrites this file and is then reverted. ⛔ **BOTH halves of the previous row were stale** — it read `open-guard(g1=1,g2=0,g3=0) | COVERED | ⚠ g1-only: PROVISIONAL` against the PRE-ADR-0136 body. ADR 0136 rewrote the lifecycle guard to `if v_status <> 'in_progress' and not app.is_signoff_deferral_open(...)`, which moved the opened guard from class G1 to class G3; `FROMFINDINGS=1 ARM=wrapper` re-measures nothing, so the change was invisible to it by construction. The honest re-run returned **BLIND**. ⭐ **HAND-CLASSIFICATION, which the PROVISIONAL note asked for and nobody had done: none of this wrapper's `if` guards is the authorization gate.** They are a domain probe, a LIFECYCLE window, and two shape checks. `sign_section` is INVOKER, so the authorization decision is the RLS `WITH CHECK` on `response_section_signoffs.signoffs_insert` (`signed_by = auth.uid() AND app.can_sign_section(...)`), which the INSERT reaches as the caller — COVERED independently in the write-path sweep (`251_authz_p0_isolation.sql`) and in the door sweep's predicate arm (`app.can_sign_section`). The BLIND was therefore about the LIFECYCLE guard, and it was real: §5 of 367 pinned that bound by calling the PREDICATES directly, and a predicate call is not a walk through the door. ⭐ Closed by 367 §14, whose FIRST version did **not** close it — keyed on SQLSTATE `23514` alone, it stayed green with the guard open, because the INSERT trigger `guard_submitted_signoffs` shares the same window AND the same SQLSTATE. §14.1 is pinned to the wrapper's own MESSAGE, which is the only thing that separates the two locks. |
| public.cancel_meeting(p_meeting_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | COVERED | 327_invoker_wrapper_meeting_authority.sql |
| public.distribute_meeting(p_meeting_id uuid) | invoker | open-guard(g1=0,g2=1,g3=0) | COVERED | 327_invoker_wrapper_meeting_authority.sql |
| public.set_meeting_quorum_met(p_meeting_id uuid, p_quorum_met boolean) | invoker | open-guard(g1=0,g2=1,g3=0) | COVERED | 327_invoker_wrapper_meeting_authority.sql |
| public.update_meeting_minutes(p_meeting_id uuid, p_minutes_md text) | invoker | open-guard(g1=0,g2=1,g3=0) | COVERED | 327_invoker_wrapper_meeting_authority.sql |

## UNSUPPORTED — no openable guard (NOT a verdict; still owed a keystone)

These stay in `supabase/tests/mutation/authz-unswept-backlog.txt`.

| gate / policy | arm | direction | verdict | why unsupported |
|---|---|---|---|---|
| public.add_template_narrative(p_template_version_id uuid, p_narrative_type_id uuid, p_title text, p_instructions text, p_is_expected boolean) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.assert_profile_tenant_has_org() | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.clone_form_version(p_source_version_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.clone_template_version(p_source_version_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.create_form(p_commission_id uuid, p_title text, p_description text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.create_process_template(p_commission_id uuid, p_title text, p_description text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.delete_section_moving_items(p_section_id uuid, p_target_section_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.discard_response(p_response_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.discard_template_draft(p_template_version_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.form_item_options_code_immutable() | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.guard_profile_no_delete() | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.mark_all_notifications_read() | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.publish_form_version(p_form_version_id uuid, p_approved_by uuid, p_effective_date date, p_review_cycle_months integer, p_review_due_date date) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.publish_process_template(p_template_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.publish_template_version(p_template_version_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.reference_candidates(p_response_id uuid, p_item_id uuid, p_query text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.remove_template_narrative(p_narrative_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.reorder_case_layout_template(p_template_version_id uuid, p_ordered jsonb) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.reorder_item(p_item_id uuid, p_direction text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.reorder_section(p_section_id uuid, p_direction text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.reorder_template_phase(p_phase_id uuid, p_direction text) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.save_section_answers(p_response_id uuid, p_section_id uuid, p_answers jsonb, p_clear_item_ids uuid[], p_observations jsonb, p_selections jsonb, p_other_text jsonb, p_instance_answers jsonb, p_matrix_cells jsonb, p_risk_matrix jsonb, p_references jsonb) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.set_notification_preferences(p_surface text, p_enabled boolean) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.set_template_phase_blocks(p_phase_id uuid, p_blocks integer[]) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.skip_phase(p_case_phase_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.start_or_resume_phase(p_case_phase_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.start_or_resume_response(p_form_version_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.submit_response(p_response_id uuid) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.update_template_narrative(p_narrative_id uuid, p_title text, p_instructions text, p_is_expected boolean, p_clear_title boolean, p_clear_instructions boolean) | invoker | open-guard | UNSUPPORTED | no RLS probe / identity assert / identity condition to open |
| public.add_group_instance(p_response_id uuid, p_group_item_id uuid) | invoker | open-guard | UNSUPPORTED | identity assert called in ASSIGNMENT form (`v := app.assert_*`) — no type-safe neutral value; any verdict here would be about a different guard |
| public.add_meeting_attendee(p_meeting_id uuid, p_user_id uuid, p_external_name text, p_external_org text, p_role text, p_attendance text, p_note text) | invoker | open-guard | UNSUPPORTED | identity assert called in ASSIGNMENT form (`v := app.assert_*`) — no type-safe neutral value; any verdict here would be about a different guard |
| public.seed_expected_meeting_attendees(p_meeting_id uuid) | invoker | open-guard | UNSUPPORTED | identity assert called in ASSIGNMENT form (`v := app.assert_*`) — no type-safe neutral value; any verdict here would be about a different guard |
| public.seed_selected_meeting_attendees(p_meeting_id uuid, p_user_ids uuid[]) | invoker | open-guard | UNSUPPORTED | identity assert called in ASSIGNMENT form (`v := app.assert_*`) — no type-safe neutral value; any verdict here would be about a different guard |
| public.update_meeting(p_meeting_id uuid, p_title text, p_scheduled_start timestamp with time zone, p_modality text, p_meeting_type_id uuid, p_scheduled_end timestamp with time zone, p_location_text text, p_meeting_url text, p_minutes_md text) | invoker | open-guard | UNSUPPORTED | identity assert called in ASSIGNMENT form (`v := app.assert_*`) — no type-safe neutral value; any verdict here would be about a different guard |

## ERROR triage — neither is a blind spot

`ERROR` means the run shape differed from baseline, so the classifier refuses to call it
COVERED. That refusal is correct and deliberate; it is not a pass, so both are triaged
here by hand rather than left bare (CLAUDE.md §6 step 1: "`ERROR` is not a pass").

Both are **coverage-with-abort**: the suite DID go `Result: FAIL`, but a keystone raised
rather than failing cleanly, which aborted its file mid-plan and moved the test count off
baseline. The classifier cannot tell "a keystone failed an assertion" from "a keystone
blew up", and it should not guess.

| gate | evidence | reading |
| --- | --- | --- |
| `public.close_case(p_case_id uuid)` | 3 keystones failed by name, incl. `11.11 ⭐ WALL (M7): close_case refuses the tenancy admin on AUTHORITY`, plus `close_case: an Administrativo cannot conclude a case (42501)` | COVERED in substance — the authority gate is keystoned, and one of the three is a starred WALL test written for exactly this decision |
| `public.reconcile_item_options(p_item_id uuid, p_options jsonb)` | `Result: FAIL`, a file aborted at 4 of 17 planned tests | COVERED in substance. ⚠ Its verdict is also `g1-only`, so what was opened may be a domain probe; the coverage claim rests on the suite reacting at all, not on the guard class |

Neither is allowlisted and neither is in the backlog: they carry a verdict (`ERROR`), which
is what ARM 3 requires, and the hand-reading above is what ARM 5 needs to not treat them as
un-keystoned.
