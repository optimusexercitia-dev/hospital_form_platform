# BACKEND-STATE-SERVICE-ROLE-SEAM — QA review

**Unit:** BACKEND-STATE-SERVICE-ROLE-SEAM · **Branch:** `backend-state-service-role-seam` ·
**Tip:** `f2af19404d0f553a368eabeb27b1f3d97f171ffb` · **Base:** `main` @
`3c66efb61baca68e182b792a79e3f2bb3237b20b` · **Date:** 2026-09-11 · **Reviewer:** qa

Scope per the spawn prompt: a docs-plus-one-gate-constant unit (ADR 0196 D4's remedy applied to
`docs/backend-state/authorization-and-audit.md`'s service-role DML registry). Read-only review;
every finding below is backed by a command run in this session, not by trusting the hub/record.

## Findings

| # | Severity | Where | What | Evidence |
|---|---|---|---|---|
| — | — | — | No findings. Every acceptance-criteria bullet, every README maintenance rule checked, and every gate reproduced exactly as claimed. | see command log below |

No MAJOR, MINOR, or NOTE findings survived verification.

## Verification log (command → result)

1. **Verbatim move, exact boundaries.** `git show main:...authorization-and-audit.md \| sed -n '362,584p'` vs `sed -n '94,316p' docs/backend-state/service-role-dml.md` → `diff` empty, rc 0 (223 lines each). Confirmed line 362 (main) / line 94 (new file, heading) is exactly the `## Service-role DML registry …` heading and line 584 (main) is the line immediately before the next `## AE3 …` heading (line 586) — the range is the whole slice, not a sub-range. New file's slice runs to its own EOF (line 316).
2. **Headings preserved.** `grep -n "^## "` on `main` vs branch `authorization-and-audit.md`: all 20 headings present on both, same text, same order (line numbers shift only because content between them shrank). The `## Service-role DML registry …` heading is retained as a stub (rule 2), not deleted.
3. **Stub marker form.** `sed -n '355,363p'` on the branch file: `⚠ **Superseded** — moved to its own seam, VERBATIM: … See service-role-dml.md § Service-role DML registry (AE1.4 …)` sits directly under the heading and names both FILE and HEADING, satisfying README rule 2 (the pre-2026-09-09 `§ 9999` form that "validated nothing" is not present here).
4. **README rules read** (`docs/backend-state/README.md` lines 57–207): rules 1, 2, 4, 7, 9 and § Writing/refreshing a block, § The seam axis — all quoted and applied below.
5. **New file's `## Current state` block traced sentence-by-sentence** (rule 4 "write from the frozen text"): sampled 6 bullets — "Seven columns … Key · Site · Owner · Reason · Revalidation mechanism · Audit event · Test" (table headers, lines 203/228/244/257/266), "machine-readable half … `path::symbol::writeKind::target`" (frozen line 117), "grouped by what re-establishes authority" (frozen line 101), "census UNDER-COUNTS by design" (frozen lines 136/148/165-166), "'None found in TS' is not 'unaudited'" (frozen line 104, verbatim), "`door: X` is a door's NAME" (frozen lines 162/205) — every sampled bullet traces to a source sentence in the moved slice. No dates, unit codes, or self-owned figures found in the block.
6. **Old file's block re-cut, bound preserved not lost.** Diffed `main`'s old Invariants bullet ("Every service-role write site is registered and machine-diffed by gate 11 … three own reds … under-counts by design … 'None found in TS' … `door: X` is a door's NAME only") against the branch: the bullet is deleted whole from `authorization-and-audit.md`'s Invariants, and every clause in it reappears — expanded, not weakened — in the new file's Invariants (the "three own reds" bound is now spelled out as three explicit shapes; the callDoor under-count gained the bidirectional-substitution detail; the two `⚠` caveats are verbatim). Same pattern for the Open-edges `NONE`/`UNCONFIRMED` sentence: removed from the old file, restated with more precision in the new file's Open edges. Surface bullet on the old file replaced by the exact two-line pointer the hub describes. § Where the detail lives on the old file marks the entry `(a stub + forward marker; the slice is in service-role-dml.md)`.
7. **Preamble byte-identical (gate 16 check A/C).** `diff <(sed -n '3,5p' service-role-dml.md) <(sed -n '3,5p' notifications.md)` → empty, rc 0.
8. **Gate 11 (`node scripts/check-service-role-registry.mjs`)** → `Service-role DML registry: OK -- 45 derived site(s) == 45 registry row(s) (census 40 + callDoor() 5).` rc 0 — byte-identical to the baseline the record quotes.
9. **Gate 11 script diff** (`git diff main -- scripts/check-service-role-registry.mjs`) → exactly 3 hunks: header comment (history note), `DOC` constant (now `service-role-dml.md`), and the failure-path remediation string. `ROOT = process.cwd()` untouched. No behavioural change.
10. **Gate 16 (`node scripts/check-backend-state.mjs`)** → rc 0, no `[D]` warn line anywhere in the output, `12 domain seam(s)`, `16 seam file(s) + README.md, … largest authorization-and-audit.md at 120.3 KB (warn 160 KB / cap 200 KB)`.
11. **Seam-axis arithmetic.** `ls docs/backend-state/*.md \| wc -l` → 17 (incl. README). 17 − 1 (README) − 4 (`generated-*.md`) = 12, matching README's "Eight of these twelve files … four … cross-cutting" (8 + 4 = 12). Arithmetic is true, not asserted.
12. **Citation sweep** (`grep -rn "authorization-and-audit" --include=*.md --include=*.mjs --include=*.sh --include=*.json . \| grep -v node_modules \| grep -i "service-role\|registry"`): `docs/lint-gates.md:25` and `docs/followups/FUP-SERVICE-ROLE-WRITE-SITES-NO-GUARD-VANISH-TEST.md:9` are correctly re-pointed to `service-role-dml.md` (diffed both — confirmed). All other surviving hits are dated historical claims (ADR 0206 itself, `docs/reviews/backend-state-split-review.md` — a review of the *2026-09-09* split, `docs/progress/backend-state-service-role-seam.md`, the follow-up register entry) or the README's own cross-cutting-file list — correctly left alone per ADR 0196 D10. `CONTEXT.md:359/374` diff empty (`git diff main -- CONTEXT.md`) — the privilege-budget citation was untouched, as claimed. No reverse-direction bad link found: `service-role-dml.md`'s own inbound links (README router, gate 11, the two re-pointed follow-ups) resolve to real headings/paths.
13. **ADR 0206.** Header carries `**Status:** Accepted (2026-09-11, at PO ruling)`, `**Area:**`, `**Amends:** [0196](...)` with a full-sentence justification (CLAUDE.md §8 form). Numbering: `ls docs/decisions` shows 0205 is the highest on `main`; `git branch -r` + `git ls-tree` on `origin/main`, `origin/authz-ae5-matrix-arm3-cells` (both top out at 0205), `origin/authz-c2-tier1` (tops at 0180), `origin/authz-enforcement-manifest` (tops at 0193) confirm 0205 is the highest on ANY live ref, so 0206 is correctly `highest + 1` (0202/0204 correctly skipped as reserved). `node scripts/build-adr-index.mjs --check` → `OK (202 ADRs indexed, next free 0207)`, rc 0. The generated back-pointer on 0196 reads `amended by 0197, 0198, 0199, 0206` (`git diff main -- docs/decisions/0196-...md` shows only that one generated line changed). Considered options (A–E) present, specific, and each gives a real rejection reason; Consequences section is self-limiting (explicitly disclaims making the moved projection TRUE) and does not claim anything beyond the diff.
14. **Full chain.** `npm run lint` → rc 0 (read bare after redirecting to a file), eslint at 0 errors/0 warnings (silent invocation under `--max-warnings=0` with overall rc 0), all named gates (11, 13, 15, 16) print OK inside it. `npm run typecheck` → rc 0. `git diff --stat main -- supabase/migrations src` → no output, rc 0 — confirms `test:db`/`e2e:prod` are correctly not owed (no SQL, no `src/` change).
15. **Gate 13** (`check-docs-registers`, inside the lint chain) → `OK (self-test + 24 hubs, 22 records, 96 ledger rows, 164 bugs, … 451 md files scanned for retired citations)`, `build-features-index: OK (24 hubs; index in sync)`. Both indices' diffs (`docs/decisions/INDEX.md`, `docs/features/INDEX.md`) contain only the expected generated rows (202 ADRs / next free 0207; 24 hubs / in progress 1, new BACKEND-STATE-SERVICE-ROLE-SEAM row).
16. **Follow-up body.** `git diff main -- docs/followups/FUP-AE5-MATRIX-ARM3-CELLS-AUTHZ-SEAM-CROSSED-ITS-WARN-LINE.md` → a single appended `**Landed (pending QA):** …` block (dated, names branch/ADR, leaves the sha for the lead, explicitly states "This entry stays OPEN until QA and human approval"). Index header (`status open`) unchanged. The addition is a dated, append-only note consistent with docs/INDEX.md's follow-up shape (Filed · Owner · Severity · Closes when · Status) — it adds information without touching the Status field or prematurely closing the entry.
17. **44-vs-45 inconsistency.** Confirmed pre-existing on `main`: the frozen slice's Summary says `44/44` (`main` slice line 182) and family totals sum to 44, while `grep -c '^| \`'` on the same slice returns **45** on both `main` and the branch's new file — identical inconsistency, not introduced by this unit. Gate 11 parses 45 on both sides of the diff (baseline quoted in the record and the post-move run above are byte-identical). Leaving it frozen (ADR 0196 D5: never delete a posted section) while pointing the new block at gate 11's own output as the live count, rather than re-quoting the tally, is the correct application of README rule 4 ("cut paraphrase, never a bound") — the block does not repeat the wrong number, it names the instrument.

## Could not verify

- The claim that the *2026-09-09* attempt at the link-check mutation "asserted itself applied before the gate ran" (record, §2, "A frozen inconsistency…" paragraph and the LEARN-090 paragraph) is a claim about a **prior session's** run, not reproducible from the current tree — not re-run here. The mutate/restore cycle's *current* correctness was not independently re-run by this review (the record's own bare rc-0/rc-1/rc-0 sequence is internally consistent and the mechanism it describes — check F on a mutated relative link — is plausible given gate 16's `[LINKS]` output format observed in a clean run, but I did not perform a live mutation myself to avoid touching tracked files outside the report I own).
- Exact byte counts quoted in the record (e.g. "123,224 B" for the re-cut old file, "47,349 B" / "316 lines" for the new file) were not individually re-measured with `wc -c`; the line-count and heading-boundary checks above (items 1–2) independently establish the same facts at a coarser grain and found no discrepancy, so this is a completeness gap, not a contradiction.

## Verdict

**Verdict:** APPROVED
