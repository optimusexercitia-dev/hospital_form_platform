---
id: DEFINER-TEMP-TABLE-CONVERGENCE
title: "The four temp-table DEFINERs converge to search_path = '' by narrow ALTER FUNCTION, and the 419 freeze shrinks by exactly four rows"
status: complete
kind: fup-fix
program: AUTHZ
phase: "pre-AE5 remediation — ADR 0208 D4 on touch, D6 precondition discharged by pgTAP 420 (ruled 2026-09-11)"
branch: definer-temp-table-convergence   # cut from main @ b1e9b924
plan: ../plans/authz-evolution.md
progress: ../progress/definer-temp-table-convergence.md
reviews: ["../reviews/definer-temp-table-convergence-review.md"]
adrs: ["0208", "0209"]
handoff: ~
fup: FUP-DEFINER-SEARCH-PATH-NARROW-FIX-FOUR-TEMP-TABLE-DEFINERS-MEASURED-FREE-TO-CONVERGE
---

# DEFINER-TEMP-TABLE-CONVERGENCE — the four measured-free members converge

Closes `FUP-DEFINER-SEARCH-PATH-NARROW-FIX-FOUR-TEMP-TABLE-DEFINERS-MEASURED-FREE-TO-CONVERGE`
(its *Closes when* was **PO to rule**; the PO ruled **converge** on 2026-09-11). ADR
[0208](../decisions/0208-the-candidate-fanout-is-structurally-dominated-and-empty-search-path-is-the-sole-forward-convention.md)
D4 is the standing convention (*"converge to the empty form on touch"*); D6's precondition —
targeted tests before any ALTER — was discharged by pgTAP `420` in `DEFINER-SEARCH-PATH-NARROW-FIX`.
No new ADR: D4's verbatim ruling (*"they may not grow and converge to the empty form on touch"*) already orders this; nothing here is a new convention.

## Acceptance criteria

- [x] **AC-1 — the narrow migration, four members.** One migration (`20261003007420_*`) carrying
      exactly four `ALTER FUNCTION … SET search_path = ''` statements for `app.copy_response_answers`
      · `app.copy_template_version_children` · `app.copy_version_children` · `public.clone_framework`,
      no body re-emission, mirroring `20261003007410`; `prosecdef`/`proconfig` of each MEASURED on the
      live catalog before writing and stated in the header. ⛔ No other function is touched.
- [x] **AC-2 — the freeze shrinks by exactly four.** `supabase/tests/vectors/definer_search_path_freeze.psql`
      regenerated by `--write` ONLY; the diff is a PURE four-row deletion (865 → 861); `419 § 0c`
      rows pin and `§ 0d` md5 pin move in the same change; gate 18 check F reports `removed 4, added 0`.
- [x] **AC-3 — `420` re-cast as the convergence's regression guard, not left pinning the old state.**
      Each of the four is asserted to carry `search_path=""` in `pg_proc` (the ALTER PROVEN applied,
      from the catalog), and each call's EFFECTS (items/sections/phases/answers/selopts/standards/rewired
      counts) are asserted equal to the values `420` measured before convergence. `§ 5`/`§ 5b`'s
      "all four back on the three-schema path" / "4 non-empty" pins flip to the converged truth. The
      `§ 6` planted-control discrimination half is KEPT (the instrument still proves it can fail).
- [x] **AC-4 — the seam.** `docs/backend-state/authorization-and-audit.md`: a new frozen slice appended
      at the bottom AND the `## Current state` block replaced (⛔ NOT converged → converged; 865 → 861;
      stamp re-dated). Nothing else in the frozen text edited in place.
- [x] **AC-5 — gates.** Fresh `supabase db reset --local` + `test:db`; `npm run lint` (gate 18 green)
      + `typecheck` + unit; `gen:types` no diff; authz arms census/hat/floor + wrapper; diff-scoped
      door sweep, predicate + policy arms from ONE invocation, deriver exit read bare; `e2e:prod` once.

**Complete 2026-09-11** — PO approved (*"Approved"*); ledger row in `docs/progress/phase-ledger.md`; detail in the [record](../progress/definer-temp-table-convergence.md).
