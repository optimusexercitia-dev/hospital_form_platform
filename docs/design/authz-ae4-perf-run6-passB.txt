Timing is on.
Pager usage is off.
Time: 0.986 ms
Time: 0.300 ms

################################################################
## SECTION 0 — FIXTURE GATE                                   ##
##  A fixture that did not fully load, or was never ANALYZEd, ##
##  cannot reach the failing state this acceptance is written ##
##  against. Nothing below runs unless both hold.             ##
################################################################
psql:<stdin>:148: NOTICE:  AE4 fixture gate OK — declared scale present, chain tables analyzed.
DO
Time: 7.193 ms
psql:<stdin>:168: NOTICE:  schema "pg_temp" does not exist, skipping
DROP TABLE
Time: 0.339 ms
CREATE TABLE
Time: 3.190 ms
CREATE FUNCTION
Time: 0.910 ms
 ae4_say 
---------
 
(1 row)

Time: 0.819 ms
 ae4_say 
---------
 
(1 row)

Time: 0.151 ms
 ae4_say 
---------
 
(1 row)

Time: 0.238 ms
 ae4_say 
---------
 
(1 row)

Time: 0.208 ms
 ae4_say 
---------
 
(1 row)

Time: 0.190 ms
 ae4_say 
---------
 
(1 row)

Time: 0.184 ms
 ae4_say 
---------
 
(1 row)

Time: 0.177 ms
--- cardinalities the plans below are taken over ---
           t           | count 
-----------------------+-------
 audit_log (census)    |   264
 commissions           |   966
 form_items            | 16020
 hospitals             |   124
 memberships           | 48799
 professional_profiles | 10001
 profiles              | 12036
(7 rows)

Time: 10.716 ms

################################################################
## SECTION 1 — POSITIVE CONTROL (AE0.2 idiom)                 ##
##  If these arms do not DIFFER, the claims GUC is not        ##
##  reaching the policies and every plan below is vacuous.    ##
################################################################
psql:<stdin>:212: NOTICE:  table "ae4_control" does not exist, skipping
DROP TABLE
Time: 0.255 ms
CREATE TABLE
Time: 1.374 ms
GRANT
Time: 0.719 ms
BEGIN
Time: 0.078 ms
SET
Time: 0.131 ms
SET
Time: 0.134 ms
INSERT 0 1
Time: 15391.013 ms (00:15.391)
RESET
Time: 0.154 ms
COMMIT
Time: 1.054 ms
BEGIN
Time: 0.094 ms
SET
Time: 0.167 ms
SET
Time: 0.249 ms
INSERT 0 1
Time: 17299.132 ms (00:17.299)
RESET
Time: 0.131 ms
COMMIT
Time: 1.067 ms
BEGIN
Time: 0.118 ms
SET
Time: 0.170 ms
SET
Time: 0.160 ms
INSERT 0 1
Time: 40062.521 ms (00:40.063)
RESET
Time: 0.143 ms
COMMIT
Time: 0.739 ms
BEGIN
Time: 0.090 ms
SET
Time: 0.125 ms
SET
Time: 0.250 ms
INSERT 0 1
Time: 46629.459 ms (00:46.629)
RESET
Time: 0.136 ms
COMMIT
Time: 0.839 ms
BEGIN
Time: 0.082 ms
SET
Time: 0.168 ms
INSERT 0 1
Time: 2463.739 ms (00:02.464)
RESET
Time: 0.194 ms
COMMIT
Time: 0.171 ms
INSERT 0 1
Time: 1.974 ms
                     arm                      | prof_rows | form_rows | can_edit 
----------------------------------------------+-----------+-----------+----------
 1a MEASURED principal / hat=staff_admin      |     10000 |     16000 | t
 1b LEGACY-arm principal / hat=org_admin      |     10000 |     16000 | t
 1c MEASURED principal / NO active_role claim |         0 |         0 | f
 1d MEASURED principal / WRONG hat=staff      |         0 |         0 | f
 1e authenticated / NO claims at all          |         0 |         0 | 
 1f postgres / RLS BYPASSED (the trap)        |     10001 |     16020 | 
(6 rows)

Time: 0.268 ms
psql:<stdin>:293: NOTICE:  AE4 positive control OK: 3 distinct readings; hat is load-bearing.
DO
Time: 1.091 ms

################################################################
## SECTION 2 — THE PRINCIPAL PROOF                            ##
##  "Its ONLY grant path is the permission arm" — proven, one ##
##  assertion per competing arm, not asserted in prose.       ##
################################################################
BEGIN
Time: 0.103 ms
SET
Time: 0.141 ms
                                       check                                        | result 
------------------------------------------------------------------------------------+--------
 a. app.can_edit_commission_forms  (must be TRUE)                                   | t
 b. app.is_tenancy_admin_of_for    (must be FALSE — the ONLY other arm)             | f
 c. authz.has_permission           (must be TRUE — the arm that grants)             | t
 d. app.is_admin()                 (must be FALSE)                                  | f
 e. app.can_manage_professional    (must be FALSE — the org-authority arm)          | f
 f. app.can_read_professional_profile (must be TRUE)                                | t
 g. authz.has_permission org.professionals.read (must be TRUE)                      | t
 h. principal holds org_admin/hospital_admin anywhere (must be FALSE)               | f
 i. profiles.is_admin (must be FALSE)                                               | f
 j. case-participant traversal arm of can_read_professional_profile (must be FALSE) | f
(10 rows)

Time: 5.989 ms
psql:<stdin>:376: NOTICE:  AE4 principal proof OK — every competing arm is FALSE and the authorizers are TRUE.
DO
Time: 3.110 ms
ROLLBACK
Time: 0.111 ms

################################################################
## SECTION 3 — THE ABLATION                                   ##
##  Disable LAYER 2 only (authz.roles.state) and both         ##
##  authorizers must go FALSE. Membership, hat, profile and   ##
##  every legacy arm are untouched, so anything still TRUE    ##
##  here is granted by an arm this measurement does not see.  ##
##  Then ROLLBACK must bring both back to TRUE: a probe that  ##
##  moves but does not restore is a broken harness, not a     ##
##  finding.                                                  ##
################################################################
BEGIN
Time: 0.059 ms
UPDATE 1
Time: 0.329 ms
SET
Time: 0.095 ms
                           check                            | result 
------------------------------------------------------------+--------
 ABLATED: app.can_edit_commission_forms (must be FALSE)     | f
 ABLATED: app.can_read_professional_profile (must be FALSE) | f
(2 rows)

Time: 2.503 ms
DO
Time: 2.561 ms
psql:<stdin>:418: NOTICE:  AE4 ablation OK — layer 2 off => both authorizers FALSE.
ROLLBACK
Time: 0.136 ms
BEGIN
Time: 0.135 ms
SET
Time: 0.139 ms
psql:<stdin>:438: NOTICE:  AE4 ablation rollback OK — both authorizers restored to TRUE.
DO
Time: 2.987 ms
ROLLBACK
Time: 0.187 ms
           check            |    code     |     state     
----------------------------+-------------+---------------
 authz.roles.state restored | staff_admin | authoritative
(1 row)

Time: 0.211 ms

################################################################
## PASS A — EXPLAIN (ANALYZE, BUFFERS), 3 reps per path       ##
##  Rep 1 pays the cold-cache cost; reps 2-3 are warm. All    ##
##  three are reported: the difference is signal.             ##
################################################################

=== M1 — READ, unfiltered: public.professional_profiles ===
=== sole permissive SELECT policy => can_read_professional_profile
=== is evaluated ONCE PER ROW with nothing able to short-circuit it.
BEGIN
Time: 0.106 ms
SET
Time: 0.123 ms
SET
Time: 0.109 ms
                                                                       QUERY PLAN                                                                        
---------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2810.56..2810.57 rows=1 width=8) (actual time=10.864..10.866 rows=1 loops=1)
   Buffers: shared hit=735
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.003 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2798.03 rows=5000 width=0) (actual time=9.372..10.508 rows=10000 loops=1)
         Filter: CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END
         Rows Removed by Filter: 1
         Buffers: shared hit=735
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=2.207..3.111 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.137 ms
 Execution Time: 10.917 ms
(16 rows)

Time: 11.317 ms
                                                                       QUERY PLAN                                                                        
---------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2810.56..2810.57 rows=1 width=8) (actual time=8.777..8.779 rows=1 loops=1)
   Buffers: shared hit=734
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.002 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2798.03 rows=5000 width=0) (actual time=7.376..8.420 rows=10000 loops=1)
         Filter: CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END
         Rows Removed by Filter: 1
         Buffers: shared hit=734
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=1.378..2.220 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.086 ms
 Execution Time: 8.812 ms
(16 rows)

Time: 9.135 ms
                                                                       QUERY PLAN                                                                        
---------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2810.56..2810.57 rows=1 width=8) (actual time=13.636..13.639 rows=1 loops=1)
   Buffers: shared hit=734
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.003 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2798.03 rows=5000 width=0) (actual time=12.089..13.285 rows=10000 loops=1)
         Filter: CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END
         Rows Removed by Filter: 1
         Buffers: shared hit=734
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=1.332..2.342 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.062 ms
 Execution Time: 13.671 ms
(16 rows)

Time: 14.079 ms
RESET
Time: 0.170 ms
ROLLBACK
Time: 0.194 ms

=== M1b — READ, org-filtered (the realistic list read). THIS IS THE
=== P5 STATEMENT: the same rows are reachable by the legacy-arm
=== principal below, so the two runs differ ONLY in which arm grants.
BEGIN
Time: 0.106 ms
SET
Time: 0.118 ms
SET
Time: 0.143 ms
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=4.189..4.190 rows=1 loops=1)
   Buffers: shared hit=402
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=2.558..3.812 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=402
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=1.648..2.533 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.001..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.140 ms
 Execution Time: 4.255 ms
(16 rows)

Time: 4.649 ms
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=4.122..4.124 rows=1 loops=1)
   Buffers: shared hit=402
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=2.612..3.743 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=402
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=1.520..2.596 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.085 ms
 Execution Time: 4.153 ms
(16 rows)

Time: 4.477 ms
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=3.967..3.968 rows=1 loops=1)
   Buffers: shared hit=402
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=2.475..3.576 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=402
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=1.571..2.457 rows=2 loops=1)
                 Buffers: shared hit=237
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.095 ms
 Execution Time: 3.999 ms
(16 rows)

Time: 4.329 ms
RESET
Time: 0.121 ms
ROLLBACK
Time: 0.118 ms

=== M1b-LEGACY — the SAME statement, the LEGACY-arm principal.
=== org_admin reaches these rows through app.can_manage_professional
=== and never evaluates authz.has_permission. P5 = M1b / M1b-LEGACY.
BEGIN
Time: 0.097 ms
SET
Time: 0.107 ms
SET
Time: 0.124 ms
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=2522.402..2522.404 rows=1 loops=1)
   Buffers: shared hit=160408
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.007..0.007 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=4.128..2519.494 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=160408
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=3.790..3.791 rows=0 loops=1)
                 Buffers: shared hit=243
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.001..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.111 ms
 Execution Time: 2522.442 ms
(16 rows)

Time: 2522.792 ms (00:02.523)
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=3125.739..3125.740 rows=1 loops=1)
   Buffers: shared hit=160408
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.008..0.008 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=4.093..3121.244 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=160408
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=3.778..3.778 rows=0 loops=1)
                 Buffers: shared hit=243
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.133 ms
 Execution Time: 3125.778 ms
(16 rows)

Time: 3126.213 ms (00:03.126)
                                                                                                           QUERY PLAN                                                                                                           
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2835.56..2835.57 rows=1 width=8) (actual time=2528.270..2528.273 rows=1 loops=1)
   Buffers: shared hit=160408
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.044..0.044 rows=1 loops=1)
   ->  Seq Scan on professional_profiles  (cost=7.77..2823.03 rows=5000 width=0) (actual time=5.742..2525.387 rows=10000 loops=1)
         Filter: ((organization_id = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'::uuid) AND CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END)
         Rows Removed by Filter: 1
         Buffers: shared hit=160408
         SubPlan 1
           ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual time=5.176..5.176 rows=0 loops=1)
                 Buffers: shared hit=243
                 ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual time=0.000..0.001 rows=1 loops=1)
 Planning:
   Buffers: shared hit=1
 Planning Time: 0.079 ms
 Execution Time: 2528.306 ms
(16 rows)

Time: 2528.764 ms (00:02.529)
RESET
Time: 0.180 ms
ROLLBACK
Time: 0.230 ms

=== M2 — WRITE, single row: public.forms.
=== forms_staff_admin_write is the ONLY policy applicable to an UPDATE
=== (forms_select is SELECT-only), so the permission arm cannot be
=== short-circuited by a sibling policy the way a SELECT can be.
=== Rolled back — EXPLAIN ANALYZE EXECUTES the statement.
BEGIN
Time: 0.101 ms
SET
Time: 0.202 ms
SET
Time: 0.174 ms
                                                                                                              QUERY PLAN                                                                                                              
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on forms  (cost=0.27..9.29 rows=0 width=0) (actual time=6.560..6.562 rows=0 loops=1)
   Buffers: shared hit=138 dirtied=1
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.008..0.008 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.006..0.007 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.015..0.015 rows=1 loops=1)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.006..0.006 rows=1 loops=1)
   ->  Index Scan using forms_pkey on forms  (cost=0.14..9.16 rows=1 width=30) (actual time=3.313..3.314 rows=1 loops=1)
         Index Cond: (id = '24fe5757-a7bd-fc3e-77ae-12625b9f597b'::uuid)
         Filter: (app.can_edit_commission_forms(commission_id, (InitPlan 3).col1) AND (app.can_edit_commission_forms(commission_id, (InitPlan 4).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id)))
         Buffers: shared hit=56
 Planning:
   Buffers: shared hit=3
 Planning Time: 0.182 ms
 Trigger audit_forms_trg: time=3.786 calls=1
 Trigger touch_forms_updated_at: time=0.129 calls=1
 Execution Time: 10.433 ms
(20 rows)

Time: 11.015 ms
RESET
Time: 0.139 ms
ROLLBACK
Time: 0.122 ms
BEGIN
Time: 0.069 ms
SET
Time: 0.112 ms
SET
Time: 0.109 ms
                                                                                                              QUERY PLAN                                                                                                              
--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on forms  (cost=0.27..9.29 rows=0 width=0) (actual time=3.296..3.297 rows=0 loops=1)
   Buffers: shared hit=114
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.002 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.002 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.009..0.010 rows=1 loops=1)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.002 rows=1 loops=1)
   ->  Index Scan using forms_pkey on forms  (cost=0.14..9.16 rows=1 width=30) (actual time=1.767..1.768 rows=1 loops=1)
         Index Cond: (id = '24fe5757-a7bd-fc3e-77ae-12625b9f597b'::uuid)
         Filter: (app.can_edit_commission_forms(commission_id, (InitPlan 3).col1) AND (app.can_edit_commission_forms(commission_id, (InitPlan 4).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id)))
         Buffers: shared hit=56
 Planning Time: 0.181 ms
 Trigger audit_forms_trg: time=0.628 calls=1
 Trigger touch_forms_updated_at: time=0.017 calls=1
 Execution Time: 3.963 ms
(18 rows)

Time: 4.424 ms
RESET
Time: 0.112 ms
ROLLBACK
Time: 0.142 ms

=== M3 — WRITE, one version (200 items). The per-row write gate.
BEGIN
Time: 0.097 ms
SET
Time: 0.221 ms
SET
Time: 0.213 ms
                                                                                                                                                                                                           QUERY PLAN                                                                                                                                                                                                            
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on form_items  (cost=6.40..876.15 rows=0 width=0) (actual time=731.863..731.866 rows=0 loops=1)
   Buffers: shared hit=29218 dirtied=188 written=4
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.009..0.009 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.009..0.009 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.012..0.012 rows=1 loops=1)
   InitPlan 5
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.008..0.009 rows=1 loops=1)
   InitPlan 6
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   ->  Bitmap Heap Scan on form_items  (cost=6.21..875.96 rows=68 width=10) (actual time=3.267..355.489 rows=200 loops=1)
         Recheck Cond: (form_version_id = '160fb036-65bf-aeb0-51c6-1575451ce8ad'::uuid)
         Filter: (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 4).col1) AND (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 5).col1) OR app.can_access_targeted_version(form_version_id, (InitPlan 6).col1) OR app.is_member_of(app.commission_of_version(form_version_id)) OR app.is_tenancy_admin_of(app.commission_of_version(form_version_id))))
         Heap Blocks: exact=5
         Buffers: shared hit=12023
         ->  Bitmap Index Scan on form_items_version_idx  (cost=0.00..6.19 rows=254 width=0) (actual time=0.037..0.037 rows=200 loops=1)
               Index Cond: (form_version_id = '160fb036-65bf-aeb0-51c6-1575451ce8ad'::uuid)
               Buffers: shared hit=2
 Planning:
   Buffers: shared hit=3
 Planning Time: 0.232 ms
 Trigger audit_form_items_trg: time=62.128 calls=200
 Trigger guard_published_items_trg: time=6.527 calls=200
 Execution Time: 794.114 ms
(28 rows)

Time: 794.919 ms
RESET
Time: 0.147 ms
ROLLBACK
Time: 0.172 ms
BEGIN
Time: 0.118 ms
SET
Time: 0.130 ms
SET
Time: 0.096 ms
                                                                                                                                                                                                           QUERY PLAN                                                                                                                                                                                                            
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on form_items  (cost=6.43..886.79 rows=0 width=0) (actual time=765.549..765.553 rows=0 loops=1)
   Buffers: shared hit=30022 dirtied=10 written=3
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.004..0.004 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.003..0.004 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.009..0.009 rows=1 loops=1)
   InitPlan 5
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.003..0.003 rows=1 loops=1)
   InitPlan 6
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   ->  Bitmap Heap Scan on form_items  (cost=6.23..886.60 rows=69 width=10) (actual time=2.096..375.521 rows=200 loops=1)
         Recheck Cond: (form_version_id = '160fb036-65bf-aeb0-51c6-1575451ce8ad'::uuid)
         Filter: (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 4).col1) AND (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 5).col1) OR app.can_access_targeted_version(form_version_id, (InitPlan 6).col1) OR app.is_member_of(app.commission_of_version(form_version_id)) OR app.is_tenancy_admin_of(app.commission_of_version(form_version_id))))
         Heap Blocks: exact=10
         Buffers: shared hit=12028
         ->  Bitmap Index Scan on form_items_version_idx  (cost=0.00..6.21 rows=257 width=0) (actual time=0.027..0.027 rows=395 loops=1)
               Index Cond: (form_version_id = '160fb036-65bf-aeb0-51c6-1575451ce8ad'::uuid)
               Buffers: shared hit=2
 Planning Time: 0.217 ms
 Trigger audit_form_items_trg: time=72.248 calls=200
 Trigger guard_published_items_trg: time=6.280 calls=200
 Execution Time: 837.922 ms
(26 rows)

Time: 838.546 ms
RESET
Time: 0.128 ms
ROLLBACK
Time: 0.176 ms

=== M3b — WRITE, every version of the measured commission (16 000 items).
⛔ M2 / M3 / M3b TIMINGS MAY NOT BE USED FOR A RATIO. public.form_items
   carries audit_form_items_trg, a FOR EACH ROW trigger that appends to
   the hash-chained audit_log, and its per-row cost dwarfs the policy
   predicate. A "linear growth" reading here would be a fact about the
   AUDIT TRIGGER, not about the seam. These three paths contribute PLAN
   SHAPE ONLY (pass condition P1). The linearity condition P4 is
   measured on the READ path in section 7, where no trigger fires.
BEGIN
Time: 0.075 ms
SET
Time: 0.147 ms
SET
Time: 0.122 ms
                                                                                                                                                                                                              QUERY PLAN                                                                                                                                                                                                               
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on form_items  (cost=641.41..25735.37 rows=0 width=0) (actual time=60519.269..60519.277 rows=0 loops=1)
   Buffers: shared hit=2345165 dirtied=1374 written=291
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.006..0.006 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.004..0.004 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.005..0.005 rows=1 loops=1)
   InitPlan 5
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.003..0.003 rows=1 loops=1)
   InitPlan 6
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 9
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.002 rows=1 loops=1)
   InitPlan 10
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 11
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.008..0.008 rows=1 loops=1)
   ->  Nested Loop  (cost=641.12..25735.08 rows=2851 width=22) (actual time=184.612..29618.622 rows=16000 loops=1)
         Buffers: shared hit=966214
         ->  HashAggregate  (cost=640.83..641.26 rows=43 width=28) (actual time=182.521..182.864 rows=80 loops=1)
               Group Key: v.id
               Batches: 1  Memory Usage: 32kB
               Buffers: shared hit=5668
               ->  Nested Loop  (cost=0.14..640.72 rows=43 width=28) (actual time=2.778..182.324 rows=80 loops=1)
                     Buffers: shared hit=5668
                     ->  Seq Scan on forms f  (cost=0.00..34.55 rows=28 width=22) (actual time=1.109..36.372 rows=40 loops=1)
                           Filter: ((commission_id = '0585641f-07df-2b46-928e-bfaae13d22e2'::uuid) AND (app.can_edit_commission_forms(commission_id, (InitPlan 11).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id)))
                           Rows Removed by Filter: 4
                           Buffers: shared hit=1081
                     ->  Index Scan using form_versions_form_idx on form_versions v  (cost=0.14..21.63 rows=2 width=38) (actual time=1.767..3.645 rows=2 loops=40)
                           Index Cond: (form_id = f.id)
                           Filter: (app.can_edit_commission_forms((SubPlan 8), (InitPlan 9).col1) OR app.can_access_targeted_version(id, (InitPlan 10).col1) OR app.is_member_of(app.commission_of_version(id)) OR app.is_tenancy_admin_of(app.commission_of_version(id)))
                           Buffers: shared hit=4587
                           SubPlan 8
                             ->  Index Scan using forms_pkey on forms f_1  (cost=0.17..8.94 rows=1 width=16) (actual time=0.912..0.913 rows=1 loops=80)
                                   Index Cond: (id = v.form_id)
                                   Filter: (app.can_edit_commission_forms(commission_id, (InitPlan 7).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id))
                                   Buffers: shared hit=2320
                                   InitPlan 7
                                     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.003..0.003 rows=1 loops=1)
         ->  Index Scan using form_items_version_idx on form_items  (cost=0.29..582.92 rows=66 width=26) (actual time=1.973..367.735 rows=200 loops=80)
               Index Cond: (form_version_id = v.id)
               Filter: (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 4).col1) AND (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 5).col1) OR app.can_access_targeted_version(form_version_id, (InitPlan 6).col1) OR app.is_member_of(app.commission_of_version(form_version_id)) OR app.is_tenancy_admin_of(app.commission_of_version(form_version_id))))
               Buffers: shared hit=960546
 Planning:
   Buffers: shared hit=35
 Planning Time: 0.562 ms
 Trigger audit_form_items_trg: time=5853.052 calls=16000
 Trigger guard_published_items_trg: time=529.044 calls=16000
 Execution Time: 66377.597 ms
(53 rows)

Time: 66378.701 ms (01:06.379)
RESET
Time: 0.121 ms
ROLLBACK
Time: 5.858 ms
BEGIN
Time: 0.135 ms
SET
Time: 0.130 ms
SET
Time: 0.139 ms
                                                                                                                                                                                                              QUERY PLAN                                                                                                                                                                                                               
---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 Update on form_items  (cost=641.41..50008.21 rows=0 width=0) (actual time=58085.709..58085.719 rows=0 loops=1)
   Buffers: shared hit=2435349 dirtied=299 written=294
   InitPlan 1
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.006..0.006 rows=1 loops=1)
   InitPlan 2
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.003..0.004 rows=1 loops=1)
   InitPlan 3
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 4
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.006..0.006 rows=1 loops=1)
   InitPlan 5
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.004..0.004 rows=1 loops=1)
   InitPlan 6
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 9
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.003 rows=1 loops=1)
   InitPlan 10
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
   InitPlan 11
     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.012..0.012 rows=1 loops=1)
   ->  Nested Loop  (cost=641.12..50007.92 rows=5626 width=22) (actual time=174.967..28408.400 rows=16000 loops=1)
         Buffers: shared hit=966687
         ->  HashAggregate  (cost=640.83..641.26 rows=43 width=28) (actual time=172.880..173.113 rows=80 loops=1)
               Group Key: v.id
               Batches: 1  Memory Usage: 32kB
               Buffers: shared hit=5668
               ->  Nested Loop  (cost=0.14..640.72 rows=43 width=28) (actual time=2.697..172.657 rows=80 loops=1)
                     Buffers: shared hit=5668
                     ->  Seq Scan on forms f  (cost=0.00..34.55 rows=28 width=22) (actual time=1.029..34.177 rows=40 loops=1)
                           Filter: ((commission_id = '0585641f-07df-2b46-928e-bfaae13d22e2'::uuid) AND (app.can_edit_commission_forms(commission_id, (InitPlan 11).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id)))
                           Rows Removed by Filter: 4
                           Buffers: shared hit=1081
                     ->  Index Scan using form_versions_form_idx on form_versions v  (cost=0.14..21.63 rows=2 width=38) (actual time=1.754..3.459 rows=2 loops=40)
                           Index Cond: (form_id = f.id)
                           Filter: (app.can_edit_commission_forms((SubPlan 8), (InitPlan 9).col1) OR app.can_access_targeted_version(id, (InitPlan 10).col1) OR app.is_member_of(app.commission_of_version(id)) OR app.is_tenancy_admin_of(app.commission_of_version(id)))
                           Buffers: shared hit=4587
                           SubPlan 8
                             ->  Index Scan using forms_pkey on forms f_1  (cost=0.17..8.94 rows=1 width=16) (actual time=0.876..0.877 rows=1 loops=80)
                                   Index Cond: (id = v.form_id)
                                   Filter: (app.can_edit_commission_forms(commission_id, (InitPlan 7).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id))
                                   Buffers: shared hit=2320
                                   InitPlan 7
                                     ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual time=0.002..0.003 rows=1 loops=1)
         ->  Index Scan using form_items_version_idx on form_items  (cost=0.29..1146.75 rows=131 width=26) (actual time=1.747..352.734 rows=200 loops=80)
               Index Cond: (form_version_id = v.id)
               Filter: (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 4).col1) AND (app.can_edit_commission_forms(app.commission_of_version(form_version_id), (InitPlan 5).col1) OR app.can_access_targeted_version(form_version_id, (InitPlan 6).col1) OR app.is_member_of(app.commission_of_version(form_version_id)) OR app.is_tenancy_admin_of(app.commission_of_version(form_version_id))))
               Buffers: shared hit=961019
 Planning:
   Buffers: shared hit=12
 Planning Time: 0.485 ms
 Trigger audit_form_items_trg: time=5209.032 calls=16000
 Trigger guard_published_items_trg: time=488.881 calls=16000
 Execution Time: 63299.231 ms
(53 rows)

Time: 63300.241 ms (01:03.300)
RESET
Time: 0.165 ms
ROLLBACK
Time: 9.305 ms

=== M4 — ATTRIBUTION ONLY, NOT THE ACCEPTANCE. The isolated seam:
=== ~10 000 direct authz.has_permission calls. Reported so the
=== per-call cost can be attributed, never as evidence about the
=== final path (that is exactly the mistake the FUP forbids).
⛔ The scope id VARIES per row on purpose. has_permission is STABLE, so
   with four constant arguments the planner folds it to a single
   InitPlan evaluation and the "10 000 calls" would be one call — a
   measurement of nothing that reads as a spectacularly fast seam.
⛔ M4 SEQ-SCANS public.commissions BY DESIGN — its own FROM clause asks
   for every commission, and at 966 rows with loops=1 that is the
   correct plan. It is NOT a P1 hit: P1 bounds access INSIDE the
   DEFINER bodies, which only Pass B can show. Run 1 flagged this
   line because the P1 grep was unscoped -- scope it to the nested
   region sentinels (acceptance doc section 9.7).
BEGIN
Time: 0.109 ms
SET
Time: 0.136 ms
                                                          QUERY PLAN                                                           
-------------------------------------------------------------------------------------------------------------------------------
 Aggregate  (cost=2848.08..2848.09 rows=1 width=8) (actual time=8004.022..8004.023 rows=1 loops=1)
   Buffers: shared hit=276483
   ->  Nested Loop  (cost=0.00..165.01 rows=10626 width=16) (actual time=0.019..7.667 rows=10626 loops=1)
         Buffers: shared hit=20
         ->  Function Scan on generate_series g  (cost=0.00..0.11 rows=11 width=0) (actual time=0.006..0.013 rows=11 loops=1)
         ->  Materialize  (cost=0.00..34.49 rows=966 width=16) (actual time=0.001..0.256 rows=966 loops=11)
               Buffers: shared hit=20
               ->  Seq Scan on commissions c  (cost=0.00..29.66 rows=966 width=16) (actual time=0.010..0.383 rows=966 loops=1)
                     Buffers: shared hit=20
 Planning:
   Buffers: shared hit=9
 Planning Time: 0.127 ms
 Execution Time: 8004.070 ms
(13 rows)

Time: 8004.517 ms (00:08.005)
ROLLBACK
Time: 0.128 ms

################################################################
## PASS B — NESTED BODY PLANS  (opt-in:  -v NESTED=1)         ##
##  A SECURITY DEFINER function is never inlined, so PASS A   ##
##  shows only an outer Filter / Function Scan and the BODY   ##
##  plan is invisible. Pass conditions P1, P2 and P3 are read ##
##  from THIS output and nowhere else.                        ##
##  ⛔ Row counts are deliberately BOUNDED here (200, not     ##
##  10 000): auto_explain emits one plan per nested statement ##
##  per row. These are their own named paths (M1-nested,      ##
##  M3-nested) and their numbers are NOT M1s or M3s.          ##
##                                                            ##
##  ⛔ RUN 1 (2026-09-02) NEVER REACHED THIS BLOCK. It sat     ##
##  AFTER sections 7-8 and psql died in section 7 under       ##
##  ON_ERROR_STOP, so P1/P2/P3 had no evidence and the        ##
##  absence was misread as "EXPLAIN cannot descend into a     ##
##  DEFINER body" — true of PASS A, and the wrong mechanism.  ##
##  Pass B now runs BEFORE anything that can abort, so the    ##
##  structural evidence survives a later failure.             ##
##  ⛔ AND: an absent subject must never read as a pass. The   ##
##  SUBJECT-PRESENT markers below are what the P1/P2/P3 greps ##
##  check FIRST; zero occurrences is VOID, never PASS.        ##
################################################################
@@AE4_NESTED_BEGIN@@
SET
Time: 0.187 ms
SET
Time: 0.228 ms
SET
Time: 0.162 ms
SET
Time: 0.123 ms
SET
Time: 0.139 ms
SET
Time: 0.158 ms
SET
Time: 0.134 ms
=== PASS B / M1-nested — 200 protected rows through the READ policy ===
BEGIN
Time: 0.082 ms
SET
Time: 0.125 ms
SET
Time: 0.103 ms
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.292 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=15
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=9
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.321 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.015 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'organization', $4 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 2.758 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b', $4 = 'org.professionals.read'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Rows Removed by Join Filter: 15
  Buffers: shared hit=75
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=16 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=73
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=16 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 4
              Buffers: shared hit=72
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 3.247 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b', $4 = 'org.professionals.read'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=87
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=86
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.276 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.033 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'organization', $4 = '841e0ad8-9fd5-7de0-592e-f5b79e974151'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 2.905 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '841e0ad8-9fd5-7de0-592e-f5b79e974151', $4 = 'org.professionals.read'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=4 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=72
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=4 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=70
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=4 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 16
              Buffers: shared hit=69
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 3.405 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '841e0ad8-9fd5-7de0-592e-f5b79e974151', $4 = 'org.professionals.read'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=84
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=83
psql:<stdin>:623: LOG:  duration: 7.649 ms  plan:
Query Text: 
  with candidate as materialized (
    -- PROPOSE. One id per assignment fact, deduplicated before anything is confirmed.
    -- A wrong proposal can only lose a grant; it can never invent one.
    select distinct case
             when af.scope_kind = p_resolution_kind then af.scope_id
             when p_resolution_kind = 'organization' and af.scope_kind = 'commission'
               then (select c.organization_id from public.commissions c where c.id = af.scope_id)
             when p_resolution_kind = 'organization' and af.scope_kind = 'hospital'
               then (select h.organization_id from public.hospitals h where h.id = af.scope_id)
             when p_resolution_kind = 'hospital' and af.scope_kind = 'commission'
               then (select c.hospital_id from public.commissions c where c.id = af.scope_id)
           end as scope_id
      from authz.assignment_facts(p_principal) af
  )
  -- CONFIRM. The unmodified runtime resolver is the authority, not this function.
  select c.scope_id
    from candidate c
   where c.scope_id is not null
     and authz.has_permission(p_principal, p_resolution_kind, c.scope_id, p_permission_code);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = 'org.professionals.read'
CTE Scan on candidate c  (cost=20217.58..20271.58 rows=66 width=16) (actual rows=2 loops=1)
  Filter: ((scope_id IS NOT NULL) AND authz.has_permission($1, $2, scope_id, $3))
  Buffers: shared hit=246
  CTE candidate
    ->  Unique  (cost=20212.58..20217.58 rows=200 width=16) (actual rows=2 loops=1)
          Buffers: shared hit=75
          ->  Sort  (cost=20212.58..20215.08 rows=1000 width=16) (actual rows=20 loops=1)
                Sort Key: (CASE WHEN (af.scope_kind = $2) THEN af.scope_id WHEN (($2 = 'organization'::text) AND (af.scope_kind = 'commission'::text)) THEN (SubPlan 1) WHEN (($2 = 'organization'::text) AND (af.scope_kind = 'hospital'::text)) THEN (SubPlan 2) WHEN (($2 = 'hospital'::text) AND (af.scope_kind = 'commission'::text)) THEN (SubPlan 3) ELSE NULL::uuid END)
                Sort Method: quicksort  Memory: 25kB
                Buffers: shared hit=75
                ->  Function Scan on assignment_facts af  (cost=0.25..20162.75 rows=1000 width=16) (actual rows=20 loops=1)
                      Buffers: shared hit=75
                      SubPlan 1
                        ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=17)
                              Index Cond: (id = af.scope_id)
                              Buffers: shared hit=51
                      SubPlan 2
                        ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=3)
                              Filter: (id = af.scope_id)
                              Rows Removed by Filter: 123
                              Buffers: shared hit=6
                      SubPlan 3
                        ->  Index Scan using commissions_pkey on commissions c_2  (cost=0.28..8.29 rows=1 width=16) (never executed)
                              Index Cond: (id = af.scope_id)
psql:<stdin>:623: LOG:  duration: 7.928 ms  plan:
Query Text: 
  select authz.authorized_scope_ids(
           (select auth.uid()),
           'organization',
           'org.professionals.read'
         );

ProjectSet  (cost=0.03..5.30 rows=1000 width=16) (actual rows=2 loops=1)
  Buffers: shared hit=246
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.015 ms  plan:
Query Text: v_is_admin := exists (
      select 1 from public.profiles
      where id = auth.uid() and is_admin = true
    )
Result  (cost=8.32..8.33 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.31..8.32 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid)
          Filter: is_admin
          Rows Removed by Filter: 1
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: select organization_id              from public.professional_profiles
  where id = p_profile_id
Index Scan using professional_profiles_pkey on professional_profiles  (cost=0.29..8.30 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: v_is_admin := exists (
      select 1 from public.profiles
      where id = auth.uid() and is_admin = true
    )
Result  (cost=8.32..8.33 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.31..8.32 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid)
          Filter: is_admin
          Rows Removed by Filter: 1
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.023 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'organization', $2 = '0c000000-0000-0000-0000-00000000000a', $3 = 'org_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.610 ms  plan:
Query Text: 
  select app.is_active((select auth.uid()))
     and app.has_role('organization', p_org_id, 'org_admin', (select auth.uid()));

Query Parameters: $1 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=0.07..0.57 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=9
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  InitPlan 2
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.928 ms  plan:
Query Text: 
  -- ORG AUTHORITY ONLY since AE4.7c. The commission staff_admin ascent moved to
  -- app.can_create_professional (row 43) — a staff_admin ADDS a professional, never modifies
  -- or redacts one (matrix § 12.8.5).
  --
  -- The `is_org_admin_of(p_org)` disjunct reads the CALLER's auth.uid(), not the `p_uid`
  -- target parameter, and so does `is_admin()`. With the ascent gone, `p_uid` is a null guard
  -- and nothing else: FUP-CAN-MANAGE-PROFESSIONAL-SELF-CHECK-ARM, narrowed rather than fixed.
  -- Left alone deliberately — changing it would move an answer, and this migration's claim is
  -- that only the staff_admin arm moves.
  select p_uid is not null and (
    coalesce(app.is_admin(), false)
    or app.is_org_admin_of(p_org)
  );

Query Parameters: $1 = '0c000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.386 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.019 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.034 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.020 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.015 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.049 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.016 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.057 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.030 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
          Index Cond: (id = $2)
          Buffers: shared hit=3
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.081 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.031 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 0.023 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'organization', $4 = '0c000000-0000-0000-0000-00000000000a'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (actual rows=1 loops=1)
          Filter: (id = $2)
          Rows Removed by Filter: 123
          Buffers: shared hit=2
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:623: LOG:  duration: 4.910 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '0c000000-0000-0000-0000-00000000000a', $4 = 'org.professionals.read'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=0 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=71
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=0 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=69
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=0 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 20
              Buffers: shared hit=69
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (never executed)
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (never executed)
psql:<stdin>:623: LOG:  duration: 5.466 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'organization', $3 = '0c000000-0000-0000-0000-00000000000a', $4 = 'org.professionals.read'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=83
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=0 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=82
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: select commission_id, visibility_policy
                                  from public.cases where id = p_case_id
Seq Scan on cases  (cost=0.00..1.10 rows=1 width=48) (actual rows=1 loops=1)
  Filter: (id = $1)
  Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.021 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1
    from public.case_participants cp
    join public.case_participant_roles r on r.id = cp.role_id
    join public.professional_participants pp on pp.participant_id = cp.participant_id
    join public.professional_profiles prof on prof.id = pp.professional_profile_id
    where cp.case_id = p_case_id
      and cp.removed_at is null
      and r.key = 'respondent_doctor'
      and prof.user_id = p_uid
  );

Query Parameters: $1 = 'd0000000-0000-0000-0000-0000000000c1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=23.73..23.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Nested Loop  (cost=0.27..23.73 rows=1 width=0) (actual rows=0 loops=1)
          Join Filter: (pp.professional_profile_id = prof.id)
          Buffers: shared hit=3
          ->  Nested Loop  (cost=0.15..15.57 rows=1 width=16) (actual rows=0 loops=1)
                Join Filter: (pp.participant_id = cp.participant_id)
                Buffers: shared hit=3
                ->  Nested Loop  (cost=0.15..14.55 rows=1 width=16) (actual rows=0 loops=1)
                      Buffers: shared hit=3
                      ->  Seq Scan on case_participants cp  (cost=0.00..1.04 rows=1 width=32) (actual rows=1 loops=1)
                            Filter: ((removed_at IS NULL) AND (case_id = $1))
                            Rows Removed by Filter: 2
                            Buffers: shared hit=1
                      ->  Index Scan using case_participant_roles_pkey on case_participant_roles r  (cost=0.15..8.17 rows=1 width=16) (actual rows=0 loops=1)
                            Index Cond: (id = cp.role_id)
                            Filter: (key = 'respondent_doctor'::text)
                            Rows Removed by Filter: 1
                            Buffers: shared hit=2
                ->  Seq Scan on professional_participants pp  (cost=0.00..1.01 rows=1 width=32) (never executed)
          ->  Index Scan using professional_profiles_user_id_idx on professional_profiles prof  (cost=0.12..8.14 rows=1 width=16) (never executed)
                Index Cond: (user_id = $2)
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1 from public.case_recusals cr
    where cr.case_id = p_case_id and cr.user_id = p_uid and cr.lifted_at is null
  );

Query Parameters: $1 = 'd0000000-0000-0000-0000-0000000000c1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.16..8.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=1
  InitPlan 1
    ->  Index Only Scan using case_recusals_case_user_live_idx on case_recusals cr  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((case_id = $1) AND (user_id = $2))
          Heap Fetches: 0
          Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.653 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 1.041 ms  plan:
Query Text: 
  select exists (
    select 1
      from authz.assignment_facts(p_principal) af
      join authz.roles r
        on r.code = af.role_code
     where af.role_code  = p_role_code
       and af.scope_kind = p_scope_kind
       and af.scope_id   = p_scope_id
       and r.state       = 'authoritative'
       and (
         p_principal is distinct from (select auth.uid())
         or af.role_code is not distinct from app.active_role()
       )
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'staff_admin', $3 = 'commission', $4 = 'a0000000-0000-0000-0000-0000000000a1'
Result  (cost=273.97..273.98 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 2
    ->  Nested Loop  (cost=0.28..273.97 rows=1 width=0) (actual rows=0 loops=1)
          Buffers: shared hit=12
          InitPlan 1
            ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
          ->  Function Scan on assignment_facts af  (cost=0.25..272.75 rows=1 width=32) (actual rows=0 loops=1)
                Filter: ((role_code = $2) AND (scope_kind = $3) AND (scope_id = $4) AND (($1 IS DISTINCT FROM (InitPlan 1).col1) OR (NOT (role_code IS DISTINCT FROM app.active_role()))))
                Rows Removed by Filter: 20
                Buffers: shared hit=12
          ->  Seq Scan on roles r  (cost=0.00..1.18 rows=1 width=14) (never executed)
                Filter: ((code = $2) AND ((state)::text = 'authoritative'::text))
psql:<stdin>:623: LOG:  duration: 1.519 ms  plan:
Query Text: 
  select authz.holds_role(p_user_id, 'staff_admin', 'commission', p_commission_id);

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.019 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'organization', $2 = '0c000000-0000-0000-0000-00000000000a', $3 = 'org_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'hospital_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.926 ms  plan:
Query Text: 
  select app.is_active(p_user_id) and exists (
    select 1 from public.commissions c
    where c.id = p_commission_id
      and (
        app.has_role('organization', c.organization_id, 'org_admin', p_user_id)
        or app.has_role('hospital', c.hospital_id, 'hospital_admin', p_user_id)
      )
  );

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.79..9.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.79 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = $1)
          Filter: (has_role('organization'::text, organization_id, 'org_admin'::text, $2) OR has_role('hospital'::text, hospital_id, 'hospital_admin'::text, $2))
          Rows Removed by Filter: 1
          Buffers: shared hit=9
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.021 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
      -- Same IS NOT DISTINCT FROM fix + the same incidental exists()
      -- NULL-safety note as has_role above.
      and (p_user_id is distinct from auth.uid() or m.role is not distinct from app.active_role())
  );

Query Parameters: $1 = 'commission', $2 = 'a0000000-0000-0000-0000-0000000000a1', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=10.92..10.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_principal_idx on memberships m  (cost=0.29..21.55 rows=2 width=0) (actual rows=0 loops=1)
          Index Cond: (principal_id = $3)
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END AND (($3 IS DISTINCT FROM (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid) OR (NOT (role IS DISTINCT FROM active_role()))))
          Rows Removed by Filter: 20
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.551 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role_any('commission', p_commission_id, p_user_id);

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.022 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'case_referrals'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select hospital_id from public.commissions where id = p_commission_id;

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1'
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'nsp_coordinator', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.486 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'nsp_coordinator', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.034 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'pqs_member', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Rows Removed by Filter: 1
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.526 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'pqs_member', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=7
psql:<stdin>:623: LOG:  duration: 1.145 ms  plan:
Query Text: 
  select app.is_nsp_coordinator_of_for(p_hospital_id, p_user_id)
      or app.is_pqs_member_of_for(p_hospital_id, p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=13
psql:<stdin>:623: LOG:  duration: 1.556 ms  plan:
Query Text: app.feature_enabled('case_referrals')
     and app.is_pqs_operator_of_for(app.hospital_of_commission(v_commission), p_uid)
     and exists (
       select 1 from public.case_referral r
       where r.source_case_id = p_case_id or r.target_case_id = p_case_id
     )
Result  (cost=0.53..1.29 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=20
  InitPlan 1
    ->  Seq Scan on case_referral r  (cost=0.00..1.06 rows=2 width=0) (never executed)
          Filter: ((source_case_id = $1) OR (target_case_id = $1))
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select hospital_id from public.commissions where id = p_commission_id;

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1'
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'quality_reviewer', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Rows Removed by Filter: 1
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.509 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'quality_reviewer', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=7
psql:<stdin>:623: LOG:  duration: 0.730 ms  plan:
Query Text: not v_eg
     and app.is_quality_reviewer_of_for(app.hospital_of_commission(v_commission), p_uid)
     and (select quality_oversight from public.commissions where id = v_commission) = 'visible'
Result  (cost=8.29..8.80 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=10
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=8) (never executed)
          Index Cond: (id = $5)
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'administrativo'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
      -- Same IS NOT DISTINCT FROM fix + the same incidental exists()
      -- NULL-safety note as has_role above.
      and (p_user_id is distinct from auth.uid() or m.role is not distinct from app.active_role())
  );

Query Parameters: $1 = 'commission', $2 = 'a0000000-0000-0000-0000-0000000000a1', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=10.92..10.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_principal_idx on memberships m  (cost=0.29..21.55 rows=2 width=0) (actual rows=0 loops=1)
          Index Cond: (principal_id = $3)
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END AND (($3 IS DISTINCT FROM (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid) OR (NOT (role IS DISTINCT FROM active_role()))))
          Rows Removed by Filter: 20
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.481 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role_any('commission', p_commission_id, p_user_id);

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.826 ms  plan:
Query Text: 
  select app.feature_enabled('administrativo')
     and app.is_active(p_user_id)
     and app.is_member_of_for(p_commission_id, p_user_id)
     and exists (
       select 1 from public.commission_administrativo_capabilities c
       where c.commission_id = p_commission_id
         and c.user_id = p_user_id
         and c.capability = p_capability
     );

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = 'read_cases', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.17..8.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=13
  InitPlan 1
    ->  Index Only Scan using commission_administrativo_capabilities_pkey on commission_administrativo_capabilities c  (cost=0.15..8.17 rows=1 width=0) (never executed)
          Index Cond: ((commission_id = $1) AND (user_id = $3) AND (capability = $2))
          Heap Fetches: 0
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: select read_case_content, read_case_deliberation, read_standard_phi,
           read_restricted_phi, write_case_content
    from public.case_access_grants g
    where g.case_id = p_case_id and g.principal_id = p_uid
      and g.revoked_at is null
      and (g.expires_at is null or g.expires_at > now())
Query Parameters: $1 = 'd0000000-0000-0000-0000-0000000000c1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $3 = NULL, $4 = NULL, $5 = NULL, $6 = NULL, $7 = NULL, $8 = NULL, $9 = NULL, $10 = NULL, $11 = NULL, $12 = NULL, $13 = NULL, $14 = NULL, $15 = NULL, $16 = NULL, $17 = NULL
Index Scan using case_access_grants_active_source_uniq on case_access_grants g  (cost=0.14..8.17 rows=1 width=5) (actual rows=0 loops=1)
  Index Cond: ((case_id = $1) AND (principal_id = $2))
  Filter: ((expires_at IS NULL) OR (expires_at > now()))
  Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.019 ms  plan:
Query Text: exists (select 1 from public.case_phases cp
             where cp.case_id = p_case_id and cp.assigned_to = p_uid)
     or exists (select 1 from public.case_narratives cn
                where cn.case_id = p_case_id and cn.assigned_to = p_uid)
Result  (cost=9.22..9.23 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Seq Scan on case_phases cp  (cost=0.00..1.06 rows=1 width=0) (actual rows=0 loops=1)
          Filter: ((case_id = $1) AND (assigned_to = $2))
          Rows Removed by Filter: 4
          Buffers: shared hit=1
  InitPlan 2
    ->  Index Scan using case_narratives_case_idx on case_narratives cn  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (case_id = $1)
          Filter: (assigned_to = $2)
          Rows Removed by Filter: 4
          Buffers: shared hit=2
psql:<stdin>:623: LOG:  duration: 8.053 ms  plan:
Query Text: 
  -- A2: thin projection of app._case_caps. Every arm this body used to carry
  -- (coordinator, org-admin, grant, phase/narrative assignment, the NSP referral arm,
  -- and the case_access flag-OFF member branch) now lives in the resolver, evaluated in
  -- the same order behind the same hard deny. `228` test 24 pins the flag-OFF member
  -- arm byte-for-byte; 234 K3 re-pins it.
  select app.has_case_capability(p_case_id, p_uid, 'read_case_content');

Query Parameters: $1 = 'd0000000-0000-0000-0000-0000000000c1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=104
psql:<stdin>:623: LOG:  duration: 8.116 ms  plan:
Query Text: 
  -- `can_read_case` as it meant BEFORE the S7 arm existed: content reach through
  -- a committee-plane source. Every surface that predates QO·A and was written
  -- against "any case reader" wants THIS, not the widened can_read_case.
  select app.can_read_case(p_case_id, p_uid)
     and not app.is_oversight_only_reader(p_case_id, p_uid);

Query Parameters: $1 = 'd0000000-0000-0000-0000-0000000000c1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=104
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: select commission_id, visibility_policy
                                  from public.cases where id = p_case_id
Seq Scan on cases  (cost=0.00..1.10 rows=1 width=48) (actual rows=1 loops=1)
  Filter: (id = $1)
  Rows Removed by Filter: 4
  Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1
    from public.case_participants cp
    join public.case_participant_roles r on r.id = cp.role_id
    join public.professional_participants pp on pp.participant_id = cp.participant_id
    join public.professional_profiles prof on prof.id = pp.professional_profile_id
    where cp.case_id = p_case_id
      and cp.removed_at is null
      and r.key = 'respondent_doctor'
      and prof.user_id = p_uid
  );

Query Parameters: $1 = 'dba00000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=23.73..23.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Nested Loop  (cost=0.27..23.73 rows=1 width=0) (actual rows=0 loops=1)
          Join Filter: (pp.professional_profile_id = prof.id)
          Buffers: shared hit=3
          ->  Nested Loop  (cost=0.15..15.57 rows=1 width=16) (actual rows=0 loops=1)
                Join Filter: (pp.participant_id = cp.participant_id)
                Buffers: shared hit=3
                ->  Nested Loop  (cost=0.15..14.55 rows=1 width=16) (actual rows=0 loops=1)
                      Buffers: shared hit=3
                      ->  Seq Scan on case_participants cp  (cost=0.00..1.04 rows=1 width=32) (actual rows=1 loops=1)
                            Filter: ((removed_at IS NULL) AND (case_id = $1))
                            Rows Removed by Filter: 2
                            Buffers: shared hit=1
                      ->  Index Scan using case_participant_roles_pkey on case_participant_roles r  (cost=0.15..8.17 rows=1 width=16) (actual rows=0 loops=1)
                            Index Cond: (id = cp.role_id)
                            Filter: (key = 'respondent_doctor'::text)
                            Rows Removed by Filter: 1
                            Buffers: shared hit=2
                ->  Seq Scan on professional_participants pp  (cost=0.00..1.01 rows=1 width=32) (never executed)
          ->  Index Scan using professional_profiles_user_id_idx on professional_profiles prof  (cost=0.12..8.14 rows=1 width=16) (never executed)
                Index Cond: (user_id = $2)
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1 from public.case_recusals cr
    where cr.case_id = p_case_id and cr.user_id = p_uid and cr.lifted_at is null
  );

Query Parameters: $1 = 'dba00000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.16..8.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=1
  InitPlan 1
    ->  Index Only Scan using case_recusals_case_user_live_idx on case_recusals cr  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((case_id = $1) AND (user_id = $2))
          Heap Fetches: 0
          Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.297 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.561 ms  plan:
Query Text: 
  select exists (
    select 1
      from authz.assignment_facts(p_principal) af
      join authz.roles r
        on r.code = af.role_code
     where af.role_code  = p_role_code
       and af.scope_kind = p_scope_kind
       and af.scope_id   = p_scope_id
       and r.state       = 'authoritative'
       and (
         p_principal is distinct from (select auth.uid())
         or af.role_code is not distinct from app.active_role()
       )
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'staff_admin', $3 = 'commission', $4 = 'b0000000-0000-0000-0000-0000000000b1'
Result  (cost=273.97..273.98 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 2
    ->  Nested Loop  (cost=0.28..273.97 rows=1 width=0) (actual rows=0 loops=1)
          Buffers: shared hit=12
          InitPlan 1
            ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
          ->  Function Scan on assignment_facts af  (cost=0.25..272.75 rows=1 width=32) (actual rows=0 loops=1)
                Filter: ((role_code = $2) AND (scope_kind = $3) AND (scope_id = $4) AND (($1 IS DISTINCT FROM (InitPlan 1).col1) OR (NOT (role_code IS DISTINCT FROM app.active_role()))))
                Rows Removed by Filter: 20
                Buffers: shared hit=12
          ->  Seq Scan on roles r  (cost=0.00..1.18 rows=1 width=14) (never executed)
                Filter: ((code = $2) AND ((state)::text = 'authoritative'::text))
psql:<stdin>:623: LOG:  duration: 0.949 ms  plan:
Query Text: 
  select authz.holds_role(p_user_id, 'staff_admin', 'commission', p_commission_id);

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'organization', $2 = '0c000000-0000-0000-0000-00000000000a', $3 = 'org_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'hospital_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.622 ms  plan:
Query Text: 
  select app.is_active(p_user_id) and exists (
    select 1 from public.commissions c
    where c.id = p_commission_id
      and (
        app.has_role('organization', c.organization_id, 'org_admin', p_user_id)
        or app.has_role('hospital', c.hospital_id, 'hospital_admin', p_user_id)
      )
  );

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.79..9.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.79 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = $1)
          Filter: (has_role('organization'::text, organization_id, 'org_admin'::text, $2) OR has_role('hospital'::text, hospital_id, 'hospital_admin'::text, $2))
          Rows Removed by Filter: 1
          Buffers: shared hit=9
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
      -- Same IS NOT DISTINCT FROM fix + the same incidental exists()
      -- NULL-safety note as has_role above.
      and (p_user_id is distinct from auth.uid() or m.role is not distinct from app.active_role())
  );

Query Parameters: $1 = 'commission', $2 = 'b0000000-0000-0000-0000-0000000000b1', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=10.92..10.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_principal_idx on memberships m  (cost=0.29..21.55 rows=2 width=0) (actual rows=0 loops=1)
          Index Cond: (principal_id = $3)
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END AND (($3 IS DISTINCT FROM (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid) OR (NOT (role IS DISTINCT FROM active_role()))))
          Rows Removed by Filter: 20
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.338 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role_any('commission', p_commission_id, p_user_id);

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'case_referrals'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select hospital_id from public.commissions where id = p_commission_id;

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1'
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'nsp_coordinator', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.758 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'nsp_coordinator', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'pqs_member', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Rows Removed by Filter: 1
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.357 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'pqs_member', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=7
psql:<stdin>:623: LOG:  duration: 1.225 ms  plan:
Query Text: 
  select app.is_nsp_coordinator_of_for(p_hospital_id, p_user_id)
      or app.is_pqs_member_of_for(p_hospital_id, p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=13
psql:<stdin>:623: LOG:  duration: 1.460 ms  plan:
Query Text: app.feature_enabled('case_referrals')
     and app.is_pqs_operator_of_for(app.hospital_of_commission(v_commission), p_uid)
     and exists (
       select 1 from public.case_referral r
       where r.source_case_id = p_case_id or r.target_case_id = p_case_id
     )
Result  (cost=0.53..1.29 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=20
  InitPlan 1
    ->  Seq Scan on case_referral r  (cost=0.00..1.06 rows=2 width=0) (never executed)
          Filter: ((source_case_id = $1) OR (target_case_id = $1))
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select hospital_id from public.commissions where id = p_commission_id;

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1'
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.018 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'quality_reviewer', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Rows Removed by Filter: 1
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.476 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'quality_reviewer', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=7
psql:<stdin>:623: LOG:  duration: 0.623 ms  plan:
Query Text: not v_eg
     and app.is_quality_reviewer_of_for(app.hospital_of_commission(v_commission), p_uid)
     and (select quality_oversight from public.commissions where id = v_commission) = 'visible'
Result  (cost=8.29..8.80 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=10
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=8) (never executed)
          Index Cond: (id = $5)
psql:<stdin>:623: LOG:  duration: 0.019 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'administrativo'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.016 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
      -- Same IS NOT DISTINCT FROM fix + the same incidental exists()
      -- NULL-safety note as has_role above.
      and (p_user_id is distinct from auth.uid() or m.role is not distinct from app.active_role())
  );

Query Parameters: $1 = 'commission', $2 = 'b0000000-0000-0000-0000-0000000000b1', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=10.92..10.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_principal_idx on memberships m  (cost=0.29..21.55 rows=2 width=0) (actual rows=0 loops=1)
          Index Cond: (principal_id = $3)
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END AND (($3 IS DISTINCT FROM (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid) OR (NOT (role IS DISTINCT FROM active_role()))))
          Rows Removed by Filter: 20
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.442 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role_any('commission', p_commission_id, p_user_id);

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.767 ms  plan:
Query Text: 
  select app.feature_enabled('administrativo')
     and app.is_active(p_user_id)
     and app.is_member_of_for(p_commission_id, p_user_id)
     and exists (
       select 1 from public.commission_administrativo_capabilities c
       where c.commission_id = p_commission_id
         and c.user_id = p_user_id
         and c.capability = p_capability
     );

Query Parameters: $1 = 'b0000000-0000-0000-0000-0000000000b1', $2 = 'read_cases', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.17..8.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=13
  InitPlan 1
    ->  Index Only Scan using commission_administrativo_capabilities_pkey on commission_administrativo_capabilities c  (cost=0.15..8.17 rows=1 width=0) (never executed)
          Index Cond: ((commission_id = $1) AND (user_id = $3) AND (capability = $2))
          Heap Fetches: 0
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: select read_case_content, read_case_deliberation, read_standard_phi,
           read_restricted_phi, write_case_content
    from public.case_access_grants g
    where g.case_id = p_case_id and g.principal_id = p_uid
      and g.revoked_at is null
      and (g.expires_at is null or g.expires_at > now())
Query Parameters: $1 = 'dba00000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $3 = NULL, $4 = NULL, $5 = NULL, $6 = NULL, $7 = NULL, $8 = NULL, $9 = NULL, $10 = NULL, $11 = NULL, $12 = NULL, $13 = NULL, $14 = NULL, $15 = NULL, $16 = NULL, $17 = NULL
Index Scan using case_access_grants_active_source_uniq on case_access_grants g  (cost=0.14..8.17 rows=1 width=5) (actual rows=0 loops=1)
  Index Cond: ((case_id = $1) AND (principal_id = $2))
  Filter: ((expires_at IS NULL) OR (expires_at > now()))
  Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.015 ms  plan:
Query Text: exists (select 1 from public.case_phases cp
             where cp.case_id = p_case_id and cp.assigned_to = p_uid)
     or exists (select 1 from public.case_narratives cn
                where cn.case_id = p_case_id and cn.assigned_to = p_uid)
Result  (cost=9.22..9.23 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Seq Scan on case_phases cp  (cost=0.00..1.06 rows=1 width=0) (actual rows=0 loops=1)
          Filter: ((case_id = $1) AND (assigned_to = $2))
          Rows Removed by Filter: 4
          Buffers: shared hit=1
  InitPlan 2
    ->  Index Scan using case_narratives_case_idx on case_narratives cn  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (case_id = $1)
          Filter: (assigned_to = $2)
          Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 5.545 ms  plan:
Query Text: 
  -- A2: thin projection of app._case_caps. Every arm this body used to carry
  -- (coordinator, org-admin, grant, phase/narrative assignment, the NSP referral arm,
  -- and the case_access flag-OFF member branch) now lives in the resolver, evaluated in
  -- the same order behind the same hard deny. `228` test 24 pins the flag-OFF member
  -- arm byte-for-byte; 234 K3 re-pins it.
  select app.has_case_capability(p_case_id, p_uid, 'read_case_content');

Query Parameters: $1 = 'dba00000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=84
psql:<stdin>:623: LOG:  duration: 5.603 ms  plan:
Query Text: 
  -- `can_read_case` as it meant BEFORE the S7 arm existed: content reach through
  -- a committee-plane source. Every surface that predates QO·A and was written
  -- against "any case reader" wants THIS, not the widened can_read_case.
  select app.can_read_case(p_case_id, p_uid)
     and not app.is_oversight_only_reader(p_case_id, p_uid);

Query Parameters: $1 = 'dba00000-0000-0000-0000-0000000000b1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=84
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: select commission_id, visibility_policy
                                  from public.cases where id = p_case_id
Seq Scan on cases  (cost=0.00..1.10 rows=1 width=48) (actual rows=1 loops=1)
  Filter: (id = $1)
  Rows Removed by Filter: 7
  Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.016 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1
    from public.case_participants cp
    join public.case_participant_roles r on r.id = cp.role_id
    join public.professional_participants pp on pp.participant_id = cp.participant_id
    join public.professional_profiles prof on prof.id = pp.professional_profile_id
    where cp.case_id = p_case_id
      and cp.removed_at is null
      and r.key = 'respondent_doctor'
      and prof.user_id = p_uid
  );

Query Parameters: $1 = 'ca000000-0000-0000-0000-0000000000e1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=23.73..23.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=5
  InitPlan 1
    ->  Nested Loop  (cost=0.27..23.73 rows=1 width=0) (actual rows=0 loops=1)
          Join Filter: (pp.professional_profile_id = prof.id)
          Buffers: shared hit=5
          ->  Nested Loop  (cost=0.15..15.57 rows=1 width=16) (actual rows=1 loops=1)
                Join Filter: (pp.participant_id = cp.participant_id)
                Buffers: shared hit=4
                ->  Nested Loop  (cost=0.15..14.55 rows=1 width=16) (actual rows=1 loops=1)
                      Buffers: shared hit=3
                      ->  Seq Scan on case_participants cp  (cost=0.00..1.04 rows=1 width=32) (actual rows=1 loops=1)
                            Filter: ((removed_at IS NULL) AND (case_id = $1))
                            Rows Removed by Filter: 2
                            Buffers: shared hit=1
                      ->  Index Scan using case_participant_roles_pkey on case_participant_roles r  (cost=0.15..8.17 rows=1 width=16) (actual rows=1 loops=1)
                            Index Cond: (id = cp.role_id)
                            Filter: (key = 'respondent_doctor'::text)
                            Buffers: shared hit=2
                ->  Seq Scan on professional_participants pp  (cost=0.00..1.01 rows=1 width=32) (actual rows=1 loops=1)
                      Buffers: shared hit=1
          ->  Index Scan using professional_profiles_user_id_idx on professional_profiles prof  (cost=0.12..8.14 rows=1 width=16) (actual rows=0 loops=1)
                Index Cond: (user_id = $2)
                Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select p_uid is not null and exists (
    select 1 from public.case_recusals cr
    where cr.case_id = p_case_id and cr.user_id = p_uid and cr.lifted_at is null
  );

Query Parameters: $1 = 'ca000000-0000-0000-0000-0000000000e1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.16..8.17 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=1
  InitPlan 1
    ->  Index Only Scan using case_recusals_case_user_live_idx on case_recusals cr  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((case_id = $1) AND (user_id = $2))
          Heap Fetches: 0
          Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.523 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.745 ms  plan:
Query Text: 
  select exists (
    select 1
      from authz.assignment_facts(p_principal) af
      join authz.roles r
        on r.code = af.role_code
     where af.role_code  = p_role_code
       and af.scope_kind = p_scope_kind
       and af.scope_id   = p_scope_id
       and r.state       = 'authoritative'
       and (
         p_principal is distinct from (select auth.uid())
         or af.role_code is not distinct from app.active_role()
       )
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'staff_admin', $3 = 'commission', $4 = 'a0000000-0000-0000-0000-0000000000a1'
Result  (cost=273.97..273.98 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 2
    ->  Nested Loop  (cost=0.28..273.97 rows=1 width=0) (actual rows=0 loops=1)
          Buffers: shared hit=12
          InitPlan 1
            ->  Result  (cost=0.00..0.03 rows=1 width=16) (never executed)
          ->  Function Scan on assignment_facts af  (cost=0.25..272.75 rows=1 width=32) (actual rows=0 loops=1)
                Filter: ((role_code = $2) AND (scope_kind = $3) AND (scope_id = $4) AND (($1 IS DISTINCT FROM (InitPlan 1).col1) OR (NOT (role_code IS DISTINCT FROM app.active_role()))))
                Rows Removed by Filter: 20
                Buffers: shared hit=12
          ->  Seq Scan on roles r  (cost=0.00..1.18 rows=1 width=14) (never executed)
                Filter: ((code = $2) AND ((state)::text = 'authoritative'::text))
psql:<stdin>:623: LOG:  duration: 1.155 ms  plan:
Query Text: 
  select authz.holds_role(p_user_id, 'staff_admin', 'commission', p_commission_id);

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'organization', $2 = '0c000000-0000-0000-0000-00000000000a', $3 = 'org_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'hospital_admin', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.659 ms  plan:
Query Text: 
  select app.is_active(p_user_id) and exists (
    select 1 from public.commissions c
    where c.id = p_commission_id
      and (
        app.has_role('organization', c.organization_id, 'org_admin', p_user_id)
        or app.has_role('hospital', c.hospital_id, 'hospital_admin', p_user_id)
      )
  );

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.79..9.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=12
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.79 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = $1)
          Filter: (has_role('organization'::text, organization_id, 'org_admin'::text, $2) OR has_role('hospital'::text, hospital_id, 'hospital_admin'::text, $2))
          Rows Removed by Filter: 1
          Buffers: shared hit=9
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
      -- Same IS NOT DISTINCT FROM fix + the same incidental exists()
      -- NULL-safety note as has_role above.
      and (p_user_id is distinct from auth.uid() or m.role is not distinct from app.active_role())
  );

Query Parameters: $1 = 'commission', $2 = 'a0000000-0000-0000-0000-0000000000a1', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=10.92..10.93 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_principal_idx on memberships m  (cost=0.29..21.55 rows=2 width=0) (actual rows=0 loops=1)
          Index Cond: (principal_id = $3)
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END AND (($3 IS DISTINCT FROM (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid) OR (NOT (role IS DISTINCT FROM active_role()))))
          Rows Removed by Filter: 20
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.382 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role_any('commission', p_commission_id, p_user_id);

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.014 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'case_referrals'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select hospital_id from public.commissions where id = p_commission_id;

Query Parameters: $1 = 'a0000000-0000-0000-0000-0000000000a1'
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=16) (actual rows=1 loops=1)
  Index Cond: (id = $1)
  Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.008 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'nsp_coordinator', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.305 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'nsp_coordinator', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=6
psql:<stdin>:623: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select exists (
    select 1 from public.memberships m
    where m.principal_id = p_user_id
      and m.role = p_role
      and (m.expires_at is null or m.expires_at > now())
      and case p_scope_type
            when 'organization' then m.organization_id = p_scope_id
            when 'hospital'     then m.hospital_id     = p_scope_id
            when 'commission'   then m.commission_id   = p_scope_id
            else false
          end
  )
  -- IS NOT DISTINCT FROM (not `=`) is the real fix (BUG-ACT-NULLHAT-1) --
  -- see the module-level comment above for why the exists() clause above is
  -- ALSO incidentally NULL-safe, but must not be relied on as a second line
  -- of defense.
  and (p_user_id is distinct from auth.uid() or p_role is not distinct from app.active_role());

Query Parameters: $1 = 'hospital', $2 = '05000000-0000-0000-0000-00000000000a', $3 = 'pqs_member', $4 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.45..8.74 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Index Scan using memberships_grant_uq on memberships m  (cost=0.41..8.45 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: ((principal_id = $4) AND (role = $3))
          Filter: (((expires_at IS NULL) OR (expires_at > now())) AND CASE $1 WHEN 'organization'::text THEN (organization_id = $2) WHEN 'hospital'::text THEN (hospital_id = $2) WHEN 'commission'::text THEN (commission_id = $2) ELSE false END)
          Rows Removed by Filter: 1
          Buffers: shared hit=4
psql:<stdin>:623: LOG:  duration: 0.355 ms  plan:
Query Text: 
  select app.is_active(p_user_id)
     and app.has_role('hospital', p_hospital_id, 'pqs_member', p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=7
psql:<stdin>:623: LOG:  duration: 0.760 ms  plan:
Query Text: 
  select app.is_nsp_coordinator_of_for(p_hospital_id, p_user_id)
      or app.is_pqs_member_of_for(p_hospital_id, p_user_id);

Query Parameters: $1 = '05000000-0000-0000-0000-00000000000a', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=13
psql:<stdin>:623: LOG:  duration: 1.034 ms  plan:
Query Text: app.feature_enabled('case_referrals')
     and app.is_pqs_operator_of_for(app.hospital_of_commission(v_commission), p_uid)
     and exists (
       select 1 from public.case_referral r
       where r.source_case_id = p_case_id or r.target_case_id = p_case_id
     )
Result  (cost=0.53..1.29 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=20
  InitPlan 1
    ->  Seq Scan on case_referral r  (cost=0.00..1.06 rows=2 width=0) (never executed)
          Filter: ((source_case_id = $1) OR (target_case_id = $1))
psql:<stdin>:623: LOG:  duration: 0.002 ms  plan:
Query Text: not v_eg
     and app.is_quality_reviewer_of_for(app.hospital_of_commission(v_commission), p_uid)
     and (select quality_oversight from public.commissions where id = v_commission) = 'visible'
Result  (cost=8.29..8.80 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=8) (never executed)
          Index Cond: (id = $5)
psql:<stdin>:623: LOG:  duration: 0.006 ms  plan:
Query Text: select read_case_content, read_case_deliberation, read_standard_phi,
           read_restricted_phi, write_case_content
    from public.case_access_grants g
    where g.case_id = p_case_id and g.principal_id = p_uid
      and g.revoked_at is null
      and (g.expires_at is null or g.expires_at > now())
Query Parameters: $1 = 'ca000000-0000-0000-0000-0000000000e1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $3 = NULL, $4 = NULL, $5 = NULL, $6 = NULL, $7 = NULL, $8 = NULL, $9 = NULL, $10 = NULL, $11 = NULL, $12 = NULL, $13 = NULL, $14 = NULL, $15 = NULL, $16 = NULL, $17 = NULL
Index Scan using case_access_grants_active_source_uniq on case_access_grants g  (cost=0.14..8.17 rows=1 width=5) (actual rows=0 loops=1)
  Index Cond: ((case_id = $1) AND (principal_id = $2))
  Filter: ((expires_at IS NULL) OR (expires_at > now()))
  Buffers: shared hit=1
 count 
-------
   200
(1 row)

psql:<stdin>:623: LOG:  duration: 0.012 ms  plan:
Query Text: exists (select 1 from public.case_phases cp
             where cp.case_id = p_case_id and cp.assigned_to = p_uid)
     or exists (select 1 from public.case_narratives cn
                where cn.case_id = p_case_id and cn.assigned_to = p_uid)
Result  (cost=9.22..9.23 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=2
  InitPlan 1
    ->  Seq Scan on case_phases cp  (cost=0.00..1.06 rows=1 width=0) (actual rows=0 loops=1)
          Filter: ((case_id = $1) AND (assigned_to = $2))
          Rows Removed by Filter: 4
          Buffers: shared hit=1
  InitPlan 2
    ->  Index Scan using case_narratives_case_idx on case_narratives cn  (cost=0.14..8.16 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (case_id = $1)
          Filter: (assigned_to = $2)
          Buffers: shared hit=1
psql:<stdin>:623: LOG:  duration: 4.248 ms  plan:
Query Text: 
  -- A2: thin projection of app._case_caps. Every arm this body used to carry
  -- (coordinator, org-admin, grant, phase/narrative assignment, the NSP referral arm,
  -- and the case_access flag-OFF member branch) now lives in the resolver, evaluated in
  -- the same order behind the same hard deny. `228` test 24 pins the flag-OFF member
  -- arm byte-for-byte; 234 K3 re-pins it.
  select app.has_case_capability(p_case_id, p_uid, 'read_case_content');

Query Parameters: $1 = 'ca000000-0000-0000-0000-0000000000e1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=63
psql:<stdin>:623: LOG:  duration: 4.310 ms  plan:
Query Text: 
  -- `can_read_case` as it meant BEFORE the S7 arm existed: content reach through
  -- a committee-plane source. Every surface that predates QO·A and was written
  -- against "any case reader" wants THIS, not the widened can_read_case.
  select app.can_read_case(p_case_id, p_uid)
     and not app.is_oversight_only_reader(p_case_id, p_uid);

Query Parameters: $1 = 'ca000000-0000-0000-0000-0000000000e1', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=63
psql:<stdin>:623: LOG:  duration: 18.259 ms  plan:
Query Text: exists (
    select 1
    from public.professional_participants pp
    join public.case_participants cp
      on cp.participant_id = pp.participant_id
     and cp.removed_at is null
    where pp.professional_profile_id = p_profile_id
      and app.can_read_case_committee(cp.case_id, p_uid)
  )
Result  (cost=2.81..2.81 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=253
  InitPlan 1
    ->  Nested Loop  (cost=0.00..2.81 rows=1 width=0) (actual rows=0 loops=1)
          Join Filter: (pp.participant_id = cp.participant_id)
          Buffers: shared hit=253
          ->  Seq Scan on professional_participants pp  (cost=0.00..1.01 rows=1 width=16) (actual rows=1 loops=1)
                Filter: (professional_profile_id = $1)
                Buffers: shared hit=1
          ->  Seq Scan on case_participants cp  (cost=0.00..1.78 rows=1 width=16) (actual rows=0 loops=1)
                Filter: ((removed_at IS NULL) AND can_read_case_committee(case_id, $2))
                Rows Removed by Filter: 3
                Buffers: shared hit=252
psql:<stdin>:623: LOG:  duration: 33.606 ms  plan:
Query Text: select count(*) from (select 1 from public.professional_profiles limit 200) t;
Aggregate  (cost=121.91..121.92 rows=1 width=8) (actual rows=1 loops=1)
  Buffers: shared hit=611
  ->  Limit  (cost=7.80..119.41 rows=200 width=4) (actual rows=200 loops=1)
        Buffers: shared hit=611
        InitPlan 2
          ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
        ->  Seq Scan on professional_profiles  (cost=7.77..2798.03 rows=5000 width=4) (actual rows=200 loops=1)
              Filter: CASE WHEN (ANY (organization_id = (hashed SubPlan 1).col1)) THEN true ELSE app.can_read_professional_profile(id, (InitPlan 2).col1) END
              Rows Removed by Filter: 1
              Buffers: shared hit=611
              SubPlan 1
                ->  ProjectSet  (cost=0.00..5.27 rows=1000 width=16) (actual rows=2 loops=1)
                      Buffers: shared hit=246
                      ->  Result  (cost=0.00..0.01 rows=1 width=0) (actual rows=1 loops=1)
Time: 34.221 ms
RESET
Time: 0.124 ms
ROLLBACK
Time: 0.161 ms
=== PASS B / M2-nested — the single-row WRITE gate ===
BEGIN
Time: 0.081 ms
SET
Time: 0.114 ms
SET
Time: 0.109 ms
psql:<stdin>:631: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.320 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.013 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 2.179 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=15
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=1 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=13
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=1 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 19
              Buffers: shared hit=12
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:631: LOG:  duration: 2.615 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=26
psql:<stdin>:631: LOG:  duration: 2.738 ms  plan:
Query Text: 
  -- LAYER 3 (ADR 0176 D2). The permission code is a STRING LITERAL so the enforcement site is
  -- statically greppable (D7), and it is the ONLY place `commission.forms.edit` appears in the
  -- catalog. There are no hard-deny classes on form editing, so the two arms are a plain
  -- disjunction and their ORDER carries no semantics (Postgres does not guarantee OR evaluation
  -- order in any case).
  select p_uid is not null and (
    -- RE-KEYED ARM — was `app.is_staff_admin_of(p_commission_id)` in all four policies.
    authz.has_permission(p_uid, 'commission', p_commission_id, 'commission.forms.edit')
    -- PRESERVED ARM — the tenancy admins (org_admin / hospital_admin), both `legacy` roles whose
    -- catalog grants are inert. This is the legacy-equivalence half; deleting it is the
    -- regression this migration exists to avoid. `_for` rather than the bare wrapper because
    -- layer 3 is given its principal explicitly; at every call site p_uid IS auth.uid(), so the
    -- two are the same answer (`is_tenancy_admin_of(x)` is literally
    -- `is_tenancy_admin_of_for(x, (select auth.uid()))`).
    or app.is_tenancy_admin_of_for(p_commission_id, p_uid)
  );

Query Parameters: $1 = '0585641f-07df-2b46-928e-bfaae13d22e2', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
psql:<stdin>:631: LOG:  duration: 0.012 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.007 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.365 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 2.139 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=15
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=1 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=13
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=1 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 19
              Buffers: shared hit=12
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:631: LOG:  duration: 2.741 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=26
psql:<stdin>:631: LOG:  duration: 2.865 ms  plan:
Query Text: 
  -- LAYER 3 (ADR 0176 D2). The permission code is a STRING LITERAL so the enforcement site is
  -- statically greppable (D7), and it is the ONLY place `commission.forms.edit` appears in the
  -- catalog. There are no hard-deny classes on form editing, so the two arms are a plain
  -- disjunction and their ORDER carries no semantics (Postgres does not guarantee OR evaluation
  -- order in any case).
  select p_uid is not null and (
    -- RE-KEYED ARM — was `app.is_staff_admin_of(p_commission_id)` in all four policies.
    authz.has_permission(p_uid, 'commission', p_commission_id, 'commission.forms.edit')
    -- PRESERVED ARM — the tenancy admins (org_admin / hospital_admin), both `legacy` roles whose
    -- catalog grants are inert. This is the legacy-equivalence half; deleting it is the
    -- regression this migration exists to avoid. `_for` rather than the bare wrapper because
    -- layer 3 is given its principal explicitly; at every call site p_uid IS auth.uid(), so the
    -- two are the same answer (`is_tenancy_admin_of(x)` is literally
    -- `is_tenancy_admin_of_for(x, (select auth.uid()))`).
    or app.is_tenancy_admin_of_for(p_commission_id, p_uid)
  );

Query Parameters: $1 = '0585641f-07df-2b46-928e-bfaae13d22e2', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
psql:<stdin>:631: LOG:  duration: 0.010 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.268 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.019 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 4.591 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=15
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=1 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=13
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=1 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 19
              Buffers: shared hit=12
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:631: LOG:  duration: 5.194 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=26
psql:<stdin>:631: LOG:  duration: 5.355 ms  plan:
Query Text: 
  -- LAYER 3 (ADR 0176 D2). The permission code is a STRING LITERAL so the enforcement site is
  -- statically greppable (D7), and it is the ONLY place `commission.forms.edit` appears in the
  -- catalog. There are no hard-deny classes on form editing, so the two arms are a plain
  -- disjunction and their ORDER carries no semantics (Postgres does not guarantee OR evaluation
  -- order in any case).
  select p_uid is not null and (
    -- RE-KEYED ARM — was `app.is_staff_admin_of(p_commission_id)` in all four policies.
    authz.has_permission(p_uid, 'commission', p_commission_id, 'commission.forms.edit')
    -- PRESERVED ARM — the tenancy admins (org_admin / hospital_admin), both `legacy` roles whose
    -- catalog grants are inert. This is the legacy-equivalence half; deleting it is the
    -- regression this migration exists to avoid. `_for` rather than the bare wrapper because
    -- layer 3 is given its principal explicitly; at every call site p_uid IS auth.uid(), so the
    -- two are the same answer (`is_tenancy_admin_of(x)` is literally
    -- `is_tenancy_admin_of_for(x, (select auth.uid()))`).
    or app.is_tenancy_admin_of_for(p_commission_id, p_uid)
  );

Query Parameters: $1 = '0585641f-07df-2b46-928e-bfaae13d22e2', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
psql:<stdin>:631: LOG:  duration: 0.017 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.073 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.705 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.044 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:631: LOG:  duration: 4.576 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=15
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=1 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=13
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=1 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 19
              Buffers: shared hit=12
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:631: LOG:  duration: 5.039 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=26
psql:<stdin>:631: LOG:  duration: 5.231 ms  plan:
Query Text: 
  -- LAYER 3 (ADR 0176 D2). The permission code is a STRING LITERAL so the enforcement site is
  -- statically greppable (D7), and it is the ONLY place `commission.forms.edit` appears in the
  -- catalog. There are no hard-deny classes on form editing, so the two arms are a plain
  -- disjunction and their ORDER carries no semantics (Postgres does not guarantee OR evaluation
  -- order in any case).
  select p_uid is not null and (
    -- RE-KEYED ARM — was `app.is_staff_admin_of(p_commission_id)` in all four policies.
    authz.has_permission(p_uid, 'commission', p_commission_id, 'commission.forms.edit')
    -- PRESERVED ARM — the tenancy admins (org_admin / hospital_admin), both `legacy` roles whose
    -- catalog grants are inert. This is the legacy-equivalence half; deleting it is the
    -- regression this migration exists to avoid. `_for` rather than the bare wrapper because
    -- layer 3 is given its principal explicitly; at every call site p_uid IS auth.uid(), so the
    -- two are the same answer (`is_tenancy_admin_of(x)` is literally
    -- `is_tenancy_admin_of_for(x, (select auth.uid()))`).
    or app.is_tenancy_admin_of_for(p_commission_id, p_uid)
  );

Query Parameters: $1 = '0585641f-07df-2b46-928e-bfaae13d22e2', $2 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=0.00..0.51 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
psql:<stdin>:631: LOG:  duration: 0.011 ms  plan:
Query Text: 
  select coalesce(jsonb_object_agg(col, jsonb_build_object('old', ov, 'new', nv)), '{}'::jsonb)
  from (
    select c as col,
           case when p_old is null then null else p_old -> c end as ov,
           case when p_new is null then null else p_new -> c end as nv
    from unnest(p_cols) as c
  ) d
  where ov is distinct from nv;

Query Parameters: $1 = '{"id": "24fe5757-a7bd-fc3e-77ae-12625b9f597b", "title": "AE4 Perf Formulario 1", "created_at": "2026-09-03T09:29:59.885545+00:00", "created_by": null, "updated_at": "2026-09-03T09:29:59.885545+00:00", "description": null, "commission_id": "0585641f-07df-2b46-928e-bfaae13d22e2"}', $2 = '{"id": "24fe5757-a7bd-fc3e-77ae-12625b9f597b", "title": "AE4 Perf Formulario 1", "created_at": "2026-09-03T09:29:59.885545+00:00", "created_by": null, "updated_at": "2026-09-03T09:42:08.656232+00:00", "description": null, "commission_id": "0585641f-07df-2b46-928e-bfaae13d22e2"}', $3 = '{title,description}'
Aggregate  (cost=0.28..0.29 rows=1 width=32) (actual rows=1 loops=1)
  ->  Function Scan on unnest c  (cost=0.00..0.18 rows=10 width=32) (actual rows=0 loops=1)
        Filter: (CASE WHEN ($1 IS NULL) THEN NULL::jsonb ELSE ($1 -> c) END IS DISTINCT FROM CASE WHEN ($2 IS NULL) THEN NULL::jsonb ELSE ($2 -> c) END)
        Rows Removed by Filter: 2
psql:<stdin>:631: LOG:  duration: 0.021 ms  plan:
Query Text: 
  select coalesce((select enabled from app.feature_flags where key = p_key), false);

Query Parameters: $1 = 'audit_trail'
Result  (cost=4.54..4.55 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=4
  InitPlan 1
    ->  Seq Scan on feature_flags  (cost=0.00..4.54 rows=1 width=1) (actual rows=1 loops=1)
          Filter: (key = $1)
          Rows Removed by Filter: 42
          Buffers: shared hit=4
psql:<stdin>:631: LOG:  duration: 0.022 ms  plan:
Query Text: v_is_admin := exists (
      select 1 from public.profiles
      where id = auth.uid() and is_admin = true
    )
Result  (cost=8.32..8.33 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.31..8.32 rows=1 width=0) (actual rows=0 loops=1)
          Index Cond: (id = (COALESCE(NULLIF(current_setting('request.jwt.claim.sub'::text, true), ''::text), ((NULLIF(current_setting('request.jwt.claims'::text, true), ''::text))::jsonb ->> 'sub'::text)))::uuid)
          Filter: is_admin
          Rows Removed by Filter: 1
          Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.006 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.013 ms  plan:
Query Text: select organization_id, hospital_id                            from public.commissions where id = p_commission
Index Scan using commissions_pkey on commissions  (cost=0.28..8.29 rows=1 width=32) (actual rows=1 loops=1)
  Index Cond: (id = $4)
  Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.004 ms  plan:
Query Text: SELECT pg_advisory_xact_lock(hashtextextended(v_lock_key, 0))
Result  (cost=0.00..0.01 rows=1 width=4) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 1.829 ms  plan:
Query Text: select seq, row_hash                             from public.audit_log
    where commission_id = p_commission
    order by seq desc limit 1
Limit  (cost=0.27..8.57 rows=1 width=73) (actual rows=0 loops=1)
  Buffers: shared hit=1154
  ->  Index Scan Backward using audit_log_commission_seq_key on audit_log  (cost=0.27..274.21 rows=33 width=73) (actual rows=0 loops=1)
        Index Cond: (commission_id = $4)
        Buffers: shared hit=1154
psql:<stdin>:631: LOG:  duration: 0.062 ms  plan:
Query Text: '{' || coalesce((
      select string_agg(to_json(kv.key)::text || ':' || app.jsonb_canonical(kv.value), ','
                        order by kv.key)
      from jsonb_each(p_value) as kv(key, value)
    ), '') || '}'
Result  (cost=31.09..31.10 rows=1 width=32) (actual rows=1 loops=1)
  InitPlan 1
    ->  Aggregate  (cost=31.08..31.09 rows=1 width=32) (actual rows=1 loops=1)
          ->  Sort  (cost=4.32..4.57 rows=100 width=64) (actual rows=1 loops=1)
                Sort Key: kv.key
                Sort Method: quicksort  Memory: 25kB
                ->  Function Scan on jsonb_each kv  (cost=0.00..1.00 rows=100 width=64) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.194 ms  plan:
Query Text: 
  select concat_ws(
    chr(30),  -- U+001E record separator
    p_seq::text,
    to_char(p_occurred_at at time zone 'UTC', 'YYYY-MM-DD"T"HH24:MI:SS.US"Z"'),
    coalesce(p_actor_id::text, ''),
    case when p_actor_is_admin then 'true' else 'false' end,
    coalesce(p_organization_id::text, ''),
    coalesce(p_hospital_id::text, ''),
    coalesce(p_commission_id::text, ''),
    p_action,
    p_entity_type,
    p_entity_id::text,
    p_summary,
    app.jsonb_canonical(p_metadata)
  );

Query Parameters: $1 = '1', $2 = '2026-09-03 09:42:08.656232+00', $3 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $4 = 'f', $5 = '0585641f-07df-2b46-928e-bfaae13d22e2', $6 = 'form.updated', $7 = 'form', $8 = '24fe5757-a7bd-fc3e-77ae-12625b9f597b', $9 = 'Formulário atualizado: AE4 Perf Formulario 1', $10 = '{"acting_as": "staff_admin"}', $11 = '6bd19175-606a-c9ad-88c2-1efd7bd6cf8b', $12 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822'
Result  (cost=0.00..0.30 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 0.016 ms  plan:
Query Text: SELECT 1 FROM ONLY "public"."profiles" x WHERE "id" OPERATOR(pg_catalog.=) $1 FOR KEY SHARE OF x
Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
LockRows  (cost=0.29..8.31 rows=1 width=10) (actual rows=1 loops=1)
  Buffers: shared hit=5
  ->  Index Scan using profiles_pkey on profiles x  (cost=0.29..8.30 rows=1 width=10) (actual rows=1 loops=1)
        Index Cond: (id = $1)
        Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.010 ms  plan:
Query Text: SELECT 1 FROM ONLY "public"."commissions" x WHERE "id" OPERATOR(pg_catalog.=) $1 FOR KEY SHARE OF x
Query Parameters: $1 = '0585641f-07df-2b46-928e-bfaae13d22e2'
LockRows  (cost=0.28..8.30 rows=1 width=10) (actual rows=1 loops=1)
  Buffers: shared hit=4
  ->  Index Scan using commissions_pkey on commissions x  (cost=0.28..8.29 rows=1 width=10) (actual rows=1 loops=1)
        Index Cond: (id = $1)
        Buffers: shared hit=3
psql:<stdin>:631: LOG:  duration: 0.336 ms  plan:
Query Text: insert into public.audit_log (
    occurred_at, organization_id, hospital_id, commission_id, actor_id, actor_is_admin,
    action, entity_type, entity_id, summary, metadata,
    seq, prev_hash, row_hash
  ) values (
    v_occurred, v_org, v_hospital, p_commission, v_actor, v_actor_is_admin,
    p_action, p_entity_type, p_entity_id, p_summary,
    v_metadata,
    v_seq, v_prev_hash, v_row_hash
  )
Insert on audit_log  (cost=0.00..0.01 rows=0 width=0) (actual rows=0 loops=1)
  Buffers: shared hit=15
  ->  Result  (cost=0.00..0.01 rows=1 width=305) (actual rows=1 loops=1)
psql:<stdin>:631: LOG:  duration: 3.455 ms  plan:
Query Text: SELECT app.audit_write(v_action, 'form', v_id, v_comm, v_summary, v_meta)
Result  (cost=0.00..0.26 rows=1 width=4) (actual rows=1 loops=1)
  Buffers: shared hit=1193
UPDATE 1
Time: 21.048 ms
psql:<stdin>:631: LOG:  duration: 20.564 ms  plan:
Query Text: update public.forms set title = title where id = '24fe5757-a7bd-fc3e-77ae-12625b9f597b'::uuid;
Update on forms  (cost=0.27..9.29 rows=0 width=0) (actual rows=0 loops=1)
  Buffers: shared hit=114
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  InitPlan 2
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  InitPlan 3
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  InitPlan 4
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Index Scan using forms_pkey on forms  (cost=0.14..9.16 rows=1 width=30) (actual rows=1 loops=1)
        Index Cond: (id = '24fe5757-a7bd-fc3e-77ae-12625b9f597b'::uuid)
        Filter: (app.can_edit_commission_forms(commission_id, (InitPlan 3).col1) AND (app.can_edit_commission_forms(commission_id, (InitPlan 4).col1) OR app.is_member_of(commission_id) OR app.is_tenancy_admin_of(commission_id)))
        Buffers: shared hit=56
RESET
Time: 0.127 ms
ROLLBACK
Time: 0.132 ms
=== PASS B / SEAM-nested — one direct authz.has_permission call ===
=== the cleanest view of layers 2 and 1 with no policy noise ===
BEGIN
Time: 0.087 ms
SET
Time: 0.106 ms
psql:<stdin>:639: LOG:  duration: 0.009 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:639: LOG:  duration: 0.005 ms  plan:
Query Text: 
  select coalesce(
    (
      select is_active
         and (suspended_until is null or now() >= suspended_until)
      from public.profiles
      where id = p_user_id
    ),
    false  -- absent profile / null uid => not active (fail closed).
  );

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Result  (cost=8.31..8.32 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=3
  InitPlan 1
    ->  Index Scan using profiles_pkey on profiles  (cost=0.29..8.31 rows=1 width=1) (actual rows=1 loops=1)
          Index Cond: (id = $1)
          Buffers: shared hit=3
psql:<stdin>:639: LOG:  duration: 0.257 ms  plan:
Query Text: 
  select m.role,
         m.scope_kind::text,
         case m.scope_kind
           when 'commission'   then m.commission_id
           when 'hospital'     then m.hospital_id
           when 'organization' then m.organization_id
         end
    from public.memberships m
   where app.is_active(p_principal)
     and m.principal_id = p_principal
     and (m.expires_at is null or m.expires_at > now())
     and m.scope_kind is not null

  union all

  select 'platform_admin', 'none', null::uuid
    from public.profiles p
   where p.id = p_principal
     and p.is_admin
     and app.is_active(p_principal);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'
Append  (cost=4.57..28.09 rows=5 width=59) (actual rows=20 loops=1)
  Buffers: shared hit=12
  ->  Result  (cost=4.57..19.77 rows=4 width=54) (actual rows=20 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Bitmap Heap Scan on memberships m  (cost=4.57..19.74 rows=4 width=64) (actual rows=20 loops=1)
              Recheck Cond: (principal_id = $1)
              Filter: ((scope_kind IS NOT NULL) AND ((expires_at IS NULL) OR (expires_at > now())))
              Heap Blocks: exact=1
              Buffers: shared hit=3
              ->  Bitmap Index Scan on memberships_principal_idx  (cost=0.00..4.32 rows=4 width=0) (actual rows=20 loops=1)
                    Index Cond: (principal_id = $1)
                    Buffers: shared hit=2
  ->  Result  (cost=0.29..8.30 rows=1 width=80) (actual rows=0 loops=1)
        One-Time Filter: app.is_active($1)
        Buffers: shared hit=6
        ->  Index Scan using profiles_pkey on profiles p  (cost=0.29..8.30 rows=1 width=0) (actual rows=0 loops=1)
              Index Cond: (id = $1)
              Filter: is_admin
              Rows Removed by Filter: 1
              Buffers: shared hit=3
psql:<stdin>:639: LOG:  duration: 0.003 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '0585641f-07df-2b46-928e-bfaae13d22e2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.004 ms  plan:
Query Text: 
  select nullif(current_setting('request.jwt.claims', true), '')::jsonb ->> 'active_role';

Result  (cost=0.00..0.02 rows=1 width=32) (actual rows=1 loops=1)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '16e876bd-578c-9320-3016-c4553e1fe1bf', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1b089018-b0d4-fdf3-cb62-56c59d53fa13', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '88be884e-cbca-cae8-ff9d-49d6efcae2a0', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '66b8d5ff-3c9d-968e-24c8-fe5674db8b07', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0afe53a-4290-1347-816a-7d2668dd05c6', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'b0d53eb7-c350-6564-b661-3724f9630d2e', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '893867d4-3b96-5c31-f935-43d134e70038', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'a98ae054-e172-e07a-f2f5-06705d91ee63', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '848b878f-1049-d558-3d04-f1e553d0eb43', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '6c86f675-d3f5-3d81-2924-f238f42f95f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'c38d4a0f-44ac-ad02-9374-a52448f48505', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '83613569-15b0-97be-9c66-5a3195d627a3', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '1e0cbb68-cf55-731b-0845-9157ee1d7f11', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '5a341859-4006-d902-a3dd-8a08a0ca9263', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = '3c7a30a1-2fa9-0122-e1d2-d01c9e38fb8c', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.001 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'commission', $2 = 'e0618c9c-287f-0c30-5519-8c0e4771b6ab', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'ad85a22b-ef53-333f-48dd-00d2a4b67822', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'f66a9aa0-598b-70bb-61f1-799e380302f1', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 0.002 ms  plan:
Query Text: 
  select case
    when p_assignment_kind = p_resolution_kind then
      p_assignment_id = p_requested_id

    -- The ascent. commissions.organization_id is the SAME value the join used to
    -- fetch from hospitals — commissions_hospital_org_fkey makes that an enforced
    -- invariant, not a convention. See this migration's header for the proof and
    -- for the plan measurement that motivated it.
    when p_resolution_kind = 'organization' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.organization_id
                          from public.commissions c
                         where c.id = p_assignment_id)

    when p_resolution_kind = 'organization' and p_assignment_kind = 'hospital' then
      p_requested_id = (select h.organization_id from public.hospitals h where h.id = p_assignment_id)

    when p_resolution_kind = 'hospital' and p_assignment_kind = 'commission' then
      p_requested_id = (select c.hospital_id from public.commissions c where c.id = p_assignment_id)

    else false
  end;

Query Parameters: $1 = 'hospital', $2 = 'a6316db2-149e-732d-d4a0-18c70a748fe2', $3 = 'commission', $4 = '0585641f-07df-2b46-928e-bfaae13d22e2'
Result  (cost=20.14..20.17 rows=1 width=1) (actual rows=1 loops=1)
  InitPlan 1
    ->  Index Scan using commissions_pkey on commissions c  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
  InitPlan 2
    ->  Seq Scan on hospitals h  (cost=0.00..3.55 rows=1 width=16) (never executed)
          Filter: (id = $2)
  InitPlan 3
    ->  Index Scan using commissions_pkey on commissions c_1  (cost=0.28..8.29 rows=1 width=16) (never executed)
          Index Cond: (id = $2)
psql:<stdin>:639: LOG:  duration: 1.937 ms  plan:
Query Text: 
  select af.role_code,
         rp.permission_code,
         r.state::text,
         -- The §6A ASYMMETRY, carried verbatim from AE4.4b: a THIRD-PARTY question ignores the
         -- hat (the 27 `_for` sites), a SELF question requires it (the 151 self-check sites).
         -- Neither uniform choice is correct.
         (p_principal is distinct from (select auth.uid())
          or af.role_code is not distinct from app.active_role())
    from authz.assignment_facts(p_principal) af
    join authz.roles r
      on r.code = af.role_code
    join authz.role_permissions rp
      on rp.role_code = af.role_code
    join authz.permission_implication_closure cl
      on cl.implying = rp.permission_code
     and cl.implied  = p_permission_code
   where authz.scope_reaches(af.scope_kind, af.scope_id, p_resolution_kind, p_scope_id);

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Nested Loop  (cost=3.10..266.30 rows=2 width=94) (actual rows=1 loops=1)
  Join Filter: (r.code = rp.role_code)
  Buffers: shared hit=15
  InitPlan 1
    ->  Result  (cost=0.00..0.03 rows=1 width=16) (actual rows=1 loops=1)
  ->  Hash Join  (cost=1.55..3.09 rows=1 width=41) (actual rows=1 loops=1)
        Hash Cond: (rp.permission_code = cl.implying)
        Buffers: shared hit=2
        ->  Seq Scan on role_permissions rp  (cost=0.00..1.42 rows=42 width=41) (actual rows=42 loops=1)
              Buffers: shared hit=1
        ->  Hash  (cost=1.54..1.54 rows=1 width=29) (actual rows=1 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on permission_implication_closure cl  (cost=0.00..1.54 rows=1 width=29) (actual rows=1 loops=1)
                    Filter: (implied = $4)
                    Rows Removed by Filter: 42
                    Buffers: shared hit=1
  ->  Hash Join  (cost=1.52..262.42 rows=20 width=53) (actual rows=1 loops=1)
        Hash Cond: (af.role_code = r.code)
        Buffers: shared hit=13
        ->  Function Scan on assignment_facts af  (cost=0.25..260.25 rows=333 width=32) (actual rows=1 loops=1)
              Filter: authz.scope_reaches(scope_kind, scope_id, $2, $3)
              Rows Removed by Filter: 19
              Buffers: shared hit=12
        ->  Hash  (cost=1.12..1.12 rows=12 width=21) (actual rows=12 loops=1)
              Buckets: 1024  Batches: 1  Memory Usage: 9kB
              Buffers: shared hit=1
              ->  Seq Scan on roles r  (cost=0.00..1.12 rows=12 width=21) (actual rows=12 loops=1)
                    Buffers: shared hit=1
psql:<stdin>:639: LOG:  duration: 2.407 ms  plan:
Query Text: 
  select case
    -- (1) SCOPE-KIND VALIDATION, fail closed. `is distinct from` makes BOTH an unknown code
    -- (the subselect is NULL) and a NULL p_scope_kind deny, without a separate branch.
    when p_scope_kind is distinct from (
           select pm.resolution_scope_kind::text
             from authz.permissions pm
            where pm.code = p_permission_code)
      then false
    else exists (
      select 1
        from authz.entailed_grants(p_principal, p_scope_kind, p_scope_id, p_permission_code) eg
       where eg.role_state = 'authoritative'   -- (2) THE STATE GATE. Runtime sees ONLY this.
         and eg.hat_ok
    )
  end;

Query Parameters: $1 = '7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c', $2 = 'commission', $3 = '0585641f-07df-2b46-928e-bfaae13d22e2', $4 = 'commission.forms.edit'
Result  (cost=8.04..8.05 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
  InitPlan 1
    ->  Seq Scan on permissions pm  (cost=0.00..1.54 rows=1 width=32) (actual rows=1 loops=1)
          Filter: (code = $4)
          Rows Removed by Filter: 42
          Buffers: shared hit=1
  InitPlan 2
    ->  Function Scan on entailed_grants eg  (cost=0.25..12.75 rows=2 width=0) (actual rows=1 loops=1)
          Filter: (hat_ok AND (role_state = 'authoritative'::text))
          Buffers: shared hit=26
 has_permission 
----------------
 t
(1 row)

Time: 2.869 ms
psql:<stdin>:639: LOG:  duration: 2.637 ms  plan:
Query Text: select authz.has_permission('7b5a47d9-9dac-9d5b-bba3-dd635b55cb3c'::uuid, 'commission', '0585641f-07df-2b46-928e-bfaae13d22e2'::uuid, 'commission.forms.edit');
Result  (cost=0.00..0.26 rows=1 width=1) (actual rows=1 loops=1)
  Buffers: shared hit=27
ROLLBACK
Time: 0.122 ms
RESET
Time: 0.138 ms
RESET
Time: 0.133 ms
RESET
Time: 0.125 ms
RESET
Time: 0.130 ms
RESET
Time: 0.110 ms
RESET
Time: 0.119 ms
@@AE4_NESTED_END@@
⛔ If the marker above is present but the subject greps in the
   acceptance doc section 7 return ZERO, that is a finding about the
   CAPTURE MECHANISM (auto_explain not reaching the client), not a
   pass on P2/P3. Diagnose before evaluating any bound.

################################################################
## SECTION 7 — DC2 (N-differential), P4 (linearity), P5 (arms) ##
##  All three are measured on the READ path, where no trigger  ##
##  fires, so the number is the policy predicate and nothing   ##
##  else. DC2 asks whether the cost tracks the protected-row   ##
##  count at all (flat in N => the fixture did not scale, or   ##
##  the SRF is hoisted, and a flat green number would be       ##
##  indistinguishable from a dead instrument). P4 asks whether ##
##  that growth is at worst linear. P5 compares the permission ##
##  arm against the legacy arm on an IDENTICAL statement.      ##
################################################################
psql:<stdin>:673: NOTICE:  table "ae4_timing" does not exist, skipping
DROP TABLE
Time: 0.252 ms
CREATE TABLE
Time: 2.212 ms
GRANT
Time: 0.832 ms
CREATE FUNCTION
Time: 1.055 ms
BEGIN
Time: 0.108 ms
SET
Time: 0.147 ms
SET
Time: 0.103 ms
INSERT 0 1
Time: 3.911 ms
RESET
Time: 0.141 ms
COMMIT
Time: 0.109 ms
BEGIN
Time: 0.075 ms
SET
Time: 0.074 ms
SET
Time: 0.075 ms
INSERT 0 1
Time: 2371.374 ms (00:02.371)
RESET
Time: 0.255 ms
COMMIT
Time: 0.320 ms
DO
Time: 0.917 ms
psql:<stdin>:751: NOTICE:  AE4 P5 precondition OK — both arms see the same 10000 rows.
BEGIN
Time: 0.190 ms
 ae4_time 
----------
 6.760000
(1 row)

Time: 42.205 ms
 ae4_time 
----------
 7.377000
(1 row)

Time: 39.685 ms
 ae4_time 
----------
 6.754000
(1 row)

Time: 36.259 ms
 ae4_time 
----------
 8.297000
(1 row)

Time: 44.962 ms
 ae4_time 
----------
 3.023000
(1 row)

Time: 17.730 ms
  ae4_time   
-------------
 2165.509000
(1 row)

Time: 11196.354 ms (00:11.196)
COMMIT
Time: 1.287 ms
BEGIN
Time: 0.153 ms
ALTER POLICY
Time: 0.745 ms
Time: 81.505 ms
Time: 55130.646 ms (00:55.131)
ROLLBACK
Time: 1.323 ms
       label       |   ms_best   | reps |  measured_as  
-------------------+-------------+------+---------------
 DC2/N=10          |    6.760000 |    5 | authenticated
 DC2/N=100         |    7.377000 |    5 | authenticated
 DC2/N=1000        |    6.754000 |    5 | authenticated
 DC2/N=10000       |    8.297000 |    5 | authenticated
 P5/legacy-arm     | 2165.509000 |    5 | authenticated
 P5/permission-arm |    3.023000 |    5 | authenticated
(6 rows)

Time: 0.274 ms
DO
Time: 0.420 ms
psql:<stdin>:814: NOTICE:  AE4 impersonation gate OK — every timing was taken as `authenticated`.
psql:<stdin>:854: NOTICE:  AE4 live-path N-differential (evidence, not DC2): 10 rows 6.760000 ms, 10000 rows 8.297000 ms, ratio 1.23.
psql:<stdin>:854: NOTICE:  AE4 P4 decade ratio (N=1000 -> N=10000) = 1.23 (6.754000 ms -> 8.297000 ms). Threshold <= 30.
psql:<stdin>:854: NOTICE:  AE4 P5 ratio (permission arm / legacy arm) = 0.00 (3.023000 ms / 2165.509000 ms). Threshold <= 4.
DO
Time: 1.242 ms
 ae4_say 
---------
 
(1 row)

Time: 0.356 ms

################################################################
## SECTION 8 — DISCRIMINATION CONTROL DC1 (planted cost)      ##
##  THE control that answers "could this measurement have     ##
##  shown a regression at all?". authz.assignment_facts is    ##
##  replaced, inside a rolled-back transaction, by a body     ##
##  returning the SAME rows for ~50x the work. If the measured ##
##  statement does not slow by >=10x, the instrument is blind  ##
##  and every green number above is uninterpretable.          ##
################################################################
BEGIN
Time: 0.087 ms
ALTER POLICY
Time: 0.558 ms
Time: 1142.716 ms (00:01.143)
ROLLBACK
Time: 0.238 ms
BEGIN
Time: 0.104 ms
ALTER POLICY
Time: 0.725 ms
CREATE FUNCTION
Time: 0.532 ms
  ae4_time  
------------
 373.098000
(1 row)

Time: 1901.615 ms (00:01.902)
Time: 0.342 ms
ROLLBACK
Time: 0.272 ms
BEGIN
Time: 0.096 ms
ALTER POLICY
Time: 0.696 ms
CREATE FUNCTION
Time: 0.438 ms
  ae4_time   
-------------
 3922.012000
(1 row)

Time: 21003.285 ms (00:21.003)
Time: 0.382 ms
ROLLBACK
Time: 0.375 ms
 ae4_say 
---------
 
(1 row)

Time: 0.636 ms
--- DC1 ATTRIBUTION: which term carries the per-protected-row cost ---
 assignment_facts_ratio | scope_reaches_ratio |                                   reading                                    
------------------------+---------------------+------------------------------------------------------------------------------
 1.74                   | 18.30               | a large ratio localises the cost to that term; both small = it is in neither
(1 row)

Time: 0.160 ms
DO
Time: 0.911 ms
psql:<stdin>:1073: NOTICE:  AE4 DC1 rollback OK — authz.assignment_facts is the shipped body again.
BEGIN
Time: 0.082 ms
ALTER POLICY
Time: 0.828 ms
Time: 1133.765 ms (00:01.134)
ROLLBACK
Time: 0.289 ms
                                        dc1_restore_check                                        
-------------------------------------------------------------------------------------------------
 AE4 DC1 restore OK — 214.724000 ms vs 214.261000 ms baseline (both on the pre-change predicate)
(1 row)

Time: 0.251 ms
DO
Time: 0.217 ms

################################################################
## SECTION 8b — DC3 (semantic ablation) and P7 (short-circuit) ##
##  Acceptance §13.2, ADR 0182. DC1 above is a LEGACY-PREDICATE ##
##  control: it proves the harness can see a seam, on the path  ##
##  20261003007320 replaced. These two bound the LIVE path,     ##
##  and NEITHER is a timing ratio — flattening the timing IS    ##
##  the change, so a ratio cannot be the instrument.            ##
################################################################
Time: 12.346 ms
--- DC3 preflight: the foreign organization, and the two baselines ---
BEGIN
Time: 0.103 ms
SET
Time: 0.128 ms
SET
Time: 0.087 ms
Time: 13.112 ms
RESET
Time: 0.161 ms
ROLLBACK
Time: 0.183 ms
             foreign_org              | own_org_rows_visible | foreign_org_rows_visible 
--------------------------------------+----------------------+--------------------------
 0c000000-0000-0000-0000-00000000000a | 10000                | 0
(1 row)

Time: 0.198 ms
BEGIN
Time: 0.156 ms
CREATE FUNCTION
Time: 0.523 ms
SET
Time: 0.099 ms
SET
Time: 0.147 ms
Time: 12224.870 ms (00:12.225)
RESET
Time: 0.150 ms
Time: 57245.495 ms (00:57.245)
ROLLBACK
Time: 2.621 ms
BEGIN
Time: 0.077 ms
CREATE FUNCTION
Time: 0.345 ms
SET
Time: 0.092 ms
SET
Time: 0.082 ms
Time: 0.749 ms
RESET
Time: 0.076 ms
ROLLBACK
Time: 0.110 ms
DO
Time: 6.922 ms
psql:<stdin>:1193: NOTICE:  AE4 DC3 rollback OK — authz.authorized_scope_ids is the shipped body again.
 ae4_say 
---------
 
(1 row)

Time: 0.651 ms
CREATE FUNCTION
Time: 2.202 ms
BEGIN
Time: 0.196 ms
ALTER POLICY
Time: 0.872 ms
Time: 11341.676 ms (00:11.342)
ROLLBACK
Time: 0.216 ms
Time: 4.781 ms
 ae4_say 
---------
 
(1 row)

Time: 0.475 ms

################################################################
## SECTION 9 — POSTFLIGHT: THE STACK MUST BE UNMUTATED        ##
################################################################
                             check                              | result 
----------------------------------------------------------------+--------
 any authz body still contains a DC1 planted marker (must be f) | f
 authz.roles staff_admin state is authoritative (must be t)     | t
 any public trigger left DISABLED (must be f)                   | f
(3 rows)

Time: 7.328 ms
psql:<stdin>:1299: NOTICE:  AE4 harness: stack unmutated.
DO
Time: 6.730 ms

=== AE4 perf harness complete. Evaluate P1-P6 per
=== docs/design/authz-ae4-performance-acceptance.md section 6. ===

################################################################
## SECTION 10 — THE VERDICT. One raise, at the end, after the ##
##  controls AND after the postflight, so no condition can    ##
##  abort the run before the control that validates it.       ##
################################################################
DO
Time: 0.390 ms
--- AE4 VERDICT TABLE ---
   kind    | id  | status |                                                                                                                                                    detail                                                                                                                                                    
-----------+-----+--------+--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 CONTROL   | DC1 | PASS   | DC1a assignment_facts=1.74x, DC1b scope_reaches=18.30x (pair passes when EITHER >= 10x; if neither moves, the instrument is blind)
 CONTROL   | DC2 | PASS   | 1000x the protected rows: 14.080000 ms -> 10782.694000 ms (765.82x) on the PRE-CHANGE predicate. Threshold >= 5x. A ratio below it means the fixture did not scale, not that the optimisation worked — that is why this is measured where the per-row evaluation still happens (ADR 0182, acceptance §13.6).
 CONTROL   | DC3 | PASS   | DC3a empty-set: own-org rows 10000 -> 10000 (MUST NOT move: the ELSE arm still grants) at 11034.832000 ms. DC3b over-broad: foreign-org rows 0 -> 1 (MUST become > 0: the set arm is consulted and load-bearing).
 CONDITION | P1  | UNRUN  | evaluated externally from the nested region -- acceptance doc section 9.7
 CONDITION | P2  | UNRUN  | evaluated externally from the nested region -- acceptance doc section 9.7
 CONDITION | P3  | UNRUN  | evaluated externally from the nested region -- acceptance doc section 9.7
 CONDITION | P4  | PASS   | decade ratio 1.23 (threshold 30; linear = 10)
 CONDITION | P5  | PASS   | ratio 0.00 (threshold 4)
 CONDITION | P7  | PASS   | live [hashed=t,loops1=t,never=t] — all three required: an uncorrelated set built ONCE, and the row authorizer not reached. CONTROL on the pre-change predicate [hashed=f,loops1=f,never=f] MUST NOT be all-true, or the probe is not measuring the subplan at all.
(9 rows)

Time: 0.265 ms
psql:<stdin>:1346: ERROR:  AE4 ACCEPTANCE NOT MET (6 of 9 rows PASS).
Not passing: P1=UNRUN, P2=UNRUN, P3=UNRUN
Read the verdict table above: FAIL is a regression, VOID means nothing was measured and the run is repeated, UNRUN means the row was never reached.
CONTEXT:  PL/pgSQL function inline_code_block line 8 at RAISE
Time: 0.469 ms
