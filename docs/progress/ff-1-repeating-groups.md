# FF-1 — Repeating Groups (Flexible-Forms Program, phase 1 of 5)

**Status:** ✅ COMPLETE — QA APPROVED r2, human-approved 2026-07-27. **ADR:** [0087](../decisions/0087-ff1-repeating-groups.md) (+ Amendment 1).
**Program:** [flexible-forms-program.md](../plans/flexible-forms-program.md) · ADR [0086](../decisions/0086-flexible-forms-pre-pilot.md).
**Migrations:** `20260828000000`–`…000800` + gate flip `…000900`. **Flag:** `repeating_groups` **ON**.
**Review:** [phase-FF-1-review.md](../reviews/phase-FF-1-review.md) (r1 CHANGES REQUESTED → r2 APPROVED).

Rotated out of PROGRESS.md at the §6 Record step (CLAUDE.md §7). Archive files are append-only and
are never loaded by teammate spawns — detail lives here to stay out of every spawn's context.

---

## Current Phase Tasks

<!-- Lead recreates this table at the start of each phase. At the §6 Record step the
     completed phase's task detail is archived to docs/progress/phase-N.md (or a
     feature-named file) and replaced here by a one-line pointer (CLAUDE.md §7). -->

▶ **ACTIVE: FF-1 — Repeating Groups** (Flexible-Forms Program, ADR
[0086](../../docs/decisions/0086-flexible-forms-pre-pilot.md) → [program plan](../../docs/plans/flexible-forms-program.md)).
Phase ADR **[0087](../../docs/decisions/0087-ff1-repeating-groups.md)** accepted 2026-07-27 (6 PO rulings; grilling
interview). Flag `repeating_groups` (seeded OFF → enable migration at the gate). Worktree
`worktrees/ff/flexible-forms-program`, branch `ff/flexible-forms-program`.

> ⚠ **ADR 0087 §Substrate supersedes the program plan's FF-1 text in 6 places** (live-catalog audit at phase
> start). Binding for every teammate: per-item config is **`form_items.config`**, *not* `behavior_config`
> (that column is on `form_versions`) · `response_group_instances` is **already directly writable** (the K9
> "DML denied" rule covers the six read-only tables only) → writers are **INVOKER**, RLS stays the boundary,
> and the plan's `rls_group_instances_reader_non_writer` keystone is **retired as not-applicable** ·
> a second CHECK, `form_items_conditional_not_required`, blocks conditional+required and **FF-1 drops it
> globally** · `form_items.section_id` is NOT NULL for group children, so the completeness dispatch must
> exclude `parent_item_id IS NOT NULL` from the flat arm · `app.answer_map` silently collides across
> instances · `save_section_answers` has **no** instance arm (only `null` literals) and its
> `p_clear_item_ids` delete is unscoped by instance. SQLSTATE high-water **HC098** → allocate **HC099+**.

| # | Task | Owner | Status |
|---|---|---|---|
| BE-0 | **Contract-first:** post typed stubs `frontend` depends on (instance RPC signatures, group-aware wizard/builder types in `src/lib/queries/forms.ts` + `submissions.ts`) before implementing | backend | ✅ `cd9f42b` — `ContainerItemType`/`Item.children`/`flattenItem` · `ItemConfig.min/maxInstances` · `GroupInstance` + `.instances` on `ResponseForFill`/`SubmissionDetail` · `overlayAnswerMap` · 3 instance-action stubs + `SaveSectionInput.instances`. ⚠ **9 intentional typecheck errors in `src/components/**` — FE-1's first move** (see the note under this table) |
| BE-1 | Migration wave 1: flag seeded OFF · relax the `group`/`repeating_group` arms of `form_items_input_vs_display` · **drop `form_items_conditional_not_required`** (ruling 4) · depth-1 container cap (ruling 1) · children-contiguous-after-parent enforcement | backend | ✅ `20260828000000` — flag OFF · CHECK dropped · container arm re-cut (`question_key IS NULL`) · depth cap **declarative** (2 generated cols + composite self-FK, same name + `form_items_no_nested_container`) · `rgi` unique → DEFERRABLE · `app.validate_group_layout` wired into `publish_form_version`. 8/8 smoke assertions verified live |
| BE-1a | Additions routed mid-wave: `feature-flags.ts` `repeating_groups` + `repeatingGroupsEnabled()` · `toConfig()` now parses `minInstances`/`maxInstances` (was dropping them field-by-field) · `seed.sql` flips the flag ON for local/E2E | backend | ✅ |
| BE-2 | Instance-aware **`app.answer_map`** — 2-tier overlay (top-level ⊕ instance-*I*, same-instance wins, sibling-absent never falls back) + SQL golden vectors (`20_conditions.sql`) | backend | ✅ `20260828000100` — `app.overlay_answer_map` (pure/IMMUTABLE, the parity seam) · `app.answer_map_scoped(response, instance)` = the ONE body · `app.answer_map` rewrapped **and fixed** (it had no instance filter at all — substrate 5, affecting 9 call sites) · `app.instance_answer_map`. Golden vectors land in BE-7 |
| BE-3 | INVOKER RPCs `add_/remove_/reorder_group_instances` — atomic `max(position)+1`, collision-free reorder against the non-deferrable unique, `max_instances` (ruling 5) | backend | ✅ `20260828000200` — 3 INVOKER RPCs (HC0N0–HC0N4), flag-guarded; deferred-unique reorder; **+ Amendment 1.3** correction-copy fix. ⚠ **BUG-FF1-001**: the SQL was complete but the three server actions stayed BE-0 stubs — fixed + action-layer coverage at `2c56946` |
| BE-4 | `save_section_answers` instance arm (`on conflict (response_id,item_id,group_instance_id) where group_instance_id is not null`) + **fix `p_clear_item_ids` instance scoping** | backend | ✅ `20260828000300` (write) + **`20260828000700`** (sign-off door) + the TS read arm — `buildGroupInstances` feeds `getResponseForFill` / `getSubmissionDetail` / `getResponseForSignoff`; `p_clear_item_ids` scoped; `app.answer_map_by_item` had the SAME missing filter and is fixed |
| BE-5 | **Dispatch-by-`item_type` refactor** of `app.response_required_complete` + group arm (min_instances, per-instance children, **empty-instance skip**) + `submit_response` **prune-then-check** (ruling 3) | backend | ✅ `20260828000400` — dispatch-by-`item_type` in BOTH authorities · `app.instance_is_empty` (one definition: submit PRUNES, rrc SKIPS) · flat arm = Amendment 1.2 predicate · HC0N5 · `assert_item_bounds` instance-scoped |
| BE-6 | `validate_visible_when` — reject outside-in conditions targeting a *repeating*-group child, pt-BR `check_violation` (ruling 2); plain `group` children stay legal targets | backend | ✅ `20260828000500` — outside-in ban in `validate_visible_when` (pt-BR `check_violation`); TS mirror `isConditionTargetInScope`; `conditionTargets(sectionId, forItemId?)` now descends + scopes |
| BE-7 | TS evaluator twin (`conditions.ts` instance-aware map) + `condition-vectors.json` new dimension — **SQL↔TS parity is phase-blocking (Rule 3)** | backend | ✅ `instance-map-vectors.json` (11 map + 10 eval vectors) mirrored in `20_conditions.sql`; `conditions.test.ts` 107 ✓. **`eval_condition` untouched** — it is already pure over a map, so the instance dimension lives in map construction |
| BE-8 | `dashboard.ts` explode-by-child-`question_key` + supersession-tolerant read predicate (`group_instance_id` stays **out** of the aggregation key) | backend | ✅ `20260828000600` — tally explodes by construction; **the DENOMINATOR was the real bug** (counted responses → >100% shares) and now counts answer units; supersession INHERITED from `app.submitted_form_responses` |
| BE-9 | pgTAP: 209 §B/§C re-pin + the ADR-0087 §Gate keystones, **each mutation-proven** (revert the guard → keystone goes red) | backend | ✅ `270_ff1_repeating_groups.sql` **52** keystones (was 46) + 209 §A / 51 re-pins. **136 files / 3925 tests PASS** on a fresh reset. qa round: **P0-1** (correction destroyed instance-scoped selections — my Amendment 1.3 broke a join whose own comment said "while repeating groups are inert") + **MAJOR-1/2** (two keystones that passed while proving nothing) fixed at `20260828000800`; all 4 mutations re-proven red |
| FE-1 | Builder: enable both container types in `add-block-menu.tsx` / `item-type-meta.tsx` (flag-gated); child authoring; min/max into **`form_items.config`** | frontend | ✅ `633e688` — "Estrutura" picker group (flag-gated) + CHILD mode (never offers a container — depth cap) · nested `BlockCard` children with sibling-bounded reorder · container arm in `ItemEditorDialog` (título/apoio/condição + min-max repetições) · `parseItemConfig` → `minInstances`/`maxInstances` · `addItem` `parentItemId` + descending-shift placement · **`moveItem` is now BLOCK-aware** (a plain `reorder_item` swap put a container behind its own first child) · ruling-2 condition scoping, **mutation-proven**. Typecheck handoff cleared in `44949e8`. **`588b3c6`** — picker now delegates to the SHARED `isConditionTargetInScope` (it had zero callers on the authoring path: the server-side `conditionTargets` backend fixed is unreached, since the dialog reads the pure client-safe module). Local copy deleted; picker and BE-6's publish gate can no longer drift. |
| BE-1a | **`toConfig()` (`src/lib/queries/forms.ts:525`) drops `minInstances`/`maxInstances`** — builds its return field-by-field and never reads them, so cardinality round-trips to nothing. The `ItemConfig` interface and its parser disagree. **Gates FE-1 acceptance** | backend | – |
| BE-4a | **`buildAnswerMaps()` indexes `itemsById` from `section.items` only** — no `children` descent, and the consumer does `if (!item) continue`, so a plain `group`'s (top-level-answering, ruling 6) children are silently dropped from both maps. Use `flattenItem` | backend | – |
| BE-4b | 🔴 **BE-4's TS read arm did NOT land** despite `8db6c10` being titled "BE-3/4/5" — `instances: []` is still hardcoded at `responses.ts:483` + `submissions.ts:610` while the approved plan put the read side in BE-4. **Blocks `tester` entirely**: FE's wizard / submission detail / phase-answers are all wired to consume it, so every instance E2E fails for a non-feature reason | backend | – |
| BE-4c | 🔴 **`ResponseForSignoff` (`signoffs.ts:67`) has no `instances` field** — a staff_admin counter-signing would review a response with **every repeating-group answer missing, and sign it**. Sign-off on incomplete evidence, not cosmetic. Needs the field + populating read + a keystone (a view that silently omits answers passes every test that only checks what *is* rendered) | backend | – |
| BE-6a | **`conditionTargets()` walks `s.items` flat** — client-side mirror of BE-6's publish ban; must **keep** plain-`group` children and **drop** *repeating*-group children. Open contract question: the `(sectionId)` signature cannot express "authoring for *this child* inside *this group*", so it cannot offer the same-instance siblings ruling 2 permits | backend | – |
| FE-2 | Wizard: instance add/remove/reorder controls, per-instance rendering, resume across navigation | frontend | ✅ `9ade870` — per-instance answer state in `use-wizard` (a repeating-group child never writes top-level) · add/remove/reorder go **server-first**, local state mirrors the RPC's authoritative positions · live 2-tier overlay via `overlayAnswerMap` (same-instance wins, sibling-absent never falls back) · resume via `prepare.toInstanceStates` · **ONE payload** per section save. |
| FE-3 | Wizard: `group` as a plain nested sub-section (no instance chrome) + grouped review/summary rendering | frontend | ✅ `9ade870` — `GroupBlock` = nested sub-section, zero instance chrome (ruling 6) · grouped review (flat / group list / repetition-by-repetition, showing only repetitions that SURVIVE the ruling-3 prune) · 3 container-blind read-only views fixed, 2 now render instances. |
| FE-4 | Conditional+required UX now that the CHECK is gone — *obrigatório* offered beside a condition, pt-BR errors | frontend | ✅ `633e688` — `form_items_conditional_not_required` verified GONE from `pg_constraint`; the required checkbox is no longer disabled/forced-off beside a condition, and the copy states the resolved semantics (*obrigatória apenas quando aparecer*) instead of the old prohibition. Editor test rewritten to assert the reversal (old prohibition asserted ABSENT, not merely hidden). |
| FE-5 | Keyboard-only pass over instance controls (add/remove/reorder), visible focus, labels | frontend | ✅ `9ade870` — position-naming pt-BR labels ("Remover a repetição 2 de 3"; 3 identical icon buttons/row are otherwise indistinguishable) · `<ol>`/`<li>` so count+position are structural · each instance a labelled `<section>` · outcomes announced via a polite live region (a keyboard reorder has no visual cue). Full keyboard flow is `tester`'s E2E. |

**Lead file-ownership ruling (2026-07-27, FF-1 only):** `src/lib/forms/actions.ts` + `src/lib/forms/parse-config.ts`
→ **`frontend`**. They fall outside both written scopes (CLAUDE.md §4 gives `backend` `src/lib/{supabase,queries,types}`;
`src/lib/forms/` is neither that nor `src/app`/`src/components`), no BE task touches them, and FE-1 cannot write
min/max without them. Both teammates were told, so the fence binds in both directions. The read/write seam is now
split across teammates, so **the config key names are the contract**: `frontend` writes `minInstances`/`maxInstances`
into `form_items.config` exactly as backend's `ItemConfig` declares them; neither side renames unilaterally.

**Known handoff, not a regression:** BE-0 hands `frontend` a red typecheck (9 errors, all in FE-owned files — 8 test
fixtures needing `children: []`/`instances: []` + `item-type-meta.tsx`). `children`/`instances` are **required by
design**; optional would invite the skip-the-children bug class that BE-1a/BE-4a/BE-6a all are. FE fixes them by
supplying the empty arrays, **never** by widening the type back.

> ⚠ **BE-0 hands over a RED typecheck — by design, 9 errors, all in `src/components/**` (frontend-owned).**
> Widening `ItemType` and adding the required `Item.children` / `ResponseForFill.instances` fields is the
> compile-time signal the repo relies on ("adding a member forces every consumer to handle it"). Not a
> regression; **`src/lib/**` is clean and lint is 0/0.** The 9 sites, each a one-line fix:
> `item-type-meta.tsx:25` (add `group` + `repeating_group` to `ITEM_TYPE_META` — FE-1 anyway) and 8
> test-fixture builders needing `children: []` / `instances: []` —
> `forms/describe-visibility.test.ts:19` · `forms/item-editor-dialog.test.tsx:186` ·
> `wizard/input-item.test.tsx:17` · `wizard/others-fill.test.tsx:36` · `wizard/others-persist.test.tsx:23` ·
> `wizard/prepare.test.ts:16,60` · `wizard/use-wizard.test.ts:20`.

> 🔴 **ADR 0087 §Implementation-notes SQLSTATE line is WRONG — do not allocate `HC099`.** The live
> `pg_proc` high-water is **`HC0M9`**, not `HC098` (the `HC09x` digit lane was exhausted and the
> convention moved to letter lanes `HC0A0`…`HC0M9`; `L` is skipped). FF-1 allocates the next free lane,
> **`HC0N0`+**. `HC098` is the last *digit-lane* code, which is what backend-state's table recorded.

> 🔴 **Two latent bugs FF-1 ACTIVATES** (live-catalog, not in ADR 0087): `start_correction_draft` and
> `supersede_response` copy `answers.group_instance_id` **verbatim from the predecessor** without copying
> `response_group_instances`, so a corrected response's answers would point at the *predecessor's*
> instances (frozen by `guard_submitted_group_instances_trg`; cascade-deleted with the predecessor).
> Inert today (0 instance rows) — must be fixed in the BE-3/BE-4 wave. Also `app.assert_item_bounds`
> does an unscoped `select … where response_id and item_id`, silently picking ONE instance's answer.

Every concluded phase/track lives as a **row in the
[Phase Status](../../PROGRESS.md#phase-status) table**, with its full record under `docs/progress/`; the remaining
pre-pilot work — the FF program, then the **pilot deploy** (origin push + Coolify + remote `db push`) — is
tracked in **Remaining pre-pilot work** below. Phases 18–19 stay post-pilot.

**Recently concluded** (S-track; all human-approved — detail in the linked records + the Phase Status rows):
S5 [ETH·E3a](eth-e3a-surfacing.md) · [Case Corrections](case-corrections.md) ·
[Controlled-Doc Redesign 17-v2](document-control-redesign.md) ·
[AUDIT-DOOR-BLINDNESS P0](authz-p0-door-blindness.md) · [AUTHZ Gate 1+2](authz-handoff.md) ·
S4 [ETH·E2](eth-e2-procedure.md) · [RV2 R2–R5](rv2-r2-r5-governance.md) ·
[CH](ch-charters-cadence.md) · S3 [ETH·E1](eth-e1-access-spine.md) ·
S2 [IV2](iv2-interviews.md) / [RV2·R1](rv2-r1-referrals.md) / [AI](ai-satellites.md) ·
S1 [substrate (MEM/SUP/N)](s1-substrate.md) · [S0 gate](../../docs/plans/pre-pilot-release-s0-ratification.md).

> ⚕️ **Suite-health** (deferred, non-blocking) — the `e2e:prod` gate carries a ~18–31 pre-existing flaky-red
> baseline (memory [[e2e-prod-build-flaky-baseline]]); pay-down = per-test DB isolation / `retries=0` / a
> known-flaky allowlist → [s1-substrate.md](s1-substrate.md).

### 📋 Remaining pre-pilot work / Completed work

> Both sections are **standing** backlog, not FF-1 phase detail — they were briefly rotated here
> in error and live in **PROGRESS.md**. Do not read a backlog snapshot out of an archive: it is
> stale by construction, which is the exact hazard this phase hit three times.

## Bug Log

<!-- Filed by tester; status updated ONLY by tester after re-verification.
     Resolved/closed bugs are archived at phase close → docs/bugs/archive.md. -->

**0 open bugs, 5 closed** (filed 2026-07-27, FF-1 test pass; re-verified same day, three times). All five
were application-code defects, not spec issues — each traced to a specific line via live-catalog/source
verification, not inferred.

| ID | Phase | Severity | Description / repro | Expected | Actual | Spec | Owner | Status |
| -- | ----- | -------- | ------------------- | -------- | ------ | ---- | ----- | ------ |
| BUG-FF1-001 | FF-1 | Blocker | `addGroupInstance` / `removeGroupInstance` / `reorderGroupInstances` (`src/lib/responses/actions.ts:416-454`) were BE-0's original contract-first stubs — each body was literally `throw new Error('not implemented')`. No later BE commit (BE-3 `8db6c10`, BE-6/7/8/9 `30585d4`) had filled them in, despite `wizard-runner.tsx:98-106` wiring the wizard's `addInstance`/`removeInstance`/`reorderInstances` directly to them. | Clicking "Adicionar repetição" / the remove or reorder icon buttons calls the RPC and updates the instance list. | Fixed by backend (`2c56946`). Re-verified live 2026-07-27: FF1-3 and FF1-9 now progress through real UI add/reorder/remove. | `e2e/ff1-repeating-groups.spec.ts` FF1-3, FF1-9 | backend | **closed** — verified fixed `2c56946` |
| BUG-FF1-002 | FF-1 | Blocker | `parseItemFields` (`src/lib/forms/actions.ts:851-856`, shared by `addItem`/`updateItem`) unconditionally cleared `required` whenever `visible_when` was non-null — stale pre-FF-1 "decision #9" defence for the CHECK constraint BE-1 already dropped. | ADR 0087 ruling 4: "se tipo = medicação, nome do medicamento é obrigatório" must be authorable and enforced when visible, at top level and per-instance. | Fixed by frontend (`61221f8`). Re-verified live 2026-07-27: FF1-5 passes clean (no soft-fail) — `required` survives the save and `submit_response` correctly blocks/allows per ruling 4. | `e2e/ff1-repeating-groups.spec.ts` FF1-5 | frontend | **closed** — verified fixed `61221f8` |
| BUG-FF1-003 | FF-1 | Critical | The sign-off UI never surfaced repeating-group instance answers — `ClientResponseForSignoff` had no `instances` field, `adapt.ts` dropped `data.instances`, and `review-and-sign.tsx`'s `SectionBody` only unwrapped plain `group` children. | A staff_admin reviewing a response with repeating-group answers sees every instance's answers by value before signing. | Fixed by frontend (`61221f8`). Re-verified live 2026-07-27: FF1-7 passes clean — both instance note values ("Nota da repetição UM/DOIS (ff1-sign)") render on the sign-off screen. | `e2e/ff1-repeating-groups.spec.ts` FF1-7 | frontend | **closed** — verified fixed `61221f8` |
| BUG-FF1-004 | FF-1 | Blocker | Found during BUG-FF1-001/002/003 re-verification, not in the original pass (masked by BUG-FF1-001 — the wizard couldn't add a second instance at all until that landed). Every control id/name in `input-item.tsx` was built purely from the question's static `item.id`, with no instance scoping — duplicate `id`/`name` across every instance of a repeating group's question. Two consequences: (a) accessibility — instance 2+ unreachable by accessible name; (b) choice types only — the duplicated `name` put every instance's radios in the same native browser exclusivity group, so checking instance 2's option could leave it unchecked while un-checking instance 1's, live-verified. | Every instance's inputs are independently labelled and independently interactable. | Instance 2+ inputs unlabelled to assistive tech; choice-type cross-instance corruption at the native-DOM level. | `e2e/ff1-repeating-groups.spec.ts` FF1-3, FF1-9 | frontend | **closed** — verified fixed `87d7cdd`. `fieldScope` now threads through every control id and the radio `name`; re-verified live 2026-07-27 with the STRENGTHENED cross-instance assertion (act on instance 2, then assert instance 1's value survives — the exact shape the bug broke) — both instances now hold independent, uncorrupted state through a real browser. Per the coordinator: frontend's own unit-test attempt for this was deleted as unwritable in jsdom (React re-asserts sibling radio state after a discrete event before any assertion can observe the corruption) and replaced with a `RadioNodeList`-grouping assertion at the DOM-construction layer; this Playwright case is the only place in the repo that proves the actual user-facing behavior, in either direction — kept as a permanent regression guard, not weakened. |
| BUG-FF1-005 | FF-1 | Blocker | Found while re-verifying BUG-FF1-004 (FF1-3's resume check, reachable for the first time since neither 001 nor 004 blocked it before). `saveSection` (`src/lib/responses/actions.ts` ~line 319) destructured its `input` without `instances` and never passed `p_instance_answers` to `save_section_answers` — despite the client (`collectSection`) correctly building and sending an `instances: InstanceAnswers[]` array. Invisible to FF1-4/5/7/8 because they all filled via a direct `save_section_answers` RPC call (built to route around the then-broken BUG-FF1-001 UI) — none of them exercised the wizard's own save action. FF1-3 is the only spec that saves through the real UI. | The wizard's normal save actions ("Salvar e sair"/"Próximo"/"Revisar") persist per-instance answers exactly as they persist top-level ones. | Per-instance answers were silently dropped on every section save. | `e2e/ff1-repeating-groups.spec.ts` FF1-3 | backend | **closed** — verified fixed `8048d32`. Backend's fix also caught a subtlety a verbatim pass-through would have missed: `InstanceAnswersInput` is camelCase, the RPC reads snake_case per entry (`instance_id` etc.), so the fix is a translation, not a pass-through — a naive fix would have raised HC0N2 on every save. Re-verified live 2026-07-27: FF1-3's dedicated DB-truth assertion (`answers` rows with `group_instance_id is not null` exist after "Salvar e sair") now passes. One own-spec bug found and fixed in the same re-verification: FF1-3's final DB-truth check queried `answers.value` for the CHOICE ("Turno") item — but a choice item's `value` is *always* `null` by the form-model-normalization contract (its selection lives in `answer_selected_options`); the assertion was checking the wrong table for a choice answer, not a real defect. Fixed to join `answer_selected_options`. |


---

## Test Run Summary

<!-- Full historical run log (Phases 0 → S2, 133 rows) archived → docs/progress/test-run-archive.md.
     Keep only the most recent gate's rows here; rotate the rest to the archive at each §6 Record. -->

| Date | Phase | Specs | Passed | Failed | Notes |
| ---- | ----- | ----- | ------ | ------ | ----- |
| 2026-07-27e | **FF-1 — FULL `e2e:prod` GATE (lead-run, prod standalone build, 70 spec files / 12 batches)** | full suite ×2 + 4 targeted isolation runs | run 1: **731** · run 2: **684** | run 1: **85** · run 2: **124** | **Both full runs RED; triage finds NO FF-1 regression, and the FF-1 spec is 9/9 GREEN on a prod build with 0 connection errors (`NPM_EXIT: 0`).** Every failure resolves to one of two pre-existing causes, each established **by experiment, not inference**: **(a) infra collapse — 117 of run 2's 124** sit in batches with heavy `net::ERR_CONNECTION` counts (b1:26, b5:86, b8:117, b9:1). Decisive evidence they are environmental: **the collapses MOVE between runs** — batch 3 was 23-failed/45-conn in run 1 and **totally clean** in run 2; batch 8 was **clean** in run 1 and 59-failed/117-conn in run 2. A code regression does not migrate between batches. **(b) a pre-existing test-isolation defect — the remaining 7**, identical in both runs (batch 6, **0** conn errors, so it passes the usual "conn-errors ⇒ infra" triage rule and needed real investigation). All 7 are `notifications.spec.ts` failing at one shared setup helper (`openFreshCapaPlan` → `open_capa_plan`). **Proven not-FF-1 three ways:** `open_capa_plan`'s `prosrc` references none of `response_required_complete` / `answer_map` / `save_section_answers` / `group_instance`; its `patient_safety` flag gate is ON (all 28 flags enabled — checked the `enabled` column, not the description); and empirically — `notifications.spec.ts` **alone = 8/8 GREEN**, but paired with the spec that precedes it in its batch (`member-action-items-overview.spec.ts`) = **3 failed**. Both specs predate FF-1. This is the documented per-test-DB-isolation debt (memory `e2e-prod-build-flaky-baseline`), not a phase defect. **Batch 5 investigated specifically because it contains `ff1-repeating-groups.spec.ts` (spec #25) and collapsed identically (~44 failed / ~86 conn) in BOTH runs** — unlike the batches that moved. Cleared: batch **1** also collapses reproducibly and contains **no** FF-1 content, so reproducible collapse is a property of certain batch positions, not of FF-1; and the FF-1 spec run **alone on a prod server is 9/9 with 0 conn errors**, so it neither crashes the server nor fails. **Environmental limit reached and recorded:** 4 `db reset` cycles in ~50 min degraded the local stack to `reset FAILED` (one isolation run produced 0 tests). Recovery per memory `supabase-vector-crashloop-502` — `supabase stop/start` + verifying a **real token POST returns 200** (a `/health` 200 is insufficient) — restored it each time. **Method corrections worth keeping:** run 1 was launched as `npm run e2e:prod \| tail -120`, which (i) truncated batches 1–8 out of the captured output and (ii) made the harness-reported "exit code 0" meaningless — without `pipefail` a pipeline returns the LAST command's status, so that 0 was `tail`'s. The gate itself printed `GATE RED`. Run 2 dropped the pipe and captured `NPM_EXIT` explicitly. |
| 2026-07-27d | FF-1 — BUG-FF1-005 re-verification + workaround cleanup (chromium, dev server; NOT the full suite) | `e2e/ff1-repeating-groups.spec.ts` (all 9) | **9/9** | **0** | **All 5 filed bugs now closed** — BUG-FF1-005 verified fixed at `8048d32` (backend's fix translates `InstanceAnswersInput` camelCase → the RPC's snake_case per instance entry, a real subtlety: a verbatim pass-through would have raised HC0N2 on every save). FF1-3 re-run clean; one bug found in my OWN spec during this pass (not an app bug) — the final DB-truth check queried `answers.value` for a CHOICE item, which is always `null` by the form-model-normalization contract (choice answers live in `answer_selected_options`); fixed to join the right table. **Workaround cleanup (coordinator's judgment call, "when a workaround is added for a broken layer, the fix should remove it")**: converted FF1-4 (side B), FF1-7, and FF1-8's fills from the `save_section_answers` RPC-bypass to the real wizard UI, now that BUG-FF1-001/004/005 are all fixed — each hit and required fixing a genuine timing gap in the process (a `.click()` on "Adicionar repetição" resolves once the event fires, not once the async add + revalidation lands; a `.fill()` only updates client state until a save action runs), which is itself worth recording: the conversions were not mechanical swaps, each needed an explicit visibility-wait or a persist step before reading DB truth. **Two bypasses kept, and documented in the spec why**: FF1-4 Side A (0 instances, minInstances unmet) and FF1-5's reject case (a required+visible+blank field) both need `handleNext`'s client-side `validateInstances` to NOT block navigation to review — but it does, by design, for both scenarios, so there is no real-UI path to the exact `submit_response` rejection message; both stay on `submitViaRpc` with an inline comment explaining the client-side block is structural, not a leftover BUG-FF1-001 workaround. Lint 0/0, typecheck clean. One `FF1-1` failure on a full-suite run traced to dev-server cold-start (re-ran clean in isolation) — not a regression. |
| 2026-07-27c | FF-1 — BUG-FF1-004 re-verification (chromium, dev server; NOT the full suite) | `e2e/ff1-repeating-groups.spec.ts` (all 9) | **8/9** | **1** | **BUG-FF1-004 CLOSED, verified fixed `87d7cdd`** — strengthened FF1-3/FF1-9 per the coordinator's explicit request (assert the FIRST instance's value AFTER acting on the second, the only order that can fail against the original bug; both now HARD assertions, not soft, so a regression fails loudly). Both pass that check cleanly through a real browser. **One new bug found in the process: BUG-FF1-005** (blocker) — `saveSection` never forwards the client's `instances` payload to `save_section_answers`'s `p_instance_answers`, so the wizard's real save actions ("Salvar e sair"/"Próximo"/"Revisar") silently drop every per-instance answer; FF1-3 is the only spec in this file that saves through the real UI, so it is the only one that could have caught this — invisible to FF1-4/5/7/8, which all fill via a direct RPC call built to route around the (now-fixed) BUG-FF1-001. **Audit requested by the coordinator — which specs actually rendered 2 real instances via the wizard fill UI**: only FF1-3 (radio + textbox, full interaction) and FF1-9 (textbox, keyboard-driven) do; both correctly caught real bugs. FF1-4/6/8 never render the fill UI with instances present (RPC-only fill, or no instances at all — FF1-6 by design, ruling 6). FF1-7 renders 2 instances only on the READ-ONLY sign-off screen, a different component/rendering path (text-based `AnswerSummary`, not id/label-associated `<input>`s) legitimately unaffected by BUG-FF1-004's mechanism. FF1-5 DOES render 2 real instances in the wizard fill UI (RPC-fill + page reload) but only checks TEXT VISIBILITY of a conditionally-shown question, never interacts with or checks control identity — a legitimate check for ITS OWN claim (per-instance conditional visibility, which doesn't depend on unique ids), but it would not have caught BUG-FF1-004 or BUG-FF1-005 either way. None of the pre-BUG-FF1-004 "green" specs' verdicts are compromised by this — each was checked against what it actually claims, not against instance-rendering claims it never made. Lint 0/0 repo-wide, typecheck clean. |

| Date | Phase | Specs | Passed | Failed | Notes |
| ---- | ----- | ----- | ------ | ------ | ----- |
| 2026-07-27b | FF-1 — re-verification of BUG-FF1-001/002/003 against their fixes (chromium, dev server; NOT the full suite — `e2e:prod` gate stays the lead's) | `e2e/ff1-repeating-groups.spec.ts` (all 9, run individually + in small groups to isolate infra noise from real failures) | **7/9** | **2** | **BUG-FF1-001 (backend `2c56946`), BUG-FF1-002 + BUG-FF1-003 (frontend `61221f8`) all confirmed FIXED** — FF1-4/5/6/7/8 pass clean, FF1-3/9 progress well past the old failure points into real add/reorder/remove UI flows. **One new bug found during this pass: BUG-FF1-004** (blocker) — `input-item.tsx` builds every control's `id`/`name` from the question's static `item.id` alone, no instance scoping, so every input duplicates across a repeating group's instances; breaks accessible-name lookup for instance 2+ (WCAG) and, for choice types, puts every instance's radios in the SAME native browser exclusivity group (cross-instance answer corruption, live-verified: instance 2's click doesn't end up checked). This was invisible in the original pass — BUG-FF1-001 blocked ever reaching a 2-instance state through the UI, so it never got exercised. Full repro/evidence in the Bug Log. **Also found and fixed in my own spec** (not app bugs): a `test.describe.configure({ mode: 'serial' })` copied from another spec's convention was masking real per-test results by skipping everything after one failure (removed, per `docs/testing/e2e-prod-build-gate.md` rec #4); a Chrome accname quirk (`getByLabel('Valor', {exact:true})` never matches a `<select>` nested in a `<label>` — the browser folds the selected option's text into the computed name) — switched to id-suffix selectors for the ConditionBuilder's target/value `<select>`s. Lint (0/0) + typecheck clean throughout. Infra note: 2 dev-server `net::ERR_CONNECTION_REFUSED` cascades hit during this pass after a very long single-session test-running window (consistent with the documented dev-server-under-sustained-load class of flake, not app regressions) — resolved by re-running individually, each in a fresh server. |
| 2026-07-27 | FF-1 — initial phase-gate pass | `e2e/ff1-repeating-groups.spec.ts` (new, 9 tests) | **5/9** | **4** | Original filing: BUG-FF1-001 (blocker, FF1-3/FF1-9), BUG-FF1-002 (blocker, FF1-5), BUG-FF1-003 (critical, FF1-7). FF1-1/2/4/6/8 passed clean, including FF1-4's ruling-3 pruning tested at the RPC/server-authority layer (bypassing BUG-FF1-001's broken UI wrapper) and FF1-8's BE-8 denominator-fix verification (exact 67%/33% shares, not just a ≤100% sanity bound). Superseded by the 2026-07-27b re-verification row above. |
| 2026-07-27b | ETH·E3a (T-1) — post-fix-loop re-verification of BUG-E3A-001/002 (chromium, dev server; NOT the full suite — `e2e:prod` gate is the lead's to run) | `e2e/ethics-e3a-surfacing.spec.ts` (all 21) | **21/21**, run on 2 independent fresh resets (deterministic) | **0** | **Both bugs re-verified fixed, marked RESOLVED in the Bug Log.** FE `ef5c38b` (BUG-E3A-001) + BE `1ce0876` (BUG-E3A-002) landed since the round-1 report; re-ran the full spec twice from a clean `supabase db reset` each time — same 21/21 both times. TERM-3 (case-term heading "Denúncia …") and EVT-2/EVT-3 (coordinator_only badge, auto-derived + manual) now pass outright. **TERM-4 needed a spec-only selector fix, not a re-file**: the new `CasePrimarySubjectPanel`'s heading and the seeded respondent's `case_participant_roles.display_name` are BOTH the literal string "Médico denunciado" (ADR 0064 D4's own example reuses the words for both the term and the role), so my original `page.getByText('Médico denunciado')` hit a Playwright strict-mode violation (2 matches) rather than a real failure — retargeted to `getByRole('region', {name:'Médico denunciado'})` scoped to the panel + a non-vacuous check that the actual respondent ("Dra. Denunciada") resolves inside it, not just an empty-state placeholder. Confirmed the other 17 tests are unaffected by the new primary-subject card / heading change (no regression). **Housekeeping**: BE-8 (`1173900`) renumbered the E3a pgTAP files `260`-`263` → `266`-`269` (resolved the CH/case-corrections numbering collision) — fixed the two stale `263_ethics_e3a_dashboard.sql` references in my own spec (`DASH-SETUP`'s title + comment, ~line 545) to `269_...`; confirmed no other stale `26[0-3]_ethics_e3a` references remain. Lint (`eslint --max-warnings=0`) + `tsc --noEmit` clean. **▶ Next: QA.** |
| 2026-07-27 | ETH·E3a (T-1) — pgTAP re-confirm (fresh reset, `supabase test db`) + new E2E spec (chromium, dev server; NOT the full suite — `e2e:prod` gate is the lead's to run) | pgTAP: `260_ethics_e3a_surfacing.sql` + `261_ethics_e3a_autoderive.sql` + `262_ethics_e3a_terminology_reads.sql` + `263_ethics_e3a_dashboard.sql` (full suite `supabase test db`, 135 files) · E2E: `ethics-e3a-surfacing.spec.ts` (new, 21 tests) | **pgTAP 63/63** (20+20+9+14; full suite `Files=135, Tests=3848, Result: PASS`) · **E2E 17/21** (run 3× on independent fresh resets, fully deterministic — same 4 fail every time) | **pgTAP 0** · **E2E 4** (2 real bugs, 2 failing tests each) | **pgTAP independently confirmed** — do not trust a self-reported total; re-ran the FULL ordered suite myself on a clean reset per protocol. **E2E coverage**: terminology (TERM-1/2/5 pass: ethics case → "Cronologia processual" tab; non-ethics case → unchanged "Linha do tempo" + "Casos" board heading) · kind-widen/visibility floor + auto-derive (EVT-1/4/5/6 pass: 3 real E2 RPCs — `decide_admissibility`/`add_ethics_allegation`/`record_ethics_finding` — correctly auto-emit labeled `case_events`; an ordinary granted reader sees the 2 `case_readers` events and NOT the `coordinator_only` one; a respondent AND a recused reader are denied the case route entirely, RLS-floor-correct) · dashboard confidentiality keystone (DASH-0a/ROLE-GATE/0e/SETUP/1/EXCLUDE-SETUP/EXCLUDE/5, all pass: coordinator's count rises by exactly the fixture (+3 total, +1 pending, +2 admissible, +2 issued, +1 each sanction); **discovered mid-run that `/dashboard/ethics` is gated `staff_admin`-only at the PAGE level** — a plain granted `staff` member cannot reach it at all (verified via live source + `notFound()`), so redesigned the "excluded viewer" scenario around the coordinator themselves made respondent/recused on throwaway cases — proves the deny-terms bind even the commission's OWN staff_admin, a stronger form of acceptance #6/#7 than the original design; foreign-commission coordinator, same org, unaffected) · keyboard-only (KBD-1/2 pass: board → tab-to-case-link → Enter → tab-to-"Cronologia processual" → Enter lands on `/timeline`; the dashboard page itself has no in-app nav link yet — O-2 follow-up, already tracked in PROGRESS.md, not a new finding — so its keyboard check starts from a direct URL nav and confirms no focus trap). **2 bugs filed** (BUG-E3A-001 MAJOR terminology, BUG-E3A-002 MINOR visibility badge — see Bug Log for full repro/evidence). **3 own-harness bugs found+fixed during the write-up (spec-only, 0 app fixes)**: (1) a one-shot `.count()` check in my dashboard reader raced the server-streamed render and read "0 cases" on a page that hadn't finished painting — fixed by waiting for the stat-card-or-empty-state region first (the same class of bug `e2e-networkidle-purged` warns about, web-first assertions only); (2) `new RegExp(rowLabel)` on a sanction label containing literal parentheses ("Advertência (E3a Dash)") silently elided the required parens — the exact trap `ethics-e1-access-spine.spec.ts`'s own AC-9 comment documents for a document title — fixed by using Playwright's plain-string substring match instead; (3) fixture creation initially lived in `beforeAll` (which runs before EVERY test including the "before" baseline capture), collapsing every delta to ~0/nonsensical — fixed by moving fixture creation into an explicit mid-sequence test. **Also flagged (non-blocking, INFO)**: the E3a pgTAP files are numbered `260`–`263`, colliding with the ALREADY-SHIPPED `260_charters.sql`/`261_charters_rpcs.sql`/`262_charter_notifications.sql` (CH, Phase 21) and `263_correction_readers.sql` (case-corrections) — no functional collision (`supabase test db` runs by full filename, not numeric prefix; confirmed both sets ran and passed independently), but the numeric echo is confusing across PROGRESS.md phase rows and worth a rename (e.g. `266`–`269`) at the next convenient touch. Lint (`eslint --max-warnings=0`) + `tsc --noEmit` clean on the new spec. Commit `2956aaf`. **Update:** BE-8 (`1173900`) did the rename → `266`-`269` right after this row; the 2 bugs were fixed + re-verified 21/21 → see the **2026-07-27b** row above. |

> **Most recent gate: S5 · ETH·E3a** (2026-07-27) — the 2 rows above are the tester's clean-reset
> re-verification + the pgTAP/E2E gate run. The lead's full `e2e:prod` declaration (triaged **GREEN, 0
> deterministic regression**) is recorded in the ETH·E3a summary above + [eth-e3a-surfacing.md](eth-e3a-surfacing.md).
>
> **Prior closed gate: AUTHZ Gate 2** (2026-07-17, 0 regressions → [authz-gate-2-review.md](../../docs/reviews/authz-gate-2-review.md)).
> That row + the Case-Correction T-1/T-2 (2026-07-24 / 24b), case-custom-fields (2026-07-23), and
> stale-spec-drift (2026-07-23b) rows rotated → [test-run-archive.md](test-run-archive.md) at
> the 2026-07-27 cleanup. Full run history (Phases 0 → S4) lives there.



---

## FUP-FF1-2 — FF-1 QA non-blocking items (detail rotated out of PROGRESS.md 2026-07-28)

Still OPEN; the live tracker keeps a one-line pointer. Canonical analysis remains
[phase-FF-1-review.md](../reviews/phase-FF-1-review.md).

### ▶ FUP-FF1-2 — FF-1 QA non-blocking items (review r2: 4 MINOR / 6 INFO)

All ruled non-blocking by `qa`; [review](../../docs/reviews/phase-FF-1-review.md). INFO-1 and INFO-5 are
already discharged and are listed only so the numbering reconciles against the report.

- [ ] **MINOR-1 — `completeness_authorities_agree` is one-directional in pgTAP.** Neutralising *every*
  blocking arm of `submit_response` (both `HC011` raises **and** the `HC0N5` minInstances raise) still
  leaves the suite 52/52. H1c is a `lives_ok` (asserts submit *succeeds*), and `HC0N5`'s only occurrence
  in `supabase/tests/` is inside H1c's **description string**, never a `throws_ok`. No live hole — E2E
  FF1-4/FF1-5 carry the blocking direction — but **the fast gate cannot see a submit-side group
  blocking regression.** Fix: one `throws_ok(… 'HC0N5')` at the H4a state.
- [ ] **MINOR-2 — the suite header documents a keystone that does not exist.** *"MUTATION F5: drop the
  re-pack UPDATE from `remove_group_instance` → F5 red"* — there is no F5, and `remove_group_instance`
  is never called in `supabase/tests/`. E2E covers it (FF1-9 removes position 0 of 2, exercising the
  re-pack). Fix: write F5, or delete the claim — a documented mutation for a non-existent keystone is
  worse than no note.
- [ ] **MINOR-3 — MUTATION F3 names the wrong mutation.** Removing `set constraints … deferred` leaves
  the suite green; the real guard is BE-1's `DEFERRABLE` alter (proven both directions in r1:
  non-deferrable ⇒ the naive swap raises `23505`). F3 *is* a keystone — for the alter, not the
  in-function statement. Fix: correct the header.
- [ ] **MINOR-4 — stale-comment asymmetry in `supersede_response`.** The *"…while repeating groups are
  inert"* comment is gone from `start_correction_draft` but survives in `supersede_response`, so two
  sibling functions now carry **contradictory premises about the same join**. Zero functional effect
  (the corrected mechanism is spelled out directly below it). `qa`'s ruling: **do not mint a migration
  whose entire content is a comment rewrite** — that adds another body-rewrite to the chain CLAUDE.md
  §graphify already treats as a hazard. **Let it ride the next migration touching those bodies** — which
  INFO-6 guarantees is coming.
- [ ] **INFO-2 — no coherence guard on the direct-DML path.** Nothing ties
  `response_group_instances.group_item_id` to a `repeating_group` of the response's own `form_version`
  (the FK is only `→ form_items(id)`). The RPC path is fully gated (`assert_group_writable`, `HC0N4`);
  ruling 5 deliberately leaves direct DML open, so the exposure is a user writing junk into **their own**
  draft — no cross-tenant read, no PHI. Belongs with **FUP-FF1-1**.
- [ ] **INFO-3 —** `src/lib/responses/actions.ts:384-390`: the `p_instance_answers` comment block is
  duplicated verbatim.
- [ ] **INFO-4 — the parity vectors have no drift detector.** Hand-mirrored SQL ↔ JSON, byte-equal today
  (21/21), matching the pre-existing `condition-vectors.json` convention — so not an FF-1 regression. But
  **Rule 3 calls evaluator drift phase-blocking and nothing detects it.** Worth a real detector before
  FF-3, which adds a second evaluator pair.
- [x] **INFO-1 — superseded by MINOR-4** (r2: the stale comment was removed from
  `start_correction_draft`; only `supersede_response` retains it).
- [x] **INFO-5 — DISCHARGED at the Record step 2026-07-27**: `20260828000900_enable_repeating_groups.sql`
  written and applied (flag verified `enabled = true`, 198 files = 198 registered), and
  `docs/backend-state.md`'s high-water corrected (**`HC0M9`**, not `HC098`; FF-1 allocated `HC0N0`–`HC0N5`).
- [x] **INFO-6 — CARRIED FORWARD as a binding FF-2/FF-5 requirement**, not left as a note →
  [flexible-forms-program.md](../../docs/plans/flexible-forms-program.md) §3 FF-2/FF-5, with named keystones
  (`correction_copies_matrix_answers` / `correction_copies_reference_answers`).

