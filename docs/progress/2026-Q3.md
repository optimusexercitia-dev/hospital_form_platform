# Concluded § Now rotations — 2026 Q3

Registered quarterly destination (ADR
[0139](../decisions/0139-quarterly-home-for-concluded-now-rotations.md), amends ADR 0124)
for **concluded § Now bullets and narrative fragments** rotated **verbatim** out of
[PROGRESS.md](../../PROGRESS.md). The file is chosen by the **rotation date** (the day the
cut is made), not the date the work concluded. Follow-ups and bugs never land here —
resolved index lines → [follow-ups-archive.md](follow-ups-archive.md), closed bugs →
[bug-log-archive.md](bug-log-archive.md), completed phase rows →
[phase-ledger.md](phase-ledger.md). Mechanics: `docs/lead-playbook.md` §5; link-checked by
`lint:progress`, which discovers quarterly archives by the `<YYYY>-Q<n>.md` pattern — no
script edit per quarter.

Predecessor: the Q3 rotations of 2026-08-21 → 2026-08-24 live in
[now-concluded-2026-08.md](now-concluded-2026-08.md), **frozen** — inbound references by
name and by line number make a rename or merge lossy. August 2026 splits between that
file and this one by decision, not accident.

---

*(No rotations yet — the first § Now cut after 2026-08-24 lands here.)*

## Rotated 2026-08-24 — ADR 0136 (deferred `staff_admin` sign-off), at the Record step

_Verbatim from PROGRESS.md § Now; links repointed for this file's depth (the standing trap — a verbatim rotation 404s its own links, and `lint:progress` catches it). The live pointer that replaced it names the ledger row and the one fact that outlives the cut: the flip migration is what changes production, and only when pushed._

- **✅ ADR 0136 — deferred `staff_admin` sign-off: BUILT + COMMITTED `1069711c` 2026-08-24,
  ⛔ NOT PUSHED (`main` is ahead of `origin/main` by 2 — measure, never quote), awaiting §6
  step 3 (QA) + step 4 (human approval).** Migration `20261003001900_deferred_staff_signoff.sql`
  (20 changes, every body re-emitted from `pg_get_functiondef`), pgTAP **367** (**79** assertions
  after the follow-up round below), E2E `deferred-staff-signoff.spec.ts` (**6**), plan
  [deferred-staff-admin-signoff.md](../plans/deferred-staff-admin-signoff.md), ADR
  [0136 § Amendment 1](../decisions/0136-deferred-staff-admin-signoff-attests-frozen-content.md).
  Ships behind `deferred_staff_signoff`, **OFF in production** — seed forces it on for local/E2E, and
  ⛔ **the production flip is a separate migration, not written**, so local + E2E green says nothing
  about production behaviour yet.
  ⛔ **What a reader must not skip — the § Size table and the Consequences were wrong in EIGHT places,
  every one in the reassuring direction. The figures live in ADR 0136 § Amendment 1, never here.** The
  four that changed the build:
  1. **A third signing door the ADR never names** — `public.sign_section` (INVOKER) carries its own
     `in_progress` gate. Widening only the two named leaves every deferred signature refused with
     every policy/trigger assertion green.
  2. **D7's ruled decline path did not exist** — `file_correction_request` admitted `completed` only,
     so a declined phase would be stuck forever behind the widened `close_case` gate. A deadlock, not
     the cost D7 accepted.
  3. **D5 moved naively lands HC061 on the coordinator** for something only the filler can fix and no
     longer can. The COMPUTATION moved; the PRECONDITION stayed on the submit.
  4. **The DB was never the whole gate** — the wizard's own submit button stayed `disabled`. The
     feature was unreachable in the product with pgTAP fully green; only E2E saw it.
  ⭐ A live defect was found by the pre-build measurement and is FIXED here —
  **BUG-SIGNOFF-GROUPCOND-001** (§ Bug Log).
  ⚠ **Gate state 2026-08-24, re-run after the follow-up round (`d899ceb3` + `20261003002000`):**
  step 1 ✅ · step 2 ✅ **by COMPOSITION, not by one run** · step 3 ✅ **APPROVED**
  ([review](../reviews/adr-0136-deferred-signoff-review.md)) · step 4 ⛔ **NOT GIVEN** — human approval
  is the only thing left before the Record step.
  ⛔ **Read § Test Run Summary, not this line, for what step 2 actually is.** The FULL `e2e:prod` exited
  **5 (RED, nothing proven)**: zero assertion failures anywhere, but **33 tests never ran** when the
  server died in batches 5 and 13. A targeted re-run of exactly those 10 specs was GREEN. Every
  collected test therefore has a passing verdict, but **not from a single run**, which is what §6 step 2
  asks for. ⚠ Batches 4 and 18 — which hold `deferred-staff-signoff` and `phase6-signoffs` — were
  clean and fully accounted, so the RED does not overlap this ADR's surface.
  ✅ **The earlier shared-stack QUALIFICATION is DISCHARGED for these figures** — the stack was measured
  quiet (0 non-infra connections) before this run, which is the discharge ADR 0136 § Amdt 1 named.
  ⭐ **FOLLOW-UP ROUND, 2026-08-24 — all five ADR-0136 follow-ups RESOLVED, ⛔ UNCOMMITTED on top of
  `1069711c`** (index lines rotated below; each body in `follow-ups.md` carries its evidence).
  Three things a reader must not skip, because none was in the follow-ups as filed:
  1. **A DATABASE defect fell out of the route fix** — `start_or_resume_response`'s resume query
     lacked the `case_phase_id is null` conjunct its own unique index carries, so "Preencher" handed
     back the caller's CASE-PHASE draft. Migration `20261003002000`; the state is **not in
     `seed.sql`**, so it was CONSTRUCTED and pinned red-first (pgTAP `367` §15).
  2. **The stale invoker verdict was not merely stale — the honest re-run said BLIND**, and the
     guard CLASS had moved g1→g3. Closed by `367` §14, whose FIRST version did not close it: keyed
     on SQLSTATE `23514` it stayed green, because the INSERT trigger shares that exact code. Pinned
     to the wrapper's MESSAGE instead → COVERED.
  3. **The door sweep's arm was widened by PROPERTY** (ADR [0079](../decisions/0079-authz-door-blindness-standing-invariant.md)
     **Amdt 9**; domain 102→110). It found what it was widened to find: 6 COVERED · **1 BLIND** ·
     1 ERROR → two NEW follow-ups below. ⛔ `FUP-RCA-WRITER-CAN-WRITE-IS-BLIND` blocks a phase.
  ⛔ **Same shared-stack caveat applies to THIS round's figures** — ~8 `db reset`s were issued on a
  stack ADR 0136 § Amdt 1 records as shared by three sessions. And it took THREE sweep runs to get
  trustworthy verdicts: **a green baseline is not evidence the DB is fit to mutate** (ADR 0079
  Amdt 9 records the mechanism as UNMEASURED — stale state and a concurrent session fit equally).

> ⚠ **Editorial note, added 2026-08-24 — deliberately OUTSIDE the verbatim block above.** That
> block's line *"⛔ `FUP-RCA-WRITER-CAN-WRITE-IS-BLIND` blocks a phase"* was true when rotated and
> is **no longer**: the gate was keystoned (`142_rca.sql` §K) and re-swept **COVERED** the same day
> — closure + evidence in [follow-ups-archive.md](follow-ups-archive.md). ⛔ Its sibling
> `FUP-DOOR-SWEEP-BROAD-GATE-ABORTS-A-FILE` **is still open**, so the block's "two NEW follow-ups"
> have diverged and neither status may be read off it. Rotated text is frozen by contract, so the
> correction is annotated beside it rather than edited into it — and status is read from
> [PROGRESS.md](../../PROGRESS.md) § Follow-ups, never from an archive.

## Rotated 2026-08-24 — the SUPERSEDED § State remote row (`20261003001800`)

_A **§ State** row, not a § Now bullet — rotated here because that row carries its own cut
instruction (**"Superseded by the next remote-affecting change"**) and the ADR 0136 push is that
change. Verbatim apart from link repointing. ⛔ **Nothing in it may be quoted as a live fact**; the
live row is in [PROGRESS.md](../../PROGRESS.md) § State and even that one must be re-measured._

| live fact (SUPERSEDED 2026-08-24 by the ADR 0136 push) |
| --- |
| ✅ **REMOTE IS CURRENT — `20261003001800` PUSHED 2026-08-24 (code FIRST, then schema).** ⭐ **The order was REVERSED for this one migration, deliberately, and the reason generalises:** it REMOVES a key the old build reads, so deploying new code first closes the window entirely — the batch's schema-first rule is about ADDING, and it does not survive a removal. ⭐ **Verified in the REMOTE CATALOG, not from `db push`'s report:** `get_case_detail` mentions `patient_enabled` **0** times while still emitting `patient_mode` AND `patient_required_fields` (both asserted — a body that lost all three would satisfy the first check alone). `schema_migrations` = **450**, head **`20261003001800`**; `migration list --linked` showed **1** pending and **0** drifted before the push. ⚠ Data re-measured: `auth.users` = **36**, **0** non-`@test.local` — still the E2E seed fixture. ⛔ Coolify auto-deployed on the `git push`; **that deploy's outcome is again NOT recorded here** — check Coolify. Earlier record of the batch push: ✅ **the ADR 0137 batch SUPERSEDED the AFF2 row here, which is what that row said would happen.** Measured ON THE REMOTE 2026-08-24, after `db:push`: `schema_migrations` = **449**, head **`20261003001700`**; `origin/main..main` was **0** when measured. ⛔ **The head sha this row used to quote (`ec1271a8`) was stale within the day** — HEAD moved to `2b69f19f`, the Record commit that wrote the row, which is the off-by-one-by-construction that [rule `live-facts-measure-dont-quote`](../../.claude/rules/live-facts-measure-dont-quote.md) now stands against. Measure the sha; never read one here. ⭐ **The 449 was MEASURED, not computed** — “444 + 5” would have been right today and is the habit that makes it wrong the day a migration lands from elsewhere. ⭐ **Verified in the remote CATALOG, not from the push output:** `cases` + `process_template_versions` carry `patient_mode` and `patient_required_fields`; `collects_patient` / `patient_enabled` are **absent**; `public.set_referral_patient` contains the draft-only arm (ADR 0137 Amdt 1). ⚠ **Data, re-measured the same day:** `auth.users` = **36**, **0** non-`@test.local`, `cases` = **14** — still the E2E seed fixture and no real customer data, so the row below it stands. ⚠ **`patient_mode='required'` versions on the remote: 0** — the compliance mode is live but nobody has turned it on. ⛔ **Schema went BEFORE code, deliberately — but NOT because old-code/new-schema is safe** (it is not: ADR 0137 Amdt 2), rather because the reverse breaks sooner and wider. ⛔ Superseded by the next remote-affecting change — **re-measure, do not quote** |

## Rotated 2026-08-24 — the ADR 0137 batch § Now bullet, all four items concluded

_Moved verbatim from PROGRESS.md § Now at the point the last open item closed; the five root-relative links are repointed to this directory's depth, which is the only alteration. The batch itself was complete, PO-approved and pushed earlier the same day — this bullet outlived that by carrying **four items that were not yet settled**, which is exactly what § Now is for and why the bullet could not rotate with the ledger row._

⛔ **The bullet's own header line — *"What is still LIVE: item 4 alone"* — was true when written and is FALSE as archived.** Item 4 closed a few hours later, in the same session, and the header was not rewritten because a verbatim rotation does not get to tidy its subject. ⚠ Read item 4's body, which carries its own ✅ CLOSED stamp; the header is a snapshot of an earlier moment, not a claim about this file.

⭐ **What closed them, for the record:** item 1 by a **human observation** (the PO checked the Coolify UI — nothing in this repo can read deploy status, so no gate could ever have closed it); item 2 by a **PO ruling** plus migration `20261003001800`; item 3 by **measurement** (`origin/main..main` = 0); item 4 by the **full `e2e:prod` GATE GREEN at `77b0a467`** — 1227p / 0f / 0 infra / 3 flaky / **0 did-not-run**, 21 batches, reconciled per batch. That run also discharged [ADR 0136 § Amendment 1](../decisions/0136-deferred-staff-admin-signoff-attests-frozen-content.md)'s prescribed remedy (one `test:db` + one `e2e:prod` on a stack **confirmed** quiet) — the gate row is in [test-run-archive.md](test-run-archive.md) once superseded, and in PROGRESS.md § Test Run Summary until then.

- **✅ ADR 0137 batch — COMPLETE, PO-APPROVED, PUSHED 2026-08-24** (schema first, then code; both
  halves re-measured on the remote catalog, never accepted on the command's own report). Compacted
  in § Now 2026-08-24 — the completed-run detail (per-increment commits + file inventories, gate
  figures, the QA rounds, the deploy-window measurement) is in the **ledger row 0137** +
  [adr-0137-batch.md](adr-0137-batch.md) · reviews
  [r1](../reviews/adr-0137-batch-review.md) → [r2](../reviews/adr-0137-batch-review-r2.md)
  (`APPROVED`) · ADR [0137](../decisions/0137-mrn-erasure-key-and-case-referral-usability-batch.md)
  **Amdt 1–3**. ⛔ Re-measure remote figures from § State; do not re-derive Amdt 2 here.
  ⛔ **What is still LIVE: item 4 alone.** Items 1–3 are closed, kept here for the record until this
  bullet rotates.
  1. ✅ **The Coolify deploy SUCCEEDED — confirmed by the PO in the Coolify UI, 2026-08-24.** ⛔ This
     is a **human observation, not a gate**: nothing in this repo can read Coolify status, so the line
     is testimony and cannot be re-derived from anything here. It closes the deploy window that the
     *"no active users"* ruling in item 2 only **bounded** — the build is now known good, not merely
     unattended.
  2. ✅ **`FUP-0137-PHI-MODE-SHIMS` is CLOSED 2026-08-24** — migration `20261003001800` drops
     `get_case_detail`'s derived `patient_enabled` key; keystone pgTAP **`366`** (10 tests,
     RED-PROVEN, restore verified byte-identical). ⛔ **The deploy gate was discharged by PO RULING,
     not by a check** — nothing in this repo can read Coolify status, and the ruling was *"no active
     users"*, which bounds the window rather than confirming the build. ⛔ **This migration is
     PUSHED 2026-08-24 and verified in the remote catalog** (§ State). ⚠ The gap it surfaced — the MRN floor never reached the
     processless door — was **PO-ruled EXPECTED the same day and closed**, scoping D1–D3 to cases
     minted from a template version: **ADR 0137 Amendment 4**. ⛔ Closed by RULING, not by a code
     change, so re-measuring `create_case` still reproduces the mechanism; read Amdt 4 before
     re-filing it.
  3. **Both halves are PUSHED — code, then schema — and both were re-measured, not accepted on
     report.** ⛔ **Do not read a push count out of this line**: measure it with
     `git rev-list --count origin/main..main` ([rule](../../.claude/rules/live-facts-measure-dont-quote.md)),
     because the Record commit that writes such a figure is itself uncounted by it.
  4. ✅ **CLOSED 2026-08-24 — a full `e2e:prod` gate now covers HEAD: GATE GREEN, exit 0.** Run at
     **`77b0a467`**, which was HEAD **and a clean tree** for the whole run. **1227 passed · 0 failed ·
     0 infra · 3 flaky · 0 did-not-run · 21 batches** (+11 by-design skips). ⭐ **Reconciled per batch,
     not read off the summary:** all 21 batch rows present with no gap, every one `accounted N/N`, and
     the per-batch sums equal the summary exactly — the check that catches a dropped batch flattering
     its denominator. §6 step 2 is now satisfied for what sits on `main`.
     ⭐ **This is a single-run green, and that is the distinction from the 0136 gate**, which was exit 5
     with 33 tests never executed and green only once a human composed a separate targeted re-run into
     it. Here **batch 2 hit `server_dead=1` / `conn_errors=37` (19 failures classified INFRA, 36 did not
     run)** and the harness's own `INFRA_RETRY=1` re-ran it on a fresh server + fresh DB inside the same
     invocation → 67/67, 0 did-not-run. ⛔ **So "0 infra" in the summary is the POST-RETRY state, not a
     clean first pass** — the harness recovered, which is the gate's contract; it is not evidence that
     `FUP-E2E-SERVER-DEAD-1` has stopped happening.
     ⭐ **The 3 flaky tests are NAMED here**, which no prior gate row did — `FUP-E2E-PIN-RECORDS-COUNTS-
     NOT-IDENTITIES` exists because a count cannot be diffed: `act-role-assumption.spec.ts:157` ·
     `action-items-satellites.spec.ts:609` · `phase2-auth-shell.spec.ts:268`.
     ⚠ **Stack measured QUIET first, unlike the 0136 run** — postmaster stable 17 min, all 12 DB
     connections owned by Supabase's own services (realtime / PostgREST / pg_net / pg_cron / storage),
     **no host-side client**, so no leftover Next server to half-apply a reset. Fresh `db reset` exit 0,
     then `test:db` **218 files / 7232 tests / PASS**, 0 `not ok`, **no "planned N but ran 0"** — the
     unattributable symptom of the 0136 window did not recur. That discharges ADR 0136 Amdt 1's own
     prescribed remedy. ⛔ It does **not** retroactively validate the 0136 figures; nothing can.

## Rotated 2026-08-25 — the gate-tooling round and 22-v3 REG·KIND, both concluded

Rotated at the PDF·P3 build, ahead of the Record step, because PROGRESS.md was within
1,100 bytes of the 80 KB cap and `lint:progress` reds the whole team when it goes over.
⛔ Item (3) of the first bullet — nvm still defaulting to Node 20, so `npm run lint` dies
at gate 8 — was NOT rotated: it is a live action with a resolution event and it existed
in no other file. It stays in § Now as its own line.

## Rotated from PROGRESS.md § Now — 2026-08-25

The gate-tooling + follow-up round, concluded 2026-08-24/25. Verbatim; links repointed for this
directory. ⚠ Its two standing cautions were rotated WITH it and are live facts, not history:
`ARM=floor` needs a fresh `supabase db reset` (110 never-called doors stale vs 72 fresh), and
nvm still defaults to Node 20 where `npm run lint` dies at gate 8.

- **✅ Gate-tooling + follow-up round — COMPLETE 2026-08-24/25. Not a phase; maintenance.** Commits
  `06cc2a0b` · `1b78c11a` · this one. ⚠ **A FIFTH, `FUP-RCA-WRITER-CAN-WRITE-IS-BLIND`, was closed
  IN PARALLEL on `origin/main` (`77b0a467`) by another session and is NOT this round's** — both sessions
  keystoned the same door on the same day; theirs (`142_rca.sql` §K, four principals, re-swept COVERED)
  was kept and this round's duplicate in `300_rowdoor_gate_keystones.sql` was dropped at the merge.
  ⭐ The collision is the lesson: nothing in the tracker showed the door was already being worked. **Four follow-ups closed**, **three filed**, and — the part that
  matters most — ⛔ **the step-1 authz gate suite was not running on this platform AT ALL**, in two
  independent committed ways: all three `p0-authz-*.sh` hardcoded a foreign Windows scratchpad as `WORK`,
  so every arm read missing files and `ARM=census` printed `INVARIANT HOLDS` **at exit 0 having enumerated
  ZERO gates**; and `act-hat-blind-sweep.sh` could not be PARSED by bash 3.2 (the `/bin/bash` macOS ships),
  so `ARM=hat` could not run. Both fixed, `WORK` writability now probed and fatal; all four arms hold
  (census **546** gates · hat 6/6 self-tests · wrapper · floor). Record:
  `FUP-AUTHZ-GATE-SUITE-DID-NOT-RUN-ON-MACOS` in [follow-ups-archive.md](follow-ups-archive.md).
  ⭐ **`FUP-RESET-ROLE-DOES-NOT-CLEAR-JWT-CLAIMS` was closed by EXECUTION, not by reading** — its own text
  said no text filter could decide the population, so a claims-clear was applied to all **2171**
  `reset role;` sites (172 files) and the suite diffed against an all-green baseline: **SIX** false
  premises, not the 136 its headline implied, all six *misplaced resets* rather than the missing
  claims-clear the item assumed. ⛔ Two standing cautions came out of this round and have no other live
  line: **(1)** `ARM=floor`, and ANY claim that a function is absent from the catalog, require a **fresh
  `supabase db reset`** — measured the same day, floor reads **110** never-called doors on a stale DB and
  **72** on a fresh one; **(2)** on exactly that hazard a **false "rename" of a live PHI helper was
  recorded here and in the authz backlog, and is corrected** — `app._set_participant_patient_unchecked`
  was never renamed. **(3)** ⚠ nvm's default is still Node 20, and `npm run lint` dies at gate 8 there
  (`globSync` needs 22): `.nvmrc` + `engines` are set, `nvm alias default 22` is not.


## Rotated from PROGRESS.md § Now — 2026-08-25 (2)

22-v3 REG·KIND, closed 2026-08-25; its phase row is in `phase-ledger.md`. Verbatim, links
repointed. ⭐ Kept for its lesson, which is about a SHAPE and not this phase: a phase row states
its own gates, so evidence that discharges one *elsewhere and later* never reaches the row — this
one under-reported itself for 13 days and no gate can catch that direction.

- **✅ 22-v3 REG·KIND — CLOSED 2026-08-25, all five gate steps; row rotated → [phase-ledger.md](phase-ledger.md).**
  It had sat here since 2026-08-12 as the ONLY non-complete phase row, reading `gates 2–4 UNRUN`.
  ⛔ **Two of the three were already discharged and nobody had looked** — gate 3 by the 2026-08-12
  FUP-batch QA review (APPROVED r1; REG·KIND was reviewed inside it, under a heading this row never
  cited), gate 2 by the 2026-08-24 full `e2e:prod` GREEN at `77b0a467`, which **contains** the merge
  `9a20c8a` and runs an unfiltered `e2e/`. Only the human ✓ was genuinely missing. ⭐ **The lesson is
  the gap's shape, not its size:** a row states its own gates, so evidence that arrives *elsewhere and
  later* never reaches it — this one under-reported itself for 13 days, and no gate can catch that
  direction. ⚠ Neither discharge is a dedicated pass (E2E **inherited**, QA **catalog/schema-scoped**);
  both caveats are in the ledger cell and were stated before approval. Residue closed in the same
  commit: 3 dead mutation cases in `p0b-isolation-mutation-audit.sh` firing at objects REG·KIND
  DELETED, and `docs/backend-state.md` still calling its migrations the unpushed drift.

## Rotated from PROGRESS.md § Now 2026-08-25 — the concluded-programs bullet's tail

⛔ Trimmed from § Now for cap headroom. The 22-v3 half is already stated twice in this file; the
**0137 re-derivability** half was in NO other file, which is why the sentence moved instead of
being deleted — measured before cutting, not assumed.

The archived bullets carry the rest — including why 0137's four items are not equally
  re-derivable (four different KINDS of evidence, only one reproducible from this repo), and 22-v3's
  reusable lesson that **a phase row states its own gates, so evidence arriving elsewhere and later
  never reaches it** — it under-reported itself for 13 days and no gate can catch that direction.

## PDF·P3 (Printing Cases) — CONCLUDED, rotated from § Now 2026-08-25

⛔ **Historical.** All five gate steps closed 2026-08-25; ledger row **PDF·P3**. Links repointed
for this directory. ⚠ Gate figures here were true when written — re-measure, never quote.
⭐ The open residue stayed in PROGRESS.md by name: the five N-findings and the P3 follow-ups are
in § Follow-ups, and `BUG-CASEEVT-KIND-001` stays in § Bug Log.

- **🟡 PDF·P3 (Printing Cases) — BUILT 2026-08-25; gate 1 ✅ COMPLETE, gate 2 ⛔ RED (UNRUN) — re-run pending.**
  ADR [0144](../decisions/0144-case-printing-dossier-lock-and-phi-fork.md) **+ Amendments 1–4**,
  plan [case-printing-p3.md](../plans/case-printing-p3.md), measured substrate
  [case-printing-p3-substrate.md](../plans/case-printing-p3-substrate.md) — **read the substrate
  brief before touching P3.** Commits + amendments detail + the scoped-absence bound:
  [pdf-p3.md](pdf-p3.md). ⛔ **NOT pushed.**
  **✅ Gate 1 COMPLETE (re-measured on the final tree) · ✅ Gate 2 GREEN 2026-08-25 at `6394b95a` ·
  ✅ Gate 3 APPROVED — QA pass 2, 2026-08-25 · Gate 4 NOT GIVEN · Gate 5 not started.** 7 migrations.
  Gate evidence, pass 1's three (now closed) blocking candidates, and the amendments lesson:
  [pdf-p3.md](pdf-p3.md). ⛔ **Historical there: re-measure, never quote.**
  **🔨 2026-08-25 — all five QA findings CLOSED in code**, each mutation-proven: the catalog
  reconciliation (gate 1's authz greens **STAND** — 27 = 15 new + 12 body-only, policies 0/0,
  `A  B` read from the catalog, not the diff) · **C-1** the Art. 18 hole (ADR 0144 **Amdt 5**) ·
  **C-2** the Gotenberg beacon (ADR **0145**, amends 0014) · **C-3** all four D14 cells across both
  layers · **M-2** · **M-3** made causal. ⭐ **Four more were found by BUILDING** — all four, plus
  **Amdt 6**, in [pdf-p3.md](pdf-p3.md) § "Found by building".
  **Left: gate 4 (PO approval) → gate 5 Record.** QA's N-1…N-5 are non-blocking.
  ⭐ **RESUME → [pdf-p3-handoff.md](pdf-p3-handoff.md)** · verdict
  [phase-p3-review.md](../reviews/phase-p3-review.md) § Pass 2.
  ⭐ **PO RULINGS 2026-08-25.** ⚠ **Scope, written down because an approval's bounds are what never
  get recorded:** accepts **gate 2 at RED (UNRUN)** with its cause on record and authorises **step 3
  only** — plus, later the same day, the agent team, the **C-1 constitutive ruling**, the print
  placeholder, and fixing **the gate harness's own defects**. ⛔ NOT phase approval (step 4), NOT a
  push, NOT authority to change what the gate MEASURES.

## Rotated from PROGRESS.md § State 2026-08-25 — the SUPERSEDED § State remote row

⛔ Verbatim; links repointed for this directory. Superseded by the 2026-08-25 push of the
PDF·P3 + user-profile merge (`db:push` then `git push`), which the row's own closing
instruction anticipated: *re-measure, do not quote*. It carries the node-24 Coolify
testimony and the stale-sha history behind `live-facts-measure-dont-quote`.

| ✅ **REMOTE IS CURRENT — the ADR 0136 batch PUSHED 2026-08-24, and `deferred_staff_signoff` is ON in production.** ⭐ **Measured in the REMOTE CATALOG after the push, never from `db push`'s report:** `schema_migrations` = **453**, head **`20261003002100`**; the versions above `20261003001800` are exactly **`20261003001900`** (feature) · **`20261003002000`** (standalone-lane fix) · **`20261003002100`** (flag ON). ⭐ **The flag itself was read, because it is the fact that decides whether the feature is live and a migration list cannot answer it:** `app.feature_flags` for `deferred_staff_signoff` = **enabled** — which retires the standing "the remote runs the flag OFF whatever any document says" caveat. Surface verified beside it: `case_phases` carries **1** constraint mentioning `awaiting_signoff`, and **12** routines mention it. ⚠ **Data, re-measured the same day:** `auth.users` = **36**, **0** non-`@test.local` — still the E2E seed fixture, no real customer data, so the row above stands. ⛔ **Coolify auto-deploys on the `git push` and its outcome is NOT measured here** — check Coolify, never this row. ✅ **PO-confirmed 2026-08-25: the first build on `node:24-alpine` SUCCEEDED**, so production now runs the node 24 toolchain (`.nvmrc` / Dockerfile / `engines` all moved together, `82b067d6`; locally the image served `/login` 200 and `/` 307 through a real Supabase session round-trip). ⛔ That is **testimony, not a measurement** — no gate can read Coolify, which is why this class of fact has now needed a human three times. ⛔ Superseded by the next remote-affecting change — **re-measure, do not quote.** The superseded `20261003001800` row was rotated verbatim → [2026-Q3.md](2026-Q3.md) § "Rotated 2026-08-24 — the SUPERSEDED § State remote row"; it carries the code-before-schema reversal rationale and the stale-sha history that [rule `live-facts-measure-dont-quote`](../../.claude/rules/live-facts-measure-dont-quote.md) came from. |

## Rotated 2026-08-26 — AFF4 Track P (the pre-step), concluded

_Cut from PROGRESS.md § Now at the AFF4 build go. The pre-step is CLOSED; P4's flake
baseline and the FUP/sequencing lines stayed live in § Now because the AFF4 gate still
consumes them. Verbatim, links repointed for this directory._

  ✅ **PRE-STEP (Track P) COMPLETE 2026-08-26 and COMMITTED on `main`** — this line said
  "uncommitted" for one commit round after it no longer was; the branch forked from the committed
  pre-step. **P1** did NOT fix: its premise was refuted by measurement
  (PostgREST maps SQLSTATE class `P0*`→500; the `text/plain` body, the "every raise" claim and the
  §8/app-facing claim are all false — the app maps on `error.code`). PO-ruled *diagnose + re-file*:
  ADR [0152](../decisions/0152-postgrest-p-class-sqlstate-maps-to-500.md) amends 0151 D16a, and the
  real defect is a **73-door class** now carrying its own index line below. **P2** turned the recipe
  into `scripts/door-sweep-cases.sh` (all three Amdt 8 rulings, zero-row now **reds**). **P3** fixed
  the baseline truncation in **four** sweeps, not one, proven by a differential whose *first control
  was vacuous* — ADR [0153](../decisions/0153-subset-sweeps-write-to-scratch-not-the-committed-baseline.md).

## Rotated from PROGRESS.md § Now 2026-08-26 — the AFF4 build bullet, at the AFF4 Record step

⛔ **Verbatim** (links repointed for this directory — `](docs/progress/` → `](`, `](docs/X/` →
`](../X/`). Keyed to the **rotation date**, not the conclusion date (ADR 0139). The bullet is
concluded: AFF4 passed the §6 gate and human approval on 2026-08-26. Its live successor is a
one-liner in § Now pointing at [phase-ledger.md](phase-ledger.md) and [aff4.md](aff4.md).

⚠ **Three claims in the bullet below were already FALSE when it was cut, and each is corrected at
the destination rather than in the rotated text** (the text is verbatim, so it keeps its errors):

1. *"Eleven FUPs discharge at the AFF4 Record step"* — **TEN discharged.** ADR 0151 §
   Consequences over-forecast; `FUP-AFF2-ACTIVE-MEANS-TWO-THINGS` was held OPEN by PO ruling at the
   Record step. ⚠ And that override is itself **contested** — the QA review marks the item DONE, and
   the PO's measurement (both `profiles` SELECT policies return `f` for `expires_at` filtering) is
   *also exactly what D6 ruled should be true*. See that item's live index line for both sides.
2. *"REMAINS: `BUG-D5-REHIRE-HOSPADMIN-001` (an AFF4 regression, fix in flight)"* — **fixed** in
   `89793d43` (ADR 0159, migration `20261003004300`, pgTAP `381`); the bug row rotated to
   [bug-log-archive.md](bug-log-archive.md) in the same Record edit. The `qa` re-pass returned
   **APPROVED** and the §6 gate ran **GREEN, exit 0**.
3. *"T2–T5"* — the track is **T1–T6**. T1 (the suspend-spec repair) had already landed and T6 (the
   DatePicker locator sweep) is absent from the bullet entirely, so the line under-reports the
   track at **both** ends.

⭐ **The one thing in the bullet that was NOT superseded and is preserved live** is the
twelve-instruments finding — promoted to [aff4.md](aff4.md) § "The lesson this build is worth
remembering for", with the two constant-returning instruments named and the rule stated: *every
counting instrument in a gate report must be run once against a KNOWN FAILURE before its zero is
believed.*

↩ **The bullet, VERBATIM:**

- **🔨 AFF4 BUILD IN PROGRESS — the PO's go was given 2026-08-26; branch `feat/aff4-org-affiliation`
  has forked from `main`.** Org affiliations + per-hospital staff data + the **voided tense** (closes C5): ADR
  [0151](../decisions/0151-aff4-organization-affiliation-staff-data-voided-tense.md) · plan
  [aff4-org-affiliation.md](../plans/aff4-org-affiliation.md) · analysis
  [org-affiliation-and-staff-data-model.md](../plans/org-affiliation-and-staff-data-model.md).
  ⛔ **The plan is AUTHORITATIVE and carries the full build record — the pre-step narrative, the
  ruled PO decisions, the discovered follow-ups. This line carries only what a *status* reader
  needs.** ADRs shipped: 0152 · 0153 · 0156 · 0157.
  **Eleven FUPs discharge at the AFF4 Record step** (ADR 0151 Consequences); their ⭕ SCHEDULED
  index lines **stay put until the work lands**. **AFF4 jumps the ▶ queue below.**
  **DONE:** Track P · B1–B9 · F0–F6 · the F0-regression repair · T2–T5 · the QA-round fixes; four
  authz arms hold. **REMAINS:** `BUG-D5-REHIRE-HOSPADMIN-001` (an AFF4 regression, fix in flight),
  a `qa` re-pass, then the §6 gate — whose `e2e:prod` is the **FIRST** run covering `61e23659`.
  ⛔ **TWELVE instruments in this build reported success while measuring nothing**, two of them
  **constants** — the dangerous kind, because they are *usually right*. No conclusion was wrong; the
  confidence was. **Read the plan before quoting any gate figure from this build.**

↩ **And the § Now line that pointed at it, VERBATIM** — superseded in the same edit, because AFF4
is no longer building:

- **⚠ A PHASE *IS* ACTIVE — AFF4, see its bullet below.** ⛔ This line read "NO PHASE IS ACTIVE" for a **THIRD** time (corrected 2026-08-26, twelve lines above a bullet saying the opposite). **ADR 0144 (PDF·P3)** and the **user-profile redesign + AFF3/AUD1/AUD2** batch (ADRs 0147–0150) are the two most recent COMPLETE programs. 0136 and 0137

## Rotated from PROGRESS.md § Now 2026-08-26 — AFF4 residue (1), DISCHARGED: the remote push and its order violation

_Rotated at the AE0 branch cut (ADR 0155 / `docs/plans/authz-evolution.md`). Residue (1) had
been marked ✅ DISCHARGED and was concluded content in a live-state-only section. The
standing rule it carries — **push schema (`db push`) BEFORE code (`git push`)**, because
Coolify auto-deploys on the git push — stays live in PROGRESS.md § Now as a one-line pointer
and in `docs/plans/authz-evolution.md` rule 8. Verbatim, no links to repoint._

  **(1)** ✅ **DISCHARGED 2026-08-26 — merged, pushed, branch deleted, and all 12 migrations applied
  remotely** (`db push` exit 0; verified on the remote: `organization_affiliations` = **35 rows**,
  matching `profiles.home_organization_id` = 35, so the backfill that matches **zero** rows locally
  did its real work there; backstop `prosecdef` = **true**; 5 doors present).
  ⛔ **BUT THE DOCUMENTED ORDER WAS VIOLATED AND IT MUST BE RECORDED:** this residue said
  *"`coolify.md` documents auto-deploy on `git push`, so the order is **schema first, then code**"* —
  and the lead pushed **code first**, leaving a window in which deployed code queried a table the
  remote did not have. ⭐ **The residue named the hazard, was read, and was violated anyway**, because
  it was filed under a heading about an unmerged branch rather than beside the push. A warning is
  only as good as its position relative to the action it governs;

## Rotated from PROGRESS.md § Now 2026-08-26 — AFF4 residues (1) and (3), at the AE1 rotation

_Rotated during AE1 (ADR 0155). Residue (1) was marked ✅ DISCHARGED; its one live half — **push
SCHEMA before CODE** — became `.claude/rules/push-schema-before-code.md`, path-scoped to
`supabase/migrations/**` so it fires beside the action it governs, which is exactly what the
residue itself argued for. Residue (3) is a lesson about a build that closed. ⛔ Residue (2)
did NOT rotate — unfiled FUP obligations are live open work._

  **(1)** ✅ **DISCHARGED — merged, pushed, branch deleted, 12 migrations applied remotely 2026-08-26.**
  ⛔ **The one thing that stays live is the standing rule it broke: push SCHEMA (`db push`) BEFORE
  CODE (`git push`)** — Coolify auto-deploys on the git push, and the lead pushed code first,
  leaving a window where deployed code queried a table the remote did not have. ⭐ The warning
  existed, was read, and was violated anyway **because it sat under a heading about an unmerged
  branch instead of beside the push**. Full record rotated verbatim →
  [2026-Q3.md](2026-Q3.md) § "AFF4 residue (1)";

  **(3)** ⛔ **TWELVE instruments in this build reported success while measuring nothing**, two of
  them **constants** — the dangerous kind, because they are *usually right*. No conclusion was wrong;
  the confidence was. **Read the plan before quoting any gate figure from this build**, and never
  believe a counting instrument's zero until it has been run once against a known failure.

## § Now bullets concluded at AE1's Record step (rotated 2026-08-27)

> Moved **verbatim** from `PROGRESS.md § Now`; only relative links were repointed for this
> directory — the rotation rule's one permitted edit. AE1 completing is what concluded the
> AFF4 and ADR-0155 narratives these bullets carried.

- **✅ Six concluded programs — fully rotated** → [2026-Q3.md](2026-Q3.md) +
  [phase-ledger.md](phase-ledger.md). ⛔ **Only the two LIVE residues stay here:**
  **(1)** ⭕ **ROTATED 2026-08-27 → `.claude/rules/authz-gate-results-need-a-current-baseline.md`**
  (path-scoped, gate-verified; it also now carries AE0's trusted baseline).
  **(2)** ⭕ **CONVERTED 2026-08-27 to `FUP-QA-FINDINGS-N3-N4-UNACCOUNTED`** (indexed below) — it
  was an actionable item wearing a narrative, which the contract's three-way test says is a body
  plus one index line. ⛔ N-3 and N-4 still have **ZERO hits**; the recovery is owed, not done.
- **⚠ AE1 IS BUILDING (AE0 closed 2026-08-26; see the ADR 0155 bullet) — and
  AFF4 IS MERGED, PUSHED, AND ITS SCHEMA IS APPLIED REMOTELY**
  (`main` @ `e8abaeec`; branch deleted; 12 migrations pushed 2026-08-26). ⛔ **Do not compress the
  clauses into one; the second is what this line has destroyed four times, and "complete" has meant
  four different things here — built / merged / pushed / schema-applied.** ⛔ **Re-measure, never
  read**: `git branch --merged main` and `supabase migration list --linked`. **AFF4** is the most
  recent complete program; before it, **ADR 0144 (PDF·P3)** and the **user-profile redesign +
  AFF3/AUD1/AUD2** batch (ADRs 0147–0150). 0136 and 0137 are both complete. ⛔ This line's own
  history — it read "NO PHASE IS ACTIVE" **three times while phases ran**, and the correction that
  replaced it is rotated **verbatim** → [now-concluded-2026-08.md](now-concluded-2026-08.md)
  and [2026-Q3.md](2026-Q3.md). ⛔ **Do not add a fifth warning here.** The durable
  fix is not another warning: **re-measure** — `git branch --merged main` and § Phase Status — rather
  than reading this line.
  ⛔ **One thing must not be read as closure, and it has no other live line:** the ethics lane is
  *non-erasable by decision with two known open removal paths* — a worse state than "no path exists".
- **✅ AFF4 COMPLETE — §6 gate GREEN, QA APPROVED, human-approved 2026-08-26; Critical FUP C5 is
  closed and has left § Critical FUP.** Row → [phase-ledger.md](phase-ledger.md) ·
  detail → [aff4.md](aff4.md) · authority is still the
  [plan](../plans/aff4-org-affiliation.md). Concluded narrative rotated →
  [2026-Q3.md](2026-Q3.md). ⛔ **THE THREE RESIDUES — only (2) is still live here:**
  **(1)** ✅ **DISCHARGED — merged, pushed, schema applied remotely 2026-08-26.** The standing rule
  it broke — **push SCHEMA before CODE** — is now path-scoped so it fires while you write the
  migration: `.claude/rules/push-schema-before-code.md`. Record → [2026-Q3.md](2026-Q3.md);
  **(2)** ⚠ **~16 QA-review obligations + ~20 plan-discovered follow-ups were NOT converted into
  `FUP-*` index lines at the Record step** — several say in their own words that they need one, so
  they are invisible to the register the PO reads from until they are filed (pointer list:
  [aff4.md](aff4.md) § "Residue this Record step did NOT file");
  **(3)** ⛔ **TWELVE instruments in this build reported success while measuring nothing**, two of them
  **constants** — never believe a counting instrument’s zero until it has been run once against a
  known failure. Record → [2026-Q3.md](2026-Q3.md).
- **📐 ADR 0155 ACCEPTED (as amended) 2026-08-26 — the post-AFF4 authorization-evolution program is
  the plan of record; ▶ **AE0 ✅ COMPLETE + human-approved 2026-08-26** (row → ledger, detail →
  [authz-ae0.md](authz-ae0.md)); **AE1 IS BUILDING** on `authz-ae1-hardening`.**
  Role/permission catalog adopted via
  role-by-role **direct substitution** (`staff_admin` first — reverses the draft's own rejection);
  **pilot gate = implementation Phases 0–4**, Phase 5 (remaining roles) post-pilot; D4 →
  `profile_private_details`, single-shot pre-live; D5 demoted; D6 stays deferred with broadened
  triggers; 0151 D10's Phase 2 promoted to pre-pilot and **finally registered**
  (`FUP-AFF4-HOMEORG-PHASE2`). Eleven PO decisions recorded as G1–G11 in the ADR — the approval's
  scope is written there, not remembered. Authority: ADR
  [0155](../decisions/0155-post-aff4-tenancy-and-person-model-evolution-sequence.md) (decisions) +
  the [plan](../plans/authz-evolution.md) (execution — phases **AE0–AE7**, gates, PO decision
  points) + the [audit](../design/authorization-model-evolution-audit-2026-08-26.md) (analysis).
  ⚠ **C1a keeps its queue position (G10)** — Phases 0–1 may run in parallel with the ▶ queue below
  but do not preempt it; re-checked 2026-08-26, C1a still heads the queue and AE0 did not displace it.
  ▶ **AE1 STATUS 2026-08-27 (16 unpushed on `authz-ae1-hardening`; NOT merged, NOT pushed):**
  **AE1.1/.2/.3/.4/.5/.6 all built**; close conditions **#1–#6 ALL DONE** — **#3 closed 2026-08-27**
  by instrument + review (`scripts/authz-tier1-threat-review-ae1.sql` +
  [tier-1 threat review](../design/authz-ae1-tier1-threat-review.md)), scoped by PO ruling, not
  narrowed: ⛔ **Tier 1 is 523, not the sized 432** (the 432 was its DEFINER subset, dropping the 90
  `public` INVOKER fns — the ADR 0079 A7 class); **325/523 decided mechanically, 198 reviewed by
  class**; 4 findings, 2 filed as FUPs (`FUP-DEFINER-EXISTENCE-BEFORE-AUTHORITY` ·
  `FUP-CHILD-ENTITY-MUTATIONS-UNAUDITED`), 4 columns measured to **zero**.
  Gates on that tree, all measured, exit codes read directly: `test:db` **237/7,871 PASS** · `lint`
  **11/11** (gate 11 = `lint:service-role-registry`, added by AE1) · `typecheck` **0** · vitest **145/1,974** · all four ARM arms **0** — ⛔ **domain qualifier, always** (plan rule 2): C2's reachable command doors, **and** the six `service_role`-only person doors, are outside every arm's domain · the 63-case
  diff-scoped sweep re-run and merged (**BLIND 74⇒69, COVERED 296⇒316**; 54 of 63 measured, the 9
  named in the live record) · **`e2e:prod` GATE GREEN** (1249 passed, 0 failed, 3 flaky, 11 skipped;
  accounted 1263/1263 on the final batch lines). ⛔ **Still owed before Record: only the QA review**
  and the Record step itself (⚠ rotate this file — it is over the 82 KB target). ✅ **RV0 + RV3 DONE**
  2026-08-27 at head `…005300`, read-only, **no revoke executed — all 233 stay HELD**: 44
  property-rescued · 5 name-rescued · **23 HOLD** · 161 UNCHANGED = 233 ✓; batches reproduce
  134/43/52/4; GUARD_KEYS 11/11 live; **RV3 = YES**, Postgres re-checks EXECUTE at write time inside
  a stored CHECK (`42501`, not `23514`), proven on both languages with a one-variable differential.
  ⛔ **New, and it binds execution: 137 of the 233 revokes are a SILENT NO-OP as scoped** — they reach
  `authenticated` only via `PUBLIC`, so `revoke … from authenticated` leaves the predicate true and
  **no arm would notice**; lead re-proved it as an effective probe with a positive control
  (`FUP-AE1-REVOKE-SET-EXECUTION`). ✅ **#3's
  review DONE** (2026-08-27). ✅ **R2's HMAC deny test DONE** — and the FUP's *inference* was measured
  **false**: `route.test.ts` DOES notice the gate vanish (7/20 red); the gap was **grain**, no
  assertion at the RPC boundary (correction archived with the closed item). ✅ **AE1.5's AFTER capture
  was already DONE** at head `…004710` — the handoff listed it as owed while quoting its results as
  fact; ⚠ what nobody has ruled is whether it stays representative at `…005300` (3 later migrations,
  one of them 11 policies) — **not owed by any document, a new decision if wanted**.
  ⛔ **AE1's live record — task state, operational facts, fixture traps, and the FUP obligations it
  owes — is [authz-ae1.md](authz-ae1.md); read it before touching this phase.** Two
  AE0 results still bind: this DB has **NO planner statistics** (⛔ never `ANALYZE` before comparing
  against the AE0 baselines; a **cost-only** diff is autovacuum, not a finding), and the service-role
  surface is **45 sites, not 12**. ADR
  [0160](../decisions/0160-ae0-corrections-to-adr-0155-measured-figures.md) corrects two refuted
  0155 figures.

## Rotated from PROGRESS.md § State 2026-08-27 — the SUPERSEDED § State remote row

⛔ Verbatim; links repointed for this directory. Superseded by its own stated exit condition
— *"Superseded by the next remote-affecting change"* — which has fired. The row pins
`schema_migrations` = **463** at head **`20261003003100`**; the ledger's AE0 row already
records a later remote measurement (**475** / **`20261003004300`**), and on-disk today is
**484**. ⛔ Quote none of those three: re-measure. The row is kept because it carries the
node-24 Coolify testimony and the "measured in the REMOTE CATALOG, never from `db push`'s
report" discipline.

| ✅ **REMOTE IS CURRENT — the PDF·P3 + user-profile merge PUSHED 2026-08-25** (`db:push` FIRST per coolify.md's order, then `git push`; `main` == `origin/main` == `7c09e8ce`). ⭐ **Measured in the REMOTE CATALOG, never from `db push`'s report** (2026-08-25): `schema_migrations` = **463**, head **`20261003003100`**; registry closure holds — **463** registered == **463** files. The ten versions above `20261003002100` are P3's `…002200`–`…002800` then the user-profile batch's `…002900`–`…003100`. ⚠ **Data, same measurement:** `auth.users` = **36**, **0** non-`@test.local` — still the E2E seed fixture, no real customer data. `app.feature_flags` enabled = **42**. ✅ Production runs the **node 24** toolchain (PO-confirmed 2026-08-25) — ⛔ testimony, not a measurement; no gate can read Coolify. ⛔ **Coolify auto-deploys on the `git push` and its outcome is NOT measured here** — check Coolify, never this row. ⛔ Superseded by the next remote-affecting change — **re-measure, do not quote.** The superseded 2026-08-24 row was rotated verbatim → [2026-Q3.md](2026-Q3.md) § "Rotated from PROGRESS.md § State 2026-08-25". |

## AE2 — affiliation/person tenancy (rotated from § Now, 2026-08-28)

> Concluded and QA-APPROVED (r3). Rotated verbatim apart from link repointing, per the progress
> contract. The live successor line in § Now carries only what is NOT yet done.

- **📐 ADR 0155 — the authz-evolution program is the plan of record.** ▶ **AE0 ✅ · AE1 ✅ PO-APPROVED + MERGED to `main` 2026-08-27.** ⛔ **AE1's merge is LOCAL ONLY and `main` is UNPUSHED; the AE2 branch IS pushed** (`origin/authz-ae2-affiliation-tenancy`) — **17 migrations across two phases are NOT on the remote DB**, Coolify has not deployed, schema-first is OWED ([rule](../../.claude/rules/push-schema-before-code.md)). ▶ **AE2 IN PROGRESS** ([detail](authz-ae2.md)): **2.0–2.3a, 2.5 ✅** · **2.4 inc 1/3/4 ✅** · **R2-B1 ✅** (ADR [0166](../decisions/0166-governance-role-provisioning-implies-organization-affiliation.md) **+ Amdt 1** — governance-role provisioning implies an org affiliation; the kernel, not the callers) · **ADR [0167](../decisions/0167-commission-staff-admin-has-one-authority-on-both-sides.md) ✅ + Amdt 1/1a** (commission `staff_admin` has ONE authority both sides; a one-way door closed). Gate at head: `test:db` **245f/8224 PASS** · lint 11/11 · tsc 0 · 4 arms **0** (`census` 567/603 · `hat` · `floor` 72 · `wrapper` 41). ⛔ **The arms do NOT see the two kernels** — both return `void`, so census's green is not a verdict about them; the compensating control is a **20+2 mutant audit, all RED-PROVEN**, residual 4/40. ▶ **BOTH OWED PO RULINGS ARE NOW TAKEN (2026-08-28)** — M11 → ADR [0168](../decisions/0168-orphan-recovery-is-its-own-door.md) **+ Amdt 1/2/3**; the `staff` one-way door → ADR 0167 **Amdt 2**. ⛔ **A THIRD tenant-reachable door was then MEASURED, not read** (ADR 0168 Amdt 3): `public.grant_role` (`authenticated`) → `app.grant_role_impl` → `app.ensure_provisioned_org_affiliation` carries a **verbatim copy** of the anchorless-admitting predicate. Live probe on a fresh reset: an **`org_admin` of Rede A accepted a bare orphan uuid and the orphan gained an active org-A affiliation AND an `org_admin` membership**; a commission `staff_admin` was refused 42501, so the exposure is bounded to the `(organization, org_admin)` arm. ⭐ **The census that missed it was bounded by a NAME** (`affiliate_person%`) **and the fourth door does not carry that name** — the bound has to be the *capability* (what writes `organization_affiliations`), which is a catalog question, not a grep. PO-ruled: **split `grant_role` the same way** — `grant_role` (authenticated) → narrowed ensure, `grant_role_for` (service_role) → wide; `assignStaffAdmin` moves to the `_for` twin. ⭐ **ADR 0164's HARD PRE-CONDITION ON THE DROP IS DISCHARGED** — measured 2026-08-28 through the **public wrappers as a signed-in user**, with the same probe that found it open, and with the preconditions asserted in the same transaction so a refusal cannot be a vanished subject: `affiliate_person` (hospital_admin) **HC0R0** · `affiliate_person_to_org` (org_admin) **HC0R0** · `grant_role` (org_admin) **HC0R0** · and the ⭐ **positive control** `recover_orphan_person_to_org` (platform_admin) **ACCEPTED, row written** — three tenant doors shut, orphans NOT stranded. ▶ **REMAINING TO THE DROP** (all PO-scoped into the 2026-08-28 session): **A ✅ ADR 0168's three doors** (`test:db` 246f/8256 PASS, lead-verified) · **B ✅ the `grant_role` split** (247f/8273 PASS, lead-verified) · ADR 0167 Amdt 2 (`staff` sub-arm) — ⛔ **its compensation is the work, not the deletion: uncompensated it WEAKENS clause 1's proof, leaving site (b) with no behavioural witness in the repo** · the `is_admin` demotion backstop · detector logging · the drop increment (⛔ **includes the `seed.sql` rewrite — 6 lines still derive affiliations FROM the column and no AE2 commit has touched that file**) · re-review at the new head · then **`db reset --linked`** (PO-decided, discharges the backfill; valid only while `non_test = 0`, **expires when the pilot loads data**). ⭐ **Findings that generalise:** re-predicating one gate can **invert the failure mode** of another reading through it · an **assertion-keyed** mutation list cannot see an unexercised **cell** · a **never-denies** mutation cannot move a `lives_ok`, so a whole accept side sat unproven · and a suite can stay **green while losing the only assertion of a policy**. ⚠ **C1a keeps its queue position (G10)** — re-checked 2026-08-28. ⛔ **AE0 results still bind:** **NO planner statistics**; service-role surface **44 sites**. Concluded narrative → [2026-Q3.md](2026-Q3.md).

## Rotated from PROGRESS.md § Now 2026-08-29 — AE2 shipped to the remote, and G2 re-measured

> Concluded at the pre-AE3 branch-cut clearance. Rotated verbatim apart from link repointing,
> per the progress contract (the transform was verified by its own inverse). ⛔ **The G2 figure
> below is HISTORY, not a fact to quote** — the live successor line in § Now carries the *rule*
> (re-measure at the AE3 branch cut) without the number, because that is the half that stays true.

- Gates at completion: `test:db` 248f/8262 · lint 11/11 · typecheck 0 · vitest 2007 · 4 arms INVARIANT HOLDS · `e2e:prod` **GATE GREEN** 1248/0. ▶ **✅ SHIPPED TO THE REMOTE 2026-08-29, schema first then code** (PO-approved that day): `npm run db:push` applied the 7-migration tail `…005900`→`…006500`, **verified in the remote CATALOG, not from `db push`'s report** ([rule](../../.claude/rules/push-schema-before-code.md)) — head `20261003006500`/496, `home_organization_id` **gone**, **0** policies and **0** comment-stripped routine bodies still naming it, 3 ADR 0168 doors present, 36 profiles / 35 live org affiliations / **0 anchorless**; **then** `git push origin main` + branch (`d76abba0..28d90212`), clearing the 67 unpushed AE1 commits with it. ⭐ **The verification was a full catalog DIGEST, not spot-checks** — all 9 sections of `scripts/catalog-fingerprint.sql` over `public`+`app` are **byte-identical local↔remote** (`ZZ_TOTAL a7a9e9a9…`, 5421 rows). ⚠ Coolify holds **no live deployment** (PO-confirmed), so no maintenance window was owed; **that is why the order was still honoured rather than skipped** — the rule is about the order, and a phase that drops a column is exactly where reversing it is worst. ▶ **`db reset --linked` is NOT owed by this push** and was deliberately not run: the remote is schema-current and data-consistent. It stays available if the PO wants the remote reseeded from AE2's **rewritten** `seed.sql` (the current rows predate that rewrite) — its precondition still **expires when the pilot loads data**. ⭐ **G2 RE-MEASURED 2026-08-29 on the remote: `auth.users` = 36, non-`@test.local` = 0** — so ADR 0155 **G2's single-shot authorization for AE3 HOLDS**; ⛔ re-measure at the AE3 branch cut, never quote this ([rule](../../.claude/rules/live-facts-measure-dont-quote.md)).


## Rotated from PROGRESS.md § Now 2026-08-31 — AE3 complete (restricted personal details extracted)

_Moved verbatim at the §6 Record step, with the ONE mechanical transform this directory's own
convention sanctions (phase-ledger.md's header): repo-root link prefixes rewritten for a file that
lives in `docs/progress/`. ⛔ `npm run lint:progress` caught the untransformed links — "verbatim"
and "correct from here" are different claims, and the gate is what keeps them from being conflated._

_ AE3 shipped `public.profile_private_details`; QA APPROVED at r4 after
four rounds (3→3→2→0 blocking), PO-approved the same day. ⛔ **LOCAL ONLY — not pushed**, so the
schema-first order is owed, not discharged. Figures below were true when written; re-measure, never quote._

- **📐 ADR 0155 — the authz-evolution program.** ▶ **AE0 ✅ · AE1 ✅ · AE2 ✅ COMPLETE + QA-APPROVED (r3) 2026-08-28, and ✅ SHIPPED TO THE REMOTE schema-first on 2026-08-29.** `profiles.home_organization_id` is DROPPED. ⛔ **Re-measure `main` vs `origin/main` and the remote head, never quote them** ([rule](../../.claude/rules/live-facts-measure-dont-quote.md)). Concluded narrative → [2026-Q3.md](2026-Q3.md); increment detail [authz-ae2.md](authz-ae2.md); verdict [r3](../reviews/authz-ae2-review-r3.md). ▶ **AE3 IS IN FLIGHT** on branch `authz-ae3-private-details` ([plan](../plans/authz-evolution.md); detail [authz-ae3.md](authz-ae3.md)) — **AE3.1 census ✅** ([artifact](../design/authz-evolution-census-ae3.md); local+remote parity measured, no drift). **G2 RE-MEASURED at the branch cut 2026-08-31: HOLDS** (remote `auth.users` 36 total / **0** non-test) so AE3 keeps single-shot; ⛔ it **expires when the pilot loads data** — re-measure, never quote. **AE3.2 ✅ BUILT AND VERIFIED LOCALLY**: 3 migrations applied on a clean rebuild, `profiles.{cpf,date_of_birth,phone}` DROPPED, values in door-only `public.profile_private_details`. **Gate step 1 measured** — lint 11/11 · typecheck 0 · vitest 2016 · pgTAP **248f/8285** · `ARM=census` 569 gates/605 verdicts · `ARM=hat` 6/6 · `ARM=floor` 72 · `ARM=wrapper` BLIND 41 (⛔ all outside C2's **427** command doors, which no arm covers). ⛔ The **diff-scoped sweep exited 1 — a FINDING, ruled**, not a pass: zero cases derived, 5 functions EXCLUDED-BY-NAME; discharged by `supabase/tests/mutation/ae3-targeted-cases.sh`, **both targeted cases COVERED** with proven rollback. **Gate step 2 ✅ E2E GREEN** — `e2e:prod` full suite **1251 passed · 0 failed · 0 infra · 5 flaky · 0 did-not-run** (21 batches). ⛔ Its FIRST run was a real **FAIL** (4 failed): **3 were AE3's own** — E2E specs still reading `profiles.{cpf,date_of_birth}` — now fixed. New spec `e2e/ae3-restricted-details.spec.ts` (4 green) carries the **D4 rendered-output assertion**, the CPF-probe audit delta and the keyboard-only pass. ⚠ 5 flaky = 2 baseline members to fingerprint + **3 novel names**, none AE3-relevant → `FUP-E2E-AE3-TWO-NOVEL-FLAKES`. **Gate step 3 ⚠ QA round 1 = CHANGES REQUESTED** ([r1](../reviews/authz-ae3-review.md), on **Fable**, user-assigned) — it independently confirmed the census closes, the CHECK/partial index MOVED, the door-only posture is real, the guard was retired AND replaced, both mutation cases are genuine, and **no test was weakened to pass**. 3 blocking findings, all documentation: **B1** DSR/LGPD link absent · **B2** the rollback command wrote PLAINTEXT CPF while its own prose demanded encrypt-at-creation · **B3** a "filed" claim that was untrue. ⭐ **All 3 fixed, plus non-blocking N3/N4** (text this phase wrote). ⭐ B3 uncovered a real drift: the `ARM=census` banner's **407** was a FROZEN LITERAL, live is **427**, and C2's own **426** had drifted too — the banner now **DERIVES** it. Re-verified after: 4 arms HOLD · lint 11/11 · typecheck 0 · pgTAP **248f/8285** on a fresh reset. ⭐ **N1 + N2 also done** (PO instruction): `backend-state.md` gains a first-class § AE3 and five property-derived corrections — ⛔ its old rule *"the withheld set must be exactly `{cpf}`"* is now **FALSE** (that set is EMPTY); `ARCHITECTURE.md`'s zero-policy class now names all **8** members and says the prose is not the authority (pgTAP `382` §A0 derives it). **QA r2 = CHANGES REQUESTED** — it re-executed my B3 derivation (427 = 345+82, predicate confirmed) and found **3 new blocking items, two of them errors IN THE ROUND-1 FIXES**: **B4** the register cited 426 beside the new 427 and kept the OLD decomposition (344+82) · **B5** ⛔ the rollback snapshot used `docker exec` = the **LOCAL** container while the cutover targets **Cloud** — a confidently-encrypted artifact of the wrong DB, and r1 had reviewed that same command and flagged only the plaintext · **B6** my ARCHITECTURE rewrite listed **9** tables for a class labelled 8 (`event_patient` has a live policy) — ⛔ **I had checked the COUNT, not the NAMES.** All 3 fixed + 3 non-blocking; verification is now element-wise. New: `FUP-EVENT-PATIENT-POLICY-PREEMPTED`. **QA r3 = CHANGES REQUESTED**, 2 items, **both again inside the previous round's fix text**: **B7** ⛔ my B5 fix put the **production DB password into shell history** (3rd instance of B2's class — each inside the fix for the last) → `read -rsp` + passphrase lifetime + **rotate the password after the window** · **B8** the 4th live stale figure was in the **PLAN**, and rule 2 — the rule *requiring* the uncovered population be stated — was itself teaching from **407**; rule 1 said "all ten" for an 11-gate chain. Both now name their deriving instrument, tagged `[QA-AE3-r3 B8]`. Both fixed + 2 non-blocking. ⭐ **Trend converging: 3 → 3 → 2 blocking, each narrower.** **QA r4 = ✅ APPROVED** ([r1–r4](../reviews/authz-ae3-review.md)) — 4 rounds, **3→3→2→0** blocking. It read the cutover §4.1 cold as the 2am operator and found nothing new, and ruled the `[QA-AE3-r3 B8]` tag form acceptable (ADR 0162's scope is *decisions*; these repair citation hygiene). ⭐ Its one binding Record-step MUST — my r3 record said the follow-ups 407 cleanup was "also fixed" when **2 of 6 lines were** — is **DISCHARGED**, by property this time: every remaining `407` there is a dated snapshot, the dated header chain, a verbatim quotation, or an unrelated line range. ▶ **STILL OWED: PO APPROVAL, then Record** (Record no longer inherits the MUST). ⚠ A `db reset` ran on the shared local stack today; announce before another. ▶ **`db reset --linked` is not owed**, but stays available if the PO wants the remote reseeded from AE2's rewritten `seed.sql`. ⚠ **C1a keeps its queue position (G10).**

## Rotated from PROGRESS.md § Now 2026-08-31 — C1a DISCHARGED, and C2 Tier 1 SIZED

_Both items were queue positions 1 and 2 of the PO-sequenced list. Both concluded on 2026-08-31 and
their narratives rotate here; only their open residue stays live in § Now. ⚠ **Two statements are
corrected relative to the text that stood in § Now**, both discovered by the `db reset` that followed
the run — they are corrected here rather than archived false, and the corrections are named inline._

### 1 · C1a — the local PHI-disposal rehearsal — ✅ DISCHARGED

**§ 3 steps A–D ran end-to-end, twice** — once `documents-standard` (tier `standard`), once
`documents-phi` (tier **`phi`**) — through the **`subject_request` exemption lane** (ADR 0118 § 10,
the lane provided for exactly this case: an LGPD Art. 18 request passing a provisional retention
policy). Byte proof **earned, not asserted**: the volume walk fell by exactly the file's **168 B**
each run; evidence `byte_proof = local_volume_verified`; full cascade (`[removido]`, description
NULL, `deleted_at` set) and the audit triple `disposition_requested → retention_override → disposed`.
State was built through the **real** D9 upload machine (`reserved → uploaded → verifying →
scan_pending → clean`) and the **real** inflow door, never hand-set — only § 3 was under test.
Record + the six instruction defects it yielded → [phi-backup-run-log.md](../deployment/phi-backup-run-log.md)
§ 2026-08-31 (second run); instrument-level detail there, not restated here.

⛔ **The runbook's `THIS PROCEDURE HAS NEVER BEEN EXECUTED END-TO-END` banner is now false** and is
owed a revision — it is the first thing a reader sees.

⚠ **A first attempt was BLOCKED at Step C on `HC0DR`**, measured with two probes inside a rollback
(the differential — reason forced to `subject_request` — reached the *later* `HC0D9`, proving
retention was the only blocker). ⛔ **Step B was deliberately NOT run on that row**: deleting bytes
and *then* meeting `HC0DR` manufactures the § 4 invisible-orphan class by following § 3 as written.
**§ 3 Step B is owed a retention-gate warning.**

⛔ **CORRECTION 1 — "the seeded `entered_in_error` row" was not seeded.** `seed.sql` creates **zero**
`file_objects`, and `file_objects` is **0** after a clean reset; that row was pre-existing residue
from earlier sessions. § Now and the first run-log entry both called it fixture. **Consequence, and
the reason this matters: after a fresh reset the disposal queue is EMPTY**, so § 3 cannot be
rehearsed as written — constructing the pending state is a mandatory first step the runbook does not
mention.

⛔ **CORRECTION 2 — the orphan figure moved, and the mechanism is now observed live.** § Now recorded
*"372 files vs 22 metadata rows"*. The post-run `db reset` truncated `storage.objects` to **0** while
the volume kept **372 files / 6 637 672 B, 108 PHI-tier** — so the orphan population **grows
monotonically with every reset**. On a machine that resets often, "PHI bytes on disk with no row
pointing at them" is the steady state, not an anomaly.

### 2 · C2 Tier 1 — SIZED, and the split does not split

Instrument [`scripts/authz-c2-tier1-sizing.sql`](../../scripts/authz-c2-tier1-sizing.sql) (re-derives
every figure per run, quotes none); full record → [authz-c2-tier1-sizing.md](../design/authz-c2-tier1-sizing.md).

**The ruled predicate — "touches PHI or crosses a tenant boundary" — returns 405 of 427 (94.8 %)**,
leaving a Tier 2 of 22 internal helpers. The two disjuncts behave differently and the reason is
structural: the **PHI arm discriminates** (230–387 by marker), the **tenancy arm cannot** (395 alone
/ 92.5 %) — a DEFINER door bypasses RLS and must re-establish tenancy itself, so every gated door
reaches `profiles` (354 of 427), `memberships` (346), `commissions` (246). It measures *"is
tenancy-gated"*, not *"crosses a boundary"*.

⛔ **The PHI comment convention is not a usable marker.** Prose polarity is not machine-decidable — a
positive regex captures `patient_xref` (*"is **NOT** a PHI store"*) and `printed_documents` (*"**ZERO**
PHI in columns"*), a column-comment rule captures **0 of 6** canonical PHI stores, and 50 base tables
carry no comment at all. The PHI arm rides on the hard `has_table_privilege` door-only fact instead
(6/6). There is no catalog-side PHI-audit vocabulary to substitute: `audit_log.action` is free text
under a single dot-shape CHECK.

⛔ **Depth-0 grain is FALSIFIED by its own controls** — cheaper (194 vs 387) but it drops
`create_case` and `set_participant_patient`, the two gates of the case PHI **write** path, which
delegate to `app._set_participant_patient_unchecked`. The only population-cutting variant (262,
61.4 %) is a **hand-list** — inadmissible under the PO's own method rule — **and** falsified on
`assume_role`. Only **D (387)** and **E (405)** pass all six positive controls with no hand-list.

⚠ **The long pole is unbuilt and unsized under every option: no command-door neutralizer exists.**
All three harnesses open a *boolean* gate or a policy `USING`; these doors return `jsonb`/`uuid`/`void`.

⛔ **Sizing closed nothing** — no door has a verdict, both absorbed items (`Q1-OPEN-BYTES-CUT`,
`SIBLING-GUARD-DIFF`) stay open with their own index lines, and `assume_role` stays ERROR-shaped.
