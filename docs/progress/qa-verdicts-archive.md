# Archive — QA Verdicts (full verbose form)

> Archived verbatim from PROGRESS.md on 2026-06-25 at the §7 progress-tracker cleanup.
> This is the durable detail; PROGRESS.md keeps only a one-line pointer.

> PROGRESS.md keeps a collapsed table; full per-phase reviews are under docs/reviews/.

| Phase | Verdict | Report | Blockers/Majors | Follow-ups carried |
| ----- | ------- | ------ | --------------- | ------------------ |
| Multi-Tenancy (organizations/hospitals/org-admin, RLS rewrite, multi-org PHI guard) | **APPROVED** (2026-06-25) | [multitenancy-review.md](../reviews/multitenancy-review.md) | None (0 blockers, 0 majors). Platform_admin wall complete (74-assertion pgTAP 171 suite; zero tenant-table rows for platform_admin; PHI tables raise 42501 on direct SELECT; `get_*_patient` RPCs return null). PHI duty separation confirmed: `can_read_case_patient` excludes `is_org_admin_of_*` term at DB layer. Multi-org PHI guard: `is_pqs_member` chokepoint closes entire global-PQS surface; per-predicate `not is_multi_org()` guards are correct defense-in-depth; module-off asserts raise; 18-assertion pgTAP 173 suite covers all layers. Cross-org isolation proven for all personas across two orgs (org_admin sees own org only; commission members see own commission only; audit tier access correct). Service-role escalation minimal and correctly gated (platform/actions.ts `assignOrgAdmin` only, behind `requireAdmin()`; role hard-coded, never from formData). Route handlers gate with `getCommissionAccessByOrg` + `role !== 'staff_admin'`; no service-role in any route handler. All existing immutability invariants (audit guard trigger, form version/section/item trigger, submit RPC) unaffected. ADR 0041 present and complete. E2E 292/0/0 (124 skips = NSP+referral module-off quarantine, by design). Architecture Rules 1/6/9/10/11/12 satisfied. | INFO-1: `is_multi_org()` count scan inside `is_pqs_member` — correct and cheap in practice; future materialized-view optimization is reasonable at scale. INFO-2: 124 E2E skips are NSP/referral module-off quarantine; will lift when NSP-per-org ships. INFO-3: legacy `getCommissionAccess(slug)` (single-arg) still present in `src/lib/queries/session.ts` — no longer called by any route, but should be removed or deprecation-marked to prevent future misuse. |
| Form Builder Enhancements (mini-phase) | **APPROVED** (2026-06-23 re-review) | [form-builder-enhancements-review.md](../reviews/form-builder-enhancements-review.md) | None (0 blockers, 0 majors). **MAJOR-1 RESOLVED** (`b80c5ba`+`cf6dcfb`+`ef1e040`): `toCondition` now coerces number-target value to a JSON number; `toDrafts` read-back fixed; `assert_condition_op_target` rejects non-numeric JSON for number targets at publish time (pt-BR); E2E AC-15 covers the multi-digit lexical≠numeric case (answer 10→shows, 3→hidden). MINOR-1/2/3 and INFO-1 all cleared. Final gate state: tsc 0, lint 0, vitest 113/113, pgTAP 870/870, FBE 15/15 + phase4 8/8 = 23/23. Security/RLS/integrity PASS — relaxed CHECKs are supersets, Rule 5 immutability covers new columns (blanket guard trigger), Rule 11 holds (`observation` never in audit log; `answer_map` value-only), `get_response_for_signoff` gating byte-identical, evaluator parity confirmed. 0 net-new E2E failures vs `main` baseline. | INFO-2: `form_items_options_shape` superset is data-dependent (bare-string rows), not predicate-pure — noted, no action required. INFO-3: pre-existing full-serial-suite contamination (~17–19 on branch AND `main`) — separate spec-isolation effort, not this feature's gate. INFO-4: FBE E2E builds fixtures in seeded CCIH commission — harden to probe commission if it joins the full gate matrix. |
| `case_phase_results` (Per-Phase Categorical Result + Manual Override) | APPROVED (2026-06-23) | [phase-results-review.md](../reviews/phase-results-review.md) | None (0 blockers, 0 majors). RLS commission-scoped on `phase_results` + `case_phase_offered_results` (member-read/staff_admin-write); both DEFINER RPCs (`compute_case_phase_result`, `set_case_phase_result_override`) enforce authz internally with pinned `search_path`; dual-entry override authz correct (ativa: assignee OR staff_admin; concluida: staff_admin + non-terminal guard HC060); HC057/HC058/HC060 all mapped to pt-BR; `in_case_rpc` GUC pattern consistent with all existing case RPCs; Rule 5 (immutability) holds — ruleset frozen at publish, snapshotted at case creation, result written atomically at conclude; Rule 11 (audit) holds — override fact+option-id audited, reason NEVER copied, result rides existing status-change row via updated allow-list; `condition-vectors.json` byte-unchanged; `walkResultRuleset` drift confirmed zero (vector-driven Vitest); feature is correctly NON-PHI; `as unknown as string` casts justified; all surfaces pt-BR; a11y met; flag ships OFF. All plan deliverables implemented including post-conclusion correction. pgTAP 45/45 + Vitest 57/57 + E2E 7/7 (AC-1–AC-6, AC-K). Architecture Rules 1/3/5/9/10/11 satisfied; Rule 12 N/A (confirmed non-PHI). | INFO-1: `result_override_reason` readable via raw PostgREST on `case_phases` for any `can_read_case` user (intentional: user-authored business justification, not PHI; excluded from `get_case_detail` and audit log). INFO-2: `add/update_template_phase` do not assert `case_phase_results` flag (ruleset can be authored flag-off; compute early-returns; by design per plan). |
| 23 — Patient Identity & Cross-Committee Linkage | APPROVED (2026-06-22) | [phase-23-review.md](../reviews/phase-23-review.md) | None (0 blockers, 0 majors). `patient_xref` REVOKE + QPS-only SELECT policy confirmed; `app.app_secrets` REVOKED from authenticated+anon (GRANT SELECT to service_role only; not PostgREST-exposed; hard-fail on absent pepper); all 5 DEFINER doors re-gate on `is_pqs_member` or `can_read_referral_phi`; audit emits `patient.searched`/`patient.viewed` on GLOBAL chain with 12-char truncated key only (never raw MRN); both `dispose_*_phi` replacements machine-verified 1-line diff from originals; AFTER-DELETE xref trigger stamps retained row with real disposal reason; triggers always-on (flag gates RPCs+UI only); PHI-free trajectory bundle confirmed (entity codes + commission names only, no PHI table JOINs); `patient_access_audit` selects no `metadata`/`summary` columns; TS `as unknown as` casts justified (jsonb Returns:Json); types regenerated; typecheck+lint 0; pgTAP 39/39; E2E 15/15 (all 11 ACs); 10/10 regression sweep. All 3 binding requirements met. Architecture Rules 1/9/10/11/12 satisfied. | INFO-1: `patient.searched`/`patient.viewed` audit rows not surfaced in `patient_access_audit` (by design — key-derived UUID not in xref entity_ids; access audit shows PHI record events, not QPS search trail). Follow-ups (non-blocking, per plan): `dispose_referral_phi`; pepper rotation; case+referral "ver trajetória" QPS links (need public PQS boolean); access audit on deep-link entry; SPEC-P22-001 Phase-22 spec defect. |
| `case_patient` (Third PHI Module; ADR 0038) | APPROVED (2026-06-22) | [case-patient-review.md](../reviews/case-patient-review.md) | None (0 blockers, 0 majors). `case_patient` REVOKE + audited single-door confirmed at all layers; `can_read_case_patient` = live broad `can_read_case` (deliberate divergence, documented); write gate coordinators-only confirmed (pgTAP + E2E AC-3a-rpc); `log_audit_access` allow-list carries all 11 prior verbs + appends `case_patient.read` (no verb dropped); `dispose_case_phi` gated/one-shot (HC056)/audit-PHI-safe/skeleton-preserved/both bypass flags set; `create_case_from_template` + `get_case_detail` REPLACE additive-only (no regression); flag ships OFF; PHI never in audit log. All 8 ADR-0038 decisions implemented; all 15 E2E flows (case-patient.spec.ts) green; pgTAP 35/35. Architecture Rules 1/9/10/11/12 satisfied. | INFO-1: `dispose_case_phi` summary embeds `case_number` (non-PHI governance metadata per ADR 0036 classification; no action required). INFO-2: `dispose_referral_phi` parity gap is pre-existing (tracked in ADR 0038 Consequences; out-of-scope for this increment). CN-APP-AC4 pre-existing on `main` (not a regression). |
| 22 — Inter-Committee Case Referrals | APPROVED (2026-06-21) | [phase-22-review.md](../reviews/phase-22-review.md) | None (0 blockers, 0 majors). `referral_patient` REVOKE + audited single-door confirmed at all layers; `can_read_referral_phi` NO `is_admin` term verified; three-layer free-text PHI lockdown (SELECT-policy swap + column REVOKE + `get_referral_detail` body-gate) sound; `can_read_case` QPS term flag-gated + before `case_access` fallback + no target-commission leg; HC076 gate correct; no service-role download path; all 6 Phase-22 audit verbs on allow-list; QPS dashboard gated on `isPqsMemberSelf()`; list/hub/dashboard PHI-free. All 16 plan decisions implemented; all 8 E2E flows green. Flag ships OFF. Architecture Rules 1/6/7/9/10/11/12 satisfied. | INFO-1: ADR 0037 HC078 description omits `referral_target_analyst` from `set_referral_patient` entitlement list — prose-only omission, no security implication; fold into §6 Record ADR update. |
| PHI/HIPAA Remediation (WS 0/A/B/C/D/E) | APPROVED (2026-06-20) | [phi-remediation-review.md](../reviews/phi-remediation-review.md) | None (0 blockers, 0 majors). `event_patient` lockdown sound at all layers (DB predicate, REVOKE, DEFINER single-door RPC, TS query layer); 6 `.viewed` verbs in DB allow-list and all 6 detail-read emit sites present; `dispose_event_phi` gated/one-shot/audit-PHI-safe/skeleton-preserved; docs accurately reflect implemented controls (no residual encryption claims; LGPD/ANVISA/CFM correctly framed). Architecture Rules 1/9/11/12 satisfied. | INFO-2 (Phase 19 gate): the 22 classified PHI free-text columns must be a hard AC for Phase-19 surveyor/evidence export. |
| 0 | APPROVED | [phase-0-review.md](../reviews/phase-0-review.md) | None | MINOR-1: set lang="pt-BR" in layout.tsx (Phase 2); MINOR-2: close stale supabase-start follow-up; INFO: update ADR 0001 to note --passWithNoTests removed |
| 1 | APPROVED (re-verified 2026-06-12) | [phase-1-review.md](../reviews/phase-1-review.md) | All resolved in M8: MAJOR-1 (USING clause fix + demotion test); MAJOR-2 (3 sign-off immutability tests added); MINOR-1 (eval_condition search_path); MINOR-2 (profiles never-deleted: policy split + trigger); MINOR-3 (version/commission guard trigger) | INFO-1: consider revoking anon DML/EXECUTE grants in Phase 8 hardening |
| 2 | APPROVED + RE-REVIEW APPROVED (2026-06-12) | [phase-2-review.md](../reviews/phase-2-review.md) | All resolved: MAJOR-1 (bad-creds selector, bug P2-001 → `f1c561f`); BLOCKER P2-002 (post-login race) fixed (`c808f8d`+`760d6a4`, ADR 0009) & load-verified 430/430. Re-review of the auth hot-path fix: APPROVED | INFO-1 (ADR 0007) done; INFO-2 (ADR 0008) done; carried: prod must use asymmetric JWT signing keys (Phase 8 deploy checklist) |
| 3 | APPROVED (2026-06-12) | [phase-3-review.md](../reviews/phase-3-review.md) | None | MINOR-1 ✅ RESOLVED 2026-06-12 (backend, `revalidateCommissionPages` — assign/remove now revalidate `/admin/comissoes/[slug]` too); MINOR-2 ✅ RESOLVED 2026-06-12 (frontend, `ConfirmRemoveButton` now types against `ActionState` from `@/lib/admin/actions`); both pre-record per human request. **Carried:** INFO-1 — AC1 E2E asserts the coordinator email appears in the roster but not that the "Coordenação" RoleBadge renders (add to Phase 4 test-hardening). Post-fix verification: lint + typecheck + unit (20/20) green; full E2E NOT re-run (both fixes provably non-runtime — a `revalidatePath` cache hint can't regress a passing nav-fresh test, and a type-only import swap has no runtime effect), so the 43/43 run stands. |
| 4 | APPROVED (re-review 2026-06-12) | [phase-4-review.md](../reviews/phase-4-review.md) | All resolved: MAJOR-1 ✅ keyboard-only test added (`e2e/phase4-builder.spec.ts`, 8/8 green); MINOR-1 ✅ `contextOfItem` FK hop comment (backend); MINOR-3 ✅ `revalidateBuilder` bracket comment (backend) | MINOR-2: INFO only — no action required (version history `?v=` scope check passes defense-in-depth via RLS) |
| 5 | APPROVED (2026-06-13) | [phase-5-review.md](../reviews/phase-5-review.md) | None (no blockers or majors) | MINOR-1 (backend, Phase 6): add `p_section_id` version guard in `save_section_answers`; MINOR-2 (backend, Phase 6): distinguish `check_violation` sources in `saveSection` action error mapping; INFO-1 (tester): remove unused `Locator` import in `phase5-wizard.spec.ts` |
| 6 | APPROVED (2026-06-13) | [phase-6-review.md](../reviews/phase-6-review.md) | None (no blockers or majors). P6-001 (BLOCKER) already RESOLVED in-phase. Verified the `signoffs_insert`/`signoffs_select` rewrite (`20260613090003`) is byte-equivalent on the signer-role rule — only the eval context moved to SECURITY DEFINER; `responses_select`/`answers_select` untouched (Phase-7 in_progress-answers invariant holds); all new definer fns pin `search_path` + are internally gated. P0012 live + single-sourced with queue readiness. Phase-5 MINOR-1/MINOR-2 folded in. | INFO-1: no answer-lock between sign-off and submission (ADR 0016 v1 limitation; acceptable — submit re-checks required/visibility; future: invalidate sign-off on post-sign answer change); INFO-2: `Signoff.signedByName` typed non-nullable vs query `string \| null` — sound via adapters, consider aligning; INFO-3: Phase-8 carry-forwards unchanged (revoke anon grants; prod asymmetric JWT keys) |
| 7 | APPROVED (2026-06-13) | [phase-7-review.md](../reviews/phase-7-review.md) | None (0 blockers, 0 majors). Security invariant (in_progress-answers isolation) confirmed sound at all layers: `case_phases` schema has no answer columns; `case_phase_answer_map` is submitted-only; `list_cases_board`/`get_case_detail` definer reads expose status/submitted answers only; `responses_select`/`answers_select` unchanged. HC0xx remap complete — no live P0010–P0022 raisers remain. Evaluator reuse confirmed — `eval_condition` + `evalCondition` + `condition-vectors.json` unchanged. | MINOR-1: HC017 (create_case_from_template with unpublished form) not directly asserted in pgTAP — add to Phase 8 test-hardening; MINOR-2: `archive_process_template` lacks status guard (archived→archived is silently idempotent); INFO-1: stale comment in E2E spec referencing "P0022/HTTP 500" (should be HC022/HTTP 400); Phase 8/9 carry-forwards unchanged (revoke anon grants; prod asymmetric JWT keys) |
| 8 | APPROVED (2026-06-14) | [phase-8-review.md](../reviews/phase-8-review.md) | None (0 blockers, 0 majors). RLS crux confirmed: in_progress-answers invariant holds at list/detail/export/dashboard paths; definer RPCs gated + search_path pinned; B6 anon/PUBLIC EXECUTE revoke complete + durable (pgTAP tests 15–17). ADR 0020 consistent SQL↔TS. All 12 AC clauses covered. | MINOR-1: CSV export ignores active date filter (route reads `?form` only; RPC has no date params — thread `p_from`/`p_to` through route→query→RPC); MINOR-2: form tab badge shows all-time count while body headline shows date-filtered count (display inconsistency — either pass range to `listDashboardForms` or make all-time explicit). INFO-1: `totalSubmitted` derived from `submissionsOverTime` sum (single-source concern for future). INFO-2: prod asymmetric JWT keys → Phase 9 deploy checklist (carry-forward from Phase 2). **MINOR-1/MINOR-2 RESOLVED in-phase** (backend `919d86a` mig `…090015` + frontend `421be85`); INFO-1 addressed; re-verified 106/106. |
| 10 | APPROVED (2026-06-15) | [phase-10-review.md](../reviews/phase-10-review.md) | None (0 blockers, 0 majors). Sign-own-row DEFINER bypass + explicit `can_sign_meeting` re-check confirmed correct. Storage immutability, child-lock, anon/PUBLIC EXECUTE revoke sweep, and all HC032–HC037 guards verified. | MINOR-1: Cancel button shown on `assinada` meetings (`meeting-lifecycle-actions.tsx` L77 — add `&& status !== 'assinada'`); MINOR-2: `conclude_meeting` `present_count` snapshot + attendees-panel live count include external guests (add `user_id IS NOT NULL` filter in SQL + `&& a.userId !== null` in UI). Both pre-record per user preference. |
| Cases-Extras (R1–R5) | APPROVED (2026-06-14) | [cases-extras-review.md](../reviews/cases-extras-review.md) | None (0 blockers, 0 majors). RLS confirmed on all 6 new tables + `case-documents` storage bucket (member-read/staff_admin-write; no storage UPDATE/DELETE). `app.apply_case_status` DEFINER core: every public entry point (`set_case_status`, `close_case`, `cancel_case`) carries explicit `is_staff_admin_of OR is_admin` gate + search_path pinned. `'aberto'` liveness sweep complete — verified by dedicated pgTAP regression test (submit while `em_revisao` advances phase) + E2E AC-SubmitWhileCustomStatus. Phase-7 in_progress-answers invariant untouched. HC024–HC027 mapped to pt-BR. Architecture Rules 1/6/7/9/10 satisfied. | MINOR-1: `case-action-item-form.tsx` missing "Nunca inclua dados de paciente." PII warning (present in event-form and document-upload dialogs — CLAUDE.md §1 consistency). INFO-1: `caseStatusIsTerminal` twin duplication justified (next/headers boundary); future clean-up: extract to a side-effect-free `case-statuses-pure.ts`. INFO-2: no guard prevents archiving ALL terminal statuses (v1 operational risk only, no security implication). INFO-3: no dedicated ADRs for R1/R3/R4 (plan doc is the decision record). INFO-4: remote `db push` (092000–092006) still pending explicit human/lead go-ahead — must precede Phase 9. |
| Case data-model adjustments (D1–D15) | APPROVED (2026-06-14) | [case-model-adjustments-review.md](../reviews/case-model-adjustments-review.md) | None (0 blockers, 0 majors). Dropped-helper landmine (`app.case_status_is_terminal`) fully cleared — every liveness-sweep function restated in 093001–093003 with a fixed-enum check; grep confirms zero surviving references at runtime. RLS on all 3 new tables (member-read/staff_admin-write via `commission_of_case`/`commission_of_template`; no cross-commission leakage). HC030 same-commission guard on `process_template_outcomes` mirrors HC026. `recompute_case_status` AFTER trigger: no DELETE event (cascade-safe), depth-1, no recursion. Phase-7 in_progress-answers invariant untouched (no answer columns in extended board/detail RPCs). Anon/PUBLIC EXECUTE re-revoked on all replaced/new public functions. Fixed status single-sourced in `src/lib/cases/case-status.ts`. HC020 (activate early-exit) vs HC025 (DB trigger) distinction confirmed coherent. | MINOR-1: HC028 (outcome required) and HC031 (unsettled phases) not mapped in `mapCaseError` in `src/lib/cases/actions.ts` — server errors fall through to generic pt-BR message; client-side gate mitigates in normal usage. MINOR-2: No `docs/decisions/0024-*.md` ADR (plan doc serves as the decision record but the numbered ADR series should be extended). |
| 11 — Interviews | APPROVED (2026-06-15) | [phase-11-review.md](../reviews/phase-11-review.md) | None (0 blockers, 0 majors). New RLS shape (`can_write_interview` DEFINER + participant-write grant) verified: recursion-free, search_path pinned, anon/PUBLIC EXECUTE revoked across all 16 RPCs + 3 DEFINER helpers + 1 thin-read, storage policy path-segment indexing sound, child-lock correctly excludes attachments table per ADR intent. All 14 plan decisions implemented. All 8 PHASES.md AC clauses covered by E2E (9 tests, AC2 split into AC2a/AC2b). HC038–HC041 + HC021 mapped to pt-BR; no raw Postgres errors reach UI. Architecture Rules 1/3/6/7/9/10 satisfied. P11-001 BLOCKER resolved pre-review. | MINOR-1: `canEditContent` (which gates upload/link controls in `AttachmentsPanel`) requires `isEditableInterviewStatus`, hiding upload UI when interview is `concluida` or `cancelada` — but the server (and ADR 0026) intentionally allows post-conclusion attachment uploads (signed transcript / late evidence). UI is more restrictive than the server contract; post-conclusion uploads require a reopen/re-conclude cycle. No security implication; consider a separate `canUpload = canWrite && status !== 'cancelada'` flag to honor the ADR intent. **→ RESOLVED 2026-06-15 (P11-MINOR-1): frontend added `canManageAttachments = canWrite && status !== 'cancelada'` passed only to `AttachmentsPanel`; AC9 E2E locks it in. Re-confirmed green 152/152.** |
| 14a — NSP Foundation | CHANGES REQUESTED → ✅ APPROVED (re-verified 2026-06-18) | [phase-14a-review.md](../reviews/phase-14a-review.md) | 0 blockers, 1 major, 1 minor | M1: add SELECT policy on `pqs_department` (one-line additive migration; Rule 1); N1: add `message?` field to `ActionState`/`NotifyEventState` so success text is not returned in the `error` field **→ M1+N1 FIXED 2026-06-18 (backend: mig `…121005` + `ActionState.message`); pgTAP 409/409, 14a spec 16/16; **QA RE-VERIFIED ✅ APPROVED 2026-06-18** (M1 live `pg_policies` + anon-deny confirmed; N1/I2 closed; 4-action success-in-`error` smell → deferred INFO follow-up)** |
| 14b–14d — Triage, RCA & CAPA | CHANGES REQUESTED → ✅ APPROVED (re-verified 2026-06-18) | [phase-14-review.md](../reviews/phase-14-review.md) | 1 BLOCKING (BLOCKER-1) + 1 MINOR — both resolved | BLOCKER-1 RESOLVED: `triage_disposition` line 392 qualified as `event_triage.event_id` — no more SQLSTATE 42702; pgTAP 141 plan expanded to 44 with 5 direct RPC-return assertions (is_sentinel/verdict/review_pathway/rca_due_date/row-count), all pass. MINOR-1 RESOLVED: `set_pqs_rca_due_window` audit row corrected to `action='pqs_config.rca_due_window_changed'`, `entity_type='pqs_department'`, with old/new value diff. pgTAP 516/516 PASS. All AC, RLS, PHI isolation, immutability, state machines, conclude gates, audit allow-lists, and UX verified PASS. 0 open findings. |
| Case Narratives | CHANGES REQUESTED (2026-06-19) | [case-narratives-review.md](../reviews/case-narratives-review.md) | BLOCK-1 (functional): `narrative-type-dialog.tsx` L80 `form.set("id",…)` must be `form.set("narrativeTypeId",…)` — edit always fails silently; BLOCK-2 (a11y): `narrative-slot-card.tsx` Editar button missing `aria-label` | None carried |
| 13 — Audit Trail | APPROVED (2026-06-18) | phase-13-review (inline) | None (0 blockers, 0 majors). Core security crux verified: append-only guard (`guard_audit_immutable_trg` BEFORE UPDATE OR DELETE, `HC042`, fires even for `service_role`); RLS SELECT = `is_admin()` OR `is_staff_admin_of(commission_id)` with NO INSERT/UPDATE/DELETE policy; zero anon/PUBLIC grant (revoked by 090012 default-privileges + pgTAP test 25 confirms); hash chain commits to every semantic column (ADR 0029 Q3); per-commission + global chains independent with advisory-lock serialization; `verify_audit_chain` DEFINER, `is_staff_admin_of`/admin-gated internally; `log_audit_access` surface is positive-allow-listed to exactly 3 actions; all allow-lists exclude `answers.value`, `*_md`, `label`, `question_explanation`, `content`, `options`, `description`, `minutes_md`, `summary_md`, `note` (Rule 11 data-minimization). All 8 AC clauses covered (AC-1a–f: 6 mutations, AC-2: append-only, AC-3: RLS scoping + route gating, AC-4: .read/.export rows, AC-5: filters, AC-6: CSV count, AC-7: integrity verify, AC-8: keyboard-only). DEFINER `search_path` pinned on all 6 writer/trigger functions. `import 'server-only'` on `src/lib/audit/access.ts`; service-role key never NEXT_PUBLIC_*. Architecture Rules 1/9/10/11 satisfied. | MINOR-1: `guard_audit_immutable` raises a pt-BR message string (`'os registros de auditoria são imutáveis (somente inserção)'`) but the `HC042` constant in `src/lib/audit/messages.ts` maps it to the English-ish `AUDIT_MESSAGES.appendOnly` (`'Os registros de auditoria não podem ser alterados nem excluídos.'`) — the mapping is correct pt-BR and will only fire on an unreachable code path (no application write path touches UPDATE/DELETE on `audit_log`), so this is purely cosmetic and cannot reach the UI. **RESOLVED 2026-06-18 (backend, bundled with 14a B5):** TS-side only — `AUDIT_MESSAGES.appendOnly` aligned to the DB RAISE wording (`'Os registros de auditoria são imutáveis (somente inserção).'`); the internal HC042 RAISE text was NOT churned (no migration). typecheck + lint clean. |
| 12 — Case Timeline | APPROVED (2026-06-16) | [phase-12-review.md](../reviews/phase-12-review.md) | None (0 blockers, 0 majors). Read-only composition over existing RLS-scoped reads; no new attack surface. `case_phases_select` member-read confirmed correct for direct bar-timestamps read (bar timestamps not exposed by `getCaseDetail` RPC). Foreign-commission caller gets 404 at route + empty array at DB (Security E2E). event-model.ts is genuinely pure (zero imports). Both dedups (interview echo by `registry_event_id`; meeting echo by `kind='meeting'` drop) correct and documented in ADR 0027. Both layouts, all 8 types, adaptive axis, open/closed lifecycle, legend-filter, URL persistence, keyboard flow all verified. Architecture Rules 1/6/7/9/10 satisfied. | MINOR-1: `TimelineEventSheet` (`timeline-event-sheet.tsx`) missing `DialogPrimitive.Description` — Radix logs a dev warning and screen readers hear only the title when the dialog opens. Fix: add `<DialogPrimitive.Description className="sr-only">{meta.label} · {formatEventDate(event)}</DialogPrimitive.Description>` after `DialogPrimitive.Title` in `SheetBody`. Pre-record per user preference. **→ RESOLVED 2026-06-16 (frontend):** added the `sr-only DialogPrimitive.Description` (`{meta.label} · {formatEventDate(event)}`) after the Title in `timeline-event-sheet.tsx`; the Radix "missing Description" warning is gone. Lead also cleared 2 unused-var lint warnings in `e2e/phase12-timeline.spec.ts` (dead `SEEDED_INTERVIEW_ID` const + superseded `stickyHeader` locator — pure dead-code, **no assertion changed**). `npm run typecheck` + `npm run lint` clean (0/0); full suite re-run serially to re-declare green. |
| Case Access Control & "Meus Casos" | APPROVED (2026-06-19) | [case-access-control-review.md](../reviews/case-access-control-review.md) | None (0 blockers, 0 majors; CA-001 BLOCKER + CA-002 MAJOR both resolved in-phase). All 25 ADR-0033 decisions and all 11 ACs verified. RLS spine (`can_read_case`/`can_write_case_content`/`can_write_case_narrative`) correct, 82-point pgTAP truth-table, no anon/PUBLIC EXECUTE leak. Phase-7 submitted-only invariant structurally preserved. PHI isolation (Rule 12) untouched. Flag-OFF fallback proven equivalent. `canEditNarrative` TS mirror matches DB predicate exactly post CA-002. CA-001 regression guard (provolatile='v' + behavior assertion) durable in pgTAP. | INFO-N1: `case_interviews` still member-read (deferred fast-follow per ADR 0033 D2). INFO-N2: ~18 pre-existing E2E harness-debt flakes (animation timing + shared-DB cascade) — fix = global `reducedMotion` + per-test DB isolation, carry to next increment. INFO-N3: coordinator access panel cannot show live "currently read/write" grant indicator (no `listCaseAccess` query; deferred UX improvement). INFO-N4 (CARRY): legacy `update_case_narrative_body` raises P0002 (not 42501) for a non-reader with `case_access` ON — unreachable on the new UI path; no security implication; align error code if function is ever exposed to non-coordinators in a future increment. |

<!-- Appended 2026-07-02 at §7 cleanup: full verbose Notes for the three shared-action-items
     verdicts, rotated out of the live PROGRESS.md QA Verdicts table (which keeps a collapsed
     one-liner + report link). -->
| Action-Items Fold + `visibility_scope` + Case-Access Expiry (ADR 0050) | **APPROVED** (2026-07-02) | [report](../reviews/action-items-fold-review.md) | 0 BLOCKER · 0 MAJOR · 0 MINOR · 4 INFO. RLS surface verified **live** against the DB (matrix under `set role authenticated` + JWT). **Existence leak CLOSED:** on a clean case, a plain member sees only the `committee` item — blind to the `case_restricted` row AND both satellites (no leak via `status_history.comment` or `assignments`); phase-assignee + `case_access` grantee + staff_admin see it; `assignees_only` → assignee + staff_admin only. **Guard invariant:** `source_type='case'` force-set `case_restricted` on INSERT + re-forced on UPDATE (can't be made committee-visible); single 3-source link CHECK (old meeting-CHECK folded in) rejects all four malformed-row shapes. **ADR-0033-D4 HOLDS:** a WRITE-grantee who is only `staff` (not staff_admin) creates a case action item via the RPC; read-only grantee denied (42501); advance/complete = assignee OR content-writer (HC027 proven both ways). All committee_* + case_access RPCs SECURITY DEFINER + owner postgres + pinned search_path; **t19 anon-exec clean** (ACL = authenticated/postgres/service_role; live `set role anon` denied). **Rule 12 six-consulter expiry COMPLETE:** `(ca.expires_at is null or ca.expires_at > now())` in the live body of `can_read_case`, `can_read_case_patient`, `can_write_case_content`, `referral_target_analyst` (PHI arm), `get_member_overview`, and `list_my_cases` (BOTH arms) — expired read grant → `can_read_case`=false; expired write grant → case-item create denied. **Rule 11:** folded case rows now emit `action_item.*` audit (new coverage); diff allow-list adds only `source_case_id/source_case_phase_id/visibility_scope`, copies NO title/description/PHI; `trg_audit_case_access` tracks `level/expires_at/reason`. **Drop complete:** `case_action_items` + 4 old RPCs + `advance_action_item_core` gone (live catalog); no app-code refs. **Frontend:** no inline supabase-js (Rule 9), pt-BR (Rule 10), a11y (dialog labels, fieldset/legend radiogroup, `aria-describedby`, expired badge = icon+text not colour-alone), strict-TS clean. Backend's 2 beyond-spec calls valid: (a) folded 3-source CHECK complete; (b) `get_member_overview` redefined in migration 2 (ordering-correct — 20260708 newest, live body carries the expiry filter). typecheck clean · lint 0 errors · Vitest 193/193. INFO-1..4: hub inline vs satellite-helper (intentional), per-row parent lookup (indexed, norm), `list_case_access` returns expired rows by design (config read, not authority), BUG-AIF-001 pre-existing/out-of-scope. |
| Shared (non-PHI) `action_items` table (Option A) | **APPROVED** (2026-07-02) | [report](../reviews/shared-action-items-review.md) | 0 BLOCKER, 0 MAJOR, 1 NIT + 2 OBSERVATIONS (all non-blocking). Security props verified **live** against the DB, not just SQL-read. **RLS enabled on all 5 tables** (`relrowsecurity=t`); hub SELECT = flat membership OR org-admin (byte-identical to dropped `meeting_action_items_select`); hub WRITE (`ALL`) = staff_admin/org_admin with **both** USING+WITH CHECK (no cross-commission reassignment). Satellites SELECT-only via `commission_of_action_item`; lookups SELECT `true`; **no write policy** on either → RLS default-deny confirmed live (direct `authenticated` INSERT into assignments/history/statuses each → "violates row-level security policy"); only the DEFINER RPCs (owner postgres) write. All 8 RPCs SECURITY DEFINER + owner postgres + pinned search_path; **no anon/PUBLIC EXECUTE** (t19 clean live); authority internal + `auth.uid()`-based (create/delete staff_admin→42501; advance/complete assignee-or-admin→HC037; assignee-member→HC021; status cross-commission injection blocked). **Rule 11:** audit added (was absent on old table); `audit_diff` allow-list `{source_type,status_id,urgency_id,due_date,assigned_to,case_id}` — title/description (free-text) excluded, no PHI copied. **Rule 12:** no PHI in hub; CAPA + `case_action_items` untouched/isolated (grep-confirmed). **Rule 9/10:** mutations via `committee_*` RPCs, reads via `src/lib/queries/`, components carry no inline supabase-js, pt-BR strings + a11y (caption/scope/radiogroup/focus rings/sr-only overdue), typecheck clean. Old `meeting_action_items` table + 4 RPCs + core + guard **fully dropped** (live). Repointed `list_my_action_items`/`get_member_overview` read shared hub with zero `meeting_action_items` refs (live). Every FK indexed + one-active-owner & active/overdue partials present. pgTAP `182` (48 assertions) covers grants/RLS/create/advance/owner/guard/delete/readers/audit; `171`+`181` updated; e2e adds `manual`/Avulso. NIT-1: `resolveActionItemStatusId` uses inline supabase-js but is inside `src/lib/` (Rule 9 OK). OBS-1: policies delegate to `is_*` helpers vs `(select auth.uid())` — house pattern, intent met. OBS-2: standalone pgTAP re-run blocked by search-path/fixture harness artifact, not a failure. 18 E2E fails pre-root-caused unrelated (accepted). |
| Member Overview & "Meus itens de ação" | **APPROVED** (2026-07-02) | [report](../reviews/member-overview-action-items-review.md) | 0 BLOCKER, 0 MAJOR, 2 MINOR (informational, no change). Both `SECURITY DEFINER` RPCs (`list_my_action_items`, `get_member_overview`) strictly self-scoped (`= auth.uid()` on every arm), STABLE, `search_path` pinned, `owner postgres`, `REVOKE…FROM public` before `GRANT…authenticated` (t19 guard, asserted). Label joins expose only PHI-free `case_number`/`label`/`meeting_number`/`scheduled_start`/`full_name` — no `*_md`/free-text/answers/`case_patient`/PHI; no audit row correct (self-scoped, non-PHI — Rule 11). **Count 1** faithful to `app.can_read_case`: phase/narrative attribution counts regardless of `case_access`, grant leg flag-gated, non-terminal (excl. `concluido`/`cancelado`), `cases_extras`-gated — pgTAP t17 proves attributed-but-not-granted counted with `case_access` OFF. Flag-off sources omitted from union / counts 0/null, never raise. Frontend: Rule 9 (no inline supabase-js; RPCs in regenerated types); case link → member `casos/{id}` degrading to plain text when flag OFF (route 404s; verified `getCaseDetail` re-enforces `can_read_case` server-side), meeting → `meetings/{id}`; status badge = action-item vocab (not case enum); source badge distinct discriminator. pt-BR + a11y (table semantics, labeled radiogroup/checkbox, focus rings, overdue not color-alone, reduced-motion) + strict-TS (no `any`) all pass. Sub-entitlement note examined: an action-item assignee who isn't a `can_read_case` reader sees only PHI-free label material by design — no gated content leaks. E2E 16/16 (isolation), pgTAP 1277/1277. |

<!-- Appended 2026-07-02 at §7 cleanup: full verbose (incl. superseded ~~struck~~ trail) of the
     form-model-norm verdict, collapsed to a one-liner in the live PROGRESS.md. -->
| form-model-norm | CHANGES→**APPROVED** (2026-07-01) | [report](../reviews/form-model-normalization-review.md) | 0 BLOCKER, 0 open. **MAJOR-1 RESOLVED (`e13081f`+`e158e91`, re-verified live):** per-row position loop replaced by a single-transaction SECURITY INVOKER RPC `reconcile_item_options` (mig `20260630017000`) — delete removed→update kept→insert new in a temp band→assign ALL positions in one `UPDATE` (unnest…with ordinality), so the DEFERRABLE unique tolerates the transient collision. Reproduced my exact repro (move option UP into occupied slot + rename + add) live through the RPC → no unique violation, codes preserved; `prosecdef=f` (INVOKER); published-item reconcile still blocked by `guard_published_structure` (verified). Coverage: pgTAP `61_option_reconcile` 9 assertions (exact repro + codes preserved + atomic add/remove/reorder + kept-row update code-frozen + HC013 dup), **suite 1180/1180**; E2E **NORM-5** (edit-dialog reorder existing options into occupied slot → persists + codes preserved + survives reload + condition-on-reordered-code resolves + publishes). Fix diff scoped to reconcile+RPC+tests; evaluator/vectors byte-for-byte unchanged. **All other dimensions PASS** (original review): RLS byte-identical to `form_items`/`answers` mirrors; immutability guards sound; evaluator non-drift verified; clone copies codes verbatim; publish + template validators assert option-code existence; dashboards by code + current label; caught+fixed a real `case_phase_answer_map` bug; full wishlist delivered, translations deferred. FMN-001 (PGRST201) + FMN-002 fixed in-loop. Cleared for human approval + BE-7 squash. <!-- superseded original CHANGES-REQUESTED detail below; kept for trail --> ~~1 MAJOR, 0 BLOCKER. **MAJOR-1:** reordering an EXISTING choice item's options fails with a duplicate-key error — `reconcileOptionRows` (`src/lib/forms/actions.ts:931`) updates each kept row's `position` in a SEPARATE transaction, but `form_item_options_item_id_position_key` is DEFERRABLE-but-not-INITIALLY-DEFERRED, so moving an option UP collides with the row still holding that slot (reproduced live on the real table). `updateItem`→`MESSAGES.generic`, reorder silently NOT persisted. Uncovered by E2E (NORM-1/4 reorder only via the ADD/batch-insert path; NORM-1 edit only renames). pt-BR error, no raw leak, no RLS/immutability/corruption. Fix = single-txn reorder (INITIALLY DEFERRED / bulk update / two-phase bump / delete+reinsert preserving code) + an edit-dialog reorder E2E. **Everything else PASS:** both new tables' RLS byte-identical to `form_items`/`answers` mirrors; immutability guards (published options + submitted selections) sound; **evaluator non-drift verified byte-for-byte** (`conditions.ts`+vectors empty diff, no eval fn redefined, `answer_map`/`answer_map_by_item` rebuild correct shapes, pgTAP 21 proves the unchanged evaluator agrees); clone copies codes verbatim; publish + template validators now assert option-code existence; dashboards group by code + resolve current label; caught+fixed a real `case_phase_answer_map` bug (recommendation/result engine was blind to choice answers); full author wishlist delivered (label/color/score/analytics_code/ordering/stable code+UUID), translations correctly deferred. FMN-001 (PGRST201) + FMN-002 (sign-off review) already fixed in-loop.~~ |


## Collapsed one-line index — concluded features (rotated from PROGRESS.md 2026-08-06)

> Verbatim one-line rows from the live QA Verdicts table (Phase 0 → FF-4, incl. superseded
> ~~struck~~ rounds — they record which phases looped). This index is NOT restated rationale:
> the full analysis lives only in each linked docs/reviews/ file. The section exists so the
> feature-name → review-file mapping survives rotation out of the live file. Newer verdicts
> stay in PROGRESS.md until their milestone rotates.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **FF-4** — Power Authoring (ADR 0092 + Amendments 1–2) | ✅ APPROVED | 2026-08-03 | [review](../reviews/phase-FF-4-review.md) |
| **FF-5** — Entity Reference (ADR 0091 + Amendments 1–2) | ✅ APPROVED | 2026-07-28 | [r2](../reviews/phase-FF-5-review.md#ff-5--qa-review-r2) |
| ~~**FF-5** — Entity Reference (ADR 0091 + Amendments 1–2)~~ | ⛔ CHANGES REQUESTED | 2026-07-28 | [review](../reviews/phase-FF-5-review.md) |
| **FF-3** — Validation Engine (ADR 0090 + Amendments 1–4) | ✅ APPROVED | 2026-07-28 | [r2](../reviews/ff-3-review.md#ff-3--qa-review-r2) |
| ~~**FF-3** — Validation Engine (ADR 0090 + Amendments 1–3)~~ | ⛔ CHANGES REQUESTED | 2026-07-28 | [review](../reviews/ff-3-review.md) |
| **FF-2** — Matrix & Risk Matrix (ADR 0089) | ✅ APPROVED | 2026-07-27 | [r2](../reviews/ff-2-review-r2.md) |
| ~~**FF-2** — Matrix & Risk Matrix (ADR 0089)~~ | ⛔ CHANGES REQUESTED | 2026-07-27 | [review](../reviews/ff-2-review.md) |
| **FF-1** — Repeating Groups (ADR 0087 + Amendment 1) | ✅ APPROVED | 2026-07-27 | [review](../reviews/phase-FF-1-review.md) |
| S5 · **ETH·E3a** — Ethics terminology/UX surfacing (ADR 0064 D4 / 0072 / 0073) | ✅ APPROVED | 2026-07-27 | [review](../reviews/phase-E3a-review.md) |
| **Case Correction Lifecycle** (ADR 0085, branch `case-corrections`) | ✅ APPROVED | 2026-07-24 | [review](../reviews/case-corrections-review.md) |
| **Controlled-Document Redesign** (Phase 17 v2, ADR 0081) | ✅ APPROVED | 2026-07-21 | [review](../reviews/document-control-redesign-review.md) |
| S4 · **CH** — Committee Charters & Meeting Cadence (ADR 0080; Phase 21) | ✅ APPROVED | 2026-07-20 | [review](../reviews/phase-CH-review.md) |
| RV2 · **R2–R5** referral governance (triage/SLA · resolution lifecycle · assignment | ✅ APPROVED | 2026-07-19 | [review](../reviews/rv2-r2-r5-review.md) |
| AUTHZ · **AUDIT-DOOR-BLINDNESS P0** (ADR 0078 §7.14 / ADR 0079) | ✅ APPROVED | 2026-07-18 | [review](../reviews/authz-door-audit-p0-review.md) |
| S4 · ETH·E2 — Ethics disciplinary procedure (ADR 0073; 0078-reconciled) | ✅ APPROVED | 2026-07-18 | [review](../reviews/eth-e2-review.md) |
| AUTHZ · **Gate 2** (Stage C · F1 · N1) | ✅ APPROVED | 2026-07-17 | [review](../reviews/authz-gate-2-review.md#re-review-2026-07-17--verdict--approved) |
| ~~AUTHZ · **Gate 2** (round 1)~~ | ⛔ CHANGES REQUESTED | 2026-07-17 | [review](../reviews/authz-gate-2-review.md) |
| AUTHZ · Stage B — `case_access → case_access_grants` hard cut (B1→B5) | ✅ APPROVED | 2026-07-16 | [review](../reviews/authz-b-series-review.md) |
| AUTHZ · Exclusion Perimeter (U1+U2) — the hard deny at every door | ✅ APPROVED | 2026-07-16 | [review](../reviews/authz-exclusion-perimeter-review.md) |
| AUTHZ · A4 — org admin ceases to be a Case Content source | ✅ APPROVED | 2026-07-16 | [review](../reviews/authz-a4-review.md) |
| AUTHZ · A2 — the capability resolver (`_case_caps` + projections) | ✅ APPROVED | 2026-07-16 | [review](../reviews/authz-a2-review.md) |
| AUTHZ · M6 — `cases.visibility_policy` guarded door (ADR 0078 A1/A27; PO Q1–Q4) | ✅ APPROVED | 2026-07-16 | [review](../reviews/authz-m6-review.md) |
| AUTHZ · M5 — defect ③: the `is_active` outer gate | ⛔ CHANGES REQUESTED | 2026-07-15 | [review](../reviews/authz-m5-review.md) |
| AUTHZ · M3 — defect ① narrowing: assignment ⇏ PHI | ✅ APPROVED | 2026-07-15 | [review](../reviews/authz-m3-review.md) |
| AUTHZ · M2 — A30 bucket C: platform_admin loses PHI | ✅ APPROVED | 2026-07-15 | [review](../reviews/authz-m2-review.md) |
| AUTHZ · M1 — Exclusion durability (ADR 0078 Gate 1) | ✅ APPROVED | 2026-07-15 | [review](../reviews/authz-m1-review.md#re-review--the-b1b2-delta) |
| ~~AUTHZ · M1 (round 1)~~ | ⛔ CHANGES REQUESTED |  | [review](../reviews/authz-m1-review.md) |
| AUTHZ · A0 — Catalog-driven capability inventory | ✅ APPROVED |  | [review](../reviews/authz-a0-inventory-review.md#v3-review--the-final-a0-round) |
| S3·ETH·E1 — Ethics access spine + m2 gate release (ADR 0072) | ✅ APPROVED | 2026-07-14 | [review](../reviews/phase-ETH-E1-review.md) |
| AI track — Action-Items Satellites + Cross-Link UI (ADR 0050) | ✅ APPROVED | 2026-07-14 | [report](../reviews/phase-AI-review.md) |
| AI track — BE-6·N reminder→N scan arm (delta; ADR 0076 × 0050) | ✅ APPROVED | 2026-07-14 | [report](../reviews/phase-AI-review.md#be-6n-delta-review--remindern-scan-arm-2026-07-14) |
| S2·RV2·R1 — Referrals v2: dialogue core (ADR 0037 Amendment 1) | ✅ APPROVED | 2026-07-14 | [report](../reviews/rv2-r1-referrals-review.md) |
| S2·IV2 — Interviews v2: sessions + reporting/confidentiality (ADR 0070) | ✅ APPROVED | 2026-07-13 | [report](../reviews/iv2-interviews-review.md) |
| S1·MEM — Single `memberships` collapse (ADR 0071/0075; S0 §I) | ✅ APPROVED | 2026-07-13 | [report](../reviews/memberships-collapse-review.md) |
| F3 — Flexible-Forms Foundation (ADR 0060/0065) | ✅ APPROVED | 2026-07-12 | [report](../reviews/phase-F3-review.md) |
| F2 — Centralized Attachments (ADR 0063) | ✅ APPROVED | 2026-07-11 | [report](../reviews/phase-F2-review.md) |
| F1 — Case-Participants E0 (ADR 0064/0066) | ✅ APPROVED | 2026-07-10 | [report](../reviews/phase-F1-review.md) |
| Meeting actual-occurrence time `held_at`/`held_end` (ADR 0062) | ✅ APPROVED | 2026-07-08 | [report](../reviews/meeting-held-time-review.md) |
| Administrativo delegated-capability role (ADR 0061) | ✅ APPROVED | 2026-07-08 | [report](../reviews/administrativo-review.md) |
| Form-Builder Enhancements batch (Departments · Flagged/aggregate results · Others · w | ✅ APPROVED | 2026-07-07 | [report](../reviews/adjustments-batch-review.md) |
| 17 — Controlled-Document Lifecycle (Gestão de Documentos Controlados) | ✅ APPROVED | 2026-07-06 | [report](../reviews/phase-17-review.md) |
| 15 — Quality Indicators (Indicadores de Qualidade) | ✅ APPROVED | 2026-07-06 | [report](../reviews/phase-15-review.md) |
| Pre-Pilot DB Hardening — Wave 2 (WS-6 perf sweep: P2/P3/P4/P5) | ✅ APPROVED | 2026-07-05 | [report](../reviews/pre-pilot-hardening-wave2-review.md) |
| Pre-Pilot DB Hardening — Wave 1 (C-1…C-6, H-8; D1/D2/D4/D6-flip/D7/D9; P1/P9/P10) | ✅ APPROVED | 2026-07-05 | [report](../reviews/pre-pilot-hardening-wave1-review.md) |
| Phase B — NSP-per-hospital + `nsp_org_admin` (ADR 0052) | ✅ APPROVED | 2026-07-03 | [report](../reviews/nsp-per-hospital-review.md) |
| Phase A — Hospital-admin tier, 4-tier audit & committee titles (ADR 0051) | ⛔ CHANGES REQUESTED | 2026-07-03 | [report](../reviews/hospital-admin-tier-review.md) |
| Action-Items Fold + `visibility_scope` + Case-Access Expiry (ADR 0050) | ✅ APPROVED | 2026-07-02 | [report](../reviews/action-items-fold-review.md) |
| Shared (non-PHI) `action_items` table (Option A) | ✅ APPROVED | 2026-07-02 | [report](../reviews/shared-action-items-review.md) |
| Member Overview & "Meus itens de ação" | ✅ APPROVED | 2026-07-02 | [report](../reviews/member-overview-action-items-review.md) |
| User Registration & Identity Management | ✅ APPROVED | 2026-07-01 | [report](../reviews/user-registration-review.md) |
| ad-hoc-narratives | ✅ APPROVED | 2026-07-01 | [report](../reviews/ad-hoc-narratives-review.md) |
| answer-model-v2 | ✅ APPROVED | 2026-07-01 | [report](../reviews/answer-model-v2-review.md) |
| form-model-norm | ✅ APPROVED | 2026-07-01 | [report](../reviews/form-model-normalization-review.md) |
| result-rec | ✅ APPROVED | 2026-06-26 | [report](../reviews/result-rec-review.md) |
| NSP-per-org | ✅ APPROVED | 2026-06-25 | [core](../reviews/nsp-per-org-a-review.md) |
| Multi-Tenancy | ✅ APPROVED | 2026-06-25 | [report](../reviews/multitenancy-review.md) |
| Form Builder Enhancements | ✅ APPROVED | 2026-06-23 | [report](../reviews/form-builder-enhancements-review.md) |
| `case_phase_results` | ✅ APPROVED | 2026-06-23 | [report](../reviews/phase-results-review.md) |
| 23 — Patient Identity | ✅ APPROVED | 2026-06-22 | [report](../reviews/phase-23-review.md) |
| `case_patient` (ADR 0038) | ✅ APPROVED | 2026-06-22 | [report](../reviews/case-patient-review.md) |
| 22 — Inter-Committee Referrals | ✅ APPROVED | 2026-06-21 | [report](../reviews/phase-22-review.md) |
| PHI/HIPAA Remediation (WS0–E) | ✅ APPROVED | 2026-06-20 | [report](../reviews/phi-remediation-review.md) |
| 14b–14d — Triage/RCA/CAPA | ✅ APPROVED | 2026-06-18 | [report](../reviews/phase-14-review.md) |
| 14a — NSP Foundation | ✅ APPROVED | 2026-06-18 | [report](../reviews/phase-14a-review.md) |
| 13 — Audit Trail | ✅ APPROVED | 2026-06-18 |  |
| 12 — Case Timeline | ✅ APPROVED | 2026-06-16 | [report](../reviews/phase-12-review.md) |
| 11 — Interviews | ✅ APPROVED | 2026-06-15 | [report](../reviews/phase-11-review.md) |
| 10 — Meetings | ✅ APPROVED | 2026-06-15 | [report](../reviews/phase-10-review.md) |
| Case Access Control | ✅ APPROVED | 2026-06-19 | [report](../reviews/case-access-control-review.md) |
| Case Narratives | ⛔ CHANGES REQUESTED | 2026-06-19 | [report](../reviews/case-narratives-review.md) |
| Case data-model (D1–D15) | ✅ APPROVED | 2026-06-14 | [report](../reviews/case-model-adjustments-review.md) |
| Cases-Extras (R1–R5) | ✅ APPROVED | 2026-06-14 | [report](../reviews/cases-extras-review.md) |
| 8 — Dashboards | ✅ APPROVED | 2026-06-14 | [report](../reviews/phase-8-review.md) |
| 7 — Multi-Phase Cases | ✅ APPROVED | 2026-06-13 | [report](../reviews/phase-7-review.md) |
| 6 — Sign-offs | ✅ APPROVED | 2026-06-13 | [report](../reviews/phase-6-review.md) |
| 5 — Wizard | ✅ APPROVED | 2026-06-13 | [report](../reviews/phase-5-review.md) |
| 4 — Form Builder | ✅ APPROVED | 2026-06-12 | [report](../reviews/phase-4-review.md) |
| 3 — Admin/Users | ✅ APPROVED | 2026-06-12 | [report](../reviews/phase-3-review.md) |
| 2 — Auth & Shell | ✅ APPROVED | 2026-06-12 | [report](../reviews/phase-2-review.md) |
| 1 — Schema/RLS | ✅ APPROVED | 2026-06-12 | [report](../reviews/phase-1-review.md) |
| 0 — Scaffolding | ✅ APPROVED |  | [report](../reviews/phase-0-review.md) |

## Verbose ACT rows (rotated from PROGRESS.md 2026-08-10)

> The four ACT rows in their original verbose form, kept because the live table was cut back to the
> documented one-line shape. The **STAGE 3** row is the reason this section exists: it records three
> lead errors r2 caught (a break-glass citation that would have been green against a *broken* fix; a
> keystone assertion whose message claimed more than its expression checked; an overstated "expiry
> semantics unchanged") — that content is lead commentary and is **not** in
> `docs/reviews/act-as-stage-3-review.md`.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **ACT** — act-as role assumption, **STAGE 4 review** (ADR 0106 D14 + ADR 0107) | ✅ **APPROVED** — 0 BLOCKER, 0 MAJOR, 6 MINOR, 3 INFO. The three claims worth doubting each survived **re-derivation by property**: (a) *no `_case_caps` arm unclassified* — I counted the 11 `v_caps` assignment sites mechanically and enumerated every `app.*` callee, so completeness is a property, not a list; the audit also covers the layers above the resolver (`has_case_capability`/`case_capabilities`/the 4 projections add **no** arm). (b) *`319`'s twins can fail* — I neutralized each mutation (gave the replacement body its condition back) and both went **RED** (A5 64≠111, A7 64≠66). (c) *the sweep can find something* — self-test 4/4 reproduced, and I proved the allowlist contract exits 1 on **both** a new finding and a ghost. All 4 allowlist rulings **UPHELD**, each premise catalog-checked first (`session_context` own-rows-only · `assume_role` single-role scoped + audited · `memberships_select` 5 arms/1 raw with the other 4 hat-bound · `service_role` unkeyable, since I showed a ghost breaks every run). Migration diff **empty** ⇒ the 0079 diff-scoped sweep is genuinely not owed. Lead's blast-radius correction verified from the hook body (D11's derive is inside it ⇒ no hook = no claim for **anyone**). Top finding **MINOR-2**: the sweep self-tests only the FUNCTION path — the cross-table **policy** branch had zero evidence it can fire; I transcribed the walker and proved it correct, but that proof is not a standing artifact (needs an ST5). Also MINOR-1 (scope claims 3 carried MINORs, ships 1; S3 MINOR-2's record correction still undone at `docs/progress/act-as-role-assumption.md:175`), MINOR-3/4 (**D6 enumerates 4 members of a 6-member class**; **D5's "sees what a stranger sees" is literally false** — hatless measures 30, a stranger 0), MINOR-5 (`session_context`'s allowlist entry has the same whole-object-vs-one-arm gap the lead patched for `memberships_select`), MINOR-6 (a hatless PHI read's audit row has **no** `acting_as` key — Rule 11 met, but absence is ambiguous 3 ways). **A13/D5×D6 ruling for the PO: keep the as-built behaviour** — D5's security content is fully delivered (all 5 role arms read 0), an ACL grant names a *person* so no hat can own it, `is_active`+recusal still zero everything, and the PHI bit is narrower than the role arm that confers the same bit. Fix the prose (MINOR-3/4), not the behaviour. | 2026-08-10 | [act-as-stage-4](../reviews/act-as-stage-4-review.md) |
| **ACT** — act-as role assumption, **STAGE 3 build review** (ADR 0106) | ✅ **APPROVED (r2)** — r1 was ⛔ CHANGES REQUESTED on **1 BLOCKER + 2 MAJOR**. BLOCKER-1: `app.is_admin_for` was a **hat-blind CALLER gate on the membership-grant door** — classified a third-party helper from its SIGNATURE SHAPE, but both callers bind `p_actor` to `(select auth.uid())`, so a platform_admin in any other hat could seat an org_admin/hospital_admin (D11's exact escalation). MAJOR-1: the same class on `can_manage_professional`'s raw expired arm (10 Class-2 write RPCs). Both fixed in `20260918002800` + red-first keystone `318`. **r2 re-derived the population BY PROPERTY rather than accepting the two-instance fix** — balanced-paren argument extraction over 928 functions + transitive caller-bound propagation through the call graph: 61 caller-bound `(callee,param)` pairs, 51 hat-gated, 10 correctly hat-free, **no third member** — so the CLASS is closed, not just the instances. r2 also caught three lead errors, all corrected: a break-glass citation that would have been green against a *broken* fix (service door / service client / third-party arm), a keystone assertion whose message claimed more than its expression checked, and an overstated "expiry semantics unchanged". MAJOR-2 (LGPD Art. 18 erasure has no UI route — authorized set and reachable set are **disjoint**) accepted as NOT blocking S3, promoted to a **pilot-gate check** (*Remaining pre-pilot work* item 0). Method lesson placed durably: `authz-handoff.md` §7.17 + ADR 0079 Amendment 6. | 2026-08-10 | [act-as-stage-3 (r1+r2)](../reviews/act-as-stage-3-review.md) |
| **ACT** — act-as role assumption, PRE-BUILD plan review (ADR 0106) | ✅ **APPROVED (pre-build, r2)** — `d80d4a3` (docs-only; nothing built yet) closes **all 9** r1 findings, re-verified against the actual diff plus a fresh live-catalog check where a fix asserted a number (127 `app` boolean gates and the `can_manage_professional` 10-caller count both re-confirmed unchanged). Both BLOCKERs closed: Stage 2 now covers **8** direct `memberships` readers incl. `can_manage_professional`, with the sweep boundary re-specified as the PROPERTY (comment-stripped direct read, no `has_role*` call, over all ~127 `app`/`public` boolean gates) rather than one more name on a list — closing the root cause, not just the symptom; Stage 0's enum gains `'platform_admin'` alongside `memberships_role_check`'s values, with the D11/audit-stamp consequence threaded. All 4 MAJORs closed: ADR census corrected to 127/80 **with a second-correction provenance note** (the first fix attempt itself mis-published the outside-prefix count as the total — named, not silently overwritten); `session_context`'s `has_role_any` claim corrected (comment-only regex-hit trap named) and plan §1 reworded from an action to a no-op confirmation incl. a re-diff-the-comment step; the `profiles` co-member RLS arm gets an explicit Stage-3 raw-`pg_policies` sweep bullet with a sound fix shape (caller side hat-aware via `is_member_of`, target side unchanged) and a sibling-arm disposition rule; Stages 0/1 gates now name `ARM=census`+`ARM=floor` per 0079 Amendment 1. Both INFOs (permissive-sibling keystone guard; page.tsx role→route table) incorporated verbatim. 1 residual non-blocking note for the Stage 3 reviewer: confirm the co-member fix's final predicate calls `is_member_of(them.commission_id, auth.uid())` (or equivalent) rather than a looser approximation. No PO decision re-litigated. r1 verdict + full findings below. | 2026-08-09 | [act-as-plan (r2)](../reviews/act-as-plan-review.md#round-2--re-review-of-the-d80d4a3-fixes) |
| ~~**ACT** — act-as role assumption, PRE-BUILD plan review, round 1~~ | ⛔ **CHANGES REQUESTED (pre-build, r1)** — 2 BLOCKER: **(1)** `app.can_manage_professional` is an 8th direct `memberships` reader (`m.role = 'staff_admin'`, bypasses `has_role`), outside Stage 2's enumerated "7 direct readers" and outside the `is_*` prefix boundary both censuses used — gates 10 write RPCs (professional-profile + ethics-vocab + case-assignment-role management) that will stay hat-independent after cutover, D13's own "fails OPEN, looks normal" shape recurring on a second, un-enumerated door. **(2)** Stage 0's enum-derivation instruction ("source: `memberships_role_check`") omits `platform_admin` — confirmed absent from the live CHECK constraint — yet D11 requires it be representable for the implicit break-glass derivation and the `metadata.acting_as` audit stamp. 4 MAJOR: the ADR's own "measured, not assumed" boolean-gate census undercounts the live catalog (127 `app` boolean gates, not 80; 80 outside `is_*`, not 33 — the gap that hid finding 1); ADR §3's claim that `public.session_context` calls `app.has_role_any` is false (it independently duplicates the filter, comment-only "verbatim" claim never verified) so Stage 3's "rewired OFF `has_role_any`" task rests on a wrong premise; an RLS arm on `profiles` (`profiles_select_self_or_admin`'s co-member visibility) joins `memberships` directly with no role predicate, unreached by any stage, leaking membership-adjacent visibility across a suppressed hat (D4); Stages 0/1's gate bullets omit `ARM=census`/`ARM=floor` (CLAUDE.md §6 step 1, the exact omission ADR 0079 Amendment 1 was written to close). 1 MINOR (D12's "~30 functions already use this `current_setting` pattern" — actually 1, `is_admin`; 32 others read unrelated RPC-context GUCs) + 2 INFO (revert-twin keystone well-specified, note the permissive-sibling guard explicitly; page.tsx's 8-branch precedence → picker mapping not enumerated). The `is_*` census itself (47 total, 12/2/7/14/12 split, the 7 named readers) verifies **exactly** against the catalog; storage policies, `proxy.ts`, `has_role`/`has_role_any`/`member_can`/`_case_caps` bodies, and the JWT-claims consumption path all verified clean. No PO decision needs re-litigating — all fixes are plan/ADR completeness corrections before Stage 0 starts. | 2026-08-09 | [act-as-plan (r1)](../reviews/act-as-plan-review.md) |

## Collapsed one-line index — rotated 2026-08-10 (18 rows: QO·B → Phase 16)

> Verbatim rows from the live QA Verdicts table, rotated when the live section was cut back to the
> ACT milestone. Same rule as the 2026-08-06 index above: this is not restated rationale — the full
> analysis lives only in each linked docs/reviews/ file.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **QO·B** — org_admin/hospital_admin content wall | ✅ **APPROVED (r2)** — *(recorded by the lead from QA's committed r2, `93b0dfb`; QA's review file is authoritative)* BLOCKER-1 **closed and re-proved on the reviewer's own fixtures**: correspondence re-derived from the inventory §4.4 TEXT (not M7's array) — 27 CUT-side names armless + all present (non-vacuity), 5 ratified KEEPs still armed, `case_events` policies 0; M7's postcondition enumerates NAMES with an existence check and its regex is bare (matches `_for`). Behaviourally the r1 polarity is **inverted**: both tenancy tiers now stop at the authority code (`remove_case_participant` HC0E4/NOT-REMOVED · `record_recusal` P0002/0 rows · `case_viewer_capabilities` `can_manage_lifecycle:false`) while the coordinator twin reaches the later gate. MAJOR-1 closed **and r1's four-door set narrowed correctly**: only `cancel_case`/`close_case` had the silent-success shape (RLS-exempt `commission_of_case` lookup) — verified via a manufactured excluded-`staff_admin` instance, all four raise P0002 with a clean-coordinator control; backend's two declined-as-vacuous red-proofs judged honest (`314` §11d pins the denial, only mechanism attribution unproven). Keystones spot-neutralized independently (`remove_case_participant` + `case_viewer_capabilities` → 7 tests RED incl. both Q4 hospital_admin arms + the correspondence invariant; restore byte-identical, 0 residual). pgTAP independently reproduced **175f/5616/PASS**; no over-cut (93→73 token carriers = exactly M7's 20 edits); the `229` re-anchor's lost positive property re-homed to `314` 11.25 (verified); findings file back at 393 lines. **BUG-QOB-002 re-closure CONFIRMED.** Two self-corrections recorded: r1's MAJOR-1 was one measurement too broad; r1's `lift_recusal` INFO was the reviewer's own fixture bug (id resolved under the tested role → NULL passed into the door — *a probe whose fixture lookup runs under the role being tested measures the fixture, not the gate*). NEW MINOR-3 (stale Phase Status row + gate-evidence block) fixed by the lead in this same commit wave. | 2026-08-09 | [r2](../reviews/phase-QO-B-review.md) |
| ~~**QO·B** — round 1 (§4.4 CUT list unexecuted)~~ | ⛔ **CHANGES REQUESTED (r1)** — **1 BLOCKER: the ratified §4.4 case-plane CUT list was never executed, and BUG-QOB-002 is recorded CLOSED while still live.** M4 cut a **proxy population** — its header asserts *"the case-content mutators are exactly the functions carrying A4-Unit-2's exclusion guard"*, which is **false**; ~17 §4.4 doors carry no `assert_not_case_excluded` and so were never in the population. **MEASURED** on a fresh reset under `set local role authenticated`, fixture asserted real (`is_commission_admin_of_for`=t, `is_staff_admin_of_for`=f, **`can_read_case`=f**): `remove_case_participant` **ADMITTED, `removed_at` SET** · `record_recusal` **wrote `case_recusals` 0→1** · `set_case_participant_role`/`add_case_participant`/`schedule_ethics_hearing` pass authority and reach a LATER gate while the **`orgadmin.b` control is stopped at the authority code** (HC0E4/HC0J1) · `case_viewer_capabilities` answers **`can_read:false` + `can_manage_lifecycle:true`**. Reproduces at the **hospital_admin** tier (Q4 unfulfilled symmetrically). **Every gate was blind for a nameable reason:** M4's postcondition validates the proxy's SIZE not its CORRESPONDENCE to the ratified list; `b1` 31/31 is true but its case list inherited the same wrong population; `314` has no case-door keystone; the A/B matrix sees tables and `cases` was already A4-walled. **This is §6.3 recurring one level up** — the "re-read the CUT list against the catalog" check that produced M5/M6 was run on the response + document planes and **never on §4.4**. **MAJOR-1:** `close_case`/`cancel_case`/`set_case_outcome`/`update_case_narrative_body` are INVOKER — authority admits, RLS stops the DML, so they **return SUCCESS while writing nothing** (`close_case` also skips the HC031 gate that stops the real coordinator). ⚠ My own first probe ran as `postgres` (RLS-exempt) and over-reported these — **the role is the measurement**. **2 MINOR** (masked-but-live tenancy arms on `case_events`/`get_case_detail`/`list_my_cases`; **B.14's gate artifacts are no longer reconcilable — `spec-counts.txt` was overwritten by the scoped PDF re-runs**, the same "partial sweep overwrites the findings file" hazard, on the E2E gate) + **2 INFO** (`bulk_create_cases`/`create_case_from_template` and `lift_recusal` **unmeasured**, not cleared). **VERIFIED SOUND, re-proved not read:** M1–M3 table cuts clean in `pg_policies` (`responses_admin_all` gone; `is_member_of` is commission-scope only, so the doc/measurement policies do NOT readmit) · M5 doors `dashboard_free_text` **0 / 6** twin, export 0/6, completion 0/2 · M6 `list_commission_documents` 0/2, `documents_due_for_review` 0/1 · **storage clean (self-audit ④ closes)** · **B.8 keystones non-vacuous — I neutralized `list_commission_documents` + `supersede_response` myself, `314` tests 45+51 went RED, restore byte-identical, residual-mutation check 0** · **hospital_admin parity MEASURED (self-audit ③ closes on the walled surfaces)** · no over-cut on KEEP (17/13/1) · session contract flag-not-role with no stale consumer · guard routing splits mixed files per-function, **no CUT action gained a tenancy arm** · UI allowlist is fail-closed, `role !== null` hardened, Q3 split withholds at the READ · **BUG-QOB-004's catalog claims verified accurate** · J1c honestly provisional. | 2026-08-09 | [qo-b](../reviews/phase-QO-B-review.md) |
| **PDF·P2** — PDF document printing, Meetings (ata) | ✅ **APPROVED (r2)** — Package A wave verified by **re-probing, not by reading**. **BLOCKER-1 CLOSED and independently re-proven:** the seed contains no persona with `reach=t ∧ full_sight=f`, so t40 could not be confirmed from outside the test txn and a never-firing conjunct would have closed the blocker vacuously — I therefore built the class myself in a **rolled-back transaction** (4-row participants chain making `chefe.ccih`, a full member who reaches the meeting, the `respondent_doctor` of the linked case): `reach` **stays t** while `full_sight`→**f** and the arm→**f**. `app.can_read_full_meeting_content` diffed term-by-term against the LIVE `_project_meeting_agenda_item`: respondent term mirrors exactly; capability term adds a **presence guard** (declines to refuse where masking would null nothing — more permissive only where no content is at stake); unlinked items contribute nothing. Explicit-uid DEFINER, `search_path=''`. **MAJOR-1 CLOSED** by A8's presence-derived label — all four catalog-`PHI-BEARING` columns covered plus the process-number term; t45/t46 pin `phi/` bifurcation, t50 the clean case, **t49 that `form_response` still refuses `contains_phi=true`**. Notably **A8's correctness DEPENDS on A7** (presence-testing is only meaningful because the provider now always runs unmasked) — reasoned explicitly in-code, not a coincidence. **All 7 MINORs closed**, incl. the revoke asymmetry pinned in **both** directions (t52–t54) with the D11 citation, so it is now a decision. Kind-conditional sites re-counted from the live door body = **exactly 3** (A8 trio supersedes "exactly 2"; a 4th is the leak signal). Property diff vs my own r1 baseline byte-identical across three re-emits; `lookup_` still service_role-only. Lint 0 · tsc 0 · 22 unit tests. Keystones non-vacuous: t39 pins the participants chain is real, t40 asserts the predicate **discriminates**, **t42 is the over-grant twin** (same persona passes on the un-linked meeting — the assertion that makes t41 mean its label). **2 new INFO:** helper is fail-open **standalone** (`not exists` over zero agenda rows → `t`; safe only because `can_reach_meeting` is the first conjunct — P3/P4 must not reuse it alone) · `template-fingerprints.ts` still calls `final_signed` the "quorum line" variant after MINOR-6 set `quorum: null`. Carried forward unchanged: FUP-PDF-1..4, BUG-PDF2-002. **r1 baseline below.** | 2026-08-08 | [pdf-p2](../reviews/phase-PDF-P2-review.md) |
| ~~**PDF·P2** — round 1~~ | ⛔ CHANGES REQUESTED (r1) — **the plan §3 binding question is answered YES: P2 stayed inside provider + template + arm + tests, and the P1 abstraction absorbed a second kind with no pipeline surgery.** Both lead rulings re-derived independently, not read: the mint door has **2** semantic kind-conditional sites (every other `p_source_kind` is a pass-through arg or a column comparison), and BUG-PDF2-001 is **copy-only** (`sourceKind` was already a prop; the sole structural edit is a JSX rewrap). Property-diff vs my own P1-recorded baseline: `prosecdef`/`proacl`/`proconfig` **byte-identical** on both re-emitted functions. Meeting arm is one-line delegation to `app.can_reach_meeting` (explicit-uid DEFINER, `search_path=''`, no `auth.uid()` inside); `case`/`interview` have **no arm** and fall to the fail-closed ELSE — stronger than literal-false. I probed the NULL-propagation fail-open (`if not NULL` skips the raise): nonexistent meeting returns **false, not NULL** ✅. No-admin-arm delta proven **behaviourally on seed data** — the same org_admin is denied `meeting`, passes `form_response`, and `is_commission_admin_of_for` is genuinely true, so the denial is the missing arm and not a powerless persona. P1's 2 form fingerprints + version byte-identical to `main`. **BLOCKER-1:** the ata bakes in the **minter's** per-caller projection (`app._project_meeting_agenda_item` masks title for a case respondent and the 3 free-text fields without `read_case_deliberation`) and then re-serves it under `can_reach_meeting` = *member ∧ (commission_default ∨ attendee)*, which has no capability or respondent term — and the masked columns have **no SELECT grant to `authenticated`** at all, so the projection is the real boundary, not a UI nicety. A respondent who is a commission member downloads his own process number. Violates D11's load-bearing sentence; untested by 312/313/E2E. Not a P2 authoring defect — it is the first kind the P1 abstraction cannot fully describe, which is exactly what §3's question exists to surface. **MAJOR-1:** `minutes_md` + the 3 agenda free-text columns are catalog-classed `PHI-BEARING (Rule 11/12)` yet the payload hardcodes `containsPhi:false` → `std/` storage, no non-suppressible band; `answers`/`responses` carry **zero** such comments, so meetings really are asymmetric — PO ruling needed per D9's own "upstream ADR" clause. **7 MINOR** (unpinned `hospital_admin` control; revoke-without-sight now asymmetric + untested; t45 type-confused fixture; t30 mislabeled "minter"; watermark derivation duplicated in TSX; 4 unpinned template branches; raw enum key could print onto permanent paper) + **3 INFO**. Sound: Rule 7 **23/23** escaped incl. all 4 `formatDateTime` sites (the P1 MINOR-5 lesson carried forward), FINAL⇔`signed\|distributed` total over 6 catalog statuses and fails safe, signature filter an **allowlist**, Rule 9 clean, UI gate a strict subset of the door, and 313's fingerprint + fail-closed keystones carry real preconditions and bidirectional vacuity controls. | 2026-08-08 | [pdf-p2](../reviews/phase-PDF-P2-review.md) |
| **PDF·P1** — PDF document printing, Forms + full skeleton (ADR 0104 D1–D15) | ✅ **APPROVED (r2)** — fix wave verified by **re-probing, not by reading commits**: both r1 MAJORs CLOSED (purity gate now cuts on the PROPERTY — my r1 escapes `../supabase/server` and `../../queries/…` plus a bare-directory shape all error now, alias form unregressed; fingerprint gained `FINAL_PHI_LOGO` **with a vacuity control** proving canonical and variant cover *disjoint* branches, asserted on markup forms because bare class tokens would be vacuous) and MINOR-4/5/6/7/8/9 CLOSED. Revoke door re-read from `pg_get_functiondef`: not-found merged into the 42501 authority raise, `P0002` gone from the **live** surface, and the re-emit lost **no** property (`prosecdef`/`proconfig`/`proacl` byte-identical — the DROP+CREATE hazard checked, not assumed). t73 pins the one-active index at table level with the derived-path trap correctly threaded; t72 picks the persona that makes the flag-off assertion falsifiable. Migrations 320==320, lint + `tsc --noEmit` exit 0, PDF unit tests 18 pass. **3 MINOR deferred by agreement, still OPEN:** returns-row re-exposure · limiter granularity (+ stale "verbatim" comment) · SQLSTATE-allowlist text mapping. 2 new one-line INFO (dead `'P0002'` allowlist entry; `backend-state.md:1801` still says 69, now 73). **r1 baseline below.** 0 BLOCKER. Every security claim re-derived from the **live catalog**, none from migration text: one policy on `printed_documents` (so no permissive sibling can fake a row-assertion), zero DML grants to `authenticated`, the four withheld columns confirmed, `anon` reach **provably nil** (`has_function_privilege` + `table_privileges` both empty), `printed-documents` bucket private with **zero** storage policies, `lookup_printed_document` `proacl` service_role-ONLY, and the `form_response` arm mirroring the live `responses` read surface term-for-term (incl. `responses_admin_all` being a `FOR ALL` ⇒ read policy) via the correct `_for` helpers. **D11 noun rule proven structurally**, not just by test: a comment-stripped `prosrc` probe shows neither delegated arm (`can_read_correction_response`, `can_access_targeted_response`) nor either admin helper carries an `is_admin`/platform branch. **D8 survives a whole-repo sweep** — 0 `getPublicUrl`, 0 `createSignedUrl` naming this bucket, one column-listed `select`. Rule 7 escaping exhaustive at every payload-string interpolation. pgTAP independently mutation-audited against the §7.1 trap list (personas verified real; t1 **asserts** the flag rather than setting it); E2E has **zero** defensive branching and a real Tab/Enter flow with no `.focus()`. qa re-ran `ARM=census` (**HOLDS**), lint, `tsc --noEmit`, PDF unit tests — all green. **2 MAJOR carried forward, non-blocking, close before P2:** the D14 purity gate is bypassable by *relative* import (red-teamed via `eslint --stdin`: alias form errors, `../supabase/server` passes — an enumeration bounded by a syntax, not the property; nothing violates it today), and the D4 fingerprint fixture pins only the `draft`/no-PHI/no-logo branch, leaving the `final` chip (**the branch most real documents use**), the PHI band and the logo editable without a version bump. Plus 9 MINOR (generic-SQLSTATE allowlist can surface English Postgres text; mint/revoke composite return re-opens the 2 withheld columns; global in-process rate limiter is a DoS lever; revoke checks existence before authority; `format*` fallback unescaped at 4 sites; ADR 0104 unamended for the 4 ratified deviations; one-active index untested; 2 door-coverage gaps; raw reason-class identifier in the pt-BR UI) + 6 INFO. | 2026-08-07 | [pdf-p1](../reviews/phase-PDF-P1-review.md) |
| **QO·FUP** — follow-up close-out (F1–F8; ADRs 0101/0102) | ✅ **APPROVED (r2)** — `6b6f894` closes **R1** at all three record sites with **visible corrections** (not silent rewrites); **FUP-QO-7 re-scoped to the true finding, re-graded PHI-grade, and correctly NOT declared a defect** (blank expiry may legitimately mean "permanent" — needs its own property-bounded caller sweep + PO ruling + an executable pin either way; door untouched). Its new reachability claim re-verified from the catalog (`grant_case_access` / `create_case` / `create_case_from_template` — exactly those three). Recorded root cause is the reusable half: **a fixed-width `substring(… for N)` is a WINDOW, not a delimiter — absence inside it is not absence**, and it failed in the urgency-suppressing direction. **R2** recut onto the property "reaches `app.grant_role_impl`" — doors + SQL tier match my sweep exactly. **R3** both `platform/actions.ts` sites fixed *with the why*, plus a real bonus catch (`pqs/actions.ts` named a `(hospital,user)` PK that does not exist). Lint/typecheck/vitest 1172 re-run green after the `src/` edits. **2 MINOR carried forward, non-blocking:** the recut TS list is short by 3 (`org/actions.ts:238`/`:320`/`:385` — the callers of three SQL functions it does name; 9 → 12, none passes `p_expires_at`, ruling unaffected) + 3 line numbers drifted inside the same commit; and the R3 sweep covered `src/` only, leaving `145_pqs_membership.sql:112` + `224_memberships_collapse.sql:435` — both residuals are the same class as the finding they correct (a property's transitive closure; the union of scoped sweeps). Plus r1's R4–R6 INFO. | 2026-08-07 | [r2](../reviews/qo-fup-review.md#round-2--re-review-of-the-6b6f894-corrections) |
| ~~**QO·FUP** — round 1 (R1 case-door divergence recorded backwards)~~ | ⛔ CHANGES REQUESTED (r1) — **no shipped code needs to change**; every behavioural deliverable re-derived from the live catalog and correct, all gates green on a fresh reset (pgTAP 5371, `ARM=census` + `ARM=floor` hold, f1-seam **6/6**, a2 **12/12** with both controls, vitest 1172, lint/typecheck clean — this also discharges F8's PARKED fresh-reset gates). **R1 MAJOR (blocking):** ADR 0102 + FUP-QO-7 + backend-state.md state as *catalog-verified* that `app._grant_case_access_unchecked` "omits `expires_at`" / "keeps the do-nothing seam" — the catalog shows `expires_at = excluded.expires_at` **present and uncoalesced**, i.e. the opposite and worse: a blank-expiry re-grant **clears** a time-boxed PHI grant to permanent (reproduced through the real door), and FUP-QO-7 as filed points away from it. **R2 MINOR:** the caller sweep carrying the NULL ruling names 3 callers; the real set is 3 doors / 8+ sites (`grant_role_for` and 5 SQL callers unlisted) — conclusion survives, re-swept. **R3 MINOR:** 4 prose sites still assert the pre-F1 `on conflict do nothing`. 3 INFO. | 2026-08-07 | [qo-fup](../reviews/qo-fup-review.md) |
| **QO·A** — Quality-office oversight, Phase A (ADR 0100 D1–D11) | ✅ **APPROVED (r3)** — R1 + R2 closed by M11 (`20260911001000`) at the level that generalises. **R1:** interview family re-derived independently by **FK closure to `case_interviews`** (a structural property, not text) = 8 tables + the `attachments` `'interview'` dispatcher arm — **zero** still route the widened `can_read_case`; behaviourally reviewer **0** / coordinator **1** on both former leaks; the `'case'` arm survives and is guarded by an **inverted postcondition** (fails if it is ever changed). **R2:** 8 lattice assertions present, S3/S4 twins genuinely non-vacuous (each proves the antecedent), all 8 green pre-M11 on a true 312 catalog — pinned an invariant, did not repair a break. **§5.1** is a real derivation with an empty-set twin and mirrored as a migration postcondition. **PO ruling** pinned in the catalog (both `COMMENT ON POLICY`, in-migration so they survive a reset) and by `311` §5.2c, which reds on re-point/delete/rename. **Carried forward, non-blocking:** 1 MINOR (§6's S3/S4 fixtures also hold `staff`, so S5 supplies the deliberation half — the assertions cannot isolate the arm they name; one-line fix, **land before Phase B**) + 2 INFO (§5.1's anchor is a token so DEFINER doors fall outside it; 6.4/6.5 lack positive twins and 6.6 is a marker not a proof) + r1's m3. | 2026-08-07 | [r3](../reviews/quality-office-oversight-review.md#9-round-3--re-review-of-the-m11-closure) |
| ~~**QO·A** — round 2 (R1 interview family incomplete; R2 lattice invariant unpinned)~~ | ⛔ CHANGES REQUESTED (r2) — r1's B1/M1/M2/m1/m2 + the perf record all **CLOSED** by M10 (`20260911000900`) and closed well. **2 new, both in the remediation:** **R1 BLOCKER** — the interview-family cut is incomplete: `case_interview_links_select` and `can_read_attachment`'s `'interview'` arm still route the raw `can_read_case`, so the reviewer reads the interview's **external audio-recording URL** (`external_url` is column-granted; seeded value is a `.mp3`) and its transcript's title while `can_read_interview` is **false** — M10 cut 7 of the family's 8 tables because the enumeration boundary was a *predicate name*, and `311` §5.1's hardcoded table count cannot detect a member never in the list. **R2 MAJOR** — the lattice invariant M8+M9+M10 all rest on ("content without deliberation ⟺ S7") is pinned by no keystone; a future content-conferring arm silently over-cuts ~20 surfaces with LOST ≠ 0 and nothing reds. Consumer enumeration **independently re-derived** (71 fns / 46 policies) — agrees with M10 everywhere except R1; `case_participants` chased and cleared (`is_org_member` needs `commission_id NOT NULL`). §6 disarming sweep: **no further instance.** | 2026-08-07 | [r2](../reviews/quality-office-oversight-review.md#8-round-2--re-review-of-the-m10-remediation) |
| ~~**QO·A** — round 1 (B1 three write doors on `can_read_case`; M1 Class-2 identity; M2 interviews)~~ | ⛔ CHANGES REQUESTED — 1 BLOCKER (B1: D7 breached — `file_correction_request` / `declare_conflict` / `record_recusal` gate on `can_read_case` alone, which S7 satisfies; live-probed reachable, unpinned by any suite, and `308` 1.4 is vacuous w.r.t. them), 2 MAJOR (M1 Class-2 `professional_profiles` reachable vs. D5; M2 interviews DB-open / UI-hidden vs. Rule 1), 3 MINOR | 2026-08-07 | [r1](../reviews/quality-office-oversight-review.md) |
| **MIN** — Meeting audio → generated ata (ADR 0099 D1–D18) | ✅ **APPROVED (r2)** — BLOCKER + both MAJORs closed & independently re-verified (sweep door probed, detector proven able to find an orphan; authz probe re-run unregressed); 3 MINOR open (R1 N3 name ambiguity, R2 undiagnosed click anomaly, R3 duplicated `signCallbackBody`) | 2026-08-06 | [r2](../reviews/min-audio-minutes-review.md#round-2--re-review-of-the-remediation-delta) |
| ~~**MIN** — round 1 (B1 apply never deletes the audio; M1 the O3 sweep does not exist; M2 lazy TTL)~~ | ⛔ CHANGES REQUESTED — 1 BLOCKER, 2 MAJOR, 7 MINOR, 6 INFO | 2026-08-06 | [r1](../reviews/min-audio-minutes-review.md) |
| **AFF** — Hospital affiliation, person identity & the org people directory (ADR 0097 D1–D19 + ADR 0098) | ✅ **APPROVED (r2)** — F1–F7 closed & re-verified; 2 MINOR open (N1, N2) | 2026-08-06 | [r2](../reviews/aff-review.md#8-round-2--re-review-of-the-remediation-delta-86ce0d15b4b1df) |
| ~~**AFF** — round 1 (F1–F7 filed: Rule 10 string, detector blind to named conditions, audit-arm & CPF-probe gaps)~~ | ✅ APPROVED — 6 non-blocking follow-ups, all open at sign-off | 2026-08-06 | [r1](../reviews/aff-review.md) |
| **PCI** — Process/Case Integrity audit remediation (ADR 0095) | ✅ APPROVED (r2) | 2026-08-05 | [r2](../reviews/process-integrity-and-template-versioning-review.md#round-2--re-review-head-f6c847d) |
| **TV** — Process-Template Versioning (ADR 0096 + Amendments 1.1–1.7) | ✅ APPROVED (r2) | 2026-08-05 | [r2](../reviews/process-integrity-and-template-versioning-review.md#round-2--re-review-head-f6c847d) |
| ~~**PCI + TV** — round 1 (BUG-TV-001: dead narrative-slot edit/remove)~~ | ⛔ CHANGES REQUESTED | 2026-08-05 | [review](../reviews/process-integrity-and-template-versioning-review.md) |
| **Phase 16** — Standards Crosswalk & Readiness/Gap Engine v2 (ADR 0093 D1–D10 + Amendments 1–3) | ✅ APPROVED | 2026-08-04 | [review](../reviews/phase-16-review.md) |

## ETH·E4 loop rows (rotated from PROGRESS.md 2026-08-11 at the Record step)

The concluded r3 verdict stays live in PROGRESS.md; these are the struck loop rows and the
pre-build plan review. Full analysis: [eth-e4-review.md](../reviews/eth-e4-review.md).

| Feature | Verdict | Date | Review |
| --- | --- | --- | --- |
| ~~**ETH·E4** — round 2~~ | ⛔ **CHANGES REQUESTED (r2)** — narrow: **P0-1 CLOSED** (verified 4 ways from the catalog; my own divergence oracles prove the `set_eq` keystone reds in BOTH directions; no view or sibling projection door was missed) and MAJOR-3/4/5 + m6(a–d) confirmed. **3 small blockers:** B-1 MAJOR-2(a) is NOT fixed — the throw reaches the action and is discarded at `add-participant-dialog.tsx:589`/`:619`, so a failed search still renders "Nenhum resultado…" and steers to `no_account`; B-2 MAJOR-6's professional lane is uncovered on a premise `pg_proc` contradicts (`create_professional_profile` is a bare INSERT, not get-or-create); B-3 NEW — `cases.ts:1333` silently empties the whole E4 roster. Open items judged: MAJOR-2(b) **not a blocker once B-1 lands** (the scope limit's population is near-disjoint from where the impedimento is load-bearing — verified); m2 **not a blocker**, the P0 fix shrank it. pgTAP re-verified on a fresh reset: **5794/5794** (the short intermediate counts are the E2E-mutated-DB class, void not flaky) | 2026-08-11 | [eth-e4 (r1+r2)](../reviews/eth-e4-review.md) |
| ~~**ETH·E4** — round 1~~ | ⛔ CHANGES REQUESTED (r1) — 1 P0 / 6 MAJOR / 11 MINOR. **P0:** ADR 0108 D5's exposure argument ("discloses no case linkage") is false — `retention_pin_reason='ethics_decision_issued'` + `retention_pinned_at` sit in the table-wide `authenticated` SELECT grant, so the widening tells every org manager that a named doctor was a `respondent_doctor` on a case with an issued decision (measured live: sibling-commission `staff_admin`, 0 rows pre-D5 → 1 row post-D5; live writer, product-reachable via `issue_decision`). Verified CLEAN and stated so: `set_primary_subject`'s deltas are surgical with the M7 tenancy cut preserved, the D5 keystone and all three write-door gates falsify under my own neutralization runs (8 oracles), the `ARM=census` correction is correct, FUP-ETH-CPF-1's latency claim holds | 2026-08-11 | [eth-e4](../reviews/eth-e4-review.md) |
| **ETH·E4** — PRE-BUILD plan review (ADR 0108) | ⛔ CHANGES REQUESTED — 7 findings, all addressed pre-build | 2026-08-11 | [eth-e4-plan](../reviews/eth-e4-plan-review.md) |

<!-- Rotated from PROGRESS.md at the DM2 Record step (2026-08-13): 6 concluded rows,
     VERBATIM (FUP batch 2026-08-12 · ETH·E4 · ACT stages 4/3/plan-r2/plan-r1). -->

| **FUP batch 2026-08-12** (BUG-RDR-001 · FUP-ETH-A11Y-1 m3/m4 · FUP-ETH-KBD-1 · FUP-PDF-3 · narrative reorder 23505 · FUP-F2-BUCKETS · BUG-MIN-E2E-1 · BUG-BOOTSTRAP-001) **+ REG·KIND (ADR 0110), reviewed for the first time** | ✅ **APPROVED (r1)** — 0 BLOCKER; 6 findings, none in scope. Every claim re-derived from the **live catalog** (359 registered == 359 files) + rolled-back probes under `set local role` with real hats, never from migration text. FUP-PDF-3's DROP+CREATE doors diffed **property-by-property** — `prosecdef`/owner/volatility/leakproof/strict/`search_path`/**ACL** all intact, `returns` the only change; the `anon`-no-EXECUTE control is the right ACL-trap guard. Both "corrected premises" independently verified (**4** withheld columns, not 2; constraint was ALREADY `DEFERRABLE INITIALLY IMMEDIATE` — real cause is archived rows retaining a NOT NULL `position`). REG·KIND's table DROP is **clean: 0 orphaned policy/trigger/proc/grant/FK/type/constraint** across a full catalog sweep, 0 source refs. Bucket retirement leaves **0 reachable doors** (general `pg_policies` sweep, not by-name). §2.5's self-promotion-closure claim **tested and TRUE** (refused by `guard_profile_privileged_columns_trg`). ⚠ Two **CONFIRMED-LIVE but PRE-EXISTING** defects surfaced, outside both scopes → **FUP-REF-NOTE-1** (P2: `redact_referral_note`'s `[redigido]` masking is bypassed by 4 sibling DEFINER doors that `RETURNS referral_internal_notes` — proved as a plain `staff` **assignee**: read door serves `[redigido]`, `conclude_…` returns the real `body_md`, same txn/role/hat, plus a Rule 11 read-audit gap; the 2 doors REG·KIND rebuilt are NOT the exploitable ones) and **FUP-CASEEV-KIND-1** (P2: `case_events.kind` guarded only in TS — 16-value CHECK, no trigger, no `kind` arm in either INSERT policy; forged `decision_issued` insert **succeeded** live). Also **FUP-LINT-TEMP-1** (`npm run lint` exits 1 on gitignored-but-not-eslint-ignored `supabase/.temp/**`, so gates 2–5 **never run**; all five verified passing individually — `lint:vacuous` 178/0) + `FUP-PDF-3a` (the "can never diverge" invariant is prose-only; 323 pins today's names, not the composite ≡ GRANT set) + `FUP-RDR-001a` (all 6 alert-dialog sites structurally wired — verified each, and FocusScope's passive-effect timing confirmed in the installed source — but 3 confirm-path restores remain unmeasured, where the documented `isConnected` fall-through bites) | 2026-08-12 | [fup-batch-2026-08-12](../reviews/fup-batch-2026-08-12-review.md) |
| **ETH·E4** — ethics participant seating & professional identity (ADR 0108) | ✅ **APPROVED (r3)** — the 3 r2 blockers verified closed **at the source**, not from commit messages. ⚠ **r2’s MAJOR-1 was WRONG and its remedy would have REGRESSED the UI:** the catalog shows **six doors raise `P0002` deliberately in pt-BR** ("papel inválido", "organização não informada") and the engine cannot reach it (zero `INTO STRICT` in `public`/`app`) — `error.message` KEPT there, the measured reason recorded in-file so it is not re-filed a third time. The real §8 leak was the arm r2 bundled with it: **23514 is MIXED** (measured live — the engine yields `new row for relation "…" violates check constraint "…"`) → constant always; nothing user-visible lost, both pt-BR raises are pre-empted before the RPC. Also closed: the new gate’s **400-byte fail-open** window (measured 0 live — an unanchored grep says 16, all comment mentions), **two vacuous E2E assertions** — one of which my own first fix left **DEAD**, since no caller passed `assertLinkageGating` on that arm — plus **m1** (a pure read published as a Server-Action endpoint without `authorizeOrg`) and **m10**. P0-1 re-verified from the catalog (12/17 granted, projection ≡ granted set). Open items filed as **FUP-ETH-A11Y-1** + **FUP-E2E-SERVER-DEAD-1**; m2/m5 → PO | 2026-08-11 | [eth-e4](../reviews/eth-e4-review.md) |
| **ACT** — act-as role assumption, **STAGE 4** (D14 arm audit · standing `ARM=hat` sweep · reasoned hat-blind allowlist; ADR 0107) | ✅ **APPROVED (r1)** — 0 BLOCKER / 0 MAJOR / 6 MINOR / 3 INFO; completeness re-derived mechanically from `prosrc` as a property, both keystone mutation twins neutralized and observed red | 2026-08-10 | [act-as-stage-4](../reviews/act-as-stage-4-review.md) |
| **ACT** — act-as role assumption, **STAGE 3 build review** (ADR 0106) | ✅ **APPROVED (r2)** — r1 was ⛔ on 1 BLOCKER + 2 MAJOR (hat-blind caller gates on the membership-grant door and `can_manage_professional`), fixed in `20260918002800` + keystone `318`; r2 re-derived the population **by property** (61 caller-bound pairs, no third member) rather than accepting the two-instance fix. MAJOR-2 promoted to the pilot-gate check (*Remaining pre-pilot work* item 0). Verbose row + the three lead errors it recorded → archive | 2026-08-10 | [act-as-stage-3 (r1+r2)](../reviews/act-as-stage-3-review.md) |
| **ACT** — act-as role assumption, PRE-BUILD plan review (ADR 0106) | ✅ **APPROVED (r2)** — verbose row → archive | 2026-08-09 | [act-as-plan (r2)](../reviews/act-as-plan-review.md) |
| ~~**ACT** — act-as role assumption, PRE-BUILD plan review, round 1~~ | ⛔ CHANGES REQUESTED — verbose row → archive | 2026-08-09 | [act-as-plan (r1)](../reviews/act-as-plan-review.md) |

<!-- rotated from PROGRESS.md at the DM3 Record step, 2026-08-14 -->
| **DM2 — orchestration + Wave A** (ADR 0114 Amdt 1 / 0117 / 0118) | ✅ **APPROVED (r2)** [review](../reviews/dm2-orchestration-wave-a-review.md) — r1's 1 P0 · 3 MAJOR · 6 MINOR · 4 INFO all discharged; **P0-1's proof re-executed by QA, not accepted** (three mutations reproduced in rolled-back txns, restores md5-verified; `308` 5.2s observed RED under cut-removal with its control green; `can_read_document` coverage re-derived as 18 reds in `328` under kernel neutralization). MAJOR-1 catalog-confirmed unable to over-narrow. Two QA findings were **corrected by the engineers and the corrections adopted** (MAJOR-2's classifier contract; MINOR-4 was worse than filed). r2: **0 P0 · 0 MAJOR · 1 MINOR · 4 INFO**. ⛔ **Binding pre-merge condition (r2-1):** three sites still assert a React prop suppresses the byte corridor (`document-row.tsx:75`, `case-detail-view.tsx:727`, `quality-oversight.spec.ts:509`) — comment-only, no re-gate required, **must land before merge or any flag flip**. `e2e:prod` run-1 red ruled **not phase-attributable** (→ FUP-GATE-PDFP1-FLAKE). **Gate:** pgTAP 189f/6097 · lint 5-gate · tsc 0 · vitest 86/1258 · `ARM=census`/`hat`/`floor`/`FROMFINDINGS=1 wrapper` HOLD · diff-scoped sweep `can_read_document` COVERED (`open_document_version` out of every arm's domain by return-type syntax — ADR 0118 §12; assurance is `329` P0a–P0f + the `308` sentinel) · `e2e:prod` GREEN run 2; run-1 red triaged as non-attributable, **mechanism explicitly unproven** | 2026-08-13 | [dm2-orchestration-wave-a](../reviews/dm2-orchestration-wave-a-review.md) |
| ~~**DM2 — orchestration + Wave A**, round 1~~ | ⛔ **CHANGES REQUESTED (r1)** — **1 P0** · 3 MAJOR · 6 MINOR · 4 INFO. **P0-1: DM2 obligation 2 (the M8/M9 byte-discrimination cut) was never re-expressed in `open_document_version`** — no `read_case_deliberation` conjunct anywhere in the corridor, so the ADR-0100 quality reviewer is **SERVED PHI-tier bytes** (probed live: `SERVED tier=phi`, outsider control `P0002`); the only shipped control is `canDownload={!isOversight}`, a React prop — Architecture **Rule 1** inverted. The restored E2E asserts the *button* is absent, so the green bar now certifies a UI-only control; the record says "incl. the M8 bytes-cut contract" and contains **zero** mentions of oversight/ADR 0100. Invisible to all four arms because D8 moved the boundary from a census-covered storage policy into a `jsonb` DEFINER outside every domain (INFO-1). MAJOR: S1-O4 — a doc on a `legal_privileged` interview is readable **and its PHI bytes servable** by members who cannot see the interview (parity, but Wave A makes it live — needs a PO ruling); reconciliation is blind by construction to `failed`/`abandoned` files that DO hold bytes (undisposable PHI under a `RECONCILIATION CLEAN`); "Tentar novamente" after a verification failure is a guaranteed-fail loop, each iteration an unaudited service-role full-object download. **Verified sound and re-derived from the live catalog, not accepted:** the D15 ceiling both directions + no over-narrowing + row-absence through the whole chain + creator/staff_admin/platform_admin all denied + rank genuinely consulted; the meeting/action_item seam at **both** layers (HC0D6 write-side incl. relabel; kernel fail-closed for everyone, with a `true` control); the corridor's D9/D10 state matrix exactly; the **D11 floor exactly** (creator+standard = 0 rows, denials 0, no duplicates, sole minter in the catalog); D8's zero SELECT policies on both buckets; R6/R7/R8 as a real differential + vacuity pin. **Gate figures independently reproduced on a fresh reset:** pgTAP **189f/6059 PASS** (0 `not ok`, 0 bad plans), lint 5-gate exit 0, tsc **0 errors**, vitest 1254, `ARM=census`/`hat`/`wrapper` all HOLD (569 verdicts, BLIND 41) | 2026-08-13 | [dm2-orchestration-wave-a](../reviews/dm2-orchestration-wave-a-review.md) |
| **DM1 — substrate cutover** (ADR 0114/0116) | ✅ **APPROVED (r1)** 2026-08-13 — 0 P0 · 1 MAJOR (fixed) · 5 MINOR · 4 INFO | [review](../reviews/dm1-substrate-cutover-review.md) · [gate detail](../progress/dm1-substrate-cutover.md) |

## Verbose rows rotated from PROGRESS.md 2026-08-14 (the size rotation) — 5 rows

> Retention rule (lead-playbook §5): PROGRESS.md keeps **one line** per verdict, current milestone
> only. These five rows carried full rationale in the live file — the DM5·S3 r1 row alone was ~9 KB —
> against the explicit "never restate rationale here" rule. Preserved verbatim; the authority for each
> remains its `docs/reviews/*.md` report.
>
> **DM5·S3 r1** (kept live as one line, it is the current milestone) · **DM4 r2 + r1** · **DM3 r2 + r1**
> (concluded milestones, one-line index entries added below).

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **DM5 · S3 — printed renditions onto the core substrate** (ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D1/D6/D7/D11/D12/D13/D17/D18) — ⚠ **SLICE review, not the phase gate; DM5 phase QA is still owed at S6** | ⛔ **CHANGES REQUESTED (r1)** — **0 P0 · 2 MAJOR (both blocking) · 6 MINOR · 2 INFO.** ⭐ **The build is sound; both MAJORs are assurance/record fixes measured in lines, and NEITHER blocks S4 on behaviour** (stated explicitly in the report so the verdict is not read as barring the destructive slice). **MAJOR-1 — `342 S3f4` is a VACUOUS keystone, proved by neutralization:** with guard 4's `form_response` refusal removed from the live body, the same call **still raises `P0002`** from `can_write_document`'s fail-closed `else` (line 95, not 38) — the assertion is satisfied by a sibling lock and is structurally incapable of failing on its labelled property. That is DM5's own recorded rule (*"when a door answers one code for several causes, a test asserting that code proves nothing about which cause fired"* — `341` F7) violated one slice later, and it matters because `342`'s own header declares itself *"THE WHOLE ASSURANCE"* (every DM5 door is census-blind). Behaviour is correct — both locks refuse. **MAJOR-2 — the coordinate-withholding property was NOT preserved and `312` t51 records the opposite, as an improvement.** t51 says the retirement is *"stronger … cannot be leaked from here at all"*; probed as the print's legitimate `authenticated` viewer, the coordinate is fully reachable: `printed_documents.document_version_id` → `document_version_files` → `file_objects` yields `documents-standard \| printed/<pd>.pdf`, and **both new columns carry `SELECT → authenticated`**. ⚠ **Calibrated: NOT a live exposure** — both document buckets have **INSERT-only** policies and **no SELECT policy** (all 8 `storage.objects` policies enumerated), so the key buys nothing; the defect is a stated security property recorded backwards. **Cheap real fix: no TS consumer reads either column** (`printed-documents.ts:105` selects neither; the E2E reads via **service role**) ⇒ `REVOKE SELECT (document_id, document_version_id) … FROM authenticated` restores it. **CONFIRMED CLEAN, by probe, in one rolled-back txn each with a positive control FIRST:** D12's conjunction is a genuine **strict narrowing** — active owner serves bytes (`documents-standard\|printed/<pd>.pdf\|active`), deactivate ⇒ `can_view_printed_document` **STILL true** while `can_read_document`/`can_write_document` go **false** and `open_printed_document` raises `42501` from the resolver ⇒ **BUG-DM5-S3-INACTIVE-PRINT-1 genuinely closed, and the conjunction (not either half) is the authority**; direction 2 is **structurally unreachable** and the exhaustive implication `kernel ⇒ print` is **0 violations over all 39 profiles while BOTH sides are non-empty** (1/1 form_response, 9/9 meeting) · **the print arm is BELOW `app.is_active` in BOTH kernel doors** (`pg_get_functiondef`) · **ACLs clean in BOTH directions** on all 17 touched functions — `proacl is null` **false** everywhere, **no PUBLIC/anon grantee**, `app.resolve_document_version_bytes` = **postgres only**, every intended grantee retained, and **0** first-party `public` DEFINER functions anon/PUBLIC-executable; S3 added **nothing** to the `app` default-ACL population (FUP-ACL-APP-POPULATION untouched) · **the tier cannot be forged** — bucket+path server-derived from the single `contains_phi` input, `file_objects_bucket_from_tier` pins bidirectionally, `contains_phi=true` on a form_response ⇒ `HC0D2`, an object in the wrong bucket ⇒ `HC0D3`, and **cross-tenant byte binding is refused by `file_objects_bucket_path_uniq`** (the coordinate is bijective with the registry PK) · **PHI reads audited unconditionally** — 1 `document.downloaded` on success, **0** on refusal · **no direct-DML seam**: RLS on all five substrate tables is **SELECT-only** for `authenticated`, no write policy exists · **all FIVE write guards differential-hold** (the brief said four), incl. guard 1 **through the real door** (`HC0DK` inside `begin_document_upload`) and guard 3's **narrow** half (a superseded print IS disposable) · **the binding trigger refused four independent ways** (wrong path · wrong PHI bucket · no `printed_pdf` rendition · cross-document version) · **`add_referral_shared_item` excludes a print TWICE independently** and `342 S3b2/S3b3` assert each **alone** · the serving route maps the resolver's new `42501` **and** an empty result to the **same pt-BR 404** ⇒ no oracle, no raw PG error (§8) · **0** residual `'printed-documents'`/`storage_path` references in comment-stripped `prosrc` · **FUP-DM5-DVF-FILEOBJ stays latent** — the mint creates a **fresh** file_object and never binds a pre-existing one, as ADR 0120 required S3 to ensure · **D18 is correctly framed as presentation in EVERY site I checked** (both call sites, the ADR, the E2E docblock, `342 S3e`'s header) — **0** comments/test names implying it narrowed access. ⭐ **A measurement recorded because it did NOT discriminate:** over the **seed**, `can_view_printed_document('meeting',…)` and `is_member_of_for` **coincide exactly** (0 pairs, both directions, every meeting × every profile) — so a seed-based narrowing keystone would be vacuous; **`342 S3j` builds its own `participants_only` fixture and asserts both controls first**, which is why it earns the "only block that reds if the arm is deleted" claim. **MINORs:** `S3d2`'s `position()` assertion **passes when `is_active` is absent** (0 < N — proved; `S3c4` covers the behaviour) · the print arm in **`can_write_document` has NO keystone at all** (⚠ do **not** close FUP-DM5-330-WRITE-BLIND on `342`) · guard 5 lacks its in-suite ordinary twin · **D11's "retires superseded bytes via `disposal_state`" is not performed and CANNOT be** (probed: both stay `none`) — a superseded print must keep serving with the `SUBSTITUÍDO` overlay, so retirement is operator-initiated and **nothing schedules it** (ties to ADR 0114 **O1**) · **a print minted on a DRAFT response whose response is then deleted leaves a dangling `securable_resources` row** (no FK to `responses`) and a print **invisible to every UI surface** (0 rows) though `revoke → dispose` still works at the door · the mint's `HC0D4` handler misses the unique that actually fires first. **QA safety:** 6 mutation-bearing runs, **each one rolled-back transaction**; degenerate-body sweep **0 after every run**; `md5(pg_get_functiondef)` of the neutralized function **byte-identical** before/after; **zero fixture residue** (pd 0 · storage.objects 0 · form_response homes 0 · the one inactive profile is the seed's `desativado.conta`). ⚠ **QA's own two probes were fixture-defective and the phase's recorded lessons caught them** — a missing `active_role` hat claim made `has_role` false and five guard blocks refuse for the wrong reason (while the same predicate answered **true** as `postgres`, where `auth.uid()` is NULL and short-circuits the hat), and an id resolved through an RLS-filtered subquery arrived NULL (`341` F-block defect #2 verbatim). **NOT REVIEWED, named:** did not run `e2e:prod`/`next build`/full pgTAP/any ARM/the diff sweep (instructed; accepted as reported) · **did not execute `342`** — read all 49 and probed independently, so "49 pass" is the lead's measurement · no UI executed (S3 ships none) · Gotenberg/overlay/smoke script · the S4 half of `storage-manifest.mjs` · ~180 other assertions in `312`/`313`/`323` · whether any vitest twin pins `storage-coordinates.ts` · `case`/`interview` prints (unmintable) · remote/Cloud (nothing pushed) · `can_view_printed_document` itself (unchanged by S3) | 2026-08-14 | [dm5-s3-review](../reviews/dm5-s3-review.md) |
| **DM4 — Wave C: referrals** (ADR [0119](../decisions/0119-dm4-referral-document-substrate-decisions.md) D1–D10) | ✅ **APPROVED (r2)** [review](../reviews/dm4-referrals-review.md) — r1's **2 blocking MAJORs discharged** at `f4d03f44`; **no binding pre-merge condition**. **r2: 0 P0 · 0 MAJOR open · 8 MINOR carried · 2 MINOR new · 5 INFO carried · 1 INFO closed.** ⭐ **QA verified by EXECUTION, not from the report** (the r1 lesson applied to r1's own remediation): static check of **all 14 distinct needles** against live `pg_get_functiondef` — all match, incl. the previously-orphaned `AUDIT_CALL` · **rollback discipline proven FIRST** (`340` `begin;`@35, marker@48, `rollback;` at EOF) · **ran the committed harness myself: 18/18 RED-PROVEN, control green, `HEAD: 5e101245`, EXIT=0** · `md5(pg_get_functiondef)` of all **8** mutated functions snapshotted before and re-checked after **each of three runs — identical every time**; `pgtap` dropped (gen:types safe); tree clean so the stamp is honest. **MAJOR-1 fixed better than requested** — harness-wide `mut()` no-match guard + self-reported HEAD, converting a silent decay class into a loud one for all 18 cases. **The guard was PROVEN able to fire, against the exact pre-M5 needle that caused MAJOR-1**: `ERROR: DM4 matrix: needle matched NOTHING … (orphaned by a body rewrite)`, psql EXIT=1 — the r1 defect is now the guard's own positive control. **MAJOR-2: N14a's polarity is MEASURED, and the must-stay-green branch was proven to discriminate in BOTH directions** via scratch variants: a nonexistent green label ⇒ `[green:…]=ABSENT`, and no-mutation-but-demand-red ⇒ `[C11d]=STILL-GREEN`. Polarity is genuine, not incidental — C10a runs as `…0002` (**source** coordinator, `340:433`) and C11d as `…0005` (**target**, `340:518`), so N14a's source-only gate makes C10a's green the control that attributes C11d's red to **narrowing**; N14b independently proves C10a **can** go red. Disposition comments now enumerate coverage per successor with its proving case and correctly reclassify `D4a`/`325 t4` as **retirement pins — catalog assertions, not mutation coverage**. **MAJOR-3: PO-DEFERRED to the Phase 19 access plane (D16), recorded as an ACCEPTED OPEN GAP, not resolved** — `add_referral_shared_item` unchanged (re-confirmed at HEAD); `FUP-DM4-RECUSAL` carries the live demonstration and the "gap STANDS behind a flag that will eventually be ON" framing. ⚠ **QA caveat: carry it as an open security obligation with the flag-on date as its deadline; it must never be absorbed into "Phase 19 delivered an access plane".** **Gate-figure impact: NONE** — the discharge touches **0 `src/` files and 0 migrations**, and the `340` diff is comment-only with `plan(76)` unchanged, so pgTAP **191f/6231**, registry 391==391, tsc 0, lint 5/5, vitest 1264, `next build` 0 and all four arms **remain valid at HEAD**; only the matrix line changed, and QA re-ran it. **Two NEW MINORs, both in the assurance layer, both ~3 characters:** **MINOR-9** `run_case`'s grep is substring-bounded — `B1` also matches `B10c`; resolved **by measurement, and QA's own prediction was wrong** (both flip under N2: `not ok 10 - B1` **and** `not ok 27 - B10c`), so the verdict is correct only incidentally — swept every pattern, this is the **only** collision; anchor as `- $pat[^0-9A-Za-z]`. **MINOR-10** the HEAD stamp uses `rev-parse --short HEAD`, which **cannot see a dirty tree**, so "never citable detached from its commit" is one flag short — use `--dirty`. **r1 items closed:** MINOR-2 (ADR + `340` header + matrix now say "one predicate applied twice"), MINOR-3 (census **141 HEAD / 142 pre-DM4** recorded with the 144/145 variants beside it; re-measured at r2, still 141), INFO-6. **MINOR-8 narrowed** — N14a/N14b close the behavioural half; the catalog half (`328 K1a–K1d`, `340 D1–D7`) stays open, and ⚠ **QA retracts half its own r1 mitigation**: `u1`'s injection would flip `K1b`, but no harness runs `328` under it, so it is theoretical, not observed. Carried unchanged: MINOR-1/4/5/6/7, INFO-1..5. Exit criteria **4 and 5 now MET**; 1/2/3 unchanged | 2026-08-14 | [dm4-referrals](../reviews/dm4-referrals-review.md) |
| ~~**DM4 — Wave C: referrals**, round 1~~ | ⛔ **CHANGES REQUESTED (r1)** — **0 P0 · 3 MAJOR (2 blocking) · 8 MINOR · 6 INFO.** **MAJOR-1: "16/16 RED-PROVEN" does not reproduce at HEAD.** Matrix case **N10b**'s second `replace()` is a **silent no-op** — M5 (`726032fc`) rewrote `open_referral_snapshot_document`'s `log_audit_access` call to `jsonb_build_object(…)` and the harness still searches for the `'{}'::jsonb` form; measured live: `N10b_replace_matches = false`. `git log -- …matrix.sh` shows **one commit, `66084b4f`, predating M5**, and the "↻ Re-confirmed at HEAD" pass re-ran pgTAP + arms + build, **not** the matrix. N10b degenerates into N10a → txn aborts → `[C11b]=ABSENT` → the script exits **non-zero**. **Bounded by measurement, not assumed:** I checked all 13 other `replace()` needles against their live bodies — N1/N2/N3/N4/N5/N6/N7/N8/N9a/N9b/N10a-gate/N11/N12/N13 **all still match**, so the write seam keeps valid proofs; what is lost is the C11 family's can-fail proof. ⚠ M5's own commit cites the lesson it broke ("a replace that matches nothing silently no-ops") and guarded the *migration*, not the harness. **MAJOR-2: the retired ③TWIN lost its can-fail proof and the disposition comment claims coverage that does not exist.** `drop_snapshot_arm` + `restore_casedoc_member` were **deleted** (correctly — their target policy is gone), but `u1-mutation-audit.sh:133-140` asserts the successors are "matrix-covered by N10a/N10b/N11"; the matrix's full pattern set contains **no `C10a`, no `C11d`, no `D4a`**, and `C11b`'s only case is the broken N10b ⇒ **zero of four covered**. Polarity gap too: ③TWIN detected **over-narrowing**; only **C11d** carries that (C10a reads as the *source* coordinator — wrong side of the boundary; C11b is a deny, passes by construction; D4a is a retirement pin). **No mutation anywhere narrows the new door** — C10a/C11d are red-first-proven only against door *absence*, the record's own "weak" class. **MAJOR-3 (live, demonstrated): the freeze corridor never checks whether the caller may read the source document.** `add_referral_shared_item` (DEFINER) resolves the doc with `resource_type='case' and home_resource_id=source_case_id and status='active'` — **no `can_read_document`, no `can_read_case`**; its only gate is `is_staff_admin_of_for(source_commission_id)`, and the byte door's gate `can_read_referral_phi` **and** `_audit_access_authorized`'s `referral.viewed` arm are the *same* predicate. Proven on the live stack (rolled-back txn, `chefe.ccih` recused from case `d0000000-…c1`, real referral `efa00000-…a1`): `can_read_case = false` · `can_manage_referral_source = true` · `can_read_referral_phi = true` ⇒ a **recused/respondent** coordinator routes around the `236` exclusion perimeter to PHI-classified bytes, audited as a normal view. Pre-existing in *shape* (the narrative arm is identical) but DM4 is what makes **document bytes** reachable — and ADR 0119 D4 reasons about exactly this seam for the D15 clearance plane while never considering the **case-capability** plane. Not P0: `documents_wave_c` ships **OFF**, actor is a trusted `staff_admin`, no cross-tenant leak. **UPHELD under challenge:** **ADR 0119 D9 is correct** — `documents_select` USING = `can_read_document`, whose `case_referral` arm is `can_read_referral_metadata`, **identical** to the retired `referral_reply_attachment_select_readable`; `list_referral_reply_documents` + `get_referral_detail` gate on the same predicate with `can_open` server-computed; `app._referral_reply_documents` is unreachable (config.toml exposes `public`+`graphql_public` only); `file_objects.storage_path` is metadata-visible but the PHI/standard buckets carry **no SELECT policy at all** (8 storage policies total) ⇒ no bytes. Also verified: sweep **ERROR** disposition **ADEQUATE** (N4/N5 both still match live; the DM1-era keystones that went red cover the other arms); exit criteria **1 ✅ 2 ✅ 3 ✅** (allowlist empty, verified live: 0 `%attachment%` routines/policies/relations/grants), **4 ⛔**, **5 🟡** (the kernel/byte twin *is* proven both directions — N1/N2/N3 → B10c/B1/B4 — it is the retired storage twin that regressed); Rules **1 🟡 · 7 ✅ · 9 ✅ · 10 ✅ · 11 ✅ · 12 ✅** (`admin.ts` is `import 'server-only'`, no client module reaches it; audit carries references, never payloads). **Census reconciled: 146/150 are BOTH wrong — the exact recorded definition yields 141 at HEAD** (144 allowing `proretset`, 145 with `app`); DM4 removed exactly one member ⇒ 142 pre-DM4; the discrepancy is in the *recording*, not the population — record the query beside the number. **MINOR-2: N10a's "two locks" are ONE predicate applied twice** (`_audit_access_authorized`'s `referral.viewed` arm *is* `can_read_referral_phi`) — so with N10b broken, nothing proves 340 reds when it is removed from both. **MINOR-4: yes, the T6 caveat placement matters** — 5/6 lives only in the commit subject while the file header at `:23-25` says "All four were observed RED" in a six-test file; coverage itself is fine. **Discharge:** re-point + re-run N10b recording its HEAD · add a narrowing mutation (or correct the false disposition comments) · a recorded PO ruling on MAJOR-3 in ADR 0119 | 2026-08-14 | [dm4-referrals](../reviews/dm4-referrals-review.md) |
| **DM3 — Wave B: controlled documents** (ADR 0114 Amdt 1 + **Amdt 2 / D17**) | ✅ **APPROVED (r2)** [review](../reviews/dm3-controlled-documents-review.md) — r1's 1 blocking MAJOR **discharged by M11** (`5b35003`). **r2: 0 P0 · 0 MAJOR · 3 MINOR carried · 5 INFO · 1 new INFO. No binding pre-merge condition.** The assert moved to `begin_document_upload`, scoped `if p_resource_type = 'controlled_document'` — the first residue-producing step; two doors now assert the gate. **QA re-verified the fix AND its invited failure mode by measurement, not by reading the scope:** with the flag OFF and home types **enumerated from `securable_resources`** (not a hand list), `case`/`meeting`/`interview`/`action_item` all **BEGIN ACCEPTED**, `controlled_document` **REFUSED HC0D7**; reserved controlled-home sessions **0**. **Twin re-run, all five types before AND after removing the assert — exactly ONE cell changes** (controlled: REFUSED→ACCEPTED; the four Wave-A arms ACCEPTED both ways), residue **0→1** with a minted session id ⇒ **Wave A is NOT narrowed** and "reds T3 and only T3" is corroborated structurally. Scoping proof against evasion: the assert keys on the declared `p_resource_type`, but the lookup `where s.id=… and s.resource_type=p_resource_type` makes a mis-declared type unreachable (P0002). Both false comments corrected **as retractions**, and `seed.sql` additionally records that "every door" is **not** the target state either — closing the loop a bare correction would leave open. **The lead's "sweep not re-run" scope decision UPHELD, with the reasoning restated on the catalog rather than on the diff** (a migration-file claim is stale by design here): post-M11 `ARM=census` **HOLDS at 548 live / 569 accounted** and `public` policies **275** — both identical to the pre-M11 measurements ⇒ M11 added zero gates and zero policies, and `can_read_document`'s controlled arm is byte-identical to the r1 audit. ⚠ Carried unchanged: `can_write_document`'s sweep verdict is still **ERROR**-resolved-by-runlog and **must not later be cited as COVERED**. **MINOR-1 CLOSED** — corrected at all four sites incl. **its origin in ADR 0118 §12**. **NEW INFO:** `act-role-assumption.spec.ts:157` + `phase2-auth-shell.spec.ts:268` flaked in **both** independent `e2e:prod` runs — a pattern, not noise; not DM3's, but recommend filing now while two runs' signal exists (re-running destroys flake evidence). Re-gate accepted not re-run: `e2e:prod` 1101p/0f/3flaky/0 did-not-run (1104+6 skipped = 1110 = collected), pgTAP 190f/6152 on a fresh reset, `ARM=hat`/`wrapper`/`floor`, tsc 0, lint 5/5 | 2026-08-13 | [dm3-controlled-documents](../reviews/dm3-controlled-documents-review.md) |
| ~~**DM3 — Wave B: controlled documents**, round 1~~ | ⛔ **CHANGES REQUESTED (r1)** — **0 P0** · **1 MAJOR (blocking)** · 4 MINOR · 5 INFO. **MAJOR-1: `documents_wave_b` gates the LAST step of the corridor, not the corridor — and the tree asserts the opposite twice.** Catalog: **exactly one** function calls `app.assert_documents_wave_b_enabled` (`attach_controlled_document_version_file`). Probed live with the flag OFF: `create_controlled_document` **ACCEPTED**, `begin_document_upload` **ACCEPTED** — so a coordinator creates the doc, reserves a path, **PUTs real bytes into `documents-standard`** and finalizes; only the domain pointer refuses (HC0D7). Residue: orphaned bytes + orphaned core version + a draft whose file never appears. `documentsWaveBEnabled()` (added by DM3) is **called from nowhere** in `src/`; the UI gates on `controlled_docs` alone. Violates the phase's OWN stated contract (`seed.sql` "with it OFF every DM3 door answers HC0D7"; `documents/actions.ts:87` "every DM3 door calls (HC0D7), so a stale client cannot reach past this") and the Wave-A MIN pattern it cites ("ABSENT, not disabled"). **Not an authz hole** — authority unchanged and verified (approver reads/cannot write; plain member + outside approver both refused P0002/42501). Lands in the deploy's OWN expected interim state (plan §10: local vs prod flags "disagree"); the arm the Test Run Summary already records as **not exercised**. **Verified by QA, not accepted:** both R3 twins re-run (trigger-only → OK, door-only → OK, **both → 23503** — the two-sufficient-mechanisms finding reproduced) · the A4 widening twin genuinely reds · all **five** D17 conditions incl. the 12-arg identity with **no surviving 11-arg overload** and the re-GRANT read from `pg_proc.proacl` · D15 ceiling narrows on a case home and **fails closed** on a Wave-B home (HC0D6 + `else null`), incl. under `set local role authenticated` · zero `controlled-documents` Storage policies · `set_document_version_file`/`can_read_document_object`/`storage_path` all **0 rows** in the catalog · the trigger fix is unbypassable (BEFORE INSERT + `securable_type NOT NULL` + no INSERT policy → 42501) · DM3B-1 is real `Buffer.compare === 0` through the door-signed URL. **Gate figures independently reproduced:** tsc **0** · lint **5/5** · vitest **86f/1258** · `ARM=census` **HOLDS** (548 live/569 accounted; 273+275 re-derived exactly) · `ARM=hat` **HOLDS** · `FROMFINDINGS=1 ARM=wrapper` **HOLDS** (BLIND 41) · 385 files = 385 registered. **MINOR-1: "in no authz arm's domain" is false for ARM 2** (domain = every `public` prosecdef auth-EXECUTE fn, **411** sigs, contains both named functions) — the conclusion stands, the reason does not. Accepted not re-run: `e2e:prod`, the fresh-reset pgTAP run, `ARM=floor`, and the sweep's `can_write_document` **ERROR**-resolved-by-runlog (⚠ must not later be cited as COVERED) | 2026-08-13 | [dm3-controlled-documents](../reviews/dm3-controlled-documents-review.md) |

---

## ↩ Rotated from PROGRESS.md 2026-08-17 (the §6-step-5 size rotation) — the three DM5 slice verdicts, VERBATIM

The § QA Verdicts table declares **"ONE LINE per phase/feature: verdict + date + link — the full
analysis lives in the review."** These three rows had grown to **4.4 KB of that table's 6.4 KB**,
carrying the analysis itself. Rotated here; one-line entries left in place pointing at the reviews.
⚠ **Appended BEFORE the cut and `cmp`-verified — then relative links `](docs/…)` were repointed to
`](../…)` (8 of them), because a root-relative link copied from PROGRESS.md 404s from
`docs/progress/`. Prose verbatim, link targets repointed.**

⛔ **Each is a SLICE verdict. DM5 phase QA is still owed at S6** — none of these authorizes it.

| Phase / feature | Verdict | Date | Review |
| --- | --- | --- | --- |
| **DM5 · S5 — operational closure** (S5.R rehearsal · S5.D disposal gap + runbook · backup/restore drill · EXPLAIN baselines) — ⚠ **SLICE review; DM5 phase QA still owed at S6** — r1 ⛔ → r2 ✅ | ✅ **APPROVED (r2)** — r1's **2 MAJOR discharged and re-proved by neutralization on my own runs**: reverting the `!proof.present` branch reproduces the pre-fix red **character-identical** (`api_keys=5 volume_present=false exit=0 verdict=CONSISTENT_EMPTY`), **C15 passes with the fix reverted** (a real over-reach guard, not a companion assertion), and the affinity guard **actually refuses** a Cloud URL against a live local stack (exit 2) while permitting loopback. ⭐ **The lead's guard-set diff found a gap I had MISSED** (`{present:false,error}` → CONSISTENT_EMPTY) — the method outperforming the reviewer. New at r2: **0 P0 · 0 MAJOR · 6 MINOR · 6 INFO**. ⭐ **Both claims I was told to test HOLD**: the count comparison — the only control r1 said survives on Cloud — **passed over a both-ways-diverged bucket and left a real byte behind** (blind `delete --execute`: `deleted=5 manifest=5 MATCH`, volume `1` file surviving), so **the Cloud position is worse than r1 concluded**; and neutralizing `DIVERGED_BOTH_WAYS` left **exit=1** with only the verdict name discriminating ⇒ *an exit code is a CLASS assertion, never an IDENTITY one* (no exit-only arm exists today — a discipline, not a live gap). ⚠ **The nine-verdict count is right; it is complete on NAMES and incomplete on PATHS** — 11 reachable paths, **1 with no control** (partial byte loss). ⭐ **I closed my own r1 gap**: the drill and P2 are **no longer single-sourced** — `psql` exit 0 · **490 errors · 90 of 274 policies** reproduce exactly, P2 **0.46 ms → 353.8 ms** Seq Scan with the per-row DEFINER; two corrections fell out (the `^ERROR` anchor is **invocation-dependent**, opposite result on stdin; ARM B does **not** reach trigger parity, **227 vs 235 with zero errors**). ⚠ `e2e:prod` still **deferred by PO decision — concurred** | 2026-08-17 | [r2](../reviews/dm5-s5-review-r2.md) · [r1](../reviews/dm5-s5-review.md) |
| **DM5 · S4 — legacy storage-bucket retirement** (ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D8/D9) — ⚠ **SLICE review; DM5 phase QA still owed at S6** — r1 ⛔ → r2 ⛔ → **r3 ✅** | ✅ **APPROVED (r3)** — B1/B2 discharged at r2, **B3 discharged at `977f1d71`**. The corrected mechanism was checked clause-by-clause against my own probes + fresh catalog reads, not read for plausibility: `protect_delete()` role-agnostic, API sets the GUC, the two ABSENT policies (SELECT+DELETE) are the operative lock, and the lead's own extra fix — *"the service role succeeds by **bypassing RLS**, not via the GUC"* — is catalog-true (`service_role rolbypassrls=t`; `authenticated`/`anon` `f`). His S5.R consequence (*"nothing platform-level stops the rehearsal's deletes"*) is **correct**: the tool uses `SUPABASE_SERVICE_ROLE_KEY` + `rolbypassrls`. New at r3: **0 P0 · 0 MAJOR · 4 MINOR · 3 INFO**, all evidence-level — ⚠ *"its own **HINT**"* quotes the exception **MESSAGE** (4 places) · the *"4,402,266 B with nothing writing"* datum is a **`du`-scope artifact** (re-measured byte-identical to r2) · `200:16`'s `Asserts:` index still contradicts its own §7 (**3rd** iteration of that sweep boundary) · **MINOR-4 is 5 of 6, reported closed twice** (`follow-ups.md:753`). ⛔ Cloud UNVERIFIED; S5.R UNREHEARSED | 2026-08-17 | [r3](../reviews/dm5-s4-review-r3.md) · [r2](../reviews/dm5-s4-review-r2.md) · [r1](../reviews/dm5-s4-review.md) |
| **DM5 · S3 — printed renditions onto the core substrate** (ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D1/D6/D7/D11/D12/D13/D17/D18) — ⚠ **SLICE review, not the phase gate; DM5 phase QA is still owed at S6** — r1 ⛔ → r2 ✅ | ✅ **APPROVED (r2)** — r1's 2 MAJOR + 6 MINOR + 2 INFO all discharged at `af9a894e`, each blocking item **re-proved by neutralization** (guard 4 deleted ⇒ `S3k2` RED / `S3f4` GREEN); new at r2: **0 P0 · 0 MAJOR · 1 MINOR · 3 INFO**, all record-level. ⚠ r1's **MAJOR-2 premise was FALSE** and the corrected record is TRUE (verified independently). Does **not** authorize S4 — PO authorization owed on the day | 2026-08-14 | [dm5-s3-review](../reviews/dm5-s3-review.md) |

## ↩ Rotated from PROGRESS.md 2026-08-18 (the §6-step-5 size rotation, DM5 Record) — the DM5 PHASE QA + S6 slice rows, VERBATIM

The § QA Verdicts table declares **"ONE LINE per phase/feature: verdict + date + link"**. These two
rows carried the analysis itself — the DM5 phase QA flagged both as having **broken their own
retention contract to restore currency**. Rotated here; one-line entries left in place pointing at
the reviews.

⚠ **Appended BEFORE the cut and `cmp`-verified, then the 6 relative links `](docs/…)` were repointed
to `](../…)`** — 4 `decisions/` + 2 `reviews/` — because a root-relative link copied from PROGRESS.md
**404s from `docs/progress/`**. Prose verbatim, link targets repointed.

⚠ **A tension worth naming rather than repeating silently:** the table's own comment says *"never
restate rationale here **or in the archive**"*, since the analysis already lives in `docs/reviews/`.
Archiving verbatim follows the **2026-08-17 precedent** and loses nothing, which is why it was done —
but the two instructions genuinely conflict, and whoever next rotates this table should settle it
rather than discover it again.

⛔ **The first row is the PHASE verdict; the second is a SLICE verdict. The slice authorizes no part
of the phase.**

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **DM5 — PHASE QA** (Wave D + retirement; ADRs [0114](../decisions/0114-document-model-redesign.md)+Amdt 1/2 · [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md)/[0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md)/[0122](../decisions/0122-recusal-case-read-arm-at-the-referral-freeze-door.md)) — ⚠ **the PHASE verdict; S3/S4/S5/S6 slice verdicts authorize no part of it** | ✅ **APPROVED (r2)** — r1 ⛔ 0 P0 · 4 BLOCKING · 4 MINOR · 7 INFO, **all record defects, no code change requested** (the 7th such round); all 8 fixed same-day, **3 MINOR carried, none blocking**. ⚠ **Closes over NEITHER** 🔒 the unrehearsed runbook NOR 🔴 the supersede-serving collision. ⭐ **R1 is the S6 QA's own F1 defect recurring INSIDE F1's fix**: the flag census reproduces on neither catalog (**75/6** local, **74/6** remote vs *"52 / exactly one"*), carrying no query in the block that promises one — *correcting a claim's DIRECTION is not verifying its MAGNITUDE*. Build sound: retirement clean at every executing layer on a **wider** bound than S6 used; PHI unreachable at the **ACL** layer, not merely RLS | 2026-08-17 | [dm5-phase-review](../reviews/dm5-phase-review.md) |
| **DM5 · S6 — canon rewrite + program exit sweep** — ⚠ **SLICE review; superseded in scope by the PHASE QA above** | ✅ **APPROVED (r2)** — r1 ⛔ six findings, **all record defects**; the blocking pair were an END-STATE block that **INVERTED its source measurement** into three records at once, and a §2 `upload_state` (`active`) that **does not exist** in the CHECK — a state borrowed from `documents.status`, in the slice whose purpose was canon-vs-catalog fidelity. QA re-measured gate step 1 in full rather than arguing it unaffected | 2026-08-17 | [dm5-s6-review](../reviews/dm5-s6-review.md) |

## Verbose rows rotated from PROGRESS.md 2026-08-18 (the size rotation)

> PROGRESS.md keeps one line per verdict. Links repointed to `docs/progress/`-relative.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **DM5 — PHASE QA** (Wave D + retirement) — ⚠ **the PHASE verdict; the S3/S4/S5/S6 slice verdicts authorize no part of it** | ✅ **APPROVED (r2)** — r1 ⛔ 0 P0 · 4 BLOCKING · 4 MINOR · 7 INFO, **all record defects, no code change requested**; 3 MINOR carried, none blocking. ⚠ **Approved OVER** 🔒 the unrehearsed runbook and 🔴 the supersede collision — **both PO-ruled 2026-08-18** (§ Decisions); the rehearsal is now **Critical FUP C1** | 2026-08-17 | [dm5-phase-review](../reviews/dm5-phase-review.md) |
| **DM5 · S6 — canon rewrite + program exit sweep** — ⚠ **SLICE review; superseded in scope by the PHASE QA above** | ✅ **APPROVED (r2)** — r1 ⛔ six findings, **all record defects** | 2026-08-17 | [dm5-s6-review](../reviews/dm5-s6-review.md) |
| _Verbose form of the two rows above_ — rotated **2026-08-18** → [qa-verdicts-archive.md](qa-verdicts-archive.md) § "Rotated from PROGRESS.md 2026-08-18", verbatim (6 links repointed, `cmp`-verified) | – | – | – |
| **DM5 · S5 — operational closure** — ⚠ **SLICE review; DM5 phase QA still owed at S6** | ✅ **APPROVED (r2)** — 0 P0 · 0 MAJOR · 6 MINOR · 6 INFO; r1's 2 MAJOR re-proved by neutralization. ⭐ **Both claims QA was asked to test HOLD, and the Cloud position is WORSE than r1 concluded**: the count comparison — the only control surviving on Cloud — **passed over a both-ways-diverged bucket and left a real byte behind** | 2026-08-17 | [r2](../reviews/dm5-s5-review-r2.md) · [r1](../reviews/dm5-s5-review.md) |
| **DM5 · S4 — legacy storage-bucket retirement** ([0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D8/D9) — ⚠ **SLICE review** | ✅ **APPROVED (r3)** — 0 P0 · 0 MAJOR · 4 MINOR · 3 INFO; r1 ⛔ → r2 ⛔ → r3 ✅, every blocking item a RECORD defect (no code change ever requested). ⛔ **Cloud UNVERIFIED in all three rounds; the deploy-time byte path UNREHEARSED** | 2026-08-17 | [r3](../reviews/dm5-s4-review-r3.md) · [r2](../reviews/dm5-s4-review-r2.md) · [r1](../reviews/dm5-s4-review.md) |
| **DM5 · S3 — printed renditions onto the core substrate** ([0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D1/D6/D7/D11/D12/D13/D17/D18) — ⚠ **SLICE review** | ✅ **APPROVED (r2)** — 0 P0 · 0 MAJOR · 1 MINOR · 3 INFO; r1's blockers each **re-proved by neutralization** (guard 4 deleted ⇒ `S3k2` RED / `S3f4` GREEN). ⚠ r1's MAJOR-2 premise was FALSE and the correction verified independently | 2026-08-14 | [dm5-s3-review](../reviews/dm5-s3-review.md) |
| _Full analysis of the three rows above_ — rotated **2026-08-17** → [qa-verdicts-archive.md](qa-verdicts-archive.md) § "Rotated from PROGRESS.md 2026-08-17", verbatim | – | – | – |
| **DM4 — Wave C: referrals** (ADR [0119](../decisions/0119-dm4-referral-document-substrate-decisions.md) D1–D10) — r1 ⛔ → r2 ✅ | ✅ **APPROVED (r2)** — 0 P0 · 0 MAJOR open · 8 MINOR carried · 2 new · 5 INFO. **No binding pre-merge condition.** ⚠ MAJOR-3 was **PO-deferred to Phase 19 as an ACCEPTED OPEN GAP, not resolved** → FUP-DM4-RECUSAL, whose deadline is the flag-on date | 2026-08-14 | [dm4-referrals](../reviews/dm4-referrals-review.md) |
| **DM3 — Wave B: controlled documents** (ADR 0114 Amdt 1 + **Amdt 2 / D17**) — r1 ⛔ → r2 ✅ | ✅ **APPROVED (r2)** — 0 P0 · 0 MAJOR · 3 MINOR carried · 6 INFO. **No binding pre-merge condition.** ⚠ `can_write_document`'s sweep verdict is **ERROR**-resolved-by-runlog and **must not later be cited as COVERED** | 2026-08-13 | [dm3-controlled-documents](../reviews/dm3-controlled-documents-review.md) |
| _Phase 0 → FUP batch 2026-08-12_ — **105 concluded rows** (81 rotated 2026-08-06 + 18 rotated 2026-08-10 + 6 rotated 2026-08-13: QO·B · PDF·P2 · PDF·P1 · QO·FUP · QO·A · MIN · AFF · PCI · TV · Phase 16, incl. struck loop rows) | — | — | [collapsed index](qa-verdicts-archive.md) |

## ↩ Rotated from PROGRESS.md 2026-08-19 — the seven DM rows (the DM milestone is closed), VERBATIM

> The section's own retention rule is *current milestone only*. The DM program closed 2026-08-18,
> so these left when the prévia split became the current milestone. Link repoint only.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- |---| --- |
| DM5 — PHASE QA | APPROVED (r2) | 2026-08-17 | [dm5-phase-review](../reviews/dm5-phase-review.md) |
| DM5 · S6 — canon rewrite + program exit sweep | APPROVED (r2) | 2026-08-17 | [dm5-s6-review](../reviews/dm5-s6-review.md) |
| DM5 · S5 — operational closure | APPROVED (r2) | 2026-08-17 | [r2](../reviews/dm5-s5-review-r2.md) · [r1](../reviews/dm5-s5-review.md) |
| DM5 · S4 — legacy storage-bucket retirement | APPROVED (r3) | 2026-08-17 | [r3](../reviews/dm5-s4-review-r3.md) · [r2](../reviews/dm5-s4-review-r2.md) · [r1](../reviews/dm5-s4-review.md) |
| DM5 · S3 — printed renditions onto the core substrate | APPROVED (r2) | 2026-08-14 | [dm5-s3-review](../reviews/dm5-s3-review.md) |
| DM4 — Wave C: referrals | APPROVED (r2) | 2026-08-14 | [dm4-referrals](../reviews/dm4-referrals-review.md) |
| DM3 — Wave B: controlled documents | APPROVED (r2) | 2026-08-13 | [dm3-controlled-documents](../reviews/dm3-controlled-documents-review.md) |

## ↩ Rotated from PROGRESS.md 2026-08-20 — concluded milestone, VERBATIM apart from the link repoint

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| Prévia split — ADR 0125/0126 | APPROVED (r2) | 2026-08-19 | [previa-split-review](../reviews/previa-split-review.md) |

## ↩ Rotated from PROGRESS.md 2026-08-22 — DSR slices 3 and 4, VERBATIM apart from the link repoint

_The DSR program closed, merged and pushed on 2026-08-20/21; these five rounds are concluded. Moved under
this table's own retention rule (current milestone only) to make room for the Increment-2 verdict — PROGRESS.md
stood at **10 bytes** under the hard cap._

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| DSR Slice 4 — ADR 0130 Amdt 4 (r3) | **APPROVED** | 2026-08-20 | [dsr-slice-4-review](../reviews/dsr-slice-4-review.md) |
| ~~DSR Slice 4 — ADR 0130 Amdt 4 (r2)~~ | ~~CHANGES REQUESTED~~ | 2026-08-20 | [dsr-slice-4-review](../reviews/dsr-slice-4-review.md) |
| ~~DSR Slice 4 — ADR 0130 Amdt 4 (r1)~~ | ~~CHANGES REQUESTED~~ | 2026-08-20 | [dsr-slice-4-review](../reviews/dsr-slice-4-review.md) |
| DSR Slice 3 — ADR 0130 (r2) | **APPROVED** | 2026-08-20 | [dsr-slice-3-review](../reviews/dsr-slice-3-review.md) |
| ~~DSR Slice 3 — ADR 0130 (r1)~~ | ~~CHANGES REQUESTED~~ | 2026-08-20 | [dsr-slice-3-review](../reviews/dsr-slice-3-review.md) |

## Rotated 2026-08-23 at the AFF2 Record step — the case-split and DSR milestone rows

_Verbatim. AFF2 is now the current milestone, so these older concluded rows leave PROGRESS.md per the
contract (§5: keep only the current milestone's rows live). Struck loop rows preserved as struck._

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **Case split · Inc 2** (ADR 0134 D6 + Amdt 1–7) | ✅ **APPROVED (r2)** — 5 record conditions, none blocking merge, **all 5 discharged 2026-08-22** (C-1/C-4 here · C-2/C-3 → [increment record](case-surface-split-increment-2.md) · C-5 → `FUP-CS2-QA-RESIDUE`); r1 CHANGES REQUESTED (5 blocking, incl. a PHI write handed to `platform_admin`) | 2026-08-22 | [review](../reviews/case-surface-split-increment-2-review.md) |
| **Case surface split · Increment 1** (ADR 0134 D1–D5, D7) | ✅ **APPROVED** (r3) — 4 conditions, all for the Record edit (§8.9). No security defect in any round | 2026-08-21 | [case-surface-split-increment-1-review.md](../reviews/case-surface-split-increment-1-review.md) |
| ~~Case surface split · Increment 1 (r2)~~ | ~~CHANGES REQUESTED~~ | 2026-08-21 | [case-surface-split-increment-1-review](../reviews/case-surface-split-increment-1-review.md) |
| ~~Case surface split · Increment 1 (r1)~~ | ~~CHANGES REQUESTED~~ | 2026-08-21 | [case-surface-split-increment-1-review](../reviews/case-surface-split-increment-1-review.md) |
| **DSR operational remediation** | ✅ **APPROVED** (r2; r1 CHANGES REQUESTED — 3 blockers, all records, no engineering) | 2026-08-21 | [dsr-remediation-review.md](../reviews/dsr-remediation-review.md) |
| DSR operational remediation (r2) | **APPROVED** | 2026-08-21 | [dsr-remediation-review](../reviews/dsr-remediation-review.md) |
| ~~DSR operational remediation (r1)~~ | ~~CHANGES REQUESTED~~ | 2026-08-21 | [dsr-remediation-review](../reviews/dsr-remediation-review.md) |


## ↩ Rotated from PROGRESS.md 2026-08-24 at the ADR 0137 Record step — retention (current milestone only)

_The AFF2 pair, verbatim (links repointed). Still standing in § QA Verdicts after the ADR 0137
Record step, which is the rotation that should have moved them: 0137 is the current milestone._

| **AFF2** (ADR 0133 + Amdt 1–4) | ✅ **APPROVED (r2)** — both r1 blockers discharged at `02cc5817` (R1 fixed + future-expiry control · R2 ruled, Amdt 4 r2). 2 carried records: "strictly stricter" is false (the fix WIDENS both subset capabilities; no arm) + the seed warrant is mis-cited. r1 CHANGES REQUESTED | 2026-08-23 | [aff2-review.md](../reviews/aff2-review.md) |
| ~~AFF2 (r1)~~ | ~~CHANGES REQUESTED~~ — 2 blocking, both found outside the given remit; every remit item passed | 2026-08-23 | [aff2-review](../reviews/aff2-review.md) |

## ↩ Rotated from PROGRESS.md 2026-08-24 — the ADR 0137 row, at the point the batch fully concluded

_Verbatim apart from the link repoint. The batch was PO-approved on 2026-08-24 and its § Now bullet rotated to [2026-Q3.md](2026-Q3.md) the same day, once the last of its four items (a full `e2e:prod` covering HEAD) closed **GATE GREEN at `77b0a467`**. Retention in § QA Verdicts is the current milestone only, and 0137 is no longer it._

⚠ **The r1 finding is the part worth carrying forward, not the r2 verdict.** r1 was `CHANGES REQUESTED` on one measured fact: **`patient_mode='required'` was admitted by the CHECK and written by NOTHING** — pgTAP reached it only as `postgres`, by direct INSERT; no UI could set it and no E2E ever drove it. The ADR's load-bearing case-side MRN half was **dormant** while § Now recorded "all 4 increments BUILT". That is the shape a green suite cannot catch: a mode that is *admissible* but *unreachable*.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **ADR 0137 batch** | ✅ **APPROVED (r2)** — 4 r1 items discharged; `required` is product-reachable + E2E-driven. 4 pre-commit conditions in §7. r1 CHANGES REQUESTED | 2026-08-24 | [r2](../reviews/adr-0137-batch-review-r2.md) |

### Rotated from PROGRESS.md 2026-08-25 — the milestone before PDF·P3

⛔ Verbatim; links repointed for this directory. Rationale stays in `docs/reviews/`, never here.

0136 concluded; the user-profile redesign is the current milestone.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **ADR 0136 — deferred `staff_admin` sign-off** | ✅ **APPROVED** — D1–D7 all audited; MINOR-1 (a migration header contradicting its own code) fixed in the pass; INFO-1 recorded. ⛔ **SELF-REVIEW for `1069711c`** — the reviewing session wrote it; independent only for `d899ceb3` + `20261003002000` | 2026-08-24 | [review](../reviews/adr-0136-deferred-signoff-review.md) |

PDF·P3 + the user-profile redesign concluded 2026-08-25 (both approved; gate step 2 discharged by the merged-tree run); AFF4 is the next milestone (ruled, not started). Rotated 2026-08-25, verbatim apart from the link repoint.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **PDF·P3 (Printing Cases)** | ✅ **APPROVED** (pass 2) — 5 non-blocking N-findings, all filed. Gate 2 then re-ran **GREEN**, so the verdict no longer rests on the RED-accepted run. | 2026-08-25 | [phase-p3-review.md](../reviews/phase-p3-review.md) § Pass 2 |
| **User-profile redesign + AFF3/AUD1** | ✅ **APPROVED (r2)** — looped: r1 **CHANGES REQUESTED** on B1, a hospital-tier audit writer passing no org (real, and wider than filed — it also let platform_admin read tenant rows). Fixed as a class + leg 5 bounded; r2 re-verified by construction, 5 mutations. r1's M6/M11 withdrawn as measured-false | 2026-08-25 | [review](../reviews/user-profile-redesign-review.md) |

## Rotated at AE1's Record step, 2026-08-27

> AE1 round 1, superseded by round 2 (same report file, § ROUND 2). Kept because the round-1
> verdict and its three later-refuted specifics are part of the record.

| ~~AE1 — authz integrity & privilege hardening (round 1)~~ | ~~CHANGES REQUESTED~~ | 2026-08-27 | [review §1–13](../reviews/authz-ae1-review.md) |

## Rotated from PROGRESS.md § QA Verdicts 2026-08-27 — the AE1 round-2 and AFF4 rows

> Retention: the live table keeps the current milestone only. AE1 concluded (PO-approved and
> merged 2026-08-27), which concluded AFF4's row with it. ⚠ Both are **compressed to this
> index's shape** — feature name → verdict → date → review file. The AE1 row's rationale (the
> round-2 blocker was discharged by fixing the FIXTURE, not the mutation; re-run 16/16 · 0
> ERROR, 49 assertions before and after) is deliberately **not** restated here: it lives in
> [authz-ae1.md](authz-ae1.md) and in the linked review. Round 1 is the struck row in the
> section above, same report file.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **AE1 — authz integrity & privilege hardening (round 2)** | ⛔ **CHANGES REQUESTED → ✅ ALL DISCHARGED** | 2026-08-27 | [review](../reviews/authz-ae1-review.md) |
| AFF4 — org affiliation, staff data, voided tense | APPROVED | 2026-08-26 | [review](../reviews/aff4-review.md) |

## ↩ Rotated from PROGRESS.md § QA Verdicts 2026-08-31 at the AE3 cleanup — retention (current milestone only)

AE3 concluded 2026-08-31, so AE2's row is no longer the current milestone. Verbatim apart from the
link repoint. ⚠ The row deliberately omits a head sha — *measure, never quote* — and that omission
is preserved, not repaired.

| Phase / Feature | Verdict | Date | Report |
| --- | --- | --- | --- |
| **AE2** — affiliation/person tenancy, r3 (reviewed post-drop; ⛔ head sha deliberately omitted — measure, never quote) | **APPROVED** | 2026-08-28 | [r3](../reviews/authz-ae2-review-r3.md) |
