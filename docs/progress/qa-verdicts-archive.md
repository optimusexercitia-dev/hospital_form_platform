# Archive — QA Verdicts (full verbose form)

> Archived verbatim from PROGRESS.md on 2026-06-25 at the §7 progress-tracker cleanup.
> This is the durable detail; PROGRESS.md keeps only a one-line pointer.

> PROGRESS.md keeps a collapsed table; full per-phase reviews are under docs/reviews/.

| Phase | Verdict | Report | Blockers/Majors | Follow-ups carried |
| ----- | ------- | ------ | --------------- | ------------------ |
| Multi-Tenancy (organizations/hospitals/org-admin, RLS rewrite, multi-org PHI guard) | **APPROVED** (2026-06-25) | [multitenancy-review.md](docs/reviews/multitenancy-review.md) | None (0 blockers, 0 majors). Platform_admin wall complete (74-assertion pgTAP 171 suite; zero tenant-table rows for platform_admin; PHI tables raise 42501 on direct SELECT; `get_*_patient` RPCs return null). PHI duty separation confirmed: `can_read_case_patient` excludes `is_org_admin_of_*` term at DB layer. Multi-org PHI guard: `is_pqs_member` chokepoint closes entire global-PQS surface; per-predicate `not is_multi_org()` guards are correct defense-in-depth; module-off asserts raise; 18-assertion pgTAP 173 suite covers all layers. Cross-org isolation proven for all personas across two orgs (org_admin sees own org only; commission members see own commission only; audit tier access correct). Service-role escalation minimal and correctly gated (platform/actions.ts `assignOrgAdmin` only, behind `requireAdmin()`; role hard-coded, never from formData). Route handlers gate with `getCommissionAccessByOrg` + `role !== 'staff_admin'`; no service-role in any route handler. All existing immutability invariants (audit guard trigger, form version/section/item trigger, submit RPC) unaffected. ADR 0041 present and complete. E2E 292/0/0 (124 skips = NSP+referral module-off quarantine, by design). Architecture Rules 1/6/9/10/11/12 satisfied. | INFO-1: `is_multi_org()` count scan inside `is_pqs_member` — correct and cheap in practice; future materialized-view optimization is reasonable at scale. INFO-2: 124 E2E skips are NSP/referral module-off quarantine; will lift when NSP-per-org ships. INFO-3: legacy `getCommissionAccess(slug)` (single-arg) still present in `src/lib/queries/session.ts` — no longer called by any route, but should be removed or deprecation-marked to prevent future misuse. |
| Form Builder Enhancements (mini-phase) | **APPROVED** (2026-06-23 re-review) | [form-builder-enhancements-review.md](docs/reviews/form-builder-enhancements-review.md) | None (0 blockers, 0 majors). **MAJOR-1 RESOLVED** (`b80c5ba`+`cf6dcfb`+`ef1e040`): `toCondition` now coerces number-target value to a JSON number; `toDrafts` read-back fixed; `assert_condition_op_target` rejects non-numeric JSON for number targets at publish time (pt-BR); E2E AC-15 covers the multi-digit lexical≠numeric case (answer 10→shows, 3→hidden). MINOR-1/2/3 and INFO-1 all cleared. Final gate state: tsc 0, lint 0, vitest 113/113, pgTAP 870/870, FBE 15/15 + phase4 8/8 = 23/23. Security/RLS/integrity PASS — relaxed CHECKs are supersets, Rule 5 immutability covers new columns (blanket guard trigger), Rule 11 holds (`observation` never in audit log; `answer_map` value-only), `get_response_for_signoff` gating byte-identical, evaluator parity confirmed. 0 net-new E2E failures vs `main` baseline. | INFO-2: `form_items_options_shape` superset is data-dependent (bare-string rows), not predicate-pure — noted, no action required. INFO-3: pre-existing full-serial-suite contamination (~17–19 on branch AND `main`) — separate spec-isolation effort, not this feature's gate. INFO-4: FBE E2E builds fixtures in seeded CCIH commission — harden to probe commission if it joins the full gate matrix. |
| `case_phase_results` (Per-Phase Categorical Result + Manual Override) | APPROVED (2026-06-23) | [phase-results-review.md](docs/reviews/phase-results-review.md) | None (0 blockers, 0 majors). RLS commission-scoped on `phase_results` + `case_phase_offered_results` (member-read/staff_admin-write); both DEFINER RPCs (`compute_case_phase_result`, `set_case_phase_result_override`) enforce authz internally with pinned `search_path`; dual-entry override authz correct (ativa: assignee OR staff_admin; concluida: staff_admin + non-terminal guard HC060); HC057/HC058/HC060 all mapped to pt-BR; `in_case_rpc` GUC pattern consistent with all existing case RPCs; Rule 5 (immutability) holds — ruleset frozen at publish, snapshotted at case creation, result written atomically at conclude; Rule 11 (audit) holds — override fact+option-id audited, reason NEVER copied, result rides existing status-change row via updated allow-list; `condition-vectors.json` byte-unchanged; `walkResultRuleset` drift confirmed zero (vector-driven Vitest); feature is correctly NON-PHI; `as unknown as string` casts justified; all surfaces pt-BR; a11y met; flag ships OFF. All plan deliverables implemented including post-conclusion correction. pgTAP 45/45 + Vitest 57/57 + E2E 7/7 (AC-1–AC-6, AC-K). Architecture Rules 1/3/5/9/10/11 satisfied; Rule 12 N/A (confirmed non-PHI). | INFO-1: `result_override_reason` readable via raw PostgREST on `case_phases` for any `can_read_case` user (intentional: user-authored business justification, not PHI; excluded from `get_case_detail` and audit log). INFO-2: `add/update_template_phase` do not assert `case_phase_results` flag (ruleset can be authored flag-off; compute early-returns; by design per plan). |
| 23 — Patient Identity & Cross-Committee Linkage | APPROVED (2026-06-22) | [phase-23-review.md](docs/reviews/phase-23-review.md) | None (0 blockers, 0 majors). `patient_xref` REVOKE + QPS-only SELECT policy confirmed; `app.app_secrets` REVOKED from authenticated+anon (GRANT SELECT to service_role only; not PostgREST-exposed; hard-fail on absent pepper); all 5 DEFINER doors re-gate on `is_pqs_member` or `can_read_referral_phi`; audit emits `patient.searched`/`patient.viewed` on GLOBAL chain with 12-char truncated key only (never raw MRN); both `dispose_*_phi` replacements machine-verified 1-line diff from originals; AFTER-DELETE xref trigger stamps retained row with real disposal reason; triggers always-on (flag gates RPCs+UI only); PHI-free trajectory bundle confirmed (entity codes + commission names only, no PHI table JOINs); `patient_access_audit` selects no `metadata`/`summary` columns; TS `as unknown as` casts justified (jsonb Returns:Json); types regenerated; typecheck+lint 0; pgTAP 39/39; E2E 15/15 (all 11 ACs); 10/10 regression sweep. All 3 binding requirements met. Architecture Rules 1/9/10/11/12 satisfied. | INFO-1: `patient.searched`/`patient.viewed` audit rows not surfaced in `patient_access_audit` (by design — key-derived UUID not in xref entity_ids; access audit shows PHI record events, not QPS search trail). Follow-ups (non-blocking, per plan): `dispose_referral_phi`; pepper rotation; case+referral "ver trajetória" QPS links (need public PQS boolean); access audit on deep-link entry; SPEC-P22-001 Phase-22 spec defect. |
| `case_patient` (Third PHI Module; ADR 0038) | APPROVED (2026-06-22) | [case-patient-review.md](docs/reviews/case-patient-review.md) | None (0 blockers, 0 majors). `case_patient` REVOKE + audited single-door confirmed at all layers; `can_read_case_patient` = live broad `can_read_case` (deliberate divergence, documented); write gate coordinators-only confirmed (pgTAP + E2E AC-3a-rpc); `log_audit_access` allow-list carries all 11 prior verbs + appends `case_patient.read` (no verb dropped); `dispose_case_phi` gated/one-shot (HC056)/audit-PHI-safe/skeleton-preserved/both bypass flags set; `create_case_from_template` + `get_case_detail` REPLACE additive-only (no regression); flag ships OFF; PHI never in audit log. All 8 ADR-0038 decisions implemented; all 15 E2E flows (case-patient.spec.ts) green; pgTAP 35/35. Architecture Rules 1/9/10/11/12 satisfied. | INFO-1: `dispose_case_phi` summary embeds `case_number` (non-PHI governance metadata per ADR 0036 classification; no action required). INFO-2: `dispose_referral_phi` parity gap is pre-existing (tracked in ADR 0038 Consequences; out-of-scope for this increment). CN-APP-AC4 pre-existing on `main` (not a regression). |
| 22 — Inter-Committee Case Referrals | APPROVED (2026-06-21) | [phase-22-review.md](docs/reviews/phase-22-review.md) | None (0 blockers, 0 majors). `referral_patient` REVOKE + audited single-door confirmed at all layers; `can_read_referral_phi` NO `is_admin` term verified; three-layer free-text PHI lockdown (SELECT-policy swap + column REVOKE + `get_referral_detail` body-gate) sound; `can_read_case` QPS term flag-gated + before `case_access` fallback + no target-commission leg; HC076 gate correct; no service-role download path; all 6 Phase-22 audit verbs on allow-list; QPS dashboard gated on `isPqsMemberSelf()`; list/hub/dashboard PHI-free. All 16 plan decisions implemented; all 8 E2E flows green. Flag ships OFF. Architecture Rules 1/6/7/9/10/11/12 satisfied. | INFO-1: ADR 0037 HC078 description omits `referral_target_analyst` from `set_referral_patient` entitlement list — prose-only omission, no security implication; fold into §6 Record ADR update. |
| PHI/HIPAA Remediation (WS 0/A/B/C/D/E) | APPROVED (2026-06-20) | [phi-remediation-review.md](docs/reviews/phi-remediation-review.md) | None (0 blockers, 0 majors). `event_patient` lockdown sound at all layers (DB predicate, REVOKE, DEFINER single-door RPC, TS query layer); 6 `.viewed` verbs in DB allow-list and all 6 detail-read emit sites present; `dispose_event_phi` gated/one-shot/audit-PHI-safe/skeleton-preserved; docs accurately reflect implemented controls (no residual encryption claims; LGPD/ANVISA/CFM correctly framed). Architecture Rules 1/9/11/12 satisfied. | INFO-2 (Phase 19 gate): the 22 classified PHI free-text columns must be a hard AC for Phase-19 surveyor/evidence export. |
| 0 | APPROVED | [phase-0-review.md](docs/reviews/phase-0-review.md) | None | MINOR-1: set lang="pt-BR" in layout.tsx (Phase 2); MINOR-2: close stale supabase-start follow-up; INFO: update ADR 0001 to note --passWithNoTests removed |
| 1 | APPROVED (re-verified 2026-06-12) | [phase-1-review.md](docs/reviews/phase-1-review.md) | All resolved in M8: MAJOR-1 (USING clause fix + demotion test); MAJOR-2 (3 sign-off immutability tests added); MINOR-1 (eval_condition search_path); MINOR-2 (profiles never-deleted: policy split + trigger); MINOR-3 (version/commission guard trigger) | INFO-1: consider revoking anon DML/EXECUTE grants in Phase 8 hardening |
| 2 | APPROVED + RE-REVIEW APPROVED (2026-06-12) | [phase-2-review.md](docs/reviews/phase-2-review.md) | All resolved: MAJOR-1 (bad-creds selector, bug P2-001 → `f1c561f`); BLOCKER P2-002 (post-login race) fixed (`c808f8d`+`760d6a4`, ADR 0009) & load-verified 430/430. Re-review of the auth hot-path fix: APPROVED | INFO-1 (ADR 0007) done; INFO-2 (ADR 0008) done; carried: prod must use asymmetric JWT signing keys (Phase 8 deploy checklist) |
| 3 | APPROVED (2026-06-12) | [phase-3-review.md](docs/reviews/phase-3-review.md) | None | MINOR-1 ✅ RESOLVED 2026-06-12 (backend, `revalidateCommissionPages` — assign/remove now revalidate `/admin/comissoes/[slug]` too); MINOR-2 ✅ RESOLVED 2026-06-12 (frontend, `ConfirmRemoveButton` now types against `ActionState` from `@/lib/admin/actions`); both pre-record per human request. **Carried:** INFO-1 — AC1 E2E asserts the coordinator email appears in the roster but not that the "Coordenação" RoleBadge renders (add to Phase 4 test-hardening). Post-fix verification: lint + typecheck + unit (20/20) green; full E2E NOT re-run (both fixes provably non-runtime — a `revalidatePath` cache hint can't regress a passing nav-fresh test, and a type-only import swap has no runtime effect), so the 43/43 run stands. |
| 4 | APPROVED (re-review 2026-06-12) | [phase-4-review.md](docs/reviews/phase-4-review.md) | All resolved: MAJOR-1 ✅ keyboard-only test added (`e2e/phase4-builder.spec.ts`, 8/8 green); MINOR-1 ✅ `contextOfItem` FK hop comment (backend); MINOR-3 ✅ `revalidateBuilder` bracket comment (backend) | MINOR-2: INFO only — no action required (version history `?v=` scope check passes defense-in-depth via RLS) |
| 5 | APPROVED (2026-06-13) | [phase-5-review.md](docs/reviews/phase-5-review.md) | None (no blockers or majors) | MINOR-1 (backend, Phase 6): add `p_section_id` version guard in `save_section_answers`; MINOR-2 (backend, Phase 6): distinguish `check_violation` sources in `saveSection` action error mapping; INFO-1 (tester): remove unused `Locator` import in `phase5-wizard.spec.ts` |
| 6 | APPROVED (2026-06-13) | [phase-6-review.md](docs/reviews/phase-6-review.md) | None (no blockers or majors). P6-001 (BLOCKER) already RESOLVED in-phase. Verified the `signoffs_insert`/`signoffs_select` rewrite (`20260613090003`) is byte-equivalent on the signer-role rule — only the eval context moved to SECURITY DEFINER; `responses_select`/`answers_select` untouched (Phase-7 in_progress-answers invariant holds); all new definer fns pin `search_path` + are internally gated. P0012 live + single-sourced with queue readiness. Phase-5 MINOR-1/MINOR-2 folded in. | INFO-1: no answer-lock between sign-off and submission (ADR 0016 v1 limitation; acceptable — submit re-checks required/visibility; future: invalidate sign-off on post-sign answer change); INFO-2: `Signoff.signedByName` typed non-nullable vs query `string \| null` — sound via adapters, consider aligning; INFO-3: Phase-8 carry-forwards unchanged (revoke anon grants; prod asymmetric JWT keys) |
| 7 | APPROVED (2026-06-13) | [phase-7-review.md](docs/reviews/phase-7-review.md) | None (0 blockers, 0 majors). Security invariant (in_progress-answers isolation) confirmed sound at all layers: `case_phases` schema has no answer columns; `case_phase_answer_map` is submitted-only; `list_cases_board`/`get_case_detail` definer reads expose status/submitted answers only; `responses_select`/`answers_select` unchanged. HC0xx remap complete — no live P0010–P0022 raisers remain. Evaluator reuse confirmed — `eval_condition` + `evalCondition` + `condition-vectors.json` unchanged. | MINOR-1: HC017 (create_case_from_template with unpublished form) not directly asserted in pgTAP — add to Phase 8 test-hardening; MINOR-2: `archive_process_template` lacks status guard (archived→archived is silently idempotent); INFO-1: stale comment in E2E spec referencing "P0022/HTTP 500" (should be HC022/HTTP 400); Phase 8/9 carry-forwards unchanged (revoke anon grants; prod asymmetric JWT keys) |
| 8 | APPROVED (2026-06-14) | [phase-8-review.md](docs/reviews/phase-8-review.md) | None (0 blockers, 0 majors). RLS crux confirmed: in_progress-answers invariant holds at list/detail/export/dashboard paths; definer RPCs gated + search_path pinned; B6 anon/PUBLIC EXECUTE revoke complete + durable (pgTAP tests 15–17). ADR 0020 consistent SQL↔TS. All 12 AC clauses covered. | MINOR-1: CSV export ignores active date filter (route reads `?form` only; RPC has no date params — thread `p_from`/`p_to` through route→query→RPC); MINOR-2: form tab badge shows all-time count while body headline shows date-filtered count (display inconsistency — either pass range to `listDashboardForms` or make all-time explicit). INFO-1: `totalSubmitted` derived from `submissionsOverTime` sum (single-source concern for future). INFO-2: prod asymmetric JWT keys → Phase 9 deploy checklist (carry-forward from Phase 2). **MINOR-1/MINOR-2 RESOLVED in-phase** (backend `919d86a` mig `…090015` + frontend `421be85`); INFO-1 addressed; re-verified 106/106. |
| 10 | APPROVED (2026-06-15) | [phase-10-review.md](docs/reviews/phase-10-review.md) | None (0 blockers, 0 majors). Sign-own-row DEFINER bypass + explicit `can_sign_meeting` re-check confirmed correct. Storage immutability, child-lock, anon/PUBLIC EXECUTE revoke sweep, and all HC032–HC037 guards verified. | MINOR-1: Cancel button shown on `assinada` meetings (`meeting-lifecycle-actions.tsx` L77 — add `&& status !== 'assinada'`); MINOR-2: `conclude_meeting` `present_count` snapshot + attendees-panel live count include external guests (add `user_id IS NOT NULL` filter in SQL + `&& a.userId !== null` in UI). Both pre-record per user preference. |
| Cases-Extras (R1–R5) | APPROVED (2026-06-14) | [cases-extras-review.md](docs/reviews/cases-extras-review.md) | None (0 blockers, 0 majors). RLS confirmed on all 6 new tables + `case-documents` storage bucket (member-read/staff_admin-write; no storage UPDATE/DELETE). `app.apply_case_status` DEFINER core: every public entry point (`set_case_status`, `close_case`, `cancel_case`) carries explicit `is_staff_admin_of OR is_admin` gate + search_path pinned. `'aberto'` liveness sweep complete — verified by dedicated pgTAP regression test (submit while `em_revisao` advances phase) + E2E AC-SubmitWhileCustomStatus. Phase-7 in_progress-answers invariant untouched. HC024–HC027 mapped to pt-BR. Architecture Rules 1/6/7/9/10 satisfied. | MINOR-1: `case-action-item-form.tsx` missing "Nunca inclua dados de paciente." PII warning (present in event-form and document-upload dialogs — CLAUDE.md §1 consistency). INFO-1: `caseStatusIsTerminal` twin duplication justified (next/headers boundary); future clean-up: extract to a side-effect-free `case-statuses-pure.ts`. INFO-2: no guard prevents archiving ALL terminal statuses (v1 operational risk only, no security implication). INFO-3: no dedicated ADRs for R1/R3/R4 (plan doc is the decision record). INFO-4: remote `db push` (092000–092006) still pending explicit human/lead go-ahead — must precede Phase 9. |
| Case data-model adjustments (D1–D15) | APPROVED (2026-06-14) | [case-model-adjustments-review.md](docs/reviews/case-model-adjustments-review.md) | None (0 blockers, 0 majors). Dropped-helper landmine (`app.case_status_is_terminal`) fully cleared — every liveness-sweep function restated in 093001–093003 with a fixed-enum check; grep confirms zero surviving references at runtime. RLS on all 3 new tables (member-read/staff_admin-write via `commission_of_case`/`commission_of_template`; no cross-commission leakage). HC030 same-commission guard on `process_template_outcomes` mirrors HC026. `recompute_case_status` AFTER trigger: no DELETE event (cascade-safe), depth-1, no recursion. Phase-7 in_progress-answers invariant untouched (no answer columns in extended board/detail RPCs). Anon/PUBLIC EXECUTE re-revoked on all replaced/new public functions. Fixed status single-sourced in `src/lib/cases/case-status.ts`. HC020 (activate early-exit) vs HC025 (DB trigger) distinction confirmed coherent. | MINOR-1: HC028 (outcome required) and HC031 (unsettled phases) not mapped in `mapCaseError` in `src/lib/cases/actions.ts` — server errors fall through to generic pt-BR message; client-side gate mitigates in normal usage. MINOR-2: No `docs/decisions/0024-*.md` ADR (plan doc serves as the decision record but the numbered ADR series should be extended). |
| 11 — Interviews | APPROVED (2026-06-15) | [phase-11-review.md](docs/reviews/phase-11-review.md) | None (0 blockers, 0 majors). New RLS shape (`can_write_interview` DEFINER + participant-write grant) verified: recursion-free, search_path pinned, anon/PUBLIC EXECUTE revoked across all 16 RPCs + 3 DEFINER helpers + 1 thin-read, storage policy path-segment indexing sound, child-lock correctly excludes attachments table per ADR intent. All 14 plan decisions implemented. All 8 PHASES.md AC clauses covered by E2E (9 tests, AC2 split into AC2a/AC2b). HC038–HC041 + HC021 mapped to pt-BR; no raw Postgres errors reach UI. Architecture Rules 1/3/6/7/9/10 satisfied. P11-001 BLOCKER resolved pre-review. | MINOR-1: `canEditContent` (which gates upload/link controls in `AttachmentsPanel`) requires `isEditableInterviewStatus`, hiding upload UI when interview is `concluida` or `cancelada` — but the server (and ADR 0026) intentionally allows post-conclusion attachment uploads (signed transcript / late evidence). UI is more restrictive than the server contract; post-conclusion uploads require a reopen/re-conclude cycle. No security implication; consider a separate `canUpload = canWrite && status !== 'cancelada'` flag to honor the ADR intent. **→ RESOLVED 2026-06-15 (P11-MINOR-1): frontend added `canManageAttachments = canWrite && status !== 'cancelada'` passed only to `AttachmentsPanel`; AC9 E2E locks it in. Re-confirmed green 152/152.** |
| 14a — NSP Foundation | CHANGES REQUESTED → ✅ APPROVED (re-verified 2026-06-18) | [phase-14a-review.md](docs/reviews/phase-14a-review.md) | 0 blockers, 1 major, 1 minor | M1: add SELECT policy on `pqs_department` (one-line additive migration; Rule 1); N1: add `message?` field to `ActionState`/`NotifyEventState` so success text is not returned in the `error` field **→ M1+N1 FIXED 2026-06-18 (backend: mig `…121005` + `ActionState.message`); pgTAP 409/409, 14a spec 16/16; **QA RE-VERIFIED ✅ APPROVED 2026-06-18** (M1 live `pg_policies` + anon-deny confirmed; N1/I2 closed; 4-action success-in-`error` smell → deferred INFO follow-up)** |
| 14b–14d — Triage, RCA & CAPA | CHANGES REQUESTED → ✅ APPROVED (re-verified 2026-06-18) | [phase-14-review.md](docs/reviews/phase-14-review.md) | 1 BLOCKING (BLOCKER-1) + 1 MINOR — both resolved | BLOCKER-1 RESOLVED: `triage_disposition` line 392 qualified as `event_triage.event_id` — no more SQLSTATE 42702; pgTAP 141 plan expanded to 44 with 5 direct RPC-return assertions (is_sentinel/verdict/review_pathway/rca_due_date/row-count), all pass. MINOR-1 RESOLVED: `set_pqs_rca_due_window` audit row corrected to `action='pqs_config.rca_due_window_changed'`, `entity_type='pqs_department'`, with old/new value diff. pgTAP 516/516 PASS. All AC, RLS, PHI isolation, immutability, state machines, conclude gates, audit allow-lists, and UX verified PASS. 0 open findings. |
| Case Narratives | CHANGES REQUESTED (2026-06-19) | [case-narratives-review.md](docs/reviews/case-narratives-review.md) | BLOCK-1 (functional): `narrative-type-dialog.tsx` L80 `form.set("id",…)` must be `form.set("narrativeTypeId",…)` — edit always fails silently; BLOCK-2 (a11y): `narrative-slot-card.tsx` Editar button missing `aria-label` | None carried |
| 13 — Audit Trail | APPROVED (2026-06-18) | phase-13-review (inline) | None (0 blockers, 0 majors). Core security crux verified: append-only guard (`guard_audit_immutable_trg` BEFORE UPDATE OR DELETE, `HC042`, fires even for `service_role`); RLS SELECT = `is_admin()` OR `is_staff_admin_of(commission_id)` with NO INSERT/UPDATE/DELETE policy; zero anon/PUBLIC grant (revoked by 090012 default-privileges + pgTAP test 25 confirms); hash chain commits to every semantic column (ADR 0029 Q3); per-commission + global chains independent with advisory-lock serialization; `verify_audit_chain` DEFINER, `is_staff_admin_of`/admin-gated internally; `log_audit_access` surface is positive-allow-listed to exactly 3 actions; all allow-lists exclude `answers.value`, `*_md`, `label`, `question_explanation`, `content`, `options`, `description`, `minutes_md`, `summary_md`, `note` (Rule 11 data-minimization). All 8 AC clauses covered (AC-1a–f: 6 mutations, AC-2: append-only, AC-3: RLS scoping + route gating, AC-4: .read/.export rows, AC-5: filters, AC-6: CSV count, AC-7: integrity verify, AC-8: keyboard-only). DEFINER `search_path` pinned on all 6 writer/trigger functions. `import 'server-only'` on `src/lib/audit/access.ts`; service-role key never NEXT_PUBLIC_*. Architecture Rules 1/9/10/11 satisfied. | MINOR-1: `guard_audit_immutable` raises a pt-BR message string (`'os registros de auditoria são imutáveis (somente inserção)'`) but the `HC042` constant in `src/lib/audit/messages.ts` maps it to the English-ish `AUDIT_MESSAGES.appendOnly` (`'Os registros de auditoria não podem ser alterados nem excluídos.'`) — the mapping is correct pt-BR and will only fire on an unreachable code path (no application write path touches UPDATE/DELETE on `audit_log`), so this is purely cosmetic and cannot reach the UI. **RESOLVED 2026-06-18 (backend, bundled with 14a B5):** TS-side only — `AUDIT_MESSAGES.appendOnly` aligned to the DB RAISE wording (`'Os registros de auditoria são imutáveis (somente inserção).'`); the internal HC042 RAISE text was NOT churned (no migration). typecheck + lint clean. |
| 12 — Case Timeline | APPROVED (2026-06-16) | [phase-12-review.md](docs/reviews/phase-12-review.md) | None (0 blockers, 0 majors). Read-only composition over existing RLS-scoped reads; no new attack surface. `case_phases_select` member-read confirmed correct for direct bar-timestamps read (bar timestamps not exposed by `getCaseDetail` RPC). Foreign-commission caller gets 404 at route + empty array at DB (Security E2E). event-model.ts is genuinely pure (zero imports). Both dedups (interview echo by `registry_event_id`; meeting echo by `kind='meeting'` drop) correct and documented in ADR 0027. Both layouts, all 8 types, adaptive axis, open/closed lifecycle, legend-filter, URL persistence, keyboard flow all verified. Architecture Rules 1/6/7/9/10 satisfied. | MINOR-1: `TimelineEventSheet` (`timeline-event-sheet.tsx`) missing `DialogPrimitive.Description` — Radix logs a dev warning and screen readers hear only the title when the dialog opens. Fix: add `<DialogPrimitive.Description className="sr-only">{meta.label} · {formatEventDate(event)}</DialogPrimitive.Description>` after `DialogPrimitive.Title` in `SheetBody`. Pre-record per user preference. **→ RESOLVED 2026-06-16 (frontend):** added the `sr-only DialogPrimitive.Description` (`{meta.label} · {formatEventDate(event)}`) after the Title in `timeline-event-sheet.tsx`; the Radix "missing Description" warning is gone. Lead also cleared 2 unused-var lint warnings in `e2e/phase12-timeline.spec.ts` (dead `SEEDED_INTERVIEW_ID` const + superseded `stickyHeader` locator — pure dead-code, **no assertion changed**). `npm run typecheck` + `npm run lint` clean (0/0); full suite re-run serially to re-declare green. |
| Case Access Control & "Meus Casos" | APPROVED (2026-06-19) | [case-access-control-review.md](docs/reviews/case-access-control-review.md) | None (0 blockers, 0 majors; CA-001 BLOCKER + CA-002 MAJOR both resolved in-phase). All 25 ADR-0033 decisions and all 11 ACs verified. RLS spine (`can_read_case`/`can_write_case_content`/`can_write_case_narrative`) correct, 82-point pgTAP truth-table, no anon/PUBLIC EXECUTE leak. Phase-7 submitted-only invariant structurally preserved. PHI isolation (Rule 12) untouched. Flag-OFF fallback proven equivalent. `canEditNarrative` TS mirror matches DB predicate exactly post CA-002. CA-001 regression guard (provolatile='v' + behavior assertion) durable in pgTAP. | INFO-N1: `case_interviews` still member-read (deferred fast-follow per ADR 0033 D2). INFO-N2: ~18 pre-existing E2E harness-debt flakes (animation timing + shared-DB cascade) — fix = global `reducedMotion` + per-test DB isolation, carry to next increment. INFO-N3: coordinator access panel cannot show live "currently read/write" grant indicator (no `listCaseAccess` query; deferred UX improvement). INFO-N4 (CARRY): legacy `update_case_narrative_body` raises P0002 (not 42501) for a non-reader with `case_access` ON — unreachable on the new UI path; no security implication; align error code if function is ever exposed to non-coordinators in a future increment. |
