# PDF·P1 — PDF document printing: Forms + full skeleton (COMPLETE 2026-08-08)

> Archived phase record, rotated out of PROGRESS.md at the §6 Record step (2026-08-08).
> ADR [0104](../decisions/0104-pdf-document-printing-module.md) (+ Amendments A1–A6, ratified
> at implementation) · plan [pdf-document-printing.md](../plans/pdf-document-printing.md) §2 ·
> QA **APPROVED (r2)** [review](../reviews/phase-PDF-P1-review.md). Built on worktree branch
> `worktree-pdf-printing-p1`, started + substantially completed 2026-08-07, recorded 2026-08-08.
> Migrations `20260913000000`–`20260913000400`. Flag `document_printing` ships **OFF**
> (seed forces ON for local/E2E). *(Ships-OFF is the ORIGINAL rollout posture and is
> **superseded for prod 2026-08-10** — PO ruled the flag **ON permanently** once the Gotenberg
> resource went live; see this file's Deploy-prerequisites section.)* Commits: `0d1dca8` (contracts) · `e453c8d`+`c81f37a`
> (migrations+pgTAP+census) · `56e5529` (F1/F2 UI) · `e1daba9` (renderer+pipeline+serving+docs) ·
> QA fix wave `6da8e78` (ADR amendments) / `a33fb68` (labels) / `e53c6c9` (FIX-1..5) /
> `93f307a`+`a545b0a` (backend-state sweeps) · review `7e9c8f6`+`476b550`.

**Migration window note:** highest registered at start was `20260912000100`; the plan's
`20260911001000` figure was stale (two workstreams landed after it was written) — the
build-time re-check caught it, per the §0 substrate rule.

## Task table (final)

| # | Owner | Task | Status |
| --- | --- | --- | --- |
| B0 | backend | §8 [INF] register: catalog verification pass (audit_write signature, response-visibility predicate, door template, matcher, storage-policy shape, flag idiom) | ✅ done 2026-08-07 — findings reported to lead; key: no single response-visibility predicate exists (arm = the `responses_select`+targeted+admin disjunction, mirrored explicitly, NEVER invoker-RLS delegation); `/verificar` NOT in `PUBLIC_PATHS` → login-walled today (F1 must add it); deps: `uqr@0.1.3` + `pdf-lib@1.17.1` |
| B1 | backend | Contract-first: `src/lib/pdf/types.ts` (`DocumentPayload`) + provider/mint-action typed stubs posted for frontend | ✅ done 2026-08-07 — `src/lib/pdf/types.ts` (envelope + `form_response` body) · `src/lib/pdf-mint/actions.ts` (mint/revoke stubs) · `src/lib/queries/printed-documents.ts` (list + verification-lookup stubs); lint+typecheck green |
| B2 | backend | Migrations M1–M4 + `verification_lookups` (plan approval REQUIRED before authoring — new RLS shape + DEFINER doors) | ✅ done 2026-08-07 — lead-approved plan + Amendments A/B; migrations `20260913000000`–`…000300` applied via `migration up`; types regen; flag + seed force-ON + `FeatureFlags.document_printing` + `documentPrintingEnabled()` (anon has NO execute on `get_feature_flags` — catalog-verified); `documentId` added to `PrintedDocumentVerification` |
| B3 | backend | Pure renderer `src/lib/pdf/` + fingerprint test + ESLint purity gate (red-teamed) | ✅ done 2026-08-07 — render.ts (self-contained HTML, embedded IBM Plex data-URI faces via `scripts/generate-pdf-fonts.mjs` + @fontsource devDeps), primitives (letterhead/watermark/signature/qr-footer/section-table), `documents/form-response.ts` v1; fingerprint guard PROVEN-DETECTING (red on placeholder bootstrap AND red live on a CSS edit mid-build + intentional-mutation case); purity gate red-teamed (forbidden import → lint exit 1, recorded, removed); deps `uqr@0.1.3` + `pdf-lib@1.17.1` installed |
| B4 | backend | Forms data provider + `src/lib/pdf-mint/` orchestration (semaphore, Gotenberg client, all-or-nothing mint) | ✅ done 2026-08-07 — `src/lib/forms/pdf-payload.ts` (wizard-mirror visibility via `evalVisibility`; scalars/choices/matrix/risk/reference/repeating-group values; signoffs→attestations); providers registry (fail-closed); real mint action (Amendment A credentials pre-render, 3-permit semaphore, 30 s Gotenberg budget, upload-before-RPC + delete-on-failure, HC0D4 bounded retry); real revoke + list |
| B5 | backend | Serving route + pdf-lib overlay + verification RPC/rate-limit | ✅ done 2026-08-07 — `/api/documents/[id]` route (open door → service-role download → overlay on non-active → stream; path never reaches the client); `applyStatusOverlay` + unit tests (both stamps + active no-op byte-identity); real `lookupPrintedDocumentVerification` (in-module rate limit fronting the ONLY call path + `p_viewer` from verified session — declared-param + limiter PINNED by unit tests); **`/verificar` added to `PUBLIC_PATHS` in `src/proxy.ts` — proven live: logged-out GET `/verificar` + `/verificar/<token>` → 200, `/api/documents/<id>` → 307→/login (still gated, correct)**. ⚠ `getResponsePrintContext` is two-step: `hospitals_select` has NO member arm → letterhead names via caller-pre-authorized service-role read (documented in backend-state.md) |
| B6 | backend | pgTAP suite + ARM census/floor + diff-scoped policy sweep | ✅ done 2026-08-07 (red-first slice) — `312_printed_documents.sql` 69/69 on fresh reset; full suite 173 files/5444 PASS; A33 drills D1–D6 ALL RED-proven (mutations landed + md5-verified byte-identical restore); `ARM=census` HOLDS · `ARM=floor` HOLDS · diff-scoped door+rowdoor sweeps: 3/3 COVERED, 0 BLIND, 0 ERROR (verdicts hand-merged into findings files per the QO·A precedent) |
| B7 | backend | Docs: backend-state.md, docs/deployment/pdf-renderer.md, .env.example; local Gotenberg recipe | ✅ done 2026-08-07 — `docs/deployment/pdf-renderer.md` (pinned `gotenberg/gotenberg:8.24.0`, local recipe on :3010, Coolify runbook as USER handoff); backend-state.md PDF·P1 section; `.env.example` `PDF_RENDERER_URL` + `PDF_VERIFICATION_BASE_URL` (MIN-style server-only blocks); **end-to-end mint SMOKE GREEN** (`scripts/smoke/pdf-mint.smoke.ts` via `vitest.smoke.config.ts`, real Gotenberg 8.24.0): chefe.ccih mints a seeded submitted response → open door → downloaded canonical bytes `%PDF-`, >10 KB, **sha-256 === registry `content_hash`** → lookup returns the anemic tuple + `documentId` for the source-visible viewer |
| F1 | frontend | `/verificar` public pages (after B1 contracts) | ✅ done 2026-08-07 (`56e5529`) — new `(public)` route group + shell · `/verificar` landing (plain `<form>` + server-action redirect: works with JS off, the right floor for a damaged-QR fallback) · `/verificar/[token]` result + `loading.tsx`, `force-dynamic` so no verdict is ever cached past a revoke · `VerificationResult` renders only the anemic tuple, download link strictly on non-null `documentId`. Two lookup keys disambiguated by `?via=codigo`, never by sniffing a backend-owned format. Gated via backend's service-role `documentPrintingEnabled()`. (The "blocked pending B5" note in the row's original text cleared when B5 landed the `PUBLIC_PATHS` exemption + the real lookup.) |
| F2 | frontend | Mint dialog + printed-docs list + revoke dialog (frontend-design skill first) | ✅ done 2026-08-07 (`56e5529`) — `frontend-design` skill applied; `src/components/printing/` (mint button+dialog, "Documentos emitidos" panel, status chip, revoke dialog, shared pt-BR labels) wired into `…/dashboard/submissions/[responseId]`. Mint dialog names the FINAL/RASCUNHO mark before it exists; success moves focus to the download link (keyboard mint→download). Panel header renders immediately, only the registry read suspends; a failed read shows a notice, never an empty list. Revoke = closed reason class + mandatory PHI-free free text wired via `aria-describedby`. `canRevoke` reuses the page's existing `staff_admin` gate — no new permission signal. Downloads only via the registry's `downloadPath` (never a Storage URL). **QA MINOR fixed (`a33fb68`)**: the panel's reason-class lookup fell back to the stored value, so an unknown class printed the raw `wrong_data` identifier into the pt-BR UI; lookup moved to the single sanctioned `revokeReasonClassLabel()` in `labels.ts`, falling back to "Outro motivo" (the vocabulary's own catch-all — truthful that a class exists, silent on which). Swept `revokedReasonClass`/`reasonClass`/`REVOKE_REASON_CLASSES` across `src/`: the panel was the only site rendering a STORED class; the dialog only maps known options and already had a static pt-BR fallback |
| B8 | backend | QA fix wave (FIX-1..5, phase-PDF-P1-review) | ✅ done 2026-08-07 — FIX-1 purity gate re-cut to the PROPERTY (relative escapes banned; red-teamed 4 ways: `../supabase/server`, `../queries/printed-documents`, depth-2 `../../queries/*`, alias control — all lint exit 1, clean 0) · FIX-2 `final_phi_logo` fingerprint variant (FINAL chip + PHI band + logo pinned; 3 mutation drills each RED on the variant while canonical stayed green; + no-vacuous-fixture markup assertions) · FIX-3 migration `20260913000400` re-emits `revoke_printed_document` authority-first (not-found merged into 42501 — no existence oracle; ACL/attrs property-diffed identical; P0002 gone from the module) · FIX-4 four `format*()` call sites esc-wrapped (fingerprints UNCHANGED — proven no-op on well-formed output) · FIX-5 `312` → 73 tests (flag-off revoke 23514; platform_admin open → 0 rows + no audit row; table-level one-active 23505) + drill D7 (index dropped → t73 RED → restored+verified) + D4 re-proven on the reordered door. Gates: 312 73/73 fresh reset · test:db PASS · lint 0/0 · typecheck · vitest 81/1190 · ARM=census HOLDS |
| T1 | tester | E2E: mint→download→verify(logged-out)→revoke→overlay + keyboard-only mint flow | ✅ done 2026-08-07 — `e2e/pdf-printing.spec.ts` (7 tests) + `e2e/helpers/pdf-printing.ts`; **7/7 GREEN, run twice on independent fresh resets (14/14 total), 0 flakes, 0 bugs filed**. Covers: full lifecycle (mint FINAL-watermark confirm → panel chip "Ativo" → authenticated download 200/`%PDF-` → logged-out `/verificar/<código>` authentic tuple + no download link → revoke (reason class + free text) → chip "Anulado" → re-verify shows "Documento anulado" → overlay re-download 200/`%PDF-`, bytes differ from the pre-revoke download); supersession (2nd mint flips 1st to "Substituído", logged-out re-verify of the FIRST code renders the D6 recency sentence "Existe uma emissão mais recente deste documento…", not an error state); public reachability (`/verificar` 200 logged-out, short-code form round-trips to a real result, `/api/documents/<real-id>` logged-out redirects to `/login` rather than serving bytes); keyboard-only mint→download-link-focus (real Tab presses via `focusByTabbing`, Enter through confirm, `expect.poll` on `document.activeElement` at each hop, completed with a real keyboard-triggered download); `platform_admin` exclusion (`platform@test.local` gets a clean `404` on the response-detail route — `getCommissionAccessByOrg` returns no membership, so the page's existing `staff_admin` gate `notFound()`s before any detail read; no mint button/panel in the DOM); not-recognised code renders "Documento não reconhecido", asserted distinct from the "Não foi possível verificar agora" unavailable state. Fixtures: 5 distinct seeded CCIH submitted responses (deterministic id-ascending pool via `submittedResponseIds`, one per state-mutating test — no seed-row mutation). Run: `npm run build` → prod-standalone (`node .next/standalone/server.js`, :3000) → `npx playwright test e2e/pdf-printing.spec.ts --project=chromium --workers=1`, fresh `supabase db reset --local` before each run. Lint and `tsc --noEmit` both clean. One spec-authoring fix mid-loop (not an app bug): the mint dialog's "Fechar" ghost button and its icon-only X close button share the accessible name "Fechar" (text vs. `aria-label`) — disambiguated via `getByText('Fechar', {exact:true})` |
| Q1 | qa | Phase review `docs/reviews/phase-PDF-P1-review.md` | ✅ done 2026-08-07 — **APPROVED (r2)** ([r2 addendum](../reviews/phase-PDF-P1-review.md)): fix wave `6da8e78`/`a33fb68`/`e53c6c9`/`93f307a` verified by re-probing, not by reading commits — **both MAJORs CLOSED** (purity gate: 4 probe shapes now error incl. depth-2 and bare-directory; fingerprint: `FINAL_PHI_LOGO` variant + a vacuity control proving canonical/variant cover **disjoint** branches) and **MINOR-4/5/6/7/8/9 CLOSED**. Revoke door re-read from `pg_get_functiondef` — not-found merged into 42501, and the re-emit lost **no** property (`prosecdef`/`proconfig`/`proacl` identical). Migrations 320==320, lint + `tsc` exit 0, PDF unit tests 18 pass. **3 deferred by agreement, still open** (MINOR-1/2/3) + 2 one-line INFO (dead `'P0002'` allowlist entry `actions.ts:95` — deferred with MINOR-1; backend-state 312-count — fixed `a545b0a`). r1: **APPROVED**, 0 BLOCKER · 2 MAJOR (both guard gaps, closed in the fix wave) · 9 MINOR · 6 INFO; all security claims re-derived from the LIVE catalog; `ARM=census` re-run by qa (**HOLDS**, 450 gates/459 verdicts) |

## Lead notes (final)

- B2 plan approved with amendments: verification token/short-code generated in the mint ACTION pre-render (the QR is in the canonical bytes; door validates format + uniqueness); mint door checks the storage object exists at the derived path; 4 backend deviations acked (text+CHECK vocab · door-derived storage path · in-door overlay flag · service-role-only lookup with `p_viewer`). All ratified as ADR 0104 Amendments A1–A6 (`6da8e78`).
- Flag gate for public `/verificar`: NO anon grant on `get_feature_flags()`; backend provides a server-only service-role `documentPrintingEnabled()` helper; pages `notFound()` when off.
- **Full `e2e:prod` gate (lead, 2026-08-07): GREEN — 0 real failures / 1026 collected.** Run 1: 996 passed · 5 failed · 8 flaky · 12 did-not-run · 17 batches · 0 `reset FAILED` · accounted 1021/1026; the 5 fails + 12 did-not-run (b11 `phase14b-triage` ×3, b12 `phase16-accreditation-core` AC-0 +6 unrun, b17 `wizard-others-ux` +6 unrun) all sat in a recurring **PGRST002** PostgREST schema-cache degradation (same signature self-recovered as flakes in b3/b12/b16). Remedy per the house precedent: full `supabase stop`/`start`, then re-run of ALL affected specs through the gate → **86 passed · 0 failed · 0 did-not-run · accounted 87/87 · GATE GREEN**. No PDF·P1 spec failed in either run. (Ops note: launching `npm run e2e:prod` from PowerShell false-aborts exit 3 — the drift-check's `node -p` probes resolve empty in that spawn chain; run `bash scripts/e2e-prod-gate.sh` from Git Bash.)
- QA r2 residual, recorded for whoever deepens the tree: the purity gate's relative-escape ban enumerates depths 1–3; `src/lib/pdf/` nests one level today, so coverage exceeds reality by two levels. `import/no-restricted-paths` (path-resolving) is the proper close if the tree ever deepens.

## Open follow-ups at close (live copies in PROGRESS.md)

- `[ ]` **FUP-PDF-1** — creator-mint has no UI surface in P1 (the only response-detail screen is wholly `staff_admin`-gated; the door supports creator mint per D11). Candidate P2+.
- `[ ]` **FUP-PDF-2 (QA MINOR-1)** — SQLSTATE-allowlist can surface English Postgres text; sweep with the now-dead `'P0002'` entry in `SURFACEABLE_CODES` (`src/lib/pdf-mint/actions.ts:95`).
- `[ ]` **FUP-PDF-3 (QA MINOR-2)** — mint/revoke `returns printed_documents` re-opens the two withheld columns (`storage_path`, `verification_token`) to a direct PostgREST caller; needs a return-shape decision.
- `[ ]` **FUP-PDF-4 (QA MINOR-3)** — verification rate limiter is a global in-process counter (availability lever on a public surface); per-credential granularity + fix its stale "shown verbatim" comment.

## Deploy prerequisites (USER actions — both pending at close, ⬛ BOTH DISCHARGED 2026-08-10)

1. Remote `supabase db push` of `20260913000000`–`20260913000400` (needs user authorization).
2. Gotenberg Coolify resource per the runbook in `docs/deployment/pdf-renderer.md` (pinned 8.24.0, private network, caps). Flag `document_printing` stays OFF in prod until both are done.

**⬛ Status 2026-08-10 — both discharged; this section is history.**
1. **Remote `db push` ✅ done 2026-08-08** for the P1 batch (remote-catalog-verified), and the whole
   catalog is now level: 345 remote == 345 local files.
2. **Gotenberg Coolify resource ✅ ACTIVE AND WORKING 2026-08-10** (PO). With it live, the PO ruled
   **`document_printing` ON PERMANENTLY** in prod — **superseding ADR 0104's ships-OFF clause**. The
   flag was only ever a deploy-sequencing guard for the missing renderer. **P3/P4 must not
   re-assert the OFF posture.** ⚠ Recorded on the PO's operational confirmation — external
   infrastructure, not catalog-attestable.
