# Archive — Decisions (full verbose form)

> Archived verbatim from PROGRESS.md on 2026-06-25 at the §7 progress-tracker cleanup.
> This is the durable detail; PROGRESS.md keeps only a one-line pointer.

> PROGRESS.md keeps a collapsed one-line-per-decision table; the full rationale is here + in the linked ADRs under docs/decisions/.

| Date | Decision | ADR |
| ---- | -------- | --- |
| 2026-07-27 | **Flexible-Forms FF-1…FF-5 pulled pre-pilot (product-owner decision).** All **five** FF feature phases from ADR 0060 §3 (previously post-pilot) build **before the pilot** and **all gate the pilot deploy** (one remote deploy after the last gate; the pre-launch reset-OK window stays open throughout). Order: **FF-1 repeating groups → FF-2 matrix/risk matrix → FF-3 validation engine → FF-5 entity reference → FF-4 power authoring** — field types before the validators that target them; the riskiest surfaces (wizard/resume + instance engine, submit-authority `required_if`) earliest; FF-4 last so the library can snapshot every shipped type. Scope rulings from the grilling interview: **required-capable per phase** (FF-1/FF-2/FF-5 each relax the F3 never-required CHECK arm + extend `app.response_required_complete`); **instance-aware condition evaluation builds in FF-1** (same-instance sibling refs; both evaluators + golden vectors; aggregation contract unchanged); **`required_if` confirmed in FF-3** (+ FF-3 owns making the 4 F3 operators authorable); **FF-5 ships 3 lanes** (participant — with the INFO-2 PHI-read door — + commission + user); **FF-4 trimmed** to library + dynamic defaults — **calculated fields stay post-pilot** (`form_calculations` stays ADR-reserved). Each phase keeps its own ADR (0087+ at authoring; the 0060 "0061+" reservation was consumed) + flag (ADR 0060 §3 names, OFF-until-gate) + Phase Gate. F3's INFO-1/INFO-2/INFO-4 forward-notes become FF-2/FF-3/FF-5 obligations. Supersedes the *timing* of 0060 §3 + the 0071-plan out-of-scope line. Specs still to author: the five per-phase ADRs. | [0086](../decisions/0086-flexible-forms-pre-pilot.md) · [program](../plans/flexible-forms-program.md) |
| 2026-07-12 | **Pre-pilot release scope expansion (product-owner decision).** Pull **12** initiatives from post-pilot/deferred into the pilot release: **Phases 20–21**; **Referrals v2 R1→R5** (all); **Interviews v2**; **Ethics E1→E2→E3** (E1 = the m2 flag gate + respondent-exclusion RLS, first); **action-items hub satellites** + **case cross-link UI**; **§6.1 single-`memberships` collapse**; **supersession correction engine + UX** (0060 Gap 38). Only **Phases 18–19 stay post-pilot**; pilot ships later, after the whole block + Coolify deploy. Each item keeps its own ADR/plan/flag/Phase Gate; supersedes the *timing* in 0057/0064/0060/0050 + the §6.1 deferral. Specs still to author: Ethics E1/E2/E3, supersession, §6.1. | [0071](../decisions/0071-pre-pilot-release-scope-expansion.md) |
| 2026-07-12 | **Referrals v2 — Dialogue & Governance (pre-pilot expansion of Phase 22).** Evaluated a partner team's from-scratch referral data model vs the shipped module; adopt the missing **two-way dialogue** (Essential: `referral_messages` thread + `awaiting_information`/`waiting_on_committee_id`) + 12 deferred items (triage/SLA · reopen+answered/resolved split · `parent_referral_id` · assignments · committee-private notes · context versions · receipts · redaction · idempotency) across gated phases **R0→R5**; **avoid** `row_version` (redundant w/ DEFINER+`FOR UPDATE`), `referral_status_events` (redundant w/ hash-chained audit), PHI-in-`structured_context` (Rule 12), `referral_participants` ACL (forks auth), referral-local outbox (→ Phase 20). Extends (never contradicts) the Phase-22 core; pre-pilot/reset-OK behind `case_referrals`; R1 pilot-critical; HC block `HC0A0+`. | [0037-A1](../decisions/0037-inter-committee-case-referrals.md) · [plan](../plans/referrals-v2-dialogue-governance.md) · [eval](../design/temp/referral_data_model_evaluation.md) |
| 2026-07-10 | **Pre-Pilot Foundations Program — one collision-free plan for ADR 0060 + 0063 + 0064-E0 + DB-hardening Waves 3+4.** Sequence F0 (design gate) → F1 participants → F2 attachments → Phase 16 → F3 flexible-forms → F-cleanup. Six resolved collisions: **C-α** two ratified polymorphism dialects (owner-dispatch + typed registry) **close D12**; **C-β** `attachment_subjects` re-keys to the `participants` registry (one subject vocabulary; forces F1-before-F2); **C-γ** one merged Rule-12 sensitivity taxonomy (patient-PHI ×3 + professional-identity + attachments tier/label); **C-δ** compose disposal (participant-keyed then attachment-D10), **drop D5 patient-master** (participant model + `patient_xref` suffice); **C-ε** **cancel D6/§6.3** metadata item-types (0060 keeps the CHECK enum); **C-ζ** `form_upload` stays inert, drop `form_items.phi_policy`. R1 verified: no `is_multi_org()` in-tree; case-PHI door is per-hospital PQS-operator + per-commission. HC high-water HC093 → allocate HC094+. All pre-pilot / reset-OK; flags OFF (m2 gate). Branch `feat/pre-pilot-foundations-plan`. | [program](../plans/pre-pilot-foundations-program.md) · [0060](../decisions/0060-flexible-forms-foundation.md) · [0063](../decisions/0063-centralized-attachments-substrate.md) · [0064](../decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Case subject generalization → participants / roles / professional registry / case-types (E0 foundation for the Ethics Committee + other non-patient-centered committees)** — ONE generalized `cases` model, never a forked `ethics_cases`/`patient_cases` root; full generic `participants` + `case_participant_roles` + `case_participants` (+ no-polymorphic-FK subtype tables); `professional_profiles` as its **own audited sensitivity class** (case-scoped RLS + audited reads, NOT the patient-PHI isolated-single-door apparatus); `case_patient` generalized **1:1 → N per case**, re-keyed `patient_identifiers(participant_id)`, isolation/audited-door/disposal preserved and reworked **while the flag is still OFF ⇒ zero prod PHI migration**; **dedicated** `case_types` + `case_type_terminology` (org-scoped; `primary_subject_kind` / `default_visibility_policy`; pt-BR label bundles). Amends Rule 12 (now **patient-PHI + professional-identity** classes) + supersedes ADR 0038 cardinality. Access-spine (E1) + ethics procedure (E2) explicitly deferred. Product-owner decisions taken 2026-07-09; **not implemented**; eval [case-generalization-evaluation.md](../design/case-generalization-evaluation.md) | [0064](../decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Centralized-attachments substrate — DMS-handoff evaluation → 14e refinement** — adopt into the 14e core: single authorizing owner + non-authorizing `attachment_references`, descriptive PHI-safe `attachment_subjects`, `document_group_id`/`supersedes_id` (versioning/redaction), `confidentiality_label`, `scan_status` malware gate, `legal_hold`+`dispose_attachment_phi`; RESERVE kind-policy lookup / per-doc grants / retention; REJECT materialized effective-permissions cache + security-groups + `app_resources` + OCR pipeline. Per-feature metadata via 1:1 extension tables stays a reserved additive path (14e D2). Revised core DDL → [attachments-core-schema-draft.md](../design/attachments-core-schema-draft.md); not implemented | [0063](../decisions/0063-centralized-attachments-substrate.md) |
| 2026-06-25 | **Multi-Tenancy — pooled single-DB, `organizations → hospitals → commissions`; vendor `platform_admin` (provisioning-only, walled off all tenant data/PHI) vs customer `org_admin` (org-scoped super-user).** Pooled + silo-by-exception; org membership is a live DB read (not a JWT claim) so revocation can't lag; the ~60 `OR app.is_admin()` tenant/PHI OR-terms became `is_org_admin_of_commission`, leaving `is_admin()` only on the platform-management surface; the 6 dashboard DEFINER RPCs + `commission_overview` re-scoped; audit became a 3-tier hash chain (platform/org/commission); commission slug uniqueness moved global → per-org; routes moved `/c/[slug]` → `/o/[org]/c/[commission]`. **Three in-build amendments (ADR 0041 §Implementation amendments):** (10) the global `pqs_members`/QPS roster can't span orgs → `app.is_pqs_member` goes inert when `is_multi_org()`, making the **entire** NSP + referral PHI surface inert in multi-org (modules absent in the 2-org seed by design; 124 E2E skips; lifts with NSP-per-org); (11) `platform_admin` JWT claim is **never** an authorization grant on tenant paths — fixed the commission-shell `role===null` wall + 2 service-role staff-action escalations; (12) added `app.is_org_member` so commission members read their own org row (login-landing + commission pages). Greenfield reseed, structural (no feature flag). pgTAP 1029 · E2E 292/0/124-skip · QA APPROVED. | [0041](../decisions/0041-multi-tenancy-organizations-hospitals.md) |
| 2026-06-20 | **Round 3 — RCA-write severance + WS B (audited free-text reads & PHI classification) + WS E (efficiency).** All authored INTO the baseline; LOCAL only. **(1) RCA-write severance:** stripped the standalone `OR app.is_admin()` from the **7 `rca_*_write` policies** (`rca` update/delete + evidence/factors/members/root_causes/timeline/why_chains) — RCA writes now follow `can_write_rca` = `is_pqs_member OR assigned-non-observer` (the participant model, like `can_write_interview`; CAPA already PQS-only via `is_pqs_writer`). `can_write_rca` itself needed NO change (it never had an `is_admin` term — already routes admin via `is_pqs_member`). **Confirmed these were the LAST standalone-`is_admin` WRITE surfaces on NSP** (event/triage/lifecycle writes are DEFINER-RPC-only gated on `is_pqs_member`; CAPA via `is_pqs_writer`). A non-PQS admin can no longer read OR write any NSP/PHI content. **(2) WS B:** added 6 `.viewed` verbs (`safety_event/triage/rca/capa/meeting/interview.viewed`) to `log_audit_access`'s positive allow-list + the TS unions (`AuditAction`/`AuditAccessAction` + the `AUDIT_ACTION_LABELS` pt-BR map). Emit best-effort `.viewed` at each clinical detail-open (mirrors `getEventPatient`): `getSafetyEvent`, `getEventTriage`, `getRca`/`getRcaById`, `getCapaPlan`, `getMeetingDetail`, `getInterviewDetail` (new `auditClinicalView` helper resolves the event's reporting commission for the event-scoped ones; meeting/interview use their own commission). **Per locked decision #3 these stay APP-LAYER on the existing RLS-scoped reads (NOT new DEFINER RPCs)** — the residual app-layer bypass is the accepted tradeoff. **`case_narratives.body_md`/`case_events.body` are ALREADY covered by `case.opened` in `get_case_detail`/the case page — NOT duplicated.** Applied SQL column COMMENTs labeling **18** free-text PHI-bearing columns (authoritative list reported to lead). **(3) WS E:** M3 — verified all read-predicate support indexes exist (`case_access(user_id)`, `case_narratives(assigned_to)`, custody/owner idxs, `pqs_members` PK); none missing. M4 — added a `capa_plan.source` comment documenting the per-source CHECK growth cost + the satellite-table option (revisit before Phase 18). M2 — **DEFERRED** (documented): the per-vocab reorder/archive RPCs diverge by gate/flag/scope (commission vs global)/collision-strategy/pt-BR message; a shared helper needs injection-prone dynamic SQL and would be less auditable than the explicit per-table RPCs — net negative, not a clean win. **VERIFIED:** `db reset`+seed+10 personas; pgTAP **652/652** (`145` grew to 33: RCA-write severance — enrolled-PQS-admin writes, non-PQS-admin denied predicate + 0-row UPDATE, non-participant denied; the 6 `.viewed` accepted + forged mutation verb rejected); `db diff` "No schema changes found" (pristine reset); inventory unchanged from WS A (no new DB objects); types regenerated (+58, unchanged — `.viewed` are TS-only); typecheck+lint(0 err)+unit 34/34. **WS C completed in the same round — see the next row.** | (plan §Workstream A4/B/E; ADR 0030/0031) |
| 2026-06-20 | **WS C — PHI retention & disposal (`dispose_event_phi`; P1).** Lead-ack'd inventory plan; authored INTO the baseline (patient_safety) + `src/lib/safety/{actions,messages,types}.ts` + the TS audit union. New `public.dispose_event_phi(p_event_id, p_reason) RETURNS void` DEFINER (search_path pinned): **gate = admin OR PQS** (a compliance/erasure action — does NOT read PHI); runs under `app.in_safety_rpc='on'` (txn-local) so it works on frozen/triaged/closed records; **one-shot → HC056**. It **deletes** `event_patient` and **NULLs** the nullable PHI free-text (`patient_safety_event.description_md`, `event_triage.disposition_notes_md`, `rca.{what,expected,summary}_md+impact+scope`, `capa_plan.lessons_learned_md`, `capa_effectiveness.method_md`, `capa_measure_result.note`) and **REDACTS** the NOT-NULL PHI to sentinel `'[PHI removido]'` (`rca_factors.text`, `rca_root_causes.text`, `rca_timeline_entries.description`, `capa_action_task.description`) — CAPA children scoped to the event's plans (event- or rca-sourced). **PRESERVES** the governance skeleton (codes, status, custody ledger, structured non-PHI, audit chain). Stamps **3 new `patient_safety_event` columns** `phi_disposed_at/by/reason` (+ FK on `_by`) and sets `has_patient=false`; `phi_disposed_reason` is a **CONSTRAINED CATEGORY** CHECK (`retention_expired|subject_request|entered_in_error|duplicate|other`) — never free text (Rule 11 + LGPD Art. 18). Emits ONE `event_patient.disposed` audit MUTATION row via `app.audit_write`, metadata = `{"reason": <enum>}` only (PHI-free). TS: `event_patient.disposed` + label "Dados do paciente descartados"; `PhiDisposeReason` type + `PHI_DISPOSE_REASON_LABELS`; `disposeEventPhi(eventId, reason)` server action (42501→pt-BR; RPC is the authority); `HC056`→"PHI já descartada" mapping. **VERIFIED:** pgTAP **665/665** (`145` §G: deletes event_patient, NULLs+REDACTS across event/RCA, has_patient=false + 3 stamps, status+custody preserved, exactly ONE PHI-free `.disposed` row, **NO disposal audit row carries PHI identifiers/sentinel** [point-7 audit-PHI-safety], one-shot HC056, out-of-category reason rejected); `db reset`+seed+10 personas; `db diff` "No schema changes found" (pristine); cumulative inventory vs WS0 = intended-only (64 tables / 334 fns / 129 policies); types **+93** additive; typecheck+lint(0 err)+unit 34/34. **UI NOT built** (the "Descartar dados do paciente" button + reason-category select is a frontend follow-up). LOCAL only. | (plan §Workstream C; ADR 0030/0031) |
| 2026-06-20 | **WS A — structured-identifier lockdown (PHI-remediation round 2; P0).** Authored INTO the baseline (no incremental migration): patient_safety + grants_revoke + seed. **(1) Real PQS membership:** new `public.pqs_members(user_id PK→profiles, added_by, added_at)` + admin-only RLS (`pqs_members_admin_all`) + admin RPCs `add/remove/list_pqs_members` (DEFINER, `is_admin()`-gated, mirror `assignStaffAdmin`). `app.is_pqs_member(uid)` now `exists(select 1 from pqs_members …)` — **dropped the `is_admin_for` fallback**; `app.is_pqs_writer()` → `is_pqs_member(auth.uid())` — **dropped `or is_admin()`**. **(2) Tight identifier predicate:** new `app.can_read_event_patient(event,uid)` = `is_pqs_member(uid) OR is_staff_admin_of_for(current_owner_commission_id, uid)` — NO reporting-provenance, NO admin; NULL-custodian (PQS-held) ⇒ PQS-only (`is_staff_admin_of_for(NULL,uid)=false`, verified). **(3) Single audited door:** new `public.get_event_patient(event)` DEFINER (mirrors `get_case_detail`): re-gates via `can_read_event_patient`, emits exactly one `event_patient.read` (empty metadata, reporting-commission-attributed) on a real entitled read, returns jsonb; null+no-audit when out-of-scope OR no PHI row. **REVOKED ALL direct `authenticated` DML on `event_patient`** (fully RPC-only; writes stay on `set_event_patient` DEFINER). **(4) `patient_safety_event.has_patient bool` (default false)** set true by `set_event_patient`, replaces the now-broken `event_patient(event_id)` embed for `hasPatient`. **(5) Severed platform-admin from NSP PHI:** stripped the standalone `OR app.is_admin()` from **all 19** PHI SELECT policies (event/custody/triage/sentinel/rca×7/capa×7) — admin re-enters NSP ONLY via `pqs_members`. Non-PHI config tables (`pqs_department`/`pqs_event_types`/`pqs_sentinel_criteria`) untouched (public read). **Query layer:** `getEventPatient`→RPC (dropped direct select + app-layer `logAuditAccess`, now inside the RPC); `SAFETY_EVENT_SELECT` drops the embed, reads `has_patient`. **Seed:** admin enrolled in `pqs_members` (prod: first admin enrolls PQS staff). **VERIFIED:** `db reset`+seed+10 personas; pgTAP **641/641** (new `145_pqs_membership.sql` 22 tests: matrix custodian-sa=yes/PQS=yes/reporting-member=no/admin-not-PQS=no, direct-SELECT-denied, get_event_patient 1-audit/0-audit cases, add/remove gating; 140–143 enroll admin); `db diff` "No schema changes found"; types regenerated **+58 additive lines only**; typecheck+lint(0 err)+unit 34/34. LOCAL only. | (plan §Workstream A; ADR 0030/0031) |
| 2026-06-20 | **Migration squash → domain-partitioned baseline (PHI-remediation Workstream 0).** Replaced the 86 incremental migrations with a **dump-driven**, 12-file, domain-partitioned baseline (`20260620001000`–`012000`: extensions_app · identity · forms · responses · cases · meetings · interviews · audit · patient_safety · storage · seed_vocab · grants_revoke). Method: `db reset` → `db dump --local` as the source of truth; a partitioner routed every object to its domain (tables/constraints/indexes/policies by table; triggers by their FUNCTION; the one backward FK `responses.case_phase_id→case_phases` + the `sync_case_phase_on_submit` trigger deferred into cases), each domain file carrying a `SET check_function_bodies=false` preamble. **Three classes the schema-only dump excludes were carried separately:** (1) storage — 5 private buckets + 12 `storage.objects` policies (verbatim from the storage migrations); (2) the 2 `auth.users` triggers (`on_auth_user_created`/`on_auth_user_email_changed`) the public/app dump drops — without them signup→profiles breaks and seed FKs fail; (3) the **anon/PUBLIC revoke posture** (Phase-8 B6 + access-token-hook lockdown) — pg_dump does not re-emit a revoked-from-default ACL, so the local stack's auto-expose silently re-granted anon; a final `grants_revoke` file re-asserts it. Feature flags folded to live end-state (all 9 ON). **GATE PASSED:** `supabase db diff` → "No schema changes found"; dump-vs-dump whitespace-insensitive diff = **0 lines** (the 572-line raw diff is 100% trailing-whitespace, proven by trimmed-multiset comm = 0/0); pgTAP **619/619**; regenerated `database.ts` **byte-identical** to committed; `db reset` + `seed.sql` + all 10 E2E personas green. No security deltas this round (mechanical squash only) — Workstreams A–E follow. | (plan `i-agree-with-everything-distributed-hartmanis.md` §Workstream 0) |
| 2026-06-19 | **Case Access Control** — per-case read/write ACL + attribution-driven full-case read; restrictive `can_read_case` boundary; narrative attribution + `aberta→concluida` lifecycle; "Meus Casos" replaces "Minhas fases"; one capability-gated detail page; strictly PHI-free. 14-question requirements interview → ADR. | [0033](../decisions/0033-case-access-control.md) |
| 2026-06-11 | Scaffolding & toolchain: Next 16/React 19, shadcn (radix/neutral), `vitest.config.mts` (ESM), Supabase CLI pinned as devDep, Chromium-only Playwright | [0001](../decisions/0001-scaffolding-and-toolchain.md) |
| 2026-06-11 | `jsdom` pinned to `^25` (jsdom@27 pulls an ESM-only transitive dep that crashes Vitest's forks pool on Node 20) — revisit when vitest/jsdom resolve the incompatibility | – |
| 2026-06-12 | Admin claim via a custom access token hook reading `profiles.is_admin` (not `app_metadata`); RLS helper falls back to a DB read so correctness never depends on the hook | [0002](../decisions/0002-admin-claim-access-token-hook.md) |
| 2026-06-12 | Database tests use pgTAP via `npx supabase test db` (richer assertions, native runner, per-file txn isolation) | [0003](../decisions/0003-pgtap-for-db-tests.md) |
| 2026-06-12 | Sign-off enforcement gated by an `app.feature_flags` row read by `submit_response`; OFF in Phase 1, flipped on by a one-line Phase 6 migration | [0004](../decisions/0004-signoff-feature-flag.md) |
| 2026-06-12 | `visible_when` v1 is a single condition (no AND/OR), CHECK-enforced shape + publish-time structural validation; documented extension point for AND/OR | [0005](../decisions/0005-visible-when-shape.md) |
| 2026-06-12 | Keep legacy env var names (`..._ANON_KEY`/`..._SERVICE_ROLE_KEY`) while accepting new-style publishable/secret CLI keys | [0006](../decisions/0006-supabase-api-key-naming.md) |
| 2026-06-12 | Phase 2: added `multi@test.local` (id `…0008`) as **staff of both** `ccih` and `farmacia` to make the commission picker E2E-testable; pgTAP unaffected (hermetic `test_helpers.bootstrap`, never reads seed). Seed now 8 users / 8 memberships. | – |
| 2026-06-12 | Middleware is a coarse auth gate (refresh + authenticated/unauthenticated boundary only); role-aware landing deferred to the root `/` Server Component to avoid per-request DB queries on the edge | [0007](../decisions/0007-middleware-coarse-gate-root-landing.md) |
| 2026-06-12 | GSAP 3.15.0 pinned as the auth-hero animation dependency (dynamic import, aria-hidden, reduced-motion guard) over a CSS-only approach | [0008](../decisions/0008-gsap-animation-dependency.md) |
| 2026-06-12 | Auth identity on the request hot path via LOCAL JWT verification (`getClaims()`, ES256 vs cached JWKS) instead of a per-request `getUser()` GoTrue round trip; `is_admin` strictly from the verified claim (fails closed), SQL `app.is_admin()` DB fallback kept as RLS defense-in-depth. Fixes P2-002. **Requires asymmetric signing keys in prod.** | [0009](../decisions/0009-jwt-local-verification-gate.md) |
| 2026-06-12 | Phase 3: denormalize `email` (nullable citext) onto `public.profiles` — populated by the signup trigger, backfilled from `auth.users`, kept fresh by an `auth.users` email-change sync trigger, partial-unique-indexed. Member/admin lists resolve emails via the existing `profiles_select` RLS policy with no service-role read on the display path. Slug immutable after create (name-only edit). | [0010](../decisions/0010-denormalize-email-on-profiles.md) |
| 2026-06-12 | Phase 4: position reorder via `DEFERRABLE INITIALLY IMMEDIATE` unique constraints + single-statement CASE swap in SQL RPCs (`reorder_section`/`reorder_item`); supabase-js can't express the swap, so it lives in SQL. | [0011](../decisions/0011-position-reorder-deferrable-swap.md) |
| 2026-06-12 | Phase 4: `clone_form_version` returns the existing draft when one exists (one editable draft per form), so "editar publicado" is idempotent and never proliferates versions. | [0012](../decisions/0012-clone-returns-existing-draft.md) |
| 2026-06-12 | Phase 4: fixed latent Phase-1 RLS defect — `form_versions_staff_admin_write` WITH CHECK was self-referential (`commission_of_version(id)` can't see the new row on INSERT), rejecting all direct version inserts; repaired to resolve commission via `form_id→forms`. RPCs stay `security invoker` (RLS is the authority); not a definer bypass. | [0013](../decisions/0013-form-versions-insert-rls-fix.md) |
| 2026-06-12 | Phase 5: response-fill via two `security invoker` RPCs (`save_section_answers`, `start_or_resume_response`) — atomic single-round-trip section save (upsert + `last_section_id` + `updated_at`), orphan-clear as a parameter, `unique_violation`-catch resume, cross-version item guard. RLS is the authority; no new policies; `submit_response`/evaluator untouched. | [0015](../decisions/0015-response-fill-rpcs.md) |
| 2026-06-13 | Phase 7 (P7-002 fix): custom SQLSTATE class **`P00xx` → `HC0xx`** (`HC010`–`HC022`). PostgREST 14 maps user-defined class `P0002`–`P0999` → HTTP 500, and a 500 with a non-ASCII (accented pt-BR) message drops the JSON body (text/plain "Something went wrong"), so `error.code` was unreadable and every custom rejection showed the generic message across phases 5/6/7. An unknown class (`HC0xx`) maps to 400 + JSON `{code,message}` (version-agnostic; no config pinning — prod behind Cloudflare keeps today's behaviour, no regression). Migration `…090009` re-states the committed Phase 5/6 functions; the unshipped Phase-7 migrations carry `HC0xx` in place; action constants + pgTAP expectations renumbered. Standard codes (`P0002`/`23505`/`23514`/`42501`) unchanged. | [0018](../decisions/0018-custom-sqlstate-class.md) |
| 2026-06-13 | Phase 7: **Multi-phase cases** — 4 new tables (`process_templates`/`process_template_phases` blueprint; `cases`/`case_phases` authority); a phase is a `responses` row (new `case_phase_id`). Snapshot-at-creation pins published versions (templates need no immutability). Condition evaluator + TS mirror REUSED unchanged — `recommend_when` feeds `eval_condition` a new submitted-only `case_phase_answer_map`, qualified by `from_phase`. Coordinator board reads via definer RPCs (status only / submitted answers only), preserving the ADR-0016 Phase-7 in_progress-answers invariant. Per-commission case-number minting via advisory-lock trigger. New SQLSTATEs (originally P0016–P0022, renumbered to HC016–HC022 by ADR 0018). Dashboards→8, Deployment→9. | [0017](../decisions/0017-multi-phase-cases.md) |
| 2026-06-13 | Maintenance (post-Phase-7): **the default (anchor) section may now carry a title** and be renamed in the builder once a form has ≥2 sections. Relaxes the `form_sections_default_shape` CHECK (migration `…090010`) to forbid only `visible_when` + `requires_signoff` on the default (no longer `title`); `updateSection` persists an optional default-section title; the builder surfaces its rename control (settings/delete still hidden). Flat-render invariant unchanged; partially reverses "lead refinement #2". Builder E2E specs updated (default section now has a "Renomear seção" button). | [0019](../decisions/0019-default-section-may-carry-title.md) |
| 2026-06-13 | Phase 8: **dashboard-countable responses** — standalone form dashboards EXCLUDE case-phase responses (`case_phase_id IS NULL`) so a form's stats don't drift when a case runs (Form A dashboard = 6, not 7); the submissions browser INCLUDES them badged (`isCasePhase`). Filter single-sourced in SQL (`app.submitted_form_responses`) + TS twin (`isDashboardCountable`). Aggregations are `is_staff_admin_of`/`is_admin`-gated SECURITY DEFINER RPCs (mirror ADR 0016): checkbox unnested, per-distribution denominator = distinct submitted responses with an answer in that question's section (conditional → smaller denominator), keyed by `question_key` across versions. B6 anon-revoke also revoked the implicit **PUBLIC** EXECUTE grant (anon inherited it). | [0020](../decisions/0020-dashboard-countable-responses.md) |
| 2026-06-14 | Cases-Extras batch / **R2 — configurable per-committee case status.** `cases.status` is no longer a fixed 3-state CHECK: a new `public.case_status_defs` per-commission vocabulary (ASCII `key` stored in `cases.status`, pt-BR `label`, ordered `position`, constrained `color_token`, `is_initial`/`is_terminal`/`archived`) becomes the kanban columns. Validity moved from the dropped `cases_status_check` to the rewritten `app.guard_case_status` (HC024 undefined key, HC025 terminal-frozen; any non-terminal→any-defined allowed, no matrix). **Liveness sweep:** the hard-coded `'aberto'` "case is live" literal in `guard_case_status` + `sync_case_phase_on_submit` + `activate_phase`/`skip_phase`/`add_ad_hoc_phase`/`reassign_phase`/`create_case_from_template` is replaced by `not app.case_status_is_terminal(...)` (so a case in a custom non-terminal status still advances phases on submit — the top risk, verified live). New `set_case_status` RPC (terminal-entry reuses close_case's open-phase cleanup + closed_* stamp); `close_case`/`cancel_case` are now thin wrappers → the shared `app.apply_case_status` core. Status-vocab CRUD RPCs + `list_case_status_defs` (definer, gated). Seeding via an AFTER INSERT trigger on `public.commissions` (commissions are a bare INSERT, not an RPC). Default set: rascunho / em_andamento[initial] / em_revisao / concluido[terminal] / cancelado[terminal] (only `aberto→em_andamento` is a key rename). New `cases_extras` feature flag (OFF until the batch's enable migration) gating the NEW write RPCs; the modified CORE phase RPCs keep gating only `cases_multi_phase`. Migrations `…092000`/`…092001`. | [0023](../decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Cases-Extras batch / **R1 documents+events, R3 tags, R4 action items.** R1: `case_documents` (soft-delete `deleted_at`/`deleted_by`; immutable `case-documents` Storage bucket — private, 25 MiB, PDF/image/Office/CSV/text; upload clones `uploadFormAsset`, `upsert:false`, fresh `{commission}/{case}/{uuid}.{ext}` path) + `case_events` (edit/hard-delete free-text working notes). R3: `case_tags` + `case_tag_assignments` (BEFORE INSERT `app.guard_case_tag_assignment` → HC026 same-commission) + `case_tag_report` (definer/gated, counts on `created_at::date`). R4: `case_action_items` (open/in_progress/done/cancelled; `source_case_phase_id` ON DELETE SET NULL) + assignee-or-staff_admin lifecycle via `advance/complete_action_item` (`app.advance_action_item_core` DEFINER, HC027) + `case_action_items_kpis` (open/overdue/completed-YTD). All member-read/staff_admin-write via `commission_of_case`; write RPCs/actions gate `cases_extras` (`public.cases_extras_enabled()` for the R1 direct-table-write actions). New SQLSTATEs HC026/HC027. Migrations `…092002`–`…092005`; `cases_extras` flipped ON in `…092006`. | [0022](../decisions/0022-cross-committee-referrals.md) / [0023](../decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Post-Phase-8 feature: **due dates for case phases.** `process_template_phases.default_due_days` (planning hint) + `case_phases.default_due_days` (snapshot copy, ADR-0017 isolation) + `case_phases.due_date`. RPCs threaded additively (`add_template_phase`/`update_template_phase` gain default-days clear/replace/keep; `activate_phase` gains `p_due_date`; `create_case_from_template` snapshot-copies the default; `list_cases_board`/`get_case_detail` expose the new fields). `add_ad_hoc_phase` unchanged. No new RLS/SQLSTATE; evaluator untouched. Migration `…091000`. | [0021](../decisions/0021-phase-due-dates.md) |
| 2026-06-14 | **Case data-model adjustments — phase blocking, FIXED statuses, outcomes** (backend tasks #2–#3; supersedes the R2 configurable status). **(1) Status D12:** the configurable `case_status_defs` vocabulary + status-CRUD/`set_case_status`/`apply_case_status`/`case_status_is_terminal` + the commission seed trigger are DROPPED (`…093000`); `cases.status` is now a FIXED 5-value CHECK (`nao_iniciado`/`em_revisao`/`pendente`/`concluido`/`cancelado`) AUTO-COMPUTED by `app.recompute_case_status` + an AFTER trigger on `case_phases` (`em_revisao` if any phase ativa; else `pendente` if ≥1 concluida; else `nao_iniciado`); `concluido`/`cancelado` stay MANUAL (`close_case`/`cancel_case`, terminal-first then flip residual phases so recompute early-returns); `guard_case_status` rewritten to delegate validity to the CHECK (`…093001`). The 092001 liveness-sweep landmine handled by re-`CREATE OR REPLACE`ing every dependent (`sync_case_phase_on_submit`/`skip_phase`/`add_ad_hoc_phase`/`reassign_phase`/`cancel_case` in 093001; `activate_phase` in 093002; `create_case_from_template`/`close_case` in 093003) with a plain fixed-enum terminal check — one final definition per function. **(2) Phase blockers (D1/D4):** `blocks integer[]` on `process_template_phases` + `case_phases` (rides the recommend_when renumber/snapshot machinery; shape trigger = earlier-only, deep "position exists" in the template RPC); `activate_phase` HC018 now counts blocking phases not yet concluida/nao_necessaria (parallel-safe); `reorder/remove_template_phase` remap the `blocks` arrays across the renumber; `set_template_phase_blocks` + `add/update_template_phase` gain blocks (`…093002`). **(3) Outcomes (D8–D11/D15):** `case_outcomes` vocab (per-commission, label+`requires_action_plan`+`is_adverse`, archivable) + `process_template_outcomes` (offered set, HC030 same-commission guard) + `case_offered_outcomes` (per-case FROZEN snapshot) + `cases.outcome_id` (single FK, NO ACTION); `set_case_outcome` (HC025/HC029), conclude gate in `close_case` (HC031 unsettled phases / HC028 outcome-required), outcome CRUD + `set_process_outcomes`; `list_cases_board`/`get_case_detail` extended with answer-free outcome metadata + per-phase `blocks` (`…093003`). New SQLSTATEs **HC028/HC029/HC030/HC031** (HC031 chosen over reusing HC027 — taken by action-item entitlement). Every replaced public fn re-revoked from anon/PUBLIC. `db reset` clean locally; types regenerated; lint+typecheck+unit(24/24) green. **D12 status files NOT yet deleted** — interim-shimmed pending frontend §H (see Follow-ups). **(4) pgTAP + seed (task #4, all LOCAL):** rewrote `110_case_status.sql` to the fixed precedence table; `90_cases.sql` test 9 → parallel activation + blocker enforcement (both D4 satisfiers), test 23 → D3 conclude gate (HC031/HC028/OK); new `114_phase_blockers.sql` (HC018 via blocks, HC016 forward/self/missing, reorder+remove blocks RENUMBER, snapshot) + `115_case_outcomes.sql` (CRUD+RLS, HC030, snapshot, HC029/HC025/HC028, D15, D11 propagation, D10 non-gating); extended `100_dashboard.sql` anon-revoke; seeded `case_outcomes`+offered set+a concluded adverse Caso 0002. Surfaced+fixed a real ordering bug in `093002`: `reorder/remove_template_phase` split the position renumber and the `blocks` remap into two UPDATEs, so the BEFORE shape trigger saw a transient forward-ref — now a SINGLE atomic UPDATE per row. **Full pgTAP 18 files / 292 tests PASS.** **(5) close-out (task #8):** `mapCaseError` in `src/lib/cases/actions.ts` now maps HC028/HC031 → specific pt-BR (the D3 conclude-gate rejections are user-readable, not generic); the D12 shims (`case-statuses.ts`/`status-actions.ts`) + the `CaseStatusKey` alias deleted in lockstep after frontend §H orphaned them; integrated typecheck+lint+unit(24/24) green on the combined tree. | [0024](../decisions/0024-case-model-adjustments.md) (supersedes 0023; plan `the-current-case-data-bright-hartmanis.md`) |
| 2026-06-13 | Phase 6: staff_admin sign-off read served by two narrow `security definer` RPCs (`list_signoff_queue`, `get_response_for_signoff`), each internally gated by `is_staff_admin_of` + a pending visible staff_admin section — NOT by broadening `responses_select`/`answers_select` (preserves the Phase-7 in_progress-answers invariant, avoids RLS recursion). Queue requires submit-readiness. Also fixed a latent Phase-1 RLS defect (`signoffs_insert`/`signoffs_select` inline subqueries over `responses` were RLS-filtered, so the staff_admin counter-sign path could never succeed) via `security definer` predicates (`can_sign_section`/`can_read_signoff`) — signer-role rules byte-unchanged. ADR-0004 sign-off flag flipped ON. Known v1 limitation documented: no answer-lock between sign-off and submission. | [0016](../decisions/0016-signoff-definer-read-path.md) |
| 2026-06-16 | Phase 12: **Case Timeline** (read-only viz; NO migration, reuses existing table RLS). A new `Linha do tempo` tab aggregates the case's real sub-entities into one normalized `CaseTimelineEvent[]` (pure `src/lib/timeline/event-model.ts` — types + `anchor`/`endDay`/`durationDays`/`statusOf` helpers shared by both layouts) via `getCaseTimeline` (`src/lib/queries/case-timeline.ts`), composing existing per-entity reads + a new reverse `listCaseMeetings`. **8 event types**: lifecycle / phase(bars) / milestone(`case_events kind='decision'`) / interview / meeting / document / action / note. Two layouts (Feed default + adaptive-axis Duration/Gantt) with a URL-persisted view+density+type-filter; today-marker/upcoming live for OPEN cases, static terminal marker for CLOSED. **Two dedups**: interview→case_event via `registry_event_id`, AND the meeting-conclusion `case_events kind='meeting'` echo (every concluded meeting else double-counts as meeting+note — the reverse `meeting_cases` link is authoritative). `getCaseDetail`/`getCommissionAccess` wrapped in React `cache()` for the layout+child split. Visual-checkpoint calls: density toggle stays visible; lifecycle vs note get distinct neutral tones. Plan `when-visualizing-a-case-misty-rossum.md`. | [0027](../decisions/0027-case-timeline.md) |
| 2026-06-15 | Phase 10: **Meetings** (B1–B5) — 8 migrations (`…090000`–`…090007`). 6-state lifecycle (`agendada→realizada→em_assinatura→assinada→distribuida`+`cancelada`) via `guard_meeting_status` (gated by `app.in_meeting_rpc`); per-commission `meeting_number` advisory-lock minting. Children: agenda/attendees(user XOR guest)/cases(HC032 same-commission)/action_items(denorm commission_id)/signatures/attachments(private bucket, immutable). **`conclude_meeting` accepts agendada OR realizada** (steps through realizada) to keep the frozen `concludeMeeting()` contract — no separate "mark held" action; it snapshots quorum (maioria_simples denom = all members) + writes `case_events(kind='meeting')` per link (HC034 no-present). **Auto-flip `em_assinatura→assinada` lives in `sign_meeting`** (not a row trigger). **`sign_meeting` is DEFINER and re-asserts `app.can_sign_meeting` EXPLICITLY** (a DEFINER owned by superuser BYPASSES the sign-own-row RLS policy — relying on the policy was a real bug caught in smoke testing); HC035 double-sign (active partial-unique), HC036 ineligible. **`guard_meeting_child_lock` deliberately ignores the rpc flag** (keys purely on parent status) so authoring-child RPCs can never edit a locked meeting. `reopen_meeting` revokes signatures (rows kept). Provider-ready signature schema (`method`/`content_hash` via `extensions.digest`/`provider_*`). Fresh seed-on-commission trigger (no prior one) + backfill. `meetings` flag OFF (one-line enable migration ships last). New SQLSTATEs HC032–HC037 (HC021 reused). pgTAP `120_meetings.sql` (26); full suite **318/318**. | [0025](../decisions/0025-meetings.md) |
| 2026-06-18 | Phase 14 batch: build sub-phases **14b (Triage) → 14c (RCA) → 14d (CAPA)** in dependency order as ONE build, then run the §6 test pass + QA review ONCE after all three (human-directed scope). 14b is a hard prerequisite — `confirm_triage(pathway=rca)` mints the RCA shell + due date, and `capa_action.root_cause_id` → `rca_root_causes` — so 14c/14d cannot be built standalone. Contract-first per sub-phase; one warm `backend` + one warm `frontend` reused across all three. | (track detail [accreditation-track.md](../phases/accreditation-track.md) §14b–14d) |

## Collapsed index rows rotated out of PROGRESS.md 2026-08-04 (all decisions dated 2026-06-*)

> The verbose form of each is already above; these are the one-line rows PROGRESS.md carried until the
> 2026-08-04 size rotation, kept so the collapsed index itself survives.

| Date | Decision | Ref |
| ---- | -------- | --- |
| 2026-06-25 | **NSP-per-org** — bind the PQS roster + every PHI door to an organization (per-org `pqs_members`/`pqs_department`, `nsp_coordinator` grant via the … | [0042](../decisions/0042-nsp-per-org.md) |
| 2026-06-25 | **Multi-Tenancy** — pooled single-DB (organizations → hospitals → commissions); vendor `platform_admin` vs customer `org_admin`; RLS rewrite + … | [0041](../decisions/0041-multi-tenancy-organizations-hospitals.md) |
| 2026-06-20 | **PHI-remediation Round 3** — RCA-write severance (drop `is_admin` from 7 `rca_*_write`) + WS B audited free-text reads/PHI classification + WS E … | plan §A4/B/E; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS C** — PHI retention & disposal (`dispose_event_phi`, one-shot HC056, constrained reason enum) | plan §C; ADR 0030/0031 |
| 2026-06-20 | **PHI-remediation WS A** — structured-identifier lockdown: real `pqs_members` roster, tight `can_read_event_patient`, single audited door, sever … | plan §A; ADR 0030/0031 |
| 2026-06-20 | **Migration squash** → domain-partitioned dump-based baseline (replaced 86 incremental migrations; WS0) | plan §WS0 |
| 2026-06-19 | **Case Access Control** — per-case read/write ACL + attribution-driven full-case read; restrictive `can_read_case`; narrative lifecycle | [0033](../decisions/0033-case-access-control.md) |
| 2026-06-11 | Scaffolding & toolchain: Next 16/React 19, shadcn (radix/neutral), vitest ESM, Supabase CLI devDep, Chromium-only | [0001](../decisions/0001-scaffolding-and-toolchain.md) |
| 2026-06-11 | `jsdom` pinned `^25` (jsdom@27 ESM dep crashes Vitest forks on Node 20) | – |
| 2026-06-12 | Admin claim via custom access-token hook reading `profiles.is_admin`; RLS helper DB-read fallback | [0002](../decisions/0002-admin-claim-access-token-hook.md) |
| 2026-06-12 | DB tests via pgTAP (`npx supabase test db`) | [0003](../decisions/0003-pgtap-for-db-tests.md) |
| 2026-06-12 | Sign-off enforcement gated by an `app.feature_flags` row read by `submit_response` | [0004](../decisions/0004-signoff-feature-flag.md) |
| 2026-06-12 | `visible_when` v1 = single condition (no AND/OR), CHECK-enforced shape + publish-time validation | [0005](../decisions/0005-visible-when-shape.md) |
| 2026-06-12 | Keep legacy env-var names while accepting new publishable/secret CLI keys | [0006](../decisions/0006-supabase-api-key-naming.md) |
| 2026-06-12 | Phase 2: `multi@test.local` as staff of both ccih+farmacia (commission-picker E2E); seed 8 users/8 memberships | – |
| 2026-06-12 | Middleware is a coarse auth gate; role-aware landing deferred to root `/` Server Component | [0007](../decisions/0007-middleware-coarse-gate-root-landing.md) |
| 2026-06-12 | GSAP 3.15.0 pinned for the auth-hero animation (dynamic import, aria-hidden, reduced-motion) | [0008](../decisions/0008-gsap-animation-dependency.md) |
| 2026-06-12 | Auth identity on the request hot path via LOCAL JWT verification (`getClaims()`, ES256/JWKS) | [0009](../decisions/0009-jwt-local-verification-gate.md) |
| 2026-06-12 | Phase 3: denormalize `email` (nullable citext) onto `public.profiles` (trigger-populated) | [0010](../decisions/0010-denormalize-email-on-profiles.md) |
| 2026-06-12 | Phase 4: position reorder via DEFERRABLE INITIALLY IMMEDIATE unique constraints + CASE swap | [0011](../decisions/0011-position-reorder-deferrable-swap.md) |
| 2026-06-12 | Phase 4: `clone_form_version` returns the existing draft when one exists (idempotent edit) | [0012](../decisions/0012-clone-returns-existing-draft.md) |
| 2026-06-12 | Phase 4: fixed latent Phase-1 RLS defect in `form_versions_staff_admin_write` WITH CHECK | [0013](../decisions/0013-form-versions-insert-rls-fix.md) |
| 2026-06-12 | Phase 5: response-fill via two security-invoker RPCs (`save_section_answers`, `start_or_resume_response`) | [0015](../decisions/0015-response-fill-rpcs.md) |
| 2026-06-13 | Phase 7 (P7-002): custom SQLSTATE class `P00xx` → `HC0xx` (HC010–HC022) | [0018](../decisions/0018-custom-sqlstate-class.md) |
| 2026-06-13 | Phase 7: Multi-phase cases — 4 new tables (process_templates/phases; cases/case_phases) | [0017](../decisions/0017-multi-phase-cases.md) |
| 2026-06-13 | Maintenance: the default (anchor) section may carry a title once a form has ≥2 sections | [0019](../decisions/0019-default-section-may-carry-title.md) |
| 2026-06-13 | Phase 8: dashboard-countable responses exclude case-phase responses (`case_phase_id IS NULL`) | [0020](../decisions/0020-dashboard-countable-responses.md) |
| 2026-06-14 | Cases-Extras R2: configurable per-committee case status (no longer fixed 3-state CHECK) | [0023](../decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Cases-Extras R1 documents+events, R3 tags, R4 action items | [0023](../decisions/0023-configurable-case-status.md) |
| 2026-06-14 | Post-Phase-8: due dates for case phases (`default_due_days` + `case_phases.default_due_*`) | [0021](../decisions/0021-phase-due-dates.md) |
| 2026-06-14 | Case data-model adjustments — phase blocking, FIXED statuses, outcomes (supersedes R2) | plan `the-current-case-data-bright-hartmanis.md` |
| 2026-06-13 | Phase 6: staff_admin sign-off read via two narrow security-definer RPCs (`list_signoff_queue`, `get_response_for_signoff`) | [0016](../decisions/0016-signoff-definer-read-path.md) |
| 2026-06-16 | Phase 12: Case Timeline (read-only viz; no migration, reuses table RLS) | [0027](../decisions/0027-case-timeline.md) |
| 2026-06-15 | Phase 10: Meetings (B1–B5) — 8 migrations; 6-state lifecycle | [0025](../decisions/0025-meetings.md) |
| 2026-06-18 | Phase 14 batch: build 14b (Triage) → 14c (RCA) → 14d (CAPA) in dependency order as one build | accred-track §14b–14d |

## Verbose rows rotated out of PROGRESS.md 2026-08-10 (12 rows, 2026-08-03 → 2026-08-10)

> The live Decisions table was cut back to the documented one-line-per-decision shape; these are the
> twelve rows in their original verbose form, **verbatim**. Each decision's binding rationale lives in
> its linked ADR — this section exists so the working notes taken at ruling time (the measurements,
> the rejected alternatives and why) survive the collapse.

| Date | Decision | Ref |
| --- | --- | --- |
| 2026-08-10 | **ACT S3 — a hat-blind gate can RECEIVE the caller's uid; classify by CALL-SITE BINDING, not signature shape** (LEAD, on the QA r1 BLOCKER). A `*_for(uuid)` helper is a third-party door only if **no** caller binds that parameter to `auth.uid()` — a fact about call sites, not signatures. Fix uses the caller-only pattern `has_role` already carries, so third-party questions stay hat-independent (ADR 0106 §2). **Expiry semantics deliberately NOT touched** (BUG-ACT-EXPIRY-1 keeps its own change + gate), though the hat condition does make the arm unreachable for expired-ONLY principals. Sweep-method consequence ratified into the standing door audit. | ADR [0079](../decisions/0079-authz-door-blindness-standing-invariant.md) Amendment 6 · [§7.17](../progress/authz-handoff.md) · [0106](../decisions/0106-act-as-role-assumption.md) |
| 2026-08-09 | **ACT S0 — the role enum lands in `public.platform_role`, not `app.platform_role`** (LEAD, on a measured Stage-0 finding; schema placement was never one of the PO-locked P1–P6). `supabase/config.toml` exposes `["public","graphql_public"]`, so an `app`-schema enum is **invisible to `gen:types`** — silently voiding the plan's "the picker (via generated types)". Rejected alternatives, named so a later sweep does not "fix the inconsistency": **exposing `app`** would put all ~281 `app` DEFINER doors on PostgREST (an attack-surface change, not a typing fix); a **hand-kept TS mirror** is exactly the stale-assertion shape this repo keeps getting burned by. A bare enum TYPE in `public` is **not a relation** — no endpoint, no RLS surface — and `public.audio_job_status` is the standing precedent that a `public` enum emits a proper literal union. **11 labels = the 10 `memberships_role_check` values + `platform_admin`** (D11: not a `memberships` value, it lives in `profiles.is_admin`, but must be representable for the break-glass hat, the selections row and the `metadata.acting_as` stamp). | [0106](../decisions/0106-act-as-role-assumption.md) · [plan](../plans/act-as-role-assumption.md) |
| 2026-08-09 | **`is_commission_admin_of` → `is_tenancy_admin_of` RENAMED (PO; no shim; historical docs NOT rewritten).** The predicate resolves org_admin/hospital_admin and is FALSE for `staff_admin`, so the name asserted the opposite of its meaning — the repo's standing stale-claim hazard spelled in an identifier. **PO rulings:** ① **no backward-compatible shim** — anything missed fails loudly at migration time rather than resolving quietly (this codebase's recurring failure is a stale name that keeps working); ② **living docs only** — `backend-state.md` updated + carries the mapping note, while ADRs/reviews/plans/progress keep the old name because they record what was decided when it was called that, the same reason migrations are immutable here. ⚠ **Mechanism finding, measured not assumed:** `pg_policy` stores a PARSED tree referencing the function by **OID**, so **all 54 policies followed with zero edits**; only `pg_proc.prosrc` (text) needed rewriting (75 bodies). **The D11 lesson does NOT transfer** — that was an *enum* re-key where labels are string literals inside predicates, so policies did *not* follow. Same-shaped task, opposite substrate. | ADR [0105](../decisions/0105-rename-is-tenancy-admin-of.md) |
| 2026-08-09 | **QO·B PO RATIFICATION SESSION — the FUP-QOB-2 package worked item by item.** The PO declined a block ratification and asked to be walked through each ruling with its evidence, so every verdict below was taken against a **live-catalog measurement**, not the doc's own claim. **RATIFIED:** ① fix shape (tenancy admin = session FLAG, never a coerced role — decisive point: coercion is the direct cause of BUG-QOB-004 and makes every `role`-based gate silently wrong one route at a time) · ② `manage/audit/**` + CSV = KEEP (`audit_log_select` carries the tenancy arm verbatim — the UI is being made to agree, not widened) · ④ `manage/acreditacao/**` membership-gated (all four accreditation-plane policies measured, tenancy arm = **false** on every one; reversing would be a WIDENING) · **FUP-QOB-1's J1c pin** as the standing guard (the behavioural surface *collapsed*, so a structural pin is the strongest thing that still exists; the alternatives measure an invented grant or leave no guard at all). **LEFT OPEN BY CHOICE, not pending:** ③ `manage/charter` (status quo NOT-KEEP stands; the live argument FOR keeping it is that a charter defines the committee's scope = a *container* under D12 — but KEEP would be a widening, not a restoration) · ⑤ dual-hat precedence (measured: 3 `quality_reviewer` principals exist, **none** holds a tenancy role — unfalsifiable today; the PO declined to set a default in the abstract). **RULED:** BUG-QOB-004 = **CUT-the-arms**; `setTemplateCaseType` Q2-consistency **approved**; the `is_tenancy_admin_of` rename **re-confirmed** and sequenced LAST so it sweeps a settled catalog once (measured blast radius **77 functions + 54 policies + 318 files**). | [FUP-QOB-2](../progress/follow-ups.md) · [0100](../decisions/0100-quality-office-oversight.md) D12 |
| 2026-08-09 | **QO·B BUG-QOB-003 presentation rulings (LEAD, catalog/precedent-backed — PO ratification listed for step 4).** *(→ ①②④ RATIFIED, ③⑥ left open, ⑦ approved as its own wave — see the ratification row above.)* ① Fix shape = **flag + KEEP-scoped UI** (Phase A's quality_reviewer flag-not-role precedent + the cases-board 404 precedent; options (a)+(b) of the bug record). ② **`manage/audit/**` + its CSV export = KEEP** (`audit_log` is on the ratified §4.5 KEEP list). ③ **`manage/charter` = NOT KEEP** — catalog: `upsert_commission_charter` is staff_admin-only by explicit design (HC0K0), the charter plane never carried a tenancy arm. ④ **`manage/acreditacao/**` stays membership-gated** — catalog: no accreditation-plane policy carries a tenancy arm (`evidence_links`/`standard_assessments`/commission-owned frameworks are all `is_member_of`); pre-fix visibility was the coercion showing an empty shell, not access. ⑤ **staff-member-who-is-also-tenancy-admin gets configuration nav** (nav mirrors the `canConfigureCommission` seam). ⑥ **Dual-hat quality_reviewer+tenancy-admin keeps reviewer-shell precedence** — latent (no persona holds both), recorded for the PO rather than re-ruled mid-phase. ⑦ `setTemplateCaseType` NOT routed to the config seam — its DB door is staff_admin-only (ADR 0088); Q2-consistency would be a DB wave, PO's call. | [BUG-QOB-003](bug-log-archive.md) · B.11/B.12 |
| 2026-08-08 | **QO·B content-wall classification RATIFIED (PO, Q1–Q9)** — **KEEP as configuration:** form definitions, `process_template_*`, committee taxonomy + meeting settings (*"the admin shapes the containers, never reads what goes in them"*). **SPLIT:** indicator DEFINITIONS keep, MEASUREMENTS cut. **hospital_admin gets the SAME wall** as org_admin (identical measured reach; walling only org_admin leaves a documented bypass). **Case-ACCESS doors KEEP** (A4 scope ruling + self-escalation independently blocked — an org_admin holds no membership row so its self-grant raises *"o responsável deve ser membro da comissão"*). **Classification doors KEEP** (`set_case_visibility`/`set_case_confidentiality` — mirrors `set_commission_oversight`); **`dispose_case_phi` CUT** (a principal with zero PHI bits does not destroy Rule 12 data — D5). **Both live defects filed as bugs AND fixed** (BUG-QOB-001/002). **`is_commission_admin_of` → `is_tenancy_admin_of` rename APPROVED but DEFERRED to its own wave AFTER QO·B**, so it cannot confound the equivalence matrix. | [0100](../decisions/0100-quality-office-oversight.md) D12 · [inventory §6](../plans/quality-office-oversight-phase-b-inventory.md) |
| 2026-08-08 | **`revoke_printed_document` KEEPS the tenancy arm — the older ruling overrules the newer draft.** QO·B's §4.3 draft listed it CUT; **ADR 0104 D11** (QA MINOR-2, lead-ruled) already held revocation a **governance** act that reveals no content — the admin chain may revoke an ata print it cannot download. Per-door judgement beat the list. Pinned by a migration postcondition, keystone `314` 8.5, and mutation case `overcut_revoke_ruling`, so a future "finish the printed-doc wall" sweep reds instead of silently reversing a decision it never read. | [0104](../decisions/0104-pdf-document-printing-module.md) D11 |
| 2026-08-05 | **AFF — hospital affiliation, CPF identity & the org people directory** (PO, grilling interview) — hospital affiliation becomes a **row** (`hospital_affiliations`, soft-ended, matrícula per employment) and is a **visibility input, never a capability input** (amends 0048 D7: hospital *is* now a read boundary); `profiles.home_hospital_id` + `hospital_employee_id` **dropped**; **`profiles.cpf`** platform-unique = the person key (nullable column, required at the action layer); the already-shipping org-wide roster disclosure **ratified** via `list_org_people` gated on `is_org_admin_of OR is_org_level_admin_within` (amends 0048 D1), CPF **exact-match input only, never in the payload**; one **identifier-first** registration flow (`registerUser` keeps its collision block as backstop — amends 0048 D9); `professional_profiles` gains `cpf` but linking is **deferred and only ever from inside a case** (a registration-side match would disclose ethics-case subjecthood); person-level fields **`org_admin`-only**, account deactivation unreachable by hospital admins; single-hospital tenants seat `org_admin` + `hospital_admin` at **provisioning** (no model change, no self-grant relaxation) behind a new **dominance grid** + 2 live gap fixes. **All pre-pilot** — the schema is free only while the reset is | [0097](../decisions/0097-hospital-affiliation-person-identity.md) · [plan](../plans/hospital-affiliation-person-identity.md) |
| 2026-08-04 | **The authz door-blindness invariant becomes a PHASE STEP, diff-scoped** — §6 step 1 gains ARM 2 (~1 min) every phase + a **diff-scoped** ARM 1 whenever a phase touches an RLS policy or `prosecdef` gate (case list derived from the migration diff, never by hand); step 5 must **name the ARM, not the script**. The full ~5 h sweep stays a **periodic** audit — mandating it per phase would reproduce the failure it fixes, and a diff-scoped gate is adoptable today against the open 15-violation backlog | [0079 Amendment 1](../decisions/0079-authz-door-blindness-standing-invariant.md) · [CLAUDE.md §6](../../CLAUDE.md) |
| 2026-08-04 | **Membership-hardening + Diretor Técnico: the four open items closed** (PO) — T1.0 = **atomic replace**; **platform_admin may NOT appoint a DT** (tenant governance, not tenancy administration — the only kernel grant arm with no `is_admin_for` branch); build **W1→W4 straight through** on one branch; `technical_director` flag **ships ON** at T4.9 | [0094 Amendment 1](../decisions/0094-membership-hardening-and-technical-director.md) · [plan](../plans/membership-hardening-technical-director.md) |
| 2026-08-03 | **Phase 16 build plan authored + 4 planning rulings** (PO, interview) — CAPA evidence arm = hospital-match + `can_read_capa`; commission-owned frameworks commission-scoped SELECT (narrows D10 — licensed-text leak); visible hospital nav entry; ONA skeleton drafted by backend, PO-validated. Build not started | [0093 Amendment 1](../decisions/0093-phase-16-standards-crosswalk-replan.md) · [plan](../plans/phase-16-standards-crosswalk-program.md) |
| 2026-08-03 | **Phase 16 replanned + re-gates the pilot** (PO, interview) — pre-pilot again (supersedes 0086's deferral note); skeleton-only framework packs (licensing); ONA `level` dimension + per-level readiness; evidence enum +`charter`+`ethics_procedure` (folds ETH·E3b); worst-wins rollup + `standard_ownerships`; `hospital_readiness` re-gated off `is_admin` (noun rule) | [0093](../decisions/0093-phase-16-standards-crosswalk-replan.md) · [audit](../reviews/phase-16-external-accreditation-audit.md) |

## Verbose rows rotated out of PROGRESS.md 2026-08-14 (the size rotation) — 4 rows, 2026-08-12 → 2026-08-14

> Retention (lead-playbook §5): PROGRESS.md carries **one line per decision + ADR link**; rationale
> lives in `docs/decisions/`. These four had grown to full-paragraph form (the D17 row alone ~2.6 KB).
> Preserved verbatim; the ADR remains the authority.

| Date | Decision | Ref |
| --- | --- | --- |
| 2026-08-14 | **DM5 designs for a RESET remote — ADR 0120 D17** (PO). A full DB reset is available on local **and** remote, so S3 builds the correct D7/D11/D13 shape directly and drops the *copy → verify → switch* ceremony for production print objects. ⚠ **Not "no data migration"** — a fresh reset still produces `printed_documents` rows from the **seed** and `312`/`313`/`323`, which are rewritten to the new shape in-slice. ⭐ **No backfill is a SAFETY property:** with none, the create path is the only path, so an untaught create path fails **loudly** instead of being masked — DM3's P0 exactly. ⛔ **A CORRECTION the lead made to its own framing before recording it:** putting this to the PO, the lead said a wiped remote *"dissolves"* FUP-DM5-STORAGE-ORPHANS. **Wrong — and the follow-up already said so in writing:** a reset truncates `storage.objects` and **leaves the bytes**, so it **creates** orphans (measured: 0 rows vs 699 files / 7.0 MB / 198 PHI-tier). **Binding ordering, the reverse of the intuitive one: delete-by-manifest via the Storage API FIRST, while keys are still enumerable, and only THEN reset.** S0's manifest tool stays load-bearing. D17 dissolves only the **DB-row** half. ⛔ **D17 closes NO follow-up** — FUP-DM4-PRODROW + FUP-DM5-STORAGE-ORPHANS move to *"close by the manifest-then-reset sequence at deploy"* and stay OPEN until it runs. **No `db push` or remote reset is authorized by D17**; both need explicit PO go-ahead on the day. | ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) **D17** · [plan S3/S4](../plans/dm5-wave-d-retirement-plan.md) |
| 2026-08-13 | **DM3 ethics depth RULED — plumbing to writable, NO UI** (PO). Raised because the four Q1 discharge conditions were **incomplete**: catalog-verified, `set_ethics_decision_details` takes **11 params, none a document id**, and `src/lib/ethics/actions.ts:393` accepts `decisionLetterDocumentId` then **silently drops it** — so an FK alone would have swapped "a column pointing at nothing" for "a column pointing at documents nothing can create". Adds **condition 5**: add `p_decision_letter_document_id` to the RPC + forward it from TS. ⚠ `issue_ethics_notification`'s `p_related_document_id` **still exists** (arg 7/8) — the refusal is in the **body**, so a body change, NOT a DROP+CREATE that would discard the ACL. **No attach-a-letter UI in DM3** — a decision, not an omission: none has ever existed (verified across the ethics dialogs, every `type="file"` site, and the absence of any reader of either field), and a decision letter is the archetypal `legal_privileged` document whose UI needs the ETH·E1 spine + D15 ceiling designed as a feature → **FUP-DM3-ETHICS-UI** | ADR [0114](../decisions/0114-document-model-redesign.md) **Amdt 2 / D17** |
| 2026-08-13 | **Q1 RULED — the two orphaned ethics document seams join Wave B (DM3)** (PO). `ethics_decision_details.decision_letter_document_id` + `ethics_notifications.related_document_id` were a **scope gap, not a deferral**: ADR 0114 D13's four waves named ethics in none. A disciplinary decision letter is a *governed* document with an approval/effective lifecycle — the controlled-document shape — so it reuses Wave B's machinery. Wave A was foreclosed (DM2 closed + approved; adopting there = reopening a completed phase); a post-DM5 follow-up was rejected because it closes the retirement manifest with two columns pointing at nothing. **Four binding discharge conditions, partial ≠ discharged:** real FK to `documents(id)` · `issue_ethics_notification`'s `p_related_document_id` restored to working · fail-closed rejection removed · **keystone K8 removed** (a keystone pinning a refusal the product no longer wants is a test asserting a bug). ⚠ **Lifecycle is shared, the READER SET is not** — ethics reads run on the ADR 0072/ETH·E1 spine (`case_access_grants` + `max_confidentiality` + recusal); negative twin required | ADR [0114](../decisions/0114-document-model-redesign.md) **Amendment 2 / D17** · [plan DM3 step 5](../plans/document-model-redesign.md) |
| 2026-08-12 | **The referral's "Registros internos" file under the CASE Registro vocabulary; `referral_note_types` is deleted** (PO, both calls taken explicitly: full replacement over keep-the-table, and REQUIRED-with-default-`note` over keep-optional). Adds `update`/`follow_up`, so the shared list is six. **No snapshot column replaces `type_label`** — it existed to survive a user rename, and a fixed platform-wide list cannot be renamed, so the label resolves at render from one source. That source (`src/lib/cases/registro-kinds.ts`) is import-free and side-effect-free **so a `"use client"` component and a server data-access module can both read it** — which is the only reason one source of truth is possible; it narrows `referrals/types.ts`'s "ZERO imports" contract to "one inert import", stated in both files. ⚠ The two writers changed SIGNATURE (`uuid`→`text`) ⇒ DROP+CREATE ⇒ **the ACL is discarded**; all four grants re-issued and re-verified against the catalog | ADR [0110](../decisions/0110-shared-registro-kind-vocabulary.md) (supersedes [0109](../decisions/0109-referral-registros-and-case-access-summary.md) D2) |

## Live index rows as they stood before the 2026-08-14 compaction (32 rows, pre-2026-07 → 2026-08-10)

> Most of these were already archived verbose above (2026-08-04 / 2026-08-10 rotations); the whole
> range is copied here so the compaction below can be reversed from one place. The `2026-08-10
> document_printing ON PERMANENTLY` row had **no** verbose copy anywhere — checked, not assumed.

| Date | Decision | Ref |
| --- | --- | --- |
| 2026-08-10 | **`document_printing` is ON PERMANENTLY in production; the Gotenberg Coolify resource is active** (PO). **Supersedes ADR [0104](../decisions/0104-pdf-document-printing-module.md)'s "Flag: `document_printing`, ships **OFF**" clause** — the flag was a deploy-sequencing guard for the missing renderer sidecar, and the sidecar now exists, so the guard has no remaining job. P3/P4 must not re-assert the OFF posture. ⚠ Recorded on the PO's operational confirmation — the renderer is external infrastructure, not something the catalog can attest. | [0104](../decisions/0104-pdf-document-printing-module.md) · [runbook](../deployment/pdf-renderer.md) |
| 2026-08-10 | **ACT S3 — classify a hat-blind gate by CALL-SITE BINDING, not signature shape.** A gate that *receives* a caller uid can still be caller-bound; the population must be derived as a property (transitive caller-boundness over the call graph), never read off signatures | ADR [0079](../decisions/0079-authz-door-blindness-standing-invariant.md) Amendment 6 · [§7.17](../progress/authz-handoff.md) · [0106](../decisions/0106-act-as-role-assumption.md) |
| 2026-08-09 | **ACT S0 — the role enum lands in `public.platform_role`, not `app`** (LEAD, measured) — `config.toml` exposes only `public`/`graphql_public`, so an `app` enum is invisible to `gen:types`; exposing `app` would put ~281 DEFINER doors on PostgREST. A bare enum TYPE is not a relation. 11 labels | [0106](../decisions/0106-act-as-role-assumption.md) · [plan](../plans/act-as-role-assumption.md) |
| 2026-08-09 | **`is_commission_admin_of` → `is_tenancy_admin_of` RENAMED** (PO; no shim, living docs only) — the old name asserted the opposite of its meaning. ⚠ `pg_policy` stores the function by **OID**, so all 54 policies followed the rename with no policy edits | ADR [0105](../decisions/0105-rename-is-tenancy-admin-of.md) |
| 2026-08-09 | **QO·B PO RATIFICATION — the FUP-QOB-2 package worked item by item**, each verdict taken against a live-catalog measurement rather than the doc's own claim. Ratified ①②④ + FUP-QOB-1's J1c pin; ruled BUG-QOB-004 = CUT-the-arms | [FUP-QOB-2](../progress/follow-ups.md) · [0100](../decisions/0100-quality-office-oversight.md) D12 |
| 2026-08-09 | **QO·B BUG-QOB-003 presentation rulings** (LEAD, catalog/precedent-backed) — fix shape = flag + KEEP-scoped UI; `manage/audit/**` + CSV KEEP; `manage/charter` NOT KEEP; `manage/acreditacao/**` stays membership-gated | [BUG-QOB-003](bug-log-archive.md) · B.11/B.12 |
| 2026-08-08 | **QO·B content-wall classification RATIFIED (PO, Q1–Q9)** — configuration KEEP / content CUT (*"the admin shapes the containers, never reads what goes in them"*); indicator DEFINITIONS keep, MEASUREMENTS cut; hospital_admin gets the SAME wall; case-ACCESS + classification doors KEEP; `dispose_case_phi` CUT | [0100](../decisions/0100-quality-office-oversight.md) D12 · [inventory §6](../plans/quality-office-oversight-phase-b-inventory.md) |
| 2026-08-08 | **`revoke_printed_document` KEEPS the tenancy arm — the older ruling overrules the newer draft.** ADR 0104 D11 already held revocation a *governance* act revealing no content; pinned by keystone `314` 8.5 + mutation case `overcut_revoke_ruling` so a later wall-finishing sweep reds instead of silently reversing it | [0104](../decisions/0104-pdf-document-printing-module.md) D11 |
| 2026-08-05 | **AFF — hospital affiliation, CPF identity & the org people directory** (PO, grilling interview) — affiliation becomes a **row** and is a **visibility input, never a capability input** (amends 0048 D7); `profiles.cpf` = the person key; org-wide roster disclosure ratified (amends 0048 D1); one identifier-first registration flow | [0097](../decisions/0097-hospital-affiliation-person-identity.md) · [plan](../plans/hospital-affiliation-person-identity.md) |
| 2026-08-04 | **The authz door-blindness invariant becomes a PHASE STEP, diff-scoped** — §6 step 1 gains ARM 2 every phase + a diff-scoped ARM 1 whenever a phase touches an RLS policy or `prosecdef` gate; step 5 must **name the ARM, not the script**. The full ~5 h sweep stays a periodic audit | [0079 Amendment 1](../decisions/0079-authz-door-blindness-standing-invariant.md) · [CLAUDE.md §6](../../CLAUDE.md) |
| 2026-08-04 | **Membership-hardening + Diretor Técnico: the four open items closed** (PO) — T1.0 = atomic replace; **platform_admin may NOT appoint a DT**; build W1→W4 straight through on one branch; `technical_director` ships ON | [0094 Amendment 1](../decisions/0094-membership-hardening-and-technical-director.md) · [plan](../plans/membership-hardening-technical-director.md) |
| 2026-08-03 | **Phase 16 build plan authored + 4 planning rulings** (PO, interview) — CAPA evidence arm = hospital-match + `can_read_capa`; commission-owned frameworks commission-scoped SELECT; visible hospital nav entry; ONA skeleton PO-validated | [0093 Amendment 1](../decisions/0093-phase-16-standards-crosswalk-replan.md) · [plan](../plans/phase-16-standards-crosswalk-program.md) |
| 2026-08-03 | **Phase 16 replanned + re-gates the pilot** (PO, interview) — pre-pilot again; skeleton-only framework packs (licensing); ONA `level` dimension + per-level readiness; evidence enum +`charter`+`ethics_procedure`; worst-wins rollup; `hospital_readiness` re-gated off `is_admin` (noun rule) | [0093](../decisions/0093-phase-16-standards-crosswalk-replan.md) · [audit](../reviews/phase-16-external-accreditation-audit.md) |
| 2026-07-27 | **Flexible-Forms FF-1…FF-5 pulled pre-pilot** (PO) — all five feature phases build before the pilot and gate the pilot deploy; order … | [0086](../decisions/0086-flexible-forms-pre-pilot.md) · [program](../plans/flexible-forms-program.md) |
| 2026-07-27 | **FF-1 Repeating Groups rulings** (PO, grilling interview) — nesting **capped at depth 1, schema-enforced**; conditions resolve **inside-out** … | [0087](../decisions/0087-ff1-repeating-groups.md) |
| 2026-07-23 | **Case custom fields** — template-defined, **non-PHI** administrative descriptors on cases (e.g. M&M "Número da Declaração de Óbito"); … | [0083](../decisions/0083-case-custom-fields.md) |
| 2026-07-16 | **`manage_case_access` — KEEP (confirmed, PO).** The resolver computes `v_orgadmin` (`is_commission_admin_of_for`, ~19% of per-row cost) solely to … | [ADR 0078 D1/A16](../decisions/0078-authorization-capability-model.md) |
| 2026-07-16 | **Meeting family — ACCEPT AS-IS (exclusion-perimeter residual).** A coordinator recused from case X can conclude a multi-case meeting discussing X, … | [handoff §5](../progress/authz-handoff.md) |
| 2026-07-16 | **ADR 0078 A5 perf gate PASSED — no migration.** Resolver parity-or-faster + strictly linear on realistic (2005-case) data; qa MAJOR-1 ~10× was vs … | [ADR 0078 D2/A5](../decisions/0078-authorization-capability-model.md) · [handoff §5](../progress/authz-handoff.md) |
| 2026-07-12 | **Pre-pilot release scope expansion** — pulled 12 initiatives (Phases 20–21, Referrals v2, Interviews v2, Ethics E1–E3, action-items … | [0071](../decisions/0071-pre-pilot-release-scope-expansion.md) |
| 2026-07-12 | **Referrals v2 — Dialogue & Governance** — adopt the missing two-way dialogue (`referral_messages` thread) + defer 12 items (triage/SLA, … | [0037-A1](../decisions/0037-inter-committee-case-referrals.md) · [plan](../plans/referrals-v2-dialogue-governance.md) |
| 2026-07-10 | **Pre-Pilot Foundations Program** — one collision-free plan sequencing F0→F1 participants→F2 attachments→F3 flexible-forms→F-cleanup for … | [program](../plans/pre-pilot-foundations-program.md) |
| 2026-07-09 | **Case subject generalization → participants/roles/professional-registry/case-types** (E0 foundation for Ethics + other non-patient-centered … | [0064](../decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Centralized-attachments substrate refinement** (14e) — single authorizing owner + `attachment_references`, `attachment_subjects`, … | [0063](../decisions/0063-centralized-attachments-substrate.md) |
| 2026-07-08 | **Administrativo delegated-capability role** — per-commission appointment + curated, finite capability menu … | [0061](../decisions/0061-administrativo-delegated-role.md) |
| 2026-07-06 | **Coolify as the pre-Phase-9 dev/staging deployment target** — Dockerfile-only (Coolify's Traefik replaces the Phase-9-planned Caddy/compose … | [0059](../decisions/0059-coolify-deployment-target.md) |
| 2026-07-05 | **Phase 15/17 revision + pre-pilot re-sequencing** — build order **15 → 17 → 16** (documents pulled pre-pilot; pilot after 16; 18–21 … | [0057](../decisions/0057-indicators-doc-control-replan.md) |
| 2026-07-06 | **Phase 15 derived-measurement compute** — `compute_derived_measurement` replicates the `dashboard_distributions` mechanics (submitted spine, … | [0058](../decisions/0058-derived-measurement-compute.md) |
| 2026-07-02 | **Action-items fold + `visibility_scope` + case-access expiry** — scope-aware hub `SELECT` via `can_read_action_item`; fold `case_action_items` → … | [0050](../decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md) |
| 2026-07-01 | **Answer-Model v2** (planned) — uniform answer row + typed scalar cols (`value_number/date/time`) + instance-ready answer key … | [0045](../decisions/0045-answer-model-v2.md) |
| 2026-07-01 | **Forward-compat form capabilities** (planned) — `form_items.parent_item_id` + reserved hooks for repeating groups / file-signature-matrix answers … | [0046](../decisions/0046-forward-compat-form-capabilities.md) |
| _pre-2026-07_ | **35 earlier decision rows (Phases 0–14, 2026-06-11 → 2026-06-25) rotated 2026-08-04** — one line each, plus the verbose form of every one | [decisions-log.md](../progress/decisions-log.md) |

---

## ↩ Rotated from PROGRESS.md 2026-08-17 (the §6-step-5 size rotation) — Decisions table rows, VERBATIM

PROGRESS.md's § Decisions had grown to **11.6 KB** of one-line-per-decision rows reaching back to
2026-07-01. The live file keeps only the **head** of a cross-phase log (CLAUDE.md §7); these 23 rows
(2026-08-05 and older) are rotated here. ⚠ **Appended BEFORE the cut and `cmp`-verified — then relative links `](docs/…)` were repointed to `](../…)` (23 of them), because a root-relative link copied from PROGRESS.md 404s from `docs/progress/`. Prose verbatim, link targets repointed.** Per the
same protocol as the S5/S4/S3 rotation.

| Date | Decision | Ref |
| --- | --- | --- |
| 2026-08-05 | **AFF — hospital affiliation, CPF identity & the org people directory** (PO) — affiliation becomes a **row** and is a **visibility input, never a capability input** (amends 0048 D7); `profiles.cpf` = the person key; org-wide roster disclosure ratified (amends 0048 D1) | [0097](../decisions/0097-hospital-affiliation-person-identity.md) |
| 2026-08-04 | **The authz door-blindness invariant becomes a PHASE STEP, diff-scoped** — §6 step 1 gains ARM 2 every phase + a diff-scoped ARM 1 whenever a phase touches an RLS policy or `prosecdef` gate; step 5 must **name the ARM, not the script**. The full ~5 h sweep stays a periodic audit | [0079 Am. 1](../decisions/0079-authz-door-blindness-standing-invariant.md) |
| 2026-08-04 | **Membership-hardening + Diretor Técnico: the four open items closed** (PO) — T1.0 = atomic replace; **platform_admin may NOT appoint a DT**; build W1→W4 on one branch; `technical_director` ships ON | [0094 Am. 1](../decisions/0094-membership-hardening-and-technical-director.md) |
| 2026-08-03 | **Phase 16 build plan + 4 planning rulings** (PO) — CAPA evidence arm = hospital-match + `can_read_capa`; commission-owned frameworks commission-scoped SELECT; visible hospital nav entry; ONA skeleton PO-validated | [0093 Am. 1](../decisions/0093-phase-16-standards-crosswalk-replan.md) |
| 2026-08-03 | **Phase 16 replanned + re-gates the pilot** (PO) — skeleton-only framework packs (licensing); ONA `level` dimension; evidence enum +`charter`+`ethics_procedure`; worst-wins rollup; `hospital_readiness` re-gated off `is_admin` (the noun rule) | [0093](../decisions/0093-phase-16-standards-crosswalk-replan.md) |
| 2026-07-27 | **Flexible-Forms FF-1…FF-5 pulled pre-pilot** (PO) — all five gate the pilot deploy | [0086](../decisions/0086-flexible-forms-pre-pilot.md) |
| 2026-07-27 | **FF-1 Repeating Groups rulings** (PO) — nesting **capped at depth 1, schema-enforced**; conditions resolve **inside-out** | [0087](../decisions/0087-ff1-repeating-groups.md) |
| 2026-07-23 | **Case custom fields** — template-defined, **non-PHI** administrative descriptors on cases | [0083](../decisions/0083-case-custom-fields.md) |
| 2026-07-16 | **`manage_case_access` — KEEP (confirmed, PO)** | [0078 D1/A16](../decisions/0078-authorization-capability-model.md) |
| 2026-07-16 | **Meeting family — ACCEPT AS-IS (exclusion-perimeter residual)** — a coordinator recused from case X can conclude a multi-case meeting discussing X | [handoff §5](../progress/authz-handoff.md) |
| 2026-07-16 | **ADR 0078 A5 perf gate PASSED — no migration.** Resolver parity-or-faster + strictly linear on realistic (2005-case) data | [0078 D2/A5](../decisions/0078-authorization-capability-model.md) |
| 2026-07-12 | **Pre-pilot release scope expansion** — pulled 12 initiatives (Phases 20–21, Referrals v2, Interviews v2, Ethics E1–E3, action-items …) | [0071](../decisions/0071-pre-pilot-release-scope-expansion.md) |
| 2026-07-12 | **Referrals v2 — Dialogue & Governance** — adopt the two-way `referral_messages` thread + defer 12 items | [0037-A1](../decisions/0037-inter-committee-case-referrals.md) |
| 2026-07-10 | **Pre-Pilot Foundations Program** — one collision-free plan sequencing F0→F1→F2→F3→F-cleanup | [program](../plans/pre-pilot-foundations-program.md) |
| 2026-07-09 | **Case subject generalization → participants/roles/professional-registry/case-types** (E0 foundation for Ethics) | [0064](../decisions/0064-case-subject-generalization-participants.md) |
| 2026-07-09 | **Centralized-attachments substrate refinement** (14e) — single authorizing owner + `attachment_references`/`_subjects` | [0063](../decisions/0063-centralized-attachments-substrate.md) |
| 2026-07-08 | **Administrativo delegated-capability role** — per-commission appointment + a curated, finite capability menu | [0061](../decisions/0061-administrativo-delegated-role.md) |
| 2026-07-06 | **Coolify as the pre-Phase-9 dev/staging deployment target** — Dockerfile-only (Traefik replaces the planned Caddy/compose) | [0059](../decisions/0059-coolify-deployment-target.md) |
| 2026-07-05 | **Phase 15/17 revision + pre-pilot re-sequencing** — build order **15 → 17 → 16**; 18–21 post-pilot | [0057](../decisions/0057-indicators-doc-control-replan.md) |
| 2026-07-06 | **Phase 15 derived-measurement compute** — `compute_derived_measurement` replicates the `dashboard_distributions` mechanics | [0058](../decisions/0058-derived-measurement-compute.md) |
| 2026-07-02 | **Action-items fold + `visibility_scope` + case-access expiry** — scope-aware hub `SELECT` via `can_read_action_item` | [0050](../decisions/0050-action-items-fold-visibility-scope-case-access-expiry.md) |
| 2026-07-01 | **Answer-Model v2** — uniform answer row + typed scalar cols + instance-ready answer key | [0045](../decisions/0045-answer-model-v2.md) |
| 2026-07-01 | **Forward-compat form capabilities** — `form_items.parent_item_id` + reserved hooks | [0046](../decisions/0046-forward-compat-form-capabilities.md) |

## ↩ Rotated from PROGRESS.md 2026-08-18 (the size rotation) — 12 rows moved out + 32 verbose rows collapsed live

> PROGRESS.md was 142 KB against §7's <60 KB target. Links below are repointed from
> root-relative to `docs/progress/`-relative — a **verbatim** rotation 404s its own links.

### A. Moved out entirely (12 rows, 2026-08-08 → 2026-08-14) — no live counterpart

| Date | Decision | Ref |
| --- | --- | --- |
| 2026-08-14 | **DM5 designs for a RESET remote — ADR 0120 D17** (PO). A full reset is available local **and** remote, so S3 builds the correct D7/D11/D13 shape directly and drops the copy→verify→switch ceremony. ⚠ **Not "no data migration"** — a fresh reset still produces `printed_documents` rows from the seed + `312`/`313`/`323`. ⭐ **No backfill is a SAFETY property**: with none, the create path is the only path, so an untaught create path fails **loudly** — DM3's P0 exactly. ⛔ **A LOCAL reset creates storage orphans** (it recreates the DB while the Docker volume survives, so `storage.objects` is empty while the bytes remain — measured 0 rows vs 699 objects) — binding order, the reverse of the intuitive one: **delete-by-manifest via the Storage API FIRST, then reset**. ⚠ **CORRECTED 2026-08-14: the REMOTE half of this was false** — it generalised the local measurement by *"the same mechanism class"*; the remote mechanism was one line in the CLI's `drop.sql`, reverted [cli#3359](https://github.com/supabase/cli/pull/3359), grep-verified **absent** at v2.105.0 with an `auth` positive control. The **ordering stands on the local rationale alone**. D17 closes **NO** follow-up, and authorizes **no** `db push` or remote reset | ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) **D17** · [plan S3/S4](../plans/dm5-wave-d-retirement-plan.md) |
| 2026-08-13 | **DM3 ethics depth RULED — plumbing to writable, NO UI** (PO). Adds discharge condition 5: `p_decision_letter_document_id` on the RPC + forwarded from TS (it was accepted then **silently dropped**). ⚠ Fix `issue_ethics_notification` in the **body**, never DROP+CREATE — that discards the ACL. No attach-a-letter UI in DM3 is a **decision, not an omission** → FUP-DM3-ETHICS-UI | ADR [0114](../decisions/0114-document-model-redesign.md) **Amdt 2 / D17** |
| 2026-08-13 | **Q1 RULED — the two orphaned ethics document seams join Wave B (DM3)** (PO). A scope gap, not a deferral. **Four binding discharge conditions; partial ≠ discharged** (real FK · `p_related_document_id` restored · fail-closed rejection removed · **keystone K8 removed** — a keystone pinning a refusal the product no longer wants is a test asserting a bug). ⚠ Lifecycle is shared, the **READER SET is not** — ethics reads run on the ADR 0072/ETH·E1 spine; negative twin required | ADR [0114](../decisions/0114-document-model-redesign.md) **Amdt 2 / D17** · [plan](../plans/document-model-redesign.md) |
| 2026-08-12 | **The referral's "Registros internos" files under the CASE Registro vocabulary; `referral_note_types` is deleted** (PO — full replacement, and REQUIRED-with-default-`note`). Six shared kinds; **no snapshot column replaces `type_label`** (a fixed platform-wide list cannot be renamed). One source, `src/lib/cases/registro-kinds.ts`, import-free **so a `"use client"` component and a server module can both read it**. ⚠ Both writers changed SIGNATURE (`uuid`→`text`) ⇒ DROP+CREATE ⇒ **the ACL is discarded**; all four grants re-issued and catalog-verified | ADR [0110](../decisions/0110-shared-registro-kind-vocabulary.md) (supersedes [0109](../decisions/0109-referral-registros-and-case-access-summary.md) D2) |
| 2026-08-10 | **`document_printing` is ON PERMANENTLY in production; the Gotenberg Coolify resource is active** (PO) — **supersedes ADR 0104's "ships OFF" clause** (the flag guarded a missing sidecar that now exists). P3/P4 must not re-assert the OFF posture. ⚠ Recorded on operational confirmation — the renderer is external infra the catalog cannot attest | [0104](../decisions/0104-pdf-document-printing-module.md) · [runbook](../deployment/pdf-renderer.md) |
| 2026-08-10 | **ACT S3 — classify a hat-blind gate by CALL-SITE BINDING, not signature shape.** A gate that *receives* a caller uid can still be caller-bound; derive the population as a property over the call graph, never off signatures | [0079 Am. 6](../decisions/0079-authz-door-blindness-standing-invariant.md) · [0106](../decisions/0106-act-as-role-assumption.md) |
| 2026-08-09 | **ACT S0 — the role enum lands in `public.platform_role`, not `app`** (LEAD, measured) — `config.toml` exposes only `public`, so an `app` enum is invisible to `gen:types`, and exposing `app` would put ~281 DEFINER doors on PostgREST | [0106](../decisions/0106-act-as-role-assumption.md) |
| 2026-08-09 | **`is_commission_admin_of` → `is_tenancy_admin_of` RENAMED** (PO; no shim) — the old name asserted the opposite of its meaning. ⚠ `pg_policy` stores the function by **OID**, so all 54 policies followed with no policy edits | [0105](../decisions/0105-rename-is-tenancy-admin-of.md) |
| 2026-08-09 | **QO·B PO RATIFICATION — the FUP-QOB-2 package, item by item**, each verdict taken against a live-catalog measurement rather than the doc's claim. Ratified ①②④ + FUP-QOB-1's J1c pin; ruled BUG-QOB-004 = CUT-the-arms | [follow-ups](follow-ups.md) · [0100](../decisions/0100-quality-office-oversight.md) D12 |
| 2026-08-09 | **QO·B BUG-QOB-003 presentation rulings** (LEAD) — fix = flag + KEEP-scoped UI; `manage/audit/**` + CSV KEEP; `manage/charter` NOT KEEP; `manage/acreditacao/**` stays membership-gated | [Bug Log](bug-log-archive.md) · B.11/B.12 |
| 2026-08-08 | **QO·B content-wall classification RATIFIED (PO, Q1–Q9)** — configuration KEEP / content CUT (*"the admin shapes the containers, never reads what goes in them"*); indicator DEFINITIONS keep, MEASUREMENTS cut; hospital_admin gets the SAME wall; `dispose_case_phi` CUT | [0100](../decisions/0100-quality-office-oversight.md) D12 |
| 2026-08-08 | **`revoke_printed_document` KEEPS the tenancy arm — the older ruling overrules the newer draft** (0104 D11: revocation is a *governance* act revealing no content). Pinned by keystone `314` 8.5 + mutation case so a later wall-finishing sweep reds instead of silently reversing it | [0104](../decisions/0104-pdf-document-printing-module.md) D11 |

### B. Verbose form of the 32 rows that REMAIN live as one-liners

| Date | Decision (verbose) | Ref |
| --- | --- | --- |
| 2026-08-18 | **DM-FUP TRIAGE #1 — the Cloud orphan measurement must CONSTRUCT an orphan, not probe for one** (PO). The record called the S3 probe *"the single measurement that would settle it"*; **as framed it is not** — the remote holds **0 objects**, so a read-only probe can only show that the endpoint answers, never that it would *reveal* an orphan. Ruled: on the **linked project**, upload a synthetic non-PHI byte, delete its `storage.objects` row, ask S3 whether the byte survives, clean up. A throwaway project was **declined** — it answers for a different storage config, which is the grain error this record has already been burned by twice. ⭐ *A detector that finds nothing must be proven able to find something* | FUP-DM5-CLOUD-ORPHAN-SURFACE |
| 2026-08-18 | **DM-FUP TRIAGE #2 — `reclassifyDocument` writes `unavailable_on_platform`** (PO). ⛔ The deciding fact: **no app code branches on deployment** (`src/lib/documents/`, `src/lib/supabase/` carry zero `STORAGE_BACKEND`/`isLocal`/`NODE_ENV` checks), so ONE literal serves both environments while the honest answer differs between them. An env-derived value was **declined** — a misconfigured env writes a FALSE proof into the regulator-facing ADR 0121 D4 evidence, failing in the reassuring direction. ⛔ **Explicitly rejected: verifying at call time** — a Storage-API `list()` after `remove()` reads `storage.objects`, so it would manufacture `local_volume_verified` out of the exact metadata proxy NO-ANSWER instance 3 exists to condemn | FUP-DM5-BYTE-PROOF-NOT-ATTEMPTED |
| 2026-08-18 | **DM-FUP TRIAGE #3 — Critical FUP C1 SPLITS into C1a (local) + C1b (Cloud); C1 does NOT close on C1a** (PO). The runbook already states that a local rehearsal *"runs against a local stack by construction, so it cannot exercise the Cloud paths"* (§6) — yet the pilot runs on Cloud, so a local-only run would discharge C1's **wording** while leaving its **purpose** undischarged. C1a runs now (it debugs the procedure and produces the first backup **destination path** that `FUP-DM5-BACKUP-IS-PHI-EXPORT` is owed at first execution); **the bounded pilot-risk acceptance stays bounded by C1b, not C1a** | ADR [0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md) **Amdt 3** (amended) · **Critical FUP C1** |
| 2026-08-18 | **DM-FUP TRIAGE #4 — `document_version_files` gets `UNIQUE (file_object_id)`** (PO). All three writers (`complete_document_reclassification`, `complete_document_upload_verification`, `mint_printed_document`) mint a fresh `file_object` per insert, so 1:1 holds today by **caller discipline alone**; the constraint makes it structural, which is what ADR 0121's disposal lifecycle needs — a shared `file_object` means marking one row `disposal_pending` silently destroys another row's bytes. Knowingly forecloses rendition byte-sharing. ⛔ **Census the remote for duplicates BEFORE push** — local is **0 rows**, so a green local reset proves nothing about push-safety | FUP-DM5-DVF-FILEOBJ |
| 2026-08-18 | **DM-FUP TRIAGE #5 — the Q1 arm's NAMED successor is `app.resolve_document_version_bytes`, and the arm moves into C2 Tier 1** (PO). **Derived from the catalog, not chosen**: the byte corridor is `public.open_document_version`/`open_printed_document` (thin — flag assert + dispatch) → the `app` kernel that holds `can_read_document`, `can_read_referral_phi`, the D15 ceiling and the serving-state refusals. ⭐ **There is no `storage.objects` SELECT policy on document bytes at all** (only `form_assets_select_member` survives), so the item's *"re-point it at a policy"* framing had no target — the gate is `prosecdef = t` and **replaces** RLS. That makes it a door-mutation arm, the same machinery as C2's 407-door sweep; building that machinery twice was declined | FUP-DM5-Q1-OPEN-BYTES-CUT-BROKEN · **Critical FUP C2** |
| 2026-08-18 | ⛔⛔ **THE REMOTE WAS RESET AT `2026-08-17 11:37:35Z` — MEASURED FROM THE LOGS, ~14 h BEFORE TRIAGE #6 SEQUENCED IT LAST.** Signature: `CREATE TABLE IF NOT EXISTS schema_migrations` → all `CREATE EXTENSION` → migrations from `20260711…` **re-applied** (old migrations only re-run on an empty history table; a `db push` skips them). The known push came *after*, 2026-08-18 01:19. No `TRUNCATE`/`DROP SCHEMA` in the window — the emptying was the reset's schema rebuild. ⭐ **This EXONERATES everyone and indicts the RECORD: #6 was ruled on STALE FACTS**, sequencing "reset last, it would destroy the surface step 2 needs" against a remote where that surface was already gone. Nobody disobeyed; the ruling was moot when written, and nothing in the repo could have said so. The ungoverned-destruction judgement stands — only the tense changes, *would* → *did* | FUP-DM4-PRODROW · FUP-DM5-STACK-CYCLE-DESTROYS-BYTES |
| 2026-08-18 | **DM-FUP TRIAGE #9 — PRODROW STAYS OPEN, blocked on the C1b constructed-orphan probe** (PO; both closure options declined). It now deliberately tracks **two** questions — the erased subject **and** the ~49 likely-orphaned objects. ⛔ **Carried forward to closure time:** the probe answers the *byte* question only, so *"destroyed, not reconciled"* survives whatever it returns — no per-row re-freeze-or-tombstone decision was ever made and no manifest exists. It may **never** close as "reconciled". ⭐ *A blocked item closes on its blocker's verdict, and the half the blocker does not address is exactly the half that gets discharged by association* | FUP-DM4-PRODROW |
| 2026-08-18 | **DM-FUP TRIAGE #10 — CLAUDE.md §8 updated FIVE → SIX lint gates** (PO-approved; CLAUDE.md edits require it). Adds the `lint:set-local` bullet in the existing style, incl. the ⛔ *do not bump the watermark on a push* warning. §9's "all five" comment corrected too — the count appears twice and a half-update is the stale-record defect in miniature | FUP-DM5-SETLOCAL-MIGRATION |
| 2026-08-18 | **DM-FUP TRIAGE #11 — HOLD the `db push` of `20260928000600`/`…000700`** (PO). The census lifted the *safety* bar (0 rows ⇒ `UNIQUE` applies cleanly), but `e2e:prod` has not run on this batch and the code is not deployed. The remote is empty so there is **no drift pressure**, and the push is trivially repeatable later. ⭐ *A bar lifting is not a reason to act — it only removes the reason not to* | FUP-DM5-DVF-FILEOBJ |
| 2026-08-18 | **DM-FUP TRIAGE #6 — PRODROW is SEQUENCED, and the remote is NOT reset first** (PO). Its trigger fired when the push ran on 2026-08-18, and its own ⛔ *"do not query the linked project"* bar lifted at the same moment. Order: read-only remote census → the #1 constructed-orphan probe → **then** reconcile-vs-reset, with a manifest. ⭐ **The sanctioned "cheap path" collides with an open item**: a bare `db:reset:linked` is precisely the unmanifested, uncounted, unaudited byte destruction `FUP-DM5-STACK-CYCLE-DESTROYS-BYTES` flags — and it would destroy the surface #1 needs in order to measure | FUP-DM4-PRODROW · FUP-DM5-STACK-CYCLE-DESTROYS-BYTES |
| 2026-08-18 | **DM-FUP TRIAGE #7 — BUILD the `set local` lint gate, bounded by the FROZEN WATERMARK, not by an allowlist** (PO instrument; the lead had proposed an allowlist and was wrong). Measured: **all 12 files containing `set local` sit at or below `20260928000500`**, so a watermark-bounded gate starts green with **nothing to allowlist at all**. ⭐ **The failure directions are opposite**: a stale allowlist rots toward *weaker* — an entry outlives its file and silently skips it; a stale watermark rots toward *stricter* — it starts checking newly-frozen files and reds loudly. And because zero existing files sit above the line, the gate can be a **static check inside `npm run lint`** rather than a slow reset-log check. ⚠ Needs a positive control proving its `do $$`/`begin` nesting detection can actually fail — ✅ **BUILT + WIRED same day as `lint:set-local` (6th lint gate); positive control delivered in 3 layers and the scanner reproduces Postgres's own `25P01` ground truth exactly (4 files / 6 sites, line numbers included)** | FUP-DM5-SETLOCAL-MIGRATION |
| 2026-08-18 | ✅ **DM-FUP TRIAGE #8b — RE-RULED AND BUILT: refuse to DELETE a response that has an ACTIVE printed document** (PO), replacing the withdrawn #8. Migration `20260928000700`: `app.guard_response_active_print`, a BEFORE DELETE trigger raising **`HC069`**, mapped to pt-BR in `discardResponse`. ⭐ **The reframing that produced it:** a printed page has **left the system** — it carries a verification code and `/verificar` is a **public** route — so voiding it is a deliberate act (`revoke_printed_document`) and must not be a side effect of discarding a draft. ⭐ **`status = 'active'` only, and that bound is MEASURED:** `lookup_printed_document` reads `printed_documents` directly and joins **only** commissions/hospitals — **never `responses`** — so public verification **survives** an orphan; a revoked print must keep its row and bytes precisely so a paper-holder is still told `ANULADO`. ⛔ **A trigger, not an RLS predicate** (RLS would refuse SILENTLY as a 0-row delete), and **SECURITY DEFINER**, because `printed_documents` is RLS-protected and an invoker read would make the guard **fail open**. pgTAP `312` **77→80**, red-first (t74 red, and its delete SUCCEEDED — the orphan is real), with **t76 as the differential**: after revoke the same delete by the same principal succeeds, so the guard cannot be a blanket block. ⚠ **NOT covered: pre-existing orphans, and the dangling `securable_resources` row** | ADR [0104](../decisions/0104-pdf-document-printing-module.md) D7 (preserved) · `20260928000700` |
| 2026-08-18 | ⚠ **THE #8b MIGRATION SHIPPED A PUBLIC-EXECUTABLE `SECURITY DEFINER` FUNCTION, AND A GATE CAUGHT IT — not review, and not foresight.** Created without an explicit ACL, `app.guard_response_active_print` took Postgres' **default PUBLIC EXECUTE**, and pgTAP `320` U1 — the `FUP-ACL-APP-POPULATION` census — went RED at **237 → 238**. Fixed by `revoke all … from public` + grants mirroring the sibling guards (`guard_submitted_response`, `guard_supersession_coherent`), both `{postgres, authenticated, service_role}`. ⭐ *A new door inherits every sibling's arm, or it differs for a reason nobody wrote down* — and executing a trigger does not check EXECUTE on its function, so the revoke costs the trigger nothing. ⭐⭐ **This is the standing argument for the census as a phase gate**: the defect was invisible in the diff, in the code review, and in every functional test — `312` was fully GREEN with the door wide open | FUP-ACL-APP-POPULATION · pgTAP `320` U1 |
| 2026-08-18 | ⛔ **DM-FUP TRIAGE #8 IS WITHDRAWN THE SAME DAY — it reverses a ratified decision, and NOTHING WAS BUILT.** ADR [0104](../decisions/0104-pdf-document-printing-module.md) **D7** ratifies the `RASCUNHO` watermark whose derivation is *"Source not in final state at mint (**in_progress response**, …)"*, and its item 4 states outright: ***"Completeness does not gate minting — FINAL/RASCUNHO already encodes it."*** Draft prints are a **designed feature**. pgTAP `312` t6 pins it **by name** — *"creator sees his own in_progress draft (RASCUNHO prints are legal, D7)"* — so the ruling would have red-ed an existing keystone. ⭐ **The failure is upstream of the ruling: `FUP-DM5-DANGLING-PRINT`'s own option list proposed refusing the mint as *"narrowest, and arguably right — a draft is not a document of record"*, a justification an ADR had already refuted.** The lead repeated the item's framing instead of checking it, and the PO ruled on the lead's recommendation. ⚠ *An option list is an assertion too, and this one was authored inside the item it belongs to — the place least likely to be re-derived.* → [[verify-dont-comply]] · [[a-comment-is-an-assertion-that-goes-stale-silently]]. **Caught before implementation, by reading D7 while looking for a fixture. RE-RULE OWED** | ADR [0104](../decisions/0104-pdf-document-printing-module.md) D7 · FUP-DM5-DANGLING-PRINT-ON-DELETED-DRAFT |
| 2026-08-18 | ~~**DM-FUP TRIAGE #8 — refuse a print mint from a non-`submitted` response**~~ ⛔ **WITHDRAWN — see the row above.** (PO). The narrowest of the three filed options, and the standing principle applies: *a narrowing can be wrong and safe; a widening cannot*. A draft is not a document of record. Cheap by measurement — no spec mints from `in_progress`, and the E2E print fixture pool is `submittedResponseIds`, submitted-only by construction. ⚠ **It does not clean up the existing rows** (6 of 9 local prints are `form_response` kind) and the dangling **`securable_resources`** row needs handling under every option — every kernel arm joins through it | FUP-DM5-DANGLING-PRINT-ON-DELETED-DRAFT |
| 2026-08-18 | **#8c — THE SHIPPED GUARD'S PREDICATE IS WIDENED TO `superseded`; its stated rationale was FALSE** (PO). `20260928000700` argued *"Only an ACTIVE print represents a live page."* ⭐ **ADR 0120 D6/D8 says the opposite** — established the hard way by the D11 serving collision — *states change the overlay STAMP, never reachability*: a `superseded` print still serves bytes and still answers `/verificar`. Reachable through supported actions only: mint → re-mint (the mint's own `SUPERSEDE_ACTIVE`) → coordination revokes the active one ⇒ **zero actives, one superseded, guard open**. `revoked` stays permitted (D2, restating 000700 so it is not re-litigated). ⚠ **`312` t76 could not catch this** — its fixture has no superseded sibling, so it is equally satisfied by the `active`-only guard; the widening ships with its own red-first keystone (t78, **drilled**: with the old predicate the DELETE *succeeds*) | ADR [0123](../decisions/0123-discarding-a-draft-that-has-emitted-documents.md) **D1/D2** · `20260928000800` |
| 2026-08-18 | **#8d — THE MINT KEY-SHARE-LOCKS ITS SOURCE; the race was MEASURED, not argued** (PO). The guard is `BEFORE DELETE`, `mint_printed_document` read `responses` **unlocked**, and nothing ordered the two. Scratch-schema drill: unlocked ⇒ *delete succeeds, mint commits after,* **ORPHAN CREATED**; with `for key share` ⇒ delete blocks, trigger raises; reversed ⇒ locked select returns **zero rows** and the mint aborts on its **pre-existing** `HC0D1`. Works because Postgres takes `LockTupleExclusive` **before** running a `BEFORE DELETE` trigger body. Rewritten **in place** off `pg_get_functiondef` (7 prior migrations own that body; re-pasting 250 lines would make the new file the authority on code it does not own) with the match asserted on **both** sides — `replace` reports a miss as *success*. ⚠ **Not pinnable behaviourally** (pgTAP is single-session) ⇒ `312` t81 pins it **structurally from `pg_proc`**, and that weakness is stated rather than hidden: the alternative was a comment | ADR 0123 **D3** · `20260928000800` |
| 2026-08-18 | **#8e — BOTH REMAINDERS CLOSED, AND ONE OF THEM HAD THE OPPOSITE FIX** (PO). **D4 — pre-existing orphans: the subject is EMPTY**, measured on both environments (production 0 prints/0 responses/0 documents/0 dangling securables; local 0 after reset). ⭐ **The row above's "6 of 9 local prints are `form_response` kind" was E2E residue in a since-reset DB**, quoted forward for four days as a population — a reconcile would have had nothing to reconcile. ⛔ The measurement **expires at the pilot**. **D5 — the dangling `securable_resources` row is RETAINED BY DESIGN**: `can_read_document` *and* `can_write_document` both open with `join securable_resources` and return **false** when it misses, so ⛔ **deleting it would silently revoke `request_document_disposition` — D11's only outflow.** The obvious repair was the defect. **D6 — a THIRD defect found here is filed, not folded in**: a draft print is invisible to every coordinator but its creator | ADR 0123 **D4/D5/D6** · FUP-DM5-DRAFT-PRINT-INVISIBLE-TO-COORDINATION |
| 2026-08-18 | **DM5 GATE STEP 4 — ALL SEVEN DOCKET ITEMS RULED** (PO), closing the docket that blocked the Document Model program's final phase. Rows below carry each. ⚠ **The docket was over-wide by one**: it listed ADR 0114 **O4** as owed, but O4 was **ruled 2026-08-13** (0118; 120 s/300 s TTLs) and ADR 0120's open-items list — the phase review's own cited source — named only O1/O2. *An over-wide list of what is owed costs the same round as an under-wide one.* ⛔ **Two items are RULINGS, not completions**: C1's rehearsal and C2's Tier 1 are **work**, now carried in the new § Critical FUP | [phase review](../reviews/dm5-phase-review.md) §§5–6 · § Critical FUP |
| 2026-08-18 | ✅ **`db push` EXECUTED — all five local-only migrations applied to the remote** (PO-authorized at the docket, to carry out decision #1). Remote `20260927000360` → **`20260928000500`**; **8 legacy buckets retired** (4 survive: `documents-phi`/`documents-standard`/`form-assets`/`meeting-audio`), **0 objects**, the four `nsp_evidence`/`capa_evidence` policies **gone**, and **FUP-DM4-RECUSAL's PHI fix is LIVE** (verified via `pg_get_functiondef`, not migration text). ⭐ **The mechanism finding:** retiring the buckets *required* `20260927000400`, which also drops those four policies — **the narrower-looking Storage-API route would have deleted the buckets and orphaned the policies.** ⛔ **Zero local-only migrations remain: nothing at or below `20260928000500` may be edited in place** — the editable window did not move forward, it closed | § "State" (a TOP-LEVEL section since 2026-08-18) |
| 2026-08-18 | **#4 SUPERSEDE COLLISION RULED as (b) — supersession no longer marks bytes; the trigger moves to RETENTION EXPIRY** (PO). The widening of `app.resolve_document_version_bytes` was **declined**. ⭐ **The collision is not adjudicated, it stops occurring** — the two ratified decisions only ever touched at the supersession *instant*, so the PHI byte-serving gate is **untouched** and no diff-scoped door sweep is owed against it. D3's vocabulary survives (the `duplicate` trap is still real), its trigger is struck; D5's principle survives, its timing moves. ⚠ **One build-time detail left OPEN on purpose**: `superseded` vs `retention_expired` as the reason recorded at expiry — both true, different to a regulator. ⛔ Rebuild gated by **D1's outflow**, not by a decision | ADR [0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md) **Amdt 2** · ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) D11 |
| 2026-08-18 | 🔒 **#7 PILOT RISK ACCEPTED, BOUNDED BY ONE REHEARSAL** (PO) — the pilot may proceed over the manual-only PHI-disposal path **on the condition that `phi-disposal-runbook.md` runs end-to-end against test data BEFORE any real patient record is loaded**. ⭐ **The condition is the substance, not a caveat**: the mitigation was never missing, it had never been *observed to work* — a procedure only ever read is a claim about a procedure. ⚠ `disposal_state` = **INTENT, not a destruction guarantee**, now ratified as such; nothing user/regulator/export-facing may call it destruction. **Inverts ADR 0099 D10** — for PHI the stale row IS the harm. ⛔ D2 is **not** ratified; `343`'s K6b stays a false pin the day a scheduler lands | ADR [0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md) **Amdt 3** · **Critical FUP C1** |
| 2026-08-18 | **#2 ADR 0114 O1 + O2 RULED** (PO). **O1** — **20-yr floor on anything clinical, 5-yr default on governance metadata**; the tiering rule is *does this evidence care, or evidence process*. ⛔ **Trigger events, per-type tier assignment, and erasure-vs-retention reconciliation stay OPEN** — an implementer may not infer them, and a retention row is still **provisional** (D5-class `HC0DR` blocking still applies). **O2** — the `unscanned_accepted` acceptance now expires *"the day any external user can upload"*. ⭐ **An event, not a date, on purpose**: a calendar deadline for a risk acceptance expires while the risk is unchanged, and gets extended. Scanner selection stays open, but becomes blocking at the same moment | ADR [0114](../decisions/0114-document-model-redesign.md) Open items |
| 2026-08-18 | **#1 ADR 0120 D9 — the under-count class is RATIFIED AS ACCEPTED-UNVERIFIED on Cloud; no verification step is added** (PO), **+ a HARD STOP in the runbook**: never run `capture` against Cloud from a machine with a local stack up; never read exit 0 / `CAPTURE CLEAN` as a byte result; never record a Cloud deletion as *verified* (record **asserted**). ⭐ The Cloud danger is a **FAKE proof, not a missing one** — `locateVolume()` has no project affinity, and the configuration that produces it is the *normal* posture of anyone who can run this repo's gates. ⚠ **What made it safe to ratify does NOT generalise**: remote holds **0 objects in all 12 buckets** (re-measured 2026-08-18) and the under-count class needs objects to under-count. **Expires when the pilot loads data.** ⛔ S3 endpoint still **UNPROBED** | ADR [0120](../decisions/0120-dm5-wave-d-retirement-decisions.md) **D9** · [runbook](../deployment/phi-disposal-runbook.md) §6 |
| 2026-08-18 | **#5 THE 407-DOOR TRIAGE IS SIZED — TWO TIERS** (PO). **Tier 1** = the subset touching **PHI or crossing a tenant boundary**, swept next as its own workstream, each door getting a **recorded verdict** so a new door cannot pass by absence. **Tier 2** = the remainder, **deferred to after the pilot ships**. ⛔ **Tier 1's population is derived from the catalog AS A PROPERTY, never hand-listed** — this item was itself filed on an inferred premise that measured false, and a hand-picked "PHI-looking" list reproduces the phase's dominant failure class exactly. **Its size is not yet known; deriving it is step one.** ⚠ The 3-door sample may close nothing; `assume_role` is **ERROR-shaped, not COVERED** | **Critical FUP C2** · FUP-AUTHZ-COMMAND-DOOR-UNSWEPT |
| 2026-08-18 | **#3 UPLOADER VISIBILITY IS NOT ADDED — S1-O3 CLOSED** (PO). An unplaced file object stays invisible to everyone including its uploader; DM1 MAJOR-1's removal is the **permanent** shape. ⭐ The known cost is narrow and **unobserved** — an interrupted upload leaves an orphan the user cannot see or retry — and the pilot is what would surface one; adding a visibility path to a PHI-capable substrate to pre-empt it is the wrong trade. ⛔ If ever revisited, a future arm goes **INSIDE the kernel chain**, never beside it — outside is not a smaller version of the feature, it is the defect that was removed. `328` K13 is now a pin on a **decided** state; do not "fix" it as over-narrow | ADR [0117](../decisions/0117-dm2-s1-confidentiality-ceiling-decisions.md) § S1-O3 · [0116](../decisions/0116-dm1-substrate-cutover-decisions.md) §11 |
| 2026-08-18 | **#6 THE S2/S3/S5 `backend-state.md` SECTIONS ARE STILL WANTED — `backend` writes all three as ONE task before DM5 closes** (PO). ⛔ **Filed as `FUP-DM5-BACKEND-STATE-SLICE-SECTIONS` the same day because it had no ID** — it lived only in the S6 QA, the phase QA and the `🛑 START HERE` block, every one of which expires on answering the docket. ⭐ *Naming a thing in the document that expires is not naming it* — phase-QA finding **R3**, re-earned one docket item later, by the obligation whose stated purpose was that it *"cannot die quietly when DM5 closes"* | FUP-DM5-BACKEND-STATE-SLICE-SECTIONS |
| 2026-08-17 | **DM5·S5 step 4 — the follow-up batch's PO approval CLOSES S5 TOO** (PO). Asked rather than assumed: the approval was recorded against `phase(DM5-followups)` and said nothing about S5, which had step 2 discharged by the same shared `e2e:prod`. ⭐ **An approval's SCOPE is a fact about the approval and has to be written down like any other.** S5 = COMPLETE, all five gate steps | PROGRESS.md § Current Phase Tasks |
| 2026-08-17 | **FUP-DM5-SUPERSEDE-SERVING-COLLISION — DECIDE LATER; the D11 inflow STAYS REVERTED** (PO). Both options declined for now: widening `app.resolve_document_version_bytes` to serve `disposal_pending` bytes whose reason is `superseded`, or reinterpreting ADR 0121 D3/D5. ⭐ *A narrowing can be wrong and safe; a widening cannot* — a PHI byte-serving gate is not widened unilaterally. ⛔ **A deferral is not a closure: the item stays 🔴, D11 cannot be rebuilt until decided, and S6 may not close over it** | ADR [0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md) Amdt 1 |
| 2026-08-17 | **FUP-DM4-RECUSAL — the Phase-19 deferral is OVERTURNED; close it now with a NARROWING arm** (PO). The deadline was always the `documents_wave_c` flag-on date, and **a plane that only WIDENS cannot close an under-inclusive gate** | ADR [0122](../decisions/0122-recusal-case-read-arm-at-the-referral-freeze-door.md) |
| 2026-08-17 | **ADR 0121 ACCEPTED — D2 (cron outflow) + D4 (what `disposed` asserts) ratified as proposed** (PO). D4 in its *record-what-was-verified* form, not the stronger *block-disposal-without-byte-proof* variant. ⚠ **Only D4 shipped**; D3/D5 built then reverted, D2 never built | ADR [0121](../decisions/0121-disposal-lifecycle-inflow-outflow-and-evidence.md) |
| 2026-08-17 | **The PHI-disposal runbook's five open values SET** (PO): encryption **at creation** · location outside the repo **and** every sync root · reader set = the owner alone · retention until the next backup verifies good, **max 30 days** · destruction **key first**. ⭐ Short backup retention is a **SAFETY** property — the 20-yr duty belongs to the system of record, not to backups | `docs/deployment/phi-disposal-runbook.md` §6b |
| 2026-08-17 | **The local Storage volume is NON-DURABLE DISPOSABLE TEST RESIDUE** (PO) — no cleanup step, no gate, no local manifest discipline. Ratified as a **class**, not a number, because orphan accumulation is a standing byproduct of `db reset` | FUP-DM5-STORAGE-ORPHANS |
| 2026-08-17 | **Finish the PROGRESS.md rotation before opening DM5·S6** (PO) — the record pass takes priority over the phase's final slice | PROGRESS.md § Current Phase Tasks |
| _pre-2026-07_ | **35 earlier decision rows (Phases 0–14, 2026-06-11 → 2026-06-25) rotated 2026-08-04** — one line each, plus the verbose form of every one. ⚠ The full pre-compaction form of the 32 rows above is archived there too (2026-08-14) | [decisions-log.md](decisions-log.md) |

## ↩ Rotated from PROGRESS.md 2026-08-19 (the W5 cell-cap pass) — 3 rows, VERBATIM apart from the link repoint

> The live rows were compressed to the 300-char cell cap in the same change; these are
> the full-length originals. Cell content only — the table header is in the live file.

| Date | Decision / run | Refs |
| --- | --- | --- |
| 2026-08-18 | ✅ **REGISTRATION IS DERIVED FROM SOURCE STATE, AT THE LOCK POINT — one print action, two outcomes.** A still-editable source yields an **ephemeral `Imprimir prévia`** (no registry row, no bytes, no QR, no `código` — **audited**, streamed from the same Gotenberg render, never uploaded); a **locked** source yields the registered `Emitir documento` unchanged (PO, grilled through six branches). ⭐ **The discriminator is the LOCK point, not the watermark's finality point — measured:** `guard_meeting_status` already refuses to delete a meeting at `in_signature` and beyond (*"em_assinatura and beyond are locked"*), so **an ata circulating for signature REGISTERS** — permanent, hash-pinned, QR-verifiable — while still stamped `RASCUNHO`. `meetingWatermarkFor` is **unchanged**; registration and watermark are **two derivations** that coincide for `form_response` (both at `submitted`) and separate for `meeting`. `cancelled` is excluded by decision (locked, but no minutes to pin). ⇒ a mint at `in_signature` then again at `signed` leaves a **supersession chain**, so *"which version circulated on the 12th"* has an answer. ⭐ **Two buttons were REJECTED**: they would make *"is this a record?"* a **user choice**, and ADR 0104 D7 exists precisely so a document's declared nature is computed, never composed — deriving registration from the same predicate that already derives the watermark keeps one rule. **AMENDS 0104 D7 item 4** (*"completeness does not gate minting"* → gates **REGISTRATION**, not **PRINTING**) — knowingly and in writing, which is the whole distinction from TRIAGE #8, withdrawn for reversing D7 unknowingly. ⚠ **MEASURED: it makes HC069 structurally unreachable** — `guard_submitted_response` raises unconditionally on a submitted DELETE and the RLS policy is `responses_delete_own_draft`, so **only drafts are deletable**; with drafts unregistered the state HC069 refuses cannot be built, `312` §9's fixture becomes **unconstructible**, and its keystones would go **vacuous-and-green**. The guard is **RETAINED as a backstop** and re-pinned at table level; `…000700`/`…000800` are **not** reverted | ADR [0125](../decisions/0125-previa-ephemeral-and-emission-registered.md) · amends [0104](../decisions/0104-pdf-document-printing-module.md) D7 · discharges [0123](../decisions/0123-discarding-a-draft-that-has-emitted-documents.md) D7 |
| 2026-08-18 | ✅ **`db push` EXECUTED on PO instruction — `…000800` + `…000900` applied; remote head `20260928000900` / 415.** Verified independently, not from the migration's own notice: first-party truncatable **63 → 0**, platform residue `net, storage` unchanged as accepted, and `…000800`'s new DEFINER carries **no PUBLIC** in its ACL | FUP-PCITV-1 item 3 ⬛ · [backend-state.md](../backend-state.md) |
| 2026-08-18 | ✅ **TRUNCATE residue SWEPT (63 first-party tables) and the platform half ACCEPTED IN WRITING** — `20260928000900` + pgTAP `191` §5. ⭐ TRUNCATE fires no DELETE trigger, so it bypasses `storage.protect_delete` as well as RLS; and on Cloud a REVOKE we are not entitled to make returns **no error and changes nothing** (`t`→`t`), so the migration verifies the EFFECT, not the absence of an error | FUP-PCITV-1 item 3 ⬛ · FUP-DM5-STORAGE-ORPHANS |
