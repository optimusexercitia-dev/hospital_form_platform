# PDF·P2 — PDF printing: Meetings (ata) (COMPLETE 2026-08-08)

> Archived phase record, rotated out of PROGRESS.md at the §6 Record step (2026-08-08).
> ADR [0104](../decisions/0104-pdf-document-printing-module.md) + **Amendments A7/A8/A9**
> (the PO "Package A" ruling this phase produced) · plan
> [pdf-document-printing.md](../plans/pdf-document-printing.md) §3 · QA ⛔ r1 → **APPROVED (r2)**
> [review](../reviews/phase-PDF-P2-review.md). Built on branch `feat/pdf-p2-meetings`
> (primary checkout, shared-tree team), started + completed 2026-08-08. Migrations
> `20260914000000` (meeting arm) + `20260914000100` (A7 full-sight conjunction + A8 per-kind
> PHI gate). **NOT pushed to origin or the remote DB at close (PO instruction — the PO is
> handling the pending items next).** Commits: contract `d5947dd` · arm+pgTAP `475b0d8` ·
> provider+template `ff26d0e` · UI `5510bd3` · copy fix `2e8ef7f` · PO ruling docs `f45e75b` ·
> fix wave `7bd5a8e` · helper swap `4b4293a` · stale-comment `d129eab` · reviews `258e1a0`/`9cdcf74`.

**The phase's headline:** the binding review question (did P2 touch anything outside
provider + template + arm + mint surface + tests?) **held** — and the review process caught
the exact class it existed to catch: meetings are the first kind with per-caller content
masking, which P1's arm-parity abstraction couldn't describe. QA's BLOCKER-1 → the PO's
Package A ruling → ADR A7 (full-sight conjunction) + A8 (presence-derived conservative PHI
labeling) + A9 (formula gains "+ mint surface").

## Task table (final)

| # | Owner | Task | Status |
| --- | --- | --- | --- |
| M-B1 | backend | Substrate read (LIVE catalog + source): the meeting-visibility predicate the `meeting` arm delegates to; the minutes-approval signoff substrate for the multi-signature ata footer (MIN ADR 0099 touched it) → short migration plan for lead ack | ✅ done 2026-08-08 — arm = pure delegation to `app.can_reach_meeting(id, uid)` (explicit-uid DEFINER; the SAME predicate every meeting child policy routes; member AND (commission_default OR attendee); NO admin arm — the C7 cut, so commission/org admins are correctly denied); signatures = `meeting_signatures` (status `signed|declined|revoked`, `signed_at`, attendee→role/name) via `sign_meeting`; FINAL ⇔ `meetings.status ∈ {signed, distributed}` else RASCUNHO (lifecycle `scheduled→held→in_signature→signed→distributed`); ⚠ FLAGGED: the mint door's TWO kind-conditional blocks (template coherence + commission resolution, live-body-confirmed) structurally block meeting mints — M-B3 plan asks lead ack for the mechanical 2-branch re-emit |
| M-B2 | backend | Contract: `DocumentPayload` union gains the `meeting` body member (agenda, deliberations, action-item refs, attendance, multi-signature footer) + provider stub registration | ✅ done 2026-08-08 — `MeetingDocumentBody` (meta/quorum/minutesMd/agenda/attendance/actionItems; signatures stay in the ENVELOPE per D13 — envelope unchanged); `documents/meeting.ts` stub (TEMPLATE_KEY `meeting` v1, throwing body; no fingerprint until the real template); render.ts TEMPLATES + exhaustive-switch entries; `meeting` provider stub registered (throwing pt-BR, `phiCapable: false`); lint 0/0 + typecheck + pdf tests 18/18 (post `npm install` — the primary checkout lacked the P1 deps) |
| M-B3 | backend | Migration (`20260914000000+`): `meeting` arm in `can_view_printed_document` — a RE-EMIT: property-diff old-vs-new from the catalog (ACL/prosecdef/proconfig) + template-key coherence in the mint door; pgTAP: new arm ALLOW/DENY + `case`/`interview` still literal false + drills; diff-scoped `ARM=policy` over the changed dispatch | ✅ done 2026-08-08 (`475b0d8`) — `20260914000000`: dispatch arm = pure delegation to `can_reach_meeting`; mint door gains its two sanctioned per-kind rows (lead smell-marker ruling recorded in-file); both re-emits property-diffed identical (ACL/prosecdef/proconfig; exactly 2 meeting sites). pgTAP `313` (38): no-admin-arm delta pinned (org_admin + hospital_admin DENY + form-arm control), attendee gate both legs, fail-closed keystones RELOCATED onto REAL case/interview fixtures; `312` t9/t45 repointed (P4 note in-file). Drills: D8 11/38 red · D1 6 red both files · D2 8 red · D5 21 red — all restored, 112/112 after. Diff-scoped sweep: COVERED 0 BLIND 0 ERROR (Files=174 Tests=5486); verdict hand-merged; census+floor HOLD |
| M-B4 | backend | `src/lib/meetings/pdf-payload.ts` provider (RASCUNHO/FINAL from minutes approval state) + `documents/meeting.ts` template v1 + fingerprint entries (canonical + branch variant, mutation-proven) | ✅ done 2026-08-08 — provider (caller-session Rule 9 reads via existing meetings queries; FINAL ⇔ `status ∈ {signed, distributed}`; signed `meeting_signatures` → envelope attestations, declined/revoked excluded, `internal_eauth`→`platform_signoff`; two-step letterhead names via `getMeetingPrintContext`); template v1 with TEMPLATE-SCOPED `<style>` (form fingerprints provably unmoved); fingerprints: meeting canonical + `final_signed` variant, mutation-proven (M-A agenda edit → ONLY meeting hashes red; M-B attestation edit → meeting VARIANT red + form pins red, meeting canonical green — the unsigned-branch discrimination); SMOKE: real ata minted e2e via Gotenberg 8.24.0 (create_meeting → mint → sha-256 === content_hash → lookup kind=meeting + viewer id). Gates: fresh reset 321==321 · test:db 174/5486 PASS · lint 0/0 · typecheck · vitest 82/1194 |
| M-B5 | backend | QA r1 fix wave (BLOCKER-1→A7, MAJOR-1→A8, MINORs 1–7; ADR 0104 Amendments A7/A8/A9 = the spec) | ✅ done 2026-08-08 (`7bd5a8e`) — **FIX-A** migration `20260914000100`: `app.can_read_full_meeting_content` (mirrors the LIVE `_project_meeting_agenda_item` terms: respondent-title + presence-qualified deliberation-gated text) conjoined into the meeting arm (mint AND download); property-diff identical; **FIX-B** per-kind PHI gate = the ratified TRIO site 3 (marker recorded in-door, supersedes "exactly 2"); provider derives `contains_phi` from presence (case-linked item title / minutes_md / gated fields); action forwards it + derives `phi/` path (A8 ≠ D9 kept visibly distinct in comments); **FIX-C 1–7**: ha_b positive control t51 · revoke-without-sight PINNED both directions t52–t54 (D11 governance, lead-ruled) · 312 t45 de-confused · 313 t30 label · shared `meetingWatermarkFor` exported from `@/lib/pdf/documents/meeting` (frontend swaps its inline copy) · degenerate-branch variant re-cut + mutation-proven (empty-agenda edit → variant red, canonical green) · ENUM_FALLBACK '—' sweep. pgTAP 313 → 54 (A7 keystones t39–t48 with respondent chain + `explicit_grants_only` masked-member specimens); drills D9 5 red · D8 16 red · D2 9 red, restored 128/128; diff-scoped sweeps dispatch+helper COVERED 0 BLIND (door's authority = the swept dispatch + D2); census+floor HOLD; SMOKE 3/3 incl. coordinator masked-content mint (contains_phi=true, `phi/`, hash-match) + respondent denied mint (pt-BR 42501) AND open (0 rows); fresh reset 322==322 · test:db 174/5502 PASS · lint 0/0 · typecheck · vitest 82/1194. ⚠ one transient: the first post-Docker-restart reset ABORTED at migration 297/322 (exit 255 was REAL, caught by asserting registered==files, not the exit code) — re-run clean; the FAIL suite run on the partial DB was discarded. Post-r2 one-liner `d129eab`: stale `final_signed` fingerprint comment fixed |
| M-F1 | frontend | Mint surface + "Documentos emitidos" panel on the meeting detail screen — reuse `src/components/printing/` as-is; new components only if reuse is impossible (that's an abstraction-leak signal, report it) | ✅ done 2026-08-08 (`5510bd3`) — **zero printing components changed**; `PrintedDocumentsSection` took `sourceKind="meeting"` as-is, so the wiring is pure prop-threading on the meeting page (no abstraction leak to report). Flag read joined the page's existing parallel batch; watermark FINAL only from `signed`/`distributed` (mirrors the provider's derivation — ⚠ the slug is `distributed`, `distribuida` is only the pt-BR label); `canRevoke` reuses the page's existing `isCoordinator`; no mint surface for a non-member org_admin because the `isCommissionMember` gate already turns them away (domain gate, deliberately not compensated). Gates: lint + typecheck + real `next build` green. **Verified on a PROD standalone build** against the local stack: page 200, no RSC crash from the server-action props, section + mint button render, `listPrintedDocuments('meeting', …)` resolves through RLS to the empty state (no error notice), flight payload carries `draft` for this `held` meeting. NOT verified: the open-dialog interaction — the Browser pane was not compositing (rAF-throttled: React fibers present but Suspense reveal and Radix open never commit), which is a harness limitation, not a product defect; interactive coverage belongs to M-T1. **QA MINOR-5 fixed (`4b4293a`)**: the page's inline watermark derivation duplicated the provider's rule; now consumes the shared pure `meetingWatermarkFor(status)` from `@/lib/pdf/documents/meeting` and the local copy is deleted (behaviour identical — `signed`/`distributed` → final, all else incl. `in_signature`/`cancelled` → draft). Lint + typecheck + `next build` green |
| M-T1 | tester | E2E: ata mint→download→verify chain + RASCUNHO (unapproved minutes) vs FINAL (approved) watermark assertion | ✅ done 2026-08-08 — `e2e/pdf-printing-meetings.spec.ts` (5 tests final) + `e2e/helpers/pdf-printing-meetings.ts`; pre-wave: **4/4 GREEN ×2 fresh resets (8/8)**; P1's UNTOUCHED `e2e/pdf-printing.spec.ts` re-run → **7/7 GREEN** (2 bugs filed, LOW+MEDIUM — BUG-PDF2-001 since fixed, BUG-PDF2-002 open/spun-off). Covers: full ata lifecycle on a meeting driven to `signed` through the REAL `create_meeting`→`conclude_meeting`→`sign_meeting` flow (FINAL preview, chip "Ativo", download 200/`%PDF-`, logged-out `/verificar/<código>` "Ata de reunião" + no download link, revoke → "Anulado", re-verify "Documento anulado", overlay bytes differ); RASCUNHO↔FINAL tracked across the SAME meeting's real transition; restricted `participants_only` visibility (RLS boundary asserted via raw JWT-scoped PostgREST read → `[]` + app-level content boundary); keyboard-only mint→download-link-focus. `mintViaDialog`/`articleForShortCode` reused UNCHANGED from P1 helpers. **Post-fix-wave:** regression 12/12 ×2 (24/24; the A7/A8 conjunction changed nothing for non-case-linked fixtures) + **new "A7: the linked case respondent cannot mint or download the ata"** — composed via real RPCs (`create_meeting` → `create_meeting_agenda_item` → `link_meeting_case` onto the SEEDED ETH·E1 case, `staff4.ccih` = seed `respondent_doctor`, `chefe.ccih` holds the seeded `read_case_deliberation` grant; seed data read-only): coordinator mints (`contains_phi=true` + `phi/` DB-truth-checked) + downloads 200; respondent reaches the meeting page (200 — base reach, NOT BUG-PDF2-002) but panel empty (RLS, no leakage), own mint → verbatim pt-BR 42501, direct `downloadPath` probe under their session → 404. **3/3 GREEN ×3 runs.** A7 coverage = pgTAP 313 t39–t48 + backend smoke + this E2E — three independent layers. Two infra events triaged per house rules (PGRST002 settle-window; one Windows prod-standalone server collapse mid-12-test-run → split per-file, clean ×2) |
| M-Q1 | qa | Phase review `docs/reviews/phase-PDF-P2-review.md` | ⛔ **r1 CHANGES REQUESTED** (`258e1a0`) — binding scope question HELD (20/20 files classified; 2 semantic kind-sites re-counted; copy-only verified; property-diffs identical; NULL-propagation probed false-not-NULL; no-admin-arm delta proven behaviourally) but: **BLOCKER-1** (ata freezes the minter's projection, re-served at `can_reach_meeting` with no capability term — a respondent downloads his own process number that the DB refuses at column-grant level; violates D11) + **MAJOR-1** (all four printed columns catalog-classed `PHI-BEARING` while `containsPhi:false`/`std/`/no band; forms carry zero such comments — needs the PO per D9's upstream clause) + 7 MINOR / 3 INFO. Rule 7 clean 23/23; FINAL derivation total over 6 statuses; signature filter an allowlist. → **✅ APPROVED (r2)** (`9cdcf74`) — BLOCKER-1 closure re-proven by CONSTRUCTING the exploit class in a rolled-back txn (seed holds no `reach=t ∧ full_sight=f` persona — a vacuous conjunct would have passed silently; the 4-row participants chain flipped the arm on the conjunct alone); helper diffed term-by-term vs the LIVE masking (capability term adds a presence guard — more permissive only where nothing is at stake); MAJOR-1 closed by A8 (all 4 columns + a process-number term; t45/t46/t50 pin the bifurcation; t49 pins form_response still refusing); all 7 MINORs closed; kind-sites re-counted = exactly 3 (A8 trio); property-diffs byte-identical vs r1 baseline. 2 new INFO: helper fail-open STANDALONE (safe only behind the reach conjunct — P3/P4 must not reuse bare; → `COMMENT ON FUNCTION` in P3's first migration) · one stale fingerprint comment (fixed `d129eab`) |

## Lead notes (final)

- **Ruling: the mint door's per-kind CASE is the SQL mirror of provider registration** — extending its blocks per kind is the arm deliverable, not a leak; a data-driven refactor mid-phase would be the forbidden pipeline surgery. The sanctioned kind-conditional sites are now **exactly 3** (A8 trio: template coherence · commission resolution · PHI capability) — **a FOURTH is the abstraction-leak signal.**
- Meeting arm = `can_reach_meeting AND can_read_full_meeting_content` (A7). Deliberate, test-pinned deltas vs the form arm: no commission/org/hospital-admin arm (C7 cut) · attendee gate on restricted meetings · the full-sight conjunction. The module inherits the domain's own reach (D11).
- Fail-closed keystone relocation: P1's `t9` specimen (`meeting`) inverted when the arm landed — repointed to real `case`/`interview` fixtures; P4 repoints again (noted in-file).
- **Ruling: BUG-PDF2-001 fix scope** — frontend swept the DEFECT (all 5 "desta resposta" literals → kind-aware `labels.ts`, incl. kind-aware watermark rationales; neutral fallback; future kinds not enumerated) — the surfaced-P1-bug class, copy-only (`2e8ef7f`).
- Ruling: **revoke-without-sight asymmetry is deliberate** (revoke is a governance act, D11) — pinned both directions (t52–t54).
- **Full `e2e:prod` gate: GREEN — 0 real failures / 1030 collected** (run 1: 1002p/2f/14 did-not-run, both failures old baseline specs; clean-stack re-run of both batches 80/80). All 11 PDF specs green in-suite. *(Ran pre-fix-wave; the wave was then verified by pgTAP 128/128 + drills + tester 12/12×2 + the new A7 spec ×3 per §6's fix-loop rule.)*
- **PO RULING (2026-08-08): Package A + conservative PHI labeling → ADR 0104 A7/A8/A9** (rationale + rejected alternatives recorded in the amendments).
- Docker-daemon outage mid-wave (user-restarted): stack auto-recovered, Gotenberg restarted manually; one genuinely-aborted reset caught by registered==files, partial-DB results discarded.

## Open at close → registered live in PROGRESS.md (Follow-ups + Bug Log)

FUP-PDF-1..4 · BUG-PDF2-002 (spun off as a task chip) · P3 carry-forwards (helper bare-use hazard → `COMMENT ON FUNCTION`; P4 keystone repoint) · deploy prereqs (remote `db push` of `20260914000000`+`000100` NOT done per PO instruction · Gotenberg Coolify resource · flag `document_printing` OFF in prod). *(Deploy-prereq state as of the phase close — **superseded**; current per-item status in the next section.)*

### PDF-program item status — updated 2026-08-10 (rotated out of PROGRESS.md § Current Phase Tasks)

- `[x]` **FUP-PDF-1 — RESOLVED 2026-08-08.** The creator mint surface shipped: new respondent route
  `…/c/[commission]/respostas/[responseId]`. Carried a pre-existing find with it, **BUG-RESP-001**,
  fixed in the same wave. Entry archived → `follow-ups-archive.md`.
- `[ ]` **FUP-PDF-2..4** — still OPEN; one-line index in PROGRESS.md § Follow-ups, bodies in
  `follow-ups.md`. (P1 QA MINORs: SQLSTATE allowlist can surface English Postgres text · mint/revoke
  `returns printed_documents` re-exposes withheld columns · verification rate limiter is global +
  in-process.)
- `[x]` **BUG-PDF2-002 — RESOLVED BY-DESIGN 2026-08-08.** Next's streamed-`notFound()` contract; no
  app change, contract pinned in E2E. Full record → `bug-log-archive.md`. Session task chip withdrawn.
- `[x]` **`git push origin main` — DONE 2026-08-10** (user-authorized). The P2 merge reached `origin`
  inside the ACT push (`ff0e76a`); `origin/main` later `f3981a5`, verified in sync. The PO's
  2026-08-08 local-only hold on the *git* half is discharged.
- `[x]` **Remote `db push` of `20260914000000` + `20260914000100` — ✅ DONE 2026-08-10** (user, own
  auth; the PO hold is lifted). Catalog-verified **2/2 applied**; local and remote now agree on the
  meeting arm (345 == 345 overall).
- `[ ]` **Gotenberg Coolify resource** — runbook [pdf-renderer.md](../deployment/pdf-renderer.md).
  With the `db push` above landed, **this is now the only thing keeping flag `document_printing`
  OFF in prod.**
- **P3/P4 build carry-forwards (not user actions):** `can_read_full_meeting_content` is fail-open
  **STANDALONE** — never reuse it without the `can_reach_meeting` conjunct; it becomes a
  `COMMENT ON FUNCTION` in P3's first migration. P4 repoints the relocated fail-closed keystones
  again (noted in-file in `312`/`313`).
